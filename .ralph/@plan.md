# Implementation Plan

## NOW

- [ ] Run `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-2-envrc-parameterisation-contract.md")` — implement Story 2.2 per the eight Tasks (Task 1 `.envrc.example` + Task 2 compose parameterisation + Task 3 `.secrets.example` + Task 4 lint rule end-to-end + Task 5 `.gitignore` extension + Task 6 README § Retuning + Task 7 structural verification sweep + Task 8 sprint-status hygiene). State transition `atdd-scaffolded → in-dev`. Per § Story Lifecycle Decision Matrix row `atdd-scaffolded`. One-workflow-per-iteration guardrail: this is the full iteration task. ~large (estimate ~80K — pre-flight budget check at iteration start; if exceeded, decompose into smaller sub-tasks per Story 2.1 iter-99 partial-completion precedent).

## QUEUE (Story 2.2 `atdd-scaffolded → in-dev → traced → sm-verified → done`)

- [ ] Run `/bmad-testarch-trace (args: "yolo")` — AC→test coverage gate + traceability matrix for Story 2.2. State transition `in-dev → traced` (or `trace-fixes-pending` if Ralph-addressable coverage gaps surface; expect WAIVED per Story 2.1 iter-126 precedent for hybrid infrastructure-smoke + configuration-surface class — twelfth cumulative).
- [ ] Run `/bmad-create-story (args: "review")` — post-dev SM requirements-satisfaction verification. State transition `traced → sm-verified` OR `sm-fixes-pending`.
- [ ] Run `/bmad-code-review (args: "2")` — post-SM CR adversarial action-items gate. State transition `sm-verified → done` OR `sm-verified → fixes-pending` (multi-cycle envelope bounded by Story 2.1's iter-128 → iter-138 → iter-144 three-cycle precedent).
- [ ] _(conditional, post-Story-2.17 done)_ Transition PR #230 Draft→Open — final CI gate after all Epic 2 stories done; monitor CI; merge.
- [ ] _(conditional, post-Epic-2 merged)_ § Cross-epic transition → Epic 3 Story 3.1 via `/bmad-create-story` OR halt EPIC_DONE awaiting merge.

## BLOCKED

_(none — Story 2.2 produced as `ready-for-dev`; no prerequisites beyond the next-iteration's `/bmad-create-story (args: "review")` validation.)_

## DONE (Story 2.2 — iter-145 draft, iter-146 review-with-fixes, iter-147 ATDD-skip)

- [x] iter-147: **`/bmad-testarch-atdd` ATDD-SKIP — TWELFTH cumulative Epic precedent (Stories 1.7..1.16 + 2.1 → 2.2); Story State `validated → atdd-scaffolded`.** Skill preflight HALTs at Step 1.2 (test framework configured): no `vitest.config.*` / `jest.config.*` / `playwright.config.*` / `cypress.config.*` (frontend); no `pyproject.toml` / `go.mod` / `*.csproj` / `Gemfile` / `Cargo.toml` (backend); stack detection returns `none` (package.json exists with no react/vue/angular/next/playwright/cypress/vitest/jest deps — Epic 13 lands the framework). Story 2.2 is hybrid **infrastructure-smoke** (dotfile schemas + compose parameterisation + `.gitignore` extension — same class as Story 2.1) + **configuration-surface** (prek hook config + `INVARIANTS.md` anchor + `docs/invariants/` doc + manifest entry — same class as Stories 1.7-1.16). Both classes lack a test-runner prerequisite; both have ten Epic 1 + one Story 2.1 ATDD-skip precedents — Story 2.2's skip is consistent with the precedent envelope, not an exception. Four-ground rationale: **(a)** substrate-verification-covers-ACs at CLI-exit-code level via Task 7's structural verification + forbidden-pattern sweep (`pnpm exec prek run --all-files` exit 0 + sync-gate exit 0 + `docker compose config` exit 0 with default + override resolution + `git check-ignore -v` exit-code semantics + `pnpm keel-invariants:no-committed-dotfiles` positive/negative cases + `diff` of compose vs `.envrc.example` knob extraction + entrypoint forbidden-pattern grep regression check); **(b)** no-runner — framework prerequisite unmet (Epic 13); story's own § Testing standards block (lines 359-366) explicitly contemplates the skip ("Substrate-level Vitest unit tests do not apply to the dotfile schemas" + "Compose-file parsing + `.gitignore` behaviour are infrastructure-smoke and SHOULD skip ATDD per FR14n"); **(c)** HYBRID variant-(ii)+(iii) — adversarial AC 1-5 cross-consistency (AC 1↔AC 2 knob-list + file-header block completeness + compose mapping-table fidelity + loopback-bound port form + `.secrets.example` 6-key enumeration + prek hook `files:` filter + `INVARIANTS.md` backtick anchor + manifest doc-first sourcePath + `.gitignore` bang-suffix negation + lint-rule exit-code semantics + README § Retuning positional placement) delegated to `/bmad-code-review (args: "2")` three-layer fan-out at post-sm-verified gate; Vitest harness retro-applies at Epic 13; **(d)** upstream-provenance — Story 2.1 iter-98 ground-(d) extends to Story 2.2 via the absorbed `packages/devbox/docker-compose.yml` runtime (Story 2.2 is a post-absorption retune, not a new test-class decision); `.gitignore` rules follow direnv + act upstream conventions; prek hook follows `tokens-schema` + `tokens-contrast` Story 1.10+ pattern (also shipped without red-phase harness). Mirrors Story 1.7..1.16 + Story 2.1 v1.2 pattern exactly: **NO test-plan artefacts authored** — variant-(ii)+(iii) substitution. **Deliverable:** ONE Change Log v1.2 entry appended in-situ to `_bmad-output/implementation-artifacts/2-2-envrc-parameterisation-contract.md` (story `Status: ready-for-dev` unchanged; lifecycle state `validated → atdd-scaffolded` tracked in IP). Sprint-status UNCHANGED at `2-2-envrc-parameterisation-contract: ready-for-dev` (per Story 2.1 pattern — sprint-status flips to `in-progress` at `/bmad-dev-story` invocation, not at ATDD-skip). Budget: ~12K tokens (orient ~6K + story file 423→435 lines append ~3K + IP / RALPH.md updates ~3K — well under 117K execution budget; second-cheapest iteration in current cycle after iter-144's ZERO-PATCH close). **Next iteration:** `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-2-envrc-parameterisation-contract.md")` for `atdd-scaffolded → in-dev` (or `in-dev (partial)` if budget exceeded — re-queue per Story 2.1 iter-99 precedent).

- [x] iter-146: **`/bmad-create-story (args: "review")` pre-dev SM validation applied 9 fixes (5 CRITICAL + 2 ENHANCEMENT + 2 polish); Story State `drafted → validated`.** Three-layer parallel subagent fan-out (source-doc fidelity / repo-state alignment / adversarial gap hunt) against `_bmad-output/implementation-artifacts/2-2-envrc-parameterisation-contract.md`. Source-doc fidelity CLEAN on AC1-AC5 literals, port numbers, NFR8a framing, deferred-story numbers. Repo-state alignment CLEAN on compose TODO line 61-66, env_file block 49-51, `.gitignore` 35-39 block, `.pre-commit-config.yaml:3` hook-budget comment, hook ordering, `package.json:21` bin block, `invariants.manifest.ts:248-255` last entry, AI-7 precedent `sourcePath: docs/invariants/devbox-dind.md`, `INVARIANTS.md` backtick-wrapped ID convention, README.md section order, ESLint rule `.js` extension. **Fixes landed in story file:** (1) AC1 knob list adds `KEEL_DEVBOX_TMPFS_LOGS_MB` (was shipped in reference defaults L32 but missing from enumerated AC1 text L17 — AC-vs-deliverable drift); (2) AC1 inline-comment example swaps `docker-compose.yml § deploy.resources.limits.cpus` → `§ cpus (service-level, non-swarm form)` (contradicted AC2 non-swarm mandate); (3) AC2 mapping table rows rewritten to canonical non-swarm form (`cpus:` / `mem_limit:` / `shm_size:` at service level, with explicit "NOT `deploy.resources.limits.*`" inline annotations) — eliminates two-instructions-in-conflict between table and following scope-clarification; (4) AC5 prek hook config adds `files: '(^|/)\\.(envrc|envrc\\.local|secrets)$'` filter matching existing keel-invariants hooks at `.pre-commit-config.yaml:31,37` — scopes invocation to only when a matching file is staged; (5) Task 4 manifest template resolves `sourcePath` ambiguity to `docs/invariants/gitignored-secret-commit-deny.md` + `sha256sum` of the DOC (matches AI-7 precedent at `invariants.manifest.ts:252` — doc-first for primary-documentary invariants); (6) Task 4 `INVARIANTS.md` anchor format updated to backtick-wrapped ID (`` **`INV-gitignored-secret-commit-deny`** ``) + new `### Gitignored-secret commit-deny (Story 2.2)` section-heading convention matching `INVARIANTS.md:20-94` per-story grouping; (7) Task 5 `git check-ignore -v` verification expanded to call out exit-code semantics (exit 1 = no-rule-match = un-ignored; exit 0 = rule-match = ignored) so dev agent doesn't misinterpret stdout; (8) Task 3 `.secrets.example` gets a consumer-flow paragraph clarifying gitignored `.secrets` copy-seed; (9) Open Question #3 (manifest `sourcePath` doc-vs-impl) marked RESOLVED: doc-first per AI-7 precedent verified at `invariants.manifest.ts:252`. Story Status stays `ready-for-dev`; lifecycle state `drafted → validated` tracked in IP (not in story file). Sprint-status UNCHANGED: `2-2: ready-for-dev`. **Next iteration:** `/bmad-testarch-atdd` for Story 2.2 red-phase scaffolds (or skip-rationale per FR14n); state `validated → atdd-scaffolded`.

- [x] iter-145: **`/bmad-create-story` produced `_bmad-output/implementation-artifacts/2-2-envrc-parameterisation-contract.md` (Story State: `_(no story) → drafted`; sprint-status `2-2: backlog → ready-for-dev`).** Exhaustive context-engine pass per skill contract: Epic 2 context loaded from `epics.md:1142-1231`; Architecture I3/I5/I5a/I6 sections loaded from `architecture.md:269-342`; repo-root `.gitignore` (L35-39) + `.pre-commit-config.yaml` + `packages/keel-invariants/` (manifest shape, eslint-rules precedent, sync-gate flow) + `packages/devbox/docker-compose.yml` (post-iter-144 state) + Story 2.1 patterns (iter-128..iter-144 CR chain) all extracted into Dev Notes. Story file structure: AC1-AC5 verbatim from epics + enriched with 14 scope clarifications covering (tmpfs-naming drift MB vs GB, authoritative M4-Pro reference defaults table, file-header block contract, inline-comment format, compose parameterisation mapping table with deploy-stanza anti-pattern callout + non-swarm cpus/mem_limit/shm_size canonical form + loopback-bound ports for Story 2.12 reduced-rework, tmpfs/SSH/shared DEFERRED to Stories 2.5/2.11/2.12, `.secrets.example` 6-key scaffold matching architecture.md:328 verbatim, `.gitignore` bang-suffix negation pattern, keel-invariants lint rule mechanism-ii prek hook design vs ESLint rule mechanism-i, `INV-gitignored-secret-commit-deny` manifest entry + `docs/invariants/` shape matching AI-7 iter-135 precedent, sourcePath doc-vs-impl decision deferred to dev-time). 8 Tasks (Task 1 `.envrc.example`, Task 2 compose parameterisation, Task 3 `.secrets.example`, Task 4 lint rule end-to-end, Task 5 `.gitignore` extension, Task 6 README § Retuning, Task 7 structural verification sweep, Task 8 sprint-status hygiene) with AC references + grep-friendly verification literals. Testing standards: Vitest unit test for lint rule natural; compose/gitignore are infrastructure-smoke; ATDD decision deferred to `/bmad-testarch-atdd` invocation per FR14n. Open questions enumerated for implementation-time resolution (tmpfs naming drift, `.secrets.example` location drift, manifest sourcePath doc-vs-impl). Sprint-status: `2-2: backlog → ready-for-dev`, `last_updated: 2026-04-21 Story-2-2-ready-for-dev UTC`. **Next iteration:** `/bmad-create-story (args: "review")` pre-dev SM validation per § Story Lifecycle Decision Matrix row `drafted`.

- [x] iter-144: Story 2.1 third CR gate ZERO-PATCH verdict (`fixes-pending → done`; sprint-status `2-1: in-progress → done`). Full detail in `9dbe6e3` commit message + story-2.1 file § Review Findings `#### iter-144 RE-RUN-2`. Twenty-eighth cumulative Epic post-SM CR / CR-re-run iteration; multi-cycle upper envelope bounded to three cycles matching Story 1.9 precedent.

## Context

- **Phase:** 4-implementation — Epic 2 open at 1/17 stories done (2.1 done at iter-144); Story 2.2 `atdd-scaffolded` as of iter-147 (`ready-for-dev` sprint-status since iter-145; Story `Status: ready-for-dev` unchanged).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available` § Backend contract). Iteration-context bind-mount denial persists; operator-workstation resolves via native Docker Desktop under host-shared `/Users/...` OR backend-A isolated DinD harness.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Version matrix in `packages/devbox/VERSIONS.md § Bake log`.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; Story 2.1 done; Story 2.2 ready-for-dev; Stories 2.3..2.17 remain in `backlog`.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2; PR #230 transitions to Open at epic completion).
- **Story:** 2.2 — `.envrc` parameterisation contract.
- **Story File:** `_bmad-output/implementation-artifacts/2-2-envrc-parameterisation-contract.md`.
- **Story State:** `atdd-scaffolded` — iter-147 `/bmad-testarch-atdd` ATDD-SKIP per FR14n row 3 with v1.2 Change Log entry appended to story file (TWELFTH cumulative Epic ATDD-skip; second Epic 2 + second infrastructure-smoke; ground-a-b-c-d rationale; variant-(ii)+(iii) substitution; no test-plan artefacts). Next iteration's `/bmad-dev-story` transitions to `in-dev` (or `in-dev (partial)` if budget exceeded).
- **GitHub Issue:** Story 2.2 issue TBD — ralph.py injects `RALPH_ISSUE_NUMBER` / `RALPH_ISSUE_URL` next iteration. Parent Epic 2 → #10 (unchanged). Commit trailer pattern `Refs #<story-issue>` once RALPH_ISSUE_NUMBER resolves; `Refs #10` meanwhile for Epic traceability.
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2; no CI runners pre-Epic-13; transitions to Open at Story 2.17 done).

## Notes

- **DinD is fork-time substrate, not transitional.** `INV-devbox-dind-available` — every fork's cc-devbox-equivalent must provide Docker. Podman/rootless-Docker conformant if daemon reachability holds (backend C extension per iter-144 defer).
- **Backend B is the reference environment at 2026-04-21.** Host socket-passthrough. Broad-state-mutation scripts MUST detect backend + refuse destructive ops by default (`benchmark.sh` reference from iter-122). Bind-mount compose runtime requires host-shared File Sharing allowlist coverage — operator-owned from iteration context.
- **NFR2 authority unchanged.** AC 4 cold ≤ 300s / warm ≤ 30s remains authoritative on M4-Pro native. Story 2.2 is infrastructure-parameterisation — no runtime-performance impact on NFR2; AC3 retunability verified via `docker compose config` static YAML expansion (no daemon required).
- **Story 2.2 parameterises knobs for Story 2.5 (tmpfs) + Story 2.11 (shared) + Story 2.12 (loopback ports / SSH).** Those downstream stories activate the tmpfs / shared / SSH knobs that Story 2.2 adds to `.envrc.example`. Story 2.2 MUST NOT partially implement those — defer per § Story 2.1 "NFRs explicitly DEFERRED" posture.
- **Twelfth cumulative Epic ATDD-skip pattern validated** (iter-147; second Epic 2 — Stories 1.7..1.16 + 2.1 → 2.2; hybrid infrastructure-smoke + configuration-surface class with Task 7 structural verification + forbidden-pattern sweep covering AC 1-5 at CLI-exit-code level; story's own § Testing standards block contemplated the skip explicitly; ground-(d) upstream-provenance from cc-devbox absorption holds for Story 2.2 because compose-parameterisation is a post-absorption retune; budget cheap at ~12K tokens — only Change Log v1.2 append + IP/RALPH.md updates, no test-plan artefacts authored per variant-(ii)+(iii) substitution pattern). **Cross-epic auto-advance pattern validated** (iter-96). **Partial-completion pattern validated** (iter-99). **Backend-B safety-gate pattern established** (iter-122). **Review-with-operator-owned-carve-out pattern validated** (iter-125). **SM-verified-with-operator-owned-carve-out pattern validated** (iter-127). **CR-fixes-pending-adversarial-default pattern validated** (iter-128). **CR-re-run-adversarial-default + action-item-bundling pattern validated** (iter-138). **Doc-drift two-row bundle closure pattern validated** (iter-141). **Story-artefact spec-vs-code drift closure pattern validated** (iter-142). **Operator-UX polish pattern validated** (iter-143). **ZERO-PATCH third-gate close pattern validated** (iter-144; three-cycle upper envelope for Epic-2 infrastructure-smoke stories). **Context-engine-exhaustive story-creation pattern validated** (iter-145; Story 2.2 file with 14 scope clarifications + AC-mapping tables + 8 concrete Tasks + grep-friendly verification literals at file-producer time; designed to minimise dev-agent decision surface + prevent disasters by framing mapping tables + naming drift + mechanism-ii prek design + deferral posture up front). **Pre-dev SM validation with parallel-subagent-review pattern validated** (iter-146; three-layer fan-out — source-doc fidelity / repo-state alignment / adversarial gap hunt — against the iter-145 exhaustive story still found 9 small cross-file drifts (AC-vs-deliverable knob-list, AC1↔AC2 non-swarm-form contradiction, prek `files:` filter absence, manifest `sourcePath` doc-vs-impl ambiguity resolved via AI-7 precedent lookup at `invariants.manifest.ts:252`, `INVARIANTS.md` backtick-convention, check-ignore exit-code semantics, `.secrets` consumer-flow note). **Lesson:** even exhaustively-drafted stories benefit from the post-draft review pass — it surfaces consistency drifts that the single-threaded drafting pass cannot internally audit. Parallel subagents (each loading disjoint reference surfaces) compensate for the drafter's tunnel-vision.
