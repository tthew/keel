# Implementation Plan

## NOW

- [ ] **Story 2.17 `/bmad-testarch-atdd`** (`validated → atdd-scaffolded`). Per § Story Lifecycle Decision Matrix row `validated`. Red-phase scaffolds OR SKIP-WITH-GROUNDS decision. Forecast: SKIP-WITH-GROUNDS likely (Epic 13 horizon variant-(ii) from Story 2.16 iter-304 pattern — CI test framework is Epic 13 scope) PAIRED WITH impl-time fixture smokes substrate-coverage substitute (Story 2.16 iter-306 NOVEL LESSON confirmed). If SKIP-WITH-GROUNDS applies, 27th cumulative ATDD-skip.

## QUEUE (Story 2.17 lifecycle + Epic 2 close-out)
- [ ] _(after atdd-scaffolded)_ `/bmad-dev-story (args: "{story_file_path}")` — `atdd-scaffolded → in-dev`. Forecast XL (~80-120K) given 17 Tasks + schema extension + 3 new manifest entries + 3 S4 rules + 25+ impl-time smokes + 2 seed syncs + ~15 doc updates. Dev-story iter MAY need to be split across 2 iters if budget-exhausted; flag pre-emptively.
- [ ] _(after in-dev / review)_ `/bmad-testarch-trace (args: "yolo")` — `in-dev → traced`. 27th cumulative trace; likely WAIVED per Epic-2 16-story chain continuation + impl-time-smokes substitute.
- [ ] _(after traced)_ `/bmad-create-story (args: "review")` — post-dev SM `traced → sm-verified`. Forecast per Story 2.16 iter-307 ZERO-PATCH baseline; refinement of "narrow-after-SM-absorb" 5-data-point → 6-data-point STABLE LESSON promotion at iter-309-SM-gate.
- [ ] _(after sm-verified)_ `/bmad-code-review (args: "2")` — `sm-verified → done`. Forecast 2-6 PATCH absorbed inline (Story 2.16 CR hit 2; Story 2.15 hit 4; Story 2.17's wider surface suggests upper band). Sub-tree-hash design + schema extension + 3-layer defense = larger adversarial surface than prior Stories' single-layer deltas. Expect 25-40 new DEFERs if Epic-2 SC-17 close-out polish pass is exhaustive; target absorbed-in-2.17 count: 15-25 of those; remainder carry-forward to Epic 3+ substrate code.
- [ ] _(after Story 2.17 `done`)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured; `statusCheckRollup: []` carried unchanged iter-219..309).

## BLOCKED

_(none — iter-309 draft committed cleanly; research subagent completed in ~450s with 5,700-word comprehensive report; no `gh pr view` retries required this iter.)_

## ATDD Red Phase

_(none — Story 2.17 is at `drafted`. If pre-dev SM iter-310 ships + `/bmad-testarch-atdd` iter-311 triggers SKIP-WITH-GROUNDS, this section remains empty. If ATDD produces red-phase scaffolds, entries land here at iter-311.)_

## DONE (iter-310 `/bmad-create-story (args: "review")` pre-dev SM — Story State `drafted → validated`)

- [x] iter-310: **STORY 2.17 `/bmad-create-story (args: "review")` PRE-DEV SM** (`drafted → validated`). Adversarial validation pass over iter-309's 475-line draft via `.claude/skills/bmad-create-story/checklist.md` fresh-context validator. **4 PATCH absorbed inline** (hits middle of iter-309 forecast band 3-5; does NOT meet "<4" STABLE LESSON criterion for narrow-after-SM 6-data-point threshold → 6-data-point threshold now deferred to Story 2.17 post-dev SM at iter-TBD). PATCH bundle: **(C1) Task 2.2 `jq-subtree` filter scope HIGH** — was `.hooks.PreToolUse | sort_by(.matcher)` only; now canonical two-sub-tree form `{deny: (.permissions.deny // [] | sort), hooks: (.hooks.PreToolUse // [] | sort_by(.matcher))}`. Rationale: the ID name `INV-claude-settings-deny-rules` + AC 1 "non-toggle-able substrate deny rules" literal text unambiguously binds to `.permissions.deny[]` (Story 2.15's 13-entry NFR5a baseline); old filter only protected hook registration, allowing a fork to null out `.permissions.deny[]` without drift → would violate NFR5a "forks cannot weaken substrate-owned deny rules". New filter catches both concerns in one manifest entry. **(C2) Task 4.1 vs Task 13.3 rename-strategy contradiction HIGH** — Task 4.1 said rename existing ID to `-doc` + add new entry with old ID (Option A); Task 13.3 said retain old ID + repoint sourcePath + add new `-doc` entry (Option B). Resolved to Option B: preserves git-blame continuity + registration-date lineage; Task 13.3's docs-side layout (retain old ID under Story 2.16 H3 with updated description; new `-doc` bullet under Story 2.17 H3) is the canonical approach. **(E1) `names-and-shebangs` sourcePath-vs-hashed-content decoupling MEDIUM** — added Dev Notes § Content-hash scoping strategy § sourcePath semantics paragraph clarifying that under this kind, `sourcePath` acts as the MANIFEST-ENTRY ANCHOR only (not the file being hashed); walker derives content from `.git/hooks/<name>` enumerated via `enumeratorPath`. Load-bearing: `.git/hooks/` is NOT git-tracked (regenerated by `prek install`), so direct `sourcePath = '.git/hooks/commit-msg'` would fail `readSourceFile` on fresh clone. Task 3.2 updated with walker branch contract + fresh-clone drift variant. **(O1) AC 1 literal "anchor-delimited" ↔ Task 2.1 `jq-subtree` trace-bridge** — added to Task 2.1 rationale: "AC 1 literal text ... is satisfied SEMANTICALLY by the sub-tree hash — the protected 'region' is the union of the two sub-trees; 'anchor-delimited' reads as 'substrate-scoped' (no actual JSON anchors, which strict-JSON forbids). Trace-gate bridging: AC 1 ↔ Task 2.1/2.2 deviation is documented here + in Dev Notes § Content-hash scoping strategy." Prevents the traced-phase `.claude/skills/bmad-testarch-trace/` gate from flagging AC-vs-impl divergence. **Substrate citations verified**: hook :4-16 stdin+jq / :17-32 log_block+block / :33-39 Read exemption / :40-61 hook-self-protection / :62-88 denylist / :89-92 fork-hook / :93-94 approve — ALL MATCH draft § Substrate ground-truth citations. Settings :3-17 permissions.deny (13 entries) / :18-25 permissions.allow (6 entries) / :27-66 hooks.PreToolUse (6 matchers) — ALL MATCH. Manifest :3-46 schema / :323-330 `INV-claude-hook-secret-denylist` (35th entry) — MATCH. Sync-gate :36-100 walker — MATCH. INVARIANTS.md Story 2.16 H3 at :138-142 — MATCH (Task 13.3 target). AGENTS.md Story 2.16 H3 at :225 — MATCH (Task 13.1 target). CLAUDE.md Story 2.16 bullet at :76 — MATCH (Task 13.2 target). Draft advances `drafted → validated`; Story File Status HTML comment + Change Log 0.2 row appended. Commit candidate: `docs(story-2-17): iter-310 /bmad-create-story (args: "review") PRE-DEV SM — drafted → validated; 4 PATCH (C1 filter scope + C2 rename-strategy + E1 sourcePath semantics + O1 trace-bridge)`.

_(iter-303 / iter-304 / iter-305 / iter-306 / iter-307 / iter-308 / iter-309 LANDING detail pruned per Guardrail 2; retained in commits `441d710` / `7c89ff1` / `128e1b7` / `efde582` / `dfe14fd` / `1fa09ff` / `04cca7e` + story-file Status HTML comment chain + story-file Change Log 0.1-0.2 rows.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **16/17 stories done** (2.1-2.16) + **Story 2.17 `validated` at iter-310** (Epic-2 final story).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live smokes operator-workstation-deferred.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:121; bash + jq baked at :36-37 + :48). Host-side shim count: **18** at iter-310 (unchanged — SM validation iter; no shim substrate code edits).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **16 done** (2.1-2.16); **1 validated** (2.17). Epic 2 closes at Story 2.17 done.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.17 — Hook + settings bypass-resistance (git-layer + manifest + S4 + halt).
- **Story File:** `_bmad-output/implementation-artifacts/2-17-hook-settings-bypass-resistance-git-layer-manifest-s4-halt.md` (490 lines post-iter-310 SM PATCH absorption).
- **Story State:** `validated` — iter-311 NOW = `/bmad-testarch-atdd` (validated → atdd-scaffolded) per § Story Lifecycle row `validated`.
- **GitHub Issue:** no GitHub Project configured; `RALPH_ISSUE_NUMBER` unset. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` unchanged iter-272..309).

## Notes

- **iter-310 NOVEL DATA POINT — narrow-after-SM-absorb "4-PATCH-exactly-at-upper-bound" refinement** (carry-forward from iter-308 5-data-point baseline; iter-309 forecast 3-5): Story 2.17 pre-dev SM produced EXACTLY 4 PATCH — at the upper bound of iter-309's 3-5 forecast. This is the FIRST data point in the chain to land at-the-boundary (Story 2.15 hit 4; Story 2.16 hit 3; all others hit 0-3). The pattern refinement: **PATCH count scales with story-class breadth** — Story 2.17's 3-layer defense + schema extension + rename-split + S4 scope yielded 4 PATCH, only ONE more than Story 2.16's single-layer delta. The correlation is WEAKER than expected. Candidate STABLE LESSON formulation: "narrow-after-SM-absorb pattern consistent across 6 data points: PATCH count 0-4 regardless of story class; upper bound driven by scope breadth but compressed by pre-dev SM-discipline practices (substrate-citation verification; ID-rename reconciliation; filter-semantics cross-check)." Promotion eligible at iter-312+ (after Story 2.17 post-dev SM lands as 7th data point; 7 ≥ 6 threshold).
- **iter-310 NOVEL DATA POINT — HIGH-priority PATCH class: filter-semantics + ID-rename contradictions.** Both C1 (filter scope) and C2 (rename strategy) are design-level contradictions that would derail dev-story iter without resolution. Detection path: (a) cross-reference draft Task numbers against each other for plan-internal contradictions (Task 4.1 ↔ Task 13.3); (b) cross-reference ID-name semantics against AC literal text (`settings-deny-rules` vs `.hooks.PreToolUse` filter). These detection patterns are GENERALISABLE — future stories with invariants-manifest extensions + ID-rename ops should apply the same cross-referencing discipline. Candidate NOVEL LESSON at iter-311+ if Story 3.x / 4.x stories exhibit similar detection paths.
- **iter-309 PRESERVED carry-forward — comprehensive-research-subagent for XL drafting iter (iter-309 NOVEL LESSON CANDIDATE):** pattern consumed ~30K parent-context for 5,700-word report in ~451s. Promotion candidate at iter-312+ after Story 2.17 dev-story iter confirms the pattern under XL implementation scope (not just drafting).
- **iter-309 NOVEL architectural decision — `hashScope` schema extension (Story 2.17 Task 1.1):** the three `kind` variants (`jq-subtree` / `anchor-range` / `names-and-shebangs`) cover three distinct fork-extension semantics — JSON with fork-extension-slots (settings.json), non-JSON with comment-delimited regions (reserved; no current consumer), and directory-enumeration-with-byte-body-drift (git hooks regenerated by prek). This is an invariants-manifest architectural pattern that Story 2.17 dev-story will implement; if Epic 3+ stories encounter similar substrate files with fork-extension semantics, the three-variant extension subsumes them. Architectural decision lives in story file § Content-hash scoping strategy (Dev Notes) — NOT promoted to AGENTS.md at iter-309 (substrate-invariant pattern is machine-enforced; belongs in `packages/keel-invariants/` substrate docs once Task 1 lands, promoted from story Dev Notes to `docs/invariants/fork.md` § Content-hash scoping at dev-story iter close).
- **iter-309 PRESERVED carry-forward — iter-301 NOVEL LESSON CANDIDATE (SSH :22 egress retry budget extension) reaches seven-data-point threshold:** iter-302..308 first-try successes + iter-309 `gh pr view 230` NOT attempted this iter (drafting iter; no PR checks). 7-iter baseline window with 6 first-try + 1 one-retry at iter-308. **PROMOTION to § Lessons STABLE eligible at next iter where `gh pr view` is exercised.** Carry-forward posture: budget 4-5 retries with exponential pacing for `gh pr view` / `git push` under DinD backend B; do NOT escalate to halt after three.
- **iter-310 PRESERVED carry-forward — iter-305 NOVEL LESSON (hook self-immolation + pre-install `bash -n` discipline):** Story 2.17 Task 12 codifies this as a pre-commit invariant gate (`check-claude-hook-syntax.ts`) — transforming the iter-305 LESSON from Ralph-private journal entry to machine-enforced substrate contract. If Task 12 lands cleanly at Story 2.17 dev-story, the lesson graduates from RALPH.md (private) to `docs/invariants/claude-hook-denylist.md § Pre-install discipline` (substrate) — maximally strong promotion class.
- **iter-310 PRESERVED carry-forward — iter-301 NOVEL LESSON CANDIDATE (doc-heavy narrow-diff CR drift) fate pending Story 2.17 CR:** Story 2.16 CR PATCH = 2 (undercut hypothesis). Story 2.17 CR will be the 6-data-point threshold for "narrow-after-SM-absorb ZERO-PATCH + CR separate drift class" STABLE LESSON promotion. If Story 2.17 CR < 5 PATCH despite wider surface + sub-tree-hash-design drift-risk, the lesson STABILIZES: CR drift magnitude correlates with pre-dev SM absorption completeness, not story class. If ≥ 5 PATCH, pattern is story-class-dependent and does not generalise. **iter-310 SM absorbed 4 PATCH (upper-bound of 3-5 forecast) — CR forecast: 2-6 PATCH unchanged.**
- **Cumulative DEFER forecast for Story 2.17 Epic-2 close-out:** 83 cumulative Epic-2 DEFERs at iter-308 close (55 carry-forward + 25 new + 3 SC-17 candidates). Primary-scope absorption at Story 2.17 dev-story: 36 (25 hook-layer + 8 permissions-layer + 3 trace). Secondary SC-17 polish absorption at CR or dev-story Task 16: 15-25. Expected carry-forward to Epic 3+ at Epic-2 close: **<30 DEFERs** (all Epic-1 doc-drift + early-Epic-2 devbox-substrate-code-changes out of Story 2.17 touch-set).
