# Implementation Plan

## NOW

- [ ] **Run `/bmad-create-story (args: "review")`** for Story 2.5 pre-dev SM validation per § Story Lifecycle `drafted → validated`. Fresh context; adversarial competition-checklist re-analysis against Story 2.5 file (342 LOC, 19 SCs, 12 Tasks). Expected envelope: ZERO-PATCH (Story 2.4 iter-173 pre-dev SM precedent) OR N-improvement-items triaged via "all/critical/select/none/details" menu. FR14n Story State transitions `drafted → validated` at this iteration's close. ~medium.

## QUEUE (Story 2.5 lifecycle → Epic 2 Story 2.6..2.17 → Epic 2 close-out)

- [ ] _(after 2.5 validated)_ `/bmad-testarch-atdd` red-phase scaffolds per § Story Lifecycle `validated → atdd-scaffolded` (skip rationale recorded inline if hybrid ground-(c)+(ii)+(iii) applies — Epic-2 precedent at Stories 2.1..2.4 all ATDD-skipped for infrastructure-security stories with no testable unit ACs).
- [ ] _(after 2.5 atdd-scaffolded)_ `/bmad-dev-story` per § Story Lifecycle `atdd-scaffolded → in-dev`.
- [ ] _(after 2.5 in-dev)_ `/bmad-testarch-trace (args: "yolo")` per § Story Lifecycle `in-dev → traced`.
- [ ] _(after 2.5 traced)_ `/bmad-create-story (args: "review")` post-dev SM per § Story Lifecycle `traced → sm-verified`.
- [ ] _(after 2.5 sm-verified)_ `/bmad-code-review (args: "2")` CR opener per § Story Lifecycle `sm-verified → (fixes-pending | done)`.
- [ ] _(conditional, post-Story-2.5 done)_ `/bmad-create-story` for Story 2.6 (host-side `pnpm devbox:*` CLI surface) per § Story Lifecycle + § Cross-epic within-epic path. 12 backlog stories remaining in Epic 2 (2.6..2.17) after 2.5 done.
- [ ] _(conditional, post-Story-2.17 done)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge. Epic 2 completion.
- [ ] _(conditional, post-Epic-2 merged)_ § Cross-epic transition → Epic 3 Story 3.1 OR halt EPIC_DONE awaiting merge.

## BLOCKED

_(none — Story 2.5 drafted cleanly via `/bmad-create-story` at iter-184 per § Story Lifecycle `_(no story)_ → drafted`. Story file `_bmad-output/implementation-artifacts/2-5-container-hardening-non-root-user-capabilities-tmpfs-noexec-named-volume.md` created at 342 LOC with 19 SCs + 12 Tasks; sprint-status row `2-5: backlog → ready-for-dev`; FR14n Story State `_(no story)_ → drafted`.)_

## ATDD Red Phase

_(none — Story 2.5 has not yet reached `validated → atdd-scaffolded` state. With Story 2.4 closed this section resets for Story 2.5 which will make its own ATDD decision at iter-186+ per § Story Lifecycle row `validated → atdd-scaffolded`.)_

## DONE (Story 2.5 lifecycle — keep current story chain, prune after Story 2.6 starts)

- [x] iter-184: **`/bmad-create-story` drafted Story 2.5 — Container hardening (non-root user + capabilities + tmpfs noexec + named volume).** Story file created at `_bmad-output/implementation-artifacts/2-5-container-hardening-non-root-user-capabilities-tmpfs-noexec-named-volume.md` (342 LOC; 19 SC-1..SC-19; 12 Tasks with ~45 subtasks). **Key design tension surfaced as SC-4 three-cap reconciliation:** AC 2 as drafted reads `cap_add: [NET_ADMIN, NET_RAW]` only, but under `cap_drop: [ALL]` the container cap bounding set excludes NET_BIND_SERVICE — neither root nor non-root processes can bind port 53 (dnsmasq fails at start). SC-4 extends cap_add to `[NET_ADMIN, NET_RAW, NET_BIND_SERVICE]` with explicit NFR7-intent-preservation rationale; AC 2 text updated inline to reflect the three-cap list with SC-4 pointer. **Touched files (static):** story file only at iter-184; sprint-status.yaml row `2-5: backlog → ready-for-dev` + two `last_updated` markers (canonical + audit-trail comment). **Expected implementation surface at dev-story time (Task 1-12):** Dockerfile (useradd + chown + setcap + USER directive; ~10 new LOC + 2 comment blocks); docker-compose.yml (cap_drop/cap_add/security_opt/tmpfs/volumes; ~15 new LOC); entrypoint.sh (WORKSPACE_OWNER default flip; ~1 LOC + comment refresh); docs/invariants/devbox-hardening.md (NEW; ~150 LOC); packages/keel-invariants/src/invariants.manifest.ts (new INV-devbox-homedev-named-volume entry; ~10 LOC + sync-gate contentHash); INVARIANTS.md (new H3 bullet; ~4 LOC); AGENTS.md (new H3 subsection under Devbox env; ~20 LOC); packages/devbox/README.md (new § Hardening H2; ~80 LOC). Total ~100-150 patch lines across 8 files — surgical-security class comparable to Story 2.3 infrastructure-security-class surface. **SC-14 Story-2.4 compatibility posture:** Task 5 includes empirical-verification gate for `/run/` tmpfs ownership under USER dev; if non-writable, conditional relocation of whitelist.sh state files to `/tmp/keel-state/` (tmpfs but state-files-not-executable so noexec is fine). **iter-185 NOW = `/bmad-create-story (args: "review")` pre-dev SM validation** fresh context; FR14n Story State `drafted → validated` at iter-185 close. PR #230 stays Draft.

## Context

- **Phase:** 4-implementation — Epic 2 at 4/17 stories `done` (2.1 + 2.2 + 2.3 + 2.4) in sprint-status enum + 12/17 `backlog` (2.6..2.17) + 1/17 `ready-for-dev` (2.5) after iter-184 `/bmad-create-story` flipped row `2-5: backlog → ready-for-dev`.
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.5 live smokes (Task 11.1–11.8) operator-workstation-deferred per Story 2.4 SC-17 precedent — DinD-B cannot safely exercise full `docker exec` sequences against cap-dropped containers without poisoning the host's docker state. Dev-story SHOULD attempt a dry compose-parse + container-start in DinD-B to confirm happy path; full AC-verification matrix is M4-Pro-native-authoritative.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Story 2.5 is the first Epic-2 story to touch `docker-compose.yml` in a structural way (cap_drop/cap_add/security_opt/tmpfs/volumes) + entrypoint.sh WORKSPACE_OWNER default flip + Dockerfile USER directive + setcap on dnsmasq/nft binaries. Expect rebuild requirement for Dockerfile changes to take effect.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; 2.1 + 2.2 + 2.3 + 2.4 done; 2.5 ready-for-dev; 2.6..2.17 in `backlog`. Epic 2 closes at Story 2.17 (PR #230 transitions Draft→Open at that point).
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2; PR #230 stays Draft until Story 2.17 completion triggers the final-CI-gate + Draft→Open transition).
- **Story:** 2.5 — Container hardening (non-root user + capabilities + tmpfs noexec + named volume).
- **Story File:** `_bmad-output/implementation-artifacts/2-5-container-hardening-non-root-user-capabilities-tmpfs-noexec-named-volume.md`
- **Story State:** `drafted` — at iter-185's close transitions to `validated` upon `/bmad-create-story (args: "review")` pre-dev SM approval.
- **GitHub Issue:** Story 2.5 → _(not yet created; RALPH_ISSUE_NUMBER unset at iter-184)_; Epic 2 → #10 (stays open; Ralph closes epic issues on EPIC_DONE halt only).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **iter-184 single-iteration drafting envelope.** Story 2.5 drafted in one iteration per § Story Lifecycle `_(no story)_ → drafted` — fresh context consumed `/bmad-create-story` skill workflow (step 1 auto-discovery → step 2 epics.md + previous-story-2.4 intelligence → step 3 architecture.md I5 + NFR7/8/10 extraction → step 4 web-research-skipped [established Docker/Linux primitives, no version-research required] → step 5 template materialization → step 6 checklist-validation deferred to iter-185 per Ralph's one-task-per-iteration + competition-checklist-runs-in-fresh-context posture). Drafting output density comparable to Story 2.4 iter-172's 19-SC + 13-Task envelope; SC count matches the infrastructure-security class complexity (Story 2.3 had 17 SCs, Story 2.4 had 19 SCs, Story 2.5 has 19 SCs).
- **Story 2.5 SC-4 three-cap reconciliation (load-bearing spec-drift handled at draft).** The single spec tension in Story 2.5 is the AC 2 literal `cap_add: [NET_ADMIN, NET_RAW]` vs runtime port-53-bind requirement. PRD NFR7 (prd.md:1078) + architecture line 73 both read "NET_ADMIN/NET_RAW-only kernel caps" in prose, but under `cap_drop: [ALL]` the Linux capability bounding set semantics mean NET_BIND_SERVICE is stripped — dnsmasq CANNOT bind :53 as either root or non-root without NET_BIND_SERVICE in the bounding set. SC-4 pins the three-cap resolution `[NET_ADMIN, NET_RAW, NET_BIND_SERVICE]` with explicit narrow-cap rationale (each cap has a distinct substrate-operational purpose; NET_BIND_SERVICE grants only port-<1024 bind, no other escalation vector). This is an AC-reconciliation not an AC-rewrite — AC 2 inline wording updated to reflect the three-cap list with SC-4 pointer. Future adversarial CR: if spec-drift flagged, dev-story's response is SC-4 reasoning verbatim + the `docs/invariants/devbox-hardening.md` § Capability bounding set sub-contract.
- **Story 2.5 SC-14 Story-2.4 whitelist.sh compatibility empirics.** The only empirical uncertainty in the story is whether Docker's tmpfs auto-mount of `/run/` preserves image-layer ownership under USER dev. Task 5.1 is a dev-story-time smoke; Task 5.2 conditionally relocates whitelist.sh state files to `/tmp/keel-state/` if the empirical result requires. This is surfaced as SC-14 so dev-story has an unambiguous decision path without halting for correct-course.
- **Story 2.5 NFR2 budget impact forecast.** Dockerfile changes (useradd + chown + setcap) add ~100ms to cold-start (image-build cached after first bake); compose stanzas are constant-time kernel ops at container-start with no measurable impact. Story 2.5 SHOULD NOT measurably change cold-start or warm-start benchmarks — no NFR2 re-measurement required at story close. M4-Pro-native NFR2 authority remains Story 2.1's iter-130 capture per SC-12 forecast.
- **Story 2.5 implementation-surface forecast (for dev-story iteration planning).** 12 Tasks with ~45 subtasks; expected dev-story single-iteration landing per Story 2.4 iter-174 precedent (~360 LOC in one iter). Task 5's conditional relocation is the only branch that could expand to 2 iterations; Tasks 1-4 + 6-12 are surgical and deterministic. ~100-150 patch LOC baseline; with Task 5.2b relocation branch, up to ~200 patch LOC.
- **Substrate sanity check (post-iter-184):** Story 2.5 drafting-iteration touches ONLY: story file (NEW), sprint-status.yaml (row flip + last_updated markers), IP + RALPH.md (iter-184 signpost). No Dockerfile / docker-compose / entrypoint / invariants-manifest edits at iter-184 — those land at iter-187 dev-story (after pre-dev SM iter-185 + ATDD-or-skip iter-186). `INV-devbox-egress-contract` contentHash `cd1b5da1…` unchanged through iter-184; `INV-devbox-dind-available` unchanged; no invariant manifest drift.
