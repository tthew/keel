# Implementation Plan

## NOW

- [ ] **Run `/bmad-code-review (args: "2")`** — CR opener per § Story Lifecycle matrix row `sm-verified → fixes-pending | done`. args `"2"` pre-selects "Create action items". Forecast: 4-6 PATCH for user-facing-CLI class (matches Story 2.4 5-PATCH envelope; Story 2.6 broader surface but per-script simpler). Adversarial fan-out: Blind Hunter + Edge Case Hunter + Acceptance Auditor against cumulative Story 2.6 substrate diff (13 new host-side scripts + 13 pnpm entries + 3 docs surfaces + 2 invariant-sourcePath contentHash refreshes). Per-finding → QUEUE fix task (file, line, requested change) per matrix row `fixes-pending`. On ZERO-PATCH close → direct `done`. Fresh context; fresh iteration. ~small.

## QUEUE (Story 2.6 lifecycle → Epic 2 Story 2.7..2.17 → Epic 2 close-out)

- [ ] _(post-CR-drain; QUEUE empty)_ `/bmad-code-review (args: "2")` — CR-closure gate (expected ZERO-PATCH per Epic-2 3-story precedent: 2.3 iter-171, 2.4 iter-183, 2.5 iter-197).
- [ ] _(post-Story-2.6-done; through Story 2.17-done)_ Iterate Story 2.7..2.17 through the full lifecycle — 11 remaining substrate stories spanning ralph-auto-start + claude OAuth + gh OAuth + prereq-check + shared mode + port-publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-22 per iter-197..iter-203 pre-flight — "no checks reported on the 'feat/epic-2-packaged-devbox' branch").

## BLOCKED

_(none)_

## ATDD Red Phase

_(none — Story 2.6 ATDD-skipped at iter-200 per § Story Lifecycle `validated → atdd-scaffolded` (skip branch). 16th-cumulative FR14n ATDD-skip precedent. iter-202 trace gate WAIVED confirms the ATDD-skip-trace-WAIVED pairing — SEVENTEENTH cumulative pairing across Epic 1 + 2.)_

## DONE (Story 2.6 lifecycle — keep current story chain, prune after Story 2.7 starts)

- [x] iter-203: **`/bmad-create-story (args: "review")` — Story 2.6 post-dev SM requirements-satisfaction verification, `traced → sm-verified`.** ZERO unmet-AC findings across all 9 ACs against `0fe36ae` HEAD (iter-201 implementation + iter-202 trace-WAIVED substrate). Per-AC satisfaction recorded in story file `## Review Findings § Post-dev SM (iter-203, v1.1) — sm-verified` section with file:line anchors: AC 1 (13 scripts + 13 pnpm entries); AC 2 (build.sh:28 / rebuild.sh:32); AC 3 (start.sh:57-98 healthcheck poll; bare-running pinned by SC-9 as Story 2.13-dependent stub-friendly posture); AC 4 (stop/restart/clean three-tier); AC 5 (shell.sh:34-37); AC 6 (attach.sh:34); AC 7 (status/logs/monitor-host; monitor semantic = SC-15 PRD-authoritative JSONL tail reconciliation); AC 8 (env-check.sh:60-130); AC 9 (whitelist-host.sh:32 passthrough). AC 7 monitor + AC 3 bare-running acceptance both confirmed as documented cross-source/scope-boundary reconciliations pinned at pre-dev SM (iter-199 v0.2 AI-2) + SC-9 respectively — not unmet-AC findings. All 25 SCs verified; scope-isolation audit PASS (Story 2.1/2.3/2.4/2.5 surfaces unchanged; AR-7/AR-9 escalation paths NOT triggered per SC-19/SC-20 conditional gate). Change Log v1.1 appended. Story State `traced → sm-verified` (IP lifecycle); sprint-status stays `review` (BMad-skill-internal state). 4th cumulative Epic-2 user-facing-CLI-class ZERO-FINDING post-dev SM pass (after 2.1 + 2.4 + 2.5 precedent — see RALPH.md signposts).
- [x] iter-202: **`/bmad-testarch-trace (args: "yolo")` — Story 2.6 WAIVED.** 16th-cumulative FR14n trace-WAIVED precedent. 2nd user-facing-CLI class trace-WAIVED after Story 2.4. 9 P2 ACs × 13 host-side scripts; coverage NONE across all 9; gate WAIVED per hybrid ground-(a)-(b)-(c)-(d) variant-(ii)+(iii) rationale. Iteration-env evidence STRONGER than Story 2.5 — 3 end-to-end env-check branches (exit 3/0/2) + `bash -n` × 13 + pnpm run wiring-count + dispatcher + compose parse-smoke all PASSED. Per-AC lifecycle live smokes (AC 2-7 end-to-end) deferred to M4-Pro operator workstation per SC-13 + Story 2.4 SC-17 + Story 2.5 SC-13 backend-B live-smoke precedent. 4 trace artefacts landed under `_bmad-output/test-artifacts/traceability/`. Story State `in-dev → traced`. 17th cumulative ATDD-skip-trace-WAIVED pairing overall.
- [x] iter-201: **`/bmad-dev-story` — Story 2.6 host-side `pnpm devbox:*` CLI surface LANDED.** 13-script + 13-pnpm-entry + 4-docs-surface single-iter landing matching Story 2.4 iter-174 + Story 2.5 iter-187 user-facing-CLI-class precedent. Scope-owner for DEFERs landed unconditionally: AR-10 operator-migration docs (SC-21), AR-11 envvar shape validation (SC-14), AR-12 COMPOSE_PROJECT_NAME docs polish (SC-22). AR-7 + AR-9 NOT triggered. Sync-gate 2× fired: `INV-prek-prepare-lifecycle` + `INV-devbox-homedev-named-volume` contentHashes refreshed. All quality gates GREEN; iteration-env-safe smokes all PASSED. Sprint-status `ready-for-dev → review`. Story State `atdd-scaffolded → in-dev`.
- [x] iter-200: **`/bmad-testarch-atdd` → ATDD-skip (FR14n Story State `validated → atdd-scaffolded`).** 16th-cumulative FR14n ATDD-skip precedent. Grounds (c)+(ii)+(iii).
- [x] iter-199: **`/bmad-create-story (args: "review")` pre-dev SM validation of Story 2.6.** 8 findings applied as v0.2 Change Log entry (AI-1..AI-8): AI-1 CRITICAL SC-2 monitor-naming self-contradiction resolved; AI-2 CRITICAL PRD `:494` vs epics AC 7 semantic drift reconciled; AI-3 ENHANCEMENT Task 10.6 split; AI-4..AI-8 OPTIMIZATION. Story State `drafted → validated`.

## Context

- **Phase:** 4-implementation — Epic 2 at 5/17 stories `done` (2.1 + 2.2 + 2.3 + 2.4 + 2.5) + 12/17 remaining; Story 2.6 sm-verified (review) + 11 backlog (2.7..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.6 live lifecycle smokes operator-workstation-deferred per Story 2.4 SC-17 + Story 2.5 iter-187 precedent. 5-gate quality chain for docs-surface iters: sync-gate + typecheck + lint + format:check + docker compose parse-smoke.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Story 2.5 substrate live. Story 2.6 is a host-side CLI layer — no Dockerfile changes at iter-201/iter-202/iter-203 (SC-25 scope-boundary forbids; AR-9 escalation path conditional + not triggered).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; 5 done (2.1-2.5); Story 2.6 sm-verified (review); 11 backlog (2.7..2.17). Epic 2 closes at Story 2.17 (PR #230 Draft→Open transition at that point).
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.6 — host-side `pnpm devbox:*` CLI surface. Scope-owner for AR-7 `/run` relocation (SC-19, conditional — NOT triggered) + AR-9 `/etc/*` chown (SC-20, conditional — NOT triggered) + AR-10 pre-Story-2.5 volume migration docs (SC-21, LANDED iter-201) + AR-11 envvar shape validation (SC-14, LANDED iter-201) + AR-12 COMPOSE_PROJECT_NAME docs polish (SC-22, LANDED iter-201).
- **Story File:** `_bmad-output/implementation-artifacts/2-6-host-side-pnpm-devbox-cli-surface.md`
- **Story State:** `sm-verified` — transitioned at iter-203 via `/bmad-create-story (args: "review")` post-dev SM requirements-satisfaction verification. ZERO unmet-AC findings. Story file Status stays `review` (BMad-skill-internal); IP lifecycle Story State tracks the coverage/requirements/quality gate chain separately. iter-204 runs `/bmad-code-review (args: "2")` to transition `sm-verified → fixes-pending | done` (CR opener adversarial fan-out).
- **GitHub Issue:** Story 2.6 TBD (will be surfaced by ralph.py at next iter if the GH project is configured). Epic 2 → #10 (Ralph closes on EPIC_DONE halt).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **Post-dev SM ZERO-FINDING precedent — 4th cumulative Epic-2 user-facing-CLI-class pass (iter-203):** Story 2.1 + Story 2.4 + Story 2.5 + now Story 2.6 all closed the post-dev SM gate with ZERO unmet-AC findings in a single iteration. Carry-forward rule: when pre-dev SM (iter-199 here) applies full AI-1..AI-N patch set + trace substrate_evidence exercises each AC end-to-end (iter-202 here), the post-dev SM gate consistently clears clean. Story 2.7+ forecast: ZERO-FINDING post-dev SM remains the baseline; deviation is a signal to re-examine pre-dev SC coverage at that story's iter-N.
- **Epic-2 story-class CR forecast validated (iter-198 + iter-199 + iter-201 + iter-202 + iter-203):** Story 2.6 is user-facing-CLI class. CR-opener forecast unchanged from iter-198: 4-6 PATCH. Post-dev SM cleared clean at iter-203 (forecast 0-2 → actual 0). Carry-forward for Stories 2.7, 2.8, 2.9, 2.10, 2.11, 2.12 (likely user-facing-CLI class): expect 3-6 PATCH CR opener, 0-2 SM finding (default 0).
- **Single-iter landing precedent — user-facing-CLI class (iter-201 formalises, iter-203 carries forward):** Story 2.4 iter-174 + Story 2.5 iter-187 + Story 2.6 iter-201 all single-iter landings. Larger Story-2.6 surface (13 scripts) did NOT require multi-iter splitting because per-script complexity stayed low (most scripts <50 LOC wrappers). Carry-forward budget benchmark: ~95-105K per single-iter landing of this class.
- **Cross-source semantic-drift audit rule (iter-199 LESSON, iter-201 VALIDATED, iter-202 TRACE-RECORDED, iter-203 SM-VERIFIED):** SC-15 pinned PRD `:494` as authoritative for `pnpm devbox:monitor` semantic. iter-203 post-dev SM confirms the monitor-host.sh JSONL-tail implementation satisfies AC 7 via the SC-15 PRD-authoritative reconciliation — epics AC 7 literal phrasing is a documented cross-source deferment, not an unmet AC. Carry-forward rule for Stories 2.7 + 2.8 + 2.9 + 2.12 + 2.15 remains live.
- **`<verb>-host.sh` host-side shim naming convention (iter-199 LESSON, iter-201 EXTENDED, iter-202 TRACED, iter-203 SM-VERIFIED):** Story 2.6 ships `monitor-host.sh` + `whitelist-host.sh` as the canonical host-side shim naming pattern. Stories 2.7 (Ralph TUI wrapper), 2.8 (claude wrapper?), 2.9 (gh wrapper?), 2.12 (ssh wrapper?) candidates for the same pattern when in-container primitives exist.
- **Iteration-env-safe smoke delta vs Story 2.5 (iter-202 LESSON):** Story 2.6 has SIGNIFICANTLY stronger iteration-env runtime evidence than Story 2.5 for user-facing-CLI class stories — 3 end-to-end env-check branches (exit 3/0/2) + bash -n × 13 + pnpm run wiring count + dispatcher usage smokes all exercise real runtime behaviour. Carry-forward rule: for future user-facing-CLI class stories (Stories 2.7-2.12), exercise every branch that can execute without docker lifecycle mutation AND record each branch in the trace substrate_evidence list — this produces a tighter CR opener (fewer Blind Hunter "what does this actually do?" questions).
- **Within-epic § Cross-epic transition (iter-203 sm-verified → iter-204 CR opener):** Story 2.6 `sm-verified` + Epic 2 has 12/17 backlog stories → NO halt required. Next iter runs `/bmad-code-review (args: "2")` per lifecycle matrix row `sm-verified → fixes-pending | done`. Epic 2 stays `in-progress`.
