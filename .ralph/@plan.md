# Implementation Plan

## NOW

- [ ] Run `/bmad-testarch-atdd` — Story 2.3 red-phase scaffolds OR ATDD-skip per § Story Lifecycle Decision Matrix row `validated → atdd-scaffolded`. **Projected ATDD-skip (thirteenth cumulative precedent).** Story 2.3 is infrastructure-security class: dnsmasq daemon + nftables kernel rules + file-locked shell-script atomic-reload + JSONL log tailer — all outside Vitest/Playwright idiom; runtime verification (SC-7 `nft list chain` + AC 5 `curl`-unwhitelisted) is operator-workstation live-smoke (Task 12.1-12.8, backend-B deferred). ATDD-skip ground is **(c)** hybrid: (ii) downstream Story 2.5 integration-gate covers hardening-context AC re-verification, AND (iii) spec-declared adversarial coverage substitution — the 12 Dev-agent guardrails + CR envelope (6-9 iter fix-chain projected per iter-155 equation) substitute for red-phase scaffolds. Record rationale in IP § ATDD Decision + Story 2.3 Change Log v1.2 before transitioning. Budget: ~25K tokens (rationale authorship only, no scaffold files). Post-decision: `Story State = validated → atdd-scaffolded`; next iter = `/bmad-dev-story`.

## QUEUE (Story 2.3 lifecycle — mid-flight)

- [ ] _(post-Story-2.3-atdd-scaffolded)_ `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-3-egress-policy-dnsmasq-nftables-fail-closed-ipv4-ipv6-parity-atomic-reload.md")` — `atdd-scaffolded → in-dev`. Heavy iter expected: 11 new files + 6 modified files + invariant doc + manifest entry + sync-gate refresh. Budget likely ≥80K; may complete single-iter (Story 2.2 iter-148 landed 8-task 5-AC story single-pass) OR split across 2 iters with `in-dev (partial)` marker (Story 2.3 has larger surface — entrypoint surgery + bash-script-heavy daemons + runtime-verification deferral).
- [ ] _(post-Story-2.3-in-dev)_ `/bmad-testarch-trace (args: "yolo")` — AC→test coverage gate; WAIVED precedent likely HOLDS (thirteenth cumulative Epic-precedent under ATDD-skip-trace-WAIVED co-application rule).
- [ ] _(post-Story-2.3-traced)_ `/bmad-create-story (args: "review")` post-dev SM verification — `traced → sm-verified`.
- [ ] _(post-Story-2.3-sm-verified)_ `/bmad-code-review (args: "2")` — CR gate; forecast envelope per iter-155 lesson (carve-out × 3 + live-AC-coverage × 3 + impl-surface-LOC / 100): 0-carve-out + backend-B live-smoke defer (+3) + ~600 LOC impl (+6) → 6-9 iter fix-chain projected; likely LOOSER than Story 2.2's 4-iter + TIGHTER than Story 2.1's 16-iter.
- [ ] _(conditional, post-Story-2.17 done)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge. Epic 2 completion.
- [ ] _(conditional, post-Epic-2 merged)_ § Cross-epic transition → Epic 3 Story 3.1 OR halt EPIC_DONE awaiting merge.

## BLOCKED

_(none — Story 2.3 validated at iter-156 with 5 pre-dev SM PATCHes applied; Epic 2 at 2/17 stories done + 1/17 validated ready-for-dev; no carry-forward blockers from Story 2.2 CR closure.)_

## DONE (current story 2.3 lifecycle)

- [x] iter-156: **`/bmad-create-story (args: "review")` pre-dev SM validation landed.** Four parallel fresh-context Sonnet subagents independently re-analysed sources (epics/PRD / architecture / repo prior-art / previous-story intelligence); Ralph triage produced **5 PATCH + 1 DEFER + 6 DISMISS** against the v1.0 draft. **PATCHES applied (5):** (1) [HIGH] Task 10.3 manifest entry — removed non-schema `name` field (verified `InvariantSchema` at `packages/keel-invariants/src/invariants.manifest.ts:3-15` has only `id/description/sourcePath/contentHash/anchors`) + fixed `anchors` from H3 header `'### Devbox egress (Story 2.3)'` to backtick-ID `'INV-devbox-egress-contract'` (matches convention across all 19 existing entries) + switched `contentHash` value format from `sha256:<hex>` to bare 64-hex (schema regex `^[0-9a-f]{64}$`); Task 10.4 text clarified to distinguish category H3 heading from walker anchor target. (2) [MED] Dev Notes — new subsection "User-account timeline (Story 2.3 → Story 2.5 handoff)" pre-empts dev-agent confusion about root-at-entrypoint vs Story 2.5's `user: dev` + `cap_drop: [ALL]`; cap_add is pre-wiring not immediate enforcement. (3) [MED] Task 10.2 `docs/invariants/devbox-egress.md` structure pinned to `devbox-dind.md` precedent (H1 + prose metadata + H2 `## INV-devbox-egress-contract` anchor + no frontmatter) + **MUST embed SC-3 JSONL schema verbatim** in new `## JSONL query log schema` section so `contentHash` locks six-field contract against drift (Story 2.2 iter-151 AR-2 lesson: contract language scattered outside `sourcePath` doc drifts silently). (4) [LOW] Task 12.7 log-rotation smoke re-annotated backend-B operator-workstation deferred, forbidding committed threshold changes; matches Story 2.1 iter-144 DEFER-annotation precedent. (5) [LOW] Dev-agent Guardrail 11 extended from `.envrc` → `.envrc` + `.envrc.local` (Story 2.2 prek deny-list covers both; Story 2.3 MUST NOT slide into per-fork-override path, Story 2.4 scope). **DEFERS (1, not applied this gate):** zero-entries whitelist warning output path — dev-story may pin (polish-level, not spec-blocking). **DISMISSED (6):** Scope-boundary Epic 4 row stylistic asymmetry (external consumer — forward-looking phrasing acceptable); Guardrail 12 multi-doc ambiguity (intent clear); architecture silent on script decomposition (allowed invention); JSONL schema invention (architecture left it open); `docker-compose.yml:10` stale TODO (Task 9.1 replaces — intentional); entrypoint.sh line-ref approximate (dev-story pins exact lines). Subagent A (epics/PRD) returned ZERO patches — AC verbatim check PASS, user-story PASS, PRD FR1a/NFR6/Devbox-Implementation-Contract/FR37 all covered, no adjacent-story scope overlap. Story Status remains `ready-for-dev`; sprint-status row unchanged (pre-dev review does NOT flip sprint-status — only Change Log records the gate). Story file Change Log v1.1 appended. **RALPH.md Decisions (iter-156) pins two new guardrails:** (a) manifest schema is **five-field** (`id/description/sourcePath/contentHash/anchors` — NO `name`), `anchors` holds backtick-ID literals not H3 headers, `contentHash` is bare 64-hex (no `sha256:` prefix); dev-story Task 10.3 MUST cite `InvariantSchema` line 3-15 verbatim; (b) invariant doc bodies for multi-faceted contracts MUST embed the pinned sub-schemas verbatim (SC-3 JSONL schema into devbox-egress.md) so the `contentHash` gate locks the contract — Story 2.2 iter-151 AR-2 generalisation: the sync-gate only binds what's IN the `sourcePath` doc, not what's in external prose; prose-in-story-spec drifts silently. Budget: ~55K tokens used (four parallel subagents + synthesis + five surgical Edits + v1.1 Change Log entry + IP update + RALPH.md update). Commit captures story file v1.1 + RALPH.md Decisions + this IP update.
- [x] iter-155: **`/bmad-create-story` Story 2.3 exhaustive-context-engine draft landed.** Four parallel Sonnet subagents (epics+PRD + architecture + prev-story-intelligence + repo-prior-art) produced 5 ACs (verbatim from epics.md Story 2.3) + 17 scope-clarifications + 13 Tasks (~50 subtasks). Three iter-155 guardrails pinned in RALPH.md Decisions. 11 new files + 6 modified files. Backend-B live smokes deferred to operator workstation. JSONL schema pinned (6 stable fields) for Epic 4 FR37 consumer hard-reference. Atomic-reload pinned (flock + `nft -f` kernel-atomic + dnsmasq SIGHUP + release). One `.envrc.example` knob (`KEEL_DEVBOX_DNS_UPSTREAM=1.1.1.1`). Sprint-status flip `2-3-…: backlog → ready-for-dev`.

## Context

- **Phase:** 4-implementation — Epic 2 at 2/17 stories `done` (2.1 iter-144 + 2.2 iter-154) + 1/17 `validated` (2.3 iter-156; sprint-status = ready-for-dev).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.3 dev-story live smokes deferred to operator workstation (backend-B iteration-context bind-mount denial persists).
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Story 2.3 Task 1 appends `dnsmasq` + `nftables` apt packages → re-bake needed at dev-story close.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; 2.1 + 2.2 done; 2.3 validated ready-for-dev; 2.4..2.17 in `backlog`.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2; PR #230 transitions to Open at Story 2.17 completion).
- **Story:** 2.3 — egress-policy-dnsmasq-nftables-fail-closed-ipv4-ipv6-parity-atomic-reload.
- **Story File:** `_bmad-output/implementation-artifacts/2-3-egress-policy-dnsmasq-nftables-fail-closed-ipv4-ipv6-parity-atomic-reload.md` (v1.1 at iter-156; 5 PATCHes applied).
- **Story State:** `validated` — pre-dev SM verdict landed iter-156; next transition is `validated → atdd-scaffolded` via `/bmad-testarch-atdd`.
- **GitHub Issue:** Story 2.3 → #43; Epic 2 → #10.
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **DinD is fork-time substrate, not transitional.** `INV-devbox-dind-available` — every fork's cc-devbox-equivalent must provide Docker. Story 2.3's nftables/dnsmasq entrypoint-init + live smokes exercise the daemon path.
- **Backend B is the reference env at 2026-04-21.** Broad-state-mutation scripts MUST detect backend + refuse destructive ops by default (Story 2.1 `benchmark.sh` is the reference implementation).
- **NFR2 authority unchanged.** Cold ≤ 300s / warm ≤ 30s remains authoritative on M4-Pro native; DinD runs are indicative only.
- **Cumulative Epic-2 CR metrics (Story 2.1 + 2.2):** Story 2.1 = 9 patches across 3 gates (16-iter fix-chain); Story 2.2 = 2 patches across 1 gate (4-iter fix-chain). Story 2.3 forecast per iter-151/154 equation: 6-9 iter fix-chain likely (0-carve-out + backend-B live-smoke defer + ~600 LOC impl).
- **iter-156 SM pre-dev validation outcome:** 5 PATCH / 1 DEFER / 6 DISMISS against v1.0 draft; HIGH-severity finding = manifest schema drift (5-field, not 6) — sets precedent that dev-agent MUST cite `InvariantSchema` verbatim when authoring manifest entries (iter-156 RALPH.md Decisions entry).
- **Validated patterns at iter-156:** Four-parallel-Sonnet fresh-context SM validation (same fan-out pattern as iter-155 exhaustive-context-engine); adversarial triage with direct schema/repo-state verification (Ralph reads `InvariantSchema` + `INVARIANTS.md` to confirm factual claims before applying PATCH); one-doc-one-invariant rule extended: multi-faceted contract docs MUST embed pinned sub-schemas verbatim in the `sourcePath` body so `contentHash` gate covers the schema.
