# Implementation Plan

## NOW

- [ ] **Story 2.16 `/bmad-testarch-atdd` → atdd-scaffolded** (`validated → atdd-scaffolded`). Per § Story Lifecycle Decision Matrix row `validated`. **Forecast: SKIP-WITH-GROUNDS-(ii) single-ground waiver** ("no test runner at 1.0; Epic 13 scope owns CI test framework wiring; impl-time fixture smokes in Task 1 verification gates substitute as adversarial-coverage at Story 2.16 landing"). FIRST Epic-2 single-ground (ii) waiver in 25-precedent ATDD-skip chain (Stories 2.5/2.7-2.14/2.15 all carried (c) or (ii)+(iii) tuples). META guard watch: if skill or post-dev SM suggests grounds-(ii) alone insufficient, fall back to (ii)+(iii) per iter-297 precedent — AC 8 (live `claude` subprocess hook-firing + Epic 4 FR37 consumer integration) is operator-smoke-class + downstream-Epic-class, that's the (iii) anchor.

## QUEUE (Story 2.16 lifecycle + 2.17 substrate queue)

- [ ] _(after Story 2.16 atdd-scaffolded)_ Story 2.16 `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-16-claude-pretooluse-hooks-for-secret-file-denylist-ralph-compatible.md")` → in-dev / review. Tasks 1-8: Task 1 (`.claude/hooks/block-secret-access.sh` ~150-line bash + 5+ functional smokes; uses iter-298 NOVEL LESSON heredoc-bash workaround for `.claude/` write-class operations); Task 2 (`.claude/settings.json` `hooks.PreToolUse` block append — additive to Story 2.15 baseline); Task 3 (3-site lockstep: invariant doc + manifest 35th entry + INVARIANTS.md anchor + post-rebuild sync-gate); Task 4 (`.ralph/config.toml` NEW with `[hooks].self_protection_halt_threshold = 3`); Task 5 (AGENTS.md H3 ~80-120 lines, 9 bullets per Task 5 (a)-(i)); Task 6 (CLAUDE.md single bullet append); Task 7 (packages/devbox/README.md H2 ~40-60 lines); Task 8 (substrate-to-seed lockstep: 2 seed files + keel-templates README bullet append + AMEND existing settings.json bullet). Forecast XL-class iter (~80-120K tokens). META guard watch: may land partial with `in-dev (partial)` requiring follow-up per iter-268 Story 2.12 + iter-283 Story 2.13 recovery precedent.
- [ ] _(after Story 2.16 in-dev)_ Story 2.16 `/bmad-testarch-trace (args: "yolo")` → traced. Forecast WAIVED per Story 2.13 iter-284 + Story 2.14 iter-292 + Story 2.15 iter-299 precedent (substantive ACs covered by impl-time smokes + sync-gate; AC 8 Epic 4 consumer + AC 5 Epic 3 halt-write are downstream-Epic-class operator-smoke-deferred). Watch for novel grounds-class application — Story 2.16 is the FIRST Epic-2 story shipping executable bash logic (case-glob denylist matching) — PARTIAL-AC ground-(c) variant-(ii) iter-299 precedent may apply on AC 2 (unit signal = bash parse + impl-time fixture smoke; behavioural signal = live `claude` subprocess hook-firing operator-deferred).
- [ ] _(after Story 2.16 traced)_ Story 2.16 `/bmad-create-story (args: "review")` post-dev SM → sm-verified. Forecast band 0-3 PATCH per iter-270 + iter-300 narrow-after-SM-absorb precedent.
- [ ] _(after Story 2.16 sm-verified)_ Story 2.16 `/bmad-code-review (args: "2")` CR → done or fixes-pending. Forecast 1-4 PATCH per iter-301 NOVEL LESSON CANDIDATE (doc-heavy CR drift) + Story 2.16 wider doc-touch surface; explicit subagent-budget guidance per iter-301 LESSON: enumerate the 12 touched files in Blind Hunter prompt to keep token consumption < 100K per subagent.
- [ ] _(after Story 2.16 done)_ Story 2.17 full lifecycle — hook-settings bypass-resistance + Epic 2 SC-17 close-out polish pass. SC-17 reconciliation absorbs cumulative DEFER queue at iter-302 close: **58 cumulative Epic-2 DEFERs (47 carry-forward from Story 2.14 CR + 11 from Story 2.15 CR) + 3 SC-17 close-out candidates (D-7/D-8/D-9 from iter-299 trace)** = 61 DEFERs total + whatever Story 2.16 lifecycle accrues. Story 2.16 expected to MITIGATE 3 of 11 Story 2.15 CR DEFERs at hook layer (D-1/D-3/D-6) — final SC-17 absorption count narrows to ~58.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured; `statusCheckRollup: []` carried unchanged iter-219..303).

## BLOCKED

_(none — iter-303 push expected on first try per iter-302 orient baseline; iter-301 NOVEL LESSON CANDIDATE SSH :22 retry budget carry-forward remains ready if flake recurs.)_

## ATDD Red Phase

_(none at iter-303 landing. Story 2.16 ATDD-applicability pending `/bmad-testarch-atdd` invocation at iter-304; forecast SKIP-WITH-GROUNDS-(ii) single-ground per Dev Notes § ATDD-applicability predicate — if red-phase scaffolds land instead, they'll populate here at iter-304.)_

## DONE (iter-303 `/bmad-create-story (args: "review")` pre-dev SM — Story State `drafted → validated`)

- [x] iter-303: **STORY 2.16 `/bmad-create-story (args: "review")` pre-dev SM LANDING** (`drafted → validated`). Three-subagent fan-out (iter-281 pattern — extended to Story 2.16 wider-surface posture) produced **3 PATCH bundled close** (middle of iter-286 NOVEL LESSON 6-10 forecast band, on track for Epic-2 substantive-story cumulative lifecycle): PATCH-1 Change Log 0.1 row AC-count phrasing "7 ACs (verbatim ... + AC 8 added)" reconciled to "8 ACs (7 verbatim + AC 8 added)"; PATCH-2 Dev Notes § Fork-extension precedence line 401 label "(5-site lockstep update)" relabelled to "(7-site AMEND coordination — superset of the 5-site substrate↔seed byte-identity lockstep)" + enumerated the 5/2 partition (sites a+b+c+f+g = byte-identity; d+e = coordinated metadata) reconciling with the story's other 5-site references (AC 7 line 35, Task 3 line 228, Dev Notes § Previous story intelligence line 408); PATCH-3 Task 8c removed "Optionally amend the existing bullet" (contradicted its own next-sentence "Decision: amend") → made AMEND mandatory with explicit SC-15 exception justification for evolving consumer-contract descriptions. Change Log 0.2 row appended. Story Status HTML comment updated for iter-303 landing (previous iter-302 text pruned). Technical-correctness subagent ZERO PATCH (file-path refs + line refs + manifest count 34 + Dockerfile pins :36-37/:48/:121 + FR/NFR refs NFR33a/FR14k/FR37 all verified clean). Previous-story-intelligence + Story 2.15 cross-refs CLEAN. Commit candidate: `docs(story-2-16): iter-303 /bmad-create-story (args: "review") pre-dev SM — drafted → validated; 3 PATCH bundled close`.

_(iter-302 `/bmad-create-story` LANDING detail pruned per Guardrail 2; retained in commit `d9e031b` + story-file Status HTML comment chain + story-file Change Log 0.1 row.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **15/17 stories done** (2.1-2.15) + 2.16 `ready-for-dev` (validated at iter-303) + 2.17 backlog.
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live smokes operator-workstation-deferred.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:121; bash + jq baked at :36-37 + :48 — load-bearing for Story 2.16 hook script). Host-side shim count: **18** at iter-303 (unchanged — review iter; no devbox-shim substrate code edits).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **15 done** (2.1-2.15); **1 ready-for-dev/validated** (2.16); **1 backlog** (2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.16 — Claude PreToolUse hooks for secret-file denylist (Ralph-compatible). **VALIDATED at iter-303.**
- **Story File:** `_bmad-output/implementation-artifacts/2-16-claude-pretooluse-hooks-for-secret-file-denylist-ralph-compatible.md`
- **Story State:** `validated` (sprint-status row `2-16-…: ready-for-dev` unchanged — "validated" is Ralph-internal). Next iter (iter-304) picks up `validated → atdd-scaffolded` per § Story Lifecycle Decision Matrix row `validated → /bmad-testarch-atdd`.
- **GitHub Issue:** Story 2.16 issue unknown; `RALPH_ISSUE_NUMBER` unset (no GitHub Project configured). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` unchanged iter-272..303).

## Notes

- **iter-303 PRESERVED carry-forward — iter-302 NOVEL observation candidate (Story 2.16 is the FIRST Epic-2 story shipping executable bash logic):** Stories 2.1-2.13 ship devbox-runtime substrate (bash scripts that the OS executes; hook-script tier is in-Claude-session NOT OS-level); Story 2.14 ships pure-policy doc + branch state; Story 2.15 ships pure-config JSON. Story 2.16's `.claude/hooks/block-secret-access.sh` is bash logic exercised inside a Claude Code subprocess via the PreToolUse hook protocol — a NEW class of substrate (in-session-runtime). Implications at iter-303 pre-dev SM: spec-fidelity + technical-correctness subagents exercised normally (no novel pre-dev SM surface); the distinctive bash-script class surfaces at dev-story (Task 1 bash authoring) + CR (bash-script-correctness vs glob-pattern-completeness findings). Track Stories 2.16 + 2.17 to confirm/falsify the in-session-runtime substrate-class differentiation.
- **iter-303 PRESERVED carry-forward — iter-301 NOVEL LESSON CANDIDATE (doc-heavy narrow-diff CR drift):** Story 2.15 cumulative lifecycle PATCH = 9 (5 pre-dev SM + 4 CR) — exceeded iter-286 NOVEL LESSON 3-6 forecast band upper edge by +3. Pattern: pre-dev SM absorbs substantive surface findings (AC/Task alignment, line-range assertions); prose-level drift emerges ONLY at CR when all doc-landings are in place + cross-refs empirically testable. Story 2.16 iter-303 pre-dev SM landed 3 PATCH (middle of 2-6 band) — tracks the pre-dev SM side of the pattern; CR observation still pending at iter-30X. Story 2.16 has WIDER doc surface than Story 2.15 (4 docs + 2 JSON + 1 bash + 1 TOML + 1 invariant doc vs Story 2.15's 4 docs + 2 JSON files); cumulative 6-12 PATCH forecast stands if pattern generalises.
- **iter-303 PRESERVED carry-forward — iter-301 NOVEL LESSON CANDIDATE (SSH :22 egress retry budget extension):** iter-301 required FOUR retries with exponential pacing for `git push` under DinD backend B SSH :22 flake; iter-302 + iter-303 orient `gh pr view 230` succeeded first try (no flake). Carry-forward posture: budget 4-5 retries with exponential pacing for `git push` under DinD backend B; do NOT escalate to halt after three. BLOCKED-entry-then-clear pattern (iter-299 invented, iter-301 reproduced) is the canonical pattern. Promotion to § Lessons after one more confirming data point (iter-303 push outcome TBD at commit time).
- **iter-303 PRESERVED carry-forward — iter-300 NOVEL pattern observation (three-consecutive-ZERO-PATCH chain post-pre-dev-SM) — PARTIALLY CONFIRMED + PARTIALLY FALSIFIED:** chain held through lifecycle INTERNAL stages (dev-story, trace, post-dev SM) but CR broke it at iter-301. Refinement: post-pre-dev-SM lifecycle internal gates tend ZERO-PATCH when SM absorbed surface, but CR remains a separate drift-accrual class. Story 2.16 iter-303 pre-dev SM closed with 3 PATCH — non-ZERO at pre-dev SM but bounded; track Story 2.16 lifecycle-internal + CR for pattern refinement.
- **iter-303 PRESERVED carry-forward — iter-299 NOVEL GROUNDS VARIANT (PARTIAL-AC ground-(c) variant-(ii) on behavioural signal only):** Story 2.16 hook landing has STRUCTURAL PARITY (hook script presence + JSON-shape settings.json registration = unit signal; live hook firing on denied tool call inside a `claude` subprocess = behavioural signal). Track for Story 2.16 trace iter (iter-30X) whether the variant reproduces; if yes, promote to § Lessons.
- **iter-303 PRESERVED carry-forward — iter-298 NOVEL LESSON (permission-guard workaround for `.claude/` write-class operations):** sandbox permission guard treats `.claude/` substring as sensitive-path; three workaround patterns (heredoc-bash for new-file authoring; `cp -r` for directory creation via side-effect; `find -mindepth 1 -delete` for computed-filter deletion) apply to Story 2.16 Task 1 (new `.claude/hooks/block-secret-access.sh`) + Task 2 (`.claude/settings.json` modification — Edit tool may suffice but heredoc fallback ready) + Task 8a (seed copy under `packages/keel-templates/src/seeds/.claude/hooks/`). MUST compose patterns up-front rather than discovering ad-hoc mid-iter.
- **iter-303 PRESERVED carry-forward — iter-301 subagent-budget LESSON for Story 2.16 CR:** Blind Hunter spent ~150K tokens + 58 tool_uses exploring the repo at Story 2.15 CR — exceeded budget; should have been scoped tighter to diff-only reads of the touched files. Carry-forward for Story 2.16 CR: explicitly list the 12 touched files in the Blind Hunter prompt; enumerate touched files upfront rather than relying on agent to discover via `git diff --stat`.
- **iter-303 NOVEL observation — three-subagent fan-out sub-prompt discipline held at pre-dev SM:** iter-281 pattern extended to Story 2.16's wider-surface posture. Each subagent scoped tightly (spec-fidelity → epics.md/prd.md/architecture.md source comparison ONLY; technical-correctness → file-path + line refs + manifest count + Dockerfile pins + FR/NFR refs ONLY; lockstep/previous-story → 5-site enumeration consistency + Story 2.15 D-1/D-3/D-6 cross-refs ONLY). Token budget per subagent stayed bounded (<50K estimated per the "under N words" prompt discipline). Consolidated 3 PATCH total — on-band for pre-dev SM (iter-286 NOVEL LESSON 2-6 pre-dev SM sub-band from the 6-10 cumulative band). No META guard fired.
- **iter-303 NOVEL observation — first manifest-adding story since iter-291 (Story 2.14) carry-forward confirmed:** Stories 2.15 + 2.16 + 2.17 form a manifest-cadence cluster — 2.15 added 0 entries (deliberately deferred to 2.16); 2.16 adds 1 entry (35th `INV-claude-hook-secret-denylist` at dev-story landing); 2.17 expands the entry with content-hash anchor-delimited region for `.claude/settings.json` `hooks` block. Manifest count history: Stories 1.1-1.16 added 24; Stories 2.1-2.13 added 9 (cumulative 33); Story 2.14 added 1 (34); Stories 2.15-2.16 net +1 (Story 2.15 0; Story 2.16 1; cumulative 35); Story 2.17 net 0 (expansion of existing entry, no new ID). Per iter-257 LESSON re manifest compile step load-bearing, Story 2.16 dev-story Task 3 sequence (author invariant doc → sha256 → paste contentHash → rebuild → sync-gate → re-iterate if mismatch) is the canonical recovery path; reusing iter-283 + iter-291 recovery pattern.
- **Cumulative DEFER forecast for Stories 2.16..2.17 Epic-2 close-out:** 58 cumulative Epic-2 DEFERs pending at iter-301 Story 2.15 CR close (47 carry-forward from Story 2.14 CR + 11 from Story 2.15 CR) + 3 SC-17 close-out candidates (D-7/D-8/D-9 from iter-299 trace) = 61. Story 2.16 forecast: -3 absorbed at hook layer (D-1/D-3/D-6 mitigated via recursive globs + substring matches) + 5-10 new DEFERs across dev-story + post-dev SM + CR (substantive story; bash-script + manifest add + 5-site lockstep). Story 2.17 scope-inherent closes most + whatever Story 2.16 adds. Final SC-17 absorption at Story 2.17 CR close ~ 60-65 absorbed.
