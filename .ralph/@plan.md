# Implementation Plan

## NOW

- [ ] **Run `/bmad-testarch-trace (args: "yolo")`** per § Story Lifecycle `in-dev → traced`. 8 ACs × 13 host-side scripts (most ACs are CLI-smoke-class `pnpm devbox:<verb>` exit-code + message assertions, operator-workstation-deferred recipes live in README § Host-side CLI § Verification). Forecast: WAIVED or minimal coverage matrix — Story 2.4 iter-175 + Story 2.5 iter-188 precedent (trace-waive pattern for user-facing-CLI-class stories with no live test runner at 1.0). Fresh context; fresh iteration. ~small.

## QUEUE (Story 2.6 lifecycle → Epic 2 Story 2.7..2.17 → Epic 2 close-out)

- [ ] _(post-`traced`)_ `/bmad-create-story (args: "review")` — post-dev SM verification per § Story Lifecycle row `traced` (same skill as pre-dev validation; state governs intent).
- [ ] _(post-`sm-verified`)_ `/bmad-code-review (args: "2")` — CR opener per § Story Lifecycle row `sm-verified`. Forecast: 4-6 PATCH opener (user-facing-CLI class, matches Story 2.4 5-PATCH envelope; Story 2.6 is broader surface but simpler per-script so ~4-6 is reasonable).
- [ ] _(post-CR-drain; QUEUE empty)_ `/bmad-code-review (args: "2")` — CR-closure gate (expected ZERO-PATCH per Epic-2 3-story precedent: 2.3 iter-171, 2.4 iter-183, 2.5 iter-197).
- [ ] _(post-Story-2.6-done; through Story 2.17-done)_ Iterate Story 2.7..2.17 through the full lifecycle — 11 remaining substrate stories spanning ralph-auto-start + claude OAuth + gh OAuth + prereq-check + shared mode + port-publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-22 per iter-197..iter-200 pre-flight — "no checks reported on the 'feat/epic-2-packaged-devbox' branch").

## BLOCKED

_(none)_

## ATDD Red Phase

_(none — Story 2.6 ATDD-skipped at iter-200 per § Story Lifecycle `validated → atdd-scaffolded` (skip branch). 16th-cumulative FR14n ATDD-skip precedent. Grounds (c)+(ii)+(iii) recorded in iter-200 IP + RALPH.md signpost. No red-phase failures to drain at iter-201 dev-story landing — Task 8 iteration-env-safe smokes + Task 9 operator-workstation recipes are shipped as implementation per Story 2.4 iter-174 + Story 2.5 iter-187 precedent.)_

## DONE (Story 2.6 lifecycle — keep current story chain, prune after Story 2.7 starts)

- [x] iter-201: **`/bmad-dev-story` — Story 2.6 host-side `pnpm devbox:*` CLI surface LANDED.** 13-script + 13-pnpm-entry + 4-docs-surface single-iter landing matching Story 2.4 iter-174 + Story 2.5 iter-187 user-facing-CLI-class precedent. **Scripts:** `build.sh`, `rebuild.sh`, `start.sh`, `stop.sh`, `restart.sh`, `clean.sh`, `shell.sh`, `attach.sh`, `status.sh`, `logs.sh`, `monitor-host.sh`, `whitelist-host.sh`, `env-check.sh` (each 0755; `set -euo pipefail`; self-rooted paths; banner + SC-5 exit-code table). **Uniform exit codes:** `0/2/3/4/5-7/8/9/10/11/124` per SC-5. **`start.sh` healthcheck poll:** `docker inspect` every 2s up to `KEEL_DEVBOX_START_HEALTHCHECK_TIMEOUT_S:-120`s; accepts `healthy|running|starting(<30s grace)`; rejects `unhealthy|exited|dead|removing|paused` immediately. **`clean.sh` three-tier:** default scoped `down --rmi local` (safe under either backend); `--with-volumes` prompts `[y/N]` + under backend B additionally requires `--force-backend-b`; `--allow-broad-prune` RESERVED no-op. Backend-B detection mirrors `benchmark.sh § detect_backend`. **`env-check.sh` (AC 8 + AR-11):** 15 required vars (active `.envrc.example` knobs); 3 positive-int-shape-validated knobs (`TMPFS_{TMP,VARTMP,LOGS}_MB`). Names-only stderr for missing vars. `start.sh` calls `env-check.sh` as pre-flight unless `KEEL_DEVBOX_START_SKIP_ENV_CHECK=true`. **Monitor semantic pin (SC-15 AI-2 carry-forward):** `pnpm devbox:monitor` = FR1a JSONL DNS-event tail per PRD `:494` + architecture `:1003`; NOT `docker stats`. **Scope-owner for DEFERs:** AR-10 operator-migration docs LANDED (SC-21, new README H3), AR-11 envvar shape validation LANDED (SC-14, unconditional), AR-12 COMPOSE_PROJECT_NAME docs polish LANDED (SC-22, unconditional). AR-7 `/run` relocation + AR-9 `/etc/*` chown NOT triggered — deferred pending operator-workstation smoke outcome per SC-19 + SC-20 conditional gate. **Sync-gate 2× fired (SC-17):** `INV-prek-prepare-lifecycle` contentHash `87f37b45…` → `5960e7c4…` (package.json +13 devbox:* entries); `INV-devbox-homedev-named-volume` contentHash `f34cb62f…` → `5e868749…` (AR-12 qualifier in hardening doc). Both manifest-update + `dist/check.js` rebuild + `keel-invariants:check-all` exit 0. **Docs landing:** packages/devbox/README.md = new H3 § Operator migration (pre-Story-2.5 named-volume recovery) + AR-12 qualifier at § Verification + new H2 § Host-side CLI (Story 2.6); AGENTS.md = new H3 § Host-side CLI (Story 2.6) under § Devbox iteration environment. **Quality gates:** format:check ✓ (1 prettier auto-fix to README after manual draft); lint ✓ (16 tasks); typecheck ✓ (16 tasks); keel-invariants:check-all ✓; `docker compose config --quiet` ✓. **Iteration-env-safe smokes:** bash -n × 13 (all OK); pnpm run count = 13; env-check no-.envrc → exit 3; env-check seeded-.envrc → exit 0 + "15 of 15 required vars present"; env-check bad-tmpfs-ints → exit 2 + 2 shape violations named; clean `--unknown` → exit 2 + usage; clean `--help` → exit 0 + usage. **Operator-workstation deferrals:** AC 2-7 live lifecycle smokes (build/start/status/shell/logs/stop/restart/clean) shipped as copy-paste recipes in README § Host-side CLI § Verification. Sprint-status `ready-for-dev → review` (single-iter combined flip per Story 2.4/2.5 precedent). Story State `atdd-scaffolded → in-dev` (IP lifecycle). Story file Status `ready-for-dev → review` (BMad-skill-internal). Budget consumed: ~95-105K (orient ~12K + 13 script writes ~28K + package.json edit ~2K + README edits ~20K + invariant doc edit + manifest hash refresh + rebuild ~8K + AGENTS.md + sprint-status + story-file Dev Agent Record + Change Log ~20K + smokes + quality gates ~10K). Within 117K envelope; 10K headroom for commit + push.
- [x] iter-200: **`/bmad-testarch-atdd` → ATDD-skip (FR14n Story State `validated → atdd-scaffolded`).** 16th-cumulative FR14n ATDD-skip precedent (Stories 1.7..1.16 = 10 Epic-1 skips; 2.1 iter-98 = 11; 2.2 iter-147 = 12; 2.3 iter-157 = 13; 2.4 iter-174 lifecycle-bypass dev-advance = 14; 2.5 iter-186 = 15; 2.6 iter-200 = 16). **Grounds (c)+(ii)+(iii):** CLI-smoke-class ACs + no live test runner + ATDD would duplicate Task 8 + Task 9 recipes shipped as implementation.
- [x] iter-199: **Ran `/bmad-create-story (args: "review")` — pre-dev SM validation of Story 2.6.** 8 findings applied as v0.2 Change Log entry (AI-1..AI-8): AI-1 CRITICAL SC-2 monitor-naming self-contradiction resolved; AI-2 CRITICAL PRD `:494` vs epics AC 7 semantic drift reconciled; AI-3 ENHANCEMENT Task 10.6 split; AI-4..AI-8 OPTIMIZATION. Story State `drafted → validated`.

## Context

- **Phase:** 4-implementation — Epic 2 at 5/17 stories `done` (2.1 + 2.2 + 2.3 + 2.4 + 2.5) + 12/17 remaining; Story 2.6 in-dev (review) + 11 backlog (2.7..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.6 live lifecycle smokes operator-workstation-deferred per Story 2.4 SC-17 + Story 2.5 iter-187 precedent. 5-gate quality chain for docs-surface iters: sync-gate + typecheck + lint + format:check + docker compose parse-smoke.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Story 2.5 substrate live. Story 2.6 is a host-side CLI layer — no Dockerfile changes at iter-201 (SC-25 scope-boundary forbids; AR-9 escalation path conditional + not triggered).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; 5 done (2.1-2.5); Story 2.6 in-dev (review); 11 backlog (2.7..2.17). Epic 2 closes at Story 2.17 (PR #230 Draft→Open transition at that point).
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.6 — host-side `pnpm devbox:*` CLI surface. Scope-owner for AR-7 `/run` relocation (SC-19, conditional — NOT triggered at iter-201) + AR-9 `/etc/*` chown (SC-20, conditional — NOT triggered at iter-201) + AR-10 pre-Story-2.5 volume migration docs (SC-21, LANDED iter-201) + AR-11 envvar shape validation (SC-14, LANDED iter-201) + AR-12 COMPOSE_PROJECT_NAME docs polish (SC-22, LANDED iter-201).
- **Story File:** `_bmad-output/implementation-artifacts/2-6-host-side-pnpm-devbox-cli-surface.md`
- **Story State:** `in-dev` — transitioned at iter-201 via `/bmad-dev-story` single-iter landing. Story file Status = `review` (BMad-skill-internal); IP lifecycle Story State tracks the coverage/requirements/quality gate chain separately. iter-202 runs `/bmad-testarch-trace (args: "yolo")` to transition `in-dev → traced` (forecast: WAIVED or minimal coverage matrix per Story 2.4/2.5 precedent).
- **GitHub Issue:** Story 2.6 TBD (will be surfaced by ralph.py at next iter if the GH project is configured). Epic 2 → #10 (Ralph closes on EPIC_DONE halt).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **Epic-2 story-class prediction validated (iter-198 + iter-199 + iter-201):** Story 2.6 is user-facing-CLI class. Delivered at 13 verbs × ~20-50 LOC per script + 15 required envvar validations + 4 docs surfaces. CR-opener forecast unchanged from iter-198: 4-6 PATCH. Pre-dev SM review outcome confirmed: 2 CRITICAL + 1 ENHANCEMENT + 5 OPTIMIZATION (AI-1..AI-8) applied at iter-199 before dev-story landing.
- **Single-iter landing precedent — user-facing-CLI class (iter-201 formalises):** Story 2.4 iter-174 (4-verb whitelist CLI, 1 script) + Story 2.5 iter-187 (hardening substrate, 2 files) + Story 2.6 iter-201 (13-verb CLI + 4 docs surfaces, 19 files) are all single-iter landings. Larger Story-2.6 surface did NOT require multi-iter splitting because the per-script complexity stayed low (most scripts are <50 LOC wrappers). Epic-3+ user-facing-CLI stories (Epic 8 jobs CLI, Epic 11 locale CLI, Epic 14 research harness) can forecast similarly tight single-iter landings until per-script complexity creeps. Carry-forward budget benchmark: ~95-105K per single-iter landing of this class.
- **Cross-source semantic-drift audit rule (iter-199 LESSON, iter-201 VALIDATED):** SC-15 pinned PRD `:494` as authoritative for `pnpm devbox:monitor` semantic. iter-201 `monitor-host.sh` implementation shipped as JSONL tail (not `docker stats`) per the pin. Carry-forward rule for Stories 2.7 + 2.8 + 2.9 + 2.12 + 2.15 remains live.
- **`<verb>-host.sh` host-side shim naming convention (iter-199 LESSON, iter-201 EXTENDED):** Story 2.6 ships `monitor-host.sh` + `whitelist-host.sh` as the canonical host-side shim naming pattern. Stories 2.7 (Ralph TUI wrapper), 2.8 (claude wrapper?), 2.9 (gh wrapper?), 2.12 (ssh wrapper?) candidates for the same pattern when in-container primitives exist.
- **Sync-gate 2× refresh iter-201 LESSON:** when a story edits BOTH a non-invariant file AND an invariant-sourcePath file, two hashes drift. The `pnpm keel-invariants:check` drift report enumerates all; update each hash in the manifest; single `pnpm --filter @keel/keel-invariants build`; `keel-invariants:check-all` exit 0. Carry-forward: don't assume one-hash-per-iter — scan the full drift report.
- **`bash -n` as iteration-env-safe syntax smoke (iter-201 formalises):** every script added by a Story 2.6-class CLI-family story can be smoked via `for f in <scripts>; do bash -n "$f"; done` to catch syntax errors before push. Zero side effects; works under any backend. Add this to the iteration-env-safe smokes list in every future CLI-family story's ATDD-skip grounds.
- **Story 2.6 invariant-sourcePath stability after sync-gate (iter-201):** `INV-devbox-homedev-named-volume` now at `5e868749…` (v1.9 Story 2.6 value, superseding iter-196 v1.8 `f34cb62f…`). `INV-prek-prepare-lifecycle` now at `5960e7c4…` (v? Story 2.6 value, superseding `87f37b45…`). Both values carry into Stories 2.7+ unless those stories edit the same sourcePaths (package.json edits in Stories 2.7-2.13 are likely; hardening doc edits less likely unless AR-7/AR-9 fire).
- **Sprint-status single-iter combined flip precedent (iter-201 confirmed):** `ready-for-dev → review` in one dev-story iter, elides the `in-progress` intermediate state. Matches Story 2.4 iter-174 + Story 2.5 iter-187. Multi-iter landings (e.g., if Story 2.7's Ralph-wrapper needs >1 dev-story iter) will flip `ready-for-dev → in-progress` at first dev iter, stay at `in-progress` through `in-dev (partial)` re-queues, then flip to `review` at the final dev iter.
- **Within-epic § Cross-epic transition (iter-201 dev-story → iter-202 trace):** Story 2.6 `in-dev` + Epic 2 has 12/17 backlog stories (2.6 at `in-dev (review)`, 2.7..2.17 remain backlog) → NO halt required. Next iter runs `/bmad-testarch-trace` per lifecycle matrix row `in-dev → traced`. Epic 2 stays `in-progress`.
