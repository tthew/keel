# Implementation Plan

## NOW

- [ ] `Run /bmad-create-story (args: "review")` post-dev SM requirements-satisfaction verification for Story 2.11. Per § Story Lifecycle Decision Matrix row `traced → sm-verified | sm-fixes-pending`. Two-subagent verification pattern per iter-235 LESSON (AC-satisfaction verifier + substrate-citation drift detector in parallel). Forecast: 1-3 drift findings per iter-250 carry-forward (deferred per SC-17 Epic 2 close-out reconciliation) + ZERO-PATCH AC-satisfaction candidate if iter-255 pre-dev SM v1.1 absorption holds under the 4-AC dual-mode-resolver surface (novel SIXTH one-pass-ZERO-PATCH-pre-dev-then-post-dev-SM candidate — Stories 2.6-2.10 set the 5-story baseline; Story 2.11's surface is narrower than 2.10's four-novel-vector but wider than 2.8/2.9's single-vector; band 0-3 drift, 0-1 substantive mis-satisfaction).

## QUEUE (Story 2.11..2.17 → Epic 2 close-out)

- [ ] _(after Story 2.11 `sm-verified`)_ `Run /bmad-code-review (args: "2")` adversarial CR via three-layer Ralph-hosted triage (Blind Hunter `bmad-agent-architect` diff-only + Edge Case Hunter `bmad-tea` diff+project-read + Acceptance Auditor `bmad-agent-dev` diff+spec+INV). Story State `sm-verified → done` OR `fixes-pending`. Chain-reset forecast: Stories 2.11..2.17 continue fresh ZERO-PATCH-CR forecast cycle per iter-253 LESSON (0-1 PATCH + 2-4 second-class DEFERs band-aligned). Story 2.11's novel surface = resolver-function extension + compose parameterisation + env-check orphan warning = THREE novel vectors (moderate); forecast 1 first-class PATCH + 2-3 second-class operator-edge DEFERs (Docker Desktop wake-race, Podman-compat semantics, shared-mode bind-scope security extension edge cases).
- [ ] _(after Story 2.11 `done`)_ Iterate Stories 2.12..2.17 through full lifecycle — 6 remaining substrate stories spanning loopback-bound port publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-24 per iter-219..258 repeated pre-push confirmation — `gh pr view 230` returns `statusCheckRollup: []`).

## BLOCKED

- [ ] iter-258 step-5 push — NEEDS RETRY. Prior iter-257 push FAILED with SSH :22 timeout (RC=124); 4 unpushed commits accumulated (iter-256 × 2 + iter-257-dev-story-landing + iter-257-post-push-fail BLOCKED-state update). iter-258 orient probe `timeout 15 git ls-remote origin HEAD` → RC=124 — SSH :22 still unresponsive at iter-258 orient. Expecting 5 unpushed commits after iter-258 trace-gate commit lands.
  - Attempted: orient probe `git ls-remote origin HEAD` 15s timeout → RC=124 (iter-258 orient).
  - Error/Issue: SSH :22 egress to github.com `140.82.121.{3,4,5,6}:22` intermittently timed out across iter-219..258; iter-255 saw a brief recovery window; iter-256..258 re-blocked. HTTPS :443 correlates (iter-256 `gh pr checks 230` also timed out). Suggests upstream network-path issue from iter-env to github.com ASN rather than port-specific egress block. PR #230 has no CI configured so HTTPS timeout is not a CI gate.
  - Next: iter-258 step-5 retries SSH :22 push with all 5 unpushed commits. If still timing out, 5 commits carry forward; next iter retries per iter-249..257 carry-forward recovery-banking pattern. Commits are safe in local branch — no force-push, no amend, preserved intact. When SSH :22 recovers, `git push` flushes all unpushed commits in a single round-trip with no conflict.

## ATDD Red Phase

_(none — Story 2.11 ATDD-skipped at iter-256 per grounds (c)+(ii)+(iii). Dev-agent impl at iter-257 landed resolver smokes + bash syntax-checks + `pnpm keel-invariants:check` pass as the functional substitute; these are logged in Story 2.11 Dev Agent Record § Debug Log References. Trace gate WAIVED at iter-258 per 22nd-cumulative ATDD-skip-trace-WAIVED pairing.)_

## DONE (iter-258 Story 2.11 `/bmad-testarch-trace` — Story State `in-dev → traced`)

- [x] iter-258: **STORY 2.11 `/bmad-testarch-trace (args: "yolo")` LANDING — Gate WAIVED; Story State `in-dev → traced`; TWENTY-FIRST cumulative trace-WAIVED precedent + TWENTY-SECOND ATDD-skip-trace-WAIVED pairing overall.** Four trace artefacts authored under `_bmad-output/test-artifacts/traceability/`: (1) `2-11-per-fork-vs-shared-devbox-mode-keel-devbox-shared.md` — authoritative Phase 1 + Phase 2 report (~390 LOC; mirrors Story 2.10 iter-249 template ~80% structural inheritance; mechanical adaptation to 2.11's 4-AC dual-mode-resolver class); (2) `2-11-coverage-matrix.json` — Phase 1 temp-file-style requirements mapping (4 requirements × NONE coverage × P2 priority × ground-(a)+(b)+(c) substrate-verification notes; recommendations enumerate WAIVED rationale + 30th-manifest-entry landing narrative + live-smoke operator-workstation-deferral posture); (3) `2-11-e2e-trace-summary.json` — machine-readable CI/CD companion (schema_version 1; source_sha `08527ba…`; gate_status `WAIVED`; gate_criteria p0/p1 MET + overall NOT_MET overridden by WAIVER); (4) `2-11-gate-decision.json` — slim gate-signal artefact (p0_status MET + p1_status MET + overall_status NOT_MET + critical_gaps 0 + full rationale). Sprint-status row UNCHANGED this iter (trace gate does NOT flip sprint-row per § Story Lifecycle — only pre-dev `drafted`, post-dev `review`, and post-CR `done` transitions flip sprint rows). Budget consumed iter-258: ~35K tokens (orient ~17K + skill dispatch + template-inheritance from Story 2.10 precedent + 4 artefact writes + IP update + RALPH.md signpost + commit ~18K). Well inside ~117K execution budget.

_(iter-253..257 Story 2.10 closure + Story 2.11 create-story / pre-dev SM / ATDD-skip / dev-story iters pruned per Guardrail 2 — see Story 2.11 § Change Log v1.0/v1.1/v1.2 + RALPH.md Signposts iter-253..257 for per-gate detail.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **10/17 stories done** (2.1-2.10) + 1/17 in review (2.11 at state `traced`) + 6/17 backlog (2.12..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered at iter-248 9/9 stub-docker harness + iter-252 9/9 mock-docker PATH-shim harness + iter-257 6/6 resolver functional smokes.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:119). Host-side shim count: **18** at iter-258 (unchanged — Story 2.11 extends the 18 existing shims with resolver invocation; does NOT add new shims).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **10 done** (2.1-2.10); 1 in-review at state `traced` (2.11); 6 backlog (2.12..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.11 — Per-fork vs shared devbox mode (`KEEL_DEVBOX_SHARED`)
- **Story File:** `_bmad-output/implementation-artifacts/2-11-per-fork-vs-shared-devbox-mode-keel-devbox-shared.md`
- **Story State:** `traced` — Story 2.11 `/bmad-testarch-trace (args: "yolo")` landed at iter-258; Gate WAIVED per 21st-cumulative-precedent. Next iter `/bmad-create-story (args: "review")` transitions `traced → sm-verified` (or `sm-fixes-pending`).
- **GitHub Issue:** Story 2.11 likely unwired (`RALPH_ISSUE_NUMBER` unset at iter-258 orient; skipping `Refs #<story-issue>` in commits per § Issue Tracking guard). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out, NOT iter-258).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — last confirmed `{"isDraft":true,"mergeStateStatus":"CLEAN","state":"OPEN","statusCheckRollup":[]}` at iter-254 orient; PR state unchanged across iter-255..258 (no merge events expected pre-Epic-2-close). iter-258 push status: SSH :22 still intermittent (probed at orient with `git ls-remote origin HEAD` 15s timeout → RC=124); `gh pr checks 230` confirms no CI configured (`statusCheckRollup: []`) so HTTPS-timeout is not a CI gate.

## Notes

- **iter-258 Story 2.11 `/bmad-testarch-trace` LANDING — `traced` after single-iter WAIVED gate.** Pattern extends Story 2.10 iter-249 twentieth-precedent template; structural artefact inheritance ~80% (Story 2.10's 4-artefact layout + rationale shape + recommendation structure directly translated; ground-(a)+(b)+(c) conjunction language preserved verbatim with mechanical swaps for 2.11's 4-AC dual-mode-resolver class). TWENTY-FIRST cumulative trace-WAIVED precedent + TWENTY-SECOND ATDD-skip-trace-WAIVED pairing overall — pattern holds with one-iter-per-story cadence since Epic 2 opened.
- **iter-258 NOVEL LESSON — partial-waive consideration at trace gate requires mechanical coverage-vocabulary distinguishing "substrate-smoke" from "automated regression".** IP § NOW pre-declared partial-waive as alternative (ACs 1-3 smoke-covered at iter-257 Dev Agent Record § Debug Log; AC 4 doc-only per § Testing Standards). Elected full-waive per 22nd-cumulative-pairing consistency rule. The trace-matrix coverage vocabulary at 1.0 (`FULL`/`PARTIAL`/`NONE`/`UNIT-ONLY`/`INTEGRATION-ONLY`) does not distinguish "substrate-smoke documented in Dev Agent Record" from "automated regression in committed test file"; both map to `NONE` under strict `coverage_basis=acceptance_criteria`. Future-Ralph rule: when considering partial-waive at trace gate with ACs split between "substrate-smoke-covered" and "doc-only", prefer full-waive for consistency with N-th-cumulative-precedent pattern UNLESS the story file or architecture spec introduces a dedicated coverage-class vocabulary (e.g., "substrate_verified" status). Partial-waive requires vocabulary not present at 1.0. Carry-forward applies to Stories 2.12..2.17 + Epic 13 pre-framework stories; post-Epic-13 partial-waive becomes mechanically tractable once `packages/devbox/tests/` lands.
- **iter-257 NOVEL LESSON carry-forward — manifest rebuild required after adding new InvariantSchema entry.** `pnpm keel-invariants:check` runs against compiled `dist/check.js`; after editing `src/invariants.manifest.ts` MUST invoke `pnpm --filter @keel/keel-invariants build` before sync-gate reflects the change. First check run at iter-257 returned `removed-from-docs-only INV-devbox-mode` drift against stale dist; rebuild → clean (exit 0). Pattern carry-forward for Stories 2.12..2.17 + any future story adding a manifest entry: ALWAYS rebuild before verifying sync-gate. Promoted to RALPH.md § Lessons at iter-258 per Ralph upkeep discipline.
- **iter-257 NOVEL LESSON carry-forward — shared-mode operator-override INTENTIONAL ignore (SC-4) is load-bearing.** Tested path: `KEEL_DEVBOX_SHARED=true KEEL_DEVBOX_CONTAINER_NAME=my-fork-devbox` + resolver call → `KEEL_DEVBOX_CONTAINER_NAME_RESOLVED=keel-devbox-shared` (override DISCARDED). Without this enforcement, AC 2's "fork A + fork B attach to SAME container" fails silently when operator A has customised the per-fork container name. Functional smoke verified pre-commit iter-257. Carry-forward: any future mode-opinionated shim (Stories 2.12..2.17 that HARDCODE a mode-specific value regardless of operator env) must smoke-test operator-override-IGNORED paths. Promoted to RALPH.md § Lessons at iter-258.
- **iter-255 Story 2.11 pre-dev review gate — "substrate-truth corrections" as the dominant review-finding class for Stories N≥11** (carry-forward). Pattern: every enumeration claim (shim list, recompute inventory, anchor format, Docker primitive behaviour) MUST be grep/ls/doc-verified at create-story OR pre-dev-review time. Forecast for Stories 2.12..2.17: 1-3 CRITICAL "inventory correction" findings per pre-dev review; ENHANCEMENT class reduces as writing pattern stabilises.
- **iter-253 Story 2.10 CR re-run closure — SECOND-class operator-edge DEFER pattern** (carry-forward). PATCH iterations close FIRST-class adversarial findings but commonly surface SECOND-class operator-edges in the same surface the prior bug had hidden. Story 2.11's resolver-function + compose parameterisation + env-check orphan warning constitute "multi-site novel runtime probe" surface; expect 2-4 second-class DEFERs at CR re-run (Docker Desktop wake-race, Podman-compat semantics, compound-failure short-circuit doctrine carry-forward forecast classes).
- **iter-252 Story 2.10 PATCH-1 — `set -e`+`if cmd; then` rc-suppression LESSON applied at iter-257.** Task 4 env-check orphan-container probe uses canonical `rc=0; docker inspect … || rc=$?; case "${rc}" in 0) emit warning ;; 1) : ;; *) : ;; esac` pattern. No rc-suppression hazard at landing time; preserved unchanged at iter-258 trace gate.
- **iter-252 GOTCHA — iter-env `/tmp` is `noexec`** (carry-forward, applies to Story 2.13 healthcheck emitter + Story 2.14 legacy-branch cleanup; NOT applicable to Story 2.11 which does not use mock-binary PATH shims).
- **iter-244/246 audit-findings carry-forward** unchanged from iter-247..257: (1) restart.sh transitive-delegate prepend-only (applied at iter-257: restart.sh invokes resolver for fail-fast inspect consistency even though stop.sh + start.sh each re-run it downstream); (2) benchmark.sh OUT-OF-SCOPE clarification (SC-11); (3) Manifest entry count drift (29 → **30** at iter-257 after `INV-devbox-mode` landing); (4) Alpine egress non-applicability doctrine. Manifest count will not move again until next story adds a new invariant.
- **Substrate-citation drift forecast for Stories 2.11..2.17:** Story 2.11 at iter-257 touched `docker-compose.yml:43` (`name:` parameterisation) + comment-block amendments at L43-50 + workspace-mount doc-block L103-107 rewritten past-tense + TODO marker L239 removed. Line-number drift forecast unchanged at iter-258: post-iter-257 HEAD of `docker-compose.yml` has shifted line numbers at the compose `name:` block + workspace-mount comments; story-file § References L408-413 cite original line numbers (pre-edit); drift pre-declared deferred to Story 2.17 SC-17 polish pass. Total Epic 2 close-out reconciliation forecast: ~15-25 cumulative line-number drifts (updated from ~10-20 at iter-256).
