# Implementation Plan

## NOW

- [ ] **Fix CR-1 [MEDIUM]** — add `"required": ["light", "dark"]` to `modesOverlay` in `packages/ui/tokens.schema.json:328` (after `"additionalProperties": false`); recompute `sha256sum packages/ui/tokens.schema.json`; update `INV-tokens-schema-contract.contentHash` at `packages/keel-invariants/src/invariants.manifest.ts:133`; re-run `pnpm -w typecheck` + `pnpm keel-invariants:check` clean-path → exit 0. FR14n matrix row 10: `fixes-pending` — top QUEUE fix task; stays `fixes-pending` until QUEUE empties → re-run `/bmad-code-review (args: "2")`. ~small

## QUEUE (Story 1.10 — design-token schema, semantic + rationale contract)

_(empty — Fix CR-1 is the sole PATCH item surfaced at iter-50 CR. When discharged, iter-52 re-runs `/bmad-code-review (args: "2")` for `fixes-pending → done` per FR14n matrix row 10.)_

## BLOCKED

_(none)_

## DONE (Story 1.10 — iter-50 /bmad-code-review)

- [x] iter-50: **Adversarial code review; FR14n state `sm-verified → fixes-pending` per matrix row 9.** Ran `/bmad-code-review (args: "2")` — three-layer parallel fan-out against Story 1.10 diff `535bee0..HEAD` (1919 insertions / 19 deletions / 12 files; normative = `packages/ui/tokens.schema.json` +547 / `docs/invariants/tokens.md` +173 / `packages/keel-invariants/src/invariants.manifest.ts` +16 / `INVARIANTS.md` +5; non-normative = story + sprint-status + iter-46 traceability + `.ralph/@plan.md` + `RALPH.md`). **Raw findings:** Blind Hunter 23 (6 HIGH / 6 MEDIUM / 11 LOW) + Edge Case Hunter 23 (MEDIUM/LOW schema-portability + mode-enforcement + slug-convention + DTCG-metadata classes) + Acceptance Auditor 1 (LOW sprint-status data-field drift). **Triage: 1 PATCH + 15 DEFER + 30 DISMISS** per iter-36 two-layer-convergence doctrine. **PATCH = CR-1** (modesOverlay.required missing `["light","dark"]` — convergent Blind+Edge; spec Task 1 line 66 explicit prescription; ~small fix). **DEFER class** discharged by AC 1 scope carve-out (story line 20: "schema does not constrain numeric ranges, contrast ratios, or value quality") — entire DTCG-portability / $extensions / WCAG / regex-DRY / severity-alias class bulk-deferred to Stories 1.11-1.13; iter-46 gate-WAIVED trace artefacts also deferred. **DISMISS class** (30 items, en bloc): single-layer adversarial overreach, anchor-regex mis-reads, slug-collision claim that doesn't collide under forward-generator, Story-1.8-provenance mis-read of tokens.md:5 (correctly refers to drift-enforcement infrastructure lineage not entry-ownership). **Step-6 resync applied this iter**: story-file `Status: done → in-progress`; sprint-status.yaml `1-10: done → in-progress` + `last_updated: Story-1-10-ready-for-dev → Story-1-10-in-progress UTC` (both header-comment `:2` + data-field `:38`) — discharges Auditor's sole finding + dismisses BH/EH "preemptive Status done" findings without separate fix tasks. Story file Change Log v1.6 row appended; new `### Code review (Ralph iter-50 — ...)` subsection under `## Review Findings` with full per-finding triage. No source-code changes (CR-verification-only iteration). PR #226 CI: `no checks reported`; push safe. Tracked at GitHub issue #34 — `Refs #34` in commit trailer.

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1–1.9 done; 1.10 `fixes-pending` awaiting iter-51 Fix CR-1 + iter-52 CR re-run; 1.11–1.16 backlog → 6 more stories × full FR14n lifecycle remaining after 1.10 closure before EPIC_DONE)
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (continues through Epic 1; PR #226 stays Draft until EPIC_DONE per PROMPT_build.md step 5c)
- **Story:** 1.10 — design-token schema, semantic + rationale contract
- **Story File:** `_bmad-output/implementation-artifacts/1-10-design-token-schema-semantic-rationale-contract.md`
- **Story State:** `fixes-pending` — iter-50 adversarial CR surfaced 1 PATCH (convergent Blind+Edge; spec Task 1 explicit). Next iteration applies Fix CR-1 per FR14n matrix row 10 top-QUEUE-fix discipline (`fixes-pending` stays until QUEUE empties → re-run `/bmad-code-review (args: "2")` for `fixes-pending → done`).
- **PR:** #226 (Draft, MERGEABLE, no-CI-checks-reported — stays Draft through Epic 1; Draft→Open at EPIC_DONE)

## Notes

- **Story 1.10 positioning.** First of 4 design-token-pipeline stories in Epic 1 (1.10 schema+rationale contract → 1.11 source population Direction A → 1.12 emitter pipeline → 1.13 quality gates). **Contract-only single-pass CR hypothesis partially falsified at iter-50** (iter-7 Decisions): Story 1.10 surfaces 1 convergent Blind+Edge PATCH vs. Story 1.8's zero-finding clean CR. Closure trajectory projected to mirror the iter-47..49 SM pattern (1 small mechanical fix + 1 re-verify = 3-iter total: iter-50 CR + iter-51 Fix + iter-52 re-run). SM layer closed in 3 iterations; CR layer projected for 3 iterations. Total Story 1.10 FR14n lifecycle iterations: 11 (iter-42..52) — ~1.5× Story 1.8's 7-iter closure, consistent with the partial-falsification hypothesis.
- **Trace posture.** Gate WAIVED (fourth cumulative — 1.7 iter-5 + 1.8 iter-5 + 1.9 iter-8 + 1.10 iter-46). Seven-point rationale pinned at iter-46 trace artefacts; unchanged by iter-48 SM prose fix, iter-49 SM re-verify, or iter-50 CR-verification-only resync.
- **Post-iter-50 audit trail.** Four files touched: `_bmad-output/implementation-artifacts/1-10-design-token-schema-semantic-rationale-contract.md` (Status `done → in-progress`; new `### Code review (Ralph iter-50 — ...)` subsection under `## Review Findings`; Change Log v1.6 row); `_bmad-output/implementation-artifacts/sprint-status.yaml` (header comment + data field + `1-10:` entry all synced to `in-progress`); `.ralph/@plan.md` (this file); `RALPH.md` (new iter-50 Signpost + new 2026-04-20 Lessons-learned entry documenting the spec-AC-carve-out-as-bulk-DEFER-gate doctrine). Zero schema / rationale-doc / manifest / runtime-code / INVARIANTS.md changes. Quality gates NOT re-run (CR-verification-only; no source code touched).
- **Anchor regex landmines avoided** (Story 1.9 CR defer carry-forward — `deferred-work.md:37-38`): iter-50 added no new `INV-*` anchors; pure verification + status-resync. No column-0 anchor-shape risk at this iteration. Fix CR-1 (iter-51) also carries no anchor risk — schema edit only.
- **CR triage doctrine applied this iter** (iter-36 two-layer-convergence + iter-41 residue-family-catalogue + iter-50 new AC-carve-out-as-bulk-DEFER-gate carry-forward): convergent Blind+Edge findings against explicit spec prescriptions → PATCH. Single-layer Blind-only findings → DISMISS (class 5 stylistic-overreach residue). AC scope-carve-out + Dev-Notes-scope-reserved class → bulk-DEFER (entire class, no per-item deliberation). Gate-waived trace artefacts → DEFER to Story 1.16 test-runner landing. This is the characteristic contract-only-CR triage shape (1 PATCH / 15 DEFER / 30 DISMISS — 2% actionable, 32% deferred, 66% dismissed) vs. trace/runtime stories (Story 1.9's 30 raw → 4 PATCH shape = 13% actionable).
- **Issue Tracking:** Story 1.10 tracked at GitHub issue **#34**. `Refs #34` in every commit trailer; `Closes #34` lands in PR body only when Story 1.10 reaches `done` (matrix row 11) — earliest iter-52 (post-Fix-CR-1 + post-clean-CR-re-run). Parent Epic 1 issue **#9** — `Refs #9` optional; never `Closes` it (ralph.py transitions epic issues to Done on EPIC_DONE halt).
