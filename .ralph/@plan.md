# Implementation Plan

## NOW

- [ ] `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/1-13-token-quality-gates-schema-validation-wcag-aa-contrast-source-output-sync.md")` — FR14n row 4 `atdd-scaffolded → in-dev`; single-pass CANDIDATE under preventative-audit pre-resolution (Story 1.12 iter-63 precedent). Larger blast radius than Story 1.12: 3 new files + 2 file amendments + 4 config file edits + 7 manifest entries touched (3 new + 4 hash-PATCH). Decomposition risk: if dev-story partials at Task 3 (emitter amendment) or Task 5 (manifest growth), re-queue with `in-dev (partial)` per FR14n row 4 partial-completion semantics. ~large.

## QUEUE (Story 1.13 — design-token quality gates)

- [ ] `/bmad-testarch-trace (args: "yolo")` — FR14n row 5 `in-dev → traced`; SEVENTH WAIVED CANDIDATE; structural inheritance from Story 1.11/1.12 applies (6-AC scale, identical to Story 1.12 scaling envelope).
- [ ] `/bmad-create-story (args: "review")` post-dev — FR14n row 7 `traced → sm-verified`; THIRD cumulative ZERO-PATCH CANDIDATE per iter-58/iter-65 doctrine if preventative-audit discipline holds through dev.
- [ ] `/bmad-code-review (args: "2")` — FR14n row 9 `sm-verified → done`; ZERO-PATCH + 10-15 DEFER forecast per iter-66 scaling (gate-authoring × 3-gate + manifest cross-cuts × 7-entry footprint = larger-than-emitter surface; DEFER count skews higher).

## BLOCKED

_(none)_

## DONE (current story/phase only)

- [x] iter-69: **FR14n row 3 `validated → atdd-scaffolded` via hybrid ground-(c) variant-(ii)+(iii) ATDD-skip clause (PROMPT matrix row 3 skip carve-out).** Skill `/bmad-testarch-atdd` NOT invoked — preflight HALT confirmed: TEA `test_framework: auto` autodetects nothing; recursive `find packages/** -name 'vitest.config.*|jest.config.*|playwright.config.*|cypress.config.*'` (excluding `node_modules`, `.claude`) returns zero matches. SEVENTH cumulative Epic 1 ATDD-skip precedent (Stories 1.7 iter-2 / 1.8 iter-3 / 1.9 iter-4 / 1.10 iter-44 / 1.11 iter-55 / 1.12 iter-62 → 1.13 iter-69). **Three-ground conjunction applied** (per iter-55/iter-62 precedent): (a) substrate-verification-covers-AC — Task 7's three negative smokes directly exercise AC 1 (inject schema violation → exit 1 + structured JSON finding) / AC 2 (revert `status.info.fg` retune → exit 1 + ratio-<-threshold JSON) / AC 3 (edit `packages/ui/tokens.json` without re-emit → exit 1 + unified-diff excerpt); AC 4 verified via `pnpm keel-invariants:check-all` walking all 17 manifest entries + `INVARIANTS.md` anchors; AC 5 verified via `pnpm exec prek run --all-files` firing the two new hooks; AC 6 verified via structural inspection of Story 1.12 spec amendments at post-dev SM (iter-71). (b) No test runner at Story 1.13 time — lands Story 1.16. (c) **Hybrid variant-(ii)+(iii)** — variant (ii): Story 1.16 is the formal test-runner landing that will backfill automated per-AC unit tests (enumerated in the new Dev Notes Testing standards summary: schema-gate happy+sad; color-math pure-function fixtures; `--check` mode; SHA-resolver failure modes; diamond-DAG fixture; manifest Zod parse + shared-sourcePath superRefine; prek-fire tests; spec-amendment round-trip; `.prettierignore` contract). variant (iii): adversarial coverage of gate-behaviour (Ajv compile-cost, OKLCH→sRGB floating-point precision, pair-enumeration completeness vs tokens.json leaf enumeration, `gamutMap` 3-iteration convergence, `--check` zero-write purity, `resolveSourceSha` failure-mode distinction, alias-cycle diamond-DAG false-positive guard, `$modes.dark` walker) delegated to `/bmad-code-review (args: "2")` Blind Hunter / Edge Case Hunter / Acceptance Auditor fan-out at iter-72 CR. Hybrid strictly stronger than either variant alone (per iter-62 NEW lesson "hybrid ground-(c) variant-(ii)+(iii) unlocks BOTH ATDD-skip + trace-WAIVED gate decisions"). **Single PATCH to Story 1.13 spec-file**: inserted new `§ Dev Notes — Testing standards summary` sub-bullet (after "No conflict with Story 1.8 manifest shape"; before `### Project Structure Notes`) mirroring Story 1.12 line 243-246 structure; enumerates no-runner + substrate verification + Task 7 smokes + three-ground ATDD-skip rationale + deferred unit+integration test list per-AC. v1.2 Change Log row appended. No scope changes; no new ACs; no Task shape changes. Story State: `validated → atdd-scaffolded` (NOT `→ in-dev`; RALPH.md iter-22 precedent: "state label 'ATDD step discharged; dev-story is next'"). Sprint-status unchanged (stays `ready-for-dev`; FR14n carries lifecycle phase in IP Context). Budget spent: ~20K tokens (drafting + single PATCH + IP + RALPH.md; below iter-62 ~22K precedent). Next iter-70: `/bmad-dev-story (args: "...-1-13-...md")` — single-pass CANDIDATE under preventative-audit pre-resolution (Story 1.12 iter-63 precedent holds at 6-AC scale; 3 new files + 2 amendments + 4 config edits + 7 manifest entries; if budget exceeded, acceptable partial → `in-dev (partial)` re-queue).

- [x] iter-68: **`/bmad-create-story (args: "review")`; FR14n state `drafted → validated` per matrix row 2.** Pre-dev SM review of Story 1.13 spec-file. **TWO in-place PATCHes** applied to resolve AC 4 internal contradiction (a drafting-residual from a pre-convergence sync-gate design where AC 4's bullet list assumed a separate `check-tokens-sync.ts` CLI file but Task 5 + § AC 4 carve-out + code block + Dev Notes all converged on "emitter IS the sync gate in `--check` mode" via shared sourcePath with `INV-tokens-emitter`): (PATCH 1) Line 58 AC 4 bullet — `INV-tokens-sync-gate` sourcePath corrected from `packages/keel-invariants/src/check-tokens-sync.ts` (non-existent CLI file) → `packages/ui/scripts/generate-tokens.ts` (shared with `INV-tokens-emitter`). (PATCH 2) Line 61 AC 4 closer — "three new source files line up byte-for-byte" → "three source files — two new CLI entries plus the shared-sourcePath emitter — line up byte-for-byte with their `contentHash` values" (only 2 NEW source files; the third pins the amended emitter). Both within ≤2 PATCHes target matching Story 1.11 iter-54 (1 PATCH) + Story 1.12 iter-61 (2 PATCHes) pre-dev SM precedent. No scope changes, no new ACs, no Task shape changes — pure AC-coherence fix. v1.1 Change Log row appended documenting the fix + cross-file verification outcome. Sprint-status unchanged (stays `ready-for-dev`). Story State: `drafted → validated`. **Pre-dev SM gate PASSED with 2-PATCH delta.**

- [x] iter-67: **`/bmad-create-story`; FR14n state `_(no story) → drafted` per matrix row 1.** Authored `_bmad-output/implementation-artifacts/1-13-token-quality-gates-schema-validation-wcag-aa-contrast-source-output-sync.md` (~790 lines). 6 ACs + 7 Tasks + exhaustive Dev Notes + 28 References + v1.0 Change Log row. **Seven preventative-audit layers pre-applied at drafting time.** Sprint-status bumped `1-13-...: backlog → ready-for-dev`.

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1–1.12 done; 1.13 drafted iter-67 + SM-validated iter-68 + ATDD-skipped iter-69; 1.14–1.16 backlog → 3 more stories × full FR14n lifecycle remaining before EPIC_DONE halt).
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (continues through Epic 1; PR #226 stays Draft until EPIC_DONE per PROMPT_build.md step 5c).
- **Story:** Story 1.13 — Token quality gates (schema validation + WCAG AA contrast + source-output sync)
- **Story File:** `_bmad-output/implementation-artifacts/1-13-token-quality-gates-schema-validation-wcag-aa-contrast-source-output-sync.md`
- **Story State:** `atdd-scaffolded` — FR14n matrix rows 1 (iter-67) + 2 (iter-68) + 3 (iter-69 SKIP) complete; row 4 pending at iter-70 `/bmad-dev-story`.
- **GitHub Issue:** Story 1.13 issue TBD (ralph.py resolves on subsequent iter start). Parent Epic 1 at **#9** (OPEN — closes at EPIC_DONE halt).
- **PR:** #226 (Draft, MERGEABLE, CLEAN, empty statusCheckRollup — stays Draft through Epic 1; Draft→Open at EPIC_DONE after Stories 1.13–1.16 all complete FR14n lifecycle).

## ATDD Skip Rationale (Story 1.13 iter-69; FR14n matrix row 3)

**Skip decision:** `/bmad-testarch-atdd` NOT invoked; Story State `validated → atdd-scaffolded` per PROMPT § Story Lifecycle Decision Matrix row 3 skip carve-out ("skip → in-dev allowed only if story has no testable ACs; record rationale in IP" — reinterpreted per RALPH.md iter-22/iter-48/iter-55/iter-62 precedent as `→ atdd-scaffolded` to preserve the state-label "ATDD step discharged; dev-story is next").

**Preflight verification (iter-62 lineage):** `ls packages/*/{vitest,jest,playwright,cypress}.config.*` returns zero matches; recursive `find -maxdepth 5` under `packages/**` (excluding `node_modules` + `.claude`) returns zero matches. TEA `test_framework: auto` autodetects nothing; invoking `/bmad-testarch-atdd` would HALT at Step 1.2 preflight. Test-runner landing is Story 1.16 scope per Epic 1 roadmap (`_bmad-output/planning-artifacts/epics.md`).

**Three-ground conjunction (hybrid variant-(ii)+(iii) — strictly stronger than either alone per iter-62 lesson):**

- **(a) substrate-verification-covers-AC** — Task 7's three negative smokes exercise AC 1/2/3 at CLI-exit-code level (schema-gate inject-violation, contrast-gate revert-retune, sync-gate edit-without-reemit); AC 4 verified via `pnpm keel-invariants:check-all` walking all 17 manifest entries + `INVARIANTS.md` anchors; AC 5 verified via `pnpm exec prek run --all-files` firing the two new `tokens-schema` + `tokens-contrast` hooks; AC 6 verified via structural inspection of Story 1.12 spec amendments + `tokens.schema.json` `leafBreakpoint` pin at iter-71 post-dev SM review.
- **(b) no-runner** — no test framework wired at Story 1.13 time (Story 1.16 scope).
- **(c) hybrid variant-(ii)+(iii):**
  - _variant (ii)_: Story 1.16 is the formal test-runner landing that will backfill automated per-AC unit tests (schema-gate happy+sad paths; `color-math.ts` pure-function fixtures against Ottosson 2020 reference; `gamutMap` convergence; pair-enumeration coverage; `--check` mode happy+sad; SHA-resolver failure-mode branches; diamond-DAG fixture; manifest Zod parse + shared-sourcePath `superRefine`; prek-fire tests; Story 1.12 spec-amendment round-trip; `.prettierignore` contract enforcement).
  - _variant (iii)_: adversarial coverage of gate-behaviour (Ajv compile-cost profile, OKLCH→sRGB floating-point precision, pair-enumeration completeness vs `packages/ui/tokens.json` leaf enumeration, `gamutMap` 3-iteration convergence + hard-clamp fallback, `--check` zero-write purity, `resolveSourceSha` ENOENT/stderr/untracked branches, alias-cycle diamond-DAG false-positive guard, `$modes.dark` overlay resolution in the contrast walker) delegated to `/bmad-code-review (args: "2")` Blind Hunter / Edge Case Hunter / Acceptance Auditor fan-out at iter-72.

**Precedent load-bearing:** 7th cumulative Epic 1 application (Stories 1.7/1.8/1.9/1.10/1.11/1.12 → 1.13). Pattern: Epic-1-substrate stories with ATDD-skip at iter-N → trace gate at iter-(N+2) SHOULD emit WAIVED (iter-55/iter-64 consistency rule). Story 1.13 iter-71 trace target: SEVENTH cumulative WAIVED precedent.

**Story file v1.2 Change Log row records the ATDD-skip transition + § Dev Notes "Testing standards summary" sub-bullet insertion.**

## Notes

- **Story 1.13 forecast: 7-iteration floor CANDIDATE** held at iter-69 (iter-67 drafted → iter-68 validated → iter-69 atdd-skip → iter-70 in-dev → iter-71 traced → iter-72 sm-verified → iter-73 done; matches Story 1.11 iter-53..iter-59 + Story 1.12 iter-60..iter-66 each at exactly 7 iters). Risk-weighted alternative: +1 iter if dev-story partials.
- **Carry-forward rules INTO Story 1.13 dev + downstream gates — COMPOUNDING PRIOR ART:**
  - **Seven preventative audit layers** (iter-53/54/56/59/60/67) pre-applied at drafting; re-verify at SM + CR.
  - **Story 1.11/1.12 ZERO-PATCH doctrine** — "structurally impossible + fully documented + does not violate AC text = ACCEPT"; scales SM + CR + command-literal-vs-intent disputes.
  - **Multi-layer-corroborated authoring-choice findings are DEFER, not PATCH** (iter-59 + iter-66 at 10-DEFER ceiling; Story 1.13 target 10-15 DEFER at gate-authoring scale).
  - **Single-layer BH/EH findings default DISMISS unless corroborated** (iter-36/52/59/66 at 26 dismisses).
  - **Structural trace inheritance ~90% savings** (iter-46/57/64; scales 4-AC → 6-AC). Applies at iter-71.
  - **Scoped-build optimisation** — after manifest mutation, `pnpm --filter @keel/keel-invariants build` before `pnpm keel-invariants:check`. Applies at Task 5 manifest 14→17 + 4 hash PATCHes; umbrella `keel-invariants:check-all` recomposes cleanly.
  - **Drift-smoke revert patterns** (iter-45/56/63). Task 7 three negative smokes each follow this pattern (inject → verify exit 1 → revert → verify exit 0).
  - **Ground-(c) hybrid variant-(ii)+(iii)** unlocks ATDD-skip + trace-WAIVED for substrate+tooling + gate-authoring stories with runtime-probe deferral + spec-declared CR-substitution clause. Applied at iter-69.
  - **Compound-ZERO-PATCH achievable under 5 conditions** (iter-66): (a) seven preventative audit layers — CONFIRMED iter-67; (b) pre-dev SM ≤2 PATCHes — CONFIRMED iter-68 2-PATCH; (c) single-pass dev — iter-70 target; (d) clean trace — iter-71 target; (e) CR defenses pre-staged — iter-72 target.
  - **CR DEFER ceiling scales** (iter-66): populator ≈5 DEFER; emitter/gate ≈8-12 DEFER. Story 1.13 gate-authoring forecast: 10-15 DEFER.
  - **`/bmad-code-review (args: "2")` arg `2` = "Leave as action items"**; vacuous in ZERO-PATCH case but required to force non-interactive branch.
- **Story 1.13 DEFER absorption roadmap (FROM iter-66 deferred-work.md):** 7 defers absorbed at drafting (AA1/AA2/AA3 spec amendments; resolveSourceSha hoist + failure-mode distinction + diamond-DAG guard; walkLeaves via schema-gate ordering; parseInt breakpoint via schema regex pin). 3 defers re-routed (REPO_ROOT → Epic 3; fontSize lineHeight → Epic 7/3; contrast-retune completeness via Story 1.11 iter-59 defers at Task 2).
- **Story 1.13 NOT absorbing (route elsewhere):** REPO_ROOT brittle → reliability/Epic 3; fontSize hardcoded 1.5 → Epic 7/3; shadow-machinery → Story 1.17-TBD or Epic 7.
- **Issue Tracking carry-forward.** Parent Epic 1 at **#9** OPEN (never `Closes`; Ralph transitions at EPIC_DONE halt). Story 1.12 at **#36** OPEN-pending-PR-merge (closes on PR merge at EPIC_DONE). Story 1.13 issue TBD. iter-69 commit trailer uses `Refs #9` (parent-epic fallback; `RALPH_ISSUE_NUMBER` env var unset at iter-69 per ralph.py IP-Context-Story parse-lag since Story 1.13 `**Story:**` field set at iter-67).
