# Implementation Plan

## NOW

- [ ] Run `/bmad-create-story (args: "review")` pre-dev SM validation for Story 2.3. Per § Story Lifecycle Decision Matrix row `drafted → validated`: the same skill run with `(args: "review")` now serves the pre-dev SM requirements-satisfaction gate on the iter-155 draft at `_bmad-output/implementation-artifacts/2-3-egress-policy-dnsmasq-nftables-fail-closed-ipv4-ipv6-parity-atomic-reload.md` (17 scope-clarifications + 13 Tasks + exhaustive Dev Notes). Budget: ~60K tokens per Story 2.2 v1.1 precedent (iter-146 landed 9 pre-dev fixes against ~9-SC draft; Story 2.3's 17-SC draft may surface 10-18 fixes). Expected verdicts: (a) fixes-applied cleanly → `drafted → validated` → queue next; (b) SM surfaces surgical corrections (likely Tasks/SCs tightening, not AC re-wording since ACs are verbatim from epics.md). Story file Change Log v1.1 (at SM-review close) records fixes-applied summary + transitions `ready-for-dev → ready-for-dev` (no sprint-status flip at this gate). Post-validated: `/bmad-testarch-atdd` queued for `validated → atdd-scaffolded` (likely 13th cumulative FR14n ATDD-skip precedent — infrastructure-security class ~substrate stance).

## QUEUE (Story 2.3 lifecycle — mid-flight)

- [ ] _(post-Story-2.3-validated)_ `/bmad-testarch-atdd` — red-phase scaffolds OR ATDD-skip (projected thirteenth cumulative precedent — hybrid ground-(c) variant-(ii)+(iii): Story 2.5 downstream hardening-integration gate + spec-declared-CR-substitution for infrastructure-security adversarial coverage). Story State `validated → atdd-scaffolded`.
- [ ] _(post-Story-2.3-atdd-scaffolded)_ `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-3-egress-policy-dnsmasq-nftables-fail-closed-ipv4-ipv6-parity-atomic-reload.md")` — `atdd-scaffolded → in-dev`. Heavy iter expected: 11 new files + 6 modified files + invariant doc + manifest entry + sync-gate refresh. Budget likely ≥80K; may complete single-iteration (Story 2.2 iter-148 landed 8-task 5-AC story single-pass) OR split across 2 iters with `in-dev (partial)` marker (Story 2.3 has larger surface — entrypoint surgery + bash-script-heavy daemons + runtime-verification deferral).
- [ ] _(post-Story-2.3-in-dev)_ `/bmad-testarch-trace (args: "yolo")` — AC→test coverage gate; WAIVED precedent likely HOLDS (thirteenth cumulative Epic precedent under ATDD-skip-trace-WAIVED co-application rule).
- [ ] _(post-Story-2.3-traced)_ `/bmad-create-story (args: "review")` post-dev SM verification — `traced → sm-verified`.
- [ ] _(post-Story-2.3-sm-verified)_ `/bmad-code-review (args: "2")` — CR gate; forecast envelope per iter-155 lesson (carve-out × 3 + live-AC-coverage × 3 + impl-surface-LOC / 100): 0-carve-out + backend-B live-smoke defer (+3) + ~600 LOC impl (+6) → 6-9 iter fix-chain projected; likely LOOSER than Story 2.2's 4-iter + TIGHTER than Story 2.1's 16-iter.
- [ ] _(conditional, post-Story-2.17 done)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge. Epic 2 completion.
- [ ] _(conditional, post-Epic-2 merged)_ § Cross-epic transition → Epic 3 Story 3.1 OR halt EPIC_DONE awaiting merge.

## BLOCKED

_(none — Story 2.3 draft landed clean at iter-155; Epic 2 at 2/17 stories done + 1/17 ready-for-dev; no carry-forward blockers from Story 2.2 CR closure.)_

## DONE (current story 2.3 lifecycle)

- [x] iter-155: **`/bmad-create-story` Story 2.3 exhaustive-context-engine draft landed.** Four parallel Sonnet subagents (epics+PRD + architecture + prev-story-intelligence + repo-prior-art) extracted context across ~15 planning artefacts + 2 prior story files + full `packages/devbox/` tree + `docs/invariants/`; synthesis produced 5 ACs (verbatim from epics.md Story 2.3) + 17 scope-clarifications (SC-1..SC-17: runtime location / JSONL path+schema+rotation / atomic-reload mechanism / cap_add wiring / IPv4+IPv6 parity verification commands / whitelist boundary vs Story 2.4 / scripts output-location / invariant consolidation / entrypoint surgery / dnsmasq allow-mechanism / resolv.conf override / upstream DNS env knob / JSONL emitter process model / log-rotation safety / workspace-logs dir pre-create) + 13 Tasks (~50 subtasks). **Three new guardrails pinned in RALPH.md Decisions (iter-155):** (a) in-container runtime location override of stale sidecar TODO at docker-compose.yml:10 (AC-verbatim + architecture-post-pin-language wins); (b) ONE consolidated `INV-devbox-egress-contract` tracking ONE doc at `docs/invariants/devbox-egress.md` covering fail-closed + IPv4/IPv6 parity + atomic-reload (vs three split invariants; Story 2.2 iter-151 AR-2 asymmetry-prevention rule generalises to multi-faceted contracts); (c) scope-isolation of `scripts/whitelist.sh` (architecture-named file) → Story 2.4; Story 2.3 ships primitive `scripts/reload-egress.sh` under distinct name (prevents multi-story file-edit churn). File layout mirrors architecture tree (l.975-1004) — 11 new files + 6 modified. Backend-B iteration-env constraint carried from Story 2.1 (live smokes deferred to operator workstation). JSONL schema pinned (6 stable fields) for Epic 4 FR37 consumer hard-reference. Atomic-reload pinned (flock + `nft -f` kernel-atomic + dnsmasq SIGHUP + release). One `.envrc.example` knob added (`KEEL_DEVBOX_DNS_UPSTREAM=1.1.1.1`); no whitelist-source knob (Story 2.4) + no rotation-size knob (pinned at 50 MB per SC-4). Sprint-status flip `2-3-…: backlog → ready-for-dev` + timestamp comment. Story Change Log v1.0 entry composed. Budget: ~75K tokens used (parallel subagent fan-out + single-shot story file write + sync-gate-adjacent operations). Commit captures story file + sprint-status.yaml + RALPH.md Decisions + this IP update.

## Context

- **Phase:** 4-implementation — Epic 2 at 2/17 stories `done` (2.1 iter-144 + 2.2 iter-154) + 1/17 `ready-for-dev` (2.3 iter-155).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.3 is infrastructure-security (in-container dnsmasq + nftables + JSONL tailer + file-locked atomic reload); Docker substrate required for runtime verification once dev-story lands; iteration-context bind-mount denial persists for workspace-mounted `docker compose run` / `docker compose build` commands — live smokes (Task 12.1-12.7) MUST defer to operator workstation at dev-story closure.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Story 2.3 Task 1 appends `dnsmasq` + `nftables` apt packages → re-bake needed at dev-story close; version matrix in `packages/devbox/VERSIONS.md § Bake log` extended with new `### Egress policy (Story 2.3)` subsection.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; 2.1 + 2.2 done; 2.3 ready-for-dev; 2.4..2.17 in `backlog`.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2; PR #230 transitions to Open at Story 2.17 completion).
- **Story:** 2.3 — egress-policy-dnsmasq-nftables-fail-closed-ipv4-ipv6-parity-atomic-reload.
- **Story File:** `_bmad-output/implementation-artifacts/2-3-egress-policy-dnsmasq-nftables-fail-closed-ipv4-ipv6-parity-atomic-reload.md` (landed iter-155, Change Log v1.0).
- **Story State:** `drafted` — Story 2.3 draft at ready-for-dev; pre-dev SM validation is the next state transition (`drafted → validated`).
- **GitHub Issue:** ralph.py will auto-inject Story 2.3's issue number at the start of next iteration from sprint-status; parent Epic 2 → #10.
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **DinD is fork-time substrate, not transitional.** `INV-devbox-dind-available` — every fork's cc-devbox-equivalent must provide Docker. Story 2.3's nftables/dnsmasq entrypoint-init + live smokes exercise the daemon path.
- **Backend B is the reference env at 2026-04-21.** Broad-state-mutation scripts MUST detect backend + refuse destructive ops by default (Story 2.1 `benchmark.sh` is the reference implementation).
- **NFR2 authority unchanged.** Cold ≤ 300s / warm ≤ 30s remains authoritative on M4-Pro native; DinD runs are indicative only.
- **Cumulative Epic-2 CR metrics (Story 2.1 + 2.2):** Story 2.1 = 9 patches across 3 gates (16-iter fix-chain); Story 2.2 = 2 patches across 1 gate (4-iter fix-chain). **4× tighter envelope for hybrid-class + ZERO-carve-out stories vs multi-component-class + operator-carve-out stories.** Story 2.3 forecast per iter-151/154 equation: 6-9 iter fix-chain likely (0-carve-out + backend-B live-smoke defer + ~600 LOC impl).
- **Validated patterns at iter-155:** Four-parallel-Sonnet exhaustive-context-engine fan-out (epics+PRD / architecture / prev-story-intelligence / repo-prior-art) → single-shot story file synthesis (pattern first used Story 2.2 iter-145); 17-SC scope-clarification density for infrastructure-security class (vs ~9 SC for configuration-surface class per Story 2.2); single-file + distinct-name scope-isolation rule for architecture-tree files owned by downstream stories; one-doc-one-invariant rule for multi-faceted contracts (Story 2.2 iter-151 AR-2 asymmetry-prevention generalised).
