# Implementation Plan

## NOW

- [ ] **Run `/bmad-create-story`** — picks next `ready-for-dev` / `backlog` story from `_bmad-output/implementation-artifacts/sprint-status.yaml` (expected: **Story 1.11 — design-token source, Direction A baseline with motion + density scales**, currently `backlog` at sprint-status line 56). FR14n matrix row 1 (`_(no story)_ → drafted`). Story 1.11 is the first of 3 remaining Epic-1 design-token-pipeline stories (1.11 populate source → 1.12 emitter pipeline → 1.13 quality gates), followed by Stories 1.14 (Tailwind/Textual runtime adapters), 1.15 (CI substrate wiring), 1.16 (test-runner + retrospective). ~medium

## QUEUE (Story 1.11 — design-token source, Direction A baseline with motion + density scales)

_(empty until iter-53 drafts Story 1.11; then FR14n lifecycle iterates: drafted → validated → atdd-scaffolded → in-dev → traced → sm-verified → done. Carry-forward rules into Story 1.11 pre-dev review: iter-43 AC-vs-Task stable-ID grep-audit + iter-43 sprint-status transition wording correction + iter-47 Task-enumeration-vs-schema-enumeration three-point audit; plus iter-51 scoped-build optimisation when mutating `invariants.manifest.ts` contentHashes.)_

## BLOCKED

_(none)_

## DONE (Story 1.10 — iter-52 CR re-verify; Story 1.10 closure)

- [x] iter-52: **`/bmad-code-review (args: "2")` CR RE-RUN #2 — zero-PATCH clean pass; Story State `fixes-pending → done` per FR14n matrix row 10 single-transition edge.** Three-layer parallel fan-out (Blind Hunter + Edge Case Hunter + Acceptance Auditor Opus subagents) against `535bee0..HEAD` iter-51-post-Fix-CR-1 diff. **Verdicts:** Blind Hunter NEW_FINDINGS 8 raw (5 MEDIUM + 3 LOW — all single-layer without Edge or Auditor convergence per iter-36 doctrine); Edge Case Hunter PASS (walked every branching path in iter-51 delta — modesOverlay required guard closes iter-50 convergent gap, hash-resync byte-integrity verified, no unhandled branches introduced); Acceptance Auditor PASS with 14 positive checks across AC 1 / AC 2 / AC 3 / AC 4 (CR-1 PATCH cleanly discharged — schema structure + per-`$type` leaf defs + modesOverlay + rationale-doc stable-ID cross-links + manifest entries + INVARIANTS.md anchors + sync-gate smoke branches all satisfied). **Triage: 0 PATCH + 3 DEFER + 5 DISMISS.** DEFER class (`deferred-work.md § Deferred from: code review RE-RUN of story-1.10 (2026-04-20)`): 2 reconfirmations of iter-50 DEFER entries (manifest description `font` omission; leafFontFamily string-vs-array portability) + 1 new story-spec editorial (Dev-Notes stable-ID illustration). DISMISS class: forward-compat `$def`s reframed as dead; section-ID-enumeration vs publishable-slug role conflation; frozen trace-artefact byte-count + line-pointer drift per iter-30 paper-trail rule; spec-intentional high-contrast mode asymmetry. § Review Findings iter-50 block: CR-1 PATCH checkbox flipped `[ ] → [x]` with iter-51 fix landed-narrative; § Review Findings iter-52 subsection appended. Story file Status `in-progress → done`; sprint-status.yaml `1-10: in-progress → done` + `last_updated: Story-1-10-in-progress → Story-1-10-done` (both header-comment + data-field). Story-file Change Log v1.7 row appended documenting closure. Zero source-code changes at iter-52; zero quality gates re-run (iter-51 landed clean-path exit 0 + typecheck ✓ + scoped keel-invariants build after the fix; iter-52 is pure-prose CR-re-verification). Halving-to-zero CR trajectory confirmed for Story 1.10: iter-50 (1 PATCH + 15 DEFER + 30 DISMISS) → iter-52 re-run (0 PATCH + 3 DEFER + 5 DISMISS). **Story 1.10 FR14n lifecycle: 11 iterations (iter-42..52)** — ~1.5× Story 1.8's 7-iter closure; consistent with a contract-only story that surfaced one convergent CR PATCH. PR #226 CI: `no checks reported`; push safe. Tracked at GitHub issue #34 — `Refs #34` in iter-52 commit trailer; `Closes #34` in PR body deferred until PR transitions Draft→Open at EPIC_DONE halt.

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1–1.10 done; 1.11–1.16 backlog → 6 more stories × full FR14n lifecycle remaining before EPIC_DONE)
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (continues through Epic 1; PR #226 stays Draft until EPIC_DONE per PROMPT_build.md step 5c)
- **Story:** _(no story)_ — next iter-53 runs `/bmad-create-story` to select Story 1.11 from sprint-status
- **Story File:** n/a
- **Story State:** _(no story)_ — FR14n matrix row 1 (`_(no story)_ → drafted`) triggers at iter-53
- **PR:** #226 (Draft, MERGEABLE, no-CI-checks-reported — stays Draft through Epic 1; Draft→Open at EPIC_DONE)

## Notes

- **Story 1.10 closure trajectory vs hypothesis:** Closed in 11 iterations (iter-42..52) with 2-iter SM layer (iter-47 finding + iter-48 fix + iter-49 re-verify) + 3-iter CR layer (iter-50 review + iter-51 fix + iter-52 re-run clean). Iter-7 Decisions' "contract-only trends single-pass CR" hypothesis partially vindicated (zero-PATCH iter-52 re-run matches single-pass-trend) while partially falsified at iter-50 (1 convergent PATCH vs Story 1.8's 0). Net pattern for Story 1.11+ budget: contract-authoring stories with minor pre-dev review gaps can expect 1-2-round SM + 1-2-round CR layers, NOT zero-round (Story 1.8's zero-round remains the only clean-at-both-layers precedent; Story 1.10 established the one-PATCH-per-layer precedent for more complex contract surfaces).
- **Carry-forward rules into Story 1.11 pre-dev SM review** (iter-43 + iter-47 three-point audit): (a) **Stable-ID cross-check** — grep AC exemplars vs Task subsections' schema model vs architecture ID pattern; fix drifters at `drafted → validated` not post-dev. (b) **Sprint-status transition wording** — rewrite BMad-template `backlog → in-progress → review` to Ralph direct-jump `ready-for-dev → done`. (c) **Task-enumeration-vs-schema-group-enumeration diff** — if Task N lists required `###` sections and schema lists required groups, diff the two lists; any schema-required group without a Task-required `###` is a pre-dev fix (not post-dev SM finding). This three-point audit prevents iter-47 two-round SM closure; restores single-round pattern. (d) **Anchor regex landmines** (Story 1.9 CR defers): new INVARIANTS.md sections mandate column-0 bullets, NOT indented; rationale-doc anchor examples MUST use inline backticks, NOT triple-backtick fences containing `INV-*` or `TOKEN-*` bullets.
- **Scoped-build optimisation** (iter-51): when mutating only `packages/keel-invariants/src/invariants.manifest.ts` (any hash update or new entry), run `pnpm --filter @keel/keel-invariants build` BEFORE `pnpm keel-invariants:check` — ~10× faster than full-workspace `pnpm -w build`; only the invariants package's `dist/check.js` needs regeneration. Full-workspace build only needed when fix spans multiple packages (e.g., both source + manifest sides of a shared type). Apply at Story 1.11+ fix-pending iterations.
- **Two-layer convergence doctrine** (iter-36 + iter-52 confirmation): a CR finding needs corroboration from ≥2 of the three layers (Blind Hunter / Edge Case Hunter / Acceptance Auditor) to earn PATCH promotion. Single-layer findings (especially Blind Hunter adversarial-prose overreach) collapse into DISMISS or DEFER. iter-52's 8 single-layer BH findings against 0 Edge + 0 Auditor findings → 0 PATCH clean outcome validates the doctrine across a second Epic 1 story (first was Story 1.9 iter-41 zero-out). Apply as default triage heuristic at Story 1.11+ CR rounds.
- **Issue Tracking:** Story 1.10 tracked at GitHub issue **#34**. `Refs #34` in iter-52 commit trailer. `Closes #34` deferred to PR body when PR #226 transitions Draft→Open at EPIC_DONE halt (not at individual story closure per PROMPT_build.md § Issue Tracking — GitHub native automation closes the issue on PR merge; ralph.py then transitions to Done). Parent Epic 1 issue **#9** — `Refs #9` optional; never `Closes` it (ralph.py transitions epic issues to Done on EPIC_DONE halt).
