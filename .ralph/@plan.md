# Implementation Plan

## NOW

- [ ] PATCH-17: AC2 verification matrix + Subtask 8.2 evidence-presentation "32 cumulative post-bootstrap ATDD-skips" → "32 cumulative project ATDD-skips (4 post-bootstrap: 1.17, 1.18, 1.20 hybrid, 1.21)" at story file lines 156 + 325 — categorical error (32 is project-cumulative; only 4 are post-bootstrap). Sweep all occurrences via `grep -n 'cumulative.*post-bootstrap.*ATDD-skip\|post-bootstrap.*ATDD-skip.*cited' <file>` AT FIX-TIME (per iter-427 carry-rule + iter-428 14th-class extension: when grep returns wrong-count, fall back to PATCH spec line range as ground-truth — line range is `[1-21-…md:156,325]`). **Note (iter-428 close-out):** Local commit `1bda4be` (PATCH-16) is UNPUSHED due to SSH-egress flake at iter-428 step 5 (18th datapoint of iter-372 SSH-egress class; 2 consecutive `ssh: connect to host github.com port 22: Connection timed out` per carry-rule first-retry-resolves does NOT always hold). At iter-429 step 0h: CI for SHA `6a53fec` already GREEN (will not re-trigger from in-flight CI); proceed with PATCH-17 work; step 5 will push both commits (PATCH-16 deferred + PATCH-17 new) alongside per standard deferred-push pattern. ~small

## QUEUE (Story 1.21 CR-pass-3 fix-arc → CR-pass-4 → Epic 1 reclose)

- [ ] Re-run `/bmad-code-review (args: "2")` — CR-pass-4 to confirm `fixes-pending → done` per FR14n matrix after PATCH-14 + PATCH-16 + PATCH-17 fix-arc complete. Forecast: 0–2 residual per iter-425 carry-rule (CR-pass-(N+1) forecast must NOT extrapolate "0 residual" from CR-pass-N's success rate against the carry-rule(s) known at CR-pass-N — convergence requires 2-consecutive-zero-PATCH CR passes). ~medium
- [ ] Re-close Epic 1 (`epic-1: in-progress → done` after Story 1.21 closes); transition PR #236 Draft→Open final CI gate per § PR Transition & Final CI Gate; on success: write `EPIC_DONE` halt; on next invocation post-merge § Cross-epic transition advances to next available epic OR writes `ALL_EPICS_DONE` if no next epic.

## BLOCKED

_(none)_

## ATDD Red Phase

_(empty — Story 1.21 ATDD-skipped iter-400 via FR14n § ATDD-skip pure ground (a) substrate-verification per IP § ATDD Skip Rationale; no red-phase tests authored.)_

## DONE (current story phase only — pruned at iter-428; older fix-arc detail in RALPH.md signposts)

- [x] iter-428: **PATCH-16 LANDED — 8 CR-pass-1 patch checkboxes flipped at story-file lines 438-445 + PATCH-16 self-flip at line 512.** FR14n `fixes-pending` UNCHANGED (1 PATCH still pending: PATCH-17). Audit-trail integrity restored: action-item checkbox state now matches content-state (content fixes for PATCH-1..8 all landed iter-409..419 per CR-pass-2 § Gate matrix line 498; only PATCH-9 checkbox flipped at iter-419 close-out). Story file gained `### v1.8 (iter-428 — 2026-04-26)` Change Log entry. **NEW iter-428 carry-rule landed in RALPH.md Lessons (14th class):** IP-planner operative-grep-pattern-vs-target-set reconciliation — when IP-planner sweep specs include BOTH an operative grep pattern AND a line range AND the grep returns wrong-count matches, fall back to the line range from the PATCH spec text as ground-truth (the IP-claimed grep `^- \[ \] \[Review\]\[Patch\] PATCH-[1-8]` returned 0 matches at iter-428; PATCH-1..8 checkboxes don't carry `PATCH-N:` label prefix because that convention started at PATCH-9 — the 8 stale checkboxes have descriptive text directly after `[Review][Patch]`. PATCH-16 spec line range `[1-21-…md:438-445]` was the operative ground-truth.). Extends iter-427 13th-class carry-rule (overcount via narrative/historical mismatch) to also cover undercount via mismatched pattern. Pre-push CI gate at step 0h: api.github.com class **43rd cumulative datapoint** sub-class (a) endpoint-split (graphql `gh pr checks 236` failed with i/o timeout to `140.82.121.6:443` again; LADDER step 2 REST `gh api repos/.../commits/<sha>/check-runs` cleared first-try; both checks PASS for SHA `6a53fec` iter-427 push: node + python = success; recovery streak now 4 iterations on REST endpoint after graphql failure pattern; intermittent-graphql-flake confirmed iter-426..428). All gates GREEN at step 4: typecheck 16/16 (FULL TURBO 307ms), lint 16/16 (FULL TURBO 172ms), keel-invariants 10 files / 52 tests (851ms; 4-drift baseline UNCHANGED — 0 NEW drift from cosmetic checkbox flips), pytest 4/4 (0.20s). Cumulative pre-merge PATCH Story 1.21 lifecycle: **24** projected UNCHANGED (PATCH-16 was already counted at iter-425 +3 CR-pass-3 raise; landing it does not change the projection). NOW advances to top QUEUE PATCH-17 (categorical wording fix at lines 156 + 325; sweep all occurrences via the iter-421 sweep-completion carry-rule). **Path forward:** PATCH-17 (iter-429) → CR-pass-4 (iter-430; forecast 0–2 residual per iter-425 carry-rule) → Epic 1 reclose + PR #236 Draft→Open final CI gate + EPIC_DONE halt → § Cross-epic transition on next invocation post-merge.

- [x] iter-427: **PATCH-14 LANDED — SCP § citation fix at story-file lines 30 + 155.** FR14n `fixes-pending` UNCHANGED (2 PATCH still pending: PATCH-16 + PATCH-17). Operative `replace_all` of `SCP § Section 4.2` → `SCP § 4.1` updated lines 30 (AC2 spec text) + 155 (Subtask 8.1 verification claim) — exactly the 2 sites named in PATCH-14 spec on line 511 (`[1-21-…md:30,155]`). PATCH-14 checkbox flipped `[ ]` → `[x]` at line 511. Story file gained `### v1.7 (iter-427 — 2026-04-26)` Change Log entry documenting the land + IP-vs-PATCH-spec divergence finding. **NEW iter-427 carry-rule landed in RALPH.md Lessons (13th class):** IP-planner sweep-count vs operative-grep reconciliation — when prior-iter IP claims K occurrences AND specifies the operative grep pattern, re-run the operative grep AT FIX-TIME and trust it as source-of-truth over IP's claimed count. At iter-427: IP claimed 4 sweep targets (lines 30, 155, 511, 545); operative grep `SCP § Section 4.2|per SCP § Section 4.2` returned 2 (lines 30 + 155). Lines 511 (PATCH description quoting `"§ Section 4.2"` with quotes + no SCP prefix — different pattern; touching it would self-corrupt to `"§ 4.1" → "§ 4.1"` nonsense) and 545 (Change Log v1.0 historical entry `SCP § 4.2` without "Section" — different pattern; preserved per repo Change Log convention chronicling iter-398 belief, with v1.6 + v1.7 documenting discovery + fix). Pre-push CI gate at step 0h: api.github.com class **42nd cumulative datapoint** sub-class (a) endpoint-split (graphql `gh pr checks 236` failed with i/o timeout to `140.82.121.6:443`; LADDER step 2 REST `gh api repos/.../commits/<sha>/check-runs` cleared first-try; both checks PASS for SHA `ff1b856` iter-426 push: node + python = success; recovery streak now 3 iterations on REST endpoint after graphql failure pattern). All gates GREEN at step 4: typecheck 16/16 (FULL TURBO 137ms), lint 16/16 (FULL TURBO 119ms), build cached 17/17 (246ms), keel-invariants 10 files / 52 tests (788ms; 4-drift baseline UNCHANGED — 0 NEW drift from cosmetic .md edits), pytest 4/4 (0.20s). Cumulative pre-merge PATCH Story 1.21 lifecycle: **24** projected UNCHANGED (PATCH-14 was already counted in iter-425 +3 CR-pass-3 raise; landing it does not change projection). NOW advances to top QUEUE PATCH-16 (audit-trail checkbox flips at lines 438-445; smallest-blast-radius among PATCH-16 + PATCH-17 since checkbox edits are mechanical with no narrative-text surface). **Path forward:** PATCH-16 (iter-428) → PATCH-17 (iter-429) → CR-pass-4 (iter-430; forecast 0–2 residual per iter-425 carry-rule) → Epic 1 reclose + PR #236 Draft→Open final CI gate + EPIC_DONE halt → § Cross-epic transition on next invocation post-merge.

- [x] iter-426: **CI monitoring iteration — PR #236 SHA `8aa3e4e` (iter-425 CR-pass-3 push) CI cleared at iter-426 step 0h.** node + python both PASS (node 1m11s, python 9s — first-try clear at LADDER step 1 `gh pr checks 236 --watch --fail-fast`). iter-425 pushed commit `8aa3e4e` with CI in-progress status; iter-426 NOW overridden by step 0h from `PATCH-14` → `Monitor PR CI` per Ralph build prompt § 0h ("Do not start new work while CI is running"). CI green → mark done → move PATCH-14 back to NOW for iter-427. Cumulative api.github.com network-flake class: 41 datapoints at iter-426 (40 pre-iter-426 + 1 NEW datapoint = `gh pr view --json` graphql endpoint i/o timeout to `140.82.121.6:443`; sub-class (a) endpoint-split, graphql-only — REST `gh pr checks --watch` had already cleared first-try at LADDER step 1 → fall-through-to-IP-state per existing carry-rule no work-blocking impact). Sweep prep work for iter-427 PATCH-14 OVERCOUNTED (claimed 4 occurrences at lines 30, 155, 511, 545 vs operative-grep-truth 2 at lines 30 + 155); reconciled at iter-427 fix-time per NEW iter-427 13th-class carry-rule.

- [x] iter-425: **Story 1.21 CR-pass-3 LANDED — 18+ findings raised across 3 parallel review layers; triaged into 3 PATCH + 1 DEFER + ~14 DISMISSED.** FR14n `fixes-pending → fixes-pending` (stays — 3 NEW PATCH raised; CR-pass-3 forecast "0 residual" was wrong per NEW carry-rule below). All 6 ACs reconfirmed satisfied at operative-test level. Pre-push CI gate at step 0h: api.github.com class 40th cumulative datapoint sub-class (a) endpoint-split (graphql failed twice, REST recovered first try at LADDER step 2 against SHA `3598847`) — both checks PASS for iter-424 PATCH-12 push. Story file gained `### CR-pass-3 (iter-425)` subsection (3 PATCH + 1 DEFER + dismiss list with rationales) + v1.6 Change Log entry. Deferred-work.md gained `## Deferred from: code review of story-1-21 CR-pass-3 (2026-04-26 iter-425)` section with the AC3 grep count drift entry. **3 NEW PATCH raised:** (PATCH-14) SCP § citation "§ Section 4.2" → "§ 4.1" at story file lines 30 + 155 — FR14n is a PRD amendment (SCP § 4.1); § 4.2 is Architecture amendments which does NOT contain FR14n; (PATCH-16) flip 8 stale CR-pass-1 PATCH checkboxes `[ ]` → `[x]` at lines 438-445; (PATCH-17) "32 cumulative post-bootstrap ATDD-skips" → "32 cumulative project ATDD-skips (4 post-bootstrap)" at lines 156 + 325 (categorical error). **1 NEW DEFER:** AC3 grep count drift "29" → "30" at story-file:128/164/326 + test-debt.md:400 (recursive self-replication). **NEW carry-rule landed iter-425:** CR-pass-(N+1) forecast must NOT extrapolate "0 residual" from CR-pass-N's success rate against the carry-rule(s) known at CR-pass-N — each CR-pass-N may surface NEW finding classes invisible at CR-pass-(N-1). Conservative forecast bound = max(0, |unique-finding-classes-not-yet-surfaced|), ~1–3 PATCH per CR-pass-N≥2 until 2-consecutive-zero-PATCH CR passes confirm convergence.

- [x] iter-422 + iter-423 + iter-424: PATCH-10 + PATCH-11 + PATCH-12 LANDED (3-PATCH CR-pass-2 fix-arc complete). PATCH-10 iter-422 line 233 ("23"→"24-DEFER" + "30 cross-links"→"27"); PATCH-11 iter-423 line 241 ("~30 cross-link edits"→"27"); PATCH-12 iter-424 line 174 dropped event-count deflection clause. iter-423 surfaced api.github.com 4-rung NO-recovery sub-class (d) 2nd surfacing — fall-through-to-work permitted via ORIGIN-SHA-already-GREEN clause; confirmed intermittent at iter-424 + iter-425 (LADDER step 1 + step 2 cleared on first try). Detail in RALPH.md signposts.

- [x] iter-421: **Story 1.21 CR-pass-2 PASS — 9 findings (3 PATCH + 1 DEFER + 5 DISMISSED).** FR14n `fixes-pending → fixes-pending` (stays — 3 NEW PATCH raised). **NEW carry-rule:** Sweep-completion carry-rule (1st datapoint of CR-pass-N sweep-completion class) — when CR-pass-N PATCH targets a count-drift, sweep ALL occurrences via `grep -n` before claiming done; otherwise CR-pass-(N+1) catches residuals. PATCH-10/11/12 are this rule's first surfacing.

- [x] iter-409..420: Story 1.21 CR-pass-1 fix-arc PATCH-1..9 (9 of 9 LANDED) + multi-deferred-push tracker drains. Audit + sweep class CR-pass-1 envelope datapoint 9 PATCH (mid-band of 0–9 forecast). 14 cumulative network-flake deferred-push datapoints across iter-372 SSH-egress class + 28 across api.github.com class. Detail in RALPH.md signposts.

- [x] iter-404: **Story 1.21 `/bmad-code-review (args: "2")` CR-pass-1 — 23 findings → 9 PATCH + 6 DEFER + 8 DISMISSED.** FR14n `sm-verified → fixes-pending`. All 6 ACs reconfirmed satisfied at operative-test level.

- [x] iter-401..403: Story 1.21 dev-story (iter-401 0 PATCH; FR14n `atdd-scaffolded → in-dev → review`) → trace (iter-402 0 PATCH; `in-dev → traced`) → SM-verify (iter-403 2 PATCH; `traced → sm-verified`).

- [x] iter-398..400: Story 1.21 drafted (iter-398) → SM-validated (iter-399 7 PATCH; `drafted → validated`) → ATDD-skipped pure ground (a) substrate-verification (iter-400; `validated → atdd-scaffolded`).

- [x] iter-1: `/bmad-correct-course` on issue #233 — Sprint Change Proposal; Epic 1 REOPENED for Stories 1.17–1.21.

## Context

- **Phase:** 4-implementation — Story 1.21 CR-pass-3 fix-arc OPEN; iter-428 PATCH-16 LANDED. Path: PATCH-17 (iter-429) → CR-pass-4 (iter-430).
- **Runtime:** cc-devbox iteration env. `uv 0.11.7` + `pnpm` available. Python 3.12.3 satisfies `requires-python = ">=3.10"`. github.com / api.github.com network access intermittent (api-egress-flake class — **43 cumulative datapoints** at iter-428 step 0h: REST `gh api repos/tthew/ralph-bmad/commits/<sha>/check-runs` cleared first-try LADDER step 2 against SHA `6a53fec` (node + python both green); 1 NEW datapoint at iter-428 = `gh pr checks 236` graphql i/o timeout to 140.82.121.6:443 — sub-class (a) endpoint-split, graphql-only; LADDER step 2 REST cleared first-try = recovery-streak-4 on REST endpoint after graphql failure; intermittent-graphql-flake confirmed iter-426..428); ssh-egress-flake class distinct (17 cumulative datapoints across iter-372..420b).
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (REOPENED for Stories 1.17–1.21).
- **Epic Branch (parent):** `feat/epic-2-packaged-devbox` — this work targets it as the PR base, not `main`.
- **Working Branch:** `chore/correct-course-test-runner-233` (based on `origin/feat/epic-2-packaged-devbox`).
- **Story:** 1.21 — Sweep prior `ATDD deferred` stories into `test-debt:` follow-ups.
- **Story File:** `_bmad-output/implementation-artifacts/1-21-sweep-prior-atdd-deferred-stories-into-test-debt-followups.md`.
- **Story State:** `fixes-pending` per FR14n § Story Lifecycle Decision Matrix `fixes-pending` row. **CR-pass-3 fix-arc OPEN** at iter-425 (PATCH-14 + PATCH-16 + PATCH-17 raised); PATCH-14 LANDED iter-427; PATCH-16 LANDED iter-428; PATCH-17 still pending. Per FR14n `fixes-pending` row exit condition: drain QUEUE PATCH items (one per iter), then re-run `/bmad-code-review (args: "2")` CR-pass-4 to confirm `done`. **NOW = PATCH-17 (categorical wording fix at lines 156 + 325; sweep all occurrences per iter-421 sweep-completion carry-rule).** **Path:** PATCH-17 (iter-429) → CR-pass-4 (iter-430; forecast 0–2 residual per iter-425 carry-rule) → Epic 1 reclose + PR #236 Draft→Open final CI gate + EPIC_DONE halt → § Cross-epic transition on next invocation post-merge.
- **GitHub Issue:** Issue #233 ("Course Correction: Bootstrap test runner + CI"). PR #236 (Draft, OPEN, base: `feat/epic-2-packaged-devbox`) carries `Refs #233`; `Closes #233` lands at Story 1.21 close-out.
- **PR:** #236 (Draft, OPEN, base: `feat/epic-2-packaged-devbox`).

## Notes

- **Story 1.21 forecast envelope (post-iter-428 PATCH-16 LANDED):** Cumulative pre-merge PATCH **24 projected UNCHANGED** (PATCH-16 was already counted at iter-425 +3 CR-pass-3 raise; landing it does not change projection). Stage breakdown: 7 SM-validate + 2 SM-verify + 9 CR-pass-1 + 3 CR-pass-2 + 3 CR-pass-3 = 24. Forecast residual: 1 PATCH at iter-429 + CR-pass-4 (forecast 0–2 residual per iter-425 carry-rule). Cumulative residual envelope post-iter-428: **24–26 final** (within 13–43 first-datapoint-of-class envelope; tracking toward upper-band as multi-CR-pass class extends).
- **NEW carry-rule iter-428 (14th class — IP-planner operative-grep-pattern-vs-target-set reconciliation):** extends iter-427 13th-class carry-rule. When IP-planner sweep specs include BOTH an operative grep pattern AND a line range, BOTH must be reconciled at fix-time. If the grep returns wrong-count (under or over), fall back to the line range from the PATCH spec text as ground-truth — the IP-claimed grep pattern can be malformed (e.g. PATCH-1..8 don't carry the `PATCH-N:` label prefix because that convention started at PATCH-9; the CR-pass-1 § Patch action items section opens at line 437 and the 8 checkboxes have descriptive text directly after `[Review][Patch]`). Reference incident: iter-428 PATCH-16 spec cited operative grep `^- \[ \] \[Review\]\[Patch\] PATCH-[1-8]` returning 0 matches; PATCH-16 spec line range `[1-21-…md:438-445]` was the operative ground-truth. Detail in RALPH.md Lessons.
- **NEW carry-rule iter-427 (13th class — IP-planner sweep-count vs operative-grep reconciliation):** when prior-iter IP claims K sweep occurrences AND specifies the operative grep pattern, re-run the operative grep AT FIX-TIME and trust it as source-of-truth over IP's claimed count. IP planners (especially CI-monitor iters doing sweep prep) may overcount by including (a) the PATCH-spec narrative quoting the bad string with quotes/different prefix (would self-corrupt) or (b) historical Change Log entries with similar-but-different patterns (preserved per repo convention). Reference incident: iter-426 IP claimed 4 sweep targets for PATCH-14 (lines 30, 155, 511, 545); operative grep returned 2 (lines 30 + 155 only); reconciled at iter-427. Detail in RALPH.md Lessons.
- **CR-pass-(N+1) forecast carry-rule (iter-425; extension of iter-421 sweep-completion):** CR-pass-(N+1) forecast must NOT extrapolate "0 residual" from "100% success rate of CR-pass-N PATCH against the carry-rule(s) known at CR-pass-N". Each CR-pass-N may surface NEW finding classes invisible at CR-pass-(N-1); CR-pass-3 surfaced 3 NEW finding classes (audit-trail-checkbox + cross-reference + categorical-wording) none of which were known at CR-pass-2. Conservative forecast bound = max(0, |unique-finding-classes-not-yet-surfaced|), ~1–3 PATCH per CR-pass-N≥2 until 2-consecutive-zero-PATCH CR passes confirm convergence.
- **Sweep-completion carry-rule (iter-421; precedent applied at iter-422/423/424 with 100% success rate over 3 PATCH on the count-drift axis ONLY):** When CR-pass-N PATCH targets a count-drift, sweep ALL occurrences via `grep -n '<old-value>' <file>` before claiming PATCH-N done. PATCH-10/11/12 all applied this rule cleanly with no missed parallel sites on the count-drift axis. Scope: count-drift axis only; NOT a general defence against off-axis CR-pass-(N+1) findings. PATCH-17 will apply this rule to its sweep target (categorical wording pattern); PATCH-16 is mechanical checkbox flip with no sweep needed.
- **Audit + sweep class envelope CALIBRATED at iter-425 CR-pass-3 (6 datapoints):** dev-story 0-PATCH + trace 0-PATCH + SM-verify 2-PATCH + CR-pass-1 9-PATCH + CR-pass-2 3-PATCH + CR-pass-3 3-PATCH (mid-band 0–6). Future audit + sweep stories inherit envelope dev-story 0–3 / trace 0–2 / SM-verify 1–4 / CR-pass-1 0–9 / CR-pass-N≥2 0–4 with NEW iter-425 carry-rule on convergence forecasting + NEW iter-427 carry-rule on IP-planner trust calibration.
- **api.github.com network class (43 cumulative datapoints across iter-397..428):** Sub-classes by endpoint affected: (a) endpoint-split graphql-only (incl iter-426 + iter-427 + iter-428 — recovery-streak-4 on REST endpoint; intermittent-graphql-flake confirmed iter-426..428 = 3-consecutive-iter graphql-fail-with-REST-recovery); (b) joint-endpoint cross-iter (defer push); (c) joint-endpoint within-iter recovery; (d) **4-endpoint within-iter joint-flake NO-recovery** (iter-415 NEW; 2 surfacings iter-415 + iter-423; intermittent confirmed at iter-424..428). LADDER carry-rule: (1) `gh pr checks <PR> --watch --fail-fast`; (2) REST `gh api repos/<owner>/<repo>/commits/<sha>/check-runs`; (3) graphql `gh pr checks <PR>` one-shot; (4) defer push to next iter OR fall-through to work iff ORIGIN SHA was already proven GREEN at a PRIOR iter and no new push has intervened.
- **Course-correction precedent stack 3 deep:** issue #231 (PR #234 MERGED) → issue #232 (PR #235 MERGED) → issue #233 (PR #236 in-flight; pending Story 1.21 close + Draft→Open final CI gate). Issue #233 is the first to REOPEN a closed epic.
- **Epic 4 hard-precondition:** Epic 4 (per-iteration security verification) extends `packages/keel-invariants/` with secret/SAST/prompt-injection scanners. Stories 1.19 + 1.20 backfill IS LANDED + DONE; Epic 4 unblock requires Story 1.21 + Epic 1 reclose.
- **iter-372 SSH-egress flake carry-rule (17 cumulative datapoints across iter-372..420b):** kill-after-hang + retry-fail sub-class. First-retry-resolves carry-rule does NOT always hold.
- **Iter-358 INV-git-hooks-preservation worktree-mode drift carry-rule (carry-forward to Epic 4 hardening):** Story 1.17 + 1.18 + 1.19 + 1.20 + 1.21 disposition (c) carry-forward unchanged. 4-drift sync-gate baseline persists.
