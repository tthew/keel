# Implementation Plan

## NOW

- [ ] Run `/bmad-testarch-atdd` (fresh context) per § Story Lifecycle Decision Matrix row `validated → atdd-scaffolded`. Story 2.8 validated at iter-228 with 4 PATCHes (SC-1 pre-flight attribution, SCRIPT_DIR removal, SC-3 shim-count off-by-1, SC-12/PSN phantom deferred-work reference). FR14n matrix row 3 skip expected (eighteenth cumulative precedent); ground-(c) variant "external-service-owns-behavior-under-test" per story § Testing Standards. Scaffolder should produce an ATDD-skip artefact with three-ground conjunction: (a) substrate-verification via Task 5 stub-docker smokes, (b) no runner wired at substrate level yet, (c) upstream Anthropic CLI + external OAuth endpoint own AC 1–5 behaviors. Sprint-status UNCHANGED at skip-marker endpoint per iter-222 precedent.

## QUEUE (Story 2.8 lifecycle → Story 2.9..2.17 → Epic 2 close-out)
- [ ] _(post-Story-2.8-atdd-scaffolded)_ `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-8-claude-code-oauth-via-pnpm-claude.md")` per § Story Lifecycle row `atdd-scaffolded → in-dev`. Implementation follows v1.1 spec verbatim: Task 1 authors `packages/devbox/scripts/claude-host.sh` per post-PATCH template (no SCRIPT_DIR; `docker info --format '{{.ServerVersion}}'` tighter variant; `exec docker exec -it --user dev -w /workspace "${CONTAINER_NAME}" claude "$@"`); Task 2 wires `"claude"` into root package.json after `ralph:plan` (line 36) before `prepare` (line 37); Task 3 appends README H2 sibling after `## Ralph loop (Story 2.7)` at line 505; Task 4 appends AGENTS.md H3 sibling after `### Ralph loop (Story 2.7)` at line 114; Task 5 iteration-env-safe stub-docker smokes (Smokes 1–4); Task 6 housekeeping (no deferred-work.md entry per v1.1 PATCH 4). Story State `atdd-scaffolded → in-dev`; sprint-status `ready-for-dev → in-progress → review`.
- [ ] _(post-Story-2.8-in-dev)_ `/bmad-testarch-trace (args: "yolo")` per § Story Lifecycle row `in-dev → traced`. WAIVED forecast per § Testing Standards three-ground conjunction (substrate-smoked envelope + no runner + external-service ground-(c) variant). 4 artefacts per Story 2.7 iter-224 template: `2-8-coverage-matrix.json` + `2-8-e2e-trace-summary.json` + `2-8-gate-decision.json` + `2-8-claude-code-oauth-via-pnpm-claude.md` traceability report. Story State `in-dev → traced`; sprint-status UNCHANGED.
- [ ] _(post-Story-2.8-traced)_ `/bmad-create-story (args: "review")` per § Story Lifecycle row `traced → sm-verified` — post-dev SM requirements-satisfaction verification. Story 2.7 iter-225 pattern: ZERO-PATCH if v1.1 post-PATCH spec tightened drift latitude to zero + dev-story landed verbatim. Forecast: ZERO unmet-AC findings (first-data-point for single-script Epic-2-auth-class story; 4 iter-228 PATCHes sharpened the spec further). Story State `traced → sm-verified`; sprint-status UNCHANGED.
- [ ] _(post-Story-2.8-sm-verified)_ `/bmad-code-review (args: "2")` per § Story Lifecycle row `sm-verified → done | fixes-pending`. Forecast: 1–3 PATCH opener + 0 closure re-run; candidate for SECOND one-pass ZERO-PATCH precedent per Story 2.7 iter-226 first-precedent, strengthened by iter-228's 4-PATCH SM-gate pass (narrower drift surface at CR gate). Story State `sm-verified → done` (no findings) OR `fixes-pending` (if CR surfaces any PATCH). Sprint-status `review → done` at closure.
- [ ] _(post-Story-2.8-done; through Story 2.17-done)_ Iterate Stories 2.9..2.17 through full lifecycle — 9 remaining substrate stories spanning gh OAuth + prereq-check + shared mode + port-publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-22 per iter-219 pre-flight + iter-227 confirmation — "no checks reported on the 'feat/epic-2-packaged-devbox' branch").

## BLOCKED

_(none)_

## ATDD Red Phase

_(none — Story 2.7 closed ZERO-PATCH one-pass at iter-226; Story 2.8 validated at iter-228 without a runner. ATDD skip will be applied at Story 2.8 `validated → atdd-scaffolded` gate per FR14n matrix row 3 eighteenth cumulative precedent.)_

## DONE (iter-228 Story 2.8 VALIDATED — `drafted → validated`, 4 PATCHes, v1.1)

- [x] iter-228: **STORY 2.8 VALIDATED — Story State `drafted → validated`; sprint-status UNCHANGED; 4 PATCHes applied via pre-dev SM adversarial triage.** `/bmad-create-story (args: "review")` ran fresh-context per one-skill-per-context rule with Blind Hunter + Edge Case Hunter parallel Sonnet-subagent fan-out. **Blind Hunter verdict: ZERO-DRIFT on all 29 file:line references** (README.md:410/:505, AGENTS.md:104/:114, Dockerfile:111-120/:251-252/:268-270, compose:86-92/:109-113/:156/:158-159/:170-171, package.json:36, shell.sh:32-35/:42, attach.sh:32-36/:39, architecture.md:74-79/:489/:506/:991-1004, epics.md:1142-1165/:1157/:1417-1447, prd.md:929/:1082, INVARIANTS.md:94/:100/:106 all verified). **Edge Case Hunter + direct inspection verdict: 4 PATCHes.** PATCH 1 — SC-1 pre-flight attribution corrected: Story 2.8 inherits Story 2.7's `docker info --format '{{.ServerVersion}}'` tighter variant (ralph-build-host.sh:36 precedent; catches reachable-but-broken-daemon where ServerVersion is missing from server info output), NOT Story 2.6's bare `docker info >/dev/null 2>&1`. The draft's "duplicates Story 2.6 boilerplate verbatim" text was imprecise. PATCH 2 — Task 1 + template SCRIPT_DIR removal: SC-4's no-auto-start posture means no `"${SCRIPT_DIR}/start.sh"` sub-invoke, so SCRIPT_DIR is dead code in Story 2.8. Story 2.7's ralph wrappers DO use it (ralph-build-host.sh:29+46 sub-invoke). Story 2.8 deliberately does NOT inherit — removing prevents cargo-culting at future amendment time; template + Task 1 updated with explanatory SC-4 comment. PATCH 3 — SC-3 host-side-shim count off-by-1: "16 other host-side shims" → "15"; "16 shims at Story 2.7 landing → 17 at Story 2.8 landing" → "15 host-side shims at Story 2.7 landing [13 Story 2.6 + 2 Story 2.7] → 16 after Story 2.8 lands". SC-14's accounting at line 104 already had correct numbers (15→16); SC-3 drifted. Reconciled + directory-listing citation added. PATCH 4 — SC-12 + Project Structure Notes phantom deferred-work reference removed: draft claimed a `deferred-work.md § Deferred from: Story 2.8 draft iter-227` entry for the stale compose:156 TODO, but (a) entry was never written at iter-227, (b) deferred-work.md § header line 3 scopes entries to CR-triage-surfaced items not inaction-under-SC posture, (c) the compose:156 TODO is self-documenting (grep-discoverable via `grep 'TODO.*Story 2\.' packages/devbox/docker-compose.yml`). Forward-reference replaced with correct "no tracking entry required — TODO self-documents; deferred-work.md is CR-only scope" phrasing. **Change Log v1.1 written at top of Change Log section.** Status field UNCHANGED at `ready-for-dev` (the downstream sprint-status-facing field; Story State is an orthogonal Ralph-internal lifecycle marker per § Story Lifecycle Decision Matrix). **SM-PATCH-density: 4 PATCHes (vs Story 2.7 iter-221's 5 PATCHes)** — above the 3-PATCH floor articulated in IP iter-227 Notes as "if Story 2.8 pre-dev SM produces <3 PATCHes, it signals SM under-engagement." Pattern preserved — Story 2.7 one-pass ZERO-PATCH CR precedent remains a viable forecast for Story 2.8 CR gate. Budget consumed iter-228: ~75K (orient ~10K; story file + source artefact reads ~20K; 2 parallel adversarial subagents ~25K; 4 PATCH Edit calls + Change Log v1.1 ~10K; IP + commit ~10K remaining).

- [x] iter-227: **STORY 2.8 DRAFTED — Story State `_(no story) → drafted`; sprint-status `backlog → ready-for-dev`; Status field `ready-for-dev`.** `/bmad-create-story` ran fresh-context per one-skill-per-context rule. Auto-discovered Story 2.8 as first backlog row in sprint-status (`2-8-claude-code-oauth-via-pnpm-claude`). Exhaustive artefact analysis: epics.md:1417-1447 (Story 2.8 AC verbatim + Epic 2 preamble :1142-1165); prd.md:929 (FR3 verbatim) + :1082 (NFR10 verbatim); architecture.md:74-79 (host surface contract + one-time auth prerequisites) + :489 (pnpm claude CLI table row) + :506 (Claude auth persistence contract); Dockerfile:111-120 (`@anthropic-ai/claude-code@2.1.116` install) + :246-252 (`/home/dev/.claude/` placeholder create) + :255-270 (non-root dev user UID 1000); docker-compose.yml:86-92 + :170-171 (named volume `keel_home_dev` at `/home/dev` — subsumes `/home/dev/.claude/` persistence); shell.sh + attach.sh Story 2.6 interactive-exec + fail-closed-exit-9 precedents; Story 2.7 full lifecycle (iter-220..226) for CR-ZERO-PATCH + stub-docker-harness + SC-17 read-only patterns + ATDD-skip ground-(c) variant scaffolding. **Story file artefacts:** 5 ACs verbatim from epics; 6 tasks (claude-host.sh + package.json + README H2 + AGENTS.md H3 + smoke tests + housekeeping); 17 SCs (SC-1 composability + SC-2 upstream-CLI-owns-auth + SC-3 file placement + SC-4 no-auto-start + SC-5 exit-code-schema + SC-6 no-TTY-gate + SC-7 compose prereqs + SC-8 unset-COMPOSE + SC-9 args passthrough + SC-10 no-signal-trap + SC-11 container-name + SC-12 no-compose-edits + SC-13 Dockerfile-placeholder + SC-14 lib-deferral + SC-15 no-token-inspection + SC-16 no-help + SC-17 read-only-prior-sections). Shim structure template: 41-line bash with `unset COMPOSE_PROJECT_NAME` + dual pre-flight (docker info → exit 8; container-state → exit 9 no-auto-start) + `exec docker exec -it --user dev -w /workspace ${CONTAINER_NAME} claude "$@"` (args passthrough). Sprint-status flipped `backlog → ready-for-dev` with `last_updated: 2026-04-22 Story-2-8-ready-for-dev-iter-227 UTC`.

## Context

- **Phase:** 4-implementation — Epic 2 at **7/17 stories done** (2.1 + 2.2 + 2.3 + 2.4 + 2.5 + 2.6 + 2.7 as of iter-226 ZERO-PATCH one-pass CR closure) + 9/17 backlog (2.9..2.17) + 1/17 ready-for-dev (2.8 at iter-228 validated with 4 PATCHes).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.8 AC 1-5 live OAuth flow smokes operator-workstation-deferred per Story 2.4 SC-17 + Story 2.5 iter-187 + Story 2.6 iter-201/204/216/219 + Story 2.7 iter-223/225/226 precedent cluster.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` baked at Dockerfile:119). Story 2.5 substrate live. Story 2.6 host-side CLI live (13 verbs). Story 2.7 ralph-*-host.sh shims live (2 verbs; iter-223 landing). Story 2.8 claude-host.sh queued (iter-228 validated → iter-229+ ATDD-skip → iter-230+ implementation).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **7 done** (2.1-2.7); 1 validated (2.8 at iter-228); 9 backlog (2.9..2.17). Epic 2 closes at Story 2.17 (PR #230 Draft→Open transition at that point).
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.8 — `claude-code-oauth-via-pnpm-claude` (validated iter-228; 5 ACs + 6 tasks + 17 SCs + v1.1 Change Log entry with 4 PATCHes documented).
- **Story File:** `_bmad-output/implementation-artifacts/2-8-claude-code-oauth-via-pnpm-claude.md`.
- **Story State:** `validated` — next iter action per § Story Lifecycle Decision Matrix is `/bmad-testarch-atdd` (fresh context) for FR14n matrix row 3 skip application; Sprint-status UNCHANGED at skip endpoint.
- **GitHub Issue:** Story 2.8 TBD (GH issue wire may/may-not exist — `RALPH_ISSUE_NUMBER` env inspection at next iter start will resolve; if unset, skip `Refs #<story-issue>` in commits per § Issue Tracking block). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **iter-228 pre-dev SM PATCH density = 4 (Story 2.7 iter-221 was 5):** Pattern preserved. Above the 3-PATCH floor; below the 5-PATCH Story 2.7 precedent. The tight gap validates that dense SC pinning at draft time (17 SCs) reduces but does not eliminate SM-review patch yield — the SM gate is still producing meaningful drift correction. Carry-forward forecast: if Story 2.9's pre-dev SM produces <3 PATCHes under an equally-dense draft, it would weaken confidence in one-pass ZERO-PATCH CR closure; ≥3 PATCHes maintains the pattern.
- **iter-228 new lesson — "phantom deferred-work forward-reference" anti-pattern (PATCH 4):** Story 2.8 draft claimed a `deferred-work.md` tracking entry that was never written. Drafts should avoid forward-referring to artefacts that don't yet exist unless the artefact's creation is explicitly part of the same iteration's work. Carry-forward: pre-dev SM adversarial triage should include a "forward-reference resolution check" — grep the draft for every path reference and verify it points to extant content (for paths that should exist at draft time) or flag the creation obligation (for paths deferred to dev-story). Applies to Stories 2.9+.
- **iter-228 new lesson — "cargo-cult variable detection" (PATCH 2):** Story 2.8 template initially inherited SCRIPT_DIR from Story 2.7's ralph-wrappers template, but Story 2.8's SC-4 no-auto-start means no sub-invoke — SCRIPT_DIR had no consumer. Template-copying across adjacent stories tends to preserve variables that lose their purpose under divergent scope decisions. Carry-forward: pre-dev SM triage should audit every variable declaration in shim templates against actual downstream use. Applies to Stories 2.9 (gh-auth) + 2.13 (healthcheck) + 2.10 (prereq-check) shims.
- **iter-228 new lesson — "attribution precision in source-claim text" (PATCH 1):** Story 2.8 SC-1 claimed "duplicates Story 2.6 boilerplate verbatim" when in fact Story 2.8 inherited Story 2.7's tightened variant. Stories in an inheritance chain should attribute precisely — imprecise attribution leads to dev-agent confusion at implementation time ("wait, shell.sh doesn't use `--format '{{.ServerVersion}}'`; is the story wrong?"). Carry-forward: attribute to the NEAREST upstream (Story 2.7 here) not the ULTIMATE upstream (Story 2.6) when the nearest has already refactored. Applies to all future inheritance-chain stories.
- **iter-227 draft-density target holds (17 SCs match Story 2.7 post-PATCH density):** Per Story 2.7 iter-226 LESSON, dense SC pinning at draft time amortises through all three downstream gates (post-dev SM + CR + trace-WAIVED artefact density). Story 2.8 shipped 17 SCs at draft time — iter-228 SM review added text to SC-1, SC-3, SC-12 and 4 PATCHes total; SC count remains 17. Forward-looking: post-dev SM at iter-229+n expected to produce ZERO PATCHes (v1.1 spec is post-adversarial-triage tight).
- **iter-227 new FR14n matrix row 3 ground-(c) variant "external-service-owns-behavior-under-test":** Extends Story 2.7's "downstream-epic-owns-behavior-under-test" (Epic-3-scope) with a sibling variant for upstream-CLI + external-endpoint-scope auth-flow class. Applies to Story 2.8 (Claude; Anthropic CLI + OAuth endpoint) + Story 2.9 (gh; GitHub CLI + OAuth endpoint).
- **iter-227 ralph-host-shim count extension (16 host-side shims total at Story 2.8 landing):** 13 Story 2.6 verbs + 2 Story 2.7 ralph wrappers + 1 Story 2.8 claude-host = 16. `_lib.sh` refactor threshold (Story 2.6 AR-19) still not tripped.
- **Within-epic auto-advance works cleanly for Story 2.7 → 2.8 transition (iter-227 CONFIRMATION):** Prior execution validates "Story State `done` + Epic has backlog → `/bmad-create-story` fresh context" sequence. Third cumulative within-epic auto-advance precedent.
- **Defer-with-rationale > PATCH-everything for pre-existing parser edges (iter-219 CARRY-FORWARD):** Heuristic applies at Story 2.8 CR gate: if fix is 1-2 lines AND trigger requires unusual operator config AND operator workaround is documented → DEFER with Carry-to pointer.
- **TTY-detect at docker-exec shim (iter-214 LESSON, Story 2.8 NON-APPLICATION CONFIRMED iter-227):** Story 2.8 does NOT apply the TTY-detect gate to its `docker exec` call because the OAuth flow IS operator-interactive (SC-6 explicit). Same posture as Story 2.7's `docker attach` (SC-8).
- **`<verb>-host.sh` host-side shim naming convention (iter-199 EXTENDED, iter-219 3-PASS-CR-VERIFIED, iter-223 LANDING CONFIRMATION, iter-227 EXTENSION, iter-228 VALIDATED):** Story 2.8 ships `claude-host.sh` under `packages/devbox/scripts/` at iter-230+n, extending the pattern to 16 host-side shims total. Stories 2.9/2.12 remain candidates for the same pattern.
