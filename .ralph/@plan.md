# Implementation Plan

## NOW

- [ ] **Story 2.16 `/bmad-code-review (args: "2")` CR** (`sm-verified → done` OR `fixes-pending`). Per § Story Lifecycle Decision Matrix row `sm-verified`. Forecast 1-4 PATCH per iter-301 NOVEL LESSON CANDIDATE (doc-heavy narrow-diff CR drift) + Story 2.16 wider doc-touch surface (12 files vs Story 2.15's 8); explicit subagent-budget guidance per iter-301 LESSON: **enumerate the 12 touched files in Blind Hunter prompt upfront** to keep token consumption < 100K per subagent (D-16 mitigation). Touched files for Blind Hunter scope: `.claude/hooks/block-secret-access.sh` + `.claude/settings.json` + `.ralph/config.toml` + `docs/invariants/claude-hook-denylist.md` + `packages/keel-invariants/src/invariants.manifest.ts` + `INVARIANTS.md` + `AGENTS.md` + `CLAUDE.md` + `packages/devbox/README.md` + `packages/keel-templates/src/seeds/.claude/hooks/block-secret-access.sh` + `packages/keel-templates/src/seeds/.claude/settings.json` + `packages/keel-templates/README.md` (12 files; the story file itself MUST NOT be in the review scope per adversarial-review hygiene — the story IS the spec under review, not a review subject).

## QUEUE (Story 2.16 lifecycle + 2.17 substrate queue)

- [ ] _(after Story 2.16 done)_ Story 2.17 full lifecycle — hook-settings bypass-resistance + Epic 2 SC-17 close-out polish pass. SC-17 reconciliation absorbs cumulative DEFER queue at iter-307 close: **55 cumulative Epic-2 DEFERs** (58 at Story 2.15 CR close MINUS 3 absorbed at Story 2.16 hook layer: D-1/D-3/D-6 via recursive globs + prefix-anchor case-globs + cat-parity rules) + 3 SC-17 close-out candidates (D-7/D-8/D-9 from iter-306 trace) = **58 DEFERs pending** + whatever Story 2.16 CR iter-308 accrues. Story 2.17 bypass-resistance scope MAY codify pre-install `bash -n` discipline per iter-305 NOVEL LESSON (hook self-immolation mitigation).
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured; `statusCheckRollup: []` carried unchanged iter-219..307).

## BLOCKED

_(none — iter-307 push expected on first try per iter-302..306 consecutive first-try success baseline — 5 data points; iter-301 NOVEL LESSON CANDIDATE SSH :22 retry budget extension PROMOTED TO § Lessons STABLE if iter-307 push succeeds first-try — 6 data points = promotion-eligible threshold.)_

## ATDD Red Phase

_(none — Story 2.16 `/bmad-testarch-atdd` SKIP-WITH-GROUNDS-(ii) single-ground waiver landed at iter-304; dev-story iter-305 landed with 14 impl-time fixture smokes serving as adversarial-coverage substitute; trace iter-306 confirmed ground-(a) functional verification for all 8 ACs; post-dev SM iter-307 ZERO-PATCH confirmed all 8 ACs substrate-verified against actual implementation.)_

## DONE (iter-307 `/bmad-create-story (args: "review")` post-dev SM LANDING — Story State `traced → sm-verified`)

- [x] iter-307: **STORY 2.16 `/bmad-create-story (args: "review")` post-dev SM LANDING** (`traced → sm-verified`). **ZERO PATCH.** All 8 ACs VERIFIED-SATISFIED against actual implementation: inline verification (`pnpm keel-invariants:check-all` silent success; invariant-doc SHA256 matches manifest `85f8a539c0850f1c52ed825c6a8a904d72c6d42c0c7a87eb9f14617bc51cd7e1`; substrate↔seed byte-identity for hook + settings.json; manifest count 35; settings.json shape `{hooks,permissions}` + deny=13 + allow=6 + PreToolUse=6 + matchers=`Bash,Edit,Glob,Grep,Read,Write`; `.ralph/config.toml [hooks].self_protection_halt_threshold = 3` present + not gitignored; 8 functional smokes all GREEN including JSONL append schema, secret-access blocks (env/cat-envrc/read-oauth-token), hook-self-protection blocks (Edit hook-file/git `--no-verify`), approve-path (package.json + `.envrc.example` exempt)) + two-subagent fan-out (doc-landing audit Tasks 5/6/7/8c + invariant-doc structure + Change Log narrative + Completion Notes AC mapping) both CLEAN. Subagent 2 raised one PATCH candidate about Change Log 0.4 git-no-verify pattern wording — **false positive** (subagent read story file's Task 1 copy-ready skeleton rather than actual hook at `.claude/hooks/block-secret-access.sh:49-51`; Change Log 0.4's regex AND-combo description is empirically ACCURATE). **Cumulative lifecycle PATCH unchanged at 3** (4-data-point narrow-after-SM-absorb pattern across iter-303/305/306/307 — all post-pre-dev-SM iterations ZERO-PATCH; confirms iter-300 Story 2.15 precedent generalises despite wider 12-file doc surface). Story file Status HTML comment flipped `in-dev → traced` → `traced → sm-verified`; Change Log 0.6 row appended (chronological order restored via Python swap after Edit ordering error). Sprint-status row UNCHANGED at `review` per FR14n convention + Story 2.15 iter-300 precedent — post-dev SM does NOT flip sprint-status. Source SHA `efde582` (trace commit). ZERO PATCH (policy-only delta). Commit candidate: `docs(story-2-16): iter-307 /bmad-create-story (args: "review") post-dev SM — traced → sm-verified; ZERO-PATCH; 4-data-point narrow-after-SM-absorb pattern confirmed`.

_(iter-302 / iter-303 / iter-304 / iter-305 / iter-306 LANDING detail pruned per Guardrail 2; retained in commits `d9e031b` / `441d710` / `7c89ff1` / `128e1b7` / `efde582` + story-file Status HTML comment chain + story-file Change Log 0.1-0.6 rows.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **15/17 stories done** (2.1-2.15) + 2.16 `sm-verified` at iter-307 + 2.17 backlog.
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live smokes operator-workstation-deferred.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:121; bash + jq baked at :36-37 + :48 — load-bearing for Story 2.16 hook script functional verification confirmed at iter-307 smokes). Host-side shim count: **18** at iter-307 (unchanged — post-dev SM iter; no devbox-shim substrate code edits).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **15 done** (2.1-2.15); **1 sm-verified** (2.16); **1 backlog** (2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.16 — Claude PreToolUse hooks for secret-file denylist (Ralph-compatible). **SM-VERIFIED at iter-307** (post-dev SM ZERO-PATCH; all 8 ACs substrate-verified against actual implementation).
- **Story File:** `_bmad-output/implementation-artifacts/2-16-claude-pretooluse-hooks-for-secret-file-denylist-ralph-compatible.md`
- **Story State:** `sm-verified` — Sprint-status row `2-16-…: review` UNCHANGED per FR14n convention + Story 2.15 iter-300 precedent. Next iter (iter-308) picks up `sm-verified → /bmad-code-review (args: "2")` per § Story Lifecycle Decision Matrix row `sm-verified`.
- **GitHub Issue:** Story 2.16 issue unknown; `RALPH_ISSUE_NUMBER` unset (no GitHub Project configured). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` unchanged iter-272..307).

## Notes

- **iter-307 OBSERVATION — 4-data-point narrow-after-SM-absorb ZERO-PATCH pattern (promotion-eligible to stable LESSON):** Story 2.16 post-dev SM ZERO-PATCH at iter-307 extends the three-data-point pattern (Story 2.15 iter-300 + Story 2.16 dev-story iter-305 + Story 2.16 trace iter-306) to four. Pattern: narrow-diff doc-heavy stories where the pre-dev SM pass absorbs substantive findings (Story 2.15 5-PATCH, Story 2.16 3-PATCH) can drain the iter-286 forecast envelope entirely at pre-dev SM, leaving 0 PATCH at every subsequent lifecycle gate (ATDD/dev-story/trace/post-dev-SM). Story 2.17's post-dev SM iteration would be the 5-data-point threshold for STABLE LESSON promotion; if iter-308 Story 2.16 CR absorbs 1-4 PATCH (per iter-301 CR-drift NOVEL LESSON CANDIDATE), that's independent of the narrow-after-SM-absorb pattern (CR drift is prose-level drift across doc landings, distinct from AC-satisfaction drift at post-dev SM). Cumulative Story 2.16 lifecycle PATCH at iter-307 = 3 (all from pre-dev SM iter-303); Story 2.15 cumulative at equivalent gate = 5 + 4 CR = 9.
- **iter-307 NOVEL LESSON CANDIDATE — subagent-doc-verification scope discipline:** During the iter-307 SM audit, subagent 2 raised a false-positive PATCH candidate about Change Log 0.4's git-no-verify pattern wording — the subagent read the story file's Task 1 copy-ready skeleton (which shows plain `*"--no-verify"*` case-glob) rather than the actual hook file `.claude/hooks/block-secret-access.sh:49-51` (which uses the bash-regex AND-combo `[[ "$bash_command" =~ ^git[[:space:]]+(commit|push) ]] && [[ "$bash_command" == *--no-verify* ]]`). Change Log 0.4's description was accurate against the shipped hook; the subagent's critique was against the spec skeleton. Future-Ralph rule: for subagent prompts that verify documentation accuracy claims against implementation, explicitly enumerate BOTH (a) the spec-document-of-record AND (b) the actual implementation artefact file + line range. Prompt language: "Verify the claim at <doc-file:line> against the actual implementation at <impl-file:line>; do NOT infer the final behaviour from the story file's copy-ready skeleton (which may differ from the shipped artefact in intermediate recovery scenarios)." Promotion-eligible to RALPH.md § Lessons after ONE more subagent-doc-verification iteration (Story 2.17 post-dev SM is the next candidate). Tracking as NOVEL LESSON CANDIDATE pending replication.
- **iter-307 PRESERVED carry-forward — iter-305 NOVEL LESSON (hook self-immolation):** Pre-install `bash -n` discipline — for ANY story that installs a PreToolUse hook with self-protection rules (Story 2.17 bypass-resistance expansion, Epic 3 Story 3.32 hook-self-protection halt-wire-up), validate the hook's bash syntax via `bash -n` BEFORE registering it in `.claude/settings.json` `hooks.PreToolUse`. Install the hooks.PreToolUse registration ONLY after `bash -n` GREEN on the hook script. Recovery via `Monitor` tool escape-hatch if discovered mid-iter. **Story 2.17 consideration:** codifying the pre-install `bash -n` discipline as a committed pre-commit hook (distinct from `.git/hooks/**` which is covered by hook-self-protection) would close the loop mechanically.
- **iter-307 PRESERVED carry-forward — iter-301 NOVEL LESSON CANDIDATE (doc-heavy narrow-diff CR drift):** Story 2.15 cumulative lifecycle PATCH = 9 (5 pre-dev SM + 4 CR) — exceeded iter-286 NOVEL LESSON 3-6 forecast band upper edge by +3. Pattern: pre-dev SM absorbs substantive surface findings; prose-level drift emerges ONLY at CR when all doc-landings are in place + cross-refs empirically testable. Story 2.16 cumulative at iter-307 = 3 (all pre-dev SM at iter-303; iter-304/305/306/307 ZERO-PATCH). Story 2.16 has WIDER doc surface than Story 2.15 (12 files vs 8); iter-308 CR forecast 1-4 PATCH = cumulative 4-7 (below Story 2.15's cumulative 9); if CR absorbs 4+, Story 2.16 cumulative would approach Story 2.15's — pattern would generalise. If iter-308 CR ZERO-PATCH, that would UNDERCUT the pattern (narrow-diff doc-heavy CR drift is specific to Story 2.15 and does not generalise).
- **iter-307 PRESERVED carry-forward — iter-301 NOVEL LESSON CANDIDATE (SSH :22 egress retry budget extension):** iter-302..306 orient `gh pr view 230` succeeded first try (no flake — 5 data points); iter-307 push outcome TBD at commit time. **Six-data-point first-try success baseline** if iter-307 push succeeds first try → PROMOTION TO § Lessons STABLE threshold met. Carry-forward posture: budget 4-5 retries with exponential pacing for `git push` under DinD backend B; do NOT escalate to halt after three.
- **iter-307 PRESERVED carry-forward — iter-301 subagent-budget LESSON for Story 2.16 CR (iter-308):** Blind Hunter spent ~150K tokens + 58 tool_uses exploring the repo at Story 2.15 CR — exceeded budget; should have been scoped tighter to diff-only reads of the touched files. Carry-forward for Story 2.16 CR (iter-308): explicitly list the 12 touched files in the Blind Hunter prompt (enumerated in NOW section above); enumerate touched files upfront rather than relying on agent to discover via `git diff --stat`.
- **Cumulative DEFER forecast for Stories 2.16..2.17 Epic-2 close-out (updated at iter-307):** 58 cumulative Epic-2 DEFERs at iter-301 Story 2.15 CR close (47 carry-forward from Story 2.14 CR + 11 from Story 2.15 CR) MINUS 3 absorbed at Story 2.16 hook layer (D-1/D-3/D-6 mitigated at iter-305, VERIFIED at iter-307 post-dev SM) = **55 cumulative Epic-2 DEFERs at iter-307 post-dev SM close** + 3 SC-17 close-out candidates (D-7/D-8/D-9 from iter-306 trace) = **58 DEFERs pending Story 2.17** + whatever Story 2.16 CR iter-308 accrues. Story 2.17 scope-inherent closes most; final SC-17 absorption at Story 2.17 CR close ~55-60 absorbed.
