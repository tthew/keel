# Implementation Plan

## NOW

- [ ] **PATCH-6 (HIGH; entrypoint.sh:151-210)** — rewrite the SC-10 subshell from current `( ... ) || diag` to `set +e; (set -e; ...); rc=$?; set -e; [[ rc -ne 0 ]] && diag` rc-capture pattern. Iter-277 CR re-run #1 EMPIRICALLY DISCONFIRMED PATCH-3's stated mechanism via in-iter `bash 5.2.21` reproduction (`set -euo pipefail; ( false; echo X ) || echo Y` outputs `X`, NOT `Y` — bash(1) `||`-LHS rule disables errexit inside subshell, recursive into explicit `set -e` AND `if !` form AND `inherit_errexit`). Pre-spawn failures cascade silently → PATCH-4's `gosu dev rm -rf host_keys` runs unconditionally → destructive host-key destruction when keygen fails. Comment block at L141-150 lies about mechanism. Update comment block to describe actual mechanism (rc-capture pattern + WHY `( ) || diag` doesn't work + reference to iter-277 PATCH-6 empirical reproduction).

## QUEUE (Story 2.12 fixes-pending → 1 PATCH remaining after iter-277)
- [ ] _(after PATCH-6)_ Re-run `/bmad-code-review (args: "2")` — closure re-run #2 (`fixes-pending → done | fixes-pending` per § Story Lifecycle Decision Matrix). Per iter-264 LESSON Story 2.6/2.11 multi-pass cycle, budget TWO closure re-runs at worst case; iter-277 was re-run #1 (1-PATCH outcome — within iter-271 forecast band 0-2 PATCH); iter-279 re-run #2 forecast ZERO-PATCH (PATCH-6 is single-region rewrite per iter-264 precedent). NOTE: re-run scope should narrow to PATCH-6's `entrypoint.sh:151-210` rewrite + comment-block update (~1 file ~50 lines diff); 0-1 PATCH likely.
- [ ] _(after Story 2.12 done)_ Stories 2.13..2.17 — full-lifecycle iteration through 5 remaining substrate stories (healthcheck 2.13 + legacy branch retention 2.14 + committed settings 2.15 + hooks 2.16 + bypass-resistance 2.17). Each story full lifecycle; no cross-epic transitions until Epic 2 final story (2.17) completes.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-24 per iter-219..276 confirmation; iter-277 orient `gh pr view` GREEN — no HTTPS :443 timeout this iter; push via SSH :22 expected clean).

## BLOCKED

_(none — branch in sync with origin at iter-277 orient at `43b4e4b`.)_

## ATDD Red Phase

_(none — iter-267 ATDD-skip-with-grounds-(c)+(ii)+(iii) per FR14n; 22nd-cumulative ATDD-skip precedent. Impl-time smokes at iter-268 dev-story cover ACs 1-3; ACs 4-5 operator-workstation-deferred.)_

## DONE (iter-277 Story 2.12 CR closure re-run #1 LANDING — `fixes-pending → fixes-pending`; PATCH-6 queued)

- [x] iter-277: **STORY 2.12 CR CLOSURE RE-RUN #1 LANDED — `/bmad-code-review (args: "2")` (`fixes-pending → fixes-pending`; 1 PATCH (PATCH-6) + 9 DEFER + 3 DISMISS).** Three-layer Ralph-hosted adversarial fan-out (Blind Hunter `general-purpose` diff-only, 12 raw 3H/3M/6L; Edge Case Hunter `general-purpose` diff+project-read, 7 raw 2H/1M/4L; Acceptance Auditor `bmad-agent-architect` Winston diff+spec+context, 2 LOW + ZERO-PATCH verdict). Diff scope: PATCH-only commits `30b7d8d~1..HEAD` = 15 files / 476 lines / 227 insertions / 97 deletions (narrowed to substrate code + invariant doc + manifest; excluded process artifacts).

  - **PATCH-6 (HIGH; entrypoint.sh:151-210; 4-of-4 convergent across Blind+Edge layers + Ralph in-iter empirical disconfirmation of Winston ZERO-PATCH verdict):** Bash 5.2 (project version) `( ... ) || diagnostic` form does NOT scope `set -e` inside the subshell per the bash(1) `||`-LHS recursive rule. Empirically reproduced in-iter: `bash -c 'set -euo pipefail; ( false; echo X ) || echo Y'` outputs `X`, NOT `Y`. PATCH-3's stated mechanism (entrypoint.sh:147-150 comment block) lies. Pre-spawn failures cascade silently; PATCH-4's `gosu dev rm -rf /home/dev/.ssh/host_keys` runs unconditionally, destructively wiping host keys when keygen fails. Even explicit `set -e` inside subshell, `if ! ( ... )` form, and `shopt -s inherit_errexit` ALL reproduce the same suppression. ONLY `bash -c '...' || diag` subprocess and `set +e; (set -e; ...); rc=$?; set -e; [[ $rc -ne 0 ]] && diag` patterns escape the suppression. Fix: rewrite entrypoint.sh:151-210 with the rc-capture pattern (canonical form in iter-277 § Review Findings § PATCH-6 of story spec) + update comment block to describe actual mechanism + WHY `( ) || diag` doesn't work + reference iter-277 empirical reproduction.

  - **9 DEFER** logged in `_bmad-output/implementation-artifacts/deferred-work.md § Deferred from: code review of story-2.12 iter-277 confirmation gate (2026-04-24)`: cosmetic indent (1) + PATCH-4 line-pin drift (1) + helper future-guards x3 (COMPOSE_FILE under set -u + COMPOSE_ARGS namespace + helper file-existence) + raw-compose docs gap (1) + devbox-ssh.md example COMPOSE_FILE missing (1) + devbox-hardening.md cap_add drift (1, pre-existing) + stderr ordering (1). Class breakdown: 8 → Story 2.17 close-out; 1 → Story 2.17 invariant-doc reconciliation.

  - **3 DISMISS:** PATCH-3 backgrounded sshd dies when subshell exits (incorrect mental model — sshd reparents to entrypoint PID 1); PATCH-1 chmod target may not exist on first boot (Blind Hunter misread; verified mkdir at L155 runs before chmod at L165); manifest contentHash unverifiable (Edge Hunter ran sha256sum + verified hash matches `946f1ac1…907029` exactly).

  - **NEW LESSON (iter-277 NOVEL — bash subshell `||`-LHS errexit suppression is recursive into explicit re-enable AND `if !` form):** the bash(1) `||`-LHS rule that disables errexit "for any command executed in a `&&` or `||` list except the command following the final `&&` or `||`" applies RECURSIVELY into subshells. Empirically reproduced 4 forms: `(...) || diag` (broken), `( set -e; ...) || diag` (broken — explicit re-enable suppressed), `if ! ( ... ); then diag; fi` (broken), `shopt -s inherit_errexit; (...) || diag` (broken — flag is for `$()` not `()`). ONLY working forms: (1) `bash -c '...' || diag` separate subprocess; (2) `set +e; (set -e; ...); rc=$?; set -e; [[ $rc -ne 0 ]] && diag` rc-capture without `||`. Generalises: ALL "wrap risky block in `(...) || diag` for non-fatal posture" patterns in any future Story 2.13..2.17 / Epic 6 / Epic 13 substrate work need the rc-capture pattern OR `bash -c` subprocess. Promoted to RALPH.md § Lessons + § Signposts at iter-277.

  - **NEW LESSON (iter-277 META — empirical disconfirmation of Acceptance Auditor positive control):** Winston's ZERO-PATCH verdict was based on the (incorrect) assumption that `set -e` propagates inside the subshell. He reads the comment block at L141-150 as ground truth and assumes the documented mechanism works. Without empirical bash-test execution, the verdict was structurally unable to catch this. Generalises: when an Acceptance Auditor verdict CONTRADICTS Blind+Edge layers' convergent finding on a runtime-semantics question, FAVOUR the convergent finding + RUN AN EMPIRICAL TEST in the iteration before triaging. Three-layer convergence is high-confidence-PATCH-class, but a positive control overrides only when the positive-control basis is empirically grounded. Carry-forward: any future CR finding that turns on a runtime-semantics question (`set -e` scope, signal handling, race conditions, IPC ordering) MUST have an in-iter empirical reproduction step before the Auditor verdict is allowed to override Blind/Edge. Promoted to RALPH.md § Lessons at iter-277.

  - **Forecast band reconciliation:** iter-271 forecasted "0-2 PATCH (CR-fix-introduced regression class)" at the re-run; PATCH-6 lands at the upper bound (1 PATCH). Per iter-264 LESSON Story 2.6/2.11 multi-pass cycle, budget TWO closure re-runs at worst case → iter-277 is closure re-run #1; iter-279 closure re-run #2. Cumulative Story 2.12 PATCH count = 5 (iter-271) + 1 (iter-277) = 6 — within iter-271 LESSON 4-7 PATCH band for novel-runtime-behaviour stories. Story 2.12 will be the THIRD multi-pass-CR-cycle story in Epic 2 (precedents: Story 2.6 — 2 closure re-runs; Story 2.11 — 1 closure re-run; Story 2.12 — 1+1 = 2 closure re-runs forecast).

  - **Budget consumed:** ~62K tokens (orient ~10K + read spec + layouts ~9K + 3 subagents in parallel ~9K driver overhead + bash empirical reproduction ~3K + spec edits + Change Log ~10K + deferred-work edit ~5K + IP + RALPH.md updates + commit ~16K, stacking). Stayed within ~117K execution budget; exit cleanly per Guardrail 12.

  - **Story State:** `fixes-pending → fixes-pending` (PATCH-6 queued; closure re-run #1 outcome was 1-PATCH not 0-PATCH; one more closure re-run pending).

  - **PR:** #230 **Draft** — `statusCheckRollup: []` carries unchanged across iter-272..277; iter-277 orient `gh pr view 230` GREEN (no HTTPS :443 timeout this iter; iter-263 asymmetric-recovery LESSON didn't fire). Branch in sync with origin at `43b4e4b`. Pre-push state: about to commit + push the spec + deferred-work + IP + RALPH.md updates (no source code changes this iter; this is a CR triage iteration).

- [x] iter-276: **STORY 2.12 PATCH-5 LANDED — bash-array compose-args helper + 15-site refactor across 8 scripts + 4-doc idiom update + story DEFER-6 AR-9-citation correction.** See commit `43b4e4b`.
- [x] iter-275: **STORY 2.12 PATCH-4 LANDED** — `packages/devbox/entrypoint.sh:176-187` partial-keypair recovery via `rm -rf host_keys` before `mv -T`. See commit `820591a`. _(NOTE iter-277: PATCH-4's recovery becomes destructive cascade under PATCH-3's broken `set -e`; PATCH-6 fix re-establishes errexit so destructive `rm` no longer reaches under failed-keygen path.)_
- [x] iter-274: **STORY 2.12 PATCH-3 LANDED** — subshell-wrap opt-in block for SC-10 non-fatal pre-spawn posture. See commit `0d83fae`. _(NOTE iter-277: PATCH-3's stated mechanism EMPIRICALLY DISCONFIRMED — `( ) || diag` does NOT scope `set -e`. PATCH-6 fixes via rc-capture pattern.)_
- [x] iter-273: **STORY 2.12 PATCH-2 LANDED** — compose env via KEEL_DEVBOX_SSH_RESOLVED. See commit `1df64ab`.
- [x] iter-272: **STORY 2.12 PATCH-1 LANDED** — chmod under cap_drop via gosu dev. See commit `30b7d8d`.
- [x] iter-271: `/bmad-code-review (args: "2")` — `sm-verified → fixes-pending`; 5 PATCH + 11 DEFER + ~10 DISMISS; three-layer adversarial fan-out. See commit `a777224`.

_(iter-253..270 Story 2.10/2.11/2.12 closure + Story 2.12 drafting/pre-dev-SM/ATDD/dev-story/trace-gate/post-dev-SM iters pruned per Guardrail 2.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **11/17 stories done** (2.1-2.11) + 1/17 fixes-pending (2.12, 1 PATCH-6 remaining; closure re-run #2 forecast at iter-279) + 5/17 backlog (2.13..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered through iter-277 (PATCH-5 landed; PATCH-6 not yet — 1 NOW task; static smokes GREEN; `pnpm keel-invariants:check` GREEN at `946f1ac1…907029`).
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:120). Host-side shim count: **18** at iter-277 (unchanged — PATCH-6 will edit existing entrypoint.sh, no shim count change).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **11 done** (2.1-2.11); 1 fixes-pending-CR (2.12); 5 backlog (2.13..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.12 — Loopback-bound port publication + opt-in `KEEL_DEVBOX_SSH` sshd
- **Story File:** `_bmad-output/implementation-artifacts/2-12-loopback-bound-port-publication-opt-in-keel-devbox-ssh-sshd.md`
- **Story State:** `fixes-pending` — PATCH-1..5 LANDED iter-272..276; iter-277 CR re-run #1 added PATCH-6 (4-of-4 convergent + empirical bash disconfirmation); iter-278 lands PATCH-6 sole NOW task; iter-279 closure re-run #2 forecast ZERO-PATCH.
- **GitHub Issue:** Story 2.12 issue unknown; `RALPH_ISSUE_NUMBER` unset at iter-277 orient. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` carried unchanged iter-272..277; iter-277 orient `gh pr view 230` GREEN).

## Notes

- **iter-277 NOVEL LESSON #1 (bash subshell `||`-LHS errexit suppression is recursive):** the bash(1) `||`-LHS rule applies RECURSIVELY into subshells. Empirically reproduced 4 forms: `(...) || diag` (broken), `( set -e; ...) || diag` (broken — explicit re-enable suppressed), `if ! ( ... ); then diag; fi` (broken), `shopt -s inherit_errexit; (...) || diag` (broken — flag for `$()` not `()`). ONLY working forms: `bash -c '...' || diag` subprocess; `set +e; (set -e; ...); rc=$?; set -e` rc-capture without `||`. Generalises to ALL "wrap risky block for non-fatal posture" patterns in any future Story 2.13..2.17 / Epic 6 / Epic 13 substrate work. Promoted to RALPH.md § Lessons + § Signposts.
- **iter-277 NOVEL LESSON #2 (META — empirical disconfirmation of Acceptance Auditor positive control):** when an Acceptance Auditor verdict CONTRADICTS Blind+Edge layers' convergent finding on a runtime-semantics question, FAVOUR the convergent finding + RUN AN EMPIRICAL TEST in the iteration before triaging. Three-layer convergence is high-confidence-PATCH-class, but a positive control overrides only when the positive-control basis is empirically grounded. Carry-forward: any future CR finding that turns on a runtime-semantics question (`set -e` scope, signal handling, race conditions, IPC ordering) MUST have an in-iter empirical reproduction step before the Auditor verdict overrides Blind/Edge. Promoted to RALPH.md § Lessons.
- **iter-276 NOVEL LESSON carry-forward (hazard-class discrimination under word-splitting):** `${VAR:+-f "${VAR}"}` alt-value expansion re-tokenises by word-splitting BEFORE embedded double-quote chars re-parse. AR-9 citation removed at iter-276 PATCH-5 landing.
- **iter-276 observation carry-forward (contentHash edit cost model):** editing a contentHash-tracked source forces a 4-step cost (edit → sha256sum → manifest patch → `pnpm --filter @keel/keel-invariants build` → `pnpm keel-invariants:check`). Pre-budget for larger contentHash-tracked edits. iter-277 did NOT touch any contentHash-tracked source (CR triage only).
- **iter-275 / 274 observations:** GNU `mv -T` over non-empty target dir refuses (rm-then-mv recovery is idiomatic); subshell-wrap-with-||-diagnostic is canonical NON-FATAL pattern under `set -euo pipefail`. NOTE iter-277: the second observation is REVISED — the canonical pattern is the rc-capture form, NOT `(...) || diag` (the latter silently breaks errexit propagation per iter-277 NOVEL LESSON #1).
- **iter-273 NOVEL LESSON carry-forward (compose env-propagation sites are normalisation chokepoints):** any `docker-compose.yml` env var sourced from `.envrc` via `${VAR:-default}` MUST source the RESOLVED host-side value when a case-folding resolver exists.
- **iter-272 LESSON carry-forward (orient-time PATCH order decision framework):** evaluate (a) verification-bar parity, (b) Guardrail 5 clean-ness, (c) downstream reduction, (d) independence. iter-277 confirms linear PATCH-1..5 → PATCH-6 order remains correct (PATCH-6 emerges at re-run, downstream of PATCH-3 + PATCH-4 landing — could not have been forecast at iter-272).
- **iter-271 NOVEL LESSON carry-forward (forecast band breach via novel-runtime-behaviour surface):** 4-7 PATCH band for novel-runtime-behaviour stories; Story 2.12 cumulative count: 5 (iter-271) + 1 (iter-277) = 6 (within band). Iter-277 PATCH-6 is itself a NOVEL meta-runtime-behaviour finding (bash semantics) that even Winston's positive-control approach failed to catch — strengthens the carry-forward to Stories 2.13 (healthcheck), 6.x RLS debugger, Epic 13 CI harness smokes.
- **iter-271 LESSON carry-forward (three-subagent pattern reaffirmed):** Blind / Edge / Auditor parallel-fan-out; convergence-signal-high-confidence-triage; iter-277 used the SAME pattern + ADDED in-iter empirical reproduction step (NEW LESSON #2 above) when Auditor disconfirmed Blind+Edge. Pattern evolution: three-subagent + empirical-test override.
- **iter-270 NOVEL LESSON carry-forward (drift-band re-baseline):** substrate-citation drift BIMODAL — substantive 0-2 + mechanical 8-12 per dev-pass-touches-≥4-cited-files story. iter-277 added 1 new substantive PATCH (PATCH-6) — within band.
- **iter-264 LESSON carry-forward:** CR re-run DEFER-vs-PATCH severity-class-first; 0-3 first-class PATCH band for STATIC-SUBSTRATE stories; 4-7 band for NOVEL-RUNTIME-BEHAVIOUR stories. Story 2.12 cumulative 6 within band.
- **iter-263 LESSON carry-forward:** SSH :22 / HTTPS :443 asymmetric recovery; iter-277 HTTPS :443 GREEN (no asymmetric recovery needed this iter).
- **iter-257 LESSON reaffirmed (no contentHash edit at iter-277):** since iter-277 is CR-triage-only (no source code changes), no contentHash sync-gate concerns; `pnpm keel-invariants:check` carried GREEN from iter-276.
- **iter-244/246 audit-findings carry-forward:** restart.sh transitive-delegate; benchmark.sh OUT-OF-SCOPE (SC-11) threading compose-override; Manifest entry count **32** at iter-277 (unchanged from iter-276).
- **Substrate-citation drift cumulative forecast for Stories 2.13..2.17:** ~5-12 cumulative substantive + ~40-60 cumulative mechanical drifts. SC-17 single reconciliation at Story 2.17 landing. Story 2.12 cumulative drift count: ZERO new at iter-277 (no substrate edits this iter); 9 NEW DEFERs queued for Story 2.17 close-out (mostly cosmetic / future-guards / docs polish).
