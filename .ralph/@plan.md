# Implementation Plan

## NOW

- [ ] **PATCH-1 fix task:** wrap `chmod 0700` (`packages/devbox/entrypoint.sh:146`) and `chmod 0600` (`:162`) with `gosu dev` prefix to avoid CAP_FOWNER EPERM under `cap_drop: [ALL]`. Two-line edit. Mirrors mkdir/keygen/mv pattern in same block (dev owns the files → no CAP_FOWNER needed). Per § Story Lifecycle Decision Matrix row `fixes-pending → fixes-pending` until QUEUE empties; per Guardrail 5 ONE PATCH per iter. Forecast: PATCH-1 lands in iter-272 + smokes to verify (sha256 unchanged on entrypoint.sh / bash -n / sync-gate GREEN unchanged); Story State stays `fixes-pending`.

## QUEUE (Story 2.12 fixes-pending → 5 PATCHes from iter-271 CR)

- [ ] **PATCH-2 fix task:** change `packages/devbox/docker-compose.yml:145` env propagation from `KEEL_DEVBOX_SSH: ${KEEL_DEVBOX_SSH:-false}` to `KEEL_DEVBOX_SSH: ${KEEL_DEVBOX_SSH_RESOLVED:-false}` to fix host/container case-sensitivity drift (`True`/`TRUE` → port published but no sshd starts). One-line code change + comment update at entrypoint.sh:132-136 to point at `KEEL_DEVBOX_SSH_RESOLVED` instead of "honour the container-side env var" framing.
- [ ] **PATCH-3 fix task:** wrap entire opt-in block (`packages/devbox/entrypoint.sh:138-176`) in subshell with `|| true` + diagnostic emit to honour SC-10 non-fatal posture for pre-spawn failures. Folds in PATCH-1 (chmod via gosu) + PATCH-2 (KEEL_DEVBOX_SSH_RESOLVED) + PATCH-4 (rm-then-mv) cleanly per CR Review Findings § PATCH-3 canonical fix template. **Optimisation hint:** PATCH-3 supersedes PATCH-1 + PATCH-4 if landed first; consider order swap (PATCH-3 first, PATCH-1/PATCH-4 close as `[x] subsumed by PATCH-3`). Decision deferred to iter-272 orient.
- [ ] **PATCH-4 fix task:** replace `gosu dev mv -T host_keys.tmp host_keys` (`packages/devbox/entrypoint.sh:157`) with `gosu dev rm -rf /home/dev/.ssh/host_keys && gosu dev mv -T /home/dev/.ssh/host_keys.tmp /home/dev/.ssh/host_keys` for partial-keypair recovery scenario. Or fold into PATCH-3's subshell rewrite (`gosu dev rm -rf /home/dev/.ssh/host_keys.tmp /home/dev/.ssh/host_keys` upfront). Two-line change in isolation.
- [ ] **PATCH-5 fix task:** refactor 15 sites across 8 scripts (`build/rebuild/start/stop/status/clean/logs/benchmark.sh`) from unquoted `${VAR:+-f "${VAR}"}` to bash array idiom via NEW `packages/devbox/scripts/lib/compose-args.sh` helper. Crosses Story 2.6 AR-19 / Story 2.7 SC-14 deferred `_lib.sh` extraction threshold for the compose-args concern specifically. Update Story 2.12 Dev Notes DEFER-6 to remove misapplied AR-9 citation (different word-splitting hazard class). Larger refactor (~1 hour wall + smoke verification across all 8 shims); pre-budget per Guardrail 12 XL-task decomposition rule — may decompose into helper-create + per-shim-update sub-PATCHes.
- [ ] _(post-QUEUE-empty)_ **Re-run `/bmad-code-review (args: "2")`** for `fixes-pending → done | fixes-pending` confirmation gate. Per iter-264 LESSON Story 2.6 / 2.11 multi-pass CR cycle (Story ≥10-scripts-class), budget TWO closure re-runs at worst case. Forecast at re-run: 0-2 PATCH (CR-fix-introduced regression class — Story 2.6 iter-216 + 2.11 iter-264 precedents) + 0-3 LOW DEFER cosmetic carry-forwards.
- [ ] _(Stories 2.13..2.17)_ Iterate through full lifecycle — 5 remaining substrate stories spanning healthcheck (2.13) + legacy branch retention (2.14) + committed settings (2.15) + hooks (2.16) + bypass-resistance (2.17). Each runs full lifecycle; no cross-epic transitions until Epic 2 final story (2.17) completes.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-24 per iter-219..270 confirmation; iter-271 expects same — push via SSH unaffected per iter-263 asymmetric-recovery LESSON).

## BLOCKED

_(none — branch in sync with origin at iter-271 orient at `c2bb378`; no SSH :22 carry-forward.)_

## ATDD Red Phase

_(none — iter-267 ATDD-skip-with-grounds-(c)+(ii)+(iii) per FR14n; 22nd-cumulative ATDD-skip precedent. Impl-time smokes at iter-268 dev-story cover ACs 1-3 per Story 2.11 iter-257 dev-agent pattern; ACs 4-5 operator-workstation-deferred.)_

## DONE (iter-271 Story 2.12 `/bmad-code-review (args: "2")` LANDING — `sm-verified → fixes-pending`)

- [x] iter-271: **STORY 2.12 CR LANDING — `sm-verified → fixes-pending` (Ralph internal FR14n state); sprint-row stays `review` across CR-gate window per iter-202 + iter-260 precedent (flips to `done` only at CR closure with all PATCHes applied).** Three-layer Ralph-hosted adversarial fan-out per § BMad Workflows: Blind Hunter (`general-purpose` diff-only — 24 raw, 3 HIGH/9 MEDIUM/12 LOW) + Edge Case Hunter (`general-purpose` diff+project-read — 27 raw, 1 HIGH/10 MEDIUM/16 LOW) + Acceptance Auditor (`bmad-agent-architect` Winston diff+spec+context — 12 raw, 2 HIGH/2 MEDIUM/8 LOW). Diff range `77f1748..HEAD -- ':!.ralph/' ':!RALPH.md'` (Story 2.12 substrate scope) = 2161 lines / 26 files / 1788 insertions.
  - **Triage:** **5 PATCH + 11 DEFER + ~10 DISMISS** (post-dedup/convergence). All 5 PATCHes are HIGH/MEDIUM convergent across ≥2 reviewer layers (chmod CAP_FOWNER: Auditor solo HIGH but explicitly verified against Dockerfile L261-262 author note + capabilities(7) spec; KEEL_DEVBOX_SSH propagation: Blind+Edge+Auditor 3-of-3 convergent; set -e halt: Blind+Edge+Auditor 3-of-3 convergent; mv -T recovery: Blind+Edge+Auditor 3-of-3 convergent; ${VAR:+...} paths-with-spaces: Blind+Edge 2-of-3 convergent on idiom mis-mitigation). iter-260 LESSON convergence-signal-high-confidence-triage applies.
  - **Forecast band breach:** 5 PATCH lands ABOVE iter-270 SM ZERO-PATCH carry-forward + iter-264 LESSON 0-3 first-class PATCH band — driven by iter-266 NOVEL LESSON's "novel-runtime-behaviour" surface (sshd-host-key bootstrap + cap_drop posture interaction + entrypoint set -e discipline) which static smokes (sha256 / bash -n / sync-gate / 15-site tally) cannot exercise. iter-270 SM verified all 5 ACs PASS via static smokes only; PATCH-1 through PATCH-4 only manifest at first live container exec under cap_drop:[ALL] which iter-270 had NO EXECUTION PATH to.
  - **PATCH list (queued at TOP of QUEUE per § Story Lifecycle):**
    - **P-1 (HIGH; entrypoint.sh:146 + :162)** — chmod under cap_drop:[ALL] requires gosu dev prefix; root EPERMs without CAP_FOWNER → set -e abort entrypoint → container fails to start with KEEL_DEVBOX_SSH=true.
    - **P-2 (HIGH; docker-compose.yml:145)** — KEEL_DEVBOX_SSH host/container normalisation drift; propagate KEEL_DEVBOX_SSH_RESOLVED instead of raw value.
    - **P-3 (HIGH; entrypoint.sh:138-176)** — set -e halts entrypoint on opt-in SSH init pre-spawn failures; SC-10 non-fatal only covers post-spawn liveness check. Wrap whole block in subshell + diagnostic emit; folds in P-1+P-2+P-4 cleanly.
    - **P-4 (MEDIUM-HIGH; entrypoint.sh:141 + :157)** — `mv -T host_keys.tmp host_keys` fails when destination dir is non-empty (partial-keypair recovery); rm-then-mv fix.
    - **P-5 (MEDIUM; 15 sites across 8 scripts)** — `${VAR:+-f "${VAR}"}` paths-with-spaces hazard; refactor to bash array idiom via new `lib/compose-args.sh` helper. Story 2.12 Dev Notes citation of RALPH.md AR-9 is misapplied.
  - **11 DEFERs logged** in story file `### Review Findings` + `_bmad-output/implementation-artifacts/deferred-work.md § Deferred from: code review of story-2.12 (2026-04-24)`. Class breakdown: 5 → Story 2.17 close-out; 1 → first live operator-workstation smoke verification (PrivSep chroot); 1 → Story 2.13 healthcheck would surface loudly; 1 → post-Epic 2 hardening; 1 → Story 2.18+ if added; 2 → cumulative LOW operator-edges sweep at SC-17.
  - **~10 DISMISS:** AGENTS.md H3 nesting (MECH-DRIFT-ALREADY-TRIAGED iter-270 SM); manifest description size vs siblings (non-load-bearing metadata); Dockerfile chmod-after-COPY no-ops; LogLevel/SyslogFacility dead-code under -e flag; mv -T GNU-extension portability (Debian/Ubuntu base); AGENTS.md cross-ref anchor drift (precedent-consistent); 8 mechanical line-shift drifts (pre-triaged at iter-270); trace artifacts process-class concerns (WAIVED at iter-269); pnpm devbox:rebuild regenerates-keys claim (contradicted by named-volume persistence); SFTP discoverability concern (duplicate of D2 SFTP defer).
  - **NEW LESSON (forecast band breach via novel-runtime-behaviour surface):** when a story introduces multi-layer dynamic-runtime interactions (sshd lifecycle × cap_drop posture × set -e discipline × resolver-to-entrypoint env-var propagation), iter-264 LESSON severity-class-first rule's 0-3 first-class PATCH band breaks because static smokes have NO EXECUTION PATH to dynamic-runtime defects. iter-270 SM ZERO-PATCH verdict was structurally unable to catch PATCH-1 through PATCH-4 because all 4 only manifest at first live container exec under cap_drop:[ALL]. **Forecast adjustment:** stories with novel-runtime-behaviour surface (sshd, healthcheck, dynamic resolver-to-runtime propagation) should pre-budget 4-7 first-class PATCH at CR even with clean SM gates. Promoted to RALPH.md § Lessons + § Signposts at iter-271. Carry-forward applies to Stories 2.13 (healthcheck — runtime liveness signalling), 6.x RLS debugger (multi-process orchestration), Epic 13 CI harness smokes (live execution against substrate).
  - **Three-subagent pattern reaffirmed:** Blind Hunter / Edge Case Hunter / Acceptance Auditor parallel-fan-out per § BMad Workflows. Iter-260 multi-layer convergence-signal-high-confidence-triage rule applies. Sub-conversation context isolation kept Ralph context lean — only condensed verdicts (~6K total) reached IP § DONE despite the 50K+ raw subagent token usage.
  - **Story file Status update:** front-matter `Status: review` UNCHANGED (sprint-row stays `review`); HTML annotation block updated `sm-verified → fixes-pending` with iter-271 CR summary. Change Log v1.5 entry recorded; v1.4 + v1.3 entries also added (post-hoc capture of iter-270 SM + iter-269 trace gates that landed without Change Log updates per Story 2.11 iter-260 precedent — CR closure consolidates intermediate state-advance Change Log entries).
  - **Next iter (iter-272):** PATCH-1 (chmod under cap_drop) as sole NOW task per Guardrail 5; QUEUE order PATCH-1 → PATCH-2 → PATCH-3 (folds in 1+2+4) → PATCH-4 (likely subsumed by PATCH-3) → PATCH-5 (compose-args helper extraction). Decision on PATCH-3-first vs PATCH-1/PATCH-4-first deferred to iter-272 orient based on remaining budget + risk-of-regression analysis.
  - **Budget consumed iter-271:** ~80-90K tokens (orient ~10K + 3 parallel subagents condensed verdicts ~12K + diff-capture ~3K + story-file edits ~25K + deferred-work.md edit ~10K + IP/RALPH.md/commit ~15K + this DONE entry ~10K). Within ~117K execution budget; exit cleanly per Guardrail 12.
  - **PUSH STATUS:** branch in sync with origin at iter-271 orient (`c2bb378`); iter-271 push expected SSH :22 clean per iter-263 asymmetric-recovery + iter-268 keepalive LESSON. HTTPS :443 confirmed timed out at iter-270 orient — push via SSH unaffected. PR #230 has no CI configured (`statusCheckRollup: []` per iter-219..270 confirmation); push is unblocked at any Story State.

_(iter-253..270 Story 2.10/2.11/2.12 closure iters + Story 2.12 drafting/pre-dev-SM/ATDD/dev-story/trace-gate/post-dev-SM iters pruned per Guardrail 2.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **11/17 stories done** (2.1-2.11) + 1/17 fixes-pending (2.12) + 5/17 backlog (2.13..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered through iter-270 (iter-263 push recovery, iter-264 three-layer adversarial CR closure ZERO-PATCH, iter-265 Story 2.12 drafting, iter-266 pre-dev SM gate 8-PATCH landing, iter-267 ATDD-skip-with-grounds, iter-268 dev-story recovery-landing, iter-269 trace-gate WAIVED landing, iter-270 post-dev SM gate ZERO-PATCH landing, iter-271 CR landing 5-PATCH forecast-band-breach).
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:120). Host-side shim count: **18** at iter-271 (unchanged at CR-gate iter — CR is docs-only; no shim or compose surface touched at iter-271; PATCH iters 272+ will modify entrypoint.sh + docker-compose.yml + 15 shim sites + new compose-args.sh helper).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **11 done** (2.1-2.11); 1 fixes-pending-CR (2.12); 5 backlog (2.13..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.12 — Loopback-bound port publication + opt-in `KEEL_DEVBOX_SSH` sshd
- **Story File:** `_bmad-output/implementation-artifacts/2-12-loopback-bound-port-publication-opt-in-keel-devbox-ssh-sshd.md`
- **Story State:** `fixes-pending` — Story 2.12 CR landing at iter-271 (5 PATCH queued; iter-264 LESSON forecast-band breach via novel-runtime-behaviour surface — promoted to RALPH.md § Lessons + § Signposts). Next iter transitions through PATCH drain (5 iters min, 7 max if PATCH-3 doesn't subsume PATCH-1+PATCH-4) → re-run CR for `fixes-pending → done | fixes-pending` confirmation per § Story Lifecycle Decision Matrix.
- **GitHub Issue:** Story 2.12 issue unknown; `RALPH_ISSUE_NUMBER` unset at iter-271 orient. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` per iter-219..270 confirmation); push is unblocked at any Story State.

## Notes

- **iter-271 NOVEL LESSON (forecast band breach via novel-runtime-behaviour surface):** when a story introduces multi-layer dynamic-runtime interactions (sshd lifecycle × cap_drop posture × set -e discipline × resolver-to-entrypoint env-var propagation), iter-264 LESSON severity-class-first rule's 0-3 first-class PATCH band breaks because static smokes have NO EXECUTION PATH to dynamic-runtime defects. iter-270 SM ZERO-PATCH verdict was structurally unable to catch PATCH-1 through PATCH-4 because all 4 only manifest at first live container exec under cap_drop:[ALL]. Forecast adjustment: stories with novel-runtime-behaviour surface should pre-budget 4-7 first-class PATCH at CR even with clean SM gates. Carry-forward applies to Stories 2.13 (healthcheck — runtime liveness signalling), 6.x RLS debugger (multi-process orchestration), Epic 13 CI harness smokes. Promoted to § Signposts at iter-271 + RALPH.md § Lessons.
- **iter-271 LESSON (three-subagent pattern reaffirmed):** Blind Hunter / Edge Case Hunter / Acceptance Auditor parallel-fan-out per § BMad Workflows. Iter-260 multi-layer convergence-signal-high-confidence-triage rule applies — all 5 PATCHes are HIGH/MEDIUM convergent across ≥2 reviewer layers; convergence is the high-confidence triage signal.
- **iter-270 NOVEL LESSON carry-forward (drift-band re-baseline):** post-dev SM substrate-citation drift class is BIMODAL — substantive drift (band 0-2 per story) vs mechanical drift (band 8-12 for any story whose dev-pass touches ≥4 cited substrate files). Story 2.12 confirms: 0 substantive + 8 mechanical (within mechanical band).
- **iter-270 LESSON carry-forward (two-subagent pattern reaffirmed):** post-dev SM AC-verifier (Amelia) + drift-detector (Winston) parallel-fan-out is PROVEN at THIRD cumulative application (iter-235 + iter-259 + iter-270).
- **iter-269 trace-gate outcome carry-forward:** WAIVED per ground-(a)+(b)+(c) hybrid conjunction — 22nd-cumulative trace-WAIVED + 23rd-cumulative ATDD-skip-trace-WAIVED pairing.
- **iter-268 NOVEL LESSON carry-forward** — post-crash recovery pattern for dev-story landing iters (VERIFY+COMMIT, not re-invoke). 5-check verification.
- **iter-267 precedent-match carry-forward** — 22nd-cumulative ATDD-skip-with-grounds-(c)+(ii)+(iii); Story 2.5 iter-186 canonical pattern.
- **iter-266 NOVEL LESSONS carry-forward:** (1) container-loopback ≠ host-loopback under Docker userland-proxy — pattern TRAP, defence-in-depth `ListenAddress 127.0.0.1` is ACTIVELY BROKEN; (2) pre-dev SM PATCH-count CAN break iter-221's ~5 ceiling for novel-runtime-behaviour stories (band 6-10).
- **iter-264 NOVEL LESSON carry-forward** — CR re-run DEFER-vs-PATCH severity-class-first rule. **Iter-271 EXTENDS the rule:** the 0-3 first-class PATCH band applies to stories with STATIC SUBSTRATE surface (Stories 2.7-2.11 verified the band); stories with NOVEL-RUNTIME-BEHAVIOUR surface (Story 2.12) need a wider band (4-7).
- **iter-263 LESSON carry-forward** — SSH :22 / HTTPS :443 asymmetric recovery; iter-271 expected SSH :22 clean.
- **iter-260 LESSONS carry-forward (REFINED by iter-264 + REAFFIRMED by iter-271):** three-layer adversarial fan-out convergence signal high-confidence triage. Iter-271 verified: all 5 PATCHes were ≥2-layer convergent.
- **iter-259 LESSONS carry-forward (REAFFIRMED by iter-270):** post-dev SM two-subagent pattern (PROVEN at iter-270 — third cumulative application).
- **iter-257 LESSON reaffirmed at iter-268..271** — `pnpm keel-invariants:check` runs `dist/check.js`; rebuild required after manifest edit. Iter-270 Amelia substrate smoke confirmed sync-gate GREEN post-rebuild; iter-271 CR did not modify manifest so no rebuild needed.
- **iter-244/246 audit-findings carry-forward** unchanged: restart.sh transitive-delegate; benchmark.sh OUT-OF-SCOPE (SC-11) but threads compose-override; Manifest entry count **32** at iter-271 (unchanged from iter-270; no new INV registrations at CR gate).
- **Substrate-citation drift cumulative forecast for Stories 2.13..2.17:** iter-270 NEW LESSON re-baselines: substantive band 0-2 + mechanical band 8-12 per dev-pass-touches-≥4-cited-files story. Cumulative Epic 2 forecast: ~5-12 cumulative substantive drifts + ~40-60 cumulative mechanical drifts. SC-17 scope holds: single reconciliation pass at Story 2.17 landing condenses both bands.
