# Implementation Plan

## NOW

- [ ] **AI-1 Fix: cross-filesystem `mv` in cmd_sync** [`packages/devbox/scripts/whitelist.sh:198`] — swap `tempfile="$(mktemp /tmp/keel-whitelist-sync.XXXXXX)"` → `tempfile="$(mktemp /run/keel-whitelist-sync.XXXXXX)"` so the subsequent `mv "${tempfile}" "${COMPOSED_WHITELIST}"` (line 241) stays on tmpfs (rename(2) atomic; no EXDEV cp+unlink fallback). SC-9 spec contract restored; inline comment at line 240 ("SC-9 same-fs rename") becomes code-truthful. Surgical 1-hunk Edit; verify with `bash -n` + dual-composer parity smoke re-run (Task 7.4 harness unchanged — not affected by tempfile path). ~small. Per FR14n matrix row `fixes-pending`: top-QUEUE fix drains one-per-iter; after AI-1 lands, AI-2 moves to NOW.

## QUEUE (Story 2.4 CR drain → Epic 2 Story 2.5..2.17 → Epic 2 close-out)

- [ ] **AI-2 Fix: `grep -Fxv || true` swallows I/O errors in cmd_remove** [`packages/devbox/scripts/whitelist.sh:390`] — replace `grep -Fxv -- "${domain}" "${WHITELIST_LOCAL}" > "${tempfile}" || true` with explicit rc capture: `if ! grep -Fxv -- "${domain}" "${WHITELIST_LOCAL}" > "${tempfile}"; then rc=$?; [[ $rc -eq 1 ]] || { log "ERROR: grep -Fxv failed reading ${WHITELIST_LOCAL} (rc=${rc})"; exec 201>&-; exit 3; }; fi`. Preserves current rc=0 (matches) + rc=1 (every line matched → empty tempfile expected) semantics while surfacing rc≥2 as fail-loud exit 3. Prevents silent data loss on unreadable `whitelist.local.txt`. ~small.
- [ ] **AI-3 Fix: silent `chmod 0644` hides dnsmasq-readability failures** [`packages/devbox/scripts/whitelist.sh:242,258`] — remove `2>/dev/null || true` from both instances (COMPOSED_WHITELIST at :242 + PREVIOUS_COMPOSED at :258); let `set -e` propagate the error with chmod's stderr visible. cmd_sync runs as root pre-Story-2.5; chmod MUST succeed. Failure indicates a genuine fault (MAC / ro-mount / perm-bit restriction) that should abort sync-gate not be hidden. Aligns with Story 2.3 iter-170 AR-9 "fail loud on I/O boundaries" precedent. ~small.
- [ ] **AI-4 Fix: PID-predictable tempfile names in cmd_add / cmd_remove** [`packages/devbox/scripts/whitelist.sh:301,385`] — replace `tempfile="${WHITELIST_LOCAL}.tmp.$$"` at both sites with `tempfile="$(mktemp "${WHITELIST_LOCAL}.XXXXXX.tmp")"`. Keeps adjacency to `WHITELIST_LOCAL` (SC-9 same-fs rename preserved); adds randomised suffix (TOCTOU symlink-attack resistance once Story 2.5 hardening introduces non-root `dev` user); 0600 perms from mktemp are broadened by the existing `chmod 0644` after `mv`. Existing EXIT trap rm-f with single-quoted-inside-double-quoted substitution remains valid (mktemp output is shell-safe). ~small.
- [ ] **AI-5 Fix: README per-subcommand exit-code table completeness** [`packages/devbox/README.md:266-268`] — `add` row → `0 ok; 2 syntax; 3 unreadable; 4 lock-timeout; 5–7 reload`; `remove` row → `0 ok; 2 substrate-domain / syntax; 3 unreadable; 4 lock-timeout; 5–7 reload`. Both subcommands transitively reach `cmd_sync` which owns the full exit-code surface. Doc-only; trivial alignment. ~small.
- [ ] **Re-run `/bmad-code-review (args: "2")`** — CR re-verification gate against HEAD after AI-1..AI-5 drain. Expected verdict: ZERO-PATCH close if Acceptance Auditor confirms all 5 ACs still SATISFIED + prior-gate AI-N closures verified byte-identical + Blind/Edge Case layers produce only defensive-nit defers/dismissals (no new HIGH findings). If new HIGHs surface in CR re-run, re-enter fix drain with AR-1..AR-N from that cycle (per Story 2.3 iter-166..iter-170 AR-drain precedent). On ZERO-PATCH close: Story State `fixes-pending → done`; Status `fixes-pending → done`; sprint-status row `review → done`; epic-2-stays-at-3/17-done advances to 4/17; next iter queues `/bmad-create-story` for Story 2.5 (within-epic auto-advance per § Story Lifecycle `done` row; not § Cross-epic transition because Epic 2 has 13 open backlog stories 2.5..2.17).
- [ ] _(conditional, post-Story-2.4 done)_ `/bmad-create-story` for Story 2.5 (container hardening: non-root user `dev` + `cap_drop: [ALL]` preserving `cap_add: [NET_ADMIN, NET_RAW]` + `security_opt: [no-new-privileges:true]` + tmpfs + named volumes for OAuth) per § Cross-epic transition within-epic path. 13 backlog stories remaining in Epic 2 (2.5..2.17) after 2.4 done.
- [ ] _(conditional, post-Story-2.17 done)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge. Epic 2 completion.
- [ ] _(conditional, post-Epic-2 merged)_ § Cross-epic transition → Epic 3 Story 3.1 OR halt EPIC_DONE awaiting merge.

## BLOCKED

_(none — CR opener produced 5 PATCH + 4 DEFER + ~30 DISMISS envelope; Acceptance Auditor verified all 5 ACs SATISFIED; no ambiguous `decision-needed` findings surfaced.)_

## ATDD Red Phase

_(none — Story 2.4 ATDD-skipped at `validated → atdd-scaffolded` per 14th-cumulative-precedent iter-173; CR patches AI-1..AI-5 are surgical shell-script edits with no tests-to-green tracking. Re-verified by iter-177 CR opener: Blind/Edge Case/Auditor layers substitute for absent test-runner per hybrid ground-(c)+(ii)+(iii) declaration in Story 2.4 § Testing Standards.)_

## DONE (Story 2.4 lifecycle — keep current story chain, prune after next-story starts)

- [x] iter-177: **`/bmad-code-review (args: "2")` CR opener — FR14n Story State `sm-verified → fixes-pending`; 5 PATCH + 4 DEFER + ~30 DISMISS.** Fifth Epic-2 CR opener (2.1 iter-128 = 9-AI / 2.2 iter-151 = 2-PATCH / 2.3 iter-161 = 9-PATCH / 2.4 iter-177 = 5-PATCH); slightly over iter-175-trace + iter-176-SM 2–4 forecast envelope on defensive-boundary findings at filesystem / redirect / rename primitive layer. Three parallel fresh-context general-purpose subagents: Blind Hunter (cynical 37 findings), Edge Case Hunter (JSON path-tracer 33 entries w/ ~7 self-retracts), Acceptance Auditor (5 ACs SATISFIED zero violations + 2 LOW narrative drifts). Ralph triage deduplicated cross-layer clusters (cross-fs `mv` seen by Blind 14 + E4; `grep -Fxv || true` seen by Blind 18 + E13; fd-201 leak seen by Blind 10 + E7 + E10; `wc -l` trailing-newline seen by Blind 5 + E29 — both DISMISS because awk guarantees `\n`). **5 PATCH queued for iter-178..iter-182 one-per-iter drain:** AI-1 cross-fs `mv` at `whitelist.sh:198,241` violates SC-9 "same-fs rename" (`/tmp` container overlay-fs vs `/run` tmpfs → EXDEV → non-atomic cp+unlink fallback); AI-2 `grep -Fxv || true` at `whitelist.sh:390` swallows grep rc≥2 (real I/O error → empty tempfile → silent whitelist.local.txt truncation); AI-3 `chmod 0644 2>/dev/null || true` at `whitelist.sh:242,258` hides chmod failures (composed file stays 0600 → dnsmasq can't read → active-vs-applied policy divergence); AI-4 PID-predictable tempfile `.tmp.$$` at `whitelist.sh:301,385` (TOCTOU surface post-Story-2.5 non-root hardening; standard mktemp defensive pattern); AI-5 README per-subcommand exit-code table missing `add`→`3` + `remove`→`3,5–7` (reachable transitively via cmd_sync→reload-egress.sh). **4 DEFERs logged to `deferred-work.md § Deferred from: code review of story-2-4 (2026-04-22)`:** D1 LC_ALL=C validator (vacuous in-container; defer to Story 2.6 host-side CLI where macOS locale variance matters); D2 trap `INT TERM HUP` + fd-201 cleanup (EXIT trap + kernel-cleanup cover 1.0; defer to Story 2.5 non-root signal-handling sweep); D3 reload-egress.sh executable pre-check (zero exposure, Story 2.3 installs executable; defer to primitive-polish OR Epic 13 CI-harness); D4 CRLF line-endings in grep-Fxq (in-container vi preserves LF; defer to Story 2.6 host-ingress OR Epic 13 UX pass). **~30 DISMISS:** adversarial-default false-positives — SC-18 first-boot summary, SC-14 parity (smoke PASSED), LDH regex blocks `+`-prefix, punycode regex handles `xn--`, bash 5+ in-container, `mkdir -p /run` no-op, DOMAIN_REGEX case-sensitivity documented, idempotent-add defensive-reload Subtask 4.4, substrate-vs-override case non-bypass, `wc -l` guarded by awk, `return $?` speculation, `.gitignore` bypass out-of-scope, cc-devbox vs keel-devbox different containers (Ralph env vs substrate), Change Log v1.3 "Eight files" historic prose (corrected in v1.6 clarification), sourcing `set -euo pipefail` documented, `sed '#.*'` standard behavior, tempfile TOCTOU sub-AI-4, ~15 defensive-nit residue. **AC-SATISFIED × 5:** Auditor confirmed AC 1 (whitelist layout + override gitignored + AGENTS.md-docs), AC 2 (sync CLI compose→validate→flock→reload→diff→exit 0), AC 3 (fail-closed at 3 boundaries), AC 4 (concurrent sync serialises via fd-200 reload lock + fd-201 disjoint mutation lock + rename(2) atomicity), AC 5 (git-tracked substrate + prek gates + FR44 AMEND path documented). **Class distinction vs Story 2.3 iter-161 opener (defensive-boundary axis):** Story 2.3 opener = 9 HIGH on kernel/daemon boundaries (dnsmasq SIGHUP, nftables transaction, flock timeout, JSONL rotation race); Story 2.4 opener = 5 PATCH at CLI-infrastructure boundaries (filesystem atomicity, redirect/rename primitives, perm-change silence, tempfile predictability). Same defensive-hardening density but at user-facing-CLI layer vs kernel-daemon layer. **Pattern rule:** Epic-2 CR openers cluster at the defensive-boundary of the story's class — infrastructure-security → kernel/daemon; user-facing-CLI → filesystem/process/perms. **CR chain forecast:** 5 PATCH → 5 drain iter (178..182, one-per-iter per Guardrail 5 — surgical Edits, ~small each) + 1 re-run iter (183) = 6 iter total. Tighter than Story 2.3's 10-iter chain because AI-N patches are ~2-line surgical Edits (cf Story 2.3 AR-N multi-hunk algorithmic rewrites). Files modified iter-177: story doc (Status flip + Review Findings section + Change Log v1.6), deferred-work.md (§ Deferred from: code review of story-2-4 appendix), sprint-status.yaml (last_updated timestamp; row `2-4` STAYS `review` — 5-enum sprint-status has no `fixes-pending` state, FR14n mapping keeps row at `review` during drain), `.ralph/@plan.md`, `RALPH.md` (iter-177 signpost). Budget consumed iter-177: ~70K tokens (orient ~5K + diff capture ~2K + 3 parallel subagent fan-out ~45K + triage ~3K + artefact writes + commit + push ~15K). PR #230 stays Draft (Epic 2 closes at Story 2.17). Story State `sm-verified → fixes-pending`. Next iter: AI-1 fix.
- [x] iter-176: `/bmad-create-story (args: "review")` Story 2.4 post-dev SM verification — FR14n Story State `traced → sm-verified`; ZERO-PATCH + ZERO-DEFER + 1-DOCUMENTATION-DISMISS; 4th Epic-2 SM precedent; first user-facing-CLI class post-dev SM.
- [x] iter-175: `/bmad-testarch-trace (args: "yolo")` Story 2.4 — FR14n Story State `in-dev → traced`; Gate WAIVED (14th cumulative trace-WAIVED precedent; 15th cumulative ATDD-skip-trace-WAIVED co-application).
- [x] iter-174: `/bmad-dev-story` Story 2.4 landed single-iteration; FR14n Story State `validated → in-dev`; 7 substrate + 4 planning artefact files touched per SC-19; SC-14 parity smoke PASSED; iteration-env smokes pass; live container smokes 10.1–10.8 + 9.2 deferred.
- [x] iter-173: `/bmad-create-story (args: "review")` pre-dev SM validation of Story 2.4; FR14n Story State `drafted → validated`; 1 CRITICAL + 4 ENHANCEMENTS applied.
- [x] iter-172: `/bmad-create-story` drafted Story 2.4; Status `backlog → ready-for-dev`.

## Context

- **Phase:** 4-implementation — Epic 2 at 3/17 stories `done` (2.1 + 2.2 + 2.3) + 1/17 at `review` in sprint-status enum / `fixes-pending` in FR14n Story State (2.4 iter-174 dev-story; traced iter-175; sm-verified iter-176; CR opener iter-177 → 5 PATCH queued) + 13/17 `backlog` (2.5..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.4 live smokes (Task 10.1–10.8 + 9.2) operator-workstation-deferred per Subtask 10.10; iter-177 CR opener re-verified deferral acceptance via Acceptance Auditor ALL-5-ACs-SATISFIED verdict.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). AI-1..AI-5 patches require NO Dockerfile edits (pure shell-script + README edits).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; 2.1 + 2.2 + 2.3 done; 2.4 fixes-pending (at `review` for sprint-status; AI-1..AI-5 drain in progress); 2.5..2.17 in `backlog`.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2; PR #230 transitions to Open at Story 2.17 completion).
- **Story:** Story 2.4 — Whitelist source-of-truth + `pnpm devbox:whitelist` atomic-reload CLI.
- **Story File:** `_bmad-output/implementation-artifacts/2-4-whitelist-source-of-truth-pnpm-devbox-whitelist-atomic-reload-cli.md`.
- **Story State:** `fixes-pending` — drain AI-1..AI-5 one-per-iter (178..182) → re-run `/bmad-code-review` (183) → ZERO-PATCH close → `fixes-pending → done`.
- **GitHub Issue:** Story 2.4 → _(not yet created; RALPH_ISSUE_NUMBER unset at iter-177)_; Epic 2 → #10.
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **iter-177 CR-opener pattern affirmation (defensive-boundary axis).** Epic-2 CR openers cluster at the defensive-boundary of the story's class: Story 2.1 (infrastructure-smoke) → 9 AI on build/bake/compose seams; Story 2.2 (hybrid configuration) → 2 PATCH on parameterisation asymmetry; Story 2.3 (infrastructure-security) → 9 PATCH on dnsmasq/nftables/flock/JSONL kernel-daemon boundaries; Story 2.4 (user-facing-CLI) → 5 PATCH on filesystem/redirect/rename/perm/tempfile CLI-infrastructure boundaries. The finding count + severity depends on the story's defensive surface area, but the PATTERN is stable: adversarial fan-out against a CR gate will always concentrate at the story's class-distinctive boundary layer. Future-Ralph rule: when forecasting a CR opener envelope, sum the fix-chain equation `(carve-out × 3) + (live-smoke-defer × 3) + (impl-surface-LOC / 100)` AND add a class-boundary-density coefficient — user-facing-CLI class gets +1 defensive-boundary finding per 100 LOC on top of the base estimate (Story 2.4 actual: 0 carve-out + 3 backend-B defer + 5.44 LOC-div-100 + 5 class-boundary → fits 5 PATCH opener exactly).
- **iter-177 class-distinction: user-facing-CLI openers cluster at filesystem/process/perms.** Unlike Story 2.3 infrastructure-security openers (clustered at dnsmasq/nftables/flock/JSONL kernel-daemon boundaries with multi-hunk algorithmic fixes AR-N averaging ~30 LOC each), Story 2.4 user-facing-CLI openers cluster at filesystem atomicity (AI-1 cross-fs `mv`), redirect/rename primitives (AI-2 `grep || true`; AI-4 tempfile naming), perm-change silence (AI-3 `chmod || true`), and doc-accuracy (AI-5 README table). All 5 patches are ~2-line surgical Edits (no algorithmic rewrites). Forecasted CR chain length tighter than Story 2.3's 10-iter because per-patch LOC-surface is ~10× smaller. **Pattern rule:** future user-facing-CLI class stories (Story 2.6 `pnpm devbox:*`; Story 2.10 `pnpm devbox:env:check`; Epic 15a `create-keel-app`) will produce CR openers that cluster at the same CLI-infrastructure boundary layer — forecast 3–7 PATCH, ~small each, ~5–8 iter drain chain (tighter than infrastructure-security class, looser than hybrid-configuration class).
- **iter-177 triage-discipline carry-forward: adversarial-default routes defensive-boundary findings to DEFER/DISMISS not PATCH.** Blind Hunter surfaced 37 findings (3 CRIT + 14 HIGH + 14 MED + 6 LOW); Edge Case Hunter 33 entries; combined pre-dedup ~70. After Ralph triage via the "is this a new defect requiring THIS story's fix OR a defensive-hardening candidate deferred to downstream class-relevant story" lens: 5 PATCH (real bugs or clear spec violations) + 4 DEFER (defensive defensive-hardening with clear carry-to target — Story 2.5 hardening / Story 2.6 host-side / Story 2.3 primitive polish / Epic 13 CI-harness) + ~30 DISMISS (per-spec semantics OR false-positives OR sub-AI-N patch-level nits). **Adversarial-default IS NOT "every finding becomes a patch"** — adversarial-default IS "every finding is triaged; patches must be clear defects; defers need a concrete carry-to target; dismisses need a spec-justification OR equivalent-smoke evidence".  Story 2.3 iter-161 opener routed ~30 findings into 9 patch + 9 defer + ~9 dismiss with similar ratios. The PATCH fraction converges on 15-25% of raw findings across Epic-2 openers — Story 2.4 at 5/70 = 7% is notably tighter because Acceptance Auditor returned zero AC violations + Edge Case Hunter self-retracted ~7 findings mid-review. Healthy adversarial-to-triage conversion signal; envelope-tighter than Stories 2.1/2.3 is consistent with iter-175 trace's "SC-14 parity PASS de-risks highest-risk invariant" assessment.
- **NFR2 authority unchanged.** Cold ≤ 300s / warm ≤ 30s remains authoritative on M4-Pro native; DinD runs are indicative only. Story 2.4 substrate changes (whitelist.sh + start-egress.sh + default-txt header) do NOT affect cold-start footprint because they consume existing baked tooling; warm-start unaffected because the scripts are copied from workspace on every startup (no new image layer).
- **Substrate sanity check (post-iter-177):** Story 2.4 CR opener is pure docs + IP + RALPH.md update (no code, no config, no substrate touched). Files modified iter-177: `_bmad-output/implementation-artifacts/2-4-whitelist-source-of-truth-…-cli.md` (Status flip + Review Findings section insert + Change Log v1.6 append), `_bmad-output/implementation-artifacts/deferred-work.md` (§ Deferred from: code review of story-2-4 (2026-04-22) appendix with D1..D4), `_bmad-output/implementation-artifacts/sprint-status.yaml` (last_updated timestamp + header comment; row `2-4` STAYS `review`), `.ralph/@plan.md`, `RALPH.md` (iter-177 signpost append). PR #230 stays Draft.
- **Cumulative Epic-2 fix-chain forecast tracking:** Story 2.1 = 16-iter chain (9+5+ZERO); Story 2.2 = 4-iter chain (2 PATCH + re-run); Story 2.3 = 10-iter chain (9→0 single-cycle drain); Story 2.4 forecast = 6-iter chain (iter-175 trace DONE + iter-176 SM DONE + iter-177 CR opener DONE + iter-178..182 5-PATCH drain + iter-183 CR re-run). Trend: Story 2.4 at 6-iter is tighter than Story 2.3's 10-iter because per-patch LOC surface is ~10× smaller (user-facing-CLI ~small-Edit class vs infrastructure-security ~medium-Edit class). Cumulative Epic-2 SM metrics at iter-177: 4/4 ZERO-PATCH SM opener closes (2.1 iter-127 + 2.2 iter-150 + 2.3 iter-160 + 2.4 iter-176); CR openers = 9 / 2 / 9 / 5 PATCH (range 2–9; user-facing-CLI class at 5 lands in the middle between hybrid-configuration 2 and infrastructure-security/infrastructure-smoke 9 per class-boundary-density coefficient).
