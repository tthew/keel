# Implementation Plan

## NOW

- [ ] iter-86: `/bmad-create-story (args: "review")` — FR14n row 7 `traced → sm-verified` post-dev SM requirements-satisfaction verification. FIFTH cumulative ZERO-PATCH post-dev SM precedent CANDIDATE (Stories 1.11/1.12/1.13/1.14 → 1.15). Independent fresh-context Sonnet Explore subagent audits all 4 ACs against committed iter-84 implementation + live filesystem state. AC 1 + AC 4 substrate-verified (ZERO unmet-AC findings expected); AC 2 + AC 3 SCOPE-CARVED to downstream Renovate GitHub App runtime + Epic 13 CI gate per story scope-carve-outs.

## QUEUE (Story 1.15 — renovate config i7)

- [ ] iter-87: `/bmad-code-review (args: "2")` — FR14n row 9 `sm-verified → done`. FIFTH cumulative ZERO-PATCH CR precedent CANDIDATE (Stories 1.11/1.12/1.13/1.14 → 1.15). Three-layer adversarial fan-out (Blind Hunter + Edge Case Hunter + Acceptance Auditor) on iter-84 dev-story commit `2137c83`. Triage per convergence-doctrine + ZERO-PATCH-doctrine + adversarial-triage default. Expected outcome: 0 PATCH / 0 DECISION-NEEDED / 5-8 DEFER / ~10-13 DISMISS (lower DEFER vs Story 1.14's 10, due to smaller 4-packageRules surface + no `.release-please-manifest.json` hash-cascade concerns + no "release-please WORKFLOW = Story 13.5" consumer-deferral ambiguity — Renovate App install is a cleaner ops carve-out). After CR: Story State `sm-verified → done`; BMad Status `review → done`; sprint-status `1-15-...: review → done` + `last_updated: 2026-04-21 Story-1-15-done UTC`. Next: iter-88 starts Story 1.16 (`_(no story) → drafted` for `1-16-fork-extension-config-pattern-growth-tier-invariants-fork-md-scaffold`). Epic 1 NOT yet complete; PR #226 stays Draft through Story 1.16 completion.

## BLOCKED

_(none)_

## DONE (current epic — trailing iterations only)

- [x] iter-85: **FR14n row 5 `in-dev → traced` via `/bmad-testarch-trace (args: "yolo")` NINTH cumulative WAIVED precedent** (Stories 1.7 iter-4 + 1.8 iter-3 + 1.9 iter-3 + 1.10 iter-46 + 1.11 iter-57 + 1.12 iter-64 + 1.13 iter-71 + 1.14 iter-78 → 1.15 iter-85). Four trace artefacts authored at `_bmad-output/test-artifacts/traceability/1-15-*`: `coverage-matrix.json` (4 ACs; STRONG substrate evidence for AC 1 + AC 4; scope-carved AC 2 + AC 3) + `e2e-trace-summary.json` (deterministic 0% overall + structural-false-positive + 4 MEDIUM recommendations) + `gate-decision.json` (`gate_status: WAIVED` + ninth-cumulative rationale) + `1-15-renovate-config-with-version-pinning-rules-i7.md` (Phase 1–5 full trace report mirroring Story 1.14 iter-78). All 6 Task 6 substrate smokes re-verified LIVE byte-identical to iter-84 dev-story record: (1) `OK: static parse` (<0.1s); (2) `OK: renovate schema + config:recommended extends` (<0.1s); (3) `OK: 4 packageRules; groups=["opentelemetry","pg-uuidv7","radix-ui","vitest"]; automerge=false` (<0.1s); (4) `OK: all 4 I7 groups pin-mode` (<0.1s); (5) `OK: 22 invariants` (post-`pnpm --filter @keel/keel-invariants build` per iter-77 Gotcha, <0.1s); (6) `pnpm keel-invariants:check` exit 0 silent in **638ms** (0.638s — 68.1% margin under Story 1.9 AC 7 <2s budget; within iter-84's 0.644s / 68% band). sha256 byte-identical re-verification: `.github/renovate.json: c02f2bfe…4cd0` + `docs/invariants/renovate.md: a18a353f…1c56` both re-verified via `sha256sum`. AC 1 + AC 4 substrate-verified at CLI-exit-code level (4 smokes + 2 smokes respectively); AC 2 (auto-merge gate) + AC 3 (OTEL grouped-update) scope-carved to Renovate GitHub App runtime + Epic 13 integration-test CI gate + GH branch-protection status-check rule. Adversarial coverage delegated to iter-87 CR per hybrid ground-(c) variant-(iii). Structural trace-artefact inheritance from Stories 1.11-1.14 (~90% reasoning-cost savings; mechanical 4-AC adaptation). Change Log v1.4 row appended. Quality gates clean post-authoring: `pnpm format:check` clean + `pnpm keel-invariants:check-all` exit 0 (trace artefacts in `_bmad-output/test-artifacts/` are not sourcePath'd in the manifest; no cascade hash update).
- [x] iter-84: **FR14n row 4 `atdd-scaffolded → in-dev` via `/bmad-dev-story` SINGLE-PASS at configuration-surface scale** (Story 1.14 iter-77 precedent held). 4 files landed: 2 NEW (`.github/renovate.json` — 4 packageRules ecosystems Vitest / OpenTelemetry / Radix UI / pg_uuidv7 each `rangeStrategy: pin` + `automerge: false` + canonical `$schema` + `extends: ["config:recommended"]`; `docs/invariants/renovate.md` — H1 + 4-line header + § Overview anchor bullet + § I7 posture + § Files + § Per-package pinning rules 4×4 table + § Grouping rationale + § Fork extension + § Consumption) + 2 MODIFIED (`packages/keel-invariants/src/invariants.manifest.ts` +2 entries → 22 total; `INVARIANTS.md` +1 H3 `### Dependency upgrade discipline (Story 1.15)` + 2 column-0 bullets between Release management H3 and `## Consumption` H2). Content hashes post-prettier: `.github/renovate.json: c02f2bfe97a7811c3cdabc693e02f0c7b9d6a2a280b1c9701aee0d8d56cc4cd0`; `docs/invariants/renovate.md: a18a353f3efc1496b208bf84bf5158daf72a0728ef1ada1b9976a300b7f81c56`. All 6 Task 6 smokes GREEN verbatim to IP forecast. Task 5 explicit no-op held: zero cascade hash updates to `INV-prek-*` / `INV-tokens-*` / `INV-release-please-*` sibling entries. Story State: `atdd-scaffolded → in-dev`; BMad Status: `ready-for-dev → review`; sprint-status: `ready-for-dev → in-progress → review` + `last_updated: 2026-04-21 Story-1-15-review UTC`.
- [x] iter-83: **FR14n row 3 `validated → atdd-scaffolded` via `/bmad-testarch-atdd` hybrid ground-(c) variant-(ii)+(iii) ATDD-SKIP — NINTH cumulative precedent** (Stories 1.7 iter-14 + 1.8 iter-29 + 1.9 iter-36 + 1.10 iter-43 + 1.11 iter-50 + 1.12 iter-57 + 1.13 iter-64 + 1.14 iter-71 → 1.15 iter-83). Skill preflight HALTs at Step 1.2 (no test runner — `vitest.config.*` / `jest.config.*` / `playwright.config.*` absent). Three-ground rationale honored verbatim per story § Testing Standards lines 250–258: (a) substrate-verification-covers-ACs at CLI-exit-code level (Task 6 six smokes); (b) no-runner; (c) HYBRID variant-(ii)+(iii). 1 in-situ Change Log row (v1.2). No test-plan artefacts authored (variant-(ii)+(iii) substitution).
- [x] iter-82: **FR14n row 2 `drafted → validated` via `/bmad-create-story (args: "review")` pre-dev SM.** Three parallel fresh-context Explore-subagent audits covering L1-L7. **1 PATCH applied** (L4-class cross-file citation drift): 4 sites cited `architecture.md:3984` but actual source is `epics.md:3984`. Fixed all 4 sites per RALPH.md 2026-04-20 drift-survey-scope rule. Residual count 1 PATCH BELOW 2-PATCH 4-story precedent average Stories 1.11/1.12/1.13/1.14.

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1-1.14 done; Story 1.15 traced iter-85; Story 1.16 backlog — SM/CR remaining on Story 1.15 + 1 full × Story 1.16 before EPIC_DONE halt).
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (continues through Epic 1; PR #226 stays Draft until EPIC_DONE per PROMPT_build.md step 5c).
- **Story:** Story 1.15 — Renovate config with version-pinning rules (I7).
- **Story File:** `_bmad-output/implementation-artifacts/1-15-renovate-config-with-version-pinning-rules-i7.md`.
- **Story State:** `traced` — FR14n matrix row 5 complete at iter-85 (`/bmad-testarch-trace (args: "yolo")` NINTH cumulative WAIVED precedent; 4 artefacts authored; 6 smokes re-verified LIVE byte-identical; sync-gate 638ms); row 7 (`traced → sm-verified` via `/bmad-create-story (args: "review")` post-dev SM) next at iter-86.
- **GitHub Issue:** Story 1.15 at **#39** (https://github.com/tthew/ralph-bmad/issues/39). Parent Epic 1 at **#9** OPEN (closes at EPIC_DONE halt). Story 1.14 at #38 expected CLOSED on PR merge (ralph.py wiring; PR merge waits for EPIC_DONE).
- **PR:** #226 (Draft, empty statusCheckRollup — stays Draft through Epic 1; Draft→Open at EPIC_DONE after Stories 1.15 + 1.16 complete FR14n lifecycle).

## Notes

- **Story 1.15 iter-85 trace closed WAIVED.** Substrate evidence identical iter-84 dev-story record; sync-gate 638ms (iter-84 observed 644ms; both inside Story 1.9 AC 7 <2s budget). Change Log v1.4 appended.
- **Carry-forward rules INTO iter-86 `/bmad-create-story (args: "review")` post-dev SM FIFTH cumulative ZERO-PATCH precedent candidate:**
  - Independent fresh-context Sonnet Explore subagent audits all 4 ACs against committed iter-84 implementation + live filesystem state.
  - AC 1 + AC 4 substrate-verified (6 smokes LIVE at iter-85 already confirmed); expected ZERO unmet-AC findings.
  - AC 2 + AC 3 explicitly scope-carved to Renovate GitHub App runtime + Epic 13 CI gate per story § AC 1/2/3 scope carve-outs — expected NO finding.
  - Architectural findings raised during trace (none at iter-85 — Story 1.15 does not touch emitter) do NOT unmet ACs per iter-72 lesson. No inline fixes required.
  - Follow iter-72/79 precedent: post-dev SM verifier should ONLY verify (requirements-satisfaction check, not enhancement workshop); any enhancement suggestions drop into iter-87 CR queue as DEFER candidates, never PATCH at SM.
  - Budget ~6-8K tokens (orient ~1K + 1 Sonnet subagent fresh-context AC audit ~3-4K + v1.5 Change Log row + IP/RALPH/sprint-status updates ~2-3K).
- **Configuration-surface CR forecast (Story 1.15 iter-87):** 0 PATCH / 0 DECISION-NEEDED / 5-8 DEFER / ~10-13 DISMISS expected. Lower DEFER vs Story 1.14's 10 due to: (a) smaller 4-packageRules surface; (b) no manifest-file hash-cascade concerns; (c) no "release-please WORKFLOW = Story 13.5" consumer-deferral ambiguity.
- **Issue Tracking carry-forward.** Story 1.15 at **#39** — current iteration commit trailer uses `Refs #39`. Parent Epic 1 at **#9** OPEN.
