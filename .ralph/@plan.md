# Implementation Plan

## NOW

- [ ] `/bmad-code-review (args: "2")` — pre-selects "Create action items"; Story 2.4 CR opener per FR14n `sm-verified → fixes-pending` (default adversarial triage) OR `sm-verified → done` (if ZERO-PATCH close). Three-layer Ralph-hosted adversarial fan-out (Blind Hunter + Edge Case Hunter + Acceptance Auditor) against cumulative Story 2.4 substrate diff `b7e...4fb1e93..HEAD` filtered to in-scope files (exclude `_bmad-output/`, `.ralph/`, `RALPH.md`, `_bmad/` per Story 2.3 iter-161 `:(exclude)<path>` long-form). Expected envelope per iter-175 trace + iter-176 SM: 2–4 PATCH opener projected (0 carve-out + backend-B defer + ~360 LOC); chain length 2–4 iter (tighter than Story 2.3's 10-iter because SC-14 parity PASS in iteration env de-risks byte-identity invariant). Budget projection: ~55K tokens (orient ~5K + diff prep ~2K + 3 parallel adversarial subagents ~45K + triage ~3K + findings insertion + Change Log v1.6 prepend + IP + RALPH.md + commit + push).

## QUEUE (Story 2.4 → Story 2.5..2.17 → Epic 2 close-out)

- [ ] _(conditional, post-Story-2.4 done)_ `/bmad-create-story` for Story 2.5 (container hardening: non-root user `dev` + `cap_drop: [ALL]` preserving `cap_add: [NET_ADMIN, NET_RAW]` + `security_opt: [no-new-privileges:true]` + tmpfs + named volumes for OAuth) per § Cross-epic transition within-epic path. 13 backlog stories remaining in Epic 2 (2.5..2.17) after 2.4 done.
- [ ] _(conditional, post-Story-2.17 done)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge. Epic 2 completion.
- [ ] _(conditional, post-Epic-2 merged)_ § Cross-epic transition → Epic 3 Story 3.1 OR halt EPIC_DONE awaiting merge.

## BLOCKED

_(none — Story 2.4 SM review landed iter-176 sm-verified ZERO-PATCH; CR queued iter-177; no blockers.)_

## ATDD Red Phase

_(none — Story 2.4 ATDD-skipped at `validated → atdd-scaffolded` per 14th-cumulative-precedent (carried iter-173). Red-phase scaffolds not produced; no tests-to-green tracking. Downstream Epic 13 harness + CR adversarial coverage substitute per hybrid ground-(c)+(ii)+(iii).)_

## DONE (current story — Story 2.4 lifecycle; prune deeper when landing)

- [x] iter-176: **`/bmad-create-story (args: "review")` Story 2.4 post-dev SM verification — FR14n Story State `traced → sm-verified`; ZERO-PATCH + ZERO-DEFER + 1-DOCUMENTATION-DISMISS.** Fourth Epic 2 post-dev SM precedent (2.1 → 2.2 → 2.3 → 2.4); first user-facing-CLI class post-dev SM. Three parallel fresh-context Sonnet subagents (Layer 1 AC-by-AC substrate reduction / Layer 2 SC-1..19 consistency + dual-composer parity + defer acknowledgment / Layer 3 scope-isolation vs Stories 2.5/2.6/2.13 + Story 2.3 primitive + Epic 4 FR37); Ralph triage produced ZERO PATCH / ZERO DEFER / 1 DOC-DISMISS against v1.4 substrate. **AC-by-AC verdict SATISFIED ×5 substrate-grounded:** AC 1 (whitelist layout baseline + fragments + override gitignored + AGENTS.md-documented) SATISFIED per `whitelist.default.txt:1-28` + `whitelist/{npm,anthropic,github}.txt` + `.gitignore:47-48` + `AGENTS.md` H3 + `README.md` H3 + `whitelist.sh:87-113` compose_whitelist_into + `start-egress.sh:97-106` SC-14 extension; AC 2 (sync reads composed, validates, acquires lock, reloads atomically, exits 0 + diff) SATISFIED per `package.json:18` SC-12 + `cmd_sync:189-271` (validate BEFORE compose → invoke RELOAD_SCRIPT → propagate 5/6/7); AC 3 (syntax-invalid exit non-zero + fail-closed) SATISFIED per `validate_sources:121-173` SC-6 verbatim + 3-boundary fail-closed (validation / unreadable / reload-fail); AC 4 (concurrent sync file-lock serialises, no partial state) SATISFIED per reload-egress.sh fd-200 flock + mv rename-atomic + cmd_add/remove fd-201 mutation lock released before sync (SC-8 nested-lock avoidance); AC 5 (whitelist git-tracked + prek gates + reviewers see diffs) SATISFIED per gitignore-only-on-override + AMEND-path documented in 3 locations. **SC-consistency ALL 13 PRIORITY SCs PASS (Layer 2)** — SC-1/3/4/5/6/7/8/10/11/12/14/15/19 each verified file:line. SC-14 dual-composer byte-identity confirmed via Debug Log parity smoke + algorithmic inspection (identical mapfile + find|sort + sed-strip + awk-NF + sort-u pipelines). SC-15 consumer-only confirmed (`invariants.manifest.ts` untouched; `INV-devbox-egress-contract` contentHash `cd1b5da113871ad5fc8b7321d2d0cb33dc87e99d9cb97a0d742eaacddfdee2d5` matches on-disk SHA256). SC-19 no-scope-creep confirmed via `git show --stat 770e4e7` (exactly 8 declared substrate files + sprint-status + 2-4-*.md). **Scope-isolation ALL 7 CHECKS PASS (Layer 3)** — Story 2.5/2.6/2.13 each PASS (no `user:`/`cap_drop:`/host-side-`devbox:*`/`healthcheck:` active stanzas); Story 2.3 primitive untouched (`reload-egress.sh` @ iter-169 AR-8 `ab2a3cb`, `egress-log-tailer.sh`/`monitor.sh`/`dnsmasq.conf`/`egress.nft` all byte-identical); Story 2.3 invariant untouched; Epic 4 FR37 consumer absent (`whitelist.sh` has zero `egress-queries.jsonl` references). **1 DOC-DISMISS:** v1.4 Change Log prose cites stale hash `aad16a51…6889b` (iter-158 landing) but current manifest holds `cd1b5da1…` (iter-166 AR-5 refresh). SC-15 claim satisfied — Story 2.4 did NOT modify manifest — but prose reference is stale; corrected in v1.5 Change Log entry. Documentation-only, no substrate effect, DISMISS at CR. **Operator-workstation-defer ACCEPTED per Story 2.3 iter-160 two-ground rationale:** (a) ACs are substrate commitments + iteration-env-executable evidence (SC-14 parity PASS + dispatcher + regex + multi-error + source-attribution); (b) operator recipes pinned verbatim in README.md + AGENTS.md § Per-fork whitelist override. Class-distinction: Story 2.4 has MORE iteration-env evidence than Story 2.3 (ZERO runtime assertions); AC-satisfaction-without-live-container posture stronger. Story file Status `traced → sm-verified`; Change Log v1.5 entry appended (iter-176 row). RALPH.md iter-176 signpost appended (4th Epic-2 SM precedent, first user-facing-CLI class). Budget consumed iter-176: ~45K tokens. PR #230 stays Draft (Epic 2 closes at Story 2.17).
- [x] iter-175: `/bmad-testarch-trace (args: "yolo")` Story 2.4 — FR14n Story State `in-dev → traced`; Gate WAIVED (14th cumulative trace-WAIVED precedent).
- [x] iter-174: `/bmad-dev-story` Story 2.4 landed single-iteration; FR14n Story State `validated → in-dev`; 8 files touched per SC-19; SC-14 parity smoke PASSED; iteration-env smokes pass; live container smokes 10.1–10.8 + 9.2 deferred.
- [x] iter-173: `/bmad-create-story (args: "review")` pre-dev SM validation of Story 2.4; FR14n Story State `drafted → validated`; 1 CRITICAL + 4 ENHANCEMENTS applied.
- [x] iter-172: `/bmad-create-story` drafted Story 2.4; Status `backlog → ready-for-dev`.

## Context

- **Phase:** 4-implementation — Epic 2 at 3/17 stories `done` (2.1 + 2.2 + 2.3) + 1/17 `review` (2.4 iter-174; traced iter-175; sm-verified iter-176) + 13/17 `backlog` (2.5..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.4 live smokes (Task 10.1–10.8 + 9.2) operator-workstation-deferred per Subtask 10.10 + Story 2.1 iter-127 precedent; Task 10.9 dual-composer parity smoke ran in iteration env (PASSED, byte-identical); SM review iter-176 ACCEPTED the deferral on two grounds (substrate commitments + operator recipes).
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Story 2.4 required NO Dockerfile edits (consumes existing baked tooling).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; 2.1 + 2.2 + 2.3 done; 2.4 sm-verified (at `review` for sprint-status); 2.5..2.17 in `backlog`.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2; PR #230 transitions to Open at Story 2.17 completion).
- **Story:** Story 2.4 — Whitelist source-of-truth + `pnpm devbox:whitelist` atomic-reload CLI.
- **Story File:** `_bmad-output/implementation-artifacts/2-4-whitelist-source-of-truth-pnpm-devbox-whitelist-atomic-reload-cli.md`.
- **Story State:** `sm-verified` — next iteration transitions `sm-verified → done` via `/bmad-code-review (args: "2")` (ZERO-PATCH close) OR `sm-verified → fixes-pending` (default adversarial triage; drain iters follow).
- **GitHub Issue:** Story 2.4 → _(not yet created; RALPH_ISSUE_NUMBER unset at iter-176)_; Epic 2 → #10.
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **iter-176 SM template inheritance ~95% from Story 2.3 iter-160.** Three-layer fan-out structure (AC-by-AC / SC-consistency / scope-isolation) + two-ground defer-acknowledgment rationale + ZERO-PATCH forecast carried verbatim; mechanical adaptation of 5-AC infrastructure-security narrative to 5-AC user-facing-CLI narrative. SM iteration takes ~45K tokens at this template-inheritance rate. Apply at every future Epic-2 SM gate where the class-transition is "first of class" — the AC/SC evidence per layer changes, but the 3-layer framing + two-ground defer rationale + ZERO-PATCH forecast envelope all carry over.
- **iter-176 class-distinction affirmed (iteration-env-evidence-density axis).** Story 2.4's AC-satisfaction posture without live-container runs is STRONGER than Story 2.3's because SC-14 parity PASS + dispatcher/regex/multi-error/source-attribution smokes provide direct iteration-env executable evidence for the user-facing-CLI logic (Story 2.3 had ZERO such evidence — daemon + kernel + log-tailer all required a live container). Class-transition forecast: each Epic-2 story after 2.3 should have MORE iteration-env evidence than 2.3 — consumer-class stories cluster toward higher iteration-env evidence density; substrate-authoritative stories (2.3 primitive, 2.5 hardening) cluster toward live-container-only evidence. Trace-WAIVED + SM ZERO-PATCH rationale becomes progressively stronger as more iteration-env evidence accumulates.
- **iter-176 DOC-DISMISS pattern-forecast carry-to CR.** The v1.4 trace entry cites a stale manifest contentHash (`aad16a51…6889b` iter-158 landing) because Story 2.4's story file was drafted at iter-172 against the iter-158 manifest state, and iter-166 AR-5's Story 2.3 fix-drain refreshed the hash to `cd1b5da1…` without back-propagating the quoted reference to Story 2.4 drafts. Future Ralph-drafted stories referencing contentHash values in prose should cite the CURRENT manifest value at draft time, not the SC-15 "unchanged since registration" value. Iteration-level invariant: pull the hash from `invariants.manifest.ts` at draft time for prose references, not from the initial registration commit. RALPH.md lesson appended iter-176.
- **NFR2 authority unchanged.** Cold ≤ 300s / warm ≤ 30s remains authoritative on M4-Pro native; DinD runs are indicative only.
- **Substrate sanity check (post-iter-176):** Story 2.4 SM review is pure story-doc + IP + RALPH.md update (no code, no config, no substrate touched). Files modified iter-176: `_bmad-output/implementation-artifacts/2-4-whitelist-source-of-truth-…-cli.md` (Status flip + Change Log v1.5 prepend), `.ralph/@plan.md`, `RALPH.md` (signpost append).
- **Cumulative Epic-2 fix-chain forecast tracking:** Story 2.1 = 16-iter chain (9+5+ZERO); Story 2.2 = 4-iter chain (2 PATCH + re-run); Story 2.3 = 10-iter chain (9→0 single-cycle drain); Story 2.4 forecast = 2–4 iter chain (expected 1 trace iter-175 DONE + 1 SM iter-176 DONE + 1 CR opener + 0–1 CR re-run). Trend: smaller surface + iteration-env evidence density = tighter chain. Cumulative Epic-2 SM metrics at iter-176: 4/4 ZERO-PATCH SM opener closes (2.1 iter-127 + 2.2 iter-150 + 2.3 iter-160 + 2.4 iter-176) — SM has consistently produced ZERO-PATCH across 4 distinct story classes (infrastructure-smoke / hybrid / infrastructure-security / user-facing-CLI); SM-layer drift per fan-out design converges on AC-grounded file:line evidence, not post-hoc scope-expansion. CR-layer defers/patches concentrate in adversarial edge-case space, not AC-satisfaction space.
