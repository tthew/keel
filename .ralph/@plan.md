# Implementation Plan

## NOW

- [ ] Run `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-12-loopback-bound-port-publication-opt-in-keel-devbox-ssh-sshd.md")` — Story 2.12 dev-story phase (`atdd-scaffolded → in-dev`). 6 Tasks per story v1.1 spec: Task 1 Dockerfile openssh-server + sshd_config bake; Task 2 compose-override + `resolve_ssh_state()` resolver-lib extension + 8 existing-compose-invoking-shim wire-in via `${KEEL_DEVBOX_COMPOSE_FILE_SSH:+-f "${KEEL_DEVBOX_COMPOSE_FILE_SSH}"}` conditional-expansion idiom; Task 3 entrypoint extension (sshd_config startup handoff + first-boot host-key-gen into `/home/dev/.ssh/host_keys/` via atomic mkdir-tmpdir → generate → mv -T pattern + liveness-check `SSHD_PID="$!"; sleep 0.5; kill -0`); Task 4 `.envrc.example:40` SSH knob pinning (strict `true`-only normalisation per Story 2.11 precedent); Task 5 new `INV-devbox-ssh` manifest entry + `docs/invariants/devbox-ssh.md` + INVARIANTS.md anchor (entry count 31→32; rebuild sync-gate post-InvariantSchema add per iter-257 LESSON); Task 6 operator docs (README § Opt-in SSH + AGENTS.md H3 sibling-append). Impl-time smokes per Story 2.11 iter-257 dev-agent pattern. Forecast: ~70-100K; may split across iters as `in-dev (partial)` per FR14n.

## QUEUE (Story 2.12 lifecycle → Stories 2.13..2.17 → Epic 2 close-out)

- [ ] _(after Story 2.12 `in-dev`)_ `/bmad-testarch-trace (args: "yolo")` — coverage gate + traceability matrix (`in-dev → traced` or `trace-fixes-pending`). Forecast per iter-256 22nd-cumulative ATDD-skip-trace-WAIVED pairing: WAIVE-with-grounds for ACs 4-5 (operator-workstation-deferred + non-loopback peer infeasible in DinD backend B); ACs 1-3 covered by impl-time smokes logged in Debug Log.
- [ ] _(after Story 2.12 `traced`)_ `/bmad-create-story (args: "review")` post-dev SM verification (`traced → sm-verified` or `sm-fixes-pending`). Two-subagent pattern per iter-235 LESSON (AC-verifier + drift-detector).
- [ ] _(after Story 2.12 `sm-verified`)_ `/bmad-code-review (args: "2")` — three-layer adversarial fan-out (`sm-verified → done` or `fixes-pending`). Forecast per iter-260 LESSON: 0-3 PATCH opener. Story 2.12's TWO novel runtime surfaces (compose-override pattern + sshd entrypoint extension with liveness-check) may produce 2-4 second-class DEFERs aligning with iter-266 NOVEL LESSON refined PATCH-count forecast band (6-10 at pre-dev; 0-3 at CR post-PATCH landing).
- [ ] _(Stories 2.13..2.17)_ Iterate through full lifecycle — 5 remaining substrate stories spanning healthcheck (2.13) + legacy branch retention (2.14) + committed settings (2.15) + hooks (2.16) + bypass-resistance (2.17). Each runs full lifecycle; no cross-epic transitions until Epic 2 final story (2.17) completes.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-24 per iter-219..266 repeated pre-push confirmation — `gh pr view 230` returns `statusCheckRollup: []`).

## BLOCKED

_(none — iter-266 push was clean; iter-267 ATDD-skip is non-BLOCKED ZERO-PATCH iter per Story 2.5 iter-186 canonical pattern.)_

## ATDD Red Phase

_(none — iter-267 ATDD-skip-with-grounds-(c)+(ii)+(iii) per FR14n; 22nd-cumulative ATDD-skip precedent. No red-phase tests authored because (ii) no live test runner at 1.0. Impl-time smokes at `/bmad-dev-story` time will cover ACs 1-3 per Story 2.11 iter-257 dev-agent pattern; ACs 4-5 operator-workstation-deferred.)_

## DONE (iter-267 Story 2.12 `/bmad-testarch-atdd` SKIP — `validated → atdd-scaffolded`)

- [x] iter-267: **STORY 2.12 ATDD GATE SKIP-WITH-GROUNDS-(c)+(ii)+(iii) — `validated → atdd-scaffolded`; 22nd-cumulative ATDD-skip precedent (21 prior + Story 2.12 iter-267 = 22).** ZERO-PATCH iter per Story 2.5 iter-186 canonical ATDD-skip pattern (IP + RALPH.md + Story file Status HTML comment only; NO story-file Change Log entry). Preflight verification: `package.json` `test` script is `turbo run test` targeting zero implementing packages (all packages either typecheck-only or resolve `test` to no-op); no `playwright.config.*`/`cypress.config.*`/`vitest.config.*`/`conftest.py`/`bats` anywhere under repo; `_bmad/tea/config.yaml` has `test_stack_type: auto` + `tea_use_playwright_utils: true` but auto-detection returns no runner evidence. Skill would HALT at Step 2 Prerequisites "Test framework configured" requirement per Guardrail 3 but Ralph autonomous path applies skip-with-grounds pattern. **Grounds load-bearing, each required:** **(c) Mixed-class ACs** — AC 1 (loopback-bound port form), AC 2 (default `KEEL_DEVBOX_SSH=false`), AC 3 (opt-in sshd starts) are bash-functional-testable (`docker compose config` port-form grep + sshd_config static verification + entrypoint smoke under stub-docker); AC 4 (external non-loopback refusal) + AC 5 (operator authorized-keys pubkey flow) are explicitly operator-workstation-deferred (live LAN peer + real pubkey-auth flow infeasible in DinD backend B, same posture as Stories 2.5/2.6/…/2.11). Mixed-class matches Story 2.11 iter-256 precedent. **(ii) No live test runner at 1.0** — turbo-wired but empty; Epic 13 CI harness will land test runner downstream. Bash-testable ACs 1-3 land as impl-time smokes at `/bmad-dev-story` time per Story 2.11 iter-257 dev-agent pattern. **(iii) Adversarial-coverage substitutes** — post-dev SM two-subagent pattern (iter-235 AC-verifier + drift-detector) at `traced → sm-verified` + three-layer CR adversarial fan-out (iter-260 Blind Hunter + Edge Case Hunter + Acceptance Auditor with 2-of-3 convergence rule) at `sm-verified → done` + Story 1.9 sync-gate on new `docs/invariants/devbox-ssh.md` contentHash + Epic 13 downstream regression harness. **Artefacts:** (a) Story file Status HTML comment updated verbose-form to record 22nd-skip + grounds + next-iter pointer; (b) IP NOW rotated to `/bmad-dev-story` (QUEUE top promoted per § Story Lifecycle Decision Matrix row `atdd-scaffolded`); (c) RALPH.md signpost appended for iter-268 forecast; (d) no story-file Change Log entry (v1.1 preserved from iter-266 pre-dev SM landing; next Change Log write is v1.2 at dev-story landing per Stories 2.7-2.11 v1.0→v1.1→v1.2 rhythm). Sprint-status UNCHANGED (iter-202 precedent: ATDD gate does NOT flip sprint-row; row remains `ready-for-dev`). Budget consumed iter-267: ~20-25K tokens (orient ~12K + preflight verification ~3K + 3 file edits + IP rewrite + signpost + commit ~5-10K). Well inside ~117K execution budget; exit cleanly per Guardrail 12. Next iter (iter-268) `/bmad-dev-story` fresh-context for `atdd-scaffolded → in-dev`.

_(iter-253..266 Story 2.10/2.11 closure iters + Story 2.12 drafting/pre-dev-SM iters pruned per Guardrail 2.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **11/17 stories done** (2.1-2.11) + 1/17 atdd-scaffolded (2.12) + 5/17 backlog (2.13..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered through iter-267 (iter-263 push recovery, iter-264 three-layer adversarial CR closure ZERO-PATCH confirmation, iter-265 Story 2.12 drafting, iter-266 pre-dev SM gate 8-PATCH landing, iter-267 ATDD-skip-with-grounds).
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:119). Host-side shim count: **18** at iter-267 (unchanged at ATDD-skip iter; Story 2.12 dev-story phase will edit 8 existing compose-invoking shims to thread `${KEEL_DEVBOX_COMPOSE_FILE_SSH:+-f "${KEEL_DEVBOX_COMPOSE_FILE_SSH}"}`).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **11 done** (2.1-2.11); 1 atdd-scaffolded (2.12); 5 backlog (2.13..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.12 — Loopback-bound port publication + opt-in `KEEL_DEVBOX_SSH` sshd
- **Story File:** `_bmad-output/implementation-artifacts/2-12-loopback-bound-port-publication-opt-in-keel-devbox-ssh-sshd.md`
- **Story State:** `atdd-scaffolded` — Story 2.12 ATDD gate landed at iter-267 via `/bmad-testarch-atdd` skip-with-grounds-(c)+(ii)+(iii) per FR14n (22nd-cumulative ATDD-skip precedent; ZERO-PATCH iter). Next iter transitions `atdd-scaffolded → in-dev` via `/bmad-dev-story` per § Story Lifecycle Decision Matrix.
- **GitHub Issue:** Story 2.12 issue unknown; `RALPH_ISSUE_NUMBER` unset at iter-267 orient. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` per iter-219..266 confirmation); push is unblocked at any Story State.

## Notes

- **iter-267 NO NOVEL LESSON — precedent-match 22nd ATDD-skip-with-grounds-(c)+(ii)+(iii).** Story 2.12's skip execution follows Story 2.5 iter-186 canonical pattern verbatim: HTML-comment verbose-form + IP § DONE + RALPH.md Signposts; no Change Log entry. Hybrid-class grounds composition matches Story 2.11 iter-256 (ACs 1-3 bash-functional-testable + ACs 4-5 operator-workstation-deferred); (ii) + (iii) extend Stories 1.7-2.11 21-cumulative precedent. Carry-forward rule confirmed: default posture for Stories 2.13..2.17 remains skip-with-grounds-(c)+(ii)+(iii) until Epic 13 lands a wired test runner; per-story reverification but skip is the null hypothesis.
- **iter-266 NOVEL LESSONS carry-forward:** (1) container-loopback ≠ host-loopback under Docker userland-proxy is a pattern TRAP — defence-in-depth `ListenAddress 127.0.0.1` on top of host-side `127.0.0.1:PORT:PORT` publish is ACTIVELY BROKEN; inbound packets arrive on container eth0 regardless of Docker userland-proxy mode; future port-publishing stories must single-layer host-side-confine only. (2) pre-dev SM PATCH-count CAN break iter-221's ~5 ceiling when adversarial-lens subagents surface runtime substantive bugs; density-amortisation heuristic governs narrow-lens only; runtime substantive bugs are density-independent; budget 6-10 PATCH at pre-dev SM for novel-runtime-behaviour stories.
- **iter-265 NOVEL OBSERVATION carry-forward** — AC 1 already-satisfied pattern: grep current substrate (`packages/devbox/`) for already-implemented surface BEFORE drafting Tasks — iter-148 forward-delivered Story 2.12 AC 1 because Story 2.2's loopback-bound posture was always-on.
- **iter-264 NOVEL LESSON carry-forward** — CR re-run DEFER-vs-PATCH severity-class-first rule: classify by severity class FIRST, convergence-count SECOND. 2-of-3 convergence is high-confidence TRIAGE signal (confirms real finding) but disposition still follows severity class. Second-class drift → DEFER even when convergent; first-class correctness → PATCH even single-layer if AC-blocking.
- **iter-263 LESSON carry-forward** — SSH :22 / HTTPS :443 asymmetric recovery; push via SSH is primary progress vector even when HTTPS :443 `gh pr view` times out. Future BLOCKED recovery always attempts push first regardless of HTTPS state.
- **iter-261 LESSON carry-forward** — PATCH-surface-well-scoped-compact-iter pattern (~15-20K tokens) when single-source-of-truth + one-liner code + three-site doc lockstep.
- **iter-260 LESSONS carry-forward (REFINED by iter-264):** (1) three-layer adversarial fan-out convergence signal — 2-of-3 overlap is **high-confidence triage signal**, NOT automatic PATCH; severity class + finding nature determine final disposition per iter-264 NOVEL LESSON; (2) Blind Hunter false-positive class #1 (unset-array-access under `set -u` without REQUIRED_VARS fence verification); (3) iter-259 two-subagent post-dev SM pattern distinct from CR three-layer adversarial lens.
- **iter-259 LESSONS carry-forward unchanged:** post-dev SM two-subagent pattern; D2 architecture.md:547 7-site cascade pattern; partial-waive trace-gate vocabulary absence; manifest rebuild required after new InvariantSchema entry OR contentHash update.
- **iter-244/246 audit-findings carry-forward** unchanged: restart.sh transitive-delegate; benchmark.sh OUT-OF-SCOPE (SC-11); Manifest entry count **31** post-Story-2.11 (forecast 32 post-Story-2.12 with `INV-devbox-ssh`); Alpine egress non-applicability doctrine.
- **Substrate-citation drift cumulative forecast for Stories 2.12..2.17:** iter-260 baseline (2 MINOR + 5 SC-17 deferrals for Story 2.11) + iter-264 AR-91 (manifest description single-env-var drift D17) + Story 2.12 forecast 2-4 minor drifts (compose-override pattern is novel; 18 source citations may include inflight drifts to flag at post-dev SM). Cumulative Epic 2 forecast: ~26-39 cumulative drifts + 2-3 citation-claim misplacements. SC-17 scope holds: single reconciliation pass at Story 2.17 landing. Deferred-work.md ~580 lines at iter-264 + Story 2.12 lifecycle iters likely add ~10-30 lines.
