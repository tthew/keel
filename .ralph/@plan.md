# Implementation Plan

## NOW

- [ ] Run `/bmad-create-story (args: "review")` — Story 2.12 pre-dev SM gate (`drafted → validated`). iter-265 `/bmad-create-story` landed Story 2.12 file (~280 lines, 5 ACs, 6 Tasks, ~14 SCs); next gate per § Story Lifecycle Decision Matrix row `drafted → validated` is the post-draft pre-dev review. Single-skill-per-iter discipline (Guardrail 5): next iter runs `/bmad-create-story (args: "review")` ONLY — do NOT bundle ATDD or dev-story.

## QUEUE (Story 2.12 lifecycle → Stories 2.13..2.17 → Epic 2 close-out)

- [ ] _(after Story 2.12 `validated`)_ `/bmad-testarch-atdd` — red-phase scaffolds (`validated → atdd-scaffolded`). ATDD-applicable: AC 1-3 testable via `docker compose config` port-form assertions + entrypoint behaviour smokes; AC 4-5 operator-workstation deferral (live SSH + non-loopback peer infeasible in DinD backend B).
- [ ] _(after Story 2.12 `atdd-scaffolded`)_ `/bmad-dev-story (args: "<story-file-path>")` — implement 6 Tasks (`atdd-scaffolded → in-dev`).
- [ ] _(after Story 2.12 `in-dev`)_ `/bmad-testarch-trace (args: "yolo")` — coverage gate + traceability matrix (`in-dev → traced` or `trace-fixes-pending`). Forecast: WAIVE-with-grounds for AC 4 + AC 5 (operator-workstation-deferred per iter-256/258 ATDD-skip + AC-4-doc-only precedents).
- [ ] _(after Story 2.12 `traced`)_ `/bmad-create-story (args: "review")` post-dev SM verification (`traced → sm-verified` or `sm-fixes-pending`). Two-subagent pattern per iter-235 LESSON (AC-verifier + drift-detector).
- [ ] _(after Story 2.12 `sm-verified`)_ `/bmad-code-review (args: "2")` — three-layer adversarial fan-out (`sm-verified → done` or `fixes-pending`). Forecast: 0-3 PATCH opener; Story 2.12 fourth+ ZERO-PATCH chain extension candidate per iter-244 LESSON (chain-of-4 → base-rate expectation).
- [ ] _(Stories 2.13..2.17)_ Iterate through full lifecycle — 5 remaining substrate stories spanning healthcheck (2.13) + legacy branch retention (2.14) + committed settings (2.15) + hooks (2.16) + bypass-resistance (2.17). Each runs full lifecycle; no cross-epic transitions until Epic 2 final story (2.17) completes.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-24 per iter-219..264 repeated pre-push confirmation — `gh pr view 230` returns `statusCheckRollup: []`).

## BLOCKED

_(none — iter-265 `/bmad-create-story` Story 2.12 landing expected push clean per RALPH.md § Lessons iter-263 SSH :22 / HTTPS :443 asymmetric recovery rule.)_

## ATDD Red Phase

_(none yet — Story 2.12 ATDD scaffolds queued for next+1 iter. AC 1-3 testable via `docker compose config` port-form assertions; AC 4-5 operator-workstation-deferred. Story 2.11 ATDD-skip carried-forward stays empty.)_

## DONE (iter-265 Story 2.12 `/bmad-create-story` landing — `_(no story) → drafted`)

- [x] iter-265: **STORY 2.12 `/bmad-create-story` LANDING — `_(no story) → drafted`; sprint-row `2-12-…: backlog → ready-for-dev`.** Authored `_bmad-output/implementation-artifacts/2-12-loopback-bound-port-publication-opt-in-keel-devbox-ssh-sshd.md` (~280 lines, 5 ACs, 6 Tasks, ~14 SCs). Synthesis: AC 1 (loopback-bound port publication) ALREADY satisfied at Story 2.2 landing (`docker-compose.yml:153-157` already `127.0.0.1:`-form per Story-2.12-reduced-rework note at `.envrc.example:33`); Story 2.12 PINS as `INV-devbox-ssh § Loopback-bound port publication contract`. Real new work: opt-in sshd via (a) Dockerfile `openssh-server` apt addition; (b) NEW `packages/devbox/sshd/sshd_config` template (mirrors Story 2.3 `dnsmasq/`+`nftables/` template-dir convention); (c) NEW `packages/devbox/docker-compose.ssh.yml` compose-override conditionally publishing port 2222 — included via NEW `resolve_ssh_state()` resolver function (Story 2.11 pattern precedent) exporting `KEEL_DEVBOX_COMPOSE_FILES_ARGS` consumed by 9 compose-invoking shims; (d) entrypoint.sh first-boot host-key-gen into `/home/dev/.ssh/host_keys/` (named-volume persistence; AC 3 + AC 5); (e) NEW `INV-devbox-ssh` invariant + `docs/invariants/devbox-ssh.md` + INVARIANTS.md anchor (manifest entry count 31 → 32 forecast). Compose-override pattern is novel for Epic 2 (Stories 2.1-2.11 single-file compose). Defence-in-depth via `ListenAddress 127.0.0.1` in sshd_config + Docker's iptables-NAT loopback-bind. Sshd runs as `dev` UID 1000 (port 2222 >1024 → no NET_BIND_SERVICE needed). 18 source citations across PRD/architecture/epics/.envrc.example/Dockerfile/compose/entrypoint/resolver-lib/manifest/sync-gate. SC-17 sibling-append docs convention preserved. Sprint-status row flipped `2-12-…: backlog → ready-for-dev` + new `last_updated: 2026-04-24 Story-2-12-ready-for-dev-iter-265` comment + canonical `last_updated:` field. RALPH.md § Signposts iter-265 entry appended. Per § Story Lifecycle Decision Matrix row `drafted → validated`: NOW = `/bmad-create-story (args: "review")` next iter (pre-dev SM gate). Forecast per iter-264 LESSON + iter-260 NOVEL LESSON: 0-3 PATCH CR opener; Story 2.12 fourth+ ZERO-PATCH-chain extension candidate per iter-244 LESSON (chain-of-4 → base-rate expectation for Stories 2.10..2.17).

_(iter-253..264 Story 2.10/2.11 closure iters pruned per Guardrail 2 — see RALPH.md § Signposts iter-253..264 + Story 2.10/2.11 Change Logs for per-gate detail.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **11/17 stories done** (2.1-2.11) + 1/17 drafted (2.12 ready-for-dev) + 5/17 backlog (2.13..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered through iter-265 (iter-263 push recovery, iter-264 three-layer adversarial CR closure ZERO-PATCH confirmation, iter-265 Story 2.12 `/bmad-create-story` landing).
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:119). Host-side shim count: **18** at iter-265 (unchanged at create-story phase; Story 2.12 dev-story phase will not add new shims — 9 existing compose-invoking shims will be edited to consume `${KEEL_DEVBOX_COMPOSE_FILES_ARGS}` resolver output).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **11 done** (2.1-2.11); 1 ready-for-dev (2.12); 5 backlog (2.13..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.12 — Loopback-bound port publication + opt-in `KEEL_DEVBOX_SSH` sshd
- **Story File:** `_bmad-output/implementation-artifacts/2-12-loopback-bound-port-publication-opt-in-keel-devbox-ssh-sshd.md`
- **Story State:** `drafted` — Story 2.12 file authored at iter-265 via `/bmad-create-story`. Next iter transitions `drafted → validated` via `/bmad-create-story (args: "review")` per § Story Lifecycle Decision Matrix.
- **GitHub Issue:** Story 2.12 issue unknown; `RALPH_ISSUE_NUMBER` typically unset for this project (last confirmed unset at iter-263 orient). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` per iter-219..264 confirmation); push is unblocked at any Story State.

## Notes

- **iter-265 NOVEL OBSERVATION — AC 1 already-satisfied finding.** Story 2.12 AC 1 (loopback-bound port publication) was already implemented at Story 2.2 landing (iter-148) per the explicit `.envrc.example:33` "127.0.0.1 loopback-bound per Story 2.12; published by Story 2.2" comment + `docker-compose.yml:153-157` ports block evidence. Story 2.12's Task surface for AC 1 reduces to (a) PINS the posture as `INV-devbox-ssh § Loopback-bound port publication contract`, (b) extends the contract to ALL future port additions (including the new sshd override file). Real new work concentrates on ACs 2-5 (opt-in sshd path). Pattern carry-forward for Stories 2.13..2.17: when reading new-story epic ACs, grep current substrate (`packages/devbox/`) for already-implemented surface BEFORE drafting Tasks — iter-148 forward-delivered Story 2.12 AC 1 because Story 2.2's loopback-bound posture was always-on, not opt-in.
- **iter-264 NOVEL LESSON carry-forward — CR re-run DEFER-vs-PATCH severity-class-first rule.** When PATCH iter updates a hashed source-file AND its manifest contentHash but leaves the manifest `description` string untouched, the description field drifts agent-readable but sync-gate-undetectable. Convergent 2-of-3 flagging on this drift class is legitimate signal BUT iter-260 LESSON's "2-of-3 = PATCH" rule was distilled from FIRST-class correctness findings; second-class agent-readable drift follows severity class → DEFER even when convergent. Manifest `description` string is architecturally out-of-sync-gate-hash-surface by Story 1.9 design; drifts there belong in SC-17 close-out reconciliation pass.
- **iter-263 LESSON carry-forward** — SSH :22 / HTTPS :443 asymmetric recovery; push via SSH is primary progress vector even when HTTPS :443 `gh pr view` times out. Future BLOCKED recovery always attempts push first regardless of HTTPS state.
- **iter-261 LESSON carry-forward** — PATCH-surface-well-scoped-compact-iter pattern (~15-20K tokens) when single-source-of-truth + one-liner code + three-site doc lockstep.
- **iter-260 LESSONS carry-forward (REFINED by iter-264):** (1) three-layer adversarial fan-out convergence signal — 2-of-3 overlap is **high-confidence triage signal**, NOT automatic PATCH; severity class + finding nature determine final disposition per iter-264 NOVEL LESSON; (2) Blind Hunter false-positive class #1 (unset-array-access under `set -u` without REQUIRED_VARS fence verification); (3) iter-259 two-subagent post-dev SM pattern distinct from CR three-layer adversarial lens.
- **iter-259 LESSONS carry-forward unchanged:** post-dev SM two-subagent pattern; D2 architecture.md:547 7-site cascade pattern; partial-waive trace-gate vocabulary absence; manifest rebuild required after new InvariantSchema entry OR contentHash update.
- **iter-244/246 audit-findings carry-forward** unchanged: restart.sh transitive-delegate; benchmark.sh OUT-OF-SCOPE (SC-11); Manifest entry count **31** post-Story-2.11 (forecast 32 post-Story-2.12 with `INV-devbox-ssh`); Alpine egress non-applicability doctrine.
- **Substrate-citation drift cumulative forecast for Stories 2.12..2.17:** iter-260 baseline (2 MINOR + 5 SC-17 deferrals for Story 2.11) + iter-264 AR-91 (manifest description single-env-var drift D17) + Story 2.12 forecast 2-4 minor drifts (compose-override pattern is novel; 18 source citations may include inflight drifts to flag at post-dev SM). Cumulative Epic 2 forecast: ~26-39 cumulative drifts + 2-3 citation-claim misplacements. SC-17 scope holds: single reconciliation pass at Story 2.17 landing. Deferred-work.md ~580 lines at iter-264 + Story 2.12 lifecycle iters likely add ~10-30 lines.
