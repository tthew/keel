# Implementation Plan

## NOW

- [ ] iter-94: `/bmad-code-review (args: "2")` — FR14n row 9 `sm-verified → done` SIXTH cumulative ZERO-PATCH CR candidate; Story 1.16 `done` → Epic 1 fully done → EPIC_DONE sequence begins. ~medium.

## QUEUE (Story 1.16 — fork-extension scaffold + INVARIANTS.fork scaffold)

- [ ] iter-95: Transition PR #226 Draft → Open — final CI gate via `gh pr ready && gh pr checks --watch --fail-fast`; post-CI-green mark epic-1 `in-progress → done` in sprint-status + EPIC_DONE halt write.

## BLOCKED

_(none)_

## DONE (current epic — trailing iterations only)

- [x] iter-93: **FR14n row 7 `traced → sm-verified` via `/bmad-create-story (args: "review")` post-dev SM ZERO-PATCH — SIXTH cumulative Epic-1 precedent.** (Stories 1.11 iter-58 + 1.12 iter-65 + 1.13 iter-72 + 1.14 iter-79 + 1.15 iter-86 → 1.16 iter-93.) Independent fresh-context Sonnet Explore subagent audited all 4 ACs against iter-91 committed implementation + live filesystem state + scope-carve-out clauses. **All 4 ACs SATISFIED; zero unmet-AC findings; zero patch requirements.** All 6 Task 7 substrate smokes re-verified LIVE at SM re-check byte-identical to iter-91 dev-story + iter-92 trace records: scaffold template sha256 `167ba6b2…6218` unchanged + rationale doc sha256 `be6f3d89…92f2` unchanged + static scaffold-shape + rationale-doc-shape + AGENTS.md § Fork extension + CLAUDE.md INVARIANTS.fork.md smokes all exit OK + manifest load `OK: 24 invariants` + `pnpm keel-invariants:check` exits 0 silent in **0.614s** wall-clock (69.3% margin under Story 1.9 AC 7 <2s budget; best sync-gate time across Story 1.16 lifecycle). **Per-AC audit verdicts:** AC 1 SATISFIED (AGENTS.md § Fork extension FR44 documents ESLint-extend pattern with copy-ready code block + subpath-export reference `@keel/keel-invariants/eslint` at `packages/keel-invariants/package.json:14`); AC 2 SATISFIED (AGENTS.md precedence rule via spread-at-end convention + amendment-vs-fork 3-path decision tree FORK/AMEND/DEFER; `docs/invariants/fork.md § Amendment-vs-fork decision tree` provides fuller rationale); AC 3 SATISFIED (scaffold template exists with canonical shape + CLAUDE.md 5th row in § Knowledge-file contract table + precedence paragraph); AC 4 SATISFIED (`docs/invariants/fork.md § Growth-tier opt-in` documents 1.0-default-false posture + `packages/create-keel-app/` is a Story 1.1 empty shell `export {};` with NO `--include-fork-invariants` flag + manual opt-in flow documented). **Drafting-time carve-out prose drift surfaced:** story carve-outs at iter-88 + iter-92 claimed `packages/create-keel-app/` not-yet-existing, but the package shell has actually existed since Story 1.1 monorepo scaffold (just empty `export {};`). This is INHERITED-SPEC-DRAFTING-DRIFT class per Story 1.14 iter-80 + Story 1.15 iter-87 precedent — NOT an AC violation (AC 4 is about CLI BEHAVIOUR non-auto-create, not package-directory-non-existence); routes to iter-94 CR DEFER (architectural-defer / documentation-drift class) rather than PATCH at SM. Per Story 1.15 iter-86 precedent: post-dev SM verifies requirements-satisfaction ONLY; any adversarial findings or documentation-drift corrections drop into iter-94 CR DEFER candidates, not PATCH at SM. Compound-ZERO-PATCH-discipline held through conditions (a)-(d): (a) seven preventative audit layers iter-88 drafting; (b) 3-PATCH pre-dev SM iter-89 (drift introduced at drafting, corrected pre-dev); (c) single-pass dev iter-91; (d) ZERO-PATCH trace iter-92 (TENTH cumulative WAIVED) + ZERO-PATCH post-dev SM iter-93 (SIXTH cumulative). Change Log v1.5 appended. Story State `traced → sm-verified`; BMad Status unchanged (`review` — retained until CR closes at iter-94). sprint-status `last_updated: 2026-04-21 Story-1-16-sm-verified UTC` at header-comment + data-field. **Carry-forward INTO iter-94 `/bmad-code-review (args: "2")`:** (1) 6-file audit surface — `packages/keel-invariants/templates/INVARIANTS.fork.md` (scaffold shape + FORK-naming example) + `docs/invariants/fork.md` (10 sections + anchor) + `packages/keel-invariants/src/invariants.manifest.ts` (+2 entries at array tail) + `INVARIANTS.md` (new `### Fork extension (Story 1.16)` H3 + 2 anchor bullets) + `AGENTS.md` (`## Fork extension (FR44)` H2) + `CLAUDE.md` (5th row + precedence paragraph); (2) SIXTH ZERO-PATCH CR candidate; (3) DEFER forecast 8-12 per iter-91 dev-time RALPH.md lesson — comparable to Stories 1.14/1.15 at 10 DEFER each; (4) **at-least-one DEFER candidate already surfaced** — the iter-88 + iter-92 drafting-time `packages/create-keel-app/ not-yet-existing` carve-out prose drift (the shell has existed since Story 1.1 as `export {};`; no CLI logic yet, so AC 4 is satisfied semantically but the drafting prose should be corrected when Epic 15a actually lands). Post-CR: `done` (no findings) → iter-95 PR Draft→Open + EPIC_DONE sequence.
- [x] iter-92: **FR14n row 5 `in-dev → traced` via `/bmad-testarch-trace (args: "yolo")` TENTH cumulative Epic-1 WAIVED precedent.** (Stories 1.7 iter-4 + 1.8 iter-3 + 1.9 iter-3 + 1.10 iter-46 + 1.11 iter-57 + 1.12 iter-64 + 1.13 iter-71 + 1.14 iter-78 + 1.15 iter-85 → 1.16 iter-92). Four trace artefacts authored via ~90% structural inheritance from Story 1.15 iter-85 template under `_bmad-output/test-artifacts/traceability/1-16-*`: coverage-matrix.json + e2e-trace-summary.json + gate-decision.json + full trace report. All 6 Task 7 smokes re-verified LIVE byte-identical at iter-92 trace-time. Three conjoint grounds held (a)+(b)+(c) hybrid variant-(ii)+(iii). Change Log v1.4 appended. Story State `in-dev → traced`.
- [x] iter-91: **FR14n row 4 `atdd-scaffolded → in-dev` via `/bmad-dev-story` SINGLE-PASS at configuration-surface + documentation-tier scale.** SIXTH cumulative single-pass-dev precedent (Stories 1.10/1.11/1.12/1.13/1.14/1.15 → 1.16). Authored 2 NEW files + MODIFIED 4 files; all 7 substrate-verification smokes PASS; full quality-gate suite clean; sync-gate 0.776s (61.2% margin). Seven preventative audit layers held ZERO-PATCH at dev-time. Change Log v1.3 appended.
- [x] iter-90: **FR14n row 3 `validated → atdd-scaffolded` via `/bmad-testarch-atdd` TENTH cumulative Epic-1 ATDD-SKIP precedent** (Stories 1.7 iter-14 + 1.8 iter-29 + 1.9 iter-36 + 1.10 iter-43 + 1.11 iter-50 + 1.12 iter-57 + 1.13 iter-64 + 1.14 iter-71 + 1.15 iter-83 → 1.16 iter-90). Hybrid ground-(c) variant-(ii)+(iii) substitution pattern held identical to Story 1.15 iter-83. Change Log v1.2 appended.
- [x] iter-89: **FR14n row 2 `drafted → validated` via `/bmad-create-story (args: "review")` pre-dev SM readiness review for Story 1.16.** THREE-PATCH outcome (ZERO-PATCH narrowly missed; matches 1.11-1.15 1-2 PATCH average). Change Log v1.1 appended.
- [x] iter-88: **FR14n row 1 `_(no story) → drafted` via `/bmad-create-story`** for Story 1.16 (FINAL open story in Epic 1). Sprint-status flipped `backlog → ready-for-dev`.

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1-1.15 done; Story 1.16 final open story — 2-iter FR14n lifecycle remaining before EPIC_DONE halt: iter-94 `sm-verified → done`, iter-95 PR Draft→Open + EPIC_DONE).
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (continues through Epic 1; PR #226 stays Draft until EPIC_DONE per PROMPT_build.md step 5c).
- **Story:** Story 1.16 — Fork extension-config pattern + Growth-tier `INVARIANTS.fork.md` scaffold (FR44 + FR45).
- **Story File:** `_bmad-output/implementation-artifacts/1-16-fork-extension-config-pattern-growth-tier-invariants-fork-md-scaffold.md` (iter-88 created; iter-89 3-PATCH pre-dev SM at v1.1; iter-90 v1.2 ATDD-skip; iter-91 v1.3 dev-story; iter-92 v1.4 trace-WAIVED; iter-93 v1.5 post-dev SM ZERO-PATCH).
- **Story State:** `sm-verified` — iter-94 next FR14n row 9 `sm-verified → done` via `/bmad-code-review (args: "2")` (SIXTH cumulative ZERO-PATCH CR candidate per Stories 1.11/1.12/1.13/1.14/1.15 FIFTH precedent).
- **GitHub Issue:** Story 1.16 at **#40** OPEN. Parent Epic 1 at **#9** OPEN (closes at EPIC_DONE halt).
- **PR:** #226 (Draft, `no checks reported` at iter-93 start — push-safe; stays Draft through iter-94 Story 1.16 lifecycle; Draft→Open at iter-95 post-Story-1.16 `done`).

## Notes

- **Story 1.16 iter-93 post-dev SM outcome: SIXTH cumulative Epic-1 ZERO-PATCH post-dev SM precedent** (Stories 1.11 iter-58 + 1.12 iter-65 + 1.13 iter-72 + 1.14 iter-79 + 1.15 iter-86 → 1.16 iter-93). All 4 ACs SATISFIED at substrate-layer verification; zero unmet-AC findings; zero patch requirements. Sync-gate wall-clock 0.614s — best time across Story 1.16 lifecycle (iter-91 0.776s → iter-92 byte-identical → iter-93 0.614s; improvement attributed to disk-cache warmth or prek background state; still within noise band for O(n+m) walker on 24 invariants).
- **Inherited drafting-time carve-out drift surfaced at iter-93 SM audit.** Story 1.16 carve-outs at iter-88 + iter-92 claimed `packages/create-keel-app/` not-yet-existing. The package shell has actually existed since Story 1.1 monorepo scaffold (`packages/create-keel-app/package.json` + `src/index.ts` = `export {};` — no CLI logic). This is INHERITED-DRAFTING-DRIFT class per Story 1.14 iter-80 architecture.md:597 precedent + Story 1.15 iter-87 architecture.md:305/:649 precedent — routes to iter-94 CR DEFER (NOT PATCH at SM). AC 4 is SATISFIED because the `--include-fork-invariants` flag is NOT implemented; AC 4 is about CLI BEHAVIOUR non-auto-create, not package-directory-non-existence.
- **Carry-forward rules INTO iter-94 `/bmad-code-review (args: "2")` for Story 1.16:**
  - Skill invocation: `/bmad-code-review (args: "2")` pre-selects "Create action items" mode. Three-layer fan-out: Blind Hunter (diff-only adversarial review) + Edge Case Hunter (diff + repo-read) + Acceptance Auditor (AC-verification).
  - ZERO-PATCH target per FIFTH cumulative CR precedent (Stories 1.11/1.12/1.13/1.14/1.15 all ZERO-PATCH at CR).
  - Audit surface across 6 files: `packages/keel-invariants/templates/INVARIANTS.fork.md` (scaffold shape + FORK-naming example) + `docs/invariants/fork.md` (10 sections + anchor) + `packages/keel-invariants/src/invariants.manifest.ts` (+2 entries at array tail with correct id/sourcePath/contentHash/anchors) + `INVARIANTS.md` (new `### Fork extension (Story 1.16)` H3 + 2 anchor bullets column-0 format) + `AGENTS.md` (`## Fork extension (FR44)` H2 between `## Git / PR conventions` + `## Ralph loop`) + `CLAUDE.md` (5th row in § Knowledge-file contract + precedence paragraph).
  - Expected DEFER count 8-12 per iter-91 dev-time RALPH.md lesson — comparable to Stories 1.14/1.15 at 10 DEFER each (configuration-surface + documentation-tier ceiling; adversarial surface is static content attack surface — ESLint-extend documentation fidelity + precedence-rule prose accuracy + amendment-vs-fork decision tree completeness + template-file shape integrity + CLAUDE.md contract-row accuracy + manifest-entry description faithfulness).
  - **At-least-one DEFER candidate already surfaced at iter-93 SM audit:** iter-88 + iter-92 drafting-time `packages/create-keel-app/ not-yet-existing` carve-out prose drift (the shell has existed since Story 1.1 as `export {};`; no CLI logic yet, so AC 4 is satisfied semantically). Defer-reason: inherited-drafting-drift class + Epic 15a architectural-defer (the correction lands naturally when Epic 15a authors the `--include-fork-invariants` flag and CLI tests).
  - Post-CR: `done` (no findings) → iter-95 PR Draft→Open + EPIC_DONE sequence.
- **Carry-forward rules INTO iter-95 PR Draft→Open + EPIC_DONE for Story 1.16:**
  - Skill invocation: `gh pr ready && gh pr checks --watch --fail-fast` on PR #226 (currently Draft, no checks reported).
  - Final-CI-gate scope: full quality-gate suite (typecheck + lint + format:check + keel-invariants:check-all + tokens-sync) must pass on main-branch-synced PR head. Expected green (all substrate-authoring + test-plan work at story-level; CI runners not yet authored pre-Epic-13).
  - Post-CI-green: sprint-status flips `epic-1: in-progress → done`; write EPIC_DONE halt `{"reason":"EPIC_DONE","epic":1,"pr":226}` to `$RALPH_BASE_DIR/halt`. ralph.py reads sentinel + halts loop + closes Epic-1 issue #9.
- **Issue Tracking carry-forward.** Story 1.16 at **#40** OPEN — iter-93 start re-confirms Story `**Story:**` field unchanged (ralph.py no-op on same-story iteration). Parent Epic 1 at **#9** OPEN — closes at EPIC_DONE halt after iter-95. Commit trailer: `Refs #40`.
