# Implementation Plan

## NOW

- [ ] AR-9 — replace `$(printf … | sort)` command-substitution in `packages/devbox/scripts/start-egress.sh:63` with `mapfile -t fragments < <(LC_ALL=C find "${WHITELIST_FRAGMENTS_DIR}" -maxdepth 1 -type f -name '*.txt' -print | sort); for fragment in "${fragments[@]}"` so whitespace-containing fragment filenames are preserved. Budget: ~15K tokens.

## QUEUE (Story 2.3 fix-drain; 1 patch remaining → re-run CR → done)
- [ ] `/bmad-code-review (args: "2")` re-run — verdict `done` (ZERO-PATCH close) OR cycle continues. Precedent: Story 2.1 iter-128..iter-144 3-cycle chain (9 → 5 → 0 AIs); Story 2.2 iter-151..iter-154 single fix-drain + re-run close (2 → 0 AIs). Story 2.3 forecast per iter-161 envelope: 1–2 re-run cycles (9 → likely 1–3 residual → 0 close). Budget: ~60K tokens.
- [ ] _(conditional, post-Story-2.3 done)_ `/bmad-create-story` for Story 2.4 (whitelist CLI: `packages/devbox/scripts/whitelist.sh` user-facing with add/remove/list/sync + per-fork override + diff summary + invocation of `reload-egress.sh`) per § Story Lifecycle Decision Matrix row `done` → auto-advance within-epic. Story 2.4 is infrastructure-CLI class (new class for Epic 2; may or may not inherit ATDD-skip / WAIVED-trace precedents depending on whether Epic 13 has landed a test runner by then).
- [ ] _(conditional, post-Story-2.17 done, ~Epic 2 completion)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge. Epic 2 completion.
- [ ] _(conditional, post-Epic-2 merged)_ § Cross-epic transition → Epic 3 Story 3.1 OR halt EPIC_DONE awaiting merge.

## BLOCKED

_(none — Story 2.3 CR opener landed iter-161 fixes-pending; 9 PATCH fix-drain queue is deterministic.)_

## ATDD Red Phase

_(none — ATDD-skipped at iter-157; no red-phase failures to carry.)_

## DONE (current story 2.3 fix-drain; prune oldest as drain progresses)

- [x] iter-169: **AR-8 fix-drain landed — rebuilt `cleanup_files` array via explicit loop at `packages/devbox/scripts/reload-egress.sh:292-303` instead of bash parameter-substitution `${cleanup_files[@]/${dnsmasq_rendered}}`.** Pre-fix form substituted the matched element to empty string — array length stayed unchanged at 3 (`[nft_rendered, '', <whatever else]`) and the EXIT trap `cleanup()` only skipped the `''` entry because of its `[[ -n "${f}" && -e "${f}" ]]` guard. Post-fix: `new_cleanup=(); for f in "${cleanup_files[@]}"; do [[ "$f" != "${dnsmasq_rendered}" ]] && new_cleanup+=("$f"); done; cleanup_files=("${new_cleanup[@]}")` actually removes the consumed tempfile so the array content matches its semantic invariant ("live tempfiles pending cleanup, nothing else"). 9-line inline comment pins rationale (old form harmless in practice but obscures intent + relies on downstream guard for correctness). **Ran 5-probe isolated harness against both old and new forms:** (i) OLD behavior — length stays 3 with `[1]=''` empty leak at the matched index (confirms CR diagnosis); (ii) NEW behavior — length drops to 2 with `[0]=/tmp/a, [1]=/tmp/c` clean removal; (iii) defensive — element-not-present returns array unchanged, no corruption; (iv) empty-result edge — sole element removed, length 0 under `set -euo pipefail` clean (bash 5.2 empty-array assignment is safe); (v) trap-iteration against OLD array — confirms `-n "${f}"` guard catches empty, so bug was truly harmless, just intent-obscuring. `bash -n` exit 0. No invariant-doc contentHash refresh needed (verified at iter-165/166/167: zero `reload-egress.sh` contentHash references in `packages/keel-invariants/`; `INV-devbox-egress-contract` hashes `docs/invariants/devbox-egress.md` only, which this PATCH does not touch). Budget: ~10K tokens (on forecast). Promoted bash `${array[@]/pattern}` blanking-semantics lesson to RALPH.md § Gotchas as a general shell-scripting rule (applies to future Stories 2.3/2.4/2.5 + Epic 13 CI harness shell helpers). **Drain trajectory:** 8/9 PATCH landed (AR-1..AR-8); 1 remains (AR-9). AR-8 closes the shell-array-semantics defensive-edge cluster; AR-9 closes the command-substitution-vs-mapfile cluster, then re-run `/bmad-code-review (args: "2")` to confirm `done` OR cycle continues.
- [x] iter-168: **AR-7 fix-drain landed — introduced per-chain marker names in `packages/devbox/nftables/egress.nft` (`KEEL_EGRESS_V4_MARKER_{START,END}` inside `output_v4`; `KEEL_EGRESS_V6_MARKER_{START,END}` inside `output_v6`) + chain-scoped awk substitution in `packages/devbox/scripts/reload-egress.sh` (dropped the `chain` state variable entirely).** Refactor also parameterised `validate_markers()` to accept a marker-name argument (same exit-6 diagnostic; label now includes `marker=<NAME>`); 3 preflight call sites (v4 + v6 chain-scoped + unchanged dnsmasq at `KEEL_EGRESS_ALLOWLIST_MARKER`). Design rationale pinned in the egress.nft header comment (L13-L20): shared-marker + chain-state awk was structural-order-coupled (reorder chains, rename a header past the regex, add a third chain → silent mis-route); chain-scoped names make injection self-disambiguating so the awk state machine no longer carries position as correctness. **Ran 6-probe verification harness against isolated-copy templates:** (i) happy path — real egress.nft + synthetic v4/v6 rule blocks → injection correct per chain, no cross-family leakage (v4 region contains 192.0.2.10/.11, no 2001:db8; v6 region contains 2001:db8::10, no 192.0.2); (ii) fault — deleted V6_END marker → exit 6 diagnostic "found 1 START / 0 END" with marker=KEEL_EGRESS_V6_MARKER; (iii) fault — duplicated V4_START → exit 6 "found 2 START / 1 END" marker=KEEL_EGRESS_V4_MARKER; (iv) fault — unknown marker name → exit 6 "found 0 START / 0 END"; (v) dnsmasq template still validates cleanly at expected=1 with the unchanged KEEL_EGRESS_ALLOWLIST_MARKER; (vi) **chain-swap structural-order independence** — built a python-reordered egress.nft with v6 chain above v4 chain and re-rendered: v4 injection correct, v6 injection correct, zero cross-contamination. This is THE guarantee AR-7 closes. `bash -n` exit 0. No invariant-doc contentHash refresh needed (egress.nft not content-hashed; `docs/invariants/devbox-egress.md` — which IS content-hashed under `INV-devbox-egress-contract` — does not reference the marker name literally, verified via grep). Budget: ~22K tokens (under ~25K forecast). **Drain trajectory:** 7/9 PATCH landed (AR-1 + AR-2 + AR-3 + AR-4 + AR-5 + AR-6 + AR-7); 2 remain (AR-8 + AR-9). AR-7 closes the structural-order-independence cluster; AR-8 + AR-9 are the residual shell-script-defensive-edge cluster.
- [x] iter-167: **AR-6 fix-drain landed — added `validate_markers()` helper + per-awk-invocation preflight in `packages/devbox/scripts/reload-egress.sh`.** Helper defined immediately after `log()` at L46; called twice — expected-count `2` for `NFT_TEMPLATE` immediately before the nft awk (L199-203 block) and expected-count `1` for `DNSMASQ_TEMPLATE` immediately before the dnsmasq awk (L243-245 block). Grep pattern `^[[:space:]]*# KEEL_EGRESS_ALLOWLIST_MARKER_{START,END}` mirrors the awk substitution regex character-for-character so preflight and substitution agree on what counts as a marker (non-anchored grep would false-match the `KEEL_EGRESS_ALLOWLIST_MARKER_{START,END}` brace-expansion reference in template header comments — verified the anchored form ignores those). Preflight fails exit 6 (same code as template-missing) with a 4-line diagnostic: `ERROR: <label> template marker imbalance: <path>` + `expected: N START / N END` + `found: X START / Y END` + `silent awk substitution would corrupt the allow-list region — aborting reload`. `bash -n` exit 0. **Ran five verification probes against isolated-extract harness:** (i) happy path — real egress.nft (2/2) + real dnsmasq.conf (1/1) both pass exit 0; (ii) fault A — deleted one END marker in nft copy — exit 6 diagnostic "found 2 START / 1 END"; (iii) fault B — duplicated a START marker in nft copy — exit 6 diagnostic "found 3 START / 2 END"; (iv) fault C — real dnsmasq.conf with wrong expected=2 — exit 6 diagnostic "found 1 START / 1 END" (proves the expected-count check fires independently of the balance check); (v) fault D — empty template (0 markers) — exit 6 diagnostic "found 0 START / 0 END". No invariant-doc contentHash refresh needed (verified: zero `reload-egress.sh` contentHash references in `packages/keel-invariants/`; `INV-devbox-egress-contract` hashes `docs/invariants/devbox-egress.md` only — which this PATCH does not touch). Budget: ~18K tokens (under ~20K forecast). **Drain trajectory:** 6/9 PATCH landed (AR-1 + AR-2 + AR-3 + AR-4 + AR-5 + AR-6); 3 remain (AR-7 + AR-8 + AR-9). AR-6 closes the defensive-preflight cluster at the awk-substitution boundary; AR-7 continues into the structural-order-independence cluster (per-chain marker names, drop the `chain` state variable).
- [x] iter-166: **AR-5 fix-drain landed — replaced `pgrep -x dnsmasq` process-table match in `packages/devbox/scripts/start-egress.sh:117-130` with a composite positive-serving liveness probe.** Added constant `DNSMASQ_PID_FILE="/run/dnsmasq.pid"` alongside the existing `TAILER_PID_FILE` + `COMPOSED_WHITELIST` top-of-file constants. The probe now composes TWO independent positive signals per 5-attempt retry loop, BOTH required: (i) `[[ -s "${DNSMASQ_PID_FILE}" ]] && kill -0 "$(cat …)"` proves dnsmasq started cleanly enough to populate its pidfile (catches pgrep-passes-but-pidfile-absent class, e.g. crashed mid-init); (ii) `timeout 1 bash -c '</dev/tcp/127.0.0.1/53' 2>/dev/null` proves dnsmasq bound its listening socket per dnsmasq.conf L28-30 `listen-address=127.0.0.1,::1` + `bind-interfaces` + `port=53` (catches process-present-port-unbound class, common when drop-privileges transitions fail). Diagnostic FATAL log on timeout explicitly names both failure modes: `pidfile=${DNSMASQ_PID_FILE} missing/stale OR 127.0.0.1:53 not accepting TCP`. **Operator recipe drift closed in same commit** — `docs/invariants/devbox-egress.md:85` AC-1 verification updated from bare `pgrep -x dnsmasq` to the same composite probe via `docker exec keel-devbox bash -c '[[ -s /run/dnsmasq.pid ]] && kill -0 "$(cat /run/dnsmasq.pid)" && timeout 1 bash -c "</dev/tcp/127.0.0.1/53"'`. Rejected stronger variant `getent ahostsv4 <whitelisted-domain>` (pinned rationale in inline comment + RALPH.md): couples liveness to upstream DNS reachability, turns transient upstream outages into false-negative container-startup failures. `bash -n` exit 0. **Invariant-doc contentHash refresh REQUIRED** (`docs/invariants/devbox-egress.md` IS content-hashed under `INV-devbox-egress-contract` at `packages/keel-invariants/src/invariants.manifest.ts:261`; editing the operator AC-1 recipe at L85 drifted the file) — refreshed `aad16a51…6889b → cd1b5da1…e2d5` in the same commit, rebuilt via `pnpm --filter @keel/keel-invariants build`, sync-gate `pnpm keel-invariants:check` exit 0 green. `start-egress.sh` itself has zero contentHash references. Budget: ~15K tokens (vs ~20K forecast — hash-refresh roundtrip added ~3K). **Drain trajectory:** 5/9 PATCH landed (AR-1 + AR-2 + AR-3 + AR-4 + AR-5); 4 remain (AR-6..AR-9). AR-5 closes the positive-liveness-probe cluster and opens the defensive-edge cluster (AR-6 = marker-validation preflight).
## Context

- **Phase:** 4-implementation — Epic 2 at 2/17 stories `done` (2.1 iter-144 + 2.2 iter-154) + 1/17 `in-progress` (2.3 iter-161 CR-opener-fixes-pending). Story 2.3 Status `review → in-progress`; sprint-status row flipped; fix-drain begins iter-162.
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.3 live smokes (Task 12.1 – 12.8) operator-workstation deferred; the iter-161 PATCH cluster is dominated by edges those deferred smokes would have caught at bake-time — drain order prioritises AR-1 + AR-2 first as they are container-boot kill-switches.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Story 2.3 Task 1 appended `dnsmasq` + `nftables` to image; re-bake needed at operator-workstation close-out.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; 2.1 + 2.2 done; 2.3 `in-progress` (iter-161 CR opener → fixes-pending); 2.4..2.17 in `backlog`.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2; PR #230 transitions to Open at Story 2.17 completion).
- **Story:** 2.3 — egress-policy-dnsmasq-nftables-fail-closed-ipv4-ipv6-parity-atomic-reload.
- **Story File:** `_bmad-output/implementation-artifacts/2-3-egress-policy-dnsmasq-nftables-fail-closed-ipv4-ipv6-parity-atomic-reload.md` (v1.6 at iter-161; Status `in-progress`; § Review Findings inserted between Tasks/Subtasks and Dev Notes; 9 PATCH action items + 9 DEFER checkmarks).
- **Story State:** `fixes-pending` — CR opener landed iter-161 9 PATCH; iter-162 drained AR-1 (first-boot resolver detour in reload-egress.sh); iter-163 drained AR-2 (idempotent `add table` prepend in egress.nft); iter-164 drained AR-3 (in-place resolv.conf overwrite + immutable-flag diagnostic in start-egress.sh); iter-165 drained AR-4 (split `getent` exit capture from `awk|sort` cleanup pipeline in reload-egress.sh); iter-166 drained AR-5 (pidfile+kill-0 + TCP-probe composite positive-serving liveness in start-egress.sh + mirror op-recipe at devbox-egress.md:85 + contentHash refresh); iter-167 drained AR-6 (marker-validation preflight `validate_markers()` helper + per-awk-invocation call in reload-egress.sh; 5-probe harness verified happy + 4 fault modes exit 6); iter-168 drained AR-7 (per-chain marker names `KEEL_EGRESS_V{4,6}_MARKER` in egress.nft + chain-scoped awk in reload-egress.sh with `chain` state variable dropped + `validate_markers()` parameterised on marker-name; 6-probe harness verified happy + 3 fault modes + dnsmasq unchanged + chain-swap structural-order independence); iter-169 drained AR-8 (explicit-loop rebuild of `cleanup_files` array in reload-egress.sh to actually remove the consumed `dnsmasq_rendered` tempfile rather than blanking it to `''` via `${array[@]/pattern}`; 5-probe harness verified old-vs-new semantics + defensive + empty-result edges). 1 PATCH remains (AR-9). Drain final PATCH per § Story Lifecycle Decision Matrix row `fixes-pending`; when QUEUE empties → re-run `/bmad-code-review (args: "2")` to confirm `done` OR continue cycle.
- **GitHub Issue:** Story 2.3 → #43; Epic 2 → #10.
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **Acceptance Auditor ZERO-drift at CR opener upholds iter-160 SM verdict.** The 9 PATCH findings are all bug-class (boot-sequence + shell-script defensive edges), not spec drift — reflects the Story 2.3 author's adherence to substrate commitments (ACs + SCs) while the operator-workstation-deferred smokes (Task 12.1 – 12.8) left runtime-edge coverage gaps. Iter-161 **validates** iter-160's operator-workstation-defer-acknowledgment framing: substrate is AC-correct, runtime is bake-time-verifiable, CR opener catches edges that would have surfaced at operator-workstation bake.
- **Convergent PATCH signal is the highest-confidence axis for fix-drain.** AR-1 + AR-4 + AR-5 convergent across Blind + Edge Case (all three layers see the first-boot resolver-bootstrap failure from different angles); AR-3 + AR-8 convergent. Drain these first — they're the least likely to be over-triaged. (Self-retraction + subshell-fd lessons from iter-161 promoted to RALPH.md L303/L305; removed from IP.)
- **DinD is fork-time substrate, not transitional.** `INV-devbox-dind-available` — every fork's cc-devbox-equivalent must provide Docker. Story 2.3 live smokes are operator-workstation deferred; SC-7 verbatim + AC-mapped `docker exec` recipes pinned in `docs/invariants/devbox-egress.md § Verification` + `packages/devbox/README.md § Egress policy § Verification` for the operator close-out.
- **Backend B is the reference env at 2026-04-21.** Broad-state-mutation scripts MUST detect backend + refuse destructive ops by default.
- **NFR2 authority unchanged.** Cold ≤ 300s / warm ≤ 30s remains authoritative on M4-Pro native; DinD runs are indicative only.
- **Cumulative Epic-2 CR metrics (Stories 2.1 + 2.2 + 2.3 opener):** 2.1 = 9 patches across 3 gates (16-iter fix-chain); 2.2 = 2 patches across 1 gate (4-iter fix-chain); 2.3 = 9 patches across 1-or-2 gates projected (forecast 6–9 iter fix-chain per iter-159/160 envelope). Story 2.3's 9-PATCH opener is at the upper end of iter-160's 2–6 PATCH forecast — +3 above ceiling, consistent with the +3 backend-B live-smoke-defer overhead materialising as boot-sequence patches rather than spec drift.
