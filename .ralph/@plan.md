# Implementation Plan

## NOW

- [ ] `/bmad-testarch-trace (args: "yolo")` — FR14n row 5 `in-dev → traced`. Fifth cumulative WAIVED precedent expected (Stories 1.7/1.8/1.9/1.10 + now 1.11 — contract-populator stories at substrate stage inherit the same gate posture; mirror Story 1.10's trace artefact structure per iter-46 structural-inheritance Lesson — ~90% less reasoning authoring cost).

## QUEUE (Story 1.11 — design-token source, Direction A baseline with motion + density scales)

- [ ] `/bmad-create-story (args: "review")` — FR14n row 7 `traced → sm-verified`. Expected: first-round pass if the iter-53 preventative audit + iter-54 pre-dev SM review + iter-56 dev-pass held cleanly end-to-end. Mechanical-fix round-trip budget = 1 iter if any structural gap surfaces (Story 1.10 iter-47..49 precedent). **Watch for:** surfacing of the story-vs-schema provenance-block conflict (story Task 1 subtask 2 variance) — was documented in iter-56 Debug Log but SM reviewer may flag as unmet AC unless they accept the carve-out rationale.
- [ ] `/bmad-code-review (args: "2")` — FR14n row 9 `sm-verified → done` (or `sm-verified → fixes-pending` if action items surface; budget 1-3 CR rounds per iter-7 contract-only-single-pass hypothesis + iter-52 halving-to-zero-at-round-2 trajectory for contract-populator stories with ≤1 convergent PATCH at round 1). **Potential CR findings:** (a) root-provenance-block skip rationale (Blind Hunter may flag as missing AC 1 DTCG ratification; defense: per-leaf `$description`s + Debug Log + commit trailer + Change Log v1.2 = sufficient provenance capture); (b) dark-mode `surface.inset` vs `surface.default` contrast (5pp L delta — may be flagged as too-subtle separation; defense: shadow-machinery is Story 1.12's scope, not source); (c) font-family array-vs-string deferral (pre-documented in deferred-work.md + Change Log).

## BLOCKED

_(none)_

## DONE (Story 1.11 dev complete + carry-forward context)

- [x] iter-56: **`/bmad-dev-story` implementation landed; FR14n state transition `atdd-scaffolded → in-dev` per matrix row 4.** Single-pass (no partial completion). Tasks 1-4 all complete: authored `packages/ui/tokens.json` DTCG source (~106-leaf Direction A population — 11-step neutral ramp at hue 0 chroma 0; 6-slot accent with `oklch(54% 0.18 245)` at 500; 4 surface + 5 text + 3 border aliases; 10 status leaves across 5 hue families × fg/bg; 4 severity + 4 kanban-state aliases; 8-stop type scale; 2-family font stacks; 13-stop 4px space; 5-stop radius; 6 motion + 4 density leaves with dial+tier pattern; 5 breakpoint stops; light + dark `$modes` overlays). `pnpm dlx ajv-cli@5 validate --spec=draft2020` → `packages/ui/tokens.json valid`. Appended `INV-tokens-source` manifest entry (13th; contentHash `27e8cb58...`) + INVARIANTS.md § Design-token source (Story 1.11) section + column-0 anchor. Quality gates: `pnpm install` (lockfile unchanged), `pnpm -w typecheck` (16 tasks), `pnpm -w lint` (16 tasks), `pnpm -w build` (16 tasks with scoped keel-invariants rebuild), `pnpm format:check` (canonical), `pnpm exec prek run --all-files` (3 hooks Passed). Sync-gate smokes: smoke 1 content-hash-mismatch round-tripped (pre `27e8cb58...` → newline-append `52865468...` → prettier-revert `27e8cb58...` = exit 0); smoke 2 added-to-source-only round-tripped (anchor Edit-delete → exit 1 + drift JSON → `git checkout --` INITIALLY WIPED STORY 1.11 EDITS → Edit re-apply → exit 0); smoke 3 skipped per story rationale (duplicate of Story 1.9 AC 2). Sprint-status direct-jump `1-11-...: ready-for-dev → done`; story file Status flip; Dev Agent Record populated; Change Log v1.2 row appended. **Two NEW Ralph lessons captured in RALPH.md 2026-04-20 iter-56 Signpost:** (1) story-vs-schema provenance-block conflict (Task 1 subtask 2 variance — root $schema/$description DTCG block forbidden by schema's `additionalProperties: false`; candidate preventative audit #6 for Story 1.12+ populator stories); (2) drift-smoke 2 revert gotcha extension (`git checkout -- <tracked-file>` wipes uncommitted edits; canonical pattern = tmp-file backup + restore, cost: 2 extra `cp` invocations). PR #226 CI `no checks reported` at iter-56 start; push-safe.
- [x] iter-55: **`/bmad-testarch-atdd` HALT at preflight prerequisite gate; FR14n state transition `validated → atdd-scaffolded` per matrix row 3.** Fifth cumulative ATDD-skip application (Stories 1.7/1.8/1.9/1.10/1.11). No `playwright.config.*` / `cypress.config.*` / `vitest.config.*` / `jest.config.*` / `conftest.py` at Story 1.11 time (test runner lands Story 1.16 per architecture.md:650-652). Hybrid ground-(c) rationale: (c.ii) Story 1.13 pre-commit schema-validation gate owns AC 1 integration coverage + (c.iii) `/bmad-code-review (args: "2")` adversarial fan-out is pinned substitute per story § Dev Notes lines 197-200. Zero new files; zero red-phase scaffolds; zero sprint-status mutation.
- [x] iter-54: `/bmad-create-story (args: "review")` pre-dev SM review; FR14n state `drafted → validated`. SM-1 + SM-2 applied in-place (internal hue consistency + off-by-one line ref). Change Log v1.1 row. Full context in RALPH.md iter-54 Signpost.
- [x] iter-53: `/bmad-create-story` drafted Story 1.11 file. Full context in RALPH.md iter-53 Signpost.

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1–1.10 done; 1.11 in-dev (dev-story complete) + next-up for `/bmad-testarch-trace`; 1.12–1.16 backlog → 5 more stories × full FR14n lifecycle remaining after 1.11 closes before EPIC_DONE)
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (continues through Epic 1; PR #226 stays Draft until EPIC_DONE per PROMPT_build.md step 5c)
- **Story:** Story 1.11 — Design-token source, Direction A baseline with motion + density scales
- **Story File:** `_bmad-output/implementation-artifacts/1-11-design-token-source-direction-a-baseline-with-motion-density-scales.md`
- **Story State:** `in-dev` — FR14n matrix row 4 (`atdd-scaffolded → in-dev`) landed at iter-56 via single-pass `/bmad-dev-story`; next iter-57 runs `/bmad-testarch-trace (args: "yolo")` for row 5 `in-dev → traced`.
- **GitHub Issue:** Story 1.11 tracked at **#35** (OPEN); parent Epic 1 tracked at **#9** (OPEN).
- **PR:** #226 (Draft, MERGEABLE, no-CI-checks-reported — stays Draft through Epic 1; Draft→Open at EPIC_DONE after stories 1.11–1.16 all complete FR14n lifecycle).

## Notes

- **Story 1.11 sizing actuals vs projection.** iter-55 projected `iter-56 in-dev (single-pass)` — CONFIRMED single-pass no partial. Updated trajectory: iter-53 drafted → iter-54 validated → iter-55 atdd-scaffolded → **iter-56 in-dev (done)** → iter-57 traced → iter-58 sm-verified → iter-59 CR-done = **7 iterations** (floor held). Epic 1 close-out timing unchanged at ~41 more iterations to EPIC_DONE halt (5 stories remaining × ~8 iter median each + CR round variance).
- **Iter-56 Lessons — captured in RALPH.md iter-56 Signpost; two material additions:**
  - **Story-vs-schema provenance-block conflict (Task 1 subtask 2 variance).** Candidate preventative audit #6 for Story 1.12+ populator stories: walk schema's `additionalProperties` posture at every level touched by story-mandated ADDITIONS (not just story-mandated REQUIRED leaves which iter-47 covers). Apply at drafting time (iter-53-equivalent) OR at pre-dev SM review (iter-54-equivalent).
  - **Drift-smoke 2 revert gotcha extension.** `git checkout -- <tracked-file>` wipes uncommitted edits — Story 1.10 iter-45's new-untracked-file lesson extended. Canonical pattern for Story 1.12+ drift-smoke 2: pre-drift `cp INVARIANTS.md /tmp/.bak`; post-smoke `cp /tmp/.bak INVARIANTS.md`. Cost: 2 extra `cp` invocations.
- **Carry-forward rules INTO Story 1.11 SM/CR layers** (iter-52 + iter-53 + iter-54 + iter-56):
  - **Two-layer CR convergence doctrine** (iter-36 + iter-52): single-layer Blind-Hunter findings are default DISMISS unless corroborated. Apply aggressively at Story 1.11's CR triage iterations.
  - **Trace artefact structural inheritance** (iter-46): mirror Story 1.10's trace artefact at iter-57.
  - **Preventative audits carry into SM review iteration (iter-58):** if any drift-smoke revert or manifest-hash re-computation slipped in, audit will surface via gate re-run. Expected clean.
  - **CR triage defenses pre-staged in IP QUEUE Notes above** — at iter-59 CR time, lean on: (a) root-provenance skip rationale = Debug Log + commit trailer + Change Log = sufficient; (b) dark-mode surface.inset contrast = Story 1.12 emitter scope; (c) font-family string-vs-array = deferred-work.md prior art.
- **Next iteration (iter-57) expected outcome:** `/bmad-testarch-trace (args: "yolo")` runs in a fresh context window. Expected output: `_bmad-output/test-artifacts/1-11-trace-coverage-yolo.md` structured identically to Story 1.10's equivalent (per iter-46 structural-inheritance Lesson) with WAIVED gate decision citing: (a) no test runner at Story 1.11 time (architecture.md:650-652), (b) AC 1 schema-validation evidence captured one-shot in Story 1.11 Completion Notes (`ajv-cli@5 validate` output), (c) AC 4 sync-gate smoke evidence captured end-to-end in Completion Notes (smoke 1 + smoke 2 round-trip logs), (d) CR-substitution for AC 1 value-correctness + AC 2 negative-space + AC 3 tier-enumeration per story § Dev Notes lines 197-200. Budget: small (~8K — copy-adjust Story 1.10's trace artefact structure). Commit trailer `Refs #35`; push after CI-clear check; exit.
- **Issue Tracking:** Story 1.11 tracked at GitHub issue **#35** (OPEN at iter-56 time). `Refs #35` in iter-56 commit trailer. `Closes #35` deferred to PR body when PR #226 transitions Draft→Open at EPIC_DONE halt. Parent Epic 1 issue **#9** — `Refs #9` optional; never `Closes` it.
