# Implementation Plan

## NOW

- [ ] **Story 2.16 `/bmad-create-story (args: "review")` post-dev SM** (`traced → sm-verified`). Per § Story Lifecycle Decision Matrix row `traced`. Forecast PATCH band 0-3 per iter-270 NOVEL LESSON drift-band re-baseline + iter-300 narrow-after-SM-absorb precedent (pre-dev SM at iter-303 absorbed 3 PATCH; ATDD SKIP at iter-304 ZERO-PATCH; dev-story at iter-305 ZERO-PATCH despite NOVEL hook-self-immolation incident; trace at iter-306 ZERO-PATCH — cumulative lifecycle PATCH = 3, tracking middle of iter-286 6-10 band). **Special scope at iter-307:** SM should ALSO verify the hook-self-immolation recovery trace is accurately captured in Change Log 0.4 + 0.5 + Completion Notes (NOVEL incident; Story 2.17 has scope-inherent interest in the lesson-learned for bypass-resistance pre-install `bash -n` discipline).

## QUEUE (Story 2.16 lifecycle + 2.17 substrate queue)

- [ ] _(after Story 2.16 sm-verified)_ Story 2.16 `/bmad-code-review (args: "2")` CR → done or fixes-pending. Forecast 1-4 PATCH per iter-301 NOVEL LESSON CANDIDATE (doc-heavy CR drift) + Story 2.16 wider doc-touch surface (12 files vs Story 2.15's 8); explicit subagent-budget guidance per iter-301 LESSON: enumerate the 12 touched files in Blind Hunter prompt to keep token consumption < 100K per subagent.
- [ ] _(after Story 2.16 done)_ Story 2.17 full lifecycle — hook-settings bypass-resistance + Epic 2 SC-17 close-out polish pass. SC-17 reconciliation absorbs cumulative DEFER queue at iter-306 close: **55 cumulative Epic-2 DEFERs** (58 at Story 2.15 CR close MINUS 3 absorbed at Story 2.16 hook layer: D-1/D-3/D-6 via recursive globs + prefix-anchor case-globs + cat-parity rules) + 3 SC-17 close-out candidates (D-7/D-8/D-9 from iter-306 trace) = **58 DEFERs total** + whatever Story 2.16 post-dev SM + CR accrues. Story 2.17 expected to absorb the remaining 8 Story 2.15 CR DEFERs + any Story 2.16 DEFERs. Story 2.17 bypass-resistance scope MAY codify pre-install `bash -n` discipline per iter-305 NOVEL LESSON (hook self-immolation mitigation).
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured; `statusCheckRollup: []` carried unchanged iter-219..306).

## BLOCKED

_(none — iter-306 push expected on first try per iter-302 + iter-303 + iter-304 + iter-305 consecutive first-try success baseline — 4 data points; iter-301 NOVEL LESSON CANDIDATE SSH :22 retry budget promoted to § Lessons promotion eligible if iter-306 first-try succeeds (5 data points).)_

## ATDD Red Phase

_(none — Story 2.16 `/bmad-testarch-atdd` SKIP-WITH-GROUNDS-(ii) single-ground waiver landed at iter-304 per pre-pinned forecast; dev-story iter-305 landed in a single pass with 14 impl-time fixture smokes serving as adversarial-coverage substitute per story file § Testing standards summary. NOVEL incident during dev-story iter-305 — hook self-immolation — was recovered within iter; no red-phase scaffolds pending. Trace iter-306 confirmed functional substrate verification via the 14 smokes closes the ground-(a) gate for all 8 ACs.)_

## DONE (iter-306 `/bmad-testarch-trace (args: "yolo")` LANDING — Story State `in-dev → traced`)

- [x] iter-306: **STORY 2.16 `/bmad-testarch-trace (args: "yolo")` LANDING** (`in-dev → traced`). 3 trace artefacts authored at `_bmad-output/test-artifacts/traceability/`: `2-16-claude-pretooluse-hooks-for-secret-file-denylist-ralph-compatible.md` (686 lines) + `2-16-e2e-trace-summary.json` (119 lines; parse-valid) + `2-16-gate-decision.json` (22 lines; parse-valid). **Gate: WAIVED** (structural deterministic FAIL overridden — overall 0% < 80% is false-positive since no test runner wired at 1.0). **TWENTY-SIXTH CUMULATIVE TRACE-WAIVED PRECEDENT** extending Story 2.15 iter-299 twenty-fifth (unbroken 26-story chain Stories 1.7-1.16 + 2.1-2.16). **SIXTEENTH Epic 2 trace-WAIVED + FIRST Epic-2-substrate-claude-pretooluse-hooks class + FIRST Epic-2 trace-WAIVED with executable-bash-logic fixture-smoke substrate gate** (novel ground-(a) hardening: 14 impl-time bash smokes provide FUNCTIONAL verification of hook behaviour, not just static substrate probe — distinct from 15 prior Epic-2 configuration-only deltas). Ground-(a)+(b) hybrid conjunction with BROADENED ground-(a) posture + PARTIAL ground-(c) variant-(ii) narrowed to AC 5 Epic 3 Story 3.7 halt-write consumer + AC 8 Epic 4 Story 4.13 security-evidence consumer + AC 1/2 operator-workstation-class live `claude` subprocess behavioural signal. All 8 ACs substrate-verified (P0 0/0 empty; P1 0/0 empty; P2 8/8 P2-uniform; P3 0/0 empty). Source SHA `128e1b7b75ddfd87b46c88a61085650e6fb127b0`. Story file Status comment flipped `atdd-scaffolded → review` → `in-dev → traced`; Change Log 0.5 row appended. Sprint-status row UNCHANGED at `review` per FR14n convention. ZERO PATCH (policy-only delta). Cumulative lifecycle PATCH unchanged at 3 (0 at trace — matching 25-precedent zero-PATCH trace-WAIVED norm). Commit candidate: `docs(story-2-16): iter-306 /bmad-testarch-trace (args: "yolo") — in-dev → traced; 26TH cumulative trace-WAIVED; FIRST Epic-2 with executable-bash-logic fixture-smoke substrate gate`.

_(iter-302 / iter-303 / iter-304 / iter-305 LANDING detail pruned per Guardrail 2; retained in commits `d9e031b` / `441d710` / `7c89ff1` / `128e1b7` + story-file Status HTML comment chain + story-file Change Log 0.1-0.5 rows.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **15/17 stories done** (2.1-2.15) + 2.16 `traced` at iter-306 + 2.17 backlog.
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live smokes operator-workstation-deferred.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:121; bash + jq baked at :36-37 + :48 — load-bearing for Story 2.16 hook script functional verification). Host-side shim count: **18** at iter-306 (unchanged — trace iter; no devbox-shim substrate code edits).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **15 done** (2.1-2.15); **1 traced** (2.16); **1 backlog** (2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.16 — Claude PreToolUse hooks for secret-file denylist (Ralph-compatible). **TRACED at iter-306** (trace skill GATE: WAIVED; 26th cumulative trace-WAIVED; FIRST Epic-2 with executable-bash-logic fixture-smoke substrate gate).
- **Story File:** `_bmad-output/implementation-artifacts/2-16-claude-pretooluse-hooks-for-secret-file-denylist-ralph-compatible.md`
- **Story State:** `traced` — Sprint-status row `2-16-…: review` UNCHANGED per FR14n convention (trace skill does NOT flip sprint-status). Next iter (iter-307) picks up `traced → sm-verified` per § Story Lifecycle Decision Matrix row `traced → /bmad-create-story (args: "review")`.
- **GitHub Issue:** Story 2.16 issue unknown; `RALPH_ISSUE_NUMBER` unset (no GitHub Project configured). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` unchanged iter-272..306).

## Notes

- **iter-306 OBSERVATION — FIRST Epic-2 trace-WAIVED with executable-bash-logic fixture-smoke substrate gate (promoted to RALPH.md § Lessons candidate):** Story 2.16 extends the 25-precedent Epic-2 trace-WAIVED chain with a NOVEL ground-(a) posture. Stories 2.13/2.14/2.15 were all configuration-only deltas with filesystem + JSON-shape + grep probes. Story 2.16 adds EXECUTABLE verification via 14 impl-time bash fixture smokes (5 AC-pinned + 7 D-1/D-3/D-6/env/printenv/git-hook mitigation + 2 negative approve-path) that functionally verify the hook's 27-rule denylist behaviour + JSON decision shape + JSONL append schema. Per § ATDD-applicability predicate line 426, Story 2.16 is "the FIRST Epic-2 story shipping executable bash logic that is unit-testable inside the devbox without external dependencies." This hardens ground-(a) from static-only probe to functional-bash-smoke verification — TRACK for Story 2.17 (bypass-resistance content-hash backstop scope has SIMILAR executable-bash-logic surface in the sync-gate expansion but is NOT unit-testable in the same way — it's script-landing static gate at lockstep-lint level).
- **iter-306 PRESERVED carry-forward — iter-305 NOVEL LESSON (hook self-immolation):** A PreToolUse hook that (a) registers itself for the Bash/Read/Edit/Write/Grep/Glob matchers AND (b) includes a `hook-self-protection` denylist against editing the hook file itself creates a self-immolation failure mode — any intermediate hook authoring bug traps the dev agent in an unrecoverable state for the 6 hooked tool surfaces. Claude Code 2.1.116 treats hook syntax-error exit as BLOCK with output suppression (NOT fail-open as upstream docs claim). Recovery requires an UNHOOKED tool (`Monitor` confirmed working; `TodoWrite`, `Skill`, `Agent`, `ScheduleWakeup`, `WebFetch`, `WebSearch` are all candidates — but only `Monitor` and `Agent` can run arbitrary commands). Future-Ralph rule: for ANY story that installs a PreToolUse hook with self-protection rules (Story 2.16, Story 2.17 bypass-resistance expansion, Epic 3 Story 3.32 hook-self-protection halt-wire-up), validate the hook's bash syntax via `bash -n` BEFORE registering it in `.claude/settings.json` `hooks.PreToolUse`. Install the hooks.PreToolUse registration ONLY after `bash -n` GREEN on the hook script. If discovered mid-iter (as at iter-305), use `Monitor` tool as the escape-hatch — pipe a Python rewrite script through Monitor's `command` field. A defensive convention for Story 2.17 bypass-resistance work: keep a KNOWN-GOOD hook backup somewhere outside the `.claude/hooks/*` matcher surface (e.g. `/tmp/hook-backup.sh`) so recovery is a simple `cp` rather than re-authoring from scratch. **Story 2.17 consideration:** codifying the pre-install `bash -n` discipline as a committed pre-commit hook (distinct from `.git/hooks/**` which is covered by hook-self-protection) would close the loop mechanically.
- **iter-306 PRESERVED carry-forward — iter-298 LESSON (zero-PATCH dev-story one-pass validates forecast equation):** Story 2.16 iter-305 landed all 8 Tasks + 41 subtasks in a single dev-story pass with ZERO PATCH at the implementation stage — matches iter-298 Story 2.15 precedent. Pre-dev SM at iter-303 absorbed 3 PATCH (under the iter-221-pinned ~5-PATCH SM-ceiling); dev-story stage consumed 0 of the iter-286-forecast 3-6 band; trace at iter-306 ZERO-PATCH. **Three-data-point pattern** (Story 2.15 + Story 2.16 + Story 2.16 trace re-confirms) — narrow-diff doc-heavy stories where the pre-dev SM pass absorbs substantive findings can drain the iter-286 forecast envelope entirely at SM review, leaving 0 PATCH for dev-story + 0 at trace + (forecast) 0-3 PATCH each at post-dev SM + CR. Track Story 2.17 for the 4-point confirmation; if it holds, promote from NOVEL LESSON to stable LESSON.
- **iter-306 PRESERVED carry-forward — iter-301 NOVEL LESSON CANDIDATE (doc-heavy narrow-diff CR drift):** Story 2.15 cumulative lifecycle PATCH = 9 (5 pre-dev SM + 4 CR) — exceeded iter-286 NOVEL LESSON 3-6 forecast band upper edge by +3. Pattern: pre-dev SM absorbs substantive surface findings; prose-level drift emerges ONLY at CR when all doc-landings are in place + cross-refs empirically testable. Story 2.16 iter-303 pre-dev SM 3 PATCH (middle of 2-6 band); iter-304 ATDD SKIP-WITH-GROUNDS-(ii) ZERO-PATCH; iter-305 dev-story ZERO-PATCH; iter-306 trace ZERO-PATCH. Story 2.16 has WIDER doc surface than Story 2.15 (12 files vs 8); cumulative 6-12 PATCH forecast stands if pattern generalises — expect 3-9 PATCH across post-dev SM + CR iters (307-308+). Current cumulative = 3; if post-dev SM + CR lands 3-9 more, Story 2.16 cumulative would be 6-12 — consistent with forecast.
- **iter-306 PRESERVED carry-forward — iter-301 NOVEL LESSON CANDIDATE (SSH :22 egress retry budget extension):** iter-301 required FOUR retries with exponential pacing for `git push` under DinD backend B SSH :22 flake; iter-302 + iter-303 + iter-304 + iter-305 orient `gh pr view 230` succeeded first try (no flake); iter-306 push outcome TBD at commit time. **Four-data-point first-try success baseline** (iter-302..305); iter-306 push = fifth data point if first-try succeeds → promotion-eligible to § Lessons stable. Carry-forward posture: budget 4-5 retries with exponential pacing for `git push` under DinD backend B; do NOT escalate to halt after three.
- **iter-306 PRESERVED carry-forward — iter-301 subagent-budget LESSON for Story 2.16 CR (iter-308):** Blind Hunter spent ~150K tokens + 58 tool_uses exploring the repo at Story 2.15 CR — exceeded budget; should have been scoped tighter to diff-only reads of the touched files. Carry-forward for Story 2.16 CR (iter-308): explicitly list the 12 touched files in the Blind Hunter prompt; enumerate touched files upfront rather than relying on agent to discover via `git diff --stat`.
- **Cumulative DEFER forecast for Stories 2.16..2.17 Epic-2 close-out (updated at iter-306):** 58 cumulative Epic-2 DEFERs at iter-301 Story 2.15 CR close (47 carry-forward from Story 2.14 CR + 11 from Story 2.15 CR) MINUS 3 absorbed at Story 2.16 hook layer (D-1/D-3/D-6 mitigated via recursive globs + case-globs + cat-parity rules at iter-305) = **55 cumulative Epic-2 DEFERs at iter-306 trace close** + 3 SC-17 close-out candidates (D-7/D-8/D-9 from iter-306 trace — substrate-to-seed byte-identity diff lint + NFR5a/5b deny-list minimum-entry gate + hook-self-protection halt-threshold three-site lockstep) = **58 DEFERs pending Story 2.17** + whatever Story 2.16 post-dev SM + CR iters accrue. Story 2.17 scope-inherent closes most; final SC-17 absorption at Story 2.17 CR close ~55-60 absorbed.
