# Implementation Plan

## NOW

- [ ] Run `/bmad-code-review (args: "2")` — CR opener for Story 2.18 per § Story Lifecycle Decision Matrix row `sm-verified → done | fixes-pending` (FR14n).

## QUEUE (Story 2.18)

- [ ] _(if PATCHes)_ Drain CR action items one-per-iter; re-run CR.
- [ ] Transition PR #235 Draft→Open — final CI gate (Epic 2 close-out for Story 2.18).

## BLOCKED

_(none)_

## ATDD Red Phase

_(empty — Story 2.18 took the ATDD-skip route at iter-349 per § ATDD Skip Rationale below; no red-phase scaffolds produced; nothing to clear post-dev.)_

## ATDD Skip Rationale (Story 2.18 — iter-349)

Hybrid ground (a)+(b)+(c) variant-(ii)+(iii) per FR14n § Story Lifecycle Decision Matrix ATDD-skip clause + RALPH.md iter-297 multi-ground requirement (bare ground-(b) INSUFFICIENT):

- **(a) substrate-verifies-AC.** AC1 / AC2 / AC4 + AC5 unit half + AC3 mechanism wiring all reduce to file-content commitments by the dev-agent at `atdd-scaffolded → in-dev`: `nftset=` directive emission in `reload-egress.sh:301-315` (post-dev landed); named set declarations + accept rules + static CIDR fallback in `egress.nft:64-73` + `:115-116` + `:122` + `:159` (post-dev landed); `*-rotating.txt` annotation contract + `.classification` classifier sidecar (SC-11 byte-identity) in both `compose_whitelist()` (`start-egress.sh:158-164`) and `compose_whitelist_into()` (`whitelist.sh:142-145`). Iteration-env config-render smokes (Subtasks 9.1 + 9.2 + 9.3 `bash -n`) verify all three.
- **(b) no test runner at substrate.** Zero `vitest.config.*` / `jest.config.*` / `playwright.config.*` / `cypress.config.*` / `pyproject.toml` / `go.mod` / `Gemfile` / `Cargo.toml` / `csproj` anywhere in tree. Epic 13 (PRD RS6) is the formal test-framework landing.
- **(c) variant-(ii) downstream-epic-covers.** Epic 13 formal test-framework owns kernel-state regression coverage for AC3 + AC5 behavioural half (live `nft list set` accumulation across multiple `getent` rotations + `flush table` → re-fill round-trip per SC-7). Until Epic 13 lands, backend-B operator-workstation smokes (Subtasks 9.4 + 9.5) cover the live-kernel surface — this matches Story 2.1 iter-127 + Story 2.3 iter-159 + Story 2.4 iter-176 precedent.
- **(c) variant-(iii) spec-declared-CR-substitution.** 11 pinned scope-clarifications (SC-1 through SC-11) + 4 Dev Notes risk-register entries + the forthcoming `/bmad-code-review (args: "2")` adversarial fan-out (Blind Hunter + Edge Case Hunter + Acceptance Auditor against cumulative Story 2.18 substrate diff) substitute for red-phase scaffolds.
- **PARTIAL-AC ground-(c) variant-(ii) on AC3 + AC5 behavioural half** per RALPH.md iter-299 — substrate-verifies the mechanism wiring; live-kernel state requires hardened operator workstation per Story 2.5 `cap_add: [NET_ADMIN, NET_RAW]` posture + DNS upstream connectivity (the very bug being fixed — chicken-and-egg only at first deployment).

## DONE

- [x] iter-352 (this iter): **`/bmad-create-story (args: "review")` post-dev SM verification for Story 2.18 — FR14n Story State `traced → sm-verified`.** Two-subagent SM-review fan-out (technical-correctness substrate-citation verifier + prose-density / spec-quality reviewer per RALPH.md iter-235 narrow-surface pattern + iter-281 source-range probe discipline + iter-308 diff-surface upfront enumeration). **Subagent A (technical-correctness): GREEN — zero MUST-FIX, zero unjustified PARTIAL.** Every AC substrate-met or properly partial-AC'd against post-dev substrate (AC1/AC2/AC4 substrate-verifiable; AC3+AC5 PARTIAL-AC ground-(c) variant-(ii) properly justified per RALPH.md iter-299 + Story 2.3/2.4/2.5 backend-B precedent); manifest contentHash byte-identical to source-of-truth file (Story 1.9 sync-gate clean); SC-11 dual-composer parity logically byte-identical between awk classification blocks; sprint-status row consistent. 3 NIT line-range drifts in informational pointers — deferred. **Subagent B (prose-density): GREEN-LEANING-YELLOW — 1 MUST-FIX + 3 SHOULD-FIX + 4 NIT + 1 LLM-OPT.** **Seven PATCHes applied inline at this gate** (5-PATCH bundle across 7 distinct edit sites): (i) MUST-FIX 1 SC-2 line 123 — "SC-5 LDH regex" → "Story 2.4's SC-5 line-bound regex contract" disambiguation; (ii–iv) SHOULD-FIX 1 three-site cluster — v1.1 PATCH-(g) Task 5 simplification's downstream-reference debt closed: SC-5 line 126 reworded to acknowledge timeout is hard-coded; Project Structure Notes line 198 dropped `.envrc.example` from substrate-edit touch-set; References line 219 `.envrc.example` pointer reworded to "NOT touched at this story scope"; (v) SHOULD-FIX 2 Risk-register line 194 stale "Subtask 7.4 (Story 2.4)" placeholder closed as "CLOSED at iter-350 per SC-11"; (vi) SHOULD-FIX 3 Project Structure Notes line 201 stale "Subtask 9.2 should grow" forward-promise replaced with closure language; (vii) LLM-OPT 1 AC5 verdict canonical phrasing — added missing "ground-(c) variant-(ii) per RALPH.md iter-299 (behavioural half only)" qualifier to match AC3 verdict. 4 NITs deferred (3 informational line-range pointers + Subagent B cross-story table cosmetics + "appended/prepended" phrasing + v1.3 Change Log paragraph density). Story file Change Log v1.5 entry added. **Empirical post-dev SM yield:** 1 MUST + 3 SHOULD + 1 LLM-OPT = 5 PATCH (vs pre-dev SM iter-348 9 PATCH) — confirms iter-281 + iter-348 density-amortisation lesson (post-dev SM yield LOWER than pre-dev SM yield because impl-landing absorbs technical-correctness drift; remaining drift is downstream-reference debt from earlier PATCH-band simplifications). **No substrate touched at this gate** — substrate is locked at iter-350 + iter-351 baseline; only planning-artefact PATCHes. Story `Status: review` unchanged (only Ralph's FR14n Story State changes); sprint-status row unchanged (SM-validate is Ralph-internal lifecycle). FR14n `Story State` transitions `traced → sm-verified`. **0 fix-task QUEUE entries produced** — direct promotion to `sm-verified` (zero unmet ACs and zero un-applied MUST-FIX). Forecast carried forward: 2–4 PATCH at CR opener, 4–6 iter chain length per iter-155 fix-chain equation.
- [x] iter-351: **`/bmad-testarch-trace (args: "yolo")` traceability matrix + gate decision for Story 2.18 — FR14n Story State `in-dev → traced`.** Coverage oracle resolved as `acceptance_criteria` from Story 2.18 § Acceptance Criteria (5 ACs, all P2 uniform per Epic-2 substrate precedent); `oracleResolutionMode: formal_requirements`, `oracleConfidence: high`, `externalPointerStatus: not_used`. **Coverage matrix:** 5 PARTIAL, 0 FULL, 0 NONE — overall 0% (structural artefact: no persistent test runner wired at Story 2.18 substrate stage; iteration-env config-render smokes 9.1 + 9.2 + 9.3 PASS at iter-351 trace re-verification + inline SC-11 dual-composer parity smoke at iter-350 PASS + `INV-devbox-egress-contract` manifest-anchor lockstep enforced at PR-merge via Story 1.9 sync-gate). **GATE DECISION: ⚠️ WAIVED** — EIGHTEENTH Epic-2 trace-WAIVED + TWENTY-EIGHTH cumulative trace-WAIVED precedent + **FIRST Epic-2 course-correction-origin trace-WAIVED**. Hybrid grounds (a)+(b)+(c) variant-(ii)+(iii); PARTIAL-AC ground-(c) variant-(ii) on AC3 + AC5 behavioural half (chicken-and-egg: live-kernel state requires Story 2.5 hardened container — the very bug being fixed). Trace artefacts emitted at `_bmad-output/test-artifacts/traceability/2-18-…md` + `2-18-e2e-trace-summary.json` + `2-18-gate-decision.json`; coverage matrix at `/tmp/tea-trace-coverage-matrix-2026-04-25-story-2-18.json`. Story file Change Log v1.4 entry added. **0 fix-task QUEUE entries produced** — direct promotion to `traced`.
- [x] iter-350: **`/bmad-dev-story` substrate landing for Story 2.18 — FR14n Story State `atdd-scaffolded → in-dev → review`; Story file `Status: ready-for-dev → review`; sprint-status `2-18-…: ready-for-dev → in-progress → review` (two flips this iter).** All 12 Tasks / 32 substrate subtasks landed in single iter-350 dev-story pass per Story 2.3 v1.3 / Story 2.4 v1.3 single-iter precedent (substrate-extension class). Iteration-env smokes 9.1 + 9.2 + 9.3 PASS; SC-11 dual-composer parity smoke PASS inline; backend-B operator-workstation deferred (Subtasks 1.1 + 4.5 + 9.4 + 9.5).
- [x] iter-349: **ATDD-skip via FR14n § Story Lifecycle Decision Matrix row `validated → atdd-scaffolded`.** 28th cumulative Epic ATDD-skip / 18th Epic-2 / first course-correction-origin. Hybrid ground (a)+(b)+(c) variant-(ii)+(iii); PARTIAL-AC (c)-(ii) on AC3 + AC5 behavioural half.
- [x] iter-348: **Pre-dev SM validation pass via `/bmad-create-story (args: "review")`.** FR14n `drafted → validated`. Two-subagent SM-review yielded 21 substrate citations / 19 OK / 2 MINOR drift + 14 prose findings → 9 PATCHes applied at gate.
- [x] iter-347: `/bmad-create-story` canonical drafted pass — Status `backlog → ready-for-dev`; sprint-status `2-18-…: backlog → ready-for-dev` + `epic-2: done → in-progress` (manual since /bmad-create-story only auto-flips epic on first story); path-drift corrections at Tasks 3 + 4; Task 2.4 sharpened with classifier-sidecar primitive (SC-11 extending Story 2.4 SC-14).
- [x] iter-345 (course-correction): `/bmad-correct-course` for issue #232 — Sprint Change Proposal + Story 2.18 v0.1 spec + epics.md stanza + sprint-status row + Story 2.3/2.4 Change Log forward-pointers staged.

## Context

- **Phase:** 4-implementation — Epic 2 in COURSE-CORRECTION (Story 2.18 active development; post-dev gates underway). Epic-2 PR #230 still OPEN, awaiting human merge; this branch stacks the network-whitelist fix on top.
- **Runtime:** cc-devbox iteration env. Network egress to `github.com` is the bug under fix — `gh`/`git push` use `curl --resolve api.github.com:443:140.82.121.5` workaround until Story 2.18 lands.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17/17 prior stories `done`; Story 2.18 in active dev. Sprint-status `epic-2: in-progress` (flipped iter-347 from `done`).
- **Epic Branch (parent):** `feat/epic-2-packaged-devbox` (PR #230 OPEN).
- **Working Branch:** `chore/devbox-network-whitelist-232` (course-correction host; PR #235 Draft).
- **Story:** Story 2.18 — Devbox network whitelist DNS-rotation fix (dnsmasq nftset= + GitHub CIDR fallback).
- **Story File:** `_bmad-output/implementation-artifacts/2-18-devbox-network-whitelist-dnsmasq-nftset-rotating-ip-fix.md` (v1.5 sm-verified form via `/bmad-create-story (args: "review")` iter-352).
- **Story State:** `sm-verified` — post-dev SM landed at iter-352 (FR14n `traced → sm-verified`; 5 PATCHes applied inline; 0 fix-task QUEUE entries — direct promotion); Story file `Status: review` unchanged. Next iter runs `/bmad-code-review (args: "2")` for `sm-verified → done` (or `fixes-pending` if PATCHes).
- **GitHub Issue:** [#232](https://github.com/tthew/ralph-bmad/issues/232) — devbox network whitelist DNS-rotation.
- **PR:** [#235](https://github.com/tthew/ralph-bmad/pull/235) Draft — targets `feat/epic-2-packaged-devbox`. Stays Draft until Story 2.18 done.

## Notes

- **Forecast (Story 2.18 fix-chain).** Per the iter-155 fix-chain forecast equation: 0 carve-out + backend-B live-smoke defer (+3) + ~200 LOC (+2) → ~5 ceiling → 2–4 PATCH at CR opener, 4–6 iter chain length. Tighter than Story 2.3's 10-iter chain (additive change, no algorithmic rewrite of `reload-egress.sh`). Lifecycle gate progress: iter-348 pre-dev SM 9 PATCH, iter-349 ATDD-skip 0 PATCH, iter-350 dev-story 0 PATCH (clean single-iter landing), iter-351 trace-WAIVED 0 PATCH, iter-352 post-dev SM 5 PATCH. Cumulative pre-CR PATCH count: 14 (vs forecast envelope 9–14 — within band).
- **Approach selected (Sprint Change Proposal § 4.4):** Direct Adjustment (Option 1) hybrid Option A (`dnsmasq nftset=`) + Option B (static GitHub CIDR fallback) = Option C combo. Belt-and-braces matches Story 2.3's existing two-layer egress posture.
- **Image rebuild required.** Verify Debian's `dnsmasq` package compiled with `--enable-nftset` at first build (Story 2.18 Task 1). Most recent Debian builds are; pin in `VERSIONS.md` if not already locked.
- **DNS-rotation workaround (carry-forward to all course-correction iters until Story 2.18 lands).** When `gh pr ...` / `gh api graphql` times out at `api.github.com → 140.82.121.6` (or any non-whitelisted IP), bypass via REST API + `curl --resolve api.github.com:443:140.82.121.5` (or `.121.4`). Token via `gh auth token`. **This is the bug actively biting; do not be surprised when it bites.**
- **No conflict with issue #231.** Doc-budget course-correction artefact lives at `sprint-change-proposal-2026-04-25.md`; Story 2.18 artefact has `-issue-232` suffix.
- **Story 2.3 + 2.4 status unchanged.** Both stay `done`; Change Log entries are forward-pointers per BMad convention. Story 2.18 implementation iteration refreshed `INV-devbox-egress-contract` contentHash via Story 1.9 sync-gate protocol at iter-350 (`d04b…7ff0 → 8eccef…5133`); manifest is sync-gate clean at iter-352 baseline.
- **Pre-existing sync-gate drift (iter-349 observation; NOT introduced by Story 2.18 work).** `pnpm keel-invariants:check-all` still reports two drift items at iter-352 baseline: (a) `.pre-commit-config.yaml` content-hash-mismatch (introduced at commit `9716ca5 feat(ralph): doc-budget orient-gate + pre-commit hook (issue #231)` which appended the `ralph-doc-budget` hook without refreshing `INV-prek-pre-commit-config` + `INV-prek-commit-msg-config` manifest hashes); (b) `INV-git-hooks-preservation` walker-vs-worktree interaction (iter-312 known issue). Pre-commit hooks GREEN; sync-gate is NOT in pre-commit chain (only pre-merge). **Forward-fix:** queue at Story 2.18 CR or as a separate sweep iter post-Story-2.18-done; not Story 2.18 scope.
