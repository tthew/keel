# Implementation Plan

## NOW

- [ ] **Story 2.17 `/bmad-dev-story` CONTINUATION — Task 16 sub-items (f) + (g) — bundled doc-only addition to `docs/invariants/claude-hook-denylist.md § Limitations`.** One paragraph noting (a) iter-320 NOVEL case-glob prefix-then-any matching semantics + regex word-boundary workaround for verb-class detection (single data point; promotion threshold unreached — document as "Limitations" not as a formal rule), and (b) iter-329 sub-novel `eslint.config.keel-invariants.js` self-exclusion-glob requirement (rule-authoring modules detecting their own literal needle require glob-scoped self-exclusion). Plain-`Edit` route — invariant-doc outside L1 + hook-self-protection ranges per iter-316 routing discipline; manifest `INV-claude-hook-secret-denylist-doc` contentHash pin WILL need a python3-heredoc atomic-replace after the doc Edit (same iter-315/316/325/326/327/329/331 routing for L1-protected manifest hash refresh). ~small.

## QUEUE (Story 2.17 Task-16 remainder + lifecycle close + Epic 2 close-out)
- [ ] Task 16 sub-items (d) + (e) — iter-314 NOVEL docs-promotion threshold (1 site; unmet) + iter-317 NOVEL carry-forward (iter-325 § L1 install-boundary rule § Substrate-maintenance loophole narrative already landed) — marker-only; record `defer: threshold-not-met` + `complete: pre-landed iter-325` in IP and prune. ~small.
- [ ] Task 16 (story-spec) — `deferred-work.md` triage: Epic-1 (Stories 1.8-1.16) + early-Epic-2 (Stories 2.1-2.14) DEFER audit per story-file Task 16.1-16.4 (target 15-25 absorbed citation-lockstep items + cumulative Epic 2 DEFER balance < 30 at close). Distinct from the IP's "SC-17 polish bundle" label — this is the story-spec scope proper. Medium-large own-iter item.
- [ ] Task 17 (completion): close out Story 2.17 `in-dev (partial) → in-dev` once Task 16 fully lands + verify sub-tasks 15.2/15.3/15.4/15.5 (re-run `pnpm keel-invariants:check-all` 41 entries + `pre-commit run --all-files` + `pnpm --filter @keel/keel-invariants test` 7/7 GREEN + populate Completion Notes with per-AC evidence). Ensure story DoD complete + Dev Agent Record populated + Change Log final row.
- [ ] _(after Story 2.17 Tasks fully landed)_ `/bmad-testarch-trace (args: "yolo")` — `in-dev → traced`. 27th cumulative trace; likely WAIVED per Epic-2 17-story chain continuation + impl-time-smokes substitute. Exception: iter-329 Task 5 authored 7 genuine `node:test` unit tests (the first keel-invariants tests in-repo), so SM review can cite them as AC-3 coverage evidence and trace does not have to waive for Task 5.
- [ ] _(after traced)_ `/bmad-create-story (args: "review")` — post-dev SM `traced → sm-verified`.
- [ ] _(after sm-verified)_ `/bmad-code-review (args: "2")` — `sm-verified → done`.
- [ ] _(after Story 2.17 `done`)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge. Epic 2 completion fires `EPIC_DONE` halt; on re-entry auto-advances to Epic 3 Story 3.1.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured; `statusCheckRollup: []` carried unchanged iter-219..333).

## BLOCKED

_(empty — iter-335 Task 16(c) DEFERRED `defer: conditional-on-re-emergence`; bookkeeping-only iter; no substrate / manifest / seed perturbation; pre-push gates expected GREEN.)_

## ATDD Red Phase

_(empty — iter-311 SKIP-WITH-GROUNDS-(ii)+(iii). Impl-time substrate coverage: 397 cumulative smokes GREEN across iter-315..320 runs + 7 genuine `node:test` unit tests GREEN at iter-329 for Task 5 S4 rules + 74 persisted replay fixtures GREEN at iter-332 (`packages/keel-invariants/fixtures/hooks/run-all.sh` — 55 positive + 19 negative). Total persisted/reproducible at iter-334: 7 unit tests + 74 replay fixtures.)_

## DONE (iter-335 Task 16(c) DEFERRED conditional-on-re-emergence)

- [x] iter-335: **Story 2.17 `/bmad-dev-story` PARTIAL CONTINUATION — Task 16 sub-item (c) (host-OS-aware `.git/hooks/*` config portability doc) DEFERRED `defer: conditional-on-re-emergence`.** Rationale per RALPH.md iter-313 entry: iter-312's "macOS host path" observation that `core.hooksPath` on devbox-mounted host filesystem reported `/Users/tthew/Development/ralph-bmad/.git/hooks` was a STALE pre-devbox-restart artifact — re-verified at iter-313 orient as `/workspace/ralph-bmad/.git/hooks` (Linux, correct) once the devbox container restarted post-`prek install`. Not a portability bug; not worth a docs-amend slot at Story 2.17 close-out. **Re-validation trigger:** if a fresh-fork operator on macOS host hits the macOS-path observation post-`prek install` BEFORE devbox restart AND the recovery path (devbox restart) is non-obvious, re-open as Task 16(c) at next-version `keel-devbox` rebake — IP § QUEUE re-add with new `defer:` line cleared. Bookkeeping-only iter; no substrate / hook / settings / manifest / seed perturbation (only IP + story-file Status / lifecycle HTML comment / Change Log row 0.24 mutated; precedent iter-330 pure-verify pass mutated identical two files). Pre-push gates expected GREEN as iter-330: no manifest contentHash refresh route (IP + story-file outside every hashed path); `pnpm keel-invariants:check-all` exit 0 on 41 entries unchanged + tokens-sync clean; format:check expected clean (markdown story-file Prettier-managed; one-time Prettier auto-fix on append if needed). Net Story 2.17 post-iter-335: **3 top-level Tasks remain — 15 (15.2-15.5 fold into Task 17), 16 (16(c) DEFERRED + 16(d-g) doc-only items + story-spec deferred-work.md triage), 17 (completion DoD + Dev Agent Record + Change Log final row)**.

- [x] iter-334: **Story 2.17 `/bmad-dev-story` PARTIAL CONTINUATION — Task 16 sub-item (b) — `release-assets.githubusercontent.com` whitelisted in `packages/devbox/whitelist/github.txt`.** One-line addition between `objects.githubusercontent.com:11` and `codeload.github.com:13` (post-edit: 8 hosts in 16 lines). Companion to iter-333 Dockerfile substrate-bake — Dockerfile `curl` lands at *build* time outside the runtime firewall (Docker build network not subject to the Story 2.3 dnsmasq/nftables egress policy), so iter-333 didn't strictly require this whitelist entry; **what this iter handles is the runtime egress path** for post-bake operator-flow recovery + future re-download (operator `prek` re-install via `curl github.com/.../releases/download/...` from inside the devbox; Story 2.5 future bake-at-runtime ops; Renovate release-asset metadata lookups beyond the `api.github.com` GraphQL path). **GitHub migrated release-asset CDN hosting from `objects.githubusercontent.com` to `release-assets.githubusercontent.com` in 2024**; the new host is the 302-redirect target for `https://github.com/j178/prek/releases/download/v0.3.9/prek-${arch}.tar.gz` (the URL the iter-333 Dockerfile RUN block targets at build time). **Whitelist file outside every hashed sub-tree** — verified at orient: `grep -nE "whitelist/(github|npm|python|anthropic)" packages/keel-invariants/src/invariants.manifest.ts` returned exactly one hit at `:340` inside the `INV-devbox-healthcheck` description discussing three-site lockstep on the `api.github.com` probe domain (narrative-only, NOT contentHash-pinned); L1 install-boundary regex covers `packages/keel-invariants/src/` only; seed-sync lockstep covers `.claude/hooks/**` + `.claude/settings.json` only. **No manifest contentHash refresh / no seed re-sync / no substrate hook/settings/manifest perturbation** in-iter. Pre-push gates all GREEN: `pnpm keel-invariants:check-all` exit 0 on 41 entries unchanged + tokens-sync clean (~0.5s); `pnpm -w typecheck` 16/16 (426ms — 15 cached); `pnpm -w lint` 16/16 (1.34s — 15 cached); `pnpm -w format:check` clean (whitelist `.txt` not Prettier-managed; all matched files pass). **No novel gotcha / lesson / decision — pure additive whitelist entry following the file's existing one-host-per-line convention; no RALPH.md promotion.** Net Story 2.17 post-iter-334: **3 top-level Tasks remain — 15 (15.2-15.5 fold into Task 17), 16 (16(c) CONDITIONAL defer + 16(d-g) doc-only items + story-spec deferred-work.md triage), 17 (completion)**.

- [x] iter-333: Task 16(a) substrate-bake prek native Rust binary in `packages/devbox/Dockerfile` (Story 2.5 AMEND; closes iter-313 BLOCKED class). Detail in commit `353b9f0`.

- [x] iter-332: Task 15.1 (bash-fixture persistence under `packages/keel-invariants/fixtures/hooks/` — 74/0/74 GREEN). Detail in commit `cdf4d79`.

- [x] iter-331: Task 9 (CI visibility contract stub for Epic 14) — invariant-doc § CI visibility contract H3 section + manifest contentHash refresh `486e6ee1ec6e…` → `cb32195fc573…`. Detail in commit `420f8c1`.

- [x] iter-330: Task 6 (halt-threshold verify-pass) — pure-verify iter with no file mutation. Detail in commit `718276d`.

_(iter-303..329 LANDING detail pruned per Guardrail 2; retained in commits through `58e2ff9` + story-file Status HTML comment chain + story-file Change Log 0.1-0.23 rows.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **16/17 stories done** (2.1-2.16) + **Story 2.17 `in-dev (partial)` at iter-335** (Epic-2 final story; XL partial landing continuing through Task 16 sub-item (c) DEFERRED + remaining Tasks 16/17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live smokes operator-workstation-deferred.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:121; bash + jq baked at :36-37 + :48). Host-side shim count: **18** at iter-335 (unchanged). iter-333 Dockerfile edit + iter-334 whitelist edit await operator-workstation rebake to materialize (image stays at pre-iter-333 baseline until rebake; whitelist edit takes effect on next devbox container restart since whitelist is read at startup, not baked into image).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **16 done** (2.1-2.16); **1 in-dev (partial)** (2.17 — Task 10.1 iter-320 complete; Task 13 iter-325 complete; Task 11 iter-327 complete; Task 14 iter-328 complete; Task 5 iter-329 complete; Task 6 iter-330 complete; Task 9 iter-331 complete; Task 15.1 iter-332 complete; Task 16(a) iter-333 complete; Task 16(b) iter-334 complete; Task 16(c) iter-335 DEFERRED `defer: conditional-on-re-emergence`; 3 top-level Tasks remain: 15 (15.2-15.5 fold into Task 17), 16 (16(d-g) doc-only items + story-spec deferred-work.md triage), 17).
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.17 — Hook + settings bypass-resistance (git-layer + manifest + S4 + halt).
- **Story File:** `_bmad-output/implementation-artifacts/2-17-hook-settings-bypass-resistance-git-layer-manifest-s4-halt.md` (~574-575 lines post-iter-335 Change Log 0.24 append + Status iter-335 segment append + lifecycle HTML comment iter-335 entry).
- **Story State:** `in-dev (partial)` — iter-336 NOW = `/bmad-dev-story` CONTINUATION per § Story Lifecycle dev-story invocation guidance; NOW item Task 16 sub-items (f)+(g) bundled doc-only addition to `docs/invariants/claude-hook-denylist.md § Limitations`.
- **GitHub Issue:** no GitHub Project configured; `RALPH_ISSUE_NUMBER` unset. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` unchanged iter-272..335).

## Notes

- **iter-335 NOVEL — none (pure-bookkeeping defer-record iter; precedent iter-330 pure-verify pass).** Sub-novel observation: defer-record bookkeeping iter pattern formalised — recording a `defer: <reason>` decision against a Ralph-decomposed sub-item (here Task 16(c)) consumes one iter slot and lives in IP § DONE narrative + story-file Change Log row + lifecycle HTML comment, NOT in `deferred-work.md` (which is the story-spec scope-bundle for Task 16's primary triage). Carry-forward to iter-336+ Tasks 16(d)+(e) which are next in QUEUE for the same defer-record treatment (`defer: threshold-not-met` + `complete: pre-landed iter-325`). Single data point at iter-335; promotion to RALPH.md as a "iteration-class taxonomy: defer-record" entry on second re-validation when 16(d)+(e) land.

- **iter-334 NOVEL — none (promotion threshold unreached).** Pure additive whitelist entry following the file's existing one-host-per-line convention. Sub-novel observation: **the GitHub release-asset CDN host migration `objects.githubusercontent.com` → `release-assets.githubusercontent.com` (2024)** is a single data point worth noting in case future-Ralph encounters another release-asset 302-redirect that fails the runtime egress policy — the pattern is "look at the redirect target host, not the github.com URL". Single data point; promotion to RALPH.md as a Gotcha entry on second re-validation. Also reaffirmed: whitelist files (`packages/devbox/whitelist/{github,npm,python,anthropic}.txt`) are intentionally NOT contentHash-pinned in `invariants.manifest.ts` — they are operator-extensible by design (Story 2.4 `pnpm devbox:whitelist add` CLI entry). The narrative reference at `invariants.manifest.ts:340` (within `INV-devbox-healthcheck`) is a three-site lockstep on the `api.github.com` probe domain only, not a hash on the whole file.

- **iter-333 NOVEL — none (promotion threshold unreached).** Mechanical substrate-bake Dockerfile AMEND following established arch-aware release-asset fail-fast pattern (iter-128 CR AI-6 baseline) — precedent in same Dockerfile for supabase-cli (`Dockerfile:176-191`), git-delta (`:196-212`), and aws-cli (`:152-163`). Sub-novel observation: Dockerfile AMENDs during later-story dev-story are a documented practice when narrowly substrate-fix-scoped and they close a concrete BLOCKED incident class (iter-313 BLOCKED here); change tagged "Story 2.5 AMEND" in Change Log narrative but lands in the Story 2.17 dev-story commit since SC-17 close-out motivates it. Pattern-match exercise only.

- **iter-332 NOVEL — single data point promoted to RALPH.md § Gotchas (2026-04-24 iter-332).** Hook-contract tests MUST align to the REACHABLE branch in the runtime, not to source-order case precedence. Four devbox-runtime reachability learnings: (a) D-22 resolved-path branch dominates direct-path case-globs; (b) `__unknown__` fires only on jq-parse-fail or missing `.tool_name`; (c) L1 `find -delete` regex requires specific filenames, not dir; (d) Grep/Glob `search_path` case-glob requires trailing content. Full detail in `packages/keel-invariants/fixtures/hooks/README.md § Dominance notes`. Single data point; promotion to rule-type on second re-validation.

- **iter-331 NOVEL — none (promotion threshold unreached).** Mechanical invariant-doc amendment + manifest contentHash refresh following established iter-325/329 docs-amend-plus-python3-heredoc discipline. Sub-novel observation: the "verify-pass, likely additive edit if drift found" NOW pattern is a repeated one-shot motif for contract-pin tasks (iter-330 Task 6 pure-verify; iter-331 Task 9 verify-then-additive).

- **iter-330 NOVEL — none (pure-verify pass with no file mutation).**

- **iter-329 NOVEL — `shutil.move` vs `os.replace` for /tmp-to-L1 new-file authoring (recorded in RALPH.md § Gotchas 2026-04-24 iter-329).** `os.replace` requires same-fs atomic rename; when `/tmp` is tmpfs and the repo is on a different mount, the call raises `[Errno 18] Invalid cross-device link`. `shutil.move` handles cross-fs automatically via copy2+unlink fallback.

- **iter-320 NOVEL carry-forward — shell case-globs match prefix-then-any, not word-boundary-anchored.** `case "$normalized" in rm*.claude/settings*) ...` matches `rmdir .claude/settings.json` (FP). Fix via regex `[[ "$normalized" =~ ^rm([[:space:]]|$) ]]`. **Rule:** new case-glob-based rule authoring inside `.claude/hooks/*.sh` MUST use word-boundary regex for any verb with common-prefix collision risk. Single data point at iter-320 for D-35 restructure; still unreached at iter-334. Documentation target: Task 16 sub-item (f) — bundled with (g) per iter-334 IP § QUEUE.

- **iter-320 NOVEL carry-forward — `chmod` command prefix FP-blocks seed-sync pattern.** Bash command `chmod 755 <src> && python3 -c "...'.claude/hooks/...'..."` tripped hook case-glob. **Rule:** future substrate-mutation ops needing both file-mode + atomic replace MUST be one python3 script (os.chmod + os.replace). Still unreached at iter-334.

- **iter-319 NOVEL carry-forward RE-CONFIRMED iter-320..334** — `/tmp` tmpfs `noexec,nosuid,nodev,relatime` (Story 2.5 hardening). iter-334 whitelist edit did not touch /tmp runtime (whitelist file lives under `packages/devbox/whitelist/`, not /tmp).

- **iter-318 carry-forward — `gh pr view` GraphQL timeout recipe.** Reconfirmed at iter-334 orient: `gh pr view 230 --json state,isDraft,statusCheckRollup,mergeable` timed out 3x with `Post "https://api.github.com/graphql": dial tcp 140.82.121.5:443: i/o timeout`. Per iter-318 carry-forward, three retries is the bound; proceeded with IP-recorded baseline (`statusCheckRollup: []` unchanged through iter-332; no in-progress checks; safe to push). The `gh pr view 230 --json state,isDraft,statusCheckRollup,mergeable` recipe is now a flaky-but-safe-to-retry-3x pattern; consider RALPH.md promotion if it recurs at iter-335+.

- **iter-317/316 carry-forward — D-15 + D-14 minor regex + Python-write loophole.** D-15 wrapper normalization must not strip interp-stdin readers; D-14 find-delete trailing-slash optional. Python-write loophole DOCUMENTED at iter-325 Task 13.5; iter-329/331 used python3-only route for the non-cross-fs manifest hash refresh (python3-heredoc in-place).

- **iter-319 + earlier carry-forward pool — low-activity holds.** `readlink -f` canonicalises even when target file doesn't exist; `install-boundary-protection` rule-id live-enforcing; `names-and-shebangs` runtime `.ts→.js` / `/src/→/dist/` translation; SC-17 candidates unchanged at iter-334.

- **iter-314 SM-review promotion threshold: 5→7 data points** unchanged; Story 2.17 post-dev SM estimate iter-335+ after remaining Tasks 16/17 land (3 decomposed QUEUE items: 16(c) defer-marker + 16(f-g) doc-only bundle + 16(d-e) marker-only + story-spec deferred-work.md triage + completion).
