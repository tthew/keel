# Implementation Plan

## NOW

- [ ] Exercise the freshly baked `keel-devbox:local` image to empirically cover Story 2.1 Tasks 4 + 5-run + 7.3 prerequisites: `docker compose -f packages/devbox/docker-compose.yml config` (Task 7.3) → `docker compose run --rm devbox pnpm test` + `docker compose run --rm devbox pnpm lint` (Task 4; capture the "no test runner configured at root" empirical result — expected non-zero exit per Epic-1-through-13 no-test-runner precedent; lint covers `packages/devbox/eslint.config.js` coverage OR exits with "no lint target" — record the exact outcome) → `packages/devbox/scripts/benchmark.sh --skip-cold` (Task 5-run warm-only; safe under backend B; auto-appends the backend-flagged entry to `packages/devbox/README.md § Benchmarks`). Commit + push. DO NOT pass `--allow-broad-prune`. This is the iter-124 half of the decomposed iter-123 compound NOW. ~medium

## QUEUE (Epic 2 Story 2.1)

- [ ] Re-invoke `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-1-packages-devbox-absorb-from-cc-devbox-image-compose-substrate-tooling-access.md")` — formally close Tasks 4 + 5-run (warm-only via `--skip-cold`) + 7.3 in the story file using the iter-123+iter-124 empirical evidence, advance Story State `in-dev (partial) → in-dev`, flip Story Status `in-progress → review`, queue `/bmad-testarch-trace (args: "yolo")`.
- [ ] Run `/bmad-testarch-trace (args: "yolo")` — AC→test coverage gate + traceability (`in-dev → traced` or `trace-fixes-pending`)
- [ ] Run `/bmad-create-story (args: "review")` — post-dev SM verification (`traced → sm-verified`)
- [ ] Run `/bmad-code-review (args: "2")` — CR with Create-action-items (`sm-verified → done` or `fixes-pending`)
- [ ] Implement `packages/keel-invariants/` runtime rule for `INV-devbox-dind-available` — write `packages/keel-invariants/src/check-devbox-dind.ts` asserting (a) `command -v docker && docker info` exit 0, (b) `docker run --rm hello-world` exits 0 (functional criterion per iter-122 doc rewrite), (c) record detected backend (A/B) for downstream consumers to gate on. Add unit test (Epic 13 framework pending; Story 1.9 sync-gate pattern in the meantime), wire into `pnpm keel-invariants:check-all` composite. Spec source: `docs/invariants/devbox-dind.md`. After this lands, bump `contentHash` in the manifest entry accordingly if the doc file changes.
- [ ] Monitor PR CI — queue fix tasks for any failures
- [ ] Transition PR Draft→Open — final CI gate (after all Epic 2 stories done)

## BLOCKED

_(none — cold NFR2 authoritative measurement is OPERATOR-OWNED on M4-Pro native, not a Ralph blocker. Warm-only measurement on backend B runs in-iteration via `benchmark.sh --skip-cold`.)_

## DONE (Epic 2 Story 2.1)

- [x] iter-123: **First successful devbox image bake (safe-subset, backend B) — the iter-100..120 gate-clearing delta.** Retried once around a transient buildkit `dial tcp 127.0.0.1:443` on `auth.docker.io` metadata fetch (mitigated by pre-pulling `ubuntu:24.04` directly, then re-running `docker compose build`). Produced `keel-devbox:local` — image ID `e7e91f1537f1`, 848 MB, `linux/arm64`, ~4.5 min aggregate stage time. Captured all pinned tool versions from inside the baked image via `docker run --rm keel-devbox:local bash -c …` (node 20.20.2, pnpm 10.29.2, @anthropic-ai/claude-code 2.1.116, gh 2.90.0, uv 0.11.7, AWS CLI v2 2.34.33, Supabase CLI 2.90.0, git-delta 0.19.2, psql 16.13, Ubuntu 24.04.4 LTS) and appended a full `2026-04-21 (iter-123)` entry to `packages/devbox/VERSIONS.md § Bake log` (with image stats + build time + the full version matrix + NFR2 authority reminder). iter-123 decomposed the larger NOW compound (bake + exercise + benchmark) into (a) iter-123 = bake + versions + VERSIONS append, (b) iter-124 = exercise + benchmark, per IP § Notes guardrail-9 ceiling. Backend-B safety contract held — no `docker system prune` invoked. **Two new build-wrangling gotchas logged to RALPH.md signposts** (`| tee log` swallows exit codes; `cmd > log &` inside a `run_in_background` Bash nests background and confuses the completion notification; mitigation = Monitor + terminal-state grep on the log file).

- [x] iter-122: **DinD backend discovered as `B` (host socket-passthrough) + benchmark.sh safety guard + doc/manifest correction.** On entry, `docker info` → `Name: docker-desktop`; `docker ps -a` → host-level containers from unrelated projects. `docker run --rm hello-world` exits 0 → functional criterion holds, iter-121 reachability claim correct, but backend assumption (isolated DinD) was wrong. `benchmark.sh`'s `docker system prune -af --volumes` would have destroyed host-level state. Rewrote `docs/invariants/devbox-dind.md` with § Backend contract (A isolated / B passthrough) + § Safety rule (detect backend + refuse destructive under B unless `--allow-broad-prune`) + hello-world functional criterion; added AGENTS.md safety paragraph; added `detect_backend()` + refusal gate + `--allow-broad-prune` flag to `benchmark.sh`; updated manifest contentHash `81af7752…0c32 → a9675f7d…b3ad` (iter-121 hash was pre-modification drift); bundled iter-121 staged DinD codification (INVARIANTS.md, AGENTS.md, RALPH.md, architecture.md, manifest.ts) into the same commit (step 3a permits). `bash -n` + live refusal test both green. Safe-subset operator-bake now unblocked in-iteration; destructive cold measurement remains M4-Pro-only.
- [x] iter-121: **Docker-daemon blocker resolved.** Operator installed Docker in the cc-devbox iteration environment per `docs.docker.com/engine/install/ubuntu/` (DinD; verified `/usr/bin/docker`, Docker 29.4.1, Compose v5.1.3). DinD codified as a **fork-time substrate requirement** (NOT transitional) via new invariant `INV-devbox-dind-available` — spec at `docs/invariants/devbox-dind.md`, registered in `packages/keel-invariants/src/invariants.manifest.ts`, anchored in `INVARIANTS.md § Devbox iteration substrate`, cross-referenced from `AGENTS.md § Devbox iteration environment`, `RALPH.md § Signposts`, and `_bmad-output/planning-artifacts/architecture.md § I5a`. NFR2 authority unchanged — M4-Pro native remains authoritative per AC 4 scope clarification; DinD runs are indicative baselines only. **Iter-121 caveats discovered + corrected at iter-122:** (a) contentHash `81af7752…0c32` drifted from on-disk doc `e57f77e4…4227` (now `a9675f7d…b3ad` post iter-122 rewrite); (b) doc body contained a speculative fabricated "unshare operation not permitted" iter-122 discovery narrative — replaced with empirical backend-B finding at iter-122; (c) iter-121's work was staged but never committed, so iter-122 bundled it into the correction commit. Runtime-check code (hello-world smoke + backend detection) deferred to a dedicated Ralph iteration (QUEUE item).
- [x] iter-100..120: **Rolling detect-delta no-op observations since iter-99 commit `5278738`.** Each iter re-ran the cheap detection (read `packages/devbox/README.md` lines 117-122 + `packages/devbox/VERSIONS.md § Bake log`); every iter confirmed the iter-99 "Pending first bake" / "image not baked in the Ralph container environment" entries were still the latest — no operator-bake delta. `/bmad-dev-story` re-invocation stayed deferred per the detect-gated pattern (~5-10K tokens per iter vs ~30-50K for a no-op skill call; RALPH.md signpost 2026-04-21). Pattern holding terminated at iter-121 when Docker landed in the iteration env + iter-122 when the safety gate landed.
- [x] iter-99: **Story 2.1 `/bmad-dev-story` source-level implementation pass (atdd-scaffolded → in-dev partial).** Authored `packages/devbox/{Dockerfile, docker-compose.yml, entrypoint.sh, scripts/benchmark.sh, VERSIONS.md}` + rewrote `README.md`. Static Task 7 gates PASS (runtime-install grep exit 1, Dockerfile pin-rationale header, `100755` bits on both scripts). Sprint-status flipped `2-1-…: ready-for-dev → in-progress`. Dynamic gates (Tasks 4 + 5-run + 7.3) were BLOCKED on Docker daemon — resolved iter-121 via DinD (backend-B corrected at iter-122). Change Log v1.3 in story file. Commit `5278738` was the Story 2.1 source-level baseline referenced by every subsequent detection iter.

## Context

- **Phase:** 4-implementation — Epic 2 open, Story 2.1 in-dev (partial). Safe-subset image bake landed iter-123 (image `keel-devbox:local` e7e91f1537f1, 848 MB, aarch64). Next iter (124) exercises the image; iter-125 re-invokes `/bmad-dev-story` to flip state to `in-dev`.
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available` § Backend contract). Broad-state-mutation scripts must detect + refuse destructive ops under B; `benchmark.sh` does this at iter-122.
- **Baked image:** `keel-devbox:local` (first bake iter-123 2026-04-21). Version matrix in `packages/devbox/VERSIONS.md § Bake log`. Backend B + linux/arm64 — matches the M4-Pro operator workstation architecture, but NFR2 cold-start authority still lives on M4-Pro native per AC 4 scope clarification.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; Story 2.1 is the image + compose + entrypoint foundation.
- **Epic Branch:** `feat/epic-2-packaged-devbox`.
- **Story:** 2.1 — `packages/devbox/` absorb from cc-devbox (image + compose + substrate tooling access).
- **Story File:** `_bmad-output/implementation-artifacts/2-1-packages-devbox-absorb-from-cc-devbox-image-compose-substrate-tooling-access.md`
- **Story State:** in-dev (partial) — iter-99 landed Tasks 1-3, 6, 8 + Task 7 static subtasks; Tasks 4, 5-run (warm-only), 7.3 now runnable in-iteration against backend-B Docker (NOW task). Advances to `in-dev` after NOW + next-iter `/bmad-dev-story` close.
- **GitHub Issue:** Story 2.1 → #41 (https://github.com/tthew/ralph-bmad/issues/41). Parent Epic 2 → #10. Commit trailer `Refs #41`.
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (no CI runners pre-Epic-13; Draft→Open transition queued at end of Epic 2).

## Notes

- **DinD is fork-time substrate, not transitional.** Every fork's cc-devbox-equivalent environment must provide Docker per `INV-devbox-dind-available` so Ralph can exercise full-stack vertical slices against services, architecture, and infrastructure. Forks that substitute Podman/rootless Docker remain conformant if daemon reachability + function hold; forks that break either pursue the AMEND path (source-level fork against `docs/invariants/devbox-dind.md` + manifest entry + `INVARIANTS.md` anchor), not the FORK path.
- **Backend B is the reference environment at 2026-04-21.** Host socket-passthrough to macOS Docker Desktop. Broad-state-mutation scripts MUST detect the backend + refuse destructive ops by default. `benchmark.sh` is the reference implementation at iter-122. Apply the same gate pattern to any future script that prunes / volume-removes / image-removes across project boundaries.
- **NFR2 authority unchanged.** DinD benchmark numbers are modelled-indicative-baseline only; AC 4 cold ≤ 300s / warm ≤ 30s remains authoritative on M4-Pro native. Under backend B, warm-only is the autonomous Ralph-producible number (`--skip-cold`); cold comes from native bakes only. Benchmark entries from backend B MUST carry the flag string + backend=B + `uname -a` + `docker --version` fingerprint (`scripts/benchmark.sh` auto-emits these at iter-122).
- **Detection-gated skill re-invocation pattern still applies** (RALPH.md signpost 2026-04-21) — but now for external operator-owned signals only (e.g. the M4-Pro native NFR2 cold run). In-iteration Docker-gated work closes in-iteration from iter-122 onward (safe-subset scope).
- **Cross-epic auto-advance pattern validated** (iter-96). **Partial-completion pattern validated** (iter-99). **Backend-B safety-gate pattern established** (iter-122; reference: `packages/devbox/scripts/benchmark.sh detect_backend()`).
- **Story 2.1 scope bounded by AC1-AC4 literally.** Scope carve-outs in the story file pin what 2.1 delivers vs what later Epic 2 stories own. iter-99 + iter-122 preserved this — no bleed into Stories 2.2-2.17. Substrate's pre-existing `packages/devbox/` TS scaffold preserved; Story 2.1 ADDED runtime-infrastructure siblings only.
- **Guardrail-9 note for NOW task.** The safe-subset operator-bake is framed as a single atomic outcome ("produce and commit the safe-subset operator-bake delta"). `docker compose build` + `docker compose run --rm pnpm test/lint` + `benchmark.sh --skip-cold` + version capture + VERSIONS.md append are wrapped in one deliverable. If Ralph judges it compound at next-iter entry, decompose into two iterations (build + bench → commit, then VERSIONS append + test/lint → commit) before re-invoking `/bmad-dev-story`. Ceiling: 2-3 iterations to full story close.
