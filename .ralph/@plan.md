# Implementation Plan

## NOW

- [ ] iter-93: `/bmad-create-story (args: "review")` — FR14n row 7 `traced → sm-verified` SIXTH cumulative ZERO-PATCH post-dev SM candidate. ~medium.

## QUEUE (Story 1.16 — fork-extension scaffold + INVARIANTS.fork scaffold)

- [ ] iter-94: `/bmad-code-review (args: "2")` — FR14n row 9 `sm-verified → done` SIXTH cumulative ZERO-PATCH CR candidate; Story 1.16 `done` → Epic 1 fully done → EPIC_DONE sequence begins.
- [ ] iter-95: Transition PR #226 Draft → Open — final CI gate via `gh pr ready && gh pr checks --watch --fail-fast`; post-CI-green mark epic-1 `in-progress → done` in sprint-status + EPIC_DONE halt write.

## BLOCKED

_(none)_

## DONE (current epic — trailing iterations only)

- [x] iter-92: **FR14n row 5 `in-dev → traced` via `/bmad-testarch-trace (args: "yolo")` TENTH cumulative Epic-1 WAIVED precedent.** (Stories 1.7 iter-4 + 1.8 iter-3 + 1.9 iter-3 + 1.10 iter-46 + 1.11 iter-57 + 1.12 iter-64 + 1.13 iter-71 + 1.14 iter-78 + 1.15 iter-85 → 1.16 iter-92). Authored 4 trace artefacts under `_bmad-output/test-artifacts/traceability/1-16-*` via mechanical structural inheritance from Story 1.15's iter-85 template (~90% reasoning-cost savings; 1-15 → 1-16 structural swap with fork-extension evidence replacing renovate evidence): (1) `1-16-coverage-matrix.json` — 4-AC phase-1 data with per-AC substrate-verification evidence arrays; (2) `1-16-e2e-trace-summary.json` — summary statistics + oracle metadata + 4 recommendations; (3) `1-16-gate-decision.json` — WAIVED verdict + three-ground rationale verbatim; (4) `1-16-fork-extension-config-pattern-growth-tier-invariants-fork-md-scaffold.md` — full trace report with YAML frontmatter + Phase 1 (requirements traceability) + Phase 2 (gap analysis) + Phase 3 (gate decision) + Phase 4 (recommendations) + § LIVE iter-92 re-verification evidence + § TRACE REPORT SUMMARY. **LIVE re-verification at iter-92 trace-time**: all 6 Task 7 smokes exit byte-identical to iter-91 dev-story record — scaffold template sha256 `167ba6b2a8f1153df02f7e572b1d1e31415731493b0729415f6573cc1a696218` unchanged + rationale doc sha256 `be6f3d8919e7bb2b6258d768895d8a1e4d4a37c5fef95f5121f1ca878da192f2` unchanged + manifest load `OK: 24 invariants` + `pnpm keel-invariants:check` exits 0 clean within <2s AC 7 budget + zero test-runner configs in tree (vitest/jest/playwright/cypress). **Three conjoint grounds held:** (a) substrate-verification-covers-ACs at CLI-exit-code level — Task 7 six smokes (scaffold-template shape + rationale-doc shape + AGENTS.md update + CLAUDE.md update + manifest-load + sync-gate clean) cover AC 1-4 end-to-end; (b) no-runner — Epic 13 scope; (c) HYBRID variant-(ii)+(iii) — AC 4 Epic-15a CLI runtime downstream + adversarial AC 1-4 delegated to iter-94 CR three-layer fan-out. Phase 1 deterministic FAIL signal (0% overall < 80%) is STRUCTURAL FALSE-POSITIVE — WAIVED verdict correct. Story 1.16 Change Log v1.4 appended documenting FR14n row 5 transition. Story State `in-dev → traced`; BMad Status unchanged (`review` — Ralph-loop-semantic gate). **Carry-forward INTO iter-93 `/bmad-create-story (args: "review")` post-dev SM:** story file ACs remain stable at v1.1 pre-dev correction (no post-dev AC drift introduced by iter-91 dev-story — SM review will verify impl satisfies all 4 ACs in full); six substrate smokes all re-verifiable; ZERO-PATCH target per Stories 1.11/1.12/1.13/1.14/1.15 FIFTH cumulative precedent → Story 1.16 SIXTH cumulative candidate.
- [x] iter-91: **FR14n row 4 `atdd-scaffolded → in-dev` via `/bmad-dev-story` SINGLE-PASS at configuration-surface + documentation-tier scale.** SIXTH cumulative single-pass-dev precedent for Epic-1 substrate configuration/scaffolding stories (Stories 1.10/1.11/1.12/1.13/1.14/1.15 → 1.16). Authored 2 NEW files (`packages/keel-invariants/templates/INVARIANTS.fork.md` scaffold template — H1+§Precedence+§Fork invariants index+§Consumption+§Extension+commented `FORK-<fork-slug>-<category>-<slug>` naming example; sha256 `167ba6b2...6218` + `docs/invariants/fork.md` 10-section rationale doc with ESLint-extend pattern + INVARIANTS.fork.md scaffold + substrate-wins precedence + amendment-vs-fork decision tree + Growth-tier opt-in; sha256 `be6f3d89...92f2`) + MODIFIED 4 files (`packages/keel-invariants/src/invariants.manifest.ts` +2 entries → 24 total; `INVARIANTS.md` new `### Fork extension (Story 1.16)` H3 + 2 anchor bullets between line 80 `INV-renovate-rationale` and `## Consumption`; `AGENTS.md` new `## Fork extension (FR44)` H2 between `## Git / PR conventions` and `## Ralph loop`; `CLAUDE.md` 5th row `INVARIANTS.fork.md` in § Knowledge-file contract + precedence paragraph). Iter-77 Gotcha honored — `pnpm --filter @keel/keel-invariants build` executed before runtime manifest-load smoke. All 7 substrate-verification smokes PASS: scaffold-shape, rationale-doc-shape, AGENTS.md update, CLAUDE.md update, manifest-load `OK: 24 invariants`, sync-gate clean exit 0 in **0.776s** (61.2% margin vs Story 1.9 AC 7 <2s budget; +147ms vs iter-86 baseline for +2 entries — O(n+m) walker), full quality-gate suite (typecheck 16/16 in 1.446s; lint 16/16 in 7.926s; format:check clean; keel-invariants:check-all both exit 0). Seven preventative audit layers held ZERO-PATCH at dev-time (L1 regex pass both IDs; L2 complete AC↔Task coverage; L3 sprint-status `ready-for-dev → in-progress → review` + `last_updated` bump; L4 cross-file convergence verified; L5 6th single-pass precedent; L6 no-cascade-hash — knowledge files are consumption pointers not sourcePaths; L7 scaffolding + configuration-surface + documentation-tier carve-out held). Story State `atdd-scaffolded → in-dev`; BMad Status `ready-for-dev → review`; sprint-status `ready-for-dev → in-progress → review` + `last_updated: 2026-04-21 Story-1-16-review UTC` at both header + data field. Change Log v1.3 appended. Single-pass dev forecast HELD exactly — zero partial completion, zero residual tasks. Cumulative 6-story Epic-1 substrate-story single-pass-dev precedent.
- [x] iter-90: **FR14n row 3 `validated → atdd-scaffolded` via `/bmad-testarch-atdd` TENTH cumulative Epic-1 ATDD-SKIP precedent** (Stories 1.7 iter-14 + 1.8 iter-29 + 1.9 iter-36 + 1.10 iter-43 + 1.11 iter-50 + 1.12 iter-57 + 1.13 iter-64 + 1.14 iter-71 + 1.15 iter-83 → 1.16 iter-90). Skill preflight HALTs at Step 1.2 (no test runner — vitest/jest/playwright configs absent; landing is Epic 13 / post-Epic-1). Hybrid ground-(c) variant-(ii)+(iii): (a) substrate-verification-covers-ACs at CLI-exit-code level (Task 7 six smokes: prettier-check both new MDs + manifest-load 24 entries + sync-gate clean + INVARIANTS.md regex for 2 anchors + AGENTS.md § Fork extension H2 regex + CLAUDE.md contract-table row + precedence keyword) exercise ACs 1-4 end-to-end + Story 1.9 sync-gate drift-detects via content-hash pinning on every commit; (b) no-runner framework prerequisite unmet; (c) HYBRID variant-(ii)+(iii) — AC 4 runtime carve-out (Epic 15a `create-keel-app --include-fork-invariants` default-false) DOWNSTREAM to Epic 15a CLI tests; adversarial AC 1/2/3 coverage (ESLint-extend documentation fidelity + precedence-rule accuracy + amendment-vs-fork decision tree completeness + template-file shape integrity + CLAUDE.md contract-row accuracy + manifest-entry description faithfulness) delegated to iter-94 `/bmad-code-review (args: "2")` three-layer fan-out. Deliverable: 1 in-situ Change Log bullet (v1.2) appended to story file — mirrors Story 1.15 iter-83 v1.2 pattern exactly. No test-plan artefacts authored (variant-(ii)+(iii) substitution). Story State: `validated → atdd-scaffolded`. BMad Status unchanged (`ready-for-dev`; Ralph-loop-semantic gate, not BMad status transition).
- [x] iter-89: **FR14n row 2 `drafted → validated` via `/bmad-create-story (args: "review")` pre-dev SM readiness review for Story 1.16.** THREE-PATCH outcome (ZERO-PATCH target narrowly missed; matches Story 1.11-1.15 1-2 PATCH pre-dev average). Three parallel fresh-context subagents (Explore + general-purpose) audited story against epics/prd/architecture + pre-existing file structure + manifest-ID regex + Story 1.15 pattern parity. **Sub-agent 1 found** (and I directly verified via `Grep` + `Read`): (a) prd.md FR44/FR45 actually at :1010/:1011, NOT :141/:142 as story claimed; (b) 4 of 5 architecture.md line citations in story References + Dev Notes L4 were fabricated (lines 237/913/1010/1011 point at unrelated content — Frontend / commitlint.config / test-utils / pglite-setup); only line 906 was real. `Grep "INVARIANTS\.fork"` on architecture.md returned 0 matches — architecture.md contains NO `INVARIANTS.fork.md` reference at all. **Sub-agent 2** confirmed all 12 structural pre-existing-file claims ✓ (package.json:14 / manifest.ts:4 / sync-gate.ts:24 / 22 manifest entries / INVARIANTS.md :80/:82 / AGENTS.md 66 lines / CLAUDE.md :43-48 / docs/invariants 5 files / packages/keel-invariants/templates + packages/create-keel-app both not-yet-existing). **Sub-agent 3** confirmed both new manifest IDs regex-pass + unique-ID + unique-sourcePath + shape parity with Story 1.15 `INV-renovate-rationale` + self-anchor convention matches 3 prior rationale-docs. **Three PATCHes applied** to story file: (1) References prd.md line numbers :141→:1010, :142→:1011; (2) References architecture.md — removed 4 fabricated entries, added 3 real convergence points (:49 FR41-FR45 cluster enumeration + :777 FR→Artefact mapping table + :906 directory-tree annotation, retained); (3) Dev Notes L4 cross-file-convergence paragraph rewritten to reflect ground truth. Change Log v1.1 appended. Story State: `drafted → validated`. BMad Status: `ready-for-dev` unchanged (pre-dev SM is Ralph-loop-semantic gate, not BMad status transition).
- [x] iter-88: **FR14n row 1 `_(no story) → drafted` via `/bmad-create-story`** for Story 1.16 (FINAL open story in Epic 1 + FR44 at 1.0 + FR45 Growth-tier substrate authoring). Drafted `_bmad-output/implementation-artifacts/1-16-fork-extension-config-pattern-growth-tier-invariants-fork-md-scaffold.md` with 4 ACs (verbatim from epics.md:1110-1137 with Story 1.15-style scope carve-outs) + 7 tasks (template scaffold at `packages/keel-invariants/templates/INVARIANTS.fork.md` + rationale doc at `docs/invariants/fork.md` + manifest entries `INV-fork-extension-rationale` + `INV-fork-invariants-scaffold` + anchor bullets + AGENTS.md § Fork extension (FR44) section + CLAUDE.md knowledge-file-contract table 5th row + substrate verification suite). Seven-layer audit pre-applied. Sprint-status flipped `backlog → ready-for-dev` + `last_updated: 2026-04-21 Story-1-16-drafted UTC`. Manifest growth forecast 22 → 24 (+9%). Story class: scaffolding / configuration-surface / documentation-tier — TENTH cumulative Epic-1 ATDD-skip + TENTH cumulative WAIVED-trace + SIXTH cumulative ZERO-PATCH-CR candidate.
- [x] iter-87: FR14n row 9 `sm-verified → done` via `/bmad-code-review (args: "2")` FIFTH cumulative ZERO-PATCH CR precedent (Stories 1.11/1.12/1.13/1.14/1.15). Full 7-iter lifecycle held exactly.

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1-1.15 done; Story 1.16 final open story — 3-iter FR14n lifecycle remaining before EPIC_DONE halt: iter-93 → iter-94 `traced → done`, iter-95 PR Draft→Open + EPIC_DONE).
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (continues through Epic 1; PR #226 stays Draft until EPIC_DONE per PROMPT_build.md step 5c).
- **Story:** Story 1.16 — Fork extension-config pattern + Growth-tier `INVARIANTS.fork.md` scaffold (FR44 + FR45).
- **Story File:** `_bmad-output/implementation-artifacts/1-16-fork-extension-config-pattern-growth-tier-invariants-fork-md-scaffold.md` (iter-88 created; iter-89 3-PATCH pre-dev SM correction at v1.1; iter-90 v1.2 ATDD-skip; iter-91 v1.3 dev-story; iter-92 v1.4 trace-WAIVED).
- **Story State:** `traced` — iter-93 next FR14n row 7 `traced → sm-verified` via `/bmad-create-story (args: "review")` post-dev SM review (SIXTH cumulative ZERO-PATCH candidate per Stories 1.11/1.12/1.13/1.14/1.15 FIFTH precedent).
- **GitHub Issue:** Story 1.16 at **#40** OPEN. Parent Epic 1 at **#9** OPEN (closes at EPIC_DONE halt).
- **PR:** #226 (Draft, `no checks reported` at iter-92 start — push-safe; stays Draft through iter-93..iter-94 Story 1.16 lifecycle; Draft→Open at iter-95 post-Story-1.16 `done`).

## Notes

- **Story 1.16 iter-92 trace outcome: TENTH cumulative Epic-1 WAIVED precedent** (Stories 1.7 iter-4 + 1.8 iter-3 + 1.9 iter-3 + 1.10 iter-46 + 1.11 iter-57 + 1.12 iter-64 + 1.13 iter-71 + 1.14 iter-78 + 1.15 iter-85 → 1.16 iter-92). Hybrid ground-(c) variant-(ii)+(iii) substitution pattern held identical to Story 1.15 iter-85 (structural trace-artefact inheritance from 1-15 template). Four trace artefacts authored: `1-16-coverage-matrix.json` + `1-16-e2e-trace-summary.json` + `1-16-gate-decision.json` + `1-16-fork-extension-config-pattern-growth-tier-invariants-fork-md-scaffold.md`. All six Task 7 smokes re-verified LIVE byte-identical at iter-92 trace-time (scaffold template sha256 `167ba6b2…6218` + rationale doc sha256 `be6f3d89…92f2` unchanged; manifest load `OK: 24 invariants`; sync-gate exits 0 clean within <2s AC 7 budget; no vitest/jest/playwright/cypress configs in tree). Rationale-of-record: (a) substrate-verification-covers-ACs at CLI-exit-code level; (b) no-runner (Epic 13 scope); (c) HYBRID variant-(ii)+(iii) — AC 4 runtime DOWNSTREAM to Epic 15a CLI + adversarial AC 1-4 to iter-94 CR three-layer fan-out. Phase 1 deterministic FAIL (0% < 80%) is STRUCTURAL FALSE-POSITIVE.
- **Carry-forward rules INTO iter-93 `/bmad-create-story (args: "review")` post-dev SM for Story 1.16:**
  - Skill invocation: `/bmad-create-story (args: "review")` with Story 1.16 file as target. Post-dev SM semantic = requirements-satisfaction review (verify implementation satisfies all 4 ACs fully; NOT the same as pre-dev SM readiness review at iter-89).
  - ZERO-PATCH target per FIFTH cumulative precedent (Stories 1.11/1.12/1.13/1.14/1.15 all ZERO-PATCH at post-dev SM). Story 1.16 SIXTH cumulative candidate.
  - Verification surface: all 4 ACs satisfied at substrate layer (iter-91 dev-story + iter-92 LIVE re-verification both pass byte-identical). Six Task 7 smokes directly probe AC 1-4 at CLI-exit-code level:
    - AC 1 (ESLint-extend pattern documented in AGENTS.md) ← smoke 3 AGENTS.md update + smoke 2 rationale-doc shape.
    - AC 2 (substrate-wins precedence + amendment-vs-fork decision) ← smoke 3 AGENTS.md update + smoke 2 rationale-doc shape.
    - AC 3 (Growth-tier scaffold + CLAUDE.md reference + precedence) ← smoke 1 scaffold-template shape + smoke 4 CLAUDE.md update.
    - AC 4 (1.0 default non-auto-create + documented opt-in) ← smoke 2 rationale-doc shape (§ Growth-tier opt-in section) + smoke 5 manifest-load + smoke 6 sync-gate clean.
  - No PATCHes expected at post-dev SM — all 4 ACs pass substrate-layer verification + no unmet-AC findings expected (adversarial AC 1-4 coverage deferred to iter-94 CR per § Testing Standards hybrid ground-(c) variant-(iii)). If post-dev SM fires unmet-AC finding, fix tasks queue to TOP of QUEUE per § Story Lifecycle Decision Matrix.
  - Change Log v1.5 row appended to story file documenting FR14n `traced → sm-verified` transition.
- **Carry-forward rules INTO iter-94 `/bmad-code-review (args: "2")` for Story 1.16:**
  - Skill invocation: `/bmad-code-review (args: "2")` pre-selects "Create action items" mode. Three-layer fan-out: Blind Hunter (diff-only adversarial review) + Edge Case Hunter (diff + repo-read) + Acceptance Auditor (AC-verification).
  - ZERO-PATCH target per FIFTH cumulative CR precedent (Stories 1.11/1.12/1.13/1.14/1.15 all ZERO-PATCH at CR).
  - Audit surface across 6 files: `packages/keel-invariants/templates/INVARIANTS.fork.md` (scaffold shape + FORK-naming example) + `docs/invariants/fork.md` (10 sections + anchor) + `packages/keel-invariants/src/invariants.manifest.ts` (+2 entries at array tail with correct id/sourcePath/contentHash/anchors) + `INVARIANTS.md` (new `### Fork extension (Story 1.16)` H3 + 2 anchor bullets column-0 format) + `AGENTS.md` (`## Fork extension (FR44)` H2 between `## Git / PR conventions` + `## Ralph loop`) + `CLAUDE.md` (5th row in § Knowledge-file contract + precedence paragraph).
  - Post-CR: `done` (no findings) → iter-95 PR Draft→Open + EPIC_DONE sequence.
- **Carry-forward rules INTO iter-95 PR Draft→Open + EPIC_DONE for Story 1.16:**
  - Skill invocation: `gh pr ready && gh pr checks --watch --fail-fast` on PR #226 (currently Draft, no checks reported).
  - Final-CI-gate scope: full quality-gate suite (typecheck + lint + format:check + keel-invariants:check-all + tokens-sync) must pass on main-branch-synced PR head. Expected green (all substrate-authoring + test-plan work at story-level; CI runners not yet authored pre-Epic-13).
  - Post-CI-green: sprint-status flips `epic-1: in-progress → done`; write EPIC_DONE halt `{"reason":"EPIC_DONE","epic":1,"pr":226}` to `$RALPH_BASE_DIR/halt`. ralph.py reads sentinel + halts loop + closes Epic-1 issue #9.
- **Issue Tracking carry-forward.** Story 1.16 at **#40** OPEN — iter-92 start re-confirms Story `**Story:**` field unchanged (ralph.py no-op on same-story iteration). Parent Epic 1 at **#9** OPEN — closes at EPIC_DONE halt after iter-95. Commit trailer: `Refs #40`.
