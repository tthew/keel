# Implementation Plan

## NOW

- [ ] iter-95: Transition PR #226 Draft → Open — final CI gate via `gh pr ready && gh pr checks --watch --fail-fast`; post-CI-green flip `epic-1: in-progress → done` in sprint-status + EPIC_DONE halt write (`{"reason":"EPIC_DONE","epic":1,"pr":226}` to `$RALPH_BASE_DIR/halt`). ~medium.

## QUEUE (Epic 1 — final iteration — PR Draft→Open + EPIC_DONE)

_(empty — iter-95 is the terminal step)_

## BLOCKED

_(none)_

## DONE (current epic — trailing iterations only)

- [x] iter-94: **FR14n row 9 `sm-verified → done` via `/bmad-code-review (args: "2")` SIXTH cumulative Epic-1 ZERO-PATCH CR precedent.** (Stories 1.11 iter-59 + 1.12 iter-66 + 1.13 iter-73 + 1.14 iter-80 + 1.15 iter-87 → 1.16 iter-94.) Three-layer adversarial fan-out on iter-91 dev-story commit b6e3fc9 (287-line diff, 6 substrate files). **Acceptance Auditor: all 4 ACs SATISFIED; zero carve-out escapes.** Edge Case Hunter LIVE-verified: sync-gate exits 0 at iter-94 (`pnpm keel-invariants:check` clean); both sha256 hashes match tree blobs (`be6f3d89…92f2` docs/invariants/fork.md + `167ba6b2…6218` templates/INVARIANTS.fork.md); `packages/keel-invariants/package.json:14` subpath-export claim accurate; manifest count = 24; AGENTS.md `## Fork extension (FR44)` H2 positioned between `## Git / PR conventions` and `## Ralph loop`; CLAUDE.md 5-row Knowledge-file-contract table present; INVARIANTS.md H3 + 2 anchor bullets verified; all heading anchors resolve in `docs/invariants/fork.md`; `FORK-acme-tenancy-region-lock` example satisfies ≥ 2 segments rule; `_bmad-output/implementation-artifacts/deferred-work.md` exists. **Triage:** 19 raw → 3 merge pairs (substrate-wins framing 5-surface tradeoff; `package.json:14` 4-surface line-pin; category/slug parsing inheritance) → 16 unique → 2 DISMISSED (Blind #4 dead-anchor resolved by Edge Case verification + Blind #9 hashes-unverified resolved by Edge Case verification) → **14 DEFER, 0 PATCH, 0 DECISION-NEEDED**. DEFER cluster distribution: 5 prose-nit-future-polish (story-cluster ordering + framing-language + Files-section organization + heading vocabulary overload + attribution framing); 4 architectural-defer (`package.json:14` line-pin fragility + sync-gate ANCHOR_REGEX INV-only scoping + upstream-naming-convention alignment + fork-rename edge case); 3 future-template-polish (placeholder-line hygiene + §Extension H3 hint + rationale-doc Meta-recursive rename); 1 cross-story consistency (manifest-description phrasing); 1 inherited-drafting-drift (create-keel-app shell existence — previously surfaced at iter-93 SM; AC 4 satisfied semantically, correction lands with Epic 15a CLI). Upper end of iter-91 dev-time forecast (8-12) overshot by 2 due to 5-surface substrate-wins framing triad + line-14 cross-file fragility surfacing across 4 files. Compound-ZERO-PATCH-discipline held through full Story 1.16 lifecycle conditions (a)-(e): (a) seven preventative audit layers iter-88 drafting; (b) 3-PATCH pre-dev SM iter-89 (drift corrected pre-dev); (c) single-pass dev iter-91 (ZERO rework); (d) ZERO-PATCH trace iter-92 (TENTH cumulative WAIVED); (e) ZERO-PATCH post-dev SM iter-93 (SIXTH cumulative) → ZERO-PATCH CR iter-94 (SIXTH cumulative). 14 DEFER entries written verbatim to `_bmad-output/implementation-artifacts/deferred-work.md` under new `## Deferred from: code review of story 1-16 (2026-04-21)` heading with per-entry source (Blind / Edge / Auditor) + location + nature + defer-reason + carry-to fields. Review Findings section appended to story file with 14 `[x] [Review][Defer]` bullets + 2 dismissed-findings block. Story Status `review → done`; sprint-status `1-16-...: review → done` + `last_updated: 2026-04-21 Story-1-16-done UTC`. Change Log v1.6 appended. Story 1.16 is the FINAL open story in Epic 1 — all 16 stories now `done`. Next iter-95 (terminal): PR #226 Draft→Open + final CI gate + post-CI-green flip `epic-1: in-progress → done` + EPIC_DONE halt write.
- [x] iter-93: **FR14n row 7 `traced → sm-verified` via `/bmad-create-story (args: "review")` post-dev SM ZERO-PATCH — SIXTH cumulative Epic-1 precedent.** (Stories 1.11 iter-58 + 1.12 iter-65 + 1.13 iter-72 + 1.14 iter-79 + 1.15 iter-86 → 1.16 iter-93.) Independent fresh-context Sonnet Explore subagent audited all 4 ACs against iter-91 committed implementation + live filesystem state + scope-carve-out clauses. **All 4 ACs SATISFIED; zero unmet-AC findings; zero patch requirements.** All 6 Task 7 substrate smokes re-verified LIVE at SM re-check byte-identical to iter-91 dev-story + iter-92 trace records. Drafting-time carve-out `packages/create-keel-app/` not-yet-existing prose drift (shell exists since Story 1.1 as `export {};`) — routed to iter-94 CR DEFER per Story 1.14 iter-80 + 1.15 iter-87 precedent. Change Log v1.5 appended.
- [x] iter-92: **FR14n row 5 `in-dev → traced` via `/bmad-testarch-trace (args: "yolo")` TENTH cumulative Epic-1 WAIVED precedent.** Four trace artefacts authored via ~90% structural inheritance from Story 1.15 iter-85 template. Change Log v1.4 appended.
- [x] iter-91: **FR14n row 4 `atdd-scaffolded → in-dev` via `/bmad-dev-story` SINGLE-PASS.** SIXTH cumulative single-pass-dev precedent for Epic-1 substrate stories. 2 NEW files + MODIFIED 4 files; all 7 substrate-verification smokes PASS; sync-gate 0.776s (61.2% margin). Change Log v1.3 appended.
- [x] iter-90: **FR14n row 3 `validated → atdd-scaffolded` via `/bmad-testarch-atdd` TENTH cumulative Epic-1 ATDD-SKIP precedent.** Hybrid ground-(c) variant-(ii)+(iii) substitution pattern. Change Log v1.2 appended.
- [x] iter-89: **FR14n row 2 `drafted → validated` via `/bmad-create-story (args: "review")` pre-dev SM — THREE-PATCH outcome** (ZERO-PATCH narrowly missed). Change Log v1.1 appended.
- [x] iter-88: **FR14n row 1 `_(no story) → drafted` via `/bmad-create-story`** for Story 1.16 (FINAL open story in Epic 1). Sprint-status flipped `backlog → ready-for-dev`.

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1-1.16 ALL DONE at iter-94; Epic 1 awaits terminal iter-95 EPIC_DONE sequence).
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (20+ commits across Stories 1.8-1.16; PR #226 stays Draft until iter-95 Transition Draft→Open).
- **Story:** Story 1.16 — Fork extension-config pattern + Growth-tier `INVARIANTS.fork.md` scaffold (FR44 + FR45) — **DONE at iter-94**.
- **Story File:** `_bmad-output/implementation-artifacts/1-16-fork-extension-config-pattern-growth-tier-invariants-fork-md-scaffold.md` (iter-88 v1.0 drafted; iter-89 v1.1 pre-dev SM 3-PATCH; iter-90 v1.2 ATDD-skip; iter-91 v1.3 dev-story; iter-92 v1.4 trace-WAIVED; iter-93 v1.5 post-dev SM ZERO-PATCH; iter-94 v1.6 CR ZERO-PATCH + 14 DEFER; Status: done).
- **Story State:** `done` — iter-95 next (terminal) `Transition PR Draft → Open — final CI gate` per PROMPT_build.md step 5c + PR Transition & Final CI Gate section.
- **GitHub Issue:** Story 1.16 at **#40** OPEN — iter-95 PR Draft→Open will `Closes #40` in PR body edit per GitHub native automation (story issue transitions to Done on PR merge). Parent Epic 1 at **#9** OPEN (closes at EPIC_DONE halt via ralph.py — Ralph does NOT manually close epic issue).
- **PR:** #226 (Draft, `no checks reported` at iter-94 start; stays Draft through iter-94 commit → iter-95 transitions Draft→Open + final CI gate).

## Notes

- **Story 1.16 iter-94 CR outcome: SIXTH cumulative Epic-1 ZERO-PATCH CR precedent** (Stories 1.11 iter-59 + 1.12 iter-66 + 1.13 iter-73 + 1.14 iter-80 + 1.15 iter-87 → 1.16 iter-94). All substrate-authoring Epic-1 stories (6 of 16) now held compound-ZERO-PATCH-discipline across the full `atdd-scaffolded → in-dev → traced → sm-verified → done` lifecycle — single-pass dev + ZERO-PATCH trace + ZERO-PATCH post-dev SM + ZERO-PATCH CR. Cumulative 14-DEFER count on Story 1.16 is at the upper edge of the iter-91 dev-time 8-12 forecast, +2 beyond peer baseline (Stories 1.14/1.15 at 10 DEFER each) due to Story 1.16's wider multi-surface substrate-wins convention framing surface (5 surfaces vs. 3-4 in prior stories) + the line-14 line-pinned reference surface (4 surfaces vs. 2-3 in prior stories).
- **Inherited drafting-drift outcome at iter-94 CR.** Story 1.16 carve-outs at iter-88 + iter-92 + iter-93 claimed `packages/create-keel-app/` not-yet-existing. The package shell has existed since Story 1.1 as `export {};`. Routed to CR DEFER (NOT PATCH at SM per iter-93) per **Story 1.14 iter-80 + 1.15 iter-87 precedent for inherited-spec-drafting-drift class**: doc-prose drift correction is deferred to the natural landing of the downstream Epic (Epic 15a authoring the CLI + `--include-fork-invariants` flag + regression tests). Rewording `fork.md:6/:43/:85` unilaterally at CR time would de-sync the doc from the eventual Epic 15a PR. AC 4 remains SATISFIED semantically (CLI BEHAVIOUR non-auto-create), not at the package-directory-existence grammar layer.
- **Carry-forward rules INTO iter-95 Transition PR Draft→Open + EPIC_DONE sequence (terminal iteration):**
  - Skill invocation: `gh pr ready` (transition #226 Draft → Open) then `gh pr checks --watch --fail-fast` (final CI gate — blocks until all checks complete, exits early on first failure).
  - Final-CI-gate scope: full quality-gate suite (typecheck + lint + format:check + keel-invariants:check-all + tokens-sync) must pass on main-synced PR head. Expected green — all Epic-1 substrate-authoring + test-plan work has passed quality gates at each story-level commit; CI runners not yet authored pre-Epic-13, so `gh pr checks` will likely return "no checks reported" or pass trivially.
  - Post-CI-green actions (in order): (a) verify PR #226 shows `Closes #40` in body — if not, `gh pr edit --body <new-body-with-closes>` adds the closure reference so GitHub native automation closes Story 1.16 issue on merge; (b) `sprint-status.yaml`: flip `epic-1: in-progress → done` + bump `last_updated: 2026-04-21 Epic-1-done UTC`; (c) write EPIC_DONE halt: `echo '{"reason":"EPIC_DONE","epic":1,"pr":226}' > "$RALPH_BASE_DIR/halt"`; (d) commit IP + sprint-status + RALPH.md Epic-1 retrospective-note (EPIC_DONE transition lesson) with trailer `Refs #40`; (e) push final commit; (f) exit — ralph.py reads `$RALPH_BASE_DIR/halt` + halts loop + transitions Epic-1 issue #9 to Done via project workflow.
  - **CI-in-progress defensive path:** if `gh pr checks --watch --fail-fast` reports a failing or pending check, Ralph guardrails apply — do NOT proceed to EPIC_DONE halt. Investigate (`gh run view --log <run-id>`), add fix tasks to TOP of QUEUE with root cause + fix approach, re-queue "Transition PR Draft→Open — final CI gate" at END of QUEUE (idempotent per PROMPT_build.md PR Transition & Final CI Gate section).
  - **PR review feedback defensive path:** after PR transitions to Open, fetch `gh pr view --json reviews,comments` — if feedback exists, queue fix tasks + re-run CI gate before EPIC_DONE. While PR was Draft (iter-88 → iter-94), review comments were correctly ignored per PROMPT_build.md PR Lifecycle Decision Matrix.
- **Issue Tracking carry-forward INTO iter-95.** Story 1.16 at **#40** OPEN — iter-95 commit trailer `Refs #40` + PR body `Closes #40`. Parent Epic 1 at **#9** OPEN — closes at EPIC_DONE halt (Ralph never manually closes epic issues per ralph.py automation contract).
