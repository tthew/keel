# Implementation Plan

## NOW

- [ ] `/bmad-code-review (args: "2")` re-run on Story 2.3 — after 9-PATCH drain (AR-1..AR-9 landed iter-162..iter-170); verdict `done` (ZERO-PATCH close) OR residual-cycle continues. Precedent: Story 2.1 iter-128..iter-144 3-cycle chain (9 → 5 → 0 AIs); Story 2.2 iter-151..iter-154 single fix-drain + re-run close (2 → 0 AIs). Story 2.3 forecast per iter-161 envelope: 1–2 re-run cycles (9 → likely 1–3 residual → 0 close). Budget: ~60K tokens.

## QUEUE (Story 2.3 fix-drain tail → CR re-run → done)

- [ ] _(conditional, post-Story-2.3 done)_ `/bmad-create-story` for Story 2.4 (whitelist CLI: `packages/devbox/scripts/whitelist.sh` user-facing with add/remove/list/sync + per-fork override + diff summary + invocation of `reload-egress.sh`) per § Story Lifecycle Decision Matrix row `done` → auto-advance within-epic. Story 2.4 is infrastructure-CLI class (new class for Epic 2; may or may not inherit ATDD-skip / WAIVED-trace precedents depending on whether Epic 13 has landed a test runner by then).
- [ ] _(conditional, post-Story-2.17 done, ~Epic 2 completion)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge. Epic 2 completion.
- [ ] _(conditional, post-Epic-2 merged)_ § Cross-epic transition → Epic 3 Story 3.1 OR halt EPIC_DONE awaiting merge.

## BLOCKED

_(none — Story 2.3 9-PATCH drain complete iter-170; re-run CR next.)_

## ATDD Red Phase

_(none — ATDD-skipped at iter-157; no red-phase failures to carry.)_

## DONE (current story 2.3 fix-drain; prune oldest as drain closes out)

- [x] iter-170: **AR-9 fix-drain landed — replaced unquoted `$(printf … | sort)` command-substitution in `packages/devbox/scripts/start-egress.sh:76` with `mapfile -t fragments < <(LC_ALL=C find "${WHITELIST_FRAGMENTS_DIR}" -maxdepth 1 -type f -name '*.txt' -print | sort)` + `for fragment in "${fragments[@]}"`.** Pre-fix form word-split on IFS (`$' \t\n'`) — a fragment filename `team A.txt` expanded into two tokens `team` + `A.txt`, BOTH failed the downstream `[[ -r "${fragment}" ]]` guard silently, so that fragment's domains were missing from the composed whitelist with zero diagnostic (soft-corruption class, not an error). Post-fix: `mapfile -t` splits on newlines only — filenames with whitespace preserved intact as single array elements; `"${fragments[@]}"` re-expands each name as ONE argument with embedded whitespace intact; dropped the paired `shopt -s nullglob` / `shopt -u nullglob` bracket (no longer needed — empty `find` output → empty array, safe under bash 4.4+ `set -u`). Swapped shell glob for `find -maxdepth 1 -type f -name '*.txt'` for explicit non-recursion + file-only scoping in one line. Inline comment (13-line) pins rationale + the specific failure-mode diagnosis (which fragment name → which pair of spurious tokens → why the `[[ -r ]]` guard is not a defence). **Ran 7-probe isolated harness against bash 5.2.21:** (i) OLD form in bash — reproduces the bug: `team A.txt` SKIP'd as two tokens `team` + `A.txt`; (ii) NEW form — mapfile array length 3, all fragments READ; (iii) NEW composes `MARKER_SPACEFRAG` (unique token inside `team A.txt`) exactly ONCE (expect 1, got 1 — fix works); (iv) OLD MISSES `MARKER_SPACEFRAG` completely (expect 0, got 0 — pre-fix bug confirmed); (v) empty-fragments dir — `fragments.length = 0`, for-loop no-op, no `set -u` trap; (vi) scoping — `-maxdepth 1 -type f -name '*.txt'` correctly skips README.md + subdir/nested.txt, kept only `kept.txt`; (vii) deterministic sort — `anthropic.txt < npm.txt < team A.txt < zzz.txt` LC_ALL=C order preserved. `bash -n` exit 0. No invariant-doc contentHash refresh needed (start-egress.sh has zero contentHash references — re-verified; `INV-devbox-egress-contract` hashes `docs/invariants/devbox-egress.md` only, which this PATCH does not touch; the doc describes `whitelist/*.txt` fragment composition in prose at L33/L41 but does not pin the implementation pattern — refactor is pure implementation-level). Budget: ~12K tokens (under ~15K forecast). Promoted bash word-splitting-on-unquoted-command-substitution lesson to RALPH.md § Gotchas (pair with AR-8's `${array[@]/pattern}` entry to form a bash-array-semantics section for future devbox/CI-harness shell work). **Drain trajectory:** 9/9 PATCH landed (AR-1..AR-9 complete); QUEUE fix-drain closed. NEXT iteration: re-run `/bmad-code-review (args: "2")` for `done` OR continue cycle per § Story Lifecycle Decision Matrix row `fixes-pending`.
- [x] iter-169: **AR-8 fix-drain landed — rebuilt `cleanup_files` array via explicit loop at `packages/devbox/scripts/reload-egress.sh:292-303` instead of bash parameter-substitution `${cleanup_files[@]/${dnsmasq_rendered}}`.** Pre-fix form substituted the matched element to empty string — array length stayed unchanged at 3 (`[nft_rendered, '', <whatever else]`) and the EXIT trap `cleanup()` only skipped the `''` entry because of its `[[ -n "${f}" && -e "${f}" ]]` guard. Post-fix: `new_cleanup=(); for f in "${cleanup_files[@]}"; do [[ "$f" != "${dnsmasq_rendered}" ]] && new_cleanup+=("$f"); done; cleanup_files=("${new_cleanup[@]}")` actually removes the consumed tempfile so the array content matches its semantic invariant. 5-probe isolated harness: (i) OLD — length stays 3 with `[1]=''`; (ii) NEW — length 2 clean; (iii) element-not-present unchanged; (iv) empty-result edge OK under `set -euo pipefail`; (v) trap-iteration against OLD confirms `-n` guard caught empty (bug was harmless in practice, just intent-obscuring). `bash -n` exit 0. No invariant-doc contentHash refresh needed. Budget: ~10K tokens (on forecast). Promoted bash `${array[@]/pattern}` blanking-semantics lesson to RALPH.md § Gotchas.
- [x] iter-168: **AR-7 fix-drain landed — introduced per-chain marker names in `packages/devbox/nftables/egress.nft` (`KEEL_EGRESS_V4_MARKER_{START,END}` + `KEEL_EGRESS_V6_MARKER_{START,END}`) + chain-scoped awk substitution in `packages/devbox/scripts/reload-egress.sh` (dropped the `chain` state variable entirely).** Refactor parameterised `validate_markers()` to accept marker-name argument; 3 preflight call sites. Structural-order-coupling (reorder chains → silent mis-route) closed: chain-scoped names make injection self-disambiguating. 6-probe harness verified happy + 3 fault modes exit 6 + dnsmasq unchanged + chain-swap structural-order independence. No invariant-doc contentHash refresh needed. Budget: ~22K tokens (under forecast).

## Context

- **Phase:** 4-implementation — Epic 2 at 2/17 stories `done` (2.1 + 2.2) + 1/17 `in-progress` (2.3 `fixes-pending` — 9-PATCH drain COMPLETE iter-170; re-run CR next).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.3 live smokes (Task 12.1 – 12.8) operator-workstation deferred; the iter-161 PATCH cluster was dominated by edges those deferred smokes would have caught at bake-time — drain completed without any live-runtime iterations.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Story 2.3 Task 1 appended `dnsmasq` + `nftables` to image; re-bake needed at operator-workstation close-out.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; 2.1 + 2.2 done; 2.3 `in-progress` (fixes-pending → CR re-run); 2.4..2.17 in `backlog`.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2; PR #230 transitions to Open at Story 2.17 completion).
- **Story:** 2.3 — egress-policy-dnsmasq-nftables-fail-closed-ipv4-ipv6-parity-atomic-reload.
- **Story File:** `_bmad-output/implementation-artifacts/2-3-egress-policy-dnsmasq-nftables-fail-closed-ipv4-ipv6-parity-atomic-reload.md` (v1.6 at iter-161; Status `in-progress`; § Review Findings with 9 PATCH action items + 9 DEFER checkmarks).
- **Story State:** `fixes-pending` — CR opener landed iter-161 9 PATCH; iter-162 AR-1 (first-boot resolver detour); iter-163 AR-2 (idempotent `add table` prepend); iter-164 AR-3 (in-place resolv.conf overwrite); iter-165 AR-4 (split getent from awk|sort); iter-166 AR-5 (pidfile+kill-0 + TCP-probe composite liveness + doc-hash refresh); iter-167 AR-6 (`validate_markers()` preflight); iter-168 AR-7 (per-chain marker names + chain-scoped awk); iter-169 AR-8 (explicit-loop array rebuild); iter-170 AR-9 (mapfile+find swap for whitespace-safe fragment enumeration). 9/9 PATCH landed — QUEUE empty. Next iteration re-runs `/bmad-code-review (args: "2")` per § Story Lifecycle Decision Matrix row `fixes-pending` — verdict `done` OR cycle continues.
- **GitHub Issue:** Story 2.3 → #43; Epic 2 → #10.
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **Acceptance Auditor ZERO-drift at CR opener upholds iter-160 SM verdict.** The 9 PATCH findings were all bug-class (boot-sequence + shell-script defensive edges), not spec drift — reflects the Story 2.3 author's adherence to substrate commitments (ACs + SCs) while the operator-workstation-deferred smokes (Task 12.1 – 12.8) left runtime-edge coverage gaps. Iter-170 closes the fix-drain with a 9/9 PATCH success rate and zero knock-on findings across any of the 9 drain iterations.
- **Convergent PATCH signal is the highest-confidence axis for fix-drain.** AR-1 + AR-4 + AR-5 convergent across Blind + Edge Case (all three layers saw the first-boot resolver-bootstrap failure from different angles); AR-3 + AR-8 convergent. Drain in convergence-first order held across 9 iterations without any rework.
- **DinD is fork-time substrate, not transitional.** `INV-devbox-dind-available` — every fork's cc-devbox-equivalent must provide Docker. Story 2.3 live smokes are operator-workstation deferred; SC-7 verbatim + AC-mapped `docker exec` recipes pinned in `docs/invariants/devbox-egress.md § Verification` + `packages/devbox/README.md § Egress policy § Verification` for the operator close-out.
- **Backend B is the reference env at 2026-04-21.** Broad-state-mutation scripts MUST detect backend + refuse destructive ops by default.
- **NFR2 authority unchanged.** Cold ≤ 300s / warm ≤ 30s remains authoritative on M4-Pro native; DinD runs are indicative only.
- **Cumulative Epic-2 CR metrics (Stories 2.1 + 2.2 + 2.3 opener → drain closed):** 2.1 = 9 patches across 3 gates (16-iter fix-chain); 2.2 = 2 patches across 1 gate (4-iter fix-chain); 2.3 = 9 patches across 1 gate so far (9-iter drain iter-162..iter-170; re-run next). Story 2.3 drain duration on-forecast per iter-161 envelope (projected 6–9 iter → landed at 9).
