# Implementation Plan

## NOW

- [ ] AR-3 — replace `mv tempfile /etc/resolv.conf` with in-place overwrite (`printf '…' > /etc/resolv.conf`) in `packages/devbox/scripts/start-egress.sh:41-44` so Docker-bind-mounted resolv.conf is writable (works across symlink/bind-mount/regular-file). Detect immutable-flag failure and log clear diagnostic. Drop the `mktemp` + `mv` dance. Budget: ~20K tokens.

## QUEUE (Story 2.3 fix-drain; 7 patches remaining → re-run CR → done)
- [ ] AR-4 — fix `getent` pipeline masking in `packages/devbox/scripts/reload-egress.sh:101,109` — capture raw getent output first (`ipv4_raw="$(getent ahostsv4 "${domain}" 2>/dev/null)" || { log WARN; continue; }; ipv4s="$(printf '%s' "${ipv4_raw}" | awk '{print $1}' | LC_ALL=C sort -u)"`) so real DNS failures reach the WARN branch. Parallel fix for ipv6. Budget: ~15K tokens.
- [ ] AR-5 — replace `pgrep -x dnsmasq` liveness probe in `packages/devbox/scripts/start-egress.sh:106-118` with a positive-serving probe: minimum `[[ -s /run/dnsmasq.pid ]] && kill -0 "$(cat /run/dnsmasq.pid)"`; stronger `getent ahostsv4 <known-whitelisted-domain>` via pinned resolv.conf OR `timeout 1 bash -c '</dev/tcp/127.0.0.1/53'`. Fail hard if probe fails. Budget: ~20K tokens.
- [ ] AR-6 — add marker-validation preflight in `packages/devbox/scripts/reload-egress.sh` before each awk invocation: assert `grep -c 'KEEL_EGRESS_ALLOWLIST_MARKER_START'` equals the expected count per template AND equals the END-marker count; exit 6 with clear diagnostic on imbalance. Prevents silent dnsmasq truncation. Budget: ~20K tokens.
- [ ] AR-7 — introduce per-chain marker names in `packages/devbox/nftables/egress.nft` (`KEEL_EGRESS_V4_MARKER_START`/`_END` inside `output_v4`; `KEEL_EGRESS_V6_MARKER_START`/`_END` inside `output_v6`) + update awk in `packages/devbox/scripts/reload-egress.sh:126-149` to match chain-scoped markers (drop the `chain` state variable). Structural order-independence. Invariant-doc contentHash refresh needed if the rendered `egress.nft` is referenced (it is only linked, not content-hashed — but verify manifest entries before commit). Budget: ~25K tokens.
- [ ] AR-8 — rebuild `cleanup_files` array via explicit loop in `packages/devbox/scripts/reload-egress.sh:189` (`local new=(); for f in "${cleanup_files[@]}"; do [[ "$f" != "${dnsmasq_rendered}" ]] && new+=("$f"); done; cleanup_files=("${new[@]}")`) instead of `${array[@]/pattern}` which blanks the element. Budget: ~10K tokens.
- [ ] AR-9 — replace `$(printf … | sort)` command-substitution in `packages/devbox/scripts/start-egress.sh:63` with `mapfile -t fragments < <(LC_ALL=C find "${WHITELIST_FRAGMENTS_DIR}" -maxdepth 1 -type f -name '*.txt' -print | sort); for fragment in "${fragments[@]}"` so whitespace-containing fragment filenames are preserved. Budget: ~15K tokens.
- [ ] `/bmad-code-review (args: "2")` re-run — verdict `done` (ZERO-PATCH close) OR cycle continues. Precedent: Story 2.1 iter-128..iter-144 3-cycle chain (9 → 5 → 0 AIs); Story 2.2 iter-151..iter-154 single fix-drain + re-run close (2 → 0 AIs). Story 2.3 forecast per iter-161 envelope: 1–2 re-run cycles (9 → likely 1–3 residual → 0 close). Budget: ~60K tokens.
- [ ] _(conditional, post-Story-2.3 done)_ `/bmad-create-story` for Story 2.4 (whitelist CLI: `packages/devbox/scripts/whitelist.sh` user-facing with add/remove/list/sync + per-fork override + diff summary + invocation of `reload-egress.sh`) per § Story Lifecycle Decision Matrix row `done` → auto-advance within-epic. Story 2.4 is infrastructure-CLI class (new class for Epic 2; may or may not inherit ATDD-skip / WAIVED-trace precedents depending on whether Epic 13 has landed a test runner by then).
- [ ] _(conditional, post-Story-2.17 done, ~Epic 2 completion)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge. Epic 2 completion.
- [ ] _(conditional, post-Epic-2 merged)_ § Cross-epic transition → Epic 3 Story 3.1 OR halt EPIC_DONE awaiting merge.

## BLOCKED

_(none — Story 2.3 CR opener landed iter-161 fixes-pending; 9 PATCH fix-drain queue is deterministic.)_

## ATDD Red Phase

_(none — ATDD-skipped at iter-157; no red-phase failures to carry.)_

## DONE (current story 2.3 fix-drain; prune oldest as drain progresses)

- [x] iter-163: **AR-2 fix-drain landed — idempotent `add table inet keel_egress` prepended before `flush table` in `packages/devbox/nftables/egress.nft`.** Single-file edit; 6-line prepend (5-line explanatory comment + `add table inet keel_egress` primitive) immediately BEFORE line 19 `flush table inet keel_egress`. Rationale pinned in the inline comment: `add <object>` is idempotent in nftables (no-op when the object already exists, reverse of typical CLI `create` semantics) so the first-ever `nft -f` invocation from a fresh netns has a valid `flush` target. Without this prepend, the flush errors "No such file or directory" on first-boot, aborts the atomic transaction before any chain is installed, and the fail-closed posture silently degrades to "no egress rules loaded" — the operator-workstation-deferred live smokes never exercised first-boot so this edge escaped iter-158 dev-story. No invariant-doc contentHash refresh needed (verified: zero `egress.nft` contentHash references in `packages/keel-invariants/`; single narrative path-mention in `docs/invariants/devbox-egress.md:29` which is a path reference, not a content digest). Budget: ~10K tokens (well under the ~15K forecast). **Drain trajectory:** 2/9 PATCH landed (AR-1 + AR-2); 7 remain (AR-3..AR-9). Both landed fixes target first-boot boot-sequence edges; AR-3 starts the shell-script-defensive-edge cluster.
- [x] iter-162: **AR-1 fix-drain landed — first-boot resolver chicken-and-egg detour encapsulated in `reload-egress.sh` (option (b) per NOW spec).** Single-file edit; `packages/devbox/scripts/reload-egress.sh` adds `restore_resolv_pin` function + `resolver_bootstrap_active` guard + EXIT-trap integration + dnsmasq-liveness gate BEFORE the resolution loop. Detection: `[[ -s DNSMASQ_PID_FILE ]] && kill -0 $(cat PID_FILE)` — when dnsmasq NOT running (first-boot from start-egress.sh), temporarily repoint `/etc/resolv.conf` at `${UPSTREAM_RESOLVER}` (`${KEEL_DEVBOX_DNS_UPSTREAM:-1.1.1.1}`) for the `getent` resolution phase; when dnsmasq IS running (subsequent reloads invoked by whitelist.sh or operator shell), no-op. Cleanup trap extended (merged into existing `cleanup` EXIT trap) so SC-13 pin restoration runs even on `nft -f` failure, dnsmasq render failure, dnsmasq HUP failure, or any unexpected non-zero exit. `bash -n` exit 0. Budget: ~30K tokens. **Convergent-PATCH-cluster drain** — AR-1 lesson (encapsulate bootstrap in the primitive, not the caller) carries forward to Story 2.4 whitelist.sh.
- [x] iter-161: **`/bmad-code-review (args: "2")` CR opener landed — verdict `fixes-pending`; FR14n Story State `sm-verified → fixes-pending`; 9 PATCH + 9 DEFER + ~9 DISMISS.** Third Epic-2 post-SM CR gate; FIRST infrastructure-security class CR opener. Three-layer Ralph-hosted adversarial fan-out (Blind Hunter 18 + Edge Case Hunter 15 + Acceptance Auditor 0 spec-fidelity violations — AC 5/5 + SC 17/17 SATISFIED) against cumulative Story 2.3 substrate diff. Triaged ~50 raw → 9 PATCH + 9 DEFER + ~9 DISMISS. PATCH cluster dominated by first-boot boot-sequence + shell-script defensive edges the operator-workstation-deferred smokes never exercised. Budget: ~60K tokens.

## Context

- **Phase:** 4-implementation — Epic 2 at 2/17 stories `done` (2.1 iter-144 + 2.2 iter-154) + 1/17 `in-progress` (2.3 iter-161 CR-opener-fixes-pending). Story 2.3 Status `review → in-progress`; sprint-status row flipped; fix-drain begins iter-162.
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.3 live smokes (Task 12.1 – 12.8) operator-workstation deferred; the iter-161 PATCH cluster is dominated by edges those deferred smokes would have caught at bake-time — drain order prioritises AR-1 + AR-2 first as they are container-boot kill-switches.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Story 2.3 Task 1 appended `dnsmasq` + `nftables` to image; re-bake needed at operator-workstation close-out.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; 2.1 + 2.2 done; 2.3 `in-progress` (iter-161 CR opener → fixes-pending); 2.4..2.17 in `backlog`.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2; PR #230 transitions to Open at Story 2.17 completion).
- **Story:** 2.3 — egress-policy-dnsmasq-nftables-fail-closed-ipv4-ipv6-parity-atomic-reload.
- **Story File:** `_bmad-output/implementation-artifacts/2-3-egress-policy-dnsmasq-nftables-fail-closed-ipv4-ipv6-parity-atomic-reload.md` (v1.6 at iter-161; Status `in-progress`; § Review Findings inserted between Tasks/Subtasks and Dev Notes; 9 PATCH action items + 9 DEFER checkmarks).
- **Story State:** `fixes-pending` — CR opener landed iter-161 9 PATCH; iter-162 drained AR-1 (first-boot resolver detour in reload-egress.sh); iter-163 drained AR-2 (idempotent `add table` prepend in egress.nft). 7 PATCHes remain (AR-3 → AR-9). Drain one PATCH per iter per § Story Lifecycle Decision Matrix row `fixes-pending`; when QUEUE empties → re-run `/bmad-code-review (args: "2")` to confirm `done` OR continue cycle.
- **GitHub Issue:** Story 2.3 → #43; Epic 2 → #10.
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **Acceptance Auditor ZERO-drift at CR opener upholds iter-160 SM verdict.** The 9 PATCH findings are all bug-class (boot-sequence + shell-script defensive edges), not spec drift — reflects the Story 2.3 author's adherence to substrate commitments (ACs + SCs) while the operator-workstation-deferred smokes (Task 12.1 – 12.8) left runtime-edge coverage gaps. Iter-161 **validates** iter-160's operator-workstation-defer-acknowledgment framing: substrate is AC-correct, runtime is bake-time-verifiable, CR opener catches edges that would have surfaced at operator-workstation bake.
- **Convergent PATCH signal is the highest-confidence axis for fix-drain.** AR-1 + AR-4 + AR-5 convergent across Blind + Edge Case (all three layers see the first-boot resolver-bootstrap failure from different angles); AR-3 + AR-8 convergent. Drain these first — they're the least likely to be over-triaged. (Self-retraction + subshell-fd lessons from iter-161 promoted to RALPH.md L303/L305; removed from IP.)
- **DinD is fork-time substrate, not transitional.** `INV-devbox-dind-available` — every fork's cc-devbox-equivalent must provide Docker. Story 2.3 live smokes are operator-workstation deferred; SC-7 verbatim + AC-mapped `docker exec` recipes pinned in `docs/invariants/devbox-egress.md § Verification` + `packages/devbox/README.md § Egress policy § Verification` for the operator close-out.
- **Backend B is the reference env at 2026-04-21.** Broad-state-mutation scripts MUST detect backend + refuse destructive ops by default.
- **NFR2 authority unchanged.** Cold ≤ 300s / warm ≤ 30s remains authoritative on M4-Pro native; DinD runs are indicative only.
- **Cumulative Epic-2 CR metrics (Stories 2.1 + 2.2 + 2.3 opener):** 2.1 = 9 patches across 3 gates (16-iter fix-chain); 2.2 = 2 patches across 1 gate (4-iter fix-chain); 2.3 = 9 patches across 1-or-2 gates projected (forecast 6–9 iter fix-chain per iter-159/160 envelope). Story 2.3's 9-PATCH opener is at the upper end of iter-160's 2–6 PATCH forecast — +3 above ceiling, consistent with the +3 backend-B live-smoke-defer overhead materialising as boot-sequence patches rather than spec drift.
