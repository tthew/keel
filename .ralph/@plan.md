# Implementation Plan

## NOW

- [ ] PATCH-14: AC2 + Subtask 8.1 SCP § citation "§ Section 4.2" → "§ 4.1" at story file lines 30 + 155 (FR14n is a PRD amendment in SCP § 4.1; § 4.2 = Architecture amendments which does NOT contain FR14n) — smallest blast radius first; sweep all occurrences via `grep -n 'SCP § Section 4.2\|per SCP § Section 4.2' <file>` per Sweep-completion carry-rule. ~small

## QUEUE (Story 1.21 CR-pass-3 fix-arc → CR-pass-4 → Epic 1 reclose)

- [ ] PATCH-16: Flip 8 stale CR-pass-1 PATCH checkboxes `[ ]` → `[x]` at story file lines 438-445 (PATCH-1 through PATCH-8; content fixes all landed iter-409..419 per CR-pass-2 § Gate matrix line 498 — only PATCH-9 was flipped at iter-419). Audit-trail integrity per Sweep-completion carry-rule.
- [ ] PATCH-17: AC2 verification matrix + Subtask 8.2 evidence-presentation "32 cumulative post-bootstrap ATDD-skips" → "32 cumulative project ATDD-skips (4 post-bootstrap: 1.17, 1.18, 1.20 hybrid, 1.21)" at story file lines 156 + 325 — categorical error (32 is project-cumulative; only 4 are post-bootstrap). Sweep all occurrences via `grep -n 'cumulative.*post-bootstrap.*ATDD-skip\|post-bootstrap.*ATDD-skip.*cited' <file>`.
- [ ] Re-run `/bmad-code-review (args: "2")` — CR-pass-4 to confirm `fixes-pending → done` per FR14n matrix after PATCH-14 + PATCH-16 + PATCH-17 fix-arc complete. Forecast: 0–2 residual per NEW iter-425 carry-rule (CR-pass-(N+1) forecast must NOT extrapolate "0 residual" from CR-pass-N's success rate against the carry-rule(s) known at CR-pass-N — convergence requires 2-consecutive-zero-PATCH CR passes). ~medium
- [ ] Re-close Epic 1 (`epic-1: in-progress → done` after Story 1.21 closes); transition PR #236 Draft→Open final CI gate per § PR Transition & Final CI Gate; on success: write `EPIC_DONE` halt; on next invocation post-merge § Cross-epic transition advances to next available epic OR writes `ALL_EPICS_DONE` if no next epic.

## BLOCKED

_(none)_

## ATDD Red Phase

_(empty — Story 1.21 ATDD-skipped iter-400 via FR14n § ATDD-skip pure ground (a) substrate-verification per IP § ATDD Skip Rationale; no red-phase tests authored.)_

## DONE (current story phase only — pruned at iter-425; older fix-arc detail in RALPH.md signposts)

- [x] iter-425: **Story 1.21 CR-pass-3 LANDED — 18+ findings raised across 3 parallel review layers; triaged into 3 PATCH + 1 DEFER + ~14 DISMISSED.** FR14n `fixes-pending → fixes-pending` (stays — 3 NEW PATCH raised; CR-pass-3 forecast "0 residual" was wrong per NEW carry-rule below). All 6 ACs reconfirmed satisfied at operative-test level (auditor: "AC1 + AC2 + AC3 + AC4 + AC5 + AC6 all satisfied at operative-test level"). Pre-push CI gate at step 0h: api.github.com class 40th cumulative datapoint sub-class (a) endpoint-split (graphql failed twice, REST recovered first try at LADDER step 2 against SHA `3598847`) — both checks PASS for iter-424 PATCH-12 push (node + python = success). Story file gained `### CR-pass-3 (iter-425)` subsection (3 PATCH + 1 DEFER + dismiss list with rationales) + v1.6 Change Log entry. Deferred-work.md gained `## Deferred from: code review of story-1-21 CR-pass-3 (2026-04-26 iter-425)` section with the AC3 grep count drift entry. **3 NEW PATCH raised:** (PATCH-14) SCP § citation "§ Section 4.2" → "§ 4.1" at story file lines 30 + 155 — FR14n is a PRD amendment, lives in SCP § 4.1 (PRD amendments) not § 4.2 (Architecture amendments); operative AC2 evidence at PRD:968 intact, cross-reference auxiliary; (PATCH-16) flip 8 stale CR-pass-1 PATCH checkboxes `[ ]` → `[x]` at lines 438-445 — content fixes all landed iter-409..419 per CR-pass-2 § Gate matrix line 498 ("9 PATCH from CR-pass-1 all landed cleanly across iter-409..419 ... PATCH-9 checkbox flipped"); only PATCH-9 was flipped, leaving 8 stale; (PATCH-17) "32 cumulative post-bootstrap ATDD-skips" → "32 cumulative project ATDD-skips (4 post-bootstrap)" at lines 156 + 325 — categorical error (32 is project-cumulative; only 4 stories are post-bootstrap). **1 NEW DEFER:** AC3 grep count drift "29" → "30" at story-file:128/164/326 + test-debt.md:400 — recursive self-replication caused by CR-pass-1/2 deferred-work appends quoting the cross-link string (deferred-work.md is the 4th match because its DEFER bodies reference `test-debt.md#story-` literally); updating now requires re-updating at every future CR pass — carry-to Epic 4 close-out audit (smarter grep that excludes deferred-work.md OR live-recompute methodology). **NEW carry-rule landed iter-425 (extension of iter-421 sweep-completion carry-rule):** CR-pass-(N+1) forecast must NOT extrapolate "0 residual" from "100% success rate of CR-pass-N PATCH against the carry-rule(s) known at CR-pass-N" — each CR-pass-N may surface NEW finding classes invisible at CR-pass-(N-1); CR-pass-3 surfaced audit-trail-checkbox + cross-reference + categorical-wording finding classes none of which were known at CR-pass-2. Conservative forecast for CR-pass-(N+1) = max(0, |unique-finding-classes-not-yet-surfaced|), ~1–3 PATCH per CR-pass-N≥2 until 2-consecutive-zero-PATCH CR passes confirm convergence. All gates GREEN at step 4: typecheck 16/16 (FULL TURBO ~130ms), lint 16/16 (FULL TURBO ~115ms), keel-invariants 10 files / 52 tests (~800ms; 4-drift baseline UNCHANGED — 0 NEW drift from cosmetic .md edits), pytest 4/4 (~0.20s). Cumulative pre-merge PATCH Story 1.21 lifecycle: **24** projected (was 22 final post-iter-424; +3 CR-pass-3 PATCH findings - 1 PATCH-12 already in iter-424 count = +2 net; lifecycle now: 9 CR-pass-1 + 3 CR-pass-2 + 3 CR-pass-3 = 15 CR PATCH + 7 SM-validate + 2 SM-verify = 24 cumulative). NOW advances to top QUEUE PATCH-14 (smallest blast radius). **Path forward:** PATCH-14 (iter-426) → PATCH-16 (iter-427) → PATCH-17 (iter-428) → CR-pass-4 (iter-429; forecast 0–2 residual) → Epic 1 reclose + PR #236 Draft→Open final CI gate + EPIC_DONE halt → § Cross-epic transition on next invocation post-merge.

- [x] iter-422 + iter-423 + iter-424: PATCH-10 + PATCH-11 + PATCH-12 LANDED (3-PATCH CR-pass-2 fix-arc complete). PATCH-10 iter-422 line 233 ("23"→"24-DEFER" + "30 cross-links"→"27"); PATCH-11 iter-423 line 241 ("~30 cross-link edits"→"27"); PATCH-12 iter-424 line 174 dropped event-count deflection clause. iter-423 surfaced api.github.com 4-rung NO-recovery sub-class (d) 2nd surfacing — fall-through-to-work permitted via ORIGIN-SHA-already-GREEN clause; confirmed intermittent at iter-424 + iter-425 (LADDER step 1 + step 2 cleared on first try). Detail in RALPH.md signposts.

- [x] iter-421: **Story 1.21 CR-pass-2 PASS — 9 findings (3 PATCH + 1 DEFER + 5 DISMISSED).** FR14n `fixes-pending → fixes-pending` (stays — 3 NEW PATCH raised). Auditor: "CR-pass-2 clean. Story 1.21 ready for `fixes-pending → done` flip" + Edge surfaced 2 missed-sweep residuals + 1 deflection clarification. **NEW carry-rule:** Sweep-completion carry-rule (1st datapoint of CR-pass-N sweep-completion class) — when CR-pass-N PATCH targets a count-drift, sweep ALL occurrences via `grep -n` before claiming done; otherwise CR-pass-(N+1) catches residuals. PATCH-10/11/12 are this rule's first surfacing.

- [x] iter-409..420: Story 1.21 CR-pass-1 fix-arc PATCH-1..9 (9 of 9 LANDED) + multi-deferred-push tracker drains. Audit + sweep class CR-pass-1 envelope datapoint 9 PATCH (mid-band of 0–9 forecast). 14 cumulative network-flake deferred-push datapoints across iter-372 SSH-egress class + 28 across api.github.com class. Detail in RALPH.md signposts.

- [x] iter-404: **Story 1.21 `/bmad-code-review (args: "2")` CR-pass-1 — 23 findings → 9 PATCH + 6 DEFER + 8 DISMISSED.** FR14n `sm-verified → fixes-pending`. All 6 ACs reconfirmed satisfied at operative-test level.

- [x] iter-401..403: Story 1.21 dev-story (iter-401 0 PATCH; FR14n `atdd-scaffolded → in-dev → review`) → trace (iter-402 0 PATCH; `in-dev → traced`) → SM-verify (iter-403 2 PATCH; `traced → sm-verified`).

- [x] iter-398..400: Story 1.21 drafted (iter-398) → SM-validated (iter-399 7 PATCH; `drafted → validated`) → ATDD-skipped pure ground (a) substrate-verification (iter-400; `validated → atdd-scaffolded`).

- [x] iter-1: `/bmad-correct-course` on issue #233 — Sprint Change Proposal; Epic 1 REOPENED for Stories 1.17–1.21.

## Context

- **Phase:** 4-implementation — Story 1.21 CR-pass-3 LANDED iter-425 (3 NEW PATCH raised; CR-pass-3-forecast-revision NEW carry-rule landed); CR-pass-3 fix-arc next (PATCH-14 → PATCH-16 → PATCH-17 → CR-pass-4).
- **Runtime:** cc-devbox iteration env. `uv 0.11.7` + `pnpm` available. Python 3.12.3 satisfies `requires-python = ">=3.10"`. github.com / api.github.com network access intermittent (api-egress-flake class — 40 cumulative datapoints at iter-425 step 0h: graphql failed twice (LADDER step 1 + step 3), REST cleared first try (LADDER step 2) against SHA `3598847`; sub-class (a) endpoint-split — recovery from iter-424 fully GREEN); ssh-egress-flake class distinct (17 cumulative datapoints across iter-372..420b).
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (REOPENED for Stories 1.17–1.21).
- **Epic Branch (parent):** `feat/epic-2-packaged-devbox` — this work targets it as the PR base, not `main`.
- **Working Branch:** `chore/correct-course-test-runner-233` (based on `origin/feat/epic-2-packaged-devbox`).
- **Story:** 1.21 — Sweep prior `ATDD deferred` stories into `test-debt:` follow-ups.
- **Story File:** `_bmad-output/implementation-artifacts/1-21-sweep-prior-atdd-deferred-stories-into-test-debt-followups.md`.
- **Story State:** `fixes-pending` per FR14n § Story Lifecycle Decision Matrix `fixes-pending` row. **CR-pass-3 fix-arc OPEN** at iter-425 (PATCH-14 + PATCH-16 + PATCH-17 raised). Per FR14n `fixes-pending` row exit condition: drain QUEUE PATCH items (one per iter), then re-run `/bmad-code-review (args: "2")` CR-pass-4 to confirm `done`. **NOW = PATCH-14 (smallest blast radius first; SCP § citation fix at lines 30 + 155).** **Path:** PATCH-14 (iter-426) → PATCH-16 (iter-427) → PATCH-17 (iter-428) → CR-pass-4 (iter-429; forecast 0–2 residual per NEW iter-425 carry-rule) → Epic 1 reclose + PR #236 Draft→Open final CI gate + EPIC_DONE halt → § Cross-epic transition on next invocation post-merge.
- **GitHub Issue:** Issue #233 ("Course Correction: Bootstrap test runner + CI"). PR #236 (Draft, OPEN, base: `feat/epic-2-packaged-devbox`) carries `Refs #233`; `Closes #233` lands at Story 1.21 close-out.
- **PR:** #236 (Draft, OPEN, base: `feat/epic-2-packaged-devbox`).

## Notes

- **Story 1.21 forecast envelope (post-iter-425 CR-pass-3 fix-arc OPEN):** Cumulative pre-merge PATCH **24 projected** (was 22 final post-iter-424; +3 CR-pass-3 PATCH raised − 1 PATCH-12 already counted at iter-424 = +2 net per cumulative axis). Stage breakdown: 7 SM-validate + 2 SM-verify + 9 CR-pass-1 + 3 CR-pass-2 + 3 CR-pass-3 = 24. Forecast residual: 3 PATCH across iter-426..428 + CR-pass-4 (forecast 0–2 residual per NEW iter-425 carry-rule). Cumulative residual envelope post-iter-425: **24–26 final** (within 13–43 first-datapoint-of-class envelope; tracking toward upper-band as multi-CR-pass class extends).
- **CR-pass-(N+1) forecast carry-rule (iter-425 NEW; extension of iter-421 sweep-completion carry-rule):** CR-pass-(N+1) forecast must NOT extrapolate "0 residual" from "100% success rate of CR-pass-N PATCH against the carry-rule(s) known at CR-pass-N". Each CR-pass-N may surface NEW finding classes invisible at CR-pass-(N-1); CR-pass-3 surfaced 3 NEW finding classes (audit-trail-checkbox + cross-reference + categorical-wording) none of which were known at CR-pass-2. Conservative forecast bound = max(0, |unique-finding-classes-not-yet-surfaced|), ~1–3 PATCH per CR-pass-N≥2 until 2-consecutive-zero-PATCH CR passes confirm convergence. Reference incident: iter-424 forecast "CR-pass-3 expected 0 residual — sweep-completion carry-rule applied at all 3 PATCH" was correct on the sweep-completion axis but blind to off-axis finding classes.
- **Sweep-completion carry-rule (iter-421 NEW; 1st-class precedent applied at iter-422/423/424 with 100% success rate over 3 PATCH on the count-drift axis ONLY):** When CR-pass-N PATCH targets a count-drift, sweep ALL occurrences via `grep -n '<old-value>' <file>` before claiming PATCH-N done. PATCH-10/11/12 all applied this rule cleanly with no missed parallel sites on the count-drift axis. Scope-of-application: count-drift axis only; NOT a general defence against off-axis CR-pass-(N+1) findings. PATCH-14 + PATCH-17 will apply this rule to their respective sweep targets (SCP § citation pattern + categorical wording pattern).
- **Audit + sweep class envelope CALIBRATED at iter-425 CR-pass-3 (6 datapoints):** dev-story 0-PATCH + trace 0-PATCH + SM-verify 2-PATCH + CR-pass-1 9-PATCH + CR-pass-2 3-PATCH + CR-pass-3 3-PATCH (mid-band 0–6). Future audit + sweep stories inherit envelope dev-story 0–3 / trace 0–2 / SM-verify 1–4 / CR-pass-1 0–9 / CR-pass-N≥2 0–4 with NEW iter-425 carry-rule on convergence forecasting.
- **api.github.com network class (40 cumulative datapoints across iter-397..425):** Sub-classes by endpoint affected: (a) endpoint-split graphql-only; (b) joint-endpoint cross-iter (defer push); (c) joint-endpoint within-iter recovery; (d) **4-endpoint within-iter joint-flake NO-recovery** (iter-415 NEW; 2 surfacings iter-415 + iter-423; intermittent confirmed at iter-424 + iter-425 LADDER step 2 first-try clear). LADDER carry-rule: (1) `gh pr checks <PR> --watch --fail-fast`; (2) REST `gh api repos/<owner>/<repo>/commits/<sha>/check-runs`; (3) graphql `gh pr checks <PR>` one-shot; (4) defer push to next iter OR fall-through to work iff ORIGIN SHA was already proven GREEN at a PRIOR iter and no new push has intervened.
- **Course-correction precedent stack 3 deep:** issue #231 (PR #234 MERGED) → issue #232 (PR #235 MERGED) → issue #233 (PR #236 in-flight; pending Story 1.21 close + Draft→Open final CI gate). Issue #233 is the first to REOPEN a closed epic.
- **Epic 4 hard-precondition:** Epic 4 (per-iteration security verification) extends `packages/keel-invariants/` with secret/SAST/prompt-injection scanners. Stories 1.19 + 1.20 backfill IS LANDED + DONE; Epic 4 unblock requires Story 1.21 + Epic 1 reclose.
- **iter-372 SSH-egress flake carry-rule (17 cumulative datapoints across iter-372..420b):** kill-after-hang + retry-fail sub-class. First-retry-resolves carry-rule does NOT always hold.
- **Iter-358 INV-git-hooks-preservation worktree-mode drift carry-rule (carry-forward to Epic 4 hardening):** Story 1.17 + 1.18 + 1.19 + 1.20 + 1.21 disposition (c) carry-forward unchanged. 4-drift sync-gate baseline persists.
