# Implementation Plan

## NOW

- [ ] Run `/bmad-create-story (args: "review")` post-dev SM requirements-satisfaction verification (`traced → sm-verified` or `sm-fixes-pending`). Two-subagent pattern per iter-235 LESSON (AC-verifier `bmad-agent-dev` + drift-detector `bmad-agent-architect`). Forecast per iter-266 NOVEL LESSON novel-runtime-behaviour band + pre-dev SM already at iter-266 absorbed 8 PATCHes (CRITICAL F3 liveness + F4 userland-proxy substantive findings): post-dev SM PATCH band 0-4 likely; novel-surface vectors (compose-override file + background-daemon-in-entrypoint + stateful-first-boot-keygen) already adversarial-triaged at pre-dev SM so post-dev narrows.

## QUEUE (Story 2.12 lifecycle → Stories 2.13..2.17 → Epic 2 close-out)

- [ ] _(after Story 2.12 `sm-verified`)_ `/bmad-code-review (args: "2")` — three-layer adversarial fan-out (`sm-verified → done` or `fixes-pending`). Forecast per iter-266 NOVEL LESSON: Story 2.12's post-landing CR 0-3 first-class PATCH + 2-5 second-class operator-edge DEFERs. SEVENTH one-pass ZERO-PATCH CR precedent candidate if pre-dev SM v1.1 absorption holds + post-dev SM PATCH count stays ≤4.
- [ ] _(Stories 2.13..2.17)_ Iterate through full lifecycle — 5 remaining substrate stories spanning healthcheck (2.13) + legacy branch retention (2.14) + committed settings (2.15) + hooks (2.16) + bypass-resistance (2.17). Each runs full lifecycle; no cross-epic transitions until Epic 2 final story (2.17) completes.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-24 per iter-219..268 repeated pre-push confirmation — `gh pr view 230` returns `statusCheckRollup: []`).

## BLOCKED

- [ ] POST-PUSH SSH :22 CARRY-FORWARD — three unpushed commits at iter-269 close: `12a5ebc` (iter-268 RALPH.md SSH keepalive lesson), `3dcedd3` (iter-268 docs BLOCKED note), `921c21d` (iter-269 trace-gate artefacts). Per iter-263 asymmetric-recovery LESSON, SSH :22 and HTTPS :443 recover independently — all three flush together on first clean SSH :22 window. Non-critical: all commits are docs/journal + trace artefacts only; no impl surface. PR #230 has no CI (statusCheckRollup: []).
  - Attempted iter-269 step 5: two push attempts with increasing SSH ConnectTimeout (15s then 30s) + keepalive `ServerAliveInterval=15` per iter-268 POST-PUSH LESSON both returned explicit "ssh: connect to host github.com port 22: Connection timed out" — network-layer failure to establish TCP on :22, not an in-session stall (the iter-268 keepalive fix targeted mid-session stalls; doesn't help pre-connection TCP timeout).
  - Error/Issue: transient SSH :22 network timeout at TCP handshake layer.
  - Next: iter-270 step 5 push attempts SSH :22 first per iter-263 recovery rule; if clean, all three carry-forward commits auto-flush as part of iter-270's normal push cycle. Do NOT re-write the keepalive lesson in RALPH.md — it's already recorded at iter-268 Signposts; the lesson stands for mid-session stalls, not pre-connection TCP timeouts (a refinement worth noting if the pattern persists but not yet a distinct lesson class).

## ATDD Red Phase

_(none — iter-267 ATDD-skip-with-grounds-(c)+(ii)+(iii) per FR14n; 22nd-cumulative ATDD-skip precedent. Impl-time smokes at iter-268 dev-story cover ACs 1-3 per Story 2.11 iter-257 dev-agent pattern; ACs 4-5 operator-workstation-deferred.)_

## DONE (iter-269 Story 2.12 `/bmad-testarch-trace (args: "yolo")` LANDING — `in-dev → traced`)

- [x] iter-269: **STORY 2.12 TRACE-GATE LANDING — `in-dev → traced` (Ralph internal FR14n state); sprint-row stays `review` across trace-gate window.** Executed `/bmad-testarch-trace (args: "yolo")` per § Story Lifecycle row `in-dev → traced|trace-fixes-pending`. **Outcome: WAIVED** per TWENTY-SECOND cumulative trace-WAIVED precedent + TWENTY-THIRD ATDD-skip-trace-WAIVED pairing. Ground-(a)+(b)+(c) hybrid conjunction applied — (a) substrate-verification covers ACs 1-3 at substrate layer via 5 iter-268 impl-time smokes (sha256 contentHash match; `pnpm --filter @keel/keel-invariants build && pnpm keel-invariants:check` GREEN post rebuild with 32 InvariantSchema entries; `bash -n` clean on 10 modified shells; `resolve_ssh_state` 9/9 normalisation smoke across full `true/TrUe/TRUE/false/yes/1/on/empty/unset/garbage` matrix — strict-true-only per SC-2; `js-yaml` parse + shape assertions on both compose files — base 4 Story-2.2 loopback-bound ports + `environment.KEEL_DEVBOX_SSH = ${KEEL_DEVBOX_SSH:-false}` + override 1 loopback-bound `'127.0.0.1:${KEEL_DEVBOX_SSH_PORT:-2222}:2222'`); (b) no test runner wired at Story 2.12 substrate stage — iter-269 recursive probe under repo root (excluding node_modules / .pnpm-store / _bmad/) for vitest.config.* / jest.config.* / playwright.config.* / cypress.config.* / pyproject.toml / go.mod / Gemfile / Cargo.toml returns zero matches; `tests/` dirs present only under `.claude/skills/bmad-*/scripts/tests/` (BMad skill internals, not application tests); (c) HYBRID variant-(ii) operator-workstation-deferred-AC-completion for ACs 4-5 (live non-loopback LAN peer refusal requires peer machine; live full pubkey flow + restart + rebuild persistence require real SSH client + Docker daemon + named-volume lifecycle). **Alternative partial-waive considered** — IP pre-declared WAIVE-with-grounds (partial) for ACs 4-5 with ACs 1-3 substrate-covered — elected full-waive per 23rd-cumulative-pairing consistency rule + `coverage_basis=acceptance_criteria` mechanical-matrix-absent at substrate stage (Phase-1 matrix JSON schema has no partial-waive vocabulary). **Deliverables (3 trace artefacts committed):** (i) `_bmad-output/test-artifacts/traceability/2-12-loopback-bound-port-publication-opt-in-keel-devbox-ssh-sshd.md` (authoritative trace report + Phase 1 + Phase 2 + YAML snippet + Waiver Details + Next Steps; mirrors Story 2.11 iter-258 template); (ii) `_bmad-output/test-artifacts/traceability/2-12-coverage-matrix.json` (Phase 1 machine-readable with 5 requirements entries per AC; all P2 / NONE / WAIVED); (iii) `_bmad-output/test-artifacts/traceability/2-12-gate-decision.json` (slim gate signal for CI/CD pipeline consumption); (iv) `_bmad-output/test-artifacts/traceability/2-12-e2e-trace-summary.json` (full Phase 2 machine-readable with gate_status=WAIVED). **Source SHA:** `3dcedd3` (pre-trace-commit head; encoded in e2e-trace-summary.json as `source_sha` field). **Story State advance:** FR14n `in-dev → traced`. Sprint-row stays `review` across trace-gate window (no sprint-status mutation; sprint-row flips to `done` only at CR closure per Story 2.11 iter-258 precedent). **Next iter (iter-270):** `/bmad-create-story (args: "review")` post-dev SM per § Story Lifecycle row `traced → sm-verified|sm-fixes-pending`. Two-subagent pattern per iter-235 LESSON. Forecast 0-4 PATCH at post-dev SM (iter-266 pre-dev SM absorbed 8 CRITICAL + narrow PATCHes; post-dev typically narrower). Budget consumed iter-269: ~30-40K tokens (orient ~8K + story-file read ~10K + Story 2.11 template-read ~8K + 3 JSON artefact authoring ~6K + markdown trace report authoring ~8K). Well inside ~117K execution budget; exit cleanly per Guardrail 12. **PUSH STATUS forecast:** SSH :22 may be recovered from iter-268 backlog; `12a5ebc` + `3dcedd3` + iter-269 trace-gate commit flush together on first clean push-window per iter-263 asymmetric-recovery LESSON.

_(iter-253..268 Story 2.10/2.11/2.12 closure iters + Story 2.12 drafting/pre-dev-SM/ATDD/dev-story iters pruned per Guardrail 2.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **11/17 stories done** (2.1-2.11) + 1/17 in-dev-now-traced (2.12) + 5/17 backlog (2.13..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered through iter-269 (iter-263 push recovery, iter-264 three-layer adversarial CR closure ZERO-PATCH, iter-265 Story 2.12 drafting, iter-266 pre-dev SM gate 8-PATCH landing, iter-267 ATDD-skip-with-grounds, iter-268 dev-story recovery-landing, iter-269 trace-gate WAIVED landing).
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:120). Host-side shim count: **18** at iter-269 (unchanged at trace-gate iter — trace is docs-only; 8 shims already thread compose-override via conditional-expansion idiom from iter-268 dev-story).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **11 done** (2.1-2.11); 1 traced-awaiting-SM (2.12); 5 backlog (2.13..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.12 — Loopback-bound port publication + opt-in `KEEL_DEVBOX_SSH` sshd
- **Story File:** `_bmad-output/implementation-artifacts/2-12-loopback-bound-port-publication-opt-in-keel-devbox-ssh-sshd.md`
- **Story State:** `traced` — Story 2.12 trace-gate landing at iter-269 (WAIVED per 22nd-cumulative-precedent; ground-(a)+(b)+(c) hybrid). Next iter transitions `traced → sm-verified|sm-fixes-pending` via `/bmad-create-story (args: "review")` per § Story Lifecycle Decision Matrix.
- **GitHub Issue:** Story 2.12 issue unknown; `RALPH_ISSUE_NUMBER` unset at iter-269 orient. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` per iter-219..267 confirmation; iter-268 + iter-269 `gh pr view` may have timed out on HTTPS :443 but prior iter-232 forwards is authoritative); push is unblocked at any Story State.

## Notes

- **iter-269 trace-gate outcome: WAIVED** per ground-(a)+(b)+(c) hybrid conjunction — 22nd cumulative trace-WAIVED precedent + 23rd cumulative ATDD-skip-trace-WAIVED pairing. Elected full-waive over partial-waive per consistency rule + Phase-1 matrix JSON schema vocabulary-absence; all 5 ACs at NONE / P2 / WAIVED in the machine-readable artefacts. ACs 1-3 substrate-covered via 5 iter-268 impl-time smokes; ACs 4-5 operator-workstation-deferred by AC contract (live SSH + LAN peer + named-volume-lifecycle persistence). Post-landing retrospective: zero trace-fixes-pending surfaces (no missing AC tests to backfill — all 5 ACs are uncovered-by-design at substrate stage; the waiver captures the intent).
- **iter-268 NOVEL LESSON carry-forward** — post-crash recovery pattern for dev-story landing iters (VERIFY+COMMIT, not re-invoke). 5-check verification: (1) contentHash sha256 match, (2) sync-gate post-rebuild green, (3) bash -n modified shells clean, (4) resolver smokes green across full normalisation matrix, (5) YAML shapes parseable + semantically correct. Applies to Stories 2.13..2.17 dev-story landings.
- **iter-267 precedent-match carry-forward** — 22nd-cumulative ATDD-skip-with-grounds-(c)+(ii)+(iii) per FR14n; Story 2.5 iter-186 canonical pattern. Default posture for Stories 2.13..2.17 remains skip-with-grounds-(c)+(ii)+(iii) until Epic 13 lands a wired test runner.
- **iter-266 NOVEL LESSONS carry-forward:** (1) container-loopback ≠ host-loopback under Docker userland-proxy is a pattern TRAP — defence-in-depth `ListenAddress 127.0.0.1` on top of host-side `127.0.0.1:PORT:PORT` publish is ACTIVELY BROKEN; Story 2.12 impl at iter-268 honours this (sshd_config has NO `ListenAddress` line — intentional, documented in template header comment). (2) pre-dev SM PATCH-count CAN break iter-221's ~5 ceiling when adversarial-lens subagents surface runtime substantive bugs; density-amortisation heuristic governs narrow-lens only; budget 6-10 PATCH at pre-dev SM for novel-runtime-behaviour stories.
- **iter-265 NOVEL OBSERVATION carry-forward** — AC 1 already-satisfied pattern: grep current substrate (`packages/devbox/`) for already-implemented surface BEFORE drafting Tasks — iter-148 forward-delivered Story 2.12 AC 1 because Story 2.2's loopback-bound posture was always-on.
- **iter-264 NOVEL LESSON carry-forward** — CR re-run DEFER-vs-PATCH severity-class-first rule: classify by severity class FIRST, convergence-count SECOND. 2-of-3 convergence is high-confidence TRIAGE signal (confirms real finding) but disposition still follows severity class.
- **iter-263 LESSON carry-forward** — SSH :22 / HTTPS :443 asymmetric recovery; push via SSH is primary progress vector even when HTTPS :443 `gh pr view` times out. Future BLOCKED recovery always attempts push first regardless of HTTPS state.
- **iter-261 LESSON carry-forward** — PATCH-surface-well-scoped-compact-iter pattern (~15-20K tokens) when single-source-of-truth + one-liner code + three-site doc lockstep.
- **iter-260 LESSONS carry-forward (REFINED by iter-264):** (1) three-layer adversarial fan-out convergence signal — 2-of-3 overlap is **high-confidence triage signal**, NOT automatic PATCH; severity class + finding nature determine final disposition per iter-264 NOVEL LESSON; (2) Blind Hunter false-positive class #1 (unset-array-access under `set -u` without REQUIRED_VARS fence verification); (3) iter-259 two-subagent post-dev SM pattern distinct from CR three-layer adversarial lens.
- **iter-259 LESSONS carry-forward unchanged:** post-dev SM two-subagent pattern; D2 architecture.md:547 7-site cascade pattern; partial-waive trace-gate vocabulary absence (reaffirmed at iter-269 — Phase-1 JSON schema has no partial-waive field; full-waive is the only mechanically-unambiguous posture for P2-only stories with mixed substrate-covered + operator-workstation-deferred AC mix); manifest rebuild required after new InvariantSchema entry OR contentHash update.
- **iter-257 LESSON reaffirmed at iter-268 + iter-269** — `pnpm keel-invariants:check` runs `dist/check.js`; after editing `src/invariants.manifest.ts` MUST `pnpm --filter @keel/keel-invariants build` BEFORE check or sync-gate returns stale drift. iter-268 ran rebuild first then check — GREEN on first try; iter-269 trace gate re-affirms no-drift on the already-registered INV-devbox-ssh via the trace artefacts' sha256 contentHash citation.
- **iter-244/246 audit-findings carry-forward** unchanged: restart.sh transitive-delegate; benchmark.sh OUT-OF-SCOPE (SC-11) but threads compose-override to preserve sshd-enabled benchmarks; Manifest entry count **32** at iter-269 (unchanged from iter-268 dev-story landing; trace is docs-only). Alpine egress non-applicability doctrine preserved.
- **Substrate-citation drift cumulative forecast for Stories 2.13..2.17:** iter-260 baseline + iter-264 AR-91 + Story 2.12 forecast 2-4 minor drifts likely to surface at post-dev SM gate iter-270 per iter-266 novel-runtime-behaviour pattern. Cumulative Epic 2 forecast: ~28-41 cumulative drifts + 2-3 citation-claim misplacements. SC-17 scope holds: single reconciliation pass at Story 2.17 landing.
