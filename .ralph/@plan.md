# Implementation Plan

## NOW

- [ ] Run `/bmad-testarch-trace (args: "yolo")` — Story 2.12 coverage gate + traceability matrix (`in-dev → traced` or `trace-fixes-pending`). Forecast per iter-256 22nd-cumulative ATDD-skip-trace-WAIVED pairing: WAIVE-with-grounds (partial) for ACs 4-5 (operator-workstation-deferred under DinD backend B — non-loopback peer + live pubkey flow infeasible); ACs 1-3 covered by dev-agent impl-time smokes logged in iter-268 dev-story landing (resolver normalisation 9/9 green across `true`/`TrUe`/`TRUE`/`false`/`yes`/`1`/`on`/empty/`garbage`; `pnpm keel-invariants:check` GREEN post manifest rebuild — 32 InvariantSchema entries; bash -n 10/10 clean; js-yaml parse on both compose files; base-ports 4 Story-2.2 loopback-bound + override-port 1 Story-2.12 loopback-bound verified under keel-invariants-bundled js-yaml loader). Twenty-third cumulative ATDD-skip-trace-WAIVED co-application pairing projected.

## QUEUE (Story 2.12 lifecycle → Stories 2.13..2.17 → Epic 2 close-out)

- [ ] _(after Story 2.12 `traced`)_ `/bmad-create-story (args: "review")` post-dev SM verification (`traced → sm-verified` or `sm-fixes-pending`). Two-subagent pattern per iter-235 LESSON (AC-verifier `bmad-agent-dev` + drift-detector `bmad-agent-architect`).
- [ ] _(after Story 2.12 `sm-verified`)_ `/bmad-code-review (args: "2")` — three-layer adversarial fan-out (`sm-verified → done` or `fixes-pending`). Forecast per iter-266 NOVEL LESSON: novel-runtime-behaviour stories (new daemon + new compose-override path + entrypoint extension) may surface 6-10 PATCH at pre-dev SM (iter-266 broke at 8); CR itself typically lower density post-PATCH-landing. Story 2.12's post-landing CR forecast: 0-3 first-class PATCH + 2-5 second-class operator-edge DEFERs. Precedent: iter-260 Story 2.11 CR 1-PATCH + 16-DEFER.
- [ ] _(Stories 2.13..2.17)_ Iterate through full lifecycle — 5 remaining substrate stories spanning healthcheck (2.13) + legacy branch retention (2.14) + committed settings (2.15) + hooks (2.16) + bypass-resistance (2.17). Each runs full lifecycle; no cross-epic transitions until Epic 2 final story (2.17) completes.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-24 per iter-219..267 repeated pre-push confirmation — `gh pr view 230` returns `statusCheckRollup: []`).

## BLOCKED

_(none — iter-268 is dev-story landing recovery; uncommitted state from prior-iter-268-crash found on-disk and verified coherent; sync-gate + resolver smokes + syntax green; commit + push forthcoming.)_

## ATDD Red Phase

_(none — iter-267 ATDD-skip-with-grounds-(c)+(ii)+(iii) per FR14n; 22nd-cumulative ATDD-skip precedent. Impl-time smokes at iter-268 dev-story cover ACs 1-3 per Story 2.11 iter-257 dev-agent pattern; ACs 4-5 operator-workstation-deferred.)_

## DONE (iter-268 Story 2.12 `/bmad-dev-story` LANDING — `atdd-scaffolded → in-dev`; recovery-path)

- [x] iter-268: **STORY 2.12 DEV-STORY LANDING — `atdd-scaffolded → in-dev` (Ralph internal FR14n state); Status `ready-for-dev → in-progress → review` (outward-facing); sprint-row `ready-for-dev → in-progress → review`.** RECOVERY-PATH iteration: prior iter-268 instance crashed post-edit pre-commit, leaving all 6 Tasks' file edits on-disk uncommitted (20 modified + 3 untracked per `git status`). Current iter-268 verified state coherent, ran all impl-time smokes, and committed without re-invoking `/bmad-dev-story` (which would have been destructive — the skill edits files; re-running against the already-edited tree would conflict). **Work delivered:** **Task 1** (Dockerfile) — `openssh-server` apt package added to L66 layer; `/home/dev/.ssh/` + `/home/dev/.ssh/host_keys/` pre-created with mode 0700 + owner `dev:dev` at L253-256; `packages/devbox/sshd/sshd_config` baked `COPY`→`/etc/ssh/sshd_config` at L263-267 with `chown root:root + chmod 0644`; no `setcap` invocation (port 2222>1024 needs no NET_BIND_SERVICE per Story 2.5 posture). **Task 2** (compose-override + resolver) — NEW `packages/devbox/docker-compose.ssh.yml` single-site port-2222 publish `'127.0.0.1:${KEEL_DEVBOX_SSH_PORT:-2222}:2222'`; NEW `resolve_ssh_state()` function appended to `packages/devbox/scripts/lib/main-repo-resolver.sh` L229-260 exporting `KEEL_DEVBOX_SSH_RESOLVED` + `KEEL_DEVBOX_COMPOSE_FILE_SSH` with strict-true-only normalisation via `tr '[:upper:]' '[:lower:]'` mirroring Story 2.11 idiom at :152; **15 call sites across 8 compose-invoking shims wired** via `${KEEL_DEVBOX_COMPOSE_FILE_SSH:+-f "${KEEL_DEVBOX_COMPOSE_FILE_SSH}"}` conditional-expansion (build 1 site, rebuild 1, start 1, stop 1, status 1, logs 2, clean 2, benchmark 6); all 8 shims source the resolver and call `resolve_ssh_state` after `resolve_mode_specific_state`. **Task 3** (entrypoint) — `entrypoint.sh:132-177` conditional sshd-launch block gated on `"${KEEL_DEVBOX_SSH:-false}" == "true"`; first-boot atomic host-key-gen via scratch-tmpdir → `ssh-keygen -t ed25519` + `ssh-keygen -t rsa -b 4096` → `mv -T` (partial-keypair-safe under mid-keygen kill); idempotent `chmod 0700` on `.ssh/` + `host_keys/` every boot (StrictModes silent-reject defence); `authorized_keys` idempotent touch + `chmod 0600`; `gosu dev /usr/sbin/sshd -D -e` background launch with `SSHD_PID="$!"; sleep 0.5; kill -0 "${SSHD_PID}"` liveness check (non-fatal per SC-10 background-process-lifecycle contract). **Task 4** (docker-compose.yml environment) — `KEEL_DEVBOX_SSH: ${KEEL_DEVBOX_SSH:-false}` propagation to container env (L138-145); Story 2.12 LANDED roadmap line + TODO-removal (L24 + L259). **Task 5** (invariant) — NEW `INV-devbox-ssh` manifest entry at `packages/keel-invariants/src/invariants.manifest.ts:288-297` with contentHash `e1d693cb0ffa0c7c8d6966b8ce311c2e1daf13d83155d8a0b88028d96112a3c2` (verified sha256 match); NEW `docs/invariants/devbox-ssh.md` (223 lines, 9 sections: Loopback-bound port publication, SSH signal, Opt-in sshd contract, No-SSH default contract, Resolver contract, External connection refusal, Operator authorized-keys flow, Named volume relationship, Mode orthogonality, Invariant stability, Cross-references); `INVARIANTS.md` H3 Devbox SSH section + anchor bullet added between § Devbox per-fork vs shared mode and § Gitignored-secret commit-deny; **manifest entry count 31→32 verified via `pnpm --filter @keel/keel-invariants build` + `pnpm keel-invariants:check` GREEN**. **Task 6** (operator docs) — `.envrc.example:40` past-tense update + resolver pointer + compose-override pointer; `packages/devbox/README.md § Opt-in SSH (Story 2.12)` H2 sibling-append; `AGENTS.md § Opt-in SSH (Story 2.12)` H3 sibling-append (below § Per-fork vs shared devbox mode (Story 2.11)). **Impl-time smokes this iter:** (a) `sha256sum docs/invariants/devbox-ssh.md` matches manifest contentHash; (b) `pnpm --filter @keel/keel-invariants build && pnpm keel-invariants:check` GREEN (manifest rebuild MANDATORY per iter-257 LESSON — required BEFORE check for new InvariantSchema entries); (c) `bash -n` clean on 10 modified shell scripts; (d) `resolve_ssh_state` normalisation smoke 9/9 green (`true`/`TrUe`/`TRUE` → resolved `true` + abs-path; `false`/`yes`/`1`/`on`/`<unset>`/`garbage` → resolved `false` + empty); (e) js-yaml parse on both compose files + shape assertions: base.services.devbox.ports = 4 Story-2.2 entries all `127.0.0.1:...` loopback-bound + base.environment.KEEL_DEVBOX_SSH = `${KEEL_DEVBOX_SSH:-false}` + override.services.devbox.ports = 1 entry `'127.0.0.1:${KEEL_DEVBOX_SSH_PORT:-2222}:2222'`. Live-container smokes (`docker compose config` port-form grep + `pgrep sshd` + `docker port` + operator pubkey connect from host-loopback + non-loopback LAN peer refusal + container restart + image rebuild volume persistence) operator-workstation-deferred per Story 2.5 iter-186 posture — DinD backend B iteration env cannot exercise meaningfully. **NOVEL LESSON — post-crash recovery pattern for dev-story landing iters.** When orient finds uncommitted tree matching expected dev-story output AND the ATDD-skip sprint-row precedence (`ready-for-dev → in-progress → review` flip already applied) AND story-file Status HTML comment already records landing rationale, the correct action is VERIFY + COMMIT, NOT re-invoke `/bmad-dev-story`. Re-invocation would edit files a second time — at best no-op, at worst conflict or corruption. Verification checklist: (1) manifest contentHash sha256sum match; (2) sync-gate post-rebuild green; (3) bash -n clean on modified shells; (4) resolver smokes green across full normalisation matrix; (5) YAML shapes parseable + semantically correct. If any check fails, THEN re-invoke with surgical reset. Pattern carry-forward: Story 2.13..2.17 + any future dev-story iter that may crash mid-work must follow this verify-then-commit posture rather than blind-re-invoke. Promoted to § Lessons. **Next iter (iter-269):** `/bmad-testarch-trace (args: "yolo")` per § Story Lifecycle row `in-dev → traced|trace-fixes-pending`. Expected: WAIVE-partial (ACs 1-3 covered by iter-268 impl-time smokes logged above; ACs 4-5 operator-workstation-deferred). Twenty-third cumulative ATDD-skip-trace-WAIVED co-application pairing projected (chain now 22 after Story 2.12 ATDD-skip at iter-267; trace-WAIVE at iter-269 = pair #23). Budget consumed iter-268: ~40-50K tokens (orient ~18K + uncommitted-state audit ~8K + 5 impl-time smokes ~6K + IP rewrite + RALPH.md signpost + commit ~12K). Well inside ~117K execution budget; exit cleanly per Guardrail 12. PUSH STATUS forecast: SSH :22 expected clean per iter-263 recovery rule; HTTPS :443 may still flake but non-blocking (PR #230 no CI).

_(iter-253..267 Story 2.10/2.11/2.12 closure iters + Story 2.12 drafting/pre-dev-SM/ATDD iters pruned per Guardrail 2.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **11/17 stories done** (2.1-2.11) + 1/17 in-dev-awaiting-trace (2.12) + 5/17 backlog (2.13..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered through iter-268 (iter-263 push recovery, iter-264 three-layer adversarial CR closure ZERO-PATCH confirmation, iter-265 Story 2.12 drafting, iter-266 pre-dev SM gate 8-PATCH landing, iter-267 ATDD-skip-with-grounds, iter-268 dev-story recovery-landing).
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:120). Host-side shim count: **18** at iter-268 (unchanged at dev-story-landing iter; 8 shims now thread compose-override via conditional-expansion idiom). Image rebuild forecast: not strictly required at 1.0 since dev-story is source-level only + Renovate-apt-tracked openssh-server pull will land at next image bake cycle.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **11 done** (2.1-2.11); 1 in-dev-awaiting-trace (2.12); 5 backlog (2.13..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.12 — Loopback-bound port publication + opt-in `KEEL_DEVBOX_SSH` sshd
- **Story File:** `_bmad-output/implementation-artifacts/2-12-loopback-bound-port-publication-opt-in-keel-devbox-ssh-sshd.md`
- **Story State:** `in-dev` — Story 2.12 dev-story landing at iter-268 (recovery-path). All 6 Tasks [x]; Status field + sprint-row flipped `ready-for-dev → in-progress → review` per Story 2.11 iter-257 posture. Next iter transitions `in-dev → traced` via `/bmad-testarch-trace (args: "yolo")` per § Story Lifecycle Decision Matrix.
- **GitHub Issue:** Story 2.12 issue unknown; `RALPH_ISSUE_NUMBER` unset at iter-268 orient. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` per iter-219..267 confirmation); push is unblocked at any Story State.

## Notes

- **iter-268 NOVEL LESSON — post-crash recovery pattern for dev-story landing iters (recorded above in § DONE).** When orient finds uncommitted tree matching expected dev-story output + matching ATDD-skip sprint-row precedence + story-file Status HTML comment already records landing rationale, correct action is VERIFY+COMMIT not re-invoke. 5-check verification: (1) contentHash sha256 match, (2) sync-gate post-rebuild green, (3) bash -n modified shells clean, (4) resolver smokes green across full normalisation matrix, (5) YAML shapes parseable + semantically correct. Carry-forward for Stories 2.13..2.17 dev-story landings.
- **iter-267 precedent-match carry-forward** — 22nd-cumulative ATDD-skip-with-grounds-(c)+(ii)+(iii) per FR14n; Story 2.5 iter-186 canonical pattern verbatim (HTML-comment verbose-form + IP § DONE + RALPH.md Signposts; no Change Log entry). Hybrid-class grounds composition from Story 2.11 iter-256 reapplied (ACs 1-3 bash-functional-testable + ACs 4-5 operator-workstation-deferred). Default posture for Stories 2.13..2.17 remains skip-with-grounds-(c)+(ii)+(iii) until Epic 13 lands a wired test runner; per-story reverification but skip is the null hypothesis.
- **iter-266 NOVEL LESSONS carry-forward:** (1) container-loopback ≠ host-loopback under Docker userland-proxy is a pattern TRAP — defence-in-depth `ListenAddress 127.0.0.1` on top of host-side `127.0.0.1:PORT:PORT` publish is ACTIVELY BROKEN; inbound packets arrive on container eth0 regardless of Docker userland-proxy mode; Story 2.12 impl honoured this at iter-268 (sshd_config has NO `ListenAddress` line — intentional, documented in template header comment at `packages/devbox/sshd/sshd_config:3-16`). (2) pre-dev SM PATCH-count CAN break iter-221's ~5 ceiling when adversarial-lens subagents surface runtime substantive bugs; density-amortisation heuristic governs narrow-lens only; runtime substantive bugs are density-independent; budget 6-10 PATCH at pre-dev SM for novel-runtime-behaviour stories.
- **iter-265 NOVEL OBSERVATION carry-forward** — AC 1 already-satisfied pattern: grep current substrate (`packages/devbox/`) for already-implemented surface BEFORE drafting Tasks — iter-148 forward-delivered Story 2.12 AC 1 because Story 2.2's loopback-bound posture was always-on.
- **iter-264 NOVEL LESSON carry-forward** — CR re-run DEFER-vs-PATCH severity-class-first rule: classify by severity class FIRST, convergence-count SECOND. 2-of-3 convergence is high-confidence TRIAGE signal (confirms real finding) but disposition still follows severity class. Second-class drift → DEFER even when convergent; first-class correctness → PATCH even single-layer if AC-blocking.
- **iter-263 LESSON carry-forward** — SSH :22 / HTTPS :443 asymmetric recovery; push via SSH is primary progress vector even when HTTPS :443 `gh pr view` times out. Future BLOCKED recovery always attempts push first regardless of HTTPS state.
- **iter-261 LESSON carry-forward** — PATCH-surface-well-scoped-compact-iter pattern (~15-20K tokens) when single-source-of-truth + one-liner code + three-site doc lockstep.
- **iter-260 LESSONS carry-forward (REFINED by iter-264):** (1) three-layer adversarial fan-out convergence signal — 2-of-3 overlap is **high-confidence triage signal**, NOT automatic PATCH; severity class + finding nature determine final disposition per iter-264 NOVEL LESSON; (2) Blind Hunter false-positive class #1 (unset-array-access under `set -u` without REQUIRED_VARS fence verification); (3) iter-259 two-subagent post-dev SM pattern distinct from CR three-layer adversarial lens.
- **iter-259 LESSONS carry-forward unchanged:** post-dev SM two-subagent pattern; D2 architecture.md:547 7-site cascade pattern; partial-waive trace-gate vocabulary absence; manifest rebuild required after new InvariantSchema entry OR contentHash update.
- **iter-257 LESSON reaffirmed at iter-268** — `pnpm keel-invariants:check` runs `dist/check.js`; after editing `src/invariants.manifest.ts` MUST `pnpm --filter @keel/keel-invariants build` BEFORE check or sync-gate returns stale drift. iter-268 ran rebuild first then check — GREEN on first try, no stale-dist drift surfaced.
- **iter-244/246 audit-findings carry-forward** unchanged: restart.sh transitive-delegate; benchmark.sh OUT-OF-SCOPE (SC-11) but NOW threads compose-override to preserve sshd-enabled benchmarks measuring the opt-in posture (benchmark.sh diff at iter-268 adds resolve_ssh_state sourcing + 6 call-site threading); Manifest entry count **32** post-Story-2.12 dev-story landing (iter-268; 31 pre-landing + 1 `INV-devbox-ssh`). Alpine egress non-applicability doctrine preserved.
- **Substrate-citation drift cumulative forecast for Stories 2.13..2.17:** iter-260 baseline (2 MINOR + 5 SC-17 deferrals for Story 2.11) + iter-264 AR-91 (manifest description single-env-var drift D17) + Story 2.12 forecast 2-4 minor drifts likely to surface at post-dev SM gate iter-270 per iter-266 novel-runtime-behaviour pattern. Cumulative Epic 2 forecast: ~26-39 cumulative drifts + 2-3 citation-claim misplacements. SC-17 scope holds: single reconciliation pass at Story 2.17 landing. Deferred-work.md ~580 lines at iter-264 + Story 2.12 lifecycle iters likely add ~10-30 lines.
