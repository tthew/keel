# Implementation Plan

## NOW

- [ ] `/bmad-create-story (args: "review")` fresh-context per § Story Lifecycle matrix row `traced → sm-verified | sm-fixes-pending` — post-dev SM requirements-satisfaction verification. Two-subagent substrate-citation verification pattern (iter-235 LESSON) applies; substrate-citation drift forecast HIGHER than Story 2.9's 13-drift baseline because Story 2.10 prepend-only edits ADD lines to `restart.sh` + `env-check.sh` (line numbers in v1.0-v1.3 § References may drift). Change Log v1.5 entry. ZERO-PATCH FIFTH cumulative one-pass candidate if pattern holds under novel multi-shim-edit + new-INV + alpine-probe surface. Budget forecast: ~40-50K (orient + 2 parallel Sonnet verify subagents + PATCH drafting + Change Log v1.5 + IP + commit).

## QUEUE (Story 2.10..2.17 → Epic 2 close-out)

- [ ] _(post-Story-2.10-sm-verified)_ `/bmad-code-review (args: "2")` fresh-context per § Story Lifecycle row `sm-verified → done | fixes-pending`. Adversarial triage: three-parallel layers (Blind Hunter / Edge Case Hunter / Acceptance Auditor). Novel surfaces for CR attention: (α) 17-shim refactor coverage completeness — did every shim get the right tier + correct block-swap vs prepend-only posture; (β) `INV-devbox-prereq-check` + `docs/invariants/devbox-prereq-check.md` 8 H2 sections coverage; (γ) alpine-probe image + no-bypass posture adversarial scrutiny; (δ) collateral `INV-prek-prepare-lifecycle` contentHash bump discipline — did the single commit contain both the `package.json` edit + the manifest bump. Forecast 0-3 PATCH opener; FIFTH cumulative one-pass ZERO-PATCH CR precedent candidate.
- [ ] _(post-Story-2.10-done)_ Iterate Stories 2.11..2.17 through full lifecycle — 7 remaining substrate stories spanning shared mode + port-publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-23 per iter-219..249 repeated pre-push confirmation — `gh pr view 230` shows `statusCheckRollup: []`).

## BLOCKED

_(none)_

## ATDD Red Phase

_(none — Story 2.10 ATDD-skip applied at iter-247; stub-docker smokes at iter-248 are ephemeral + torn down inside the same iter per iter-223/230/237 LESSON; no persisted red-phase artefacts.)_

## DONE (iter-249 Story 2.10 trace-WAIVED — Story State `in-dev → traced`; sprint-row `review` unchanged)

- [x] iter-249: **STORY 2.10 TRACE WAIVED via `/bmad-testarch-trace (args: "yolo")` fresh-context — TWENTIETH cumulative trace-WAIVED precedent; TWENTY-FIRST ATDD-skip-trace-WAIVED pairing overall.** Oracle resolved from § Acceptance Criteria (5 P2 ACs; formal_requirements; high confidence; not synthetic; external_pointer_status `not_used`). Deterministic gate would FAIL (overall 0% < 80%; P2 0/5; no test runner wired at substrate) → WAIVED per § Testing Standards three-ground conjunction with NEW MULTI-SERVICE COMPOSITION VARIANT (first Epic 2 application; first non-OAuth ground-(c) Epic 2): (a) substrate-verification covers AC 1-5 at iteration-env-safe layer via iter-248's 9/9 stub-docker smokes recapped inline; (b) no test runner wired (Story 1.16 deferred); (c) ground-(c) variant `external-service-owns-behavior-under-test` with multi-service composition — AC 1 Docker Desktop install-URL + Docker Engine daemon-reachability (Docker Inc. owns install flow) + AC 2/3 Stories 2.8/2.9 upstream OAuth flows already operator-workstation-verified + Story 2.5 `keel_home_dev` named-volume persistence already verified (joint ownership) + AC 4 Story 2.7 attach envelope + Epic 3 in-container TUI (reduces to `sleep infinity` per Story 2.7 SC-2) + AC 5 `INV-ralph-halt-reason-enum` `CI_BLOCKED` consumed-not-modified for Epic 3 Story 3.7 halt-write consumer + `docs/invariants/devbox-prereq-check.md § No-partial-bypass` spec-declared contract. **Four artefacts landed** under `_bmad-output/test-artifacts/traceability/`: (i) `2-10-coverage-matrix.json` (PHASE_1_COMPLETE; 5 P2 ACs; per-AC substrate_evidence arrays averaging ~12 entries each); (ii) `2-10-e2e-trace-summary.json` (gate_status WAIVED; schema_version 1; 4 recommendations); (iii) `2-10-gate-decision.json` (slim schema; p0/p1 MET; overall NOT_MET with WAIVED rationale text); (iv) `2-10-prerequisite-check-docker-runtime-claude-auth-gh-auth-with-pointer-errors.md` (frontmatter + PHASE 1 detailed mapping + PHASE 2 gate decision + Recommendations + Display Summary; ~180 substantive lines). Story file Status HTML comment flipped `Story State = in-dev → traced` inline + Change Log v1.4 prepended. **Sprint-status UNCHANGED per iter-202 precedent** (trace gate does NOT flip sprint-row; only Change Log records). RALPH.md signpost landed with zero novel LESSON (template inheritance worked as forecast; novel iter-248 discipline — collateral `INV-prek-prepare-lifecycle` contentHash bump + stub-docker 9-smoke harness generalization — already captured by iter-248 signpost). Budget iter-249: ~40K (orient ~8K; Story 2.9 template + Story 2.10 story-file read ~10K; 4 artefact writes ~15K; story file v1.4 prepend + IP update + RALPH.md signpost + commit ~7K).

_(iter-243..248 Story 2.9 closure + Story 2.10 lifecycle iters pruned per Guardrail 2 — see Story 2.10 § Change Log v1.0–v1.4 + RALPH.md Signposts for per-gate detail.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **9/17 stories done** (2.1-2.9) + 8/17 in-flight-or-backlog (2.10 `Story State = traced` sprint-row `review` at iter-249; 2.11..2.17 still backlog).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered at iter-248 9/9 stub-docker harness (workspace-based, not tmpfs).
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:119). Host-side shim count: **18** at Story 2.10 landing (17 Story 2.9 landing + 1 new `prereq-check.sh` primitive — not a `<verb>-host.sh`).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **9 done** (2.1-2.9); 1 at sprint-row `review` Story State `traced` (2.10); 7 backlog (2.11..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.10 — Prerequisite check (Docker runtime + Claude auth + gh auth) with pointer errors.
- **Story File:** `_bmad-output/implementation-artifacts/2-10-prerequisite-check-docker-runtime-claude-auth-gh-auth-with-pointer-errors.md`
- **Story State:** `traced` — next iter action per § Story Lifecycle Decision Matrix row `traced → sm-verified | sm-fixes-pending` is `/bmad-create-story (args: "review")` fresh-context post-dev SM requirements-satisfaction verification. ZERO-PATCH FIFTH cumulative one-pass CR-precedent candidate forecast if post-dev SM pattern holds.
- **GitHub Issue:** Story 2.10 likely unwired (`RALPH_ISSUE_NUMBER` unset at iter-249 check; skip `Refs #<story-issue>` in commits). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2; `statusCheckRollup: []` + `mergeStateStatus: CLEAN` at iter-247 pre-push confirmation; `isDraft: true`; iter-248/249 did not re-query due to orthogonality but prior state confirmed).

## Notes

- **iter-249 Story 2.10 trace-WAIVED — mechanical template inheritance from Story 2.9 iter-242 at ~85% (lower than Stories 2.8/2.9 90% inheritance due to Story 2.10 wider novel-surface vectors).** Zero novel RALPH.md LESSON generated at trace gate — template inheritance worked as forecast; the novel observations (collateral `INV-prek-prepare-lifecycle` contentHash discipline + stub-docker 9-smoke harness generalization to Epic-2 tier-dispatch primitives) were already captured by iter-248 RALPH.md signpost. FR14n row `in-dev → traced` transition via `/bmad-testarch-trace (args: "yolo")` fresh-context. 4 artefacts landed. Sprint-status unchanged per iter-202 precedent.
- **iter-248 Story 2.10 impl — mechanical inheritance from Stories 2.6/2.8/2.9; zero drift from v1.2 spec.** Two novel LESSONs captured in RALPH.md signposts: (a) collateral `INV-prek-prepare-lifecycle` contentHash bump discipline on root `package.json` edits (sync-gate fails on content-hash-mismatch if the manifest entry is not bumped in the same commit — Story 1.9 whole-file contentHash semantic); (b) stub-docker 9-smoke harness pattern generalises to any Epic-2 tier-dispatch primitive (Stories 2.11..2.17 candidates). Both LESSONs forecast-actionable for the next 7 stories on the Epic 2 branch.
- **iter-244/246 audit-findings carry-forward to iter-250+ post-dev SM + CR gates** (per v1.1 PATCH notes; unchanged from iter-247/248/249): (1) restart.sh transitive-delegate prepend-only pattern (documented inline); (2) benchmark.sh OUT-OF-SCOPE clarification (SC-11); (3) Manifest entry count drift (29→30 baseline); (4) Alpine egress non-applicability doctrine (SC-4 + § Alpine probe image).
- **iter-235 carry-forward — two-subagent verification pattern** re-applies at iter-250 post-dev SM gate; potentially higher drift baseline than Story 2.9's 13-drift because restart.sh + env-check.sh prepend-only edits ADD lines shifting downstream line numbers.
- **iter-234 carry-forward — killed-before-commit skill recovery pattern** unchanged; valid recovery primitive.
- **iter-221 carry-forward — pre-dev SM `~5-PATCH` ceiling** held at iter-246 (4 PATCHes at low end of 2-5 forecast band). Forecast holds for Stories 2.11..2.17.
- **TTY-detect at docker-exec shim (iter-214 LESSON) — Story 2.10 NON-APPLICATION confirmed at iter-248:** prereq-check.sh does NOT `docker exec` — it `docker run --rm` (non-interactive, no TTY, short-lived); SC-11 wire-ins preserved each shim's TTY posture unchanged.
- **`<verb>-host.sh` naming convention DEVIATION (iter-199 EXTENDED through iter-249):** Story 2.10 shipped `prereq-check.sh` per Story 2.3/2.4 primitive pattern (shared primitive called by other shims, not a pnpm-verb wrapper). `pnpm devbox:prereq:check` standalone verb IS wired (Task 2).
