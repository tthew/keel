# Implementation Plan

## NOW

- [ ] Re-run `/bmad-code-review (args: "2")` — CR-pass-5 to confirm `fixes-pending → done` per FR14n matrix after CR-pass-4 fix-arc complete (PATCH-18 + PATCH-19 both LANDED). Forecast 0–2 residual per iter-425 carry-rule extended (CR-pass-4 produced 2 PATCH at upper-band of 0–2 forecast — first time bounded-forecast envelope held; CR-pass-5 forecast 0–2 residual; full convergence requires 2-consecutive-zero-PATCH CR passes — CR-pass-4's 2-PATCH outcome resets the convergence counter, so earliest convergence is CR-pass-5 + CR-pass-6 both 0-PATCH OR CR-pass-5 with 0 finding closes `done` directly per FR14n matrix `fixes-pending` row exit condition). ~medium

## QUEUE (Story 1.21 CR-pass-5 → Epic 1 reclose)

- [ ] Re-close Epic 1 (`epic-1: in-progress → done` after Story 1.21 closes); transition PR #236 Draft→Open final CI gate per § PR Transition & Final CI Gate; on success: write `EPIC_DONE` halt; on next invocation post-merge § Cross-epic transition advances to next available epic OR writes `ALL_EPICS_DONE` if no next epic.

## BLOCKED

_(none)_

## ATDD Red Phase

_(empty — Story 1.21 ATDD-skipped iter-400 via FR14n § ATDD-skip pure ground (a) substrate-verification per IP § ATDD Skip Rationale; no red-phase tests authored.)_

## DONE (current story phase only — pruned at iter-433; older fix-arc detail in RALPH.md signposts)

- [x] iter-433: **PATCH-19 LANDED — line 513 `(matches forecast-table line 240 "32nd cumulative project ATDD-skip")` → `(matches line 234 "32nd cumulative project ATDD-skip")` + Change Log v1.12 entry + PATCH-19 self-flip at line 551.** FR14n `fixes-pending` UNCHANGED at content-state (0 PATCH still pending; CR-pass-4 fix-arc CONTENT-COMPLETE; next gate = CR-pass-5 re-run to confirm `fixes-pending → done`). Step 0h CI status: `gh pr checks 236` first-try clean — node pass 1m5s + python pass 10s (run 24959658664; HEAD SHA `da01035` iter-431 push CI is the most recent CI run; iter-432 push deferred per SSH-egress flake; this iter the 3 unpushed commits `b3aa46a` + `b6fd852` + `e07239a` carry forward to step 5 push). Operative fix: re-grep `'forecast-table line 240'` returned 4 matches before fix (line 513 operative + lines 551 PATCH-19 spec body + 591 v1.10 Change Log + 592 v1.11 Change Log all narrative-quoted/historical preservation sites per repo convention — touching 551 would self-corrupt the PATCH-19 spec into nonsense `"line 234" → "line 234"` becoming a no-op identity quote, and 591/592 are historical Change Log entries violating immutability if touched); after fix, grep returns 3 narrative/historical matches preserved. Re-grep `'32nd cumulative project ATDD-skip'` ground-truthed line 234 (forecast-table ATDD-scaffold row) as the carrying line; line 240 is the trace row (NOT the cell named in PATCH-17 cross-reference). PATCH-19 checkbox at line 551 flipped `[ ]` → `[x]` per iter-427 PATCH-land-AND-self-flip pattern (note: file was 592 lines at fix-time, NOT IP-claimed line 583 which referenced pre-iter-432 file state — iter-428 14th-class carry-rule applied: trust grep result over IP/spec line-ref). Change Log v1.12 entry appended cleanly at file tail (chronological after v1.11; no awk swap required). **Sweep-completion carry-rule outcome (iter-421 + iter-427/428 reconciliation extension):** the IP claimed 1 match (PATCH-17 narrative) — re-running at fix-time confirmed 4 matches and narrowed operative ground-truth to 1 (line 513) per IP-planner-grep-pattern reconciliation carry-rule; this is an IP-undercount (3 narrative-quoted/historical sites missed by IP-planner) — extension data point for iter-427/428 13th + 14th-class carry-rules. **NEW finding wrt iter-432 sub-class 15a carry-rule (further extended at iter-433):** PATCH-19's narrative-quoted preservation footprint (3 sites: PATCH-19 spec body line 551 + v1.10 Change Log line 591 + v1.11 Change Log line 592) is the largest yet for any single-PATCH operative target in this fix-arc — confirms accreting Change Log entries grow narrative-quoted preservation footprints per subsequent PATCH; reconciliation at fix-time via disambiguating-context Edit (prefix `categorical error: 32 is project-cumulative ATDD-skip count` + suffix `, but the post-bootstrap-only subset`) was required to scope the Edit to line 513 alone. All gates GREEN at step 4: typecheck 16/16 (FULL TURBO 195ms), lint 16/16 (FULL TURBO 131ms), keel-invariants 10 files / 52 tests (915ms; 4-drift baseline UNCHANGED — 0 NEW drift from cosmetic .md edits), pytest 4/4 (0.24s). Cumulative pre-merge PATCH Story 1.21 lifecycle: **26** projected UNCHANGED (PATCH-19 was already counted at iter-430 +2 CR-pass-4 raise; landing it does not change projection). NOW advances to top QUEUE CR-pass-5 (forecast 0–2 residual per iter-425 carry-rule extended). **Path forward:** CR-pass-5 (iter-434) → if 0 PATCH: Epic 1 reclose + PR #236 Draft→Open final CI gate + EPIC_DONE halt → § Cross-epic transition on next invocation post-merge; if 1+ PATCH: continue fix-arc per FR14n matrix.

- [x] iter-432: **PATCH-18 LANDED at commit `b3aa46a` — 2 sites period→hyphen at story-file lines 80 + 89 + Change Log v1.11 + PATCH-18 self-flip at line 549. Push FAILED at step 5 (SSH-egress flake — 19th cumulative datapoint of iter-372 class, 2 consecutive within-iter `Connection timed out`); local commits `b3aa46a` + `b6fd852` + `e07239a` UNPUSHED, carry to iter-433.** FR14n `fixes-pending` UNCHANGED (1 PATCH still pending: PATCH-19). **NEW finding wrt iter-428 14th-class carry-rule (sub-class 15a):** `replace_all` of bare `### Story 1.X` would catch all 4 matches including narrative-quoted preservation sites — distinct Edit calls keyed on disambiguating context required.

- [x] iter-431: **CI monitoring PASS — PR #236 SHA `cba5e3e` (iter-430 push) CI cleared.** node pass 1m13s + python pass 13s (run 24959581036). NOW reverted to PATCH-18 LAND (top of QUEUE).

- [x] iter-430: **CR-pass-4 LANDED — 2 PATCH (PATCH-18 + PATCH-19) + 1 DEFER + ~12 DISMISSED across 3 parallel review layers.** FR14n `fixes-pending → fixes-pending`. Cumulative pre-merge PATCH Story 1.21 lifecycle: **26** projected (was 24; +2 CR-pass-4 PATCH findings). **CR-pass-4 forecast outcome (2 PATCH at upper-band of 0–2 forecast):** envelope held; iter-425 carry-rule confirmed predictive power for 3rd consecutive CR pass.

- [x] iter-427..429: PATCH-14 + PATCH-16 + PATCH-17 LANDED (3-PATCH CR-pass-3 fix-arc complete). 13th-class (iter-427) + 14th-class (iter-428) IP-planner sweep-count vs operative-grep + operative-grep-pattern-vs-target-set reconciliation carry-rules. Detail in RALPH.md signposts.

- [x] iter-426: **CI monitoring iteration — PR #236 SHA `8aa3e4e` (iter-425 CR-pass-3 push) CI cleared at iter-426 step 0h.** node + python both PASS first-try at LADDER step 1.

- [x] iter-425: **Story 1.21 CR-pass-3 LANDED — 18+ findings raised across 3 parallel review layers; triaged into 3 PATCH (PATCH-14/16/17) + 1 DEFER + ~14 DISMISSED.** **NEW carry-rule:** CR-pass-(N+1) forecast must NOT extrapolate "0 residual" from CR-pass-N's success rate; conservative forecast bound = max(0, |unique-finding-classes-not-yet-surfaced|), ~1–3 PATCH per CR-pass-N≥2 until 2-consecutive-zero-PATCH CR passes confirm convergence.

- [x] iter-422..424: PATCH-10 + PATCH-11 + PATCH-12 LANDED (3-PATCH CR-pass-2 fix-arc complete). Detail in RALPH.md signposts.

- [x] iter-421: **Story 1.21 CR-pass-2 PASS — 9 findings (3 PATCH + 1 DEFER + 5 DISMISSED).** **NEW carry-rule:** Sweep-completion carry-rule.

- [x] iter-409..420: Story 1.21 CR-pass-1 fix-arc PATCH-1..9 (9 of 9 LANDED). Detail in RALPH.md signposts.

- [x] iter-404: **Story 1.21 `/bmad-code-review (args: "2")` CR-pass-1 — 23 findings → 9 PATCH + 6 DEFER + 8 DISMISSED.** FR14n `sm-verified → fixes-pending`.

- [x] iter-398..403: Story 1.21 drafted → SM-validated (7 PATCH) → ATDD-skipped pure ground (a) → dev-story (0 PATCH) → trace (0 PATCH) → SM-verify (2 PATCH).

- [x] iter-1: `/bmad-correct-course` on issue #233 — Sprint Change Proposal; Epic 1 REOPENED for Stories 1.17–1.21.

## Context

- **Phase:** 4-implementation — CR-pass-4 fix-arc CONTENT-COMPLETE at iter-433 (PATCH-18 LANDED iter-432 + PATCH-19 LANDED iter-433); FR14n `fixes-pending` UNCHANGED at content-state pending CR-pass-5 confirmation. **iter-433: PATCH-19 LANDED — line 513 in-line cross-reference fix `forecast-table line 240` → `line 234` + Change Log v1.12 + self-flip at line 551. NOW = CR-pass-5 for iter-434.** Path: CR-pass-5 (iter-434) → Epic 1 reclose.
- **Runtime:** cc-devbox iteration env. `uv 0.11.7` + `pnpm` available. Python 3.12.3 satisfies `requires-python = ">=3.10"`. github.com / api.github.com network access intermittent (api-egress-flake class — **45 cumulative datapoints UNCHANGED at iter-433** step 0h: `gh pr checks 236` first-try clean; REST recovery-streak extended). ssh-egress-flake class **20 cumulative datapoints UNCHANGED at iter-433** (no push attempted yet at iter-433 IP-update time; will update if step 5 push fails — 3 unpushed commits `b3aa46a` + `b6fd852` + `e07239a` carry from iter-432 + new PATCH-19 commit + this IP-update commit pending).
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (REOPENED for Stories 1.17–1.21).
- **Epic Branch (parent):** `feat/epic-2-packaged-devbox` — this work targets it as the PR base, not `main`.
- **Working Branch:** `chore/correct-course-test-runner-233` (based on `origin/feat/epic-2-packaged-devbox`).
- **Story:** 1.21 — Sweep prior `ATDD deferred` stories into `test-debt:` follow-ups.
- **Story File:** `_bmad-output/implementation-artifacts/1-21-sweep-prior-atdd-deferred-stories-into-test-debt-followups.md`.
- **Story State:** `fixes-pending` per FR14n § Story Lifecycle Decision Matrix. **CR-pass-4 fix-arc CONTENT-COMPLETE** at iter-433 (PATCH-18 LANDED iter-432 + PATCH-19 LANDED iter-433); both checkboxes flipped + Change Log v1.11 + v1.12 entries appended. Per FR14n `fixes-pending` row exit condition: NOW = CR-pass-5 re-run (confirms `fixes-pending → done` if 0 PATCH OR continues fix-arc if 1+ PATCH). **Path:** CR-pass-5 (iter-434) → if 0 PATCH: Epic 1 reclose + PR #236 Draft→Open final CI gate + EPIC_DONE halt → § Cross-epic transition on next invocation post-merge; if 1+ PATCH: continue fix-arc per FR14n matrix.
- **GitHub Issue:** Issue #233 ("Course Correction: Bootstrap test runner + CI"). PR #236 (Draft, OPEN, base: `feat/epic-2-packaged-devbox`) carries `Refs #233`; `Closes #233` lands at Story 1.21 close-out.
- **PR:** #236 (Draft, OPEN, base: `feat/epic-2-packaged-devbox`).

## Notes

- **Story 1.21 forecast envelope (post-iter-433 CR-pass-4 fix-arc CONTENT-COMPLETE):** Cumulative pre-merge PATCH **26** projected UNCHANGED. Stage breakdown: 7 SM-validate + 2 SM-verify + 9 CR-pass-1 + 3 CR-pass-2 + 3 CR-pass-3 + 2 CR-pass-4 = 26. Forecast residual: CR-pass-5 (forecast 0–2 residual per iter-425 carry-rule extended). Cumulative residual envelope post-iter-433: **26–28 final** (still within 13–43 first-datapoint-of-class envelope; tracking toward upper-band as multi-CR-pass class extends).
- **NEW carry-rule iter-428 (14th class — IP-planner operative-grep-pattern-vs-target-set reconciliation):** when IP-planner sweep specs include BOTH an operative grep pattern AND a line range, BOTH must be reconciled at fix-time. If the grep returns wrong-count, fall back to the line range from the PATCH spec text as ground-truth. **iter-433 extension:** trust grep result over IP/spec line-ref when file has accreted lines from prior Change Log entries (iter-433 PATCH-19: IP claimed line 583, actual file was 592 lines at fix-time, PATCH-19 spec line was 551 — file gained ~30 lines from iter-432 v1.11 entry; grep authoritative).
- **NEW carry-rule iter-427 (13th class — IP-planner sweep-count vs operative-grep reconciliation):** when prior-iter IP claims K sweep occurrences AND specifies the operative grep pattern, re-run the operative grep AT FIX-TIME and trust it as source-of-truth over IP's claimed count. **iter-433 IP-undercount extension:** IP may UNDERCOUNT operative target by missing narrative-quoted/historical preservation sites; re-run grep at fix-time AND apply disambiguating-context Edit per iter-432 sub-class 15a carry-rule.
- **CR-pass-(N+1) forecast carry-rule (iter-425; extension of iter-421 sweep-completion):** CR-pass-(N+1) forecast must NOT extrapolate "0 residual" from "100% success rate of CR-pass-N PATCH against the carry-rule(s) known at CR-pass-N" — each CR-pass-N may surface NEW finding classes invisible at CR-pass-(N-1). Conservative forecast bound = max(0, |unique-finding-classes-not-yet-surfaced|), ~1–3 PATCH per CR-pass-N≥2 until 2-consecutive-zero-PATCH CR passes confirm convergence.
- **Sweep-completion carry-rule (iter-421; precedent applied at iter-422/423/424 + iter-429 + iter-432 + iter-433):** When CR-pass-N PATCH targets a count-drift OR cross-reference-drift, sweep ALL occurrences via `grep -n '<old-value>' <file>` before claiming PATCH-N done. **iter-433 PATCH-19 extension:** carry-rule applies to in-line cross-reference correction class (line-ref drift); same sweep verb stays valid — preserve narrative-quoted (PATCH spec body) + historical (Change Log entries) sites while applying disambiguating-context Edit to operative target.
- **iter-432 sub-class 15a carry-rule (extends iter-428 14th-class):** `replace_all` is unsafe when the target substring also appears verbatim inside narrative quotation OR historical Change Log entries. **iter-433 extension:** PATCH-19's 3-site narrative-quoted preservation footprint (PATCH-19 spec body + 2 Change Log entries) is the largest yet for any single-PATCH operative target in this fix-arc — confirms accreting Change Log entries grow narrative-quoted preservation footprints per subsequent PATCH.
- **Audit + sweep class envelope CALIBRATED at iter-425 CR-pass-3 (6 datapoints):** dev-story 0-PATCH + trace 0-PATCH + SM-verify 2-PATCH + CR-pass-1 9-PATCH + CR-pass-2 3-PATCH + CR-pass-3 3-PATCH (mid-band 0–6). Future audit + sweep stories inherit envelope dev-story 0–3 / trace 0–2 / SM-verify 1–4 / CR-pass-1 0–9 / CR-pass-N≥2 0–4.
- **api.github.com network class (45 cumulative datapoints across iter-397..432; UNCHANGED at iter-433):** Sub-classes: (a) endpoint-split graphql-only; (b) joint-endpoint cross-iter; (c) joint-endpoint within-iter recovery; (d) 4-endpoint within-iter joint-flake NO-recovery. LADDER carry-rule: (1) `gh pr checks <PR> --watch --fail-fast`; (2) REST `gh api repos/<owner>/<repo>/commits/<sha>/check-runs`; (3) graphql `gh pr checks <PR>` one-shot; (4) defer push to next iter OR fall-through to work iff ORIGIN SHA was already proven GREEN at a PRIOR iter and no new push has intervened.
- **Course-correction precedent stack 3 deep:** issue #231 (PR #234 MERGED) → issue #232 (PR #235 MERGED) → issue #233 (PR #236 in-flight; pending Story 1.21 close + Draft→Open final CI gate). Issue #233 is the first to REOPEN a closed epic.
- **Epic 4 hard-precondition:** Epic 4 (per-iteration security verification) extends `packages/keel-invariants/` with secret/SAST/prompt-injection scanners. Stories 1.19 + 1.20 backfill IS LANDED + DONE; Epic 4 unblock requires Story 1.21 + Epic 1 reclose.
- **iter-372 SSH-egress flake carry-rule (20 cumulative datapoints across iter-372..432; UNCHANGED at iter-433 IP-update):** kill-after-hang + retry-fail sub-class. First-retry-resolves carry-rule does NOT always hold (iter-432 20th datapoint = 2 consecutive within-iter `Connection timed out`). **Will update post-step-5 if push at iter-433 fails.**
- **Iter-358 INV-git-hooks-preservation worktree-mode drift carry-rule (carry-forward to Epic 4 hardening):** Story 1.17 + 1.18 + 1.19 + 1.20 + 1.21 disposition (c) carry-forward unchanged. 4-drift sync-gate baseline persists.
