# Implementation Plan

## NOW

- [ ] `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-9-gh-cli-oauth-via-pnpm-gh-auth.md")` — Story 2.9 implementation per § Story Lifecycle Decision Matrix row `atdd-scaffolded → in-dev`. Fresh context window. 6 tasks + 30+ subtasks in v1.2 story spec (unchanged by iter-236 ATDD-skip; only § Change Log v1.2 appended). Inherits zero-drift forecast from Story 2.7 iter-223 + Story 2.8 iter-230 precedent chain (≥3 pre-dev SM PATCHes → zero-drift impl). Story 2.9 had 2 pre-dev SM PATCHes (below the 3-PATCH threshold) — watch for minor drift surfacing at post-dev SM gate (per iter-230 LESSON carry-forward). Expected: `gh-auth-host.sh` substrate-quality shim + root `package.json` wires `"gh:auth"`, raising host-side shim count to 17 at Story 2.9 landing. Budget forecast ~60K (6-task impl + 4 smoke runs per Task 5).

## QUEUE (Story 2.9 lifecycle → Stories 2.10..2.17 → Epic 2 close-out)

- [ ] _(post-Story-2.9-validated → done; Story 2.9 lifecycle)_ `atdd-scaffolded → in-dev → traced → sm-verified → done` per § Story Lifecycle Decision Matrix; FOURTH one-pass ZERO-PATCH CR precedent candidate (chain: 1.13 + 2.7 + 2.8 + 2.9 projected) assuming the Story 2.9 draft's high-composition design holds through SM + CR gates.
- [ ] _(post-Story-2.9-done)_ Iterate Stories 2.10..2.17 through full lifecycle — 8 remaining substrate stories spanning prereq-check + shared mode + port-publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-22 per iter-219..234 repeated pre-push confirmation — `gh pr view 230` shows `statusCheckRollup: []`).

## BLOCKED

_(none)_

## ATDD Red Phase

_(none — Story 2.9 FR14n ATDD-skip applied at iter-236; NINETEENTH cumulative precedent under inherited ground-(c) "external-service-owns-behavior-under-test" variant. No red-phase scaffolds produced; substrate preflight Step 1.2 test-framework-configured requirement fails (0 vitest/jest/playwright/cypress configs across repo per `find . -maxdepth 5 …` at 2026-04-22). Three-ground conjunction satisfies skip per story § Testing Standards; story file § Change Log v1.2 documents the skip; next iter queues `/bmad-dev-story`.)_

## DONE (iter-236 Story 2.9 ATDD-SKIPPED — `validated → atdd-scaffolded`; NINETEENTH cumulative precedent; sprint-status unchanged; v1.2)

- [x] iter-236: **STORY 2.9 FR14n ATDD-SKIP APPLIED — Story State `validated → atdd-scaffolded`; NINETEENTH cumulative ATDD-skip precedent (Stories 1.7-1.16 + 2.1-2.8 → 2.9); sprint-status unchanged per iter-222 + iter-229 precedent (ATDD-skip is IP-lifecycle state, not sprint-row state).** `/bmad-testarch-atdd` preflight Step 1.2 hard-prerequisite "test framework configured" FAILS (verified via `find . -maxdepth 5 -type f \( -name 'vitest.config.*' -o -name 'jest.config.*' -o -name 'playwright.config.*' -o -name 'cypress.config.*' \) | grep -v node_modules` → 0 matches); skill would HALT per step-01 fail-closed posture → skip is only autonomous path per Guardrail 3. Three-ground conjunction per story § Testing Standards: (a) substrate-verification covers AC 1 invocation envelope via Task 5 iteration-env-safe stub-docker smokes (Smokes 1/2/3a/3b/3c/3d/4 — syntax parse + pnpm wiring + pre-flight exit-8/9 + final-exec assembly capturing `docker exec -it --user dev -w /workspace keel-devbox gh auth login` + args-passthrough `--web` + no-TTY-gate grep); (b) no test runner wired at substrate (Story 1.16 scope); (c) ground-(c) variant "external-service-owns-behavior-under-test" inherited verbatim from Story 2.8 with 1:1 substitution — AC 1 (OAuth URL surface) + AC 2 (token write at `/home/dev/.config/gh/`) + AC 4 (re-auth pointer) behaviors owned by apt-installed upstream `gh` CLI (Dockerfile:123-136; Renovate `apt` manager-tracked) + GitHub's OAuth endpoint (external service; egress whitelist covers 7 `github.txt` domains per Story 2.3/2.4); AC 3 (token reuse across restart) behavior owned by Docker named-volume persistence (Story 2.5 `keel_home_dev` substrate); AC 4 NEW-element "Ralph pre-push gate halt-able" = Epic-3-scope (Story 3.7), consumes `INV-ralph-halt-reason-enum` unchanged (halt reason `CI_BLOCKED` per FR14k closed enum) — Story 2.9 pins the contract only, enforcement deferred. Story file § Change Log v1.2 appended above v1.1 (reverse-chronological per Story 2.7 iter-222 + Story 2.8 iter-229 precedent). Status field UNCHANGED at `ready-for-dev` (sprint-status-facing; orthogonal to Ralph-internal Story State). **Invariants consumed-not-modified:** `INV-ralph-halt-reason-enum`, `INV-devbox-homedev-named-volume`, `INV-devbox-egress-contract`, `INV-devbox-dind-available`. **No sprint-status.yaml flip, no code edits, no RALPH.md lesson** (pattern is nineteenth cumulative precedent — no new insight to record; pure mechanical inheritance from Story 2.8 iter-229 template). Budget iter-236: ~20K (preflight verification + Change Log append + IP update + commit). **Next iter:** `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-9-gh-cli-oauth-via-pnpm-gh-auth.md")` fresh-context per § Story Lifecycle Decision Matrix row `atdd-scaffolded → in-dev`.

- [x] iter-235: **STORY 2.9 PRE-DEV SM VALIDATED — Story State `drafted → validated`; sprint-status unchanged per iter-156 + iter-221 + iter-225 precedent (pre-dev SM gate does NOT flip sprint-status).** `/bmad-create-story (args: "review")` ran fresh-context against the Story 2.9 v1.0 draft. Dispatched two parallel verification subagents (source-file claims + planning-artefact claims) covering ~40 specific line-number citations + enumerations. **2 PATCHes applied** (within iter-221 LESSON's 3-7-PATCH forecast band; toward low end consistent with the "narrowest novel surface in Epic 2 to date" pre-review prediction): **PATCH 1 — architecture.md:490 → :75 line-cite correction (4 occurrences in story file):** draft cited `architecture.md:490` as "verbatim CLI-tool-surface table row" for `pnpm gh:auth`; actual pinning is at `architecture.md:75 § Cross-Cutting Concerns` prose bullet (NOT a table; :490 is about R2/S3 dependencies, unrelated to pnpm surface). Both `pnpm claude` + `pnpm gh:auth` are colocated in ONE architecture.md:75 sentence. Story 2.8 shipped its draft with the same mis-framing at `:489` without any SM / trace / post-dev / CR gate surfacing the drift — iter-235 lesson captured in RALPH.md. Affected in 2.9 draft: Task 2 Verb form bullet; SC-2 Source; Project Structure Notes § Alignment; References § CLI tool surface. **PATCH 2 — whitelist/github.txt enumeration adds `ghcr.io` (4 occurrences):** actual file has 7 entries (`api.github.com`, `github.com`, `raw.githubusercontent.com`, `objects.githubusercontent.com`, `codeload.github.com`, `ghcr.io`, `pkg-containers.githubusercontent.com`); draft enumerated 6 (omitting `ghcr.io`). Enumeration inherited from Story 2.8 SC-2 (non-central at Story 2.8 because Anthropic-not-GitHub OAuth target; drift dormant until Story 2.9 made github.txt central). Affected in 2.9 draft: Task 5 operator-workstation-deferred bullet; Testing Standards ground (c); Substrate contracts § Story 2.3; Unchanged § whitelist. **Story file § Change Log v1.1 appended** (~500 words documenting both PATCHes + pattern observations). **RALPH.md § Lessons** extended with two carry-forward rules: (i) line-cite-drift rule — SM reviews MUST grep-verify every `<planning-file>:<line>` citation at review time; (ii) enumeration-drift rule — when a new story promotes a previously-peripheral substrate file to central SC coverage, re-enumerate from scratch at draft. **Invariants consumed-not-modified:** `INV-ralph-halt-reason-enum`, `INV-devbox-homedev-named-volume`, `INV-devbox-egress-contract`, `INV-devbox-dind-available` (all substrate). **No sprint-status.yaml flip, no code edits, no commits to substrate files.** Budget iter-235: ~65K (2 parallel verification subagents + 7 Edits + Change Log + RALPH.md lesson + IP update). **Next iter:** `/bmad-testarch-atdd` fresh-context per § Story Lifecycle Decision Matrix row `validated → atdd-scaffolded`.

- [x] iter-234: **STORY 2.9 DRAFT LANDED — Story State `_(no story) → drafted`; sprint-status `2-9: backlog → ready-for-dev` with `last_updated: iter-234` marker.** Recovery-commit flow — prior attempt invoked `/bmad-create-story` fresh-context, wrote the 347-line story file + flipped sprint-status, then was killed before commit. iter-234 re-entered with four durable signals: (i) `_bmad-output/implementation-artifacts/2-9-gh-cli-oauth-via-pnpm-gh-auth.md` untracked, (ii) `sprint-status.yaml` modified with iter-234 last_updated header, (iii) IP.md NOW still pointing at `/bmad-create-story` (stale), (iv) `git log` tip at iter-233. Applied iter-234 LESSON (killed-before-commit recovery): committed durable artefacts, advanced IP + Story State, skipped skill re-invocation. **Draft density:** 347 lines / 70,674 bytes, 4 ACs + 6 tasks + 17 SCs — matches Story 2.8 v1.0 baseline + Story 2.7 post-PATCH density. **AC structure:** 1:1 mechanical inheritance from Story 2.8 with ground-(c) "external-service-owns-behavior-under-test" variant substituting `gh` CLI + `github.com` OAuth endpoint for claude + Anthropic OAuth; novel AC 4 element vs Story 2.8 is "Ralph's pre-push gate (Epic 3) treats this as a halt-able condition rather than silently retrying" — pins the `CI_BLOCKED` halt-reason contract but defers implementation to Epic 3 Story 3.7 (`INV-ralph-halt-reason-enum` consumed-not-modified). **17 SCs** verbatim mirror Story 2.8 SC-2..SC-17 with single-verb substitution throughout. **CR forecast:** FOURTH cumulative one-pass ZERO-PATCH CR precedent candidate (pattern chain: 1.13 + 2.7 + 2.8 + 2.9); 100% composition on Stories 2.6 + 2.7 + 2.8 patterns means narrowest novel surface in Epic 2 to date; ATDD-skip + trace-WAIVED projected per FR14n matrix row 3 (NINETEENTH cumulative ATDD-skip, TWENTIETH cumulative trace-WAIVED under ground-(c)). **Budget iter-234:** ~25K (orient + recovery-commit flow; no fresh skill invocation). **Next iter:** `/bmad-create-story (args: "review")` fresh-context per § Story Lifecycle Decision Matrix row `drafted → validated`.

_(iter-231..233 Story 2.8 closure entries pruned per Guardrail 2 — Story 2.8 done at iter-233; see git log for details.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **8/17 stories done** (2.1-2.8) + 1 drafted (2.9) + 8/17 backlog (2.10..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live OAuth AC 1-4 smokes operator-workstation-deferred per ground-(c) carry-forward.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:119). Host-side shim count: **16 verbs** at Story 2.8 landing; Story 2.9 will raise to 17 verbs once impl lands.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **8 done** (2.1-2.8); 1 drafted (2.9); 8 backlog (2.10..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.9 — `gh` CLI OAuth via `pnpm gh:auth`.
- **Story File:** `_bmad-output/implementation-artifacts/2-9-gh-cli-oauth-via-pnpm-gh-auth.md` (v1.2 post-iter-236 ATDD-skip; 4 ACs + 6 tasks + 17 SCs unchanged; § Change Log v1.2 appended only — no structural edits).
- **Story State:** `atdd-scaffolded` (skip-marker endpoint) — next iter action per § Story Lifecycle Decision Matrix is `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-9-gh-cli-oauth-via-pnpm-gh-auth.md")` in fresh context window.
- **GitHub Issue:** Story 2.9 unwired at iter-234 (`RALPH_ISSUE_NUMBER` unset per per-iteration Issue Tracking block; skip `Refs #<story-issue>` in commits). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2; `statusCheckRollup: []` at iter-236 pre-push confirmation).

## Notes

- **iter-235 new lesson — "pre-dev SM reviews MUST grep-verify every specific `<planning-file>:<line>` citation; enumeration-drift compounds across stories."** Both lessons landed in RALPH.md § Lessons. Two-subagent verification pattern (source-file claims + planning-artefact claims in parallel, each returning VERIFIED / DRIFTED / MISSING per claim) is the reference implementation — dispatch via `Explore` subagent with terse-report instruction. Applies to every future SM review at drafted → validated gate, and to post-dev SM review at traced → sm-verified gate (drift can be introduced by implementation edits to substrate files between SM gates). Story 2.8's 6-gate ZERO-PATCH closure without surfacing the `architecture.md:489` drift is the cautionary tale — the drift was detectable at every gate but no agent grep-verified.
- **iter-234 new lesson — "killed-before-commit skill recovery: inspect disk state before re-invoking."** Four durable signals indicate a prior-attempt-killed state: (i) untracked output files matching expected skill-output path, (ii) sprint-status.yaml modified with a `last_updated: <current-iter>` header advancing past last commit's iter, (iii) IP.md modified with partial updates, (iv) `git log` tip NOT matching sprint-status iter-marker. Any three present → recovery-commit mode, DO NOT re-run skill. Generalises to all fresh-context skill invocations whose primary output is committed artefacts (create-story, testarch-trace, testarch-atdd, code-review, dev-story). Preserved in RALPH.md for future-Ralph reference.
- **iter-233 new lesson — "one-pass ZERO-PATCH CR compounds with dense pre-dev SM + zero-drift impl + zero-PATCH post-dev SM" (THIRD cumulative precedent):** Story 1.13 iter-85 first; Story 2.7 iter-226 second; Story 2.8 iter-233 third. Forecast pattern crystallizes: pre-dev SM PATCH density ≥ 3 AND verbatim spec-template impl AND ATDD-skip-trace-WAIVED under ground-(c) → reliable one-pass ZERO-PATCH CR closure. Story 2.9 candidate for FOURTH one-pass ZERO-PATCH precedent (inherit verbatim; gh CLI + `github.com` ground-(c) substitution).
- **iter-233 new lesson — "deferred-work.md AR-51..AR-59 cluster adds 9 entries, bringing substrate-polish backlog to 50+ items":** The `_lib.sh` refactor target has accreted substantial evidence (AR-48 daemon-hang timeout + AR-49 symlink `readlink -f` + AR-52 TOCTOU retry + AR-53 daemon-error disambiguation + AR-54 per-state guidance + AR-55 nonexistent-vs-stopped disambiguation). Post-Epic-2 `_lib.sh` lift is the right amortization point; do NOT extract mid-Epic-2 (SC-14 inheritance chain Story 2.6 AR-19 → 2.7 SC-14 → 2.8 SC-14 → 2.9 SC-14 continues).
- **iter-221 carry-forward — pre-dev SM `~5-PATCH` ceiling is independent of draft-SC density.** Story 2.9 inherits: budget 3-7 PATCH at next iter's SM review gate regardless of draft density (17 SCs). Types that reliably appear: (a) claims about EXISTING file state (H-levels, line numbers) drifting between draft + review time, (b) template-vs-schema fidelity gaps, (c) implicit read-only assumptions a dev-agent wouldn't infer, (d) gotchas buried in Dev Notes that belong in user-facing AC/Task content.
- **iter-232 post-dev SM ZERO-PATCH SECOND precedent + iter-231 trace-WAIVED EIGHTEENTH cumulative precedent + iter-230 zero-drift implementation validates iter-228's 4-PATCH SM density** — carry-forward unchanged for Story 2.9 (gh OAuth via GitHub CLI) verbatim template inheritance with ground-(c) variant swap.
- **TTY-detect at docker-exec shim (iter-214 LESSON) — Story 2.9 CONFIRMED NON-APPLICATION at draft gate:** SC-6 explicitly pins hardcoded `-it` ("OAuth flow IS the AC"). Same deferral posture as Story 2.7's `docker attach` (SC-8) + Story 2.8 SC-6. Pattern holds for Story 2.9 gh OAuth (interactive-by-contract).
- **`<verb>-host.sh` host-side shim naming convention (iter-199 EXTENDED through iter-233):** Story 2.9 `gh-auth-host.sh` ships substrate-quality under `packages/devbox/scripts/`, raising host-side shim count to 17 at impl-landing. Remaining Epic 2 candidate: Story 2.12 (`ssh-host.sh` for loopback-bound port publication if the story adopts the same pattern). Forecast: Story 2.17 landing → 17+ total shims → post-Epic-2 `_lib.sh` refactor threshold triggered.
