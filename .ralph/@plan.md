# Implementation Plan

## NOW

- [ ] **Run `/bmad-create-story` fresh-context for Story 2.11** (Docker Compose shared mode — devbox substrate) per § Cross-epic within-epic auto-advance (sprint-status `2-11-…: backlog` row exists; current epic still has next-in-epic backlog so § Cross-epic transition routes to within-epic continuation, NOT EPIC_DONE halt). One workflow per iteration (Guardrail 5). Skill auto-flips sprint-row `backlog → ready-for-dev`; Story State enters `_(no story) → drafted`. Update IP § Context: `Story = 2.11`, `Story File = _bmad-output/implementation-artifacts/2-11-…-shared-devbox-mode-keel-devbox-shared.md`, `Story State = drafted`. Forecast: ~30-50K (orient + sprint-status read + epic-2 epics.md read + skill dispatch + story file write + IP update + RALPH.md signpost + commit).

## QUEUE (Story 2.11..2.17 → Epic 2 close-out)

- [ ] _(after Story 2.11 `drafted`)_ Iterate Stories 2.11..2.17 through full lifecycle — 7 remaining substrate stories spanning shared mode + port-publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows. Stories 2.11..2.17 CR-gate forecast (iter-251 + iter-252 + iter-253 LESSONS): when a story introduces a novel multi-step runtime probe chain, forecast ≥1 PATCH at CR gate AND 2-4 second-class DEFERs at CR re-run; pre-emptively audit at impl time for `set -e` + `if cmd; then` rc-suppression patterns + Podman-compat rc semantics + compound-failure short-circuit doctrine. Cumulative Epic 2 close-out reconciliation forecast: ~10-15 second-class DEFERs across PATCH-iter chain (incremented from iter-251's 4-DEFER baseline + iter-253's 3-new-DEFER addition = 7 cumulative; SC-17 close-out scope grows accordingly).
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-23 per iter-219..253 repeated pre-push confirmation — `gh pr view 230` returns `statusCheckRollup: []`).

## BLOCKED

- [ ] iter-249+250+251+252+253 push to `origin feat/epic-2-packaged-devbox` — REASON: SSH :22 to github.com STILL timing out at iter-252 step 5 push attempt; HTTPS :443 GraphQL `gh pr view 230` failed at iter-253 orient (`dial tcp 140.82.121.5:443: i/o timeout` — fluctuating: succeeded iter-252 orient, failed iter-253 orient). SSH/HTTPS split: push traffic → SSH → BLOCKED across 5 consecutive iters; PR-probe queries → HTTPS → INTERMITTENT. 6+ commits locally on `feat/epic-2-packaged-devbox` (5 ahead pre-iter-253 + iter-253 CR-closure commit): `aefb382` (iter-249 push-fail) + `d908d6f` (iter-249 trace-WAIVED) + `447bd84` (iter-250 post-dev SM) + `b701e97` (iter-251 CR) + `1e67b84` (iter-252 PATCH-1) + `c0b990a` (iter-252 push-fail IP) + iter-253 CR-closure (this iter).
  - Attempted: push attempts at iter-249/250/251/252 all failed with SSH :22 timeout to github.com.
  - Error/Issue: persistent egress block on SSH :22 to github.com from iter env across 5 consecutive iters; HTTPS :443 to api.github.com partially recovered but git push is hard-wired to the SSH remote URL.
  - Next: iter-253 attempts push again at step 5 (bank on intermittent recovery). If iter-253+ still fails, escalate to operator — **recommended operator action**: one-off `git remote set-url --push origin https://github.com/tthew/ralph-bmad.git` on the host to unblock. Ralph SHOULD NOT autonomously reconfigure the remote URL (shared-state mutation beyond single-iter scope per § Executing actions with care). Commits are safe in local branch — no force-push, no amend, preserved intact across iter-249..253.

## ATDD Red Phase

_(none — Story 2.10 ATDD-skip applied at iter-247; CR-re-run gate is post-impl artefact-only, no test-runner harness; mock-docker smokes at iter-252 ephemeral + torn down inside same iter per iter-223/230/237/248 LESSON.)_

## DONE (iter-253 Story 2.10 CR re-run closure — Story State `fixes-pending → done`; sprint-row `review → done`)

- [x] iter-253: **STORY 2.10 CR RE-RUN CLOSURE — `/bmad-code-review (args: "2")` re-run via three-layer Ralph-hosted adversarial fan-out CONFIRMS PATCH-1 (iter-252) closure; Story State `fixes-pending → done`; sprint-row `review → done`; 0 new PATCH / 3 new DEFER / 1 new DISMISS.** Three subagents in parallel per iter-251 pattern: **Blind Hunter** (`bmad-agent-architect`) diff-only review of PATCH-1 commit `1e67b84` returned **ZERO findings** — rc-capture mechanics correct (`set -e` not suppressed by LHS of `||`; explicit `rc=0` init; exhaustive `case` arms 0/1/`*`; quoting + redirection + `set -u` interactions all check out); **Acceptance Auditor** (`bmad-agent-dev`) re-verified **5/5 ACs + 17/17 SCs** unchanged + **4-site contract-doc lockstep HOLDS** (script header `prereq-check.sh:25-26` ↔ INV doc § Exit-code schema ↔ README L748 ↔ manifest description) + `pnpm keel-invariants:check` EXIT=0 → ZERO-PATCH verdict; **Edge Case Hunter** (`bmad-tea`) confirmed PATCH-1 closes iter-251 HIGH and surfaced 3 new operator-edge findings + 1 NIT (none closure-blocking; all DEFERed to SC-17 or out-of-scope). **Triage:** DEFER-5 (volume-inspect rc=1 daemon-degraded race ~5s window after Docker Desktop wake; Tier 1 `docker info` covers most paths), DEFER-6 (Podman-compat rc=125 vs rc=1 for missing volume; substrate is Docker-only at 1.0; SC-17 spec-clarification in INV doc § Three-check contract), DEFER-7 (first-failure short-circuit hides second-probe failure; consistent with Tier 1 fail-fast + AC 5 composite is token-class doctrine), DISMISS-23 (`rc` variable scoping NIT — `prereq-check.sh` is not a sourced library; cosmetic only). PATCH-1 (iter-251 HIGH) marked CLOSED in story file § Review Findings with iter-253 verification trail; new triage block appended; Status HTML comment flipped `fixes-pending → done`; iter-251 review trail preserved as second HTML comment for audit. Sprint-status flipped `2-10-…: review → done` + last_updated comment + trailing field bumped (iter-202 precedent: CR gate flips sprint-row only on `done` transition). Change Log v1.7 prepended (~120 lines). **NOVEL LESSON:** PATCH iterations close FIRST-class adversarial findings but commonly surface SECOND-class operator-edges in the same surface (PATCH-1's rc=1 acceptance contract exposed Docker Desktop wake-race + Podman compat + compound-failure short-circuit edges that the prior `if`-wrapper bug had hidden). Forecast carry-forward for Stories 2.11..2.17: each PATCH iter likely yields 2-4 second-class DEFERs at re-run (operator-edge ~1/1000/yr class). **Chain reset:** Story 2.10 closes as "first CR-gate PATCH on the post-precedent chain"; Stories 2.11..2.17 begin fresh ZERO-PATCH-CR forecast cycle (forecast-low-band 0-1 PATCH per story given iter-251/252/253 LESSONS now baked into impl-time discipline). Push status: HTTPS GraphQL `gh pr view 230` failed at iter-253 orient (`dial tcp 140.82.121.5:443: i/o timeout` — fluctuating from iter-252's success); SSH :22 not yet re-tested (will attempt push at step 5; iter-253 BLOCKED entry pre-emptively logged). Budget iter-253: ~50K (orient ~10K + 3 parallel adversarial subagents ~30K [Blind 14K, Edge 47K, Auditor 52K combined ~113K agent-budget] + triage + Review Findings v1.7 + Change Log + IP + sprint-status + commit ~10K).

_(iter-243..252 Story 2.9 closure + Story 2.10 lifecycle iters pruned per Guardrail 2 — see Story 2.10 § Change Log v1.0–v1.7 + RALPH.md Signposts for per-gate detail.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **10/17 stories done** (2.1-2.10) + 7/17 backlog (2.11..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered at iter-248 9/9 stub-docker harness + iter-252 9/9 mock-docker PATH-shim harness.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:119). Host-side shim count: **18** at iter-253 (unchanged from iter-248).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **10 done** (2.1-2.10); 7 backlog (2.11..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** _(no story — Story 2.10 closed at iter-253; next is Story 2.11 via `/bmad-create-story` at iter-254)_
- **Story File:** _(n/a — Story 2.11 file written at iter-254)_
- **Story State:** _(no story)_ — Story 2.10 closed `done` at iter-253; § Cross-epic within-epic auto-advance routes to Story 2.11 `_(no story) → drafted` next iter.
- **GitHub Issue:** Story 2.10 likely unwired (`RALPH_ISSUE_NUMBER` unset at iter-250/251/252/253 check; skip `Refs #<story-issue>` in commits). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out, NOT iter-253; Story 2.10 close ≠ Epic 2 close).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — last confirmed `{"isDraft":true,"mergeStateStatus":"CLEAN","state":"OPEN","statusCheckRollup":[]}` at iter-252 orient; iter-253 orient HTTPS GraphQL probe failed; PR state assumed unchanged (no merge events expected during BLOCKED push window).

## Notes

- **iter-253 Story 2.10 CR re-run closure — NOVEL LESSON on PATCH-iter SECOND-class DEFER pattern.** PATCH iterations close FIRST-class adversarial findings (rc=12 unreachable HIGH at iter-251) but commonly surface SECOND-class operator-edges in the SAME surface that the prior bug had hidden. The act of stabilising rc=1 as "absent" exposed: (a) Docker Desktop wake-race (rc=1 conflates "not found" with "daemon mid-recovery"); (b) Podman compat (rc=125 vs rc=1 for missing volume); (c) compound-failure short-circuit (operator sees only first probe failure). None are closure-blocking; all defer to SC-17 polish. **Forecast carry-forward for Stories 2.11..2.17:** each PATCH iter at CR re-run likely yields 2-4 second-class DEFERs. Cumulative Epic 2 close-out reconciliation forecast updated: ~10-15 second-class DEFERs across PATCH-iter chain.
- **iter-252 Story 2.10 PATCH-1 — scope-bounded direct edit; NOVEL LESSON on `set -e`+`if cmd; then` rc-suppression.** The rc-capture pattern is the canonical substrate form for future multi-step runtime probe primitives.
- **iter-251 Story 2.10 CR — FIRST CR-gate patch on 4-story ZERO-PATCH chain FALSIFIED.** Adversarial triage (Blind+Edge+Auditor) surfaced the `if cmd` rc-suppression gap neither SM nor narrow-AC verification caught. LESSON: adversarial CR catches what narrow-AC verification cannot; CR-after-SM separation is load-bearing for multi-step runtime probe primitives. Chain continuity: Stories 1.13 iter-85 → 2.7 iter-226 → 2.8 iter-233 → 2.9 iter-244 remain valid (4 cumulative ZERO-PATCH CR precedents); Story 2.10 was FIRST CR-gate patch on the chain. **Chain reset at iter-253:** Stories 2.11..2.17 begin fresh ZERO-PATCH-CR forecast cycle.
- **iter-250 Story 2.10 post-dev SM ZERO-PATCH — 5th cumulative precedent.** Two-subagent verification pattern (iter-235 LESSON) valid for post-dev SM gates but INSUFFICIENT for CR gates (iter-251 LESSON).
- **iter-249 Story 2.10 trace-WAIVED — 20th cumulative precedent.** Mechanical template inheritance.
- **iter-248 Story 2.10 impl — mechanical inheritance from Stories 2.6/2.8/2.9.** Two independent harness approaches at iter-248 (stub-docker workspace) + iter-252 (mock-docker PATH-shim under `/home/dev` per noexec `/tmp` GOTCHA), both green.
- **iter-244/246 audit-findings carry-forward** unchanged from iter-247..252: (1) restart.sh transitive-delegate prepend-only; (2) benchmark.sh OUT-OF-SCOPE clarification (SC-11); (3) Manifest entry count drift (29→30 baseline); (4) Alpine egress non-applicability doctrine (SC-4 + § Alpine probe image). None are iter-253 concerns.
- **Substrate-citation drift forecast for Stories 2.11..2.17 (unchanged from iter-251 Notes):** Story 2.10's 1-drift outcome confirms prepend-only edits shift fewer downstream lines. Forecast: Stories 2.11..2.17 accumulate 1-3 drifts each, deferred to Story 2.17 SC-17 polish pass. Total Epic 2 close-out reconciliation forecast: ~10-20 cumulative line-number drifts.
- **iter-252 GOTCHA — iter-env `/tmp` is `noexec`.** PATH-shimmed mock binaries fail with rc=126 "Permission denied" when dropped in `mktemp -d` default path (`/tmp/tmp.XXXXXXXXXX/docker`). Workaround: create mock dir under `/home/dev`, `/root`, or `/workspace`. Forecast-actionable for any future story using mock-binary smoke harnesses (Stories 2.13 healthcheck emitter + Story 2.14 legacy-branch cleanup are candidates).
