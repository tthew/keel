# Implementation Plan

## NOW

- [ ] Re-run `/bmad-code-review (args: "2")` CR adversarial triage against post-Fix-A/B/C diff. Expected outcome per FR14n matrix row 10: zero Blind Hunter findings → `fixes-pending → sm-verified → done` (single-transition edge). If new findings surface, queue fix tasks; stay `fixes-pending`. ~medium

## QUEUE (Story 1.9 — CR iter-14 re-run surfaced 3 fix tasks + 4 defers; all 3 fixes landed iter-15/16/17; CR re-run now)

_(empty — CR re-run is NOW)_

## BLOCKED

_(none)_

## DONE (Story 1.9 — CR iter-7 through iter-17)

- [x] iter-7: `/bmad-code-review (args: "2")` Ralph-hosted three-layer fan-out — 6 patches + 10 defers + 3 dismissed. CRITICAL: `ANCHOR_REGEX` uppercase-class bug reproduced by Edge Case Hunter + Acceptance Auditor.
- [x] iter-8 through iter-13: 6 CR fix patches landed — (1) CRITICAL `ANCHOR_REGEX` uppercase→lowercase + symmetric `added-to-source-only` emitter + 2 new Task-5 smokes + spec echoes; (2) HIGH spec line 41 stdout→stderr; (3) HIGH count 7→8 sourcePath files; (4) HIGH count 2→5 runtime smokes (narrow-scope per iter-11); (5) MEDIUM trace-bundle line citations re-anchored (31 edits); (6) MEDIUM Dev Agent Record AC 2 status prose upgraded schema→end-to-end.
- [x] iter-14: `/bmad-code-review (args: "2")` CR RE-RUN — 3-layer Ralph-hosted fan-out against post-fix diff. Verdicts: Blind Hunter NEW_FINDINGS (3 HIGH trace residue + 1 MEDIUM spec self-contradiction → queued as Fix A/B/C); Edge Case Hunter MINOR_ONLY (4 LOWs → defers); Acceptance Auditor PASS (all 7 AC with live repros). Impl CLEAN; trace-narrative STALE. **New pattern established: trace-bundle semantic-drift from impl-upgrade.**
- [x] iter-15: Fix A/4 [HIGH] landing — stale UPPERCASE regex echo swap at 2 trace-bundle sites. `[A-Z][A-Z0-9-]+` → `INV-[a-z0-9]+(?:-[a-z0-9]+)+` at trace md:141 + coverage-matrix.json:69. JSON schema validation PASS. Pure prose — zero code changes.
- [x] **iter-17: Fix C/4 [HIGH + MEDIUM] landing — broad-scope prose swap across 13+ sites in 5 artefacts.** Updated spec:120 (closing AC-list `AC 1/4/7` → `AC 1/2/4/5/7`); spec:149 ("three smoke tests materially cover 4 of 7 ACs end-to-end; remaining 3 are schema-level" → "five smoke tests materially cover 5 of 7 ACs end-to-end; remaining 2 are AC-3 schema/structural + AC-6 CLI-contract-only"); trace md — 9 target sites refreshed (§ Traceability Recommendations:307 "three runtime smoke tests → five runtime smoke tests exercised AC-1 + AC-2 + AC-4 + AC-5 + AC-7" + expanded smoke inventory; Test Results Source:349 "three Task 5 → five Task 5 runtime smoke tests" + added iter-8 AC-2/AC-5 smoke descriptions; § Why-WAIVED:436 "three Task 5 shell-invocation smoke tests exercised AC-1 + AC-4 + AC-7 end-to-end, one more AC than Story 1.8's single Task 3 smoke" → "five Task 5 shell-invocation smoke tests exercised AC-1 + AC-2 + AC-4 + AC-5 + AC-7 end-to-end, four more ACs than Story 1.8's single Task 3 smoke" + added AC-2/AC-5 detail bullets; § Waiver Reason:465 "3 runtime smoke tests exercising AC-1 + AC-4 + AC-7 → 5 runtime smoke tests exercising AC-1 + AC-2 + AC-4 + AC-5 + AC-7"; § Business Justification:486 "three highest-impact ACs end-to-end … two structural-only P1 branches" → "five of seven ACs (AC-1 + AC-2 + AC-4 + AC-5 + AC-7) end-to-end … one structural-only P1 branch (AC-3) plus one CLI-contract-only P2 branch (AC-6)"; § Stakeholder Communication:526 mirror update + iter-8 revert round-trips added; YAML recommendations:555 "Task 5 shell-invocation smoke already covers AC-1 + AC-4 + AC-7 end-to-end" → "Task 5 shell-invocation smokes (iter-8 ×5) already cover AC-1 + AC-2 + AC-4 + AC-5 + AC-7 end-to-end; only AC-3 remains schema/structural (and AC-6 CLI-contract-only)"; YAML test_results:583 "3 runtime smoke tests → 5 runtime smoke tests" + expanded smoke inventory; Related Artifacts Test Results:614 "Task 5 three runtime smoke tests → Task 5 five runtime smoke tests" + expanded inventory); coverage-matrix.json:160 recommendation updated "AC-1 + AC-4 + AC-7 → AC-1 + AC-2 + AC-4 + AC-5 + AC-7"; e2e-trace-summary.json:73 mirror update; gate-decision.json:12 rationale comprehensively refreshed ("three shell-invocation smoke tests → five shell-invocation smoke tests"; "AC 1 + AC 4 + AC 7 → AC 1 + AC 2 + AC 4 + AC 5 + AC 7"; "AC 2 / AC 3 / AC 5 realised schema-level → AC 3 remains schema/structural; AC 6 remains CLI-contract-only"). All 3 JSON files re-validated clean. Deliberately left untouched: spec:141 AC-3 mapping "not exercised in smoke" (AC-3 still not exercised end-to-end; iter-8 did not add an AC-3 smoke); spec:177 Change Log row 1.2 "All 3 runtime smoke tests green" (dated historical record describing iter-4 dev-story; iter-8 Change Log row 1.3 already captures the +2 addition); trace:51 "three-prong rationale" (FR14n three-prong, not smoke count); trace:373 "three small new files" (file count, not smoke); trace:453 "three-prong rationale" (FR14n). Pure prose — zero code changes. Story State: remains `fixes-pending` (CR re-run next).
- [x] **iter-16: Fix B/4 [HIGH] landing — stale "Not exercised in smoke" AC-5 framing refreshed at 3 sites.** Edited trace md:143 (AC-5 mapping bullet) + trace md:260 (Happy-Path-Only: "two P1 branches" → "one P1 branch (AC-3)") + coverage-matrix.json:69 substrate_verification tail. AC-5 claim flipped from "Not exercised in smoke" → "Exercised via iter-8 Task-5 docs-side orphan-anchor smoke (append `INV-fake-orphan` → exit 1 + `removed-from-docs-only`; revert; byte-identical restore)" per spec line 143 authoritative phrasing. AC-3 "not exercised in smoke" equivalents deliberately left untouched (iter-8 did NOT add an AC-3 smoke; only AC-2 + AC-5). coverage-matrix.json `coverage: NONE` + `tests: []` + `scope_notes: "Baseline now clean → AC 5 branch not actively triggered"` deliberately NOT flipped — those track test-runner-hosted coverage (still zero at Story 1.9; Story 1.16 backfills) and canonical/steady-state framing respectively. JSON re-validated clean. Pure prose — zero code changes. Story State: remains `fixes-pending` (Fix C + CR re-run still in QUEUE).

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1–1.8 done; 1.9 fixes-pending — iter-14 CR re-run surfaced 3 fix patches; Fix A/B/C landed iter-15/16/17; CR re-run next)
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (continues — PR #226 stays Draft until Epic 1 EPIC_DONE per PROMPT_build.md step 5c)
- **Story:** `1-9-invariant-sync-gate-runtime-tooling-reader-walker-drift-detector`
- **Story File:** `_bmad-output/implementation-artifacts/1-9-invariant-sync-gate-runtime-tooling-reader-walker-drift-detector.md`
- **Story State:** `fixes-pending` (Ralph FR14n matrix row 10) — sprint-status + story-file Status remain `review`; flips to `done` only on NEXT CR re-run zero-findings outcome per matrix row 9 single-transition edge.
- **PR:** #226 (Draft, MERGEABLE, no-CI-checks-reported — expected pre-Story-1.16; stays Draft through Epic 1; Draft→Open at EPIC_DONE)

## Notes

- **iter-17 Fix C executed broad-scope per iter-14 rule** (bias toward broad-scope when literal swap is mechanical AND all target sites are grep-enumerable). 14 edits across 5 artefacts, one coherent semantic upgrade ("post-iter-8 reality: 5 smokes covering AC 1+2+4+5+7 end-to-end; only AC-3 schema/structural + AC-6 CLI-contract-only"). Did NOT split into Fix C1/C2/C3 by artefact — the underlying change is one semantic and splitting would fragment a single coherent edit across iterations.
- **iter-17 Fix C carve-outs deliberately left untouched:** (a) spec:141 AC-3 mapping "not exercised in smoke" — AC-3 still lacks a dedicated smoke (iter-8 added AC-2 + AC-5 smokes only); flipping it would be drift toward fiction. (b) spec:177 Change Log row 1.2 "All 3 runtime smoke tests green" — dated historical record describing iter-4 dev-story; iter-8 Change Log row 1.3 (line 178) already documents the +2 addition. Change logs are append-only, not retroactively edited. (c) trace:51/453 "three-prong rationale" — refers to FR14n three-prong skip justification, not smoke count. (d) trace:373 "three small new files" — file count (manifest-reader / sync-gate / check), not smoke. (e) coverage-matrix.json AC-5 `coverage: NONE` / `tests: []` / line-70 scope_notes — those track test-runner-hosted coverage (still zero at Story 1.9; Story 1.16 backfills) and canonical/steady-state framing respectively. Keeps the Fix C scope surgical.
- **Pattern now stable (iter-14/15/16/17 CR re-run arc):** "semantic drift from impl-upgrade" is the class (trace/spec artefacts authored pre-iter-8 never refreshed when iter-8's +2 smokes materially upgraded substrate evidence from 3→5 ACs end-to-end). Fix A was the UPPERCASE regex residue (2 sites, mechanical); Fix B was "not exercised" → "exercised" AC-5 framing (3 sites, narrow-scope); Fix C is the cardinality + enumeration prose swap (14 sites, broad-scope). All three fixes address the same root cause from different angles.
- **CR re-run expectation (NOW):** Blind Hunter should find zero trace-residue drift findings (all 4 iter-14 HIGH patches now landed — Fix A/B/C plus the MEDIUM defer-or-fix is actually Fix C). Edge Case Hunter may re-surface the 4 iter-14 LOWs (defer-worthy). Acceptance Auditor should PASS (all 7 AC with live repros unchanged since iter-14). If clean → `fixes-pending → sm-verified → done` single-transition edge per FR14n matrix row 10.
- **Issue Tracking:** Story 1.9 issue number still unset at iter-17. Parent Epic 1 issue **#9** — `Refs #9` only.
