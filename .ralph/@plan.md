# Implementation Plan

## NOW

- [ ] Step-5 push attempt with SSH-:22 / HTTPS-:443 github.com outage carry-forward. iter-290 `/bmad-dev-story` landed Tasks 2+3+4 ON-MAIN (PARTIAL; Task 1 DEFERRED on network outage). Local commits accumulate; step-5 push via SSH `git@github.com:tthew/ralph-bmad` will share the same github.com :22 outage (confirmed at iter-290 SSH-recovery probe). **Expected outcome: push FAILURE; commit IP + exit cleanly per Ralph prompt step 5 "if push fails, document in IP and exit cleanly"; unpushed commits carry forward to next iter per iter-281 carry-forward precedent.**

## QUEUE (Story 2.14 lifecycle + 2.15..2.17 substrate queue)

- [ ] _(top priority; next iter)_ **Story 2.14 Task 1 retry — `/bmad-dev-story` re-entry.** Single-task iter: execute Task 1 branch-creation recipe (§ "Canonical recipe" in story file — `git fetch https://github.com/tthew/cc-devbox.git main:legacy-devbox` + scratch worktree + retention banner prepend + sed substitution + grep-verification guard + commit + push + worktree cleanup + verification via `git ls-remote origin refs/heads/legacy-devbox` + `git show origin/legacy-devbox:README.md | head -20`). Pre-flight: verify `github.com` reachable via `timeout 10 curl -sI https://github.com` BEFORE invoking `/bmad-dev-story` — if still blocked, re-defer + surface as operator-interactive fix task. Iter budget forecast: ~20-40K (single-task iter; most state already landed at iter-290). Transition on success: Story State `in-dev (partial) → in-dev`.
- [ ] _(after Story 2.14 Task 1 done → in-dev)_ Story 2.14 trace — `/bmad-testarch-trace (args: "yolo")` likely WAIVED via ground-(a)+(b)+(c) hybrid OR ground-(ii)+(iii) (twenty-fifth+ cumulative WAIVED precedent; Story 2.14 narrower grounds per iter-289 ATDD-skip observation — drops (c) since ACs are static-smoke-testable). Pattern candidate (iter-289 NOVEL LESSON): policy-class stories drop ground (c) cleanly when ACs resolve to pure documentation + git-branch-state + substrate-synchrony.
- [ ] _(after Story 2.14 traced)_ Story 2.14 post-dev SM — `/bmad-create-story (args: "review")` post-dev. Forecast 0-2 PATCH per iter-270 LESSON; iter-288 pre-dev SM landed 6 PATCH + 1 DEFER, so post-dev absorption band narrow.
- [ ] _(after Story 2.14 sm-verified)_ Story 2.14 CR — `/bmad-code-review (args: "2")`. Forecast 0-2 first-class PATCH per iter-279 + iter-286 ZERO-PATCH precedent for narrow-diff stories.
- [ ] _(after Story 2.14 done)_ Story 2.15 committed Claude settings.json deny/allow — full lifecycle.
- [ ] _(after Story 2.15 done)_ Story 2.16 Claude PreToolUse hooks for secret-file denylist — full lifecycle.
- [ ] _(after Story 2.16 done)_ Story 2.17 hook-settings bypass-resistance + Epic 2 SC-17 close-out polish pass. SC-17 reconciliation absorbs cumulative **31 DEFERs at Story 2.14 iter-290 dev-story partial landing** (24 carried from Story 2.12 + 6 iter-286 Story 2.13 CR defers + 1 iter-288 Story 2.14 pre-dev SM defer: D-14 Task 1 push-failure recovery + idempotent re-bootstrap) — no new DEFERs added at iter-290 (dev-story partial landing lands zero PATCH; Task 1 defer is STATE-not-PATCH); PLUS cumulative Epic 2 Story 2.14 (post-dev SM + CR forecast) + Story 2.15..2.16 DEFERs accrued during those stories.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-24 per iter-219..289 confirmation; `statusCheckRollup: []` carried unchanged; iter-290 orient `gh pr view 230` GraphQL :443 + REST :443 both timed out under the github.com outage; PR state inferred from iter-287..289 carried signal).

## BLOCKED

- [ ] **github.com network outage blocking Story 2.14 Task 1 (branch creation) + step-5 push.** REASON: iteration env cannot reach `github.com` on either :443 (HTTPS) or :22 (SSH). `codeload.github.com` reachable (HTTP/2 301 on curl probe); DNS healthy (`github.com → 140.82.114.3`). Scoped to `github.com` apex host.
  - Attempted at iter-290: `timeout 180 git fetch https://github.com/tthew/cc-devbox.git main:legacy-devbox` (exit "Failed to connect to github.com port 443 after 135605 ms"); `timeout 180 git fetch git@github.com:tthew/cc-devbox.git main:legacy-devbox` (exit "connect to host github.com port 22: Connection timed out"); `timeout 15 curl -sI https://github.com` hung; `gh pr view 230` GraphQL + REST both timed out at iter-290 orient.
  - Error/Issue: Transient host-level network partition scoped to `github.com` apex; not a substrate or invariant regression. Matches iter-281 carry-forward ("IP note on unpushed 6e0e810 + github.com network outage") pattern.
  - Next: Operator action NOT strictly required — outage is transient; next Ralph iter can re-probe and retry. If outage persists across multiple iters (e.g. 3+ consecutive), escalate as operator-interactive fix task (operator runs Task 1 recipe on host workstation where `github.com` may be reachable OR operator confirms outage scope at their ISP / network layer).

## ATDD Red Phase

_(none — Story 2.14 ATDD-skipped at iter-289 with grounds-(ii)+(iii); no red-phase tests owed. Carry-forward from iter-289.)_

## DONE (iter-290 Story 2.14 `/bmad-dev-story` PARTIAL LANDING — Tasks 2+3+4 ON-MAIN; Task 1 DEFERRED on github.com outage)

- [x] iter-290: **STORY 2.14 DEV-STORY PARTIAL LANDING.** FR14n Story State `atdd-scaffolded → in-dev (partial)` under § Story Lifecycle row `atdd-scaffolded → /bmad-dev-story`. Tasks 2+3+4 landed ON-MAIN; Task 1 DEFERRED per story file § "Iteration env caveat" on github.com network outage. **Zero substantive PATCH at dev-story iter** (Tasks 2-4 lifted verbatim from spec; no deviation).
  - **Task 2 (invariant doc)**: `docs/invariants/devbox-legacy-branch-retention.md` authored at 167 lines (within ~150-250 target); sha256 `02f6048f78cf3c4e315ec6bc5c55bd52a7278d2cba99cc1f1e7b5a5b91d0c4ca`. Six H2 sections: § Intent (PRD `:617` bootstrap-handoff framing + Story 15b.1 binding) + § Branch creation contract (mirrors Task 1 recipe + idempotence clause + egress-whitelist coverage) + § Cherry-pick workflow (`git format-patch | sed | git am` canonical pattern + IN-SCOPE/OUT-OF-SCOPE lists + "minimal-drift, not feature parity" framing) + § Triage path (canary-then-bisect + commit-5278738 anchor rationale) + § Sunset criteria (M4 + two-consecutive-M4-windows fallback) + § Retirement gate (5-step Story-15b.1 sequence + lockstep-contract clause) + § Fork extension contract ((a) follow-pattern / (b) skip-retention + substrate-wins clauses).
  - **Task 3 (manifest + INVARIANTS.md)**: `INV-devbox-legacy-branch-retention` entry appended after `INV-devbox-healthcheck` + before `INV-gitignored-secret-commit-deny` per iter-268 Devbox-block-contiguity LESSON. InvariantSchema five-field compliance verified. INVARIANTS.md H3 `### Devbox legacy-branch retention (Story 2.14)` + anchor bullet inserted between Story 2.13 and Story 2.2 H3s. Anchor bullet regex-compliant per sync-gate.ts:24 (lowercase-after-`INV-` mandatory). Sync-gate `pnpm keel-invariants:check` + `pnpm keel-invariants:check-all` GREEN first-try post `pnpm --filter @keel/keel-invariants build` rebuild (iter-257 LESSON reaffirmed). Manifest count `33 → 34` confirmed.
  - **Task 4 (README + AGENTS pointers)**: `packages/devbox/README.md` new H2 `## Legacy-devbox branch retention (Story 2.14)` inserted between § Healthcheck and § cc-devbox upstream provenance per SC-17 sibling-append. Content: intro + § Triage TL;DR + § Cherry-pick scope + § Sunset + § Machine-enforced contract. `AGENTS.md` new H3 `### Legacy-devbox branch retention (Story 2.14)` inserted after § Healthcheck within § Devbox iteration environment per SC-17. Content: one-line what + triage-first discipline + cherry-pick pointer + retirement-gate pointer + cross-references. AGENTS.md § Cross-reference uses iter-288 PATCH-6 "grep at impl time + record in Completion Notes" phrasing — NOT a static spec-endorsed claim (per iter-286 NOVEL LESSON #3 META guard); grep-at-impl-time remains owed when Task 1 runs.
  - **Task 1 DEFERRED.** iteration env cannot reach `github.com` on either :443 (HTTPS) or :22 (SSH). Per iter-263 asymmetric-recovery LESSON, BOTH paths were probed; BOTH timed out. `codeload.github.com` reachable (confirming outage is scoped to `github.com` apex). Story Task 1 § "Iteration env caveat" anticipates exactly this scenario.
  - **Novel precedent**: iter-290 is the first Epic-2 external-dependency partial landing. Prior partial landings (Story 2.1 iter-122, Story 2.12 iter-268, Story 2.13 iter-283) were internal recovery iters. Captured as iter-290 NOVEL LESSON in RALPH.md § Lessons (step-3a upkeep): "External network blocks a single task → invert the defer order — land offline-safe tasks first, document the blocked task, exit cleanly with carry-forward fix task; don't hold all tasks hostage to the blocked one."
  - **Budget consumed**: ~55K tokens (orient ~10K + network probes ~5K + invariant-doc authoring ~10K + manifest + INVARIANTS + README + AGENTS edits ~8K + story-file Dev Agent Record + Change Log ~12K + IP + RALPH.md + commit-prep ~10K). Well within ~117K execution budget.
  - **PR #230:** Draft; state unverifiable this iter (github.com GraphQL + REST both timed out at orient). IP + iter-289 carry-forward confirmed state unchanged (`state=open, draft=true, mergeable=MERGEABLE, statusCheckRollup=[]`). Step-5 push via SSH :22 shares the outage — expected PUSH FAILURE; commit + exit cleanly per Ralph prompt step 5.

- [x] iter-289: Story 2.14 ATDD-SKIP — `/bmad-testarch-atdd` SKIP-WITH-GROUNDS-(ii)+(iii); `validated → atdd-scaffolded`; 24th-cumulative ATDD-skip precedent; ZERO-PATCH iter; narrower grounds than Story 2.13 iter-282 (drops (c) for static-smoke-testable ACs). See commit `ef10b5a`.
- [x] iter-288: Story 2.14 PRE-DEV SM — `/bmad-create-story (args: "review")`; `drafted → validated`; 6 PATCH + 1 DEFER + ~5 DISMISS; two-subagent pattern. See commit `3681250`.
- [x] iter-287: Story 2.14 DRAFTING — `/bmad-create-story`; `_(no story) → drafted`; first POLICY+BRANCH-CREATION story in Epic 2. See commit `ae169c8`.

- [x] iter-286: Story 2.13 DONE — ZERO-PATCH CR closure (`sm-verified → done`). See commit `d2c49d7`.
- [x] iter-285: Story 2.13 SM-VERIFIED — 2 NARROW PATCH (Dockerfile citation drift + H2-count bookkeeping). See commit `bb3f0b3`.
- [x] iter-284: Story 2.13 TRACED — WAIVED ground-(a)+(b)+(c) hybrid. See commit `964f494`.
- [x] iter-283: Story 2.13 DEV-STORY (RECOVERY) — all 5 Tasks verified + committed; INV-devbox-healthcheck registered. See commit `1fb2fde`.
- [x] iter-282: Story 2.13 ATDD-SKIP — grounds-(c)+(ii)+(iii). See commit `3f35dd6`.
- [x] iter-280..281: Story 2.13 DRAFTED + VALIDATED — 6 PATCH + 3 DEFER at pre-dev. Commits `985aee0`/`6e0e810`/`9863358`.
- [x] iter-272..279: Story 2.12 lifecycle — PATCH-1..6 + CR closure re-runs. Commits `30b7d8d`/`1df64ab`/`0d83fae`/`820591a`/`43b4e4b`/`3eadcff`/`947cbce`/`ce3ffb4`.

_(iter-253..271 Story 2.10/2.11/2.12 closure + Story 2.12 lifecycle iters pruned per Guardrail 2. iter-285/286/287/288/289 deep-detail sections pruned at iter-290 per Guardrail 2 — full detail retained in commits + story file v1.0 Change Log + `deferred-work.md § code review of story-2.13`.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **13/17 stories done** (2.1-2.13) + 4/17 backlog/in-flight (2.14..2.17). Story 2.14 at `in-dev (partial)` after iter-290 `/bmad-dev-story` partial landing (Tasks 2+3+4 ON-MAIN; Task 1 DEFERRED on github.com outage).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered through iter-290 (`pnpm keel-invariants:check` GREEN at 34 manifest entries post iter-290 `INV-devbox-legacy-branch-retention` landing).
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:120). Host-side shim count: **18** at iter-290 (unchanged — dev-story partial landed documentation + manifest only; zero host-side shim edits).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **13 done** (2.1-2.13); **4 backlog/in-flight** (Story 2.14 at `in-dev (partial)` at iter-290; 2.15-2.17 backlog). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.14 — Legacy-devbox branch retention policy.
- **Story File:** `_bmad-output/implementation-artifacts/2-14-legacy-devbox-branch-retention-policy.md`
- **Story State:** `in-dev (partial)` at iter-291 entry (iter-290 landed `atdd-scaffolded → in-dev (partial)` via `/bmad-dev-story` Tasks 2+3+4 ON-MAIN; Task 1 DEFERRED on github.com :443 + :22 outage). Next iter (iter-291): pre-flight `curl -sI https://github.com` probe; if GREEN invoke `/bmad-dev-story` re-entry (single-task iter — Task 1 execution); if still outage carry-forward and re-queue.
- **GitHub Issue:** Story 2.14 issue unknown; `RALPH_ISSUE_NUMBER` unset at iter-290. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` carried unchanged iter-272..290 per iter-289 orient confirmation; iter-290 orient could not re-verify due to github.com network outage but IP carries the signal).

## Notes

- **iter-290 NOVEL LESSON (external-dependency partial landing — invert the defer order):** When an external network block prevents execution of a SINGLE task in a multi-task story, do NOT defer ALL tasks of the story to preserve "clean ordering." Instead, land offline-safe tasks first (documentation + manifest + pointers), document the blocked task's defer state explicitly in Dev Agent Record Completion Notes + Change Log + Status HTML comment, and carry the blocked task forward as a fix task in IP QUEUE. This inverts the story-author's draft-time defer order (which assumed Task 1 → Tasks 2-4 strict sequencing) because at dev-story execution time Tasks 2-4 are provably independent of Task 1's branch materialization — sync-gate does not assert branch existence, only doc + manifest synchrony. Rationale: forward progress > strict-ordering; the eventual contract satisfaction (branch + docs + manifest all present) happens regardless of landing order. **When to apply:** single external-dependency-block spanning one task while the story has ≥2 offline-safe tasks. **When NOT to apply:** tasks have real semantic dependencies (e.g., manifest needs the branch SHA as input) — then strict-order deferral applies. Story 2.14 Tasks 2-4 do NOT need the branch SHA as input; the invariant doc describes the recipe + contract abstractly. Pinned in RALPH.md § Lessons.
- **iter-290 observation (iter-281 github.com-outage pattern recurrence):** github.com apex-host network outage observed at iter-281 (blocking push of commit `6e0e810`) recurred at iter-290 (blocking Task 1 fetch). Both iters shared the same signature: `github.com` :443 + :22 blocked, `codeload.github.com` reachable (pinpointing the outage to `github.com` apex specifically). Next-iter probe: `timeout 10 curl -sI https://github.com` before any GitHub-dependent operation. If 3+ consecutive iters carry the outage, escalate to operator-interactive.
- **iter-290 observation (asymmetric-recovery LESSON reaffirmed):** iter-263 LESSON (SSH :22 / HTTPS :443 asymmetric recovery) attempted at iter-290 Task 1 — both paths FAILED this iter (symmetric outage, not asymmetric). The LESSON still applies as a diagnostic probe but iter-290 demonstrates it cannot be relied upon as a universal recovery path. Under a symmetric github.com apex-host outage, both :443 and :22 fail and the recovery is TIME-based (wait for outage to clear), not PROTOCOL-based (switch protocol).
- **iter-290 observation (sync-gate first-try GREEN post-rebuild — iter-257 LESSON stable):** `pnpm --filter @keel/keel-invariants build && pnpm keel-invariants:check` at iter-290 returned CLEAN on first-try; no false-positive drift on new `INV-devbox-legacy-branch-retention` entry. iter-257 LESSON (mandatory rebuild after manifest edit) validated stable across iter-290 — the second invariant-doc contentHash refresh since Story 2.13 (iter-283 was first; iter-290 is second within-lifecycle for new-doc landing).
- **iter-290 observation (invariant-doc registration three-site lockstep stable):** invariant-doc path (`docs/invariants/devbox-legacy-branch-retention.md`) + manifest entry + INVARIANTS.md H3 + anchor bullet all land in the same commit without drift. Anchor bullet regex (`/^-\s+\*\*\`(INV-[a-z0-9]+(?:-[a-z0-9]+)+)\`\*\*/gm`) GREEN first-try per Story 1.9 iter-7 LESSON (lowercase-after-`INV-` mandatory). Pattern stable across Story 2.12 iter-269 + Story 2.13 iter-283 + iter-290 = three consecutive doc-heavy-story landings.
- **iter-290 observation (iter-286 NOVEL LESSON #3 META guard applied at AGENTS.md cross-reference):** the AGENTS.md § Cross-reference bullet (e) carries the iter-288 PATCH-6 phrasing "grep at impl time + record in Completion Notes" verbatim — this is the documented META-guard pattern for adversarial-convergence-on-spec-endorsed-claim. The actual grep is owed when Task 1 runs; the AGENTS.md pointer surfaces the discipline. Zero DEFER added at iter-290 — the META-guard converts would-be DEFER into a forced verification at impl time. Novel application: applied at doc-authoring time rather than at fix-time (iter-288's PATCH-6 converted a found issue; iter-290 pre-empts a potential issue). Worth watching whether Task 1's actual grep surfaces the expected `curl :3000` or a different upstream state.
- **iter-286 NOVEL LESSON forecast-band carry-forward:** Story 2.14 cumulative PATCH count at iter-290 = 6 (iter-288 pre-dev SM) + 0 (iter-290 dev-story partial) = 6; forecast band ~6-10 across lifecycle; projected remaining 0-2 at post-dev SM + 0-2 at CR = ~6-10 total. ON-TRACK. Partial-landing at iter-290 does NOT inflate the band — the second dev-story iter (Task 1 retry) should ALSO be ZERO-PATCH (pure spec-execution; no deviation expected).
- **iter-285 LESSON stable:** same-commit citation drift audited at iter-290 — no Dockerfile-style line-number shifts in this iter (documentation-only landing; no line-anchor sensitive edits in substrate code files). Pattern holds: invariant-doc contentHash + manifest entry + INVARIANTS.md anchor + README.md + AGENTS.md all synchronous landing.
- **Cumulative DEFER forecast for Stories 2.14..2.17 Epic-2 close-out:** 31 pending at iter-290 (unchanged from iter-289). Story 2.14 iter-290 dev-story partial added 0 DEFERs (Task 1 is STATE-not-PATCH). Remaining Story 2.14 lifecycle (Task 1 retry + trace + post-dev SM + CR) + Stories 2.15-2.17 will accumulate additional. Absorption at Story 2.17 SC-17 close-out single-reconciliation pass.
