# Implementation Plan

## NOW

- [ ] Run `/bmad-create-story (args: "review")` — post-dev SM requirements-satisfaction verification. State `traced` → `sm-verified` (or `sm-fixes-pending`). ~medium

## QUEUE (Story 1.10 — design-token schema, semantic + rationale contract)

- [ ] Run `/bmad-code-review (args: "2")` — adversarial CR with action items. State `sm-verified` → `done` (or `fixes-pending`). ~medium

## BLOCKED

_(none)_

## DONE (Story 1.10 — iter-46 `/bmad-testarch-trace`)

- [x] iter-46: **`/bmad-testarch-trace (args: "yolo")` executed; FR14n state `in-dev → traced`.** Four trace artefacts written to `_bmad-output/test-artifacts/traceability/`: (a) **`1-10-coverage-matrix.json`** — Phase 1 full matrix with 4 P2 ACs at NONE coverage, tempCoverageMatrixPath anchor for Phase 2 reader; per-AC `level_notes` encode the substrate-verification-covers-AC hybrid-ground-(c) rationale (import-time Zod + sync-gate smokes + CR backstop). (b) **`1-10-e2e-trace-summary.json`** — machine-readable summary schema v1; `gate_status: WAIVED`; `p0_status/p1_status: MET`; `overall_status: NOT_MET`; 4 medium_gaps; `heuristics.ui_journey_status / ui_state_status: not_applicable` (zero UI surface). (c) **`1-10-gate-decision.json`** — slim gate signal mirroring 1-7/1-8/1-9 precedent posture; 450-char rationale enumerating the four Task 4 smoke branches (clean exit 0 + content-hash-mismatch exit 1 + added-to-source-only exit 1 + byte-identical revert) as AC 4 end-to-end evidence. (d) **`1-10-design-token-schema-semantic-rationale-contract.md`** — full trace report ~470 lines matching 1-8 precedent structure; four AC § Detailed Mapping entries with substrate_verification evidence bullets; § Gap Analysis records 4 medium gaps with explicit downstream-story deferral (AC-1 → Story 1.13 pre-commit schema-validation; AC-2 → Story 1.16 grep completeness; AC-3 → Story 1.13 source-output sync + Story 1.16 parity walker; AC-4 SUBSTRATE_VERIFIED); § Rationale seven-point enumeration citing hybrid-ground-(c) + fourth-cumulative FR14n ATDD-skip precedent + Story 1.9 sync-gate as AC-4 integration test. **Gate decision:** WAIVED (consistent with 1-7/1-8/1-9 precedent — four cumulative substrate-stage WAIVED gates). Rule-engine would emit FAIL on Rule 2 (overall 0% < 80%) — structural false-positive at pre-test-runner substrate stage. Fourth cumulative WAIVED precedent hardens the posture convention for contract-only/data-only Epic 1 stories. Pure prose + 4 new JSON/MD artefacts — zero source-code changes; zero re-run of quality gates (no TS/JS touched). Tracked at GitHub issue #34 — `Refs #34` in commit trailer; parent Epic 1 `Refs #9` optional. PR #226 CI: `no checks reported`; push safe.

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1–1.9 done; 1.10 `traced` at iter-46 via `/bmad-testarch-trace`; 1.11–1.16 backlog → 6 more stories × full FR14n lifecycle remaining after 1.10 closure before EPIC_DONE)
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (continues through Epic 1; PR #226 stays Draft until EPIC_DONE per PROMPT_build.md step 5c)
- **Story:** 1.10 — design-token schema, semantic + rationale contract
- **Story File:** `_bmad-output/implementation-artifacts/1-10-design-token-schema-semantic-rationale-contract.md`
- **Story State:** `traced` — FR14n matrix row 7 next action `/bmad-create-story (args: "review")` for `traced → sm-verified` (or `sm-fixes-pending` if unmet-AC findings). Contract-only story; expect single-pass SM review (analogous to Story 1.8 iter-5 post-dev SM review).
- **PR:** #226 (Draft, MERGEABLE, no-CI-checks-reported — stays Draft through Epic 1; Draft→Open at EPIC_DONE)

## Notes

- **Story 1.10 positioning.** First of 4 design-token-pipeline stories in Epic 1 (1.10 schema+rationale contract → 1.11 source population Direction A → 1.12 emitter pipeline → 1.13 quality gates). Story 1.10 is analogous to Story 1.8's role (`invariants.manifest.ts` contract) — contract-only, zero runtime behaviour, two artefact files + 2 manifest entries + 1 new INVARIANTS.md section. Per the contract-only / data-only pattern observed in Stories 1.3–1.6 + 1.8, expected to trend single-pass CR rather than the multi-pass behaviour of Story 1.9 (runtime sync-gate) + Story 1.7 (docs prose). Halving hypothesis from Story 1.9 was decisively falsified (iter-14 4 → iter-18 2 → iter-21 2 → iter-24 5 → iter-30 5 → iter-36 30 raw / 4 PATCH → iter-41 3 raw / 0 PATCH ZERO-OUT at round 7) — apply the four-layer carry-forward enumeration (sibling-artefact / SYMMETRIC-AC-PARALLEL / SIBLING-FIELD-PARALLEL / broader-corpus-vocabulary-propagation) from iter-22/24/30/36 RALPH Signposts + iter-36 two-layer-convergence-gates-PATCH-promotion rule to bound Story 1.10's CR loop tight.
- **Trace posture.** Gate WAIVED (fourth cumulative — 1.7 iter-5 + 1.8 iter-5 + 1.9 iter-8 + 1.10 iter-46). Rationale enumeration pinned to seven points: (1) stakeholder-approved waiver per § Testing Standards; (2) four ACs scoped-out to downstream stories (AC 1 → Story 1.13; AC 2 → Story 1.16; AC 3 → Story 1.13 + Story 1.16; AC 4 → Task 4 smokes + Story 1.9 runtime); (3) substrate verification strong (typecheck/lint/build/format:check/prek × 3 + 3 sync-gate smokes); (4) no test runner until Story 1.16; (5) FR14n ATDD-skip precedent is load-bearing (fourth cumulative); (6) Story 1.9 sync-gate is the AC-4 runtime integration test; (7) contract-only single-pass CR hypothesis (iter-7 Decisions).
- **SM-review expectation (next iter).** `/bmad-create-story (args: "review")` post-dev SM review checks requirements-satisfaction: do the implementation artefacts meet each AC as written? For Story 1.10: (AC 1) does packages/ui/tokens.schema.json define DTCG-compatible structure — YES per iter-45 Completion Notes; (AC 2) does docs/invariants/tokens.md carry TOKEN-<slug> rationale for every semantic token — YES per iter-45 + iter-45 Debug Log reconciliations (Accent six-leaf, Neutral group-level); (AC 3) are schema paths cross-linkable to rationale bullets — YES per iter-45 resolution; (AC 4) two manifest entries + two anchor bullets + sync-gate passes — YES per Task 4 smoke branches. SM-verify should pass first-round unless a cross-link parity gap the dev-agent missed surfaces. Story 1.8's post-dev SM review (iter-5) passed first-round; Story 1.10 should too.
- **Schema vs value separation.** Story 1.10 ships DTCG schema STRUCTURE + rationale PROSE only; Story 1.11 populates Direction A concrete values. This separation — contract-then-data — matches Story 1.8's posture (contract-then-enforcement in 1.9) and lets 1.10's ACs stay tight. Story 1.13's value-quality gates (schema-validation + WCAG AA contrast + source-output sync) run AFTER 1.11 source lands; Story 1.10 only needs to satisfy the structure-validates-DTCG claim of AC 1.
- **Two-entry manifest registration.** AC 4's epic phrasing at `epics.md:951` (`an entry INV-tokens-schema-contract registers tokens.schema.json + tokens.md`) conflicts with Story 1.8's single-`sourcePath`-per-`Invariant` shape. Resolution: two sibling manifest entries (`INV-tokens-schema-contract` → schema; `INV-tokens-semantic-rationale` → rationale doc), both anchored under a new `### Design-token schema + semantic rationale (Story 1.10)` section in `INVARIANTS.md`. Pattern matches `INV-eslint-shared` + `INV-eslint-import-boundary` (two IDs, shared `sourcePath`); here it's two IDs + two distinct `sourcePath` values. Documented as Story 1.10 § AC 4 scope carve-out in the story file.
- **AC 2 motion/density stable-ID carve-out (iter-43 pre-dev-review addition).** The epic's AC 2 shorthand `motion.scale.*` / `density.scale.*` is NOT a nested-group reference. Stable IDs resolve as: `TOKEN-motion-scale` (dial leaf) + `TOKEN-motion-{instant|snap|swift|smooth|drift}` (tier sibling leaves); `TOKEN-density-scale` (dial leaf) + `TOKEN-density-{compact|default|comfortable}` (tier sibling leaves). Three-segment IDs (`TOKEN-motion-scale-snap`) are ruled out — DTCG prohibits group nodes carrying `$type`, architecture.md:693 pins two-segment `<category>.<semantic-name>` pattern, and the story's Task 1 schema models the dial as a sibling of the tiers. Consistency enforced across the story's AC 2 + Task 1 schema + Task 2 rationale-doc sections.
- **Anchor regex landmines avoided** (Story 1.9 CR defer carry-forward — `deferred-work.md:37-38`): new `INVARIANTS.md` anchors use column-0 `- **\`INV-*\`**` form (regex binding at `packages/keel-invariants/src/sync-gate.ts:24`) and avoid fenced-code-block examples containing `INV-` bullets. Any rationale-doc example that needs to SHOW the anchor format uses inline backticks in prose, not triple-backtick fences.
- **Issue Tracking:** Story 1.10 tracked at GitHub issue **#34**. `Refs #34` in every commit trailer; `Closes #34` lands in PR body only when Story 1.10 reaches `done` (matrix row 11). Parent Epic 1 issue **#9** — `Refs #9` optional; never `Closes` it (ralph.py transitions epic issues to Done on EPIC_DONE halt).
