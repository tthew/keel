# Implementation Plan

## NOW

- [ ] **AI-13 — `env-check.sh:108` `${stripped#export }` single-space strip → shell-aware whitespace-class strip.** Replace the single-space strip with: `if [[ "${stripped}" =~ ^export[[:space:]] ]]; then stripped="${stripped#export}"; stripped="${stripped#"${stripped%%[![:space:]]*}"}"; fi`. Iteration-env-safe smoke: 4 harness cases — space-separator (baseline), tab-separator (`export<TAB>KEEL_DEVBOX_X=1`), double-space-separator (`export  KEEL_DEVBOX_X=1`), no-export-prefix (bare). Source-level drift-guard: regex anchor present; single-space-strip form absent post-fix. `bash -n env-check.sh` ✓. No invariant sourcePath touched (env-check.sh not in manifest). ~small.

## QUEUE (Story 2.6 closure drain → closure re-run → Epic 2 Story 2.7..2.17 → Epic 2 close-out)

- [ ] `/bmad-code-review (args: "2")` CR-closure RE-RUN after AI-12 + AI-13 drain — 2nd closure re-run. Forecast: ZERO-PATCH (Edge Case Hunter exhausted the AI-8 scope gap at iter-216; Acceptance Auditor already ZERO-FINDING; Blind Hunter NITPICKs all documented at fix-site with operator workarounds). On ZERO-PATCH → `Story State = done` next iter; within-epic auto-advance to Story 2.7 per § Cross-epic within-epic route.

- [ ] _(post-Story-2.6-done; through Story 2.17-done)_ Iterate Story 2.7..2.17 through the full lifecycle — 11 remaining substrate stories spanning ralph-auto-start + claude OAuth + gh OAuth + prereq-check + shared mode + port-publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.

- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.

- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-22 per iter-197..iter-204 pre-flight — "no checks reported on the 'feat/epic-2-packaged-devbox' branch").

## BLOCKED

_(none)_

## ATDD Red Phase

_(none — Story 2.6 ATDD-skipped at iter-200 per § Story Lifecycle `validated → atdd-scaffolded` (skip branch). 16th-cumulative FR14n ATDD-skip precedent. iter-202 trace gate WAIVED confirms the ATDD-skip-trace-WAIVED pairing — SEVENTEENTH cumulative pairing across Epic 1 + 2.)_

## DONE (Story 2.6 lifecycle — closure re-run drain AI-12 landed iter-217; iter-215 pruned)

- [x] iter-217: **AI-12 RESOLVED — `clean.sh:36` `unset COMPOSE_PROJECT_NAME` lands; AI-8 drain scope gap closed.** Mirrors the iter-212 9-script pattern verbatim: 10-line comment block + `unset COMPOSE_PROJECT_NAME` placed immediately after `set -euo pipefail`, before any `SCRIPT_DIR` / `COMPOSE_FILE` assignment → before every `docker info` + `docker compose down --rmi local [--volumes] --remove-orphans` invocation. Comment anchors the two collateral hazards driving AR-20's iter-216 promotion: (a) `--volumes` would target `${CPN}_keel_home_dev` (leaving canonical `keel-devbox_keel_home_dev` intact — silent no-op on operator intent, violating `INV-devbox-homedev-named-volume`); (b) `--remove-orphans` would remove any `${CPN}`-labelled container on the host daemon (collateral damage, acute under backend B). Iteration-env-safe env-shim smoke under workspace-local `.ai12-smoke/` (bypasses `/tmp` tmpfs noexec per Story 2.5 NFR8a, auto-cleaned): stub `docker` logs effective CPN on first invocation + answers `info` rc 0 + answers `info --format` with synthetic daemon-name bypassing well-known-host match; test path `clean.sh --with-volumes --yes --force-backend-b` with `COMPOSE_PROJECT_NAME=my-fork-ai12` exported; assertion `clean.sh first-call: CPN=<unset> set=` confirms unset precedes first docker call. Source-level ordering drift-guard: unset@L36 < first-docker@L125. `bash -n clean.sh` ✓. `clean.sh` not in any `packages/keel-invariants/invariants.manifest.ts` sourcePath → no sync-gate refresh. Quality gates: typecheck ✓, lint ✓, format:check ✓, keel-invariants:check-all ✓. deferred-work.md AR-20 updated: `CLOSED — AI-12 LANDED iter-217`. Story file Change Log v1.14 records the drain. Story State `fixes-pending` stays (1 PATCH remaining: AI-13).
- [x] iter-216: **CR closure re-run — NOT ZERO-PATCH. Breaks Epic-2 3-story precedent (2.3/2.4/2.5).** `/bmad-code-review (args: "2")` against `2f57265..HEAD` (27 files / 2120 insertions / 119 deletions — iter-201 landing + AI-1..AI-11 drain). Layer results: Blind Hunter ZERO-PATCH (4 NITPICK); Edge Case Hunter **2 PATCH + 3 NITPICK**; Acceptance Auditor ZERO-FINDING (all 8 ACs + uniform exit-code contract verified). Load-bearing finding: AI-8 scope gap — iter-212 added `unset COMPOSE_PROJECT_NAME` to the 9 CR-flagged container-discovery scripts but omitted `clean.sh`; `clean.sh:91` invokes `docker compose down --remove-orphans` + optionally `--volumes` (line 140) which is project-name-sensitive AND reaches unrelated containers. Edge Case Hunter re-triaged the iter-212 deferred AR-20 (clean.sh) as PATCH because `--remove-orphans` blast radius is higher than the iter-212 "silent no-op" analysis credited. Closure triage: **2 PATCH** (AI-12 clean.sh drain-gap, AI-13 env-check export-whitespace) + **7 DEFER** (AR-22..AR-28 mirrored to `deferred-work.md`; AR-20 updated with `PROMOTED TO AI-12 iter-217` suffix) + **0 DISMISSED**. Pre-selected "Create action items" routing (arg `2`) — no auto-fix; each PATCH becomes a § Story Lifecycle QUEUE fix task. Story State stays `fixes-pending` (QUEUE refilled with AI-12 + AI-13 + closure re-run); story file Status stays `in-progress`; sprint-status stays `in-progress`. Lesson extracted (see RALPH.md): **CR-closure forecast reliability caveat — "ZERO-PATCH forecast" is a distributional expectation, not a guarantee.** The 3-story precedent (2.3 iter-171 / 2.4 iter-183 / 2.5 iter-197) established a reasonable prior, but Story 2.6's 11 PATCH drain (vs 2-6 PATCH for 2.3/2.4/2.5) created more drain-fix surface area, and AI-8's text-narrow "9 scripts" scope created a gap the closure re-run exposed. Carry-forward for Stories 2.7+ with ≥10 scripts: when a PATCH has an explicitly-enumerated script list, audit adjacent scripts at drain time rather than relying on closure re-run to surface gaps.

## Context

- **Phase:** 4-implementation — Epic 2 at 5/17 stories `done` (2.1 + 2.2 + 2.3 + 2.4 + 2.5) + 12/17 remaining; Story 2.6 `fixes-pending` (11/11 original PATCH drained + AI-12 drained iter-217; 1 PATCH remaining — AI-13) + 11 backlog (2.7..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.6 live lifecycle smokes operator-workstation-deferred per Story 2.4 SC-17 + Story 2.5 iter-187 precedent. PATCH-drain quality chain for shell-script edits: `bash -n` per touched script + iteration-env-safe behavioural smoke per AI fix + sync-gate (when invariant sourcePaths touched) + typecheck + lint + format:check.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Story 2.5 substrate live. Story 2.6 PATCH drain is host-side script-only — no Dockerfile changes.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; 5 done (2.1-2.5); Story 2.6 `fixes-pending` (AI-12 landed iter-217; AI-13 pending + closure re-run pending); 11 backlog (2.7..2.17). Epic 2 closes at Story 2.17 (PR #230 Draft→Open transition at that point).
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.6 — host-side `pnpm devbox:*` CLI surface. Scope-owner for AR-7 `/run` relocation (SC-19, NOT triggered) + AR-9 `/etc/*` chown (SC-20, NOT triggered) + AR-10 pre-Story-2.5 volume migration docs (SC-21, LANDED iter-201) + AR-11 envvar shape validation (SC-14, LANDED iter-201) + AR-12 COMPOSE_PROJECT_NAME docs polish (SC-22, LANDED iter-201) + AI-1..AI-11 PATCH drain (LANDED iter-205..iter-215) + AI-12 PATCH (LANDED iter-217) + AI-13 PATCH drain (PENDING iter-218) + closure re-run (PENDING iter-219).
- **Story File:** `_bmad-output/implementation-artifacts/2-6-host-side-pnpm-devbox-cli-surface.md`
- **Story State:** `fixes-pending` — 11/11 original PATCH drained (AI-1 iter-205 .. AI-11 iter-215) + AI-12 drained iter-217. 1 closure-PATCH remaining (AI-13). Iter-218 drains AI-13; iter-219 runs 2nd closure re-run. On ZERO-PATCH 2nd closure → `Story State = done` iter-220 + within-epic auto-advance to Story 2.7.
- **GitHub Issue:** Story 2.6 TBD (RALPH_ISSUE_NUMBER unset in this iter env). Epic 2 → #10 (Ralph closes on EPIC_DONE halt).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **CR-closure forecast reliability caveat (iter-216 LESSON):** The 3-story ZERO-PATCH closure precedent (2.3 iter-171 / 2.4 iter-183 / 2.5 iter-197) is a distributional expectation, not a guarantee. Story 2.6 had 11 PATCH at opener (vs 2-6 for the 2.3/2.4/2.5 cohort) creating more drain-fix surface area; AI-8's text-narrow "9 scripts" scope + drain honouring the scope verbatim created a gap the closure re-run exposed. Carry-forward rule for Stories 2.7+ with ≥10 scripts OR with PATCH items that explicitly enumerate a script list: audit ADJACENT scripts for the same drift-class at drain time (i.e., the AI-fix iter) rather than relying on closure re-run to surface gaps. Specifically for AI-8-class drift (env-var scope gap): the drain iter should enumerate every script in `packages/devbox/scripts/` that invokes `docker compose` or `docker inspect` and apply the fix uniformly, flagging any intentional exclusions.
- **AR-20 promotion precedent (iter-216 LESSON):** Closure re-run CAN promote a previously-deferred item to PATCH if the blast-radius analysis shifts. iter-212 deferred clean.sh COMPOSE_PROJECT_NAME as "low-severity no-op"; iter-216 Edge Case Hunter weighted `--remove-orphans` collateral damage above that analysis. Promotion is legitimate triage, not triage-reversal; the audit trail updates the defer entry with `PROMOTED TO AI-N iter-M` suffix so the chain closes cleanly. Carry-forward: future deferred items whose analysis assumed a narrower blast radius deserve re-examination at closure re-run time, especially if new context (e.g., adjacent scripts discovered) would change the blast-radius estimate.
- **Counter-file stub pattern needs empty-string guard (iter-215 LESSON):** When writing a state-machine docker stub that progresses through values via a counter file, `: > counter` truncates to empty (not "0"); `cat ""` then yields empty string; `case "" in 0)` does NOT match (matches `*)` instead) → stub returns the default-arm value on first call, not the 0-indexed first value. Fix: `count=$(cat counter 2>/dev/null || true); count=${count:-0}` + initial `rm -f counter` (no file = default 0). Carry-forward for any future stub-based smoke that uses persistent counter state across invocations.
- **Soft-gate poll + fall-through pattern (iter-215 LESSON):** restart.sh's AI-11 fix uses a SOFT gate — on timeout, log the timeout AND proceed to start.sh regardless. This preserves forward progress under unreachable state (start.sh has its own 120s healthcheck bound), vs a hard-abort which would fail the whole restart. General rule for "wait-for-X-before-Y" gates in host-side scripts: if Y has its own downstream validation, the upstream gate can be soft. Applies to potential future poll-gates in Stories 2.7+ (Ralph auto-start readiness, claude OAuth handshake, gh auth status).
- **TTY-detect at docker-exec shim (iter-214 LESSON):** The `if [[ -t 0 ]]; then tty_flag="-it"; else tty_flag="-i"; fi` gate generalises — any host-side shim that invokes `docker exec` must TTY-detect when it wants to remain scriptable (CI / hooks / `sh -c` / `ssh host …`). Carry-forward rule for Stories 2.7+ (Ralph TUI wrapper / claude wrapper / gh wrapper / ssh wrapper): if the shim is expected to be callable from non-interactive contexts, apply the same pattern. Interactive-only shims (shell.sh, attach.sh) can keep hardcoded `-it` — interactive is the AC. **`script -qc '…' /dev/null` (util-linux) is the iteration-env-safe pty allocator** for smoking the TTY leg.
- **CR opener PATCH-count scales linearly with script-count, not story complexity (iter-204 LESSON, CONFIRMED iter-216):** Story 2.1 = 1 PATCH, Story 2.4 = 5 PATCH, Story 2.5 = 6 PATCH, Story 2.6 = **11 PATCH at opener + 2 NEW PATCH at closure re-run (13 host-side scripts)**. Iter-216 confirmation: CR-closure re-run surfaced 2 PATCH beyond the opener's 11, driven by scope gaps in the original 11. Carry-forward rule for Stories 2.7+ user-facing-CLI class: if the surface adds ≥10 new scripts, expect 8-12 PATCH at CR opener PLUS 0-3 PATCH at closure re-run (drift-class adjacency gaps). Drain-iter forecast scales similarly (Story 2.6 actual: 11-iter drain iter-205..iter-215 + 2 closure-drain iter-217..iter-218 + 2 closure re-run iter-216 + iter-219).
- **Within-epic § Story Lifecycle drain (iter-204 fixes-pending → iter-215 AI-11 landed → iter-216 closure re-run NOT ZERO-PATCH → iter-217..iter-218 AI-12 + AI-13 drain → iter-219 2nd closure re-run):** Story 2.6 `fixes-pending` absorbs the closure-surfaced PATCH pair; no halt required. Epic 2 stays `in-progress`. Within-epic auto-advance to Story 2.7 deferred to iter-220 on ZERO-PATCH 2nd closure.
- **Cross-source semantic-drift audit rule (iter-199 LESSON, VALIDATED across lifecycle gates 201/202/203/204/216):** SC-15 pinned PRD `:494` as authoritative for `pnpm devbox:monitor`. Pattern held clean through CR closure re-run at iter-216 (Acceptance Auditor confirmed AC 7 monitor-semantic satisfied). Carry-forward rule for Stories 2.7/2.8/2.9/2.12/2.15 remains live.
- **`<verb>-host.sh` host-side shim naming convention (iter-199 EXTENDED, iter-204 CR-VERIFIED, iter-216 CR-CLOSURE-VERIFIED):** Story 2.6 ships `monitor-host.sh` + `whitelist-host.sh` as canonical host-side shim naming. iter-216 closure re-run Edge Case Hunter identified `monitor-host.sh` missing `--user dev` pin (AR-28 DEFER — read-only so posture-only) — validates the shim naming held while surfacing a symmetry gap. Stories 2.7 (Ralph TUI wrapper), 2.8 (claude wrapper), 2.9 (gh wrapper), 2.12 (ssh wrapper) candidates for the same pattern.
- **Quoting-aware shell-parse `first_char` probe generalises (iter-210 LESSON):** The `first_char="${value:0:1}"` + `${rest%%${first_char}*}` pattern cleanly extracts quoted content without regex + avoids greediness pitfalls of `[[ =~ ^\"(.*)\"$ ]]`. Works identically for `"` and `'` via variable dispatch. Carry-forward: any future shell-parse work that needs quoting-aware value extraction can reuse this 3-line pattern (first-char probe → capture-to-matching-quote → trailing-ws strip). Applies to potential future parsers in `whitelist.sh` (rule-file values) and `start.sh` (status format strings) if the need arises.
- **Acceptance Auditor ZERO-FINDING holds even at upper-PATCH-count CR (iter-204 LESSON, RE-CONFIRMED iter-216):** Acceptance Auditor returned ZERO PATCH-or-higher AC deviations at BOTH the opener (iter-204) AND the closure re-run (iter-216) despite 11 PATCH at opener + 2 PATCH at closure re-run from Blind + Edge Hunters. CR-opener + CR-closure PATCH counts are code-quality issues (shell-script edge cases, error-handling gaps, scope gaps), NOT requirement-satisfaction issues. Carry-forward: when pre-dev + post-dev SM both close ZERO-FINDING, expect Acceptance Auditor at CR opener AND CR closure re-run to also close ZERO.
- **AR-12 docs-drift was only HALF the story (iter-204 LESSON, RE-EXTENDED iter-216):** iter-201 landed COMPOSE_PROJECT_NAME docs polish; iter-204 surfaced AI-8 (runtime container polling drift); iter-216 surfaced AI-12 (runtime `docker compose down` drift). Carry-forward rule: when an Edge Hunter flags an env-override drift in static docs, ALSO check whether the override breaks runtime script behaviour AT EVERY CALL-SITE across ALL scripts that invoke `docker compose` or `docker inspect`. The iter-212 AI-8 fix honoured the CR's text-narrow "9 scripts" scope; iter-216 closure surfaced the 10th (clean.sh) which was deferred at iter-212 as "safe no-op" but re-analyzed as collateral-damage risk.
