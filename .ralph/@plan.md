# Implementation Plan

## NOW

- [ ] **Re-run `/bmad-code-review (args: "2")`** for `fixes-pending → done | fixes-pending` confirmation gate. All 5 PATCHes landed iter-272..276 (PATCH-1 chmod-gosu-dev; PATCH-2 KEEL_DEVBOX_SSH_RESOLVED propagation; PATCH-3 subshell-wrap SC-10 posture; PATCH-4 rm-then-mv partial-keypair recovery; PATCH-5 compose-args.sh helper + 15-site refactor + DEFER-6 AR-9 citation correction). Per iter-264 LESSON Story 2.6 / 2.11 multi-pass CR cycle (Story ≥10-scripts-class), budget TWO closure re-runs at worst case. Forecast at re-run: 0-2 PATCH (CR-fix-introduced regression class — Story 2.6 iter-216 + 2.11 iter-264 precedents) + 0-3 LOW DEFER cosmetic carry-forwards. PATCH-5 introduced a new `lib/compose-args.sh` helper + 15 script edits + 4 doc updates (main-repo-resolver.sh / docker-compose.ssh.yml / AGENTS.md / docs/invariants/devbox-ssh.md with manifest contentHash re-pinned to 946f1ac1…907029 + dist rebuild) + story DEFER-6 correction — wide-surface change, CR-regression probability elevated slightly vs PATCH-1..4 (point-edit class).

## QUEUE (Story 2.12 fixes-pending → 0 PATCH remaining after iter-276)
- [ ] _(Stories 2.13..2.17)_ Iterate through full lifecycle — 5 remaining substrate stories spanning healthcheck (2.13) + legacy branch retention (2.14) + committed settings (2.15) + hooks (2.16) + bypass-resistance (2.17). Each runs full lifecycle; no cross-epic transitions until Epic 2 final story (2.17) completes.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-24 per iter-219..275 confirmation; iter-276 orient saw gh API HTTPS :443 timeout — iter-263 asymmetric-recovery LESSON applies; push via SSH :22 expected clean).

## BLOCKED

_(none — branch in sync with origin at iter-276 orient at `820591a`.)_

## ATDD Red Phase

_(none — iter-267 ATDD-skip-with-grounds-(c)+(ii)+(iii) per FR14n; 22nd-cumulative ATDD-skip precedent. Impl-time smokes at iter-268 dev-story cover ACs 1-3; ACs 4-5 operator-workstation-deferred.)_

## DONE (iter-276 Story 2.12 PATCH-5 LANDING — `fixes-pending` stays; final QUEUE-drain PATCH)

- [x] iter-276: **STORY 2.12 PATCH-5 LANDED — bash-array compose-args helper + 15-site refactor across 8 scripts + 4-doc idiom update + story DEFER-6 AR-9-citation correction.** Composite-scope landing honouring the iter-271 CR Acceptance-Auditor + Blind-Hunter MEDIUM-convergent PATCH-5 finding. Five-layer edit bundle:
  - **NEW helper** `packages/devbox/scripts/lib/compose-args.sh` (58 lines) — defines `resolve_compose_args()` which populates the `COMPOSE_ARGS` bash array (`(-f "${COMPOSE_FILE}")` + optional `(-f "${KEEL_DEVBOX_COMPOSE_FILE_SSH}")` when resolved-true). Header documents the hazard class (embedded-quote-under-unquoted-alt-expansion re-tokenisation) + explicit distinction from RALPH.md 2026-04-21 AR-9 (iterable word-splitting on IFS — different mechanism, different mitigation, the inline `${VAR:+…}` idiom does NOT mitigate PATCH-5 hazard). Contract: caller MUST source main-repo-resolver.sh → resolve_ssh_state THEN source compose-args.sh → resolve_compose_args; `COMPOSE_FILE` must be set. Mirrors Story 2.6 AR-19 / Story 2.7 SC-14 deferred `_lib.sh` extraction threshold (now crossed for compose-args concern specifically).
  - **15 script edits** across 8 files — each script: (a) adds `source "${SCRIPT_DIR}/lib/compose-args.sh"` + `resolve_compose_args` after the existing main-repo-resolver sourcing; (b) replaces `docker compose -f "${COMPOSE_FILE}" ${KEEL_DEVBOX_COMPOSE_FILE_SSH:+-f "${KEEL_DEVBOX_COMPOSE_FILE_SSH}"} <verb>` with `docker compose "${COMPOSE_ARGS[@]}" <verb>`. Tally exact: build.sh (1) / rebuild.sh (1) / start.sh (1) / stop.sh (1) / status.sh (1) / logs.sh (2) / clean.sh (2) / benchmark.sh (6) = 15 ✓. Post-edit grep `KEEL_DEVBOX_COMPOSE_FILE_SSH:\+` in `packages/devbox/scripts/*.sh` = 0 hits.
  - **4 doc updates** reflecting the new idiom — (i) `packages/devbox/scripts/lib/main-repo-resolver.sh:91-102` caller-wiring idiom rewritten (`COMPOSE_ARGS` + `"${COMPOSE_ARGS[@]}"`; prior inline form recorded as "superseded, word-split forks whose repo path contained whitespace"); (ii) `packages/devbox/docker-compose.ssh.yml:5-8` comment rewritten pointing at `lib/compose-args.sh`; (iii) `AGENTS.md:180` § Opt-in SSH rewritten to describe the array helper (prior inline form recorded as superseded); (iv) `docs/invariants/devbox-ssh.md:111-130` § Compose-CLI idiom rewritten as a full 6-line code block showing the source+call sequence + array idiom rationale + hazard-class distinction from AR-9.
  - **Manifest contentHash re-pinning + dist rebuild** — `docs/invariants/devbox-ssh.md` is contentHash-tracked by `INV-devbox-ssh`; edit forces re-sha256 + manifest update + dist rebuild. New hash: `946f1ac1b04bdde424df7ef46b1450b72481396cdeb8ab4038d85ae694907029` (was `e1d693cb…2a3c2`). `cd packages/keel-invariants && pnpm build` (tsc -b clean exit 0). Final `pnpm keel-invariants:check` GREEN (exit 0, zero output — sync-gate confirms sha256-of-source matches manifest-contentHash post-edit).
  - **Story DEFER-6 correction** — `_bmad-output/implementation-artifacts/2-12-…md:512` (v1.1 iter-266 pre-dev SM gate DEFER items list). Old text claimed `PATCH-2's ${VAR:+-f "${VAR}"} single-path idiom` mitigates the fork-with-whitespace-in-path hazard and cited AR-9. New text: strikethrough the claim, explain that AR-9 is a distinct hazard class (for-loop iterable word-splitting on IFS ≠ embedded-quote-under-unquoted-alt-expansion re-tokenisation), point at the actual fix (iter-276 PATCH-5 `compose-args.sh` helper). Preserves the DEFER history for traceability while correcting the misapplied citation per iter-271 CR finding.
  - **Verification:** (a) `bash -n` syntax-clean across all 8 modified scripts + new helper (9 files total) ✓; (b) 0 hits for `KEEL_DEVBOX_COMPOSE_FILE_SSH:+` in `packages/devbox/scripts/*.sh` ✓; (c) `pnpm keel-invariants:check` GREEN (exit 0) ✓; (d) `grep -c COMPOSE_ARGS packages/devbox/scripts/*.sh` = 15 (matches expected site count) ✓.
  - **Budget consumed:** ~42K tokens (orient ~10K + read 8 scripts + 3 docs ~14K + create helper ~3K + 15 Edits ~10K + 4 doc Edits ~4K + manifest+dist+story Edit ~3K + verification ~3K + IP rewrite + commit ~7K, stacking). Stayed within ~117K execution budget; exit cleanly per Guardrail 12 before starting any CR re-run.
  - **Story State:** `fixes-pending` stays (PATCH QUEUE now drained; next iter-277 re-runs CR for `fixes-pending → done | fixes-pending` confirmation gate per § Story Lifecycle).
  - **PR:** #230 **Draft** — `statusCheckRollup: []` carries unchanged across iter-272..275; iter-276 orient `gh pr view` call timed out on HTTPS :443 but branch is in sync with origin at `820591a` so push remains unblocked. Pre-push state: branch still at `820591a` (no push yet this iter); about to commit + push the 14-file bundle.
  - **NOVEL LESSON (hazard-class discrimination under word-splitting):** the `${VAR:+-f "${VAR}"}` alternate-value expansion re-tokenises by word-splitting BEFORE the embedded double-quote chars are re-parsed (they become literal `"` chars post-expansion) — distinct from AR-9's `for x in $(...)` iterable word-splitting on IFS. Citing AR-9 to justify not using a bash array is a CATEGORY ERROR. Any future "I'll just use `${VAR:+…}` to avoid AR-9" argument requires a re-check against the actual hazard-class surface. Promoted to RALPH.md § Lessons at iter-276.

- [x] iter-275: **STORY 2.12 PATCH-4 LANDED** — `packages/devbox/entrypoint.sh:176-187` partial-keypair recovery via `rm -rf host_keys` before `mv -T`. See commit `820591a` for full detail.
- [x] iter-274: **STORY 2.12 PATCH-3 LANDED** — subshell-wrap opt-in block for SC-10 non-fatal pre-spawn posture. See commit `0d83fae`.
- [x] iter-273: **STORY 2.12 PATCH-2 LANDED** — compose env via KEEL_DEVBOX_SSH_RESOLVED. See commit `1df64ab`.
- [x] iter-272: **STORY 2.12 PATCH-1 LANDED** — chmod under cap_drop via gosu dev. See commit `30b7d8d`.
- [x] iter-271: `/bmad-code-review (args: "2")` — `sm-verified → fixes-pending`; 5 PATCH + 11 DEFER + ~10 DISMISS; three-layer adversarial fan-out. See commit `a777224`.

_(iter-253..270 Story 2.10/2.11/2.12 closure + Story 2.12 drafting/pre-dev-SM/ATDD/dev-story/trace-gate/post-dev-SM iters pruned per Guardrail 2.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **11/17 stories done** (2.1-2.11) + 1/17 fixes-pending (2.12, PATCH QUEUE drained at iter-276) + 5/17 backlog (2.13..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered through iter-276 (PATCH-5 landed; static smokes GREEN).
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:120). Host-side shim count: **18** at iter-276 (unchanged — PATCH-5 edits existing shims + adds a non-shim lib file).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **11 done** (2.1-2.11); 1 fixes-pending-CR (2.12); 5 backlog (2.13..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.12 — Loopback-bound port publication + opt-in `KEEL_DEVBOX_SSH` sshd
- **Story File:** `_bmad-output/implementation-artifacts/2-12-loopback-bound-port-publication-opt-in-keel-devbox-ssh-sshd.md`
- **Story State:** `fixes-pending` — all 5 PATCHes landed at iter-272+273+274+275+276; next iter-277 re-runs `/bmad-code-review (args: "2")` for `fixes-pending → done | fixes-pending` confirmation gate.
- **GitHub Issue:** Story 2.12 issue unknown; `RALPH_ISSUE_NUMBER` unset at iter-276 orient. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` carried unchanged iter-272..275; iter-276 orient `gh pr view` HTTPS :443 timeout per iter-263 asymmetric-recovery — push via SSH :22 unaffected).

## Notes

- **iter-276 NOVEL LESSON (hazard-class discrimination under word-splitting):** `${VAR:+-f "${VAR}"}` alt-value expansion re-tokenises by word-splitting BEFORE embedded double-quote chars re-parse (they become literal post-expansion) — distinct from AR-9's `for x in $(...)` iterable word-splitting on IFS. Citing AR-9 to justify not using a bash array is a CATEGORY ERROR. Generalises: future "I'll just use `${VAR:+…}` to avoid AR-9" arguments require re-check against the actual hazard-class surface. Applies to any future Story 2.13-2.17 sub-pattern where resolver output threads into `docker`/`ssh`/`compose` CLI args. Promoted to RALPH.md § Lessons.
- **iter-276 observation (contentHash edit cost model):** editing a contentHash-tracked source (`docs/invariants/devbox-ssh.md`) forces a 4-step cost: edit source → sha256sum → manifest patch → `pnpm --filter @keel/keel-invariants build` → `pnpm keel-invariants:check`. Skipping any step makes the next iteration's orient see a drift-failing sync-gate (iter-257 LESSON precedent). For PATCH-5's scope, the cost was small (~3K tokens for the 4-step); for larger contentHash-tracked edits, pre-budget the cost explicitly. Not novel enough to promote — iter-257 LESSON already covers the rebuild/check necessity.
- **iter-275 observation (`mv -T` over non-empty target dir fails deterministically — rm-then-mv is the idiomatic recovery):** GNU `mv -T` refuses to overwrite non-empty target directory; recovery idiom is `rm -rf <dst> && mv -T <src> <dst>`. Well-known GNU mv behaviour; not promoted.
- **iter-274 observation (subshell-wrap-with-||-diagnostic):** canonical non-fatal-failure isolation pattern under `set -euo pipefail`; `( <commands> ) || <diagnostic>` scopes `set -e` so a single `||` at close handles every possible inner failure uniformly. Well-known bash idiom; not promoted.
- **iter-273 NOVEL LESSON carry-forward (compose env-propagation sites are normalisation chokepoints):** any `docker-compose.yml` env var sourced from `.envrc` via `${VAR:-default}` MUST source the RESOLVED host-side value when a case-folding resolver exists. Applies to future resolver-gated knobs (2.11 SHARED, 2.10 PREREQ_*). Promoted to RALPH.md § Lessons.
- **iter-272 LESSON carry-forward (orient-time PATCH order decision framework):** evaluate (a) verification-bar parity, (b) Guardrail 5 clean-ness, (c) downstream reduction, (d) independence. iter-272 chose linear PATCH-1..5; iter-276 confirms linear order was correct (PATCH-5 wide-surface + contentHash-tracked doc made it the right terminal step). Promoted to RALPH.md § Lessons.
- **iter-271 NOVEL LESSON carry-forward (forecast band breach via novel-runtime-behaviour surface):** 4-7 PATCH band for novel-runtime-behaviour stories; Story 2.12 final count: 5 PATCH (within band). Applies to 2.13 (healthcheck), 6.x (RLS debugger), Epic 13 (CI harness smokes).
- **iter-271 LESSON carry-forward (three-subagent pattern reaffirmed):** Blind / Edge / Auditor parallel-fan-out; convergence-signal-high-confidence-triage; all 5 PATCHes were ≥2-layer convergent.
- **iter-270 NOVEL LESSON carry-forward (drift-band re-baseline):** substrate-citation drift BIMODAL — substantive 0-2 + mechanical 8-12 per dev-pass-touches-≥4-cited-files story.
- **iter-270 LESSON carry-forward (two-subagent pattern reaffirmed):** post-dev SM AC-verifier (Amelia) + drift-detector (Winston) PROVEN at THIRD cumulative application.
- **iter-268 NOVEL LESSON carry-forward:** post-crash recovery VERIFY+COMMIT (not re-invoke); 5-check verification.
- **iter-267 precedent-match carry-forward:** 22nd-cumulative ATDD-skip-with-grounds-(c)+(ii)+(iii); Story 2.5 iter-186 canonical pattern.
- **iter-266 NOVEL LESSONS carry-forward:** container-loopback ≠ host-loopback; pre-dev SM PATCH-count CAN break iter-221 ~5 ceiling for novel-runtime-behaviour stories (band 6-10).
- **iter-264 LESSON carry-forward:** CR re-run DEFER-vs-PATCH severity-class-first; 0-3 first-class PATCH band applies to STATIC-SUBSTRATE stories; 4-7 band to NOVEL-RUNTIME-BEHAVIOUR stories.
- **iter-263 LESSON carry-forward:** SSH :22 / HTTPS :443 asymmetric recovery; iter-276 HTTPS :443 timeout observed; push via SSH :22 expected clean.
- **iter-257 LESSON reaffirmed at iter-276:** `pnpm keel-invariants:check` runs `dist/check.js`; rebuild required after manifest edit. iter-276 ran `pnpm --filter @keel/keel-invariants build` post-manifest-patch; sync-gate GREEN.
- **iter-244/246 audit-findings carry-forward:** restart.sh transitive-delegate; benchmark.sh OUT-OF-SCOPE (SC-11) threading compose-override; Manifest entry count **32** at iter-276 (unchanged; contentHash at `INV-devbox-ssh` edited in-place).
- **Substrate-citation drift cumulative forecast for Stories 2.13..2.17:** ~5-12 cumulative substantive + ~40-60 cumulative mechanical drifts. SC-17 single reconciliation at Story 2.17 landing. Story 2.12 adds a CR-iter-induced drift class: iter-276 PATCH-5 replaced 4 doc-site idiom citations (main-repo-resolver.sh comment / docker-compose.ssh.yml comment / AGENTS.md entry / devbox-ssh.md § Compose-CLI idiom) — no further drift carry-forward for the 15 script sites (fully refactored in-place).
