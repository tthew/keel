# Implementation Plan

## NOW

- [ ] **Run `/bmad-create-story (args: "review")` fresh-context pre-dev validation for Story 2.11** (Ralph-internal `Story State` = `drafted → validated` per § Story Lifecycle Decision Matrix row `drafted → validated`). Read Story 2.11 file `_bmad-output/implementation-artifacts/2-11-per-fork-vs-shared-devbox-mode-keel-devbox-shared.md` (iter-254 landing; 4 ACs + 6 Tasks + 17 SCs; novel surface = mode-resolver extension + compose project parameterisation + env-check orphan-warning + AC 4 concurrency doctrine), checklist-validate per `.claude/skills/bmad-create-story/checklist.md` (disaster-prevention + LLM-optimisation analysis), apply any improvements the validator surfaces. One workflow per iteration (Guardrail 5). Forecast: ~25-40K (orient + skill dispatch + story-file re-read + checklist-validation subagent(s) + any accepted improvements + IP + commit).

## QUEUE (Story 2.11..2.17 → Epic 2 close-out)

- [ ] _(after Story 2.11 `validated`)_ `Run /bmad-testarch-atdd` — red-phase scaffolds for AC 1-3 (AC 4 concurrency is doc-only; per-AC trace waive forecast). Story State `validated → atdd-scaffolded`. Forecast: ~40-55K. Red-phase failures go to IP § ATDD Red Phase and do NOT block the next transition (Guardrail 4 exception).
- [ ] _(after Story 2.11 `atdd-scaffolded`)_ `Run /bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-11-per-fork-vs-shared-devbox-mode-keel-devbox-shared.md")` — impl of Tasks 1-6 in a fresh context window. Story State `atdd-scaffolded → in-dev`. Forecast: ~70-100K (6 tasks; 18-shim wire-in is mechanical composition on Stories 2.6/2.8/2.9/2.10 precedent; resolver-function extension + compose edit + env-check warning + INV doc authorship are the novel surface). May split across iters (`in-dev (partial)` re-queue per FR14n).
- [ ] _(after Story 2.11 `in-dev`)_ `Run /bmad-testarch-trace (args: "yolo")` — AC→test coverage gate + traceability matrix. AC 4 partial-waive candidate (doc-only concurrency decision). Story State `in-dev → traced` OR `trace-fixes-pending` (fix tasks per coverage gap).
- [ ] _(after Story 2.11 `traced`)_ `Run /bmad-create-story (args: "review")` post-dev SM verification. Story State `traced → sm-verified` OR `sm-fixes-pending`. Two-subagent verification pattern per iter-235 LESSON.
- [ ] _(after Story 2.11 `sm-verified`)_ `Run /bmad-code-review (args: "2")` adversarial CR via three-layer Ralph-hosted triage (Blind Hunter `bmad-agent-architect` diff-only + Edge Case Hunter `bmad-tea` diff+project-read + Acceptance Auditor `bmad-agent-dev` diff+spec+INV). Story State `sm-verified → done` OR `fixes-pending`. Chain-reset forecast: Stories 2.11..2.17 begin fresh ZERO-PATCH-CR forecast cycle per iter-253 LESSON (0-1 PATCH + 2-4 second-class DEFERs band-aligned).
- [ ] _(after Story 2.11 `done`)_ Iterate Stories 2.12..2.17 through full lifecycle — 6 remaining substrate stories spanning loopback-bound port publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-23 per iter-219..254 repeated pre-push confirmation — `gh pr view 230` returns `statusCheckRollup: []`).

## BLOCKED

- [ ] iter-249+250+251+252+253+254 push to `origin feat/epic-2-packaged-devbox` — REASON: SSH :22 to github.com has been intermittently timing out across 6 consecutive iter attempts (`ssh: connect to host github.com port 22: Connection timed out` at iter-249/250/251/252/253 step 5; iter-254 step 5 attempt pending). HTTPS :443 GraphQL `gh pr view 230` INTERMITTENT: failed iter-253 orient + pre-push (`dial tcp 140.82.121.5:443: i/o timeout`); SUCCEEDED iter-254 orient (`{"isDraft":true,"mergeStateStatus":"CLEAN","state":"OPEN","statusCheckRollup":[]}`). 9 commits locally on `feat/epic-2-packaged-devbox` post-iter-254 (was 7 after iter-253; +2 from iter-253-BLOCKED-IP + iter-254-Story-2.11-draft).
  - Attempted: push attempts at iter-249/250/251/252/253 all failed with SSH :22 timeout to github.com.
  - Error/Issue: persistent egress block on SSH :22 to github.com from iter env across 5 consecutive iters; HTTPS :443 to api.github.com intermittent (succeeded iter-252 orient + iter-254 orient, failed iter-253 orient + pre-push); git push hard-wired to SSH remote URL.
  - Next: iter-254+ continues banking on intermittent SSH :22 recovery. **Operator escalation now strongly recommended after 5 consecutive iter failures** — one-off `git remote set-url --push origin https://github.com/tthew/ralph-bmad.git` on the host (or environmental egress unblock for SSH :22 to github.com `140.82.121.4/5/3/6/22`) to unblock the iter-254 backlog. Ralph SHOULD NOT autonomously reconfigure the remote URL (shared-state mutation beyond single-iter scope per § Executing actions with care). Commits are safe in local branch — no force-push, no amend, preserved intact across iter-249..254; on operator unblock, `git push` flushes all 9 commits in a single round-trip with no conflict (each commit non-destructive).

## ATDD Red Phase

_(none — Story 2.11 ATDD not yet queued; lifecycle at `drafted`. Red-phase scaffolds land at `/bmad-testarch-atdd` step later; forecast non-skip per Story 2.11 § Testing Standards.)_

## DONE (iter-254 Story 2.11 create-story — Story State `_(no story) → drafted`; sprint-row `backlog → ready-for-dev`)

- [x] iter-254: **STORY 2.11 CREATE-STORY LANDING — `/bmad-create-story` fresh-context auto-advance from Story 2.10 closure (iter-253 § Cross-epic within-epic route).** Skill workflow executed: Step 1 auto-discovered Story 2.11 as the first `backlog` row in sprint-status (`2-11-per-fork-vs-shared-devbox-mode-keel-devbox-shared`); Epic 2 already `in-progress` (no epic status change needed). Steps 2-5 loaded epics.md § Story 2.11 block (lines 1512-1538) + PRD FR4 (`prd.md:930`) + architecture § Workspace mount (`architecture.md:547`) + PRD CLI-Tool Surface § Devbox scope (`prd.md:504`) + existing substrate (`.envrc.example:41,48-49` knob publications; `docker-compose.yml:22,39,80-107,239` Story 2.11 forward-compat markers; `lib/main-repo-resolver.sh:56-100` existing resolver pattern; `clean.sh`/`env-check.sh`/`prereq-check.sh` consumer sites). Step 5 wrote story file to `_bmad-output/implementation-artifacts/2-11-per-fork-vs-shared-devbox-mode-keel-devbox-shared.md` (~30KB) with 4 ACs (verbatim from epics.md) + 6 Tasks (resolver extension / compose parameterisation / 18-shim wire-in / env-check orphan warning / INV-devbox-mode manifest+doc / operator+agent docs) + 17 SCs (volume-name invariant preservation / resolver single-site discipline / shared-mode container-name opinionation / exit-code schema stability / warning three-site lockstep / parent-dir bind scope documentation / etc.) + Dev Notes with AC 4 concurrency decision (serialised Ralph TUI; parallel non-Ralph exec) alternatives A/B/C pinned + Testing Standards + References (19 source citations) + Dev Agent Record placeholder + Change Log v1.0. Step 6 flipped sprint-row `2-11-…: backlog → ready-for-dev` + added comment `# last_updated: 2026-04-23 Story-2-11-ready-for-dev-iter-254 UTC` + updated trailing `last_updated` field; IP § Context + § NOW + § QUEUE updated. **No PRD amendment** — FR4 at prd.md:930 + architecture § Workspace mount at architecture.md:547 verbatim matches Story 2.11's contract; substrate-only delivery. **Forecast carry-forward from iter-253 LESSON:** CR PATCH forecast 0-1 first-class PATCH + 2-4 second-class operator-edge DEFERs (chain reset per Story 2.10 post-close; Stories 2.11..2.17 begin fresh ZERO-PATCH-CR forecast cycle with iter-251/253 LESSONS now baked into impl-time discipline). Story State `_(no story) → drafted`. Push status: HTTPS GraphQL `gh pr view 230` SUCCEEDED at iter-254 orient (fluctuating from iter-253's fail); SSH :22 not yet re-tested (will attempt push at step 5; iter-254 BLOCKED entry pre-emptively logged). Budget iter-254: ~45K (orient ~10K + epics.md + PRD + architecture slice reads + story-file write ~30KB + sprint-status + IP + RALPH.md signpost + commit ~5K).

_(iter-243..253 Story 2.9 closure + Story 2.10 lifecycle iters pruned per Guardrail 2 — see Story 2.10 § Change Log v1.0–v1.7 + RALPH.md Signposts for per-gate detail.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **10/17 stories done** (2.1-2.10) + 1/17 drafted (2.11) + 6/17 backlog (2.12..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered at iter-248 9/9 stub-docker harness + iter-252 9/9 mock-docker PATH-shim harness.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:119). Host-side shim count: **18** at iter-254 (unchanged from iter-253 — Story 2.11 does NOT add new shims; it extends the 18 existing ones with a resolver invocation).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **10 done** (2.1-2.10); 1 drafted (2.11); 6 backlog (2.12..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.11 — Per-fork vs shared devbox mode (`KEEL_DEVBOX_SHARED`)
- **Story File:** `_bmad-output/implementation-artifacts/2-11-per-fork-vs-shared-devbox-mode-keel-devbox-shared.md`
- **Story State:** `drafted` — Story 2.11 file landed at iter-254 via `/bmad-create-story` auto-advance from Story 2.10 closure; next iter pre-dev validation via `/bmad-create-story (args: "review")` transitions `drafted → validated`.
- **GitHub Issue:** Story 2.11 likely unwired (`RALPH_ISSUE_NUMBER` unset at iter-254 check; skip `Refs #<story-issue>` in commits). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out, NOT iter-254; Story 2.11 draft ≠ Epic 2 close).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — last confirmed `{"isDraft":true,"mergeStateStatus":"CLEAN","state":"OPEN","statusCheckRollup":[]}` at iter-254 orient; PR state unchanged (no merge events expected during BLOCKED push window).

## Notes

- **iter-254 Story 2.11 create-story landing — mechanical inheritance from Story 2.10 draft pattern.** Single-workflow iteration (Guardrail 5); `/bmad-create-story` auto-discovered the backlog row; wrote the story file with 4 ACs + 6 Tasks + 17 SCs; flipped sprint-row + IP + RALPH.md signpost. Resolver-function extension + compose `name:` parameterisation are the novel surface vs prior Epic 2 stories (Stories 2.1-2.10 did NOT modify `lib/main-repo-resolver.sh` or the compose `name:` key). Novel: shared mode's opinionated container-name hardcode (SC-4 — ignores operator `KEEL_DEVBOX_CONTAINER_NAME` override); parent-dir bind expanding blast radius beyond fork root (SC-7 + shared-mode bind scope § Dev Notes § Relevant architecture patterns).
- **iter-253 Story 2.10 CR re-run closure — NOVEL LESSON on PATCH-iter SECOND-class DEFER pattern** (carry-forward, applies to Story 2.11 CR forecast). PATCH iterations close FIRST-class adversarial findings but commonly surface SECOND-class operator-edges in the same surface the prior bug had hidden. Forecast-actionable: Story 2.11's resolver-function + compose parameterisation + env-check orphan warning constitute "multi-site novel runtime probe" surface; expect 2-4 second-class DEFERs at CR re-run (Docker Desktop wake-race, Podman-compat semantics, compound-failure short-circuit doctrine carry-forward forecast classes).
- **iter-252 Story 2.10 PATCH-1 — `set -e`+`if cmd; then` rc-suppression LESSON** (carry-forward). Story 2.11's resolver function does NOT invoke multi-step runtime probes (assignment-only branching on `KEEL_DEVBOX_SHARED`); not a recurrence risk. BUT env-check orphan-container probe (Task 4) DOES invoke `docker inspect` with rc capture — impl MUST use the canonical `rc=0; docker inspect … || rc=$?; case $rc in …` pattern per Story 2.10 PATCH-1 to avoid re-surfacing the rc-suppression class.
- **iter-252 GOTCHA — iter-env `/tmp` is `noexec`** (carry-forward, applies to Story 2.13 healthcheck emitter + Story 2.14 legacy-branch cleanup; NOT applicable to Story 2.11 which does not use mock-binary PATH shims).
- **iter-244/246 audit-findings carry-forward** unchanged from iter-247..253: (1) restart.sh transitive-delegate prepend-only; (2) benchmark.sh OUT-OF-SCOPE clarification (SC-11); (3) Manifest entry count drift (29→30 baseline at iter-248 → 30→31 at Story 2.11 landing after `INV-devbox-mode` entry); (4) Alpine egress non-applicability doctrine (SC-4 + § Alpine probe image). Story 2.11 adds `INV-devbox-mode` → manifest count becomes 31 at Story 2.11 landing (dev-story step).
- **Substrate-citation drift forecast for Stories 2.11..2.17 (updated from iter-251/253):** Story 2.11 touches `docker-compose.yml:39` (`name:` parameterisation is L-stable; surrounding comment-block amendments are L-stable within their paragraph). Forecast: Stories 2.11..2.17 accumulate 1-3 drifts each, deferred to Story 2.17 SC-17 polish pass. Total Epic 2 close-out reconciliation forecast: ~10-20 cumulative line-number drifts unchanged.
