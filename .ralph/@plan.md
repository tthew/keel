# Implementation Plan

## NOW

- [ ] `/bmad-testarch-atdd` — FR14n row 3 `validated → atdd-scaffolded` for Story 1.13; SEVENTH cumulative ATDD-skip CANDIDATE per Story 1.7-1.12 precedent (ground-(c) hybrid variant-(ii)+(iii) clause holds for gate-authoring story; Story 1.13 gates are CLI substrate-tooling verified via Task 7 negative smokes — no runtime-probe testing required until Story 1.16 test-runner landing). If skip-declared, record `skip: <rationale>` in IP + transition straight to `atdd-scaffolded` per PROMPT `/bmad-testarch-atdd` skip carve-out. ~small.

## QUEUE (Story 1.13 — design-token quality gates)

- [ ] `/bmad-dev-story (args: "{story_file_path}")` — FR14n row 4 `atdd-scaffolded → in-dev`; single-pass CANDIDATE under preventative-audit pre-resolution (Story 1.12 iter-63 precedent). Larger blast radius than Story 1.12: 3 new files + 2 file amendments + 4 config file edits + 7 manifest entries touched (3 new + 4 hash-PATCH). Decomposition risk: if dev-story partials at Task 3 (emitter amendment) or Task 5 (manifest growth), re-queue with `in-dev (partial)` per FR14n row 4 partial-completion semantics.
- [ ] `/bmad-testarch-trace (args: "yolo")` — FR14n row 5 `in-dev → traced`; SEVENTH WAIVED CANDIDATE; structural inheritance from Story 1.11/1.12 applies (6-AC scale, identical to Story 1.12 scaling envelope).
- [ ] `/bmad-create-story (args: "review")` post-dev — FR14n row 7 `traced → sm-verified`; THIRD cumulative ZERO-PATCH CANDIDATE per iter-58/iter-65 doctrine if preventative-audit discipline holds through dev.
- [ ] `/bmad-code-review (args: "2")` — FR14n row 9 `sm-verified → done`; ZERO-PATCH + 10-15 DEFER forecast per iter-66 scaling (gate-authoring × 3-gate + manifest cross-cuts × 7-entry footprint = larger-than-emitter surface; DEFER count skews higher).

## BLOCKED

_(none)_

## DONE (current story/phase only)

- [x] iter-68: **`/bmad-create-story (args: "review")`; FR14n state `drafted → validated` per matrix row 2.** Pre-dev SM review of Story 1.13 spec-file. **TWO in-place PATCHes** applied to resolve AC 4 internal contradiction (a drafting-residual from a pre-convergence sync-gate design where AC 4's bullet list assumed a separate `check-tokens-sync.ts` CLI file but Task 5 + § AC 4 carve-out + code block + Dev Notes all converged on "emitter IS the sync gate in `--check` mode" via shared sourcePath with `INV-tokens-emitter`): (PATCH 1) Line 58 AC 4 bullet — `INV-tokens-sync-gate` sourcePath corrected from `packages/keel-invariants/src/check-tokens-sync.ts` (non-existent CLI file) → `packages/ui/scripts/generate-tokens.ts` (shared with `INV-tokens-emitter`). (PATCH 2) Line 61 AC 4 closer — "three new source files line up byte-for-byte" → "three source files — two new CLI entries plus the shared-sourcePath emitter — line up byte-for-byte with their `contentHash` values" (only 2 NEW source files; the third pins the amended emitter). Both within ≤2 PATCHes target matching Story 1.11 iter-54 (1 PATCH) + Story 1.12 iter-61 (2 PATCHes) pre-dev SM precedent. No scope changes, no new ACs, no Task shape changes — pure AC-coherence fix. v1.1 Change Log row appended documenting the fix + cross-file verification outcome. Cross-file verification systematic sweep (14 citation groups via Explore subagent at ~thorough thoroughness): 13 of 14 groups ✓ all-verified; 1 group (AC 4) surfaced the contradiction. All other line-number citations (epics.md 1018-1045 Story 1.13 + 982-1016 Story 1.12 + 954-981 Story 1.11 + 925-953 Story 1.10; prd.md FR43:1009 + FR41:1007 + NFR20:1094 + NFR27:1108; architecture.md cross-runtime 85-90; 1-12-...md lines 38 + 50 + 179 PATCH targets; manifest.ts ranges 3-28 / 89-93 / 97-101 / 105-109 / 129-134 / 145-150 / 153-158 all present; sync-gate.ts:24 `ANCHOR_REGEX`; check.ts:1-13 CLI precedent; INVARIANTS.md:57-61 insertion point; tokens.json:167-215 $modes.dark overlay; generate-tokens.ts `resolveValue` 360-384; .pre-commit-config.yaml 4-hook structure; sprint-status.yaml story key + :2 + :38 sites; deferred-work.md iter-66 10-defer mapping AA1/AA2/AA3/#4/#5/#6/#7/#8/#9/#10; tokens.schema.json `$defs.leafDimension` + `breakpointGroup`) all verified. Sprint-status unchanged (stays `ready-for-dev` — that field is the drafting-downstream signal, set once at iter-67; FR14n-state carries the lifecycle phase in IP Context). Story State: `drafted → validated`. **Pre-dev SM gate PASSED with 2-PATCH delta.** Budget spent: ~65K tokens (within forecast); prior-art delta trackable via Story 1.11 iter-54 (1 PATCH) + Story 1.12 iter-61 (2 PATCHes) — Story 1.13 lands at same iter-61 precedent despite larger surface (6-AC + 7-Task + 14→17 manifest growth + 7-defer absorption). Next iter-69: `/bmad-testarch-atdd` — SEVENTH cumulative ATDD-skip CANDIDATE.

- [x] iter-67: **`/bmad-create-story`; FR14n state `_(no story) → drafted` per matrix row 1.** Authored `_bmad-output/implementation-artifacts/1-13-token-quality-gates-schema-validation-wcag-aa-contrast-source-output-sync.md` (~790 lines). 6 ACs + 7 Tasks + exhaustive Dev Notes + 28 References + v1.0 Change Log row. **Seven preventative-audit layers pre-applied at drafting time** (iter-53/54/56/59 + iter-60 L7): (L1) stable-IDs — three new IDs `INV-tokens-schema-validate` / `INV-tokens-contrast-check` / `INV-tokens-sync-gate`; sync-gate shares sourcePath with `INV-tokens-emitter` per Story 1.8 shared-sourcePath precedent (eslint.config.keel-invariants.js hosts INV-eslint-shared + INV-eslint-import-boundary). (L2) carry-forward defer mapping — 7 of 10 iter-66 CR defers absorbed (AA1 → Task 4 Story 1.12 § AC 1 literal amendment; AA2 → Task 4 Story 1.12 § Task 6 prettier-list amendment + `.prettierignore` contract Dev Notes; AA3 → Task 3/5 manifest description PATCH `(emitter re-run + diff) → (emitter --check mode)`; defer#4 → Task 3 SHA-resolver hoist to main(); defer#5 → Task 3 failure-mode distinction `git-unavailable` / `stderr-error` / `uncommitted` prefixes; defer#6 → Task 3 diamond-DAG guard via resolved-leaf-identity `inProgress` Set; defer#9 → Task 4 `leafBreakpoint` schema def with `^\d+px$` pattern). Not absorbed: defer#7 `walkLeaves` non-leaf drop → ordering carve-out at § AC 1 (schema-gate-before-emitter); defer#8 REPO_ROOT → reliability follow-up / Epic 3; defer#10 fontSize lineHeight → Epic 7/3. Story 1.11 iter-59 defers absorbed: #1-3 (contrast) → § AC 2 tokens.json retunes (info.fg L 52→42; warning.fg L 58→44; 5 status.fg dark overlay additions + text.accent + border.accent dark remaps to accent.400); #4 shadow-machinery → re-deferred (Story 1.17-TBD / Epic 7); #5 gamut → § AC 2 carve-out gamut-map before AA math. (L3) sprint-status transition wording — direct `backlog → ready-for-dev` + dual-site update (`:2` header + `:38` data-field) pre-specified in Task 7. (L4) pinned every multi-choice dev decision: Ajv-2020 vs Zod (Ajv; schema file is source-of-truth); sync-gate verb `--check` (not `re-run + diff`; canonicalises AA3); OKLCH→sRGB math (inline Ottosson 2020 matrix; zero dep); gamut-map algorithm (10%-chroma-reduce × 3 iter + hard-clamp; OkLab hue preservation); pair-enumeration (static typed table, not programmatic; ~60-70 pairs enumerated in § Task 2); per-pair threshold default 4.5 AA normal, 3.0 for border/accent non-text UI; sync-gate mechanism (emitter IS the sync gate when flagged — shared sourcePath; avoids walker-drift); pre-commit hook files-filter `^packages/ui/tokens\.json$` (not `always_run: true`); umbrella script `keel-invariants:check-all` composes Story 1.9 + Story 1.13 for Epic 13 CI wiring. (L5) cross-file line-numbers verified at Read time (epics.md:1018-1045 Story 1.13 spec; prd.md FR41-44 + NFR20/27; architecture.md:85-90 cross-runtime; 1-12-...md:38 AC1 literal + :179 Task 6 prettier + :50 fallback; invariants.manifest.ts:89-93/97-101/105-109/129-134/145-150/153-158 for six PATCH targets; INVARIANTS.md:57-61 insertion point; sync-gate.ts:24 ANCHOR_REGEX; .pre-commit-config.yaml hook structure). (L6) `leafBreakpoint` schema def addition non-breaking (additive; `leafDimension` stays for space/radius/type). (L7) gamut-map-before-AA-math absorbed as § AC 2 carve-out — explicit algorithm citation (Ottosson 2020 OkLab); not opt-out this time (gate REQUIRES gamut-map for meaningful contrast math). Cross-cutting manifest PATCHes enumerated: 3 new entries + 3 description/hash PATCHes (Stories 1.10/1.11/1.12) + 4 hash-only PATCHes (Stories 1.4/1.5 due to `.pre-commit-config.yaml` + root `package.json` edits). Budget spent ~65K tokens (within ~50-55K forecast + context for carry-forward mapping). Sprint-status bumped: `1-13-...: backlog → ready-for-dev`; `last_updated: 2026-04-21 Story-1-13-ready-for-dev UTC` (both `:2` header-comment + `:38` data-field sites). Story State: `_(no story) → drafted` (FR14n matrix row 1). **Story 1.13 drafting complete.** Next iter-68 runs `/bmad-create-story (args: "review")` for pre-dev SM review — target ≤2 PATCHes matching Story 1.11 iter-54 + Story 1.12 iter-61 precedent.

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1–1.12 done; 1.13 drafted iter-67 + SM-validated iter-68; 1.14–1.16 backlog → 3 more stories × full FR14n lifecycle remaining before EPIC_DONE halt).
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (continues through Epic 1; PR #226 stays Draft until EPIC_DONE per PROMPT_build.md step 5c).
- **Story:** Story 1.13 — Token quality gates (schema validation + WCAG AA contrast + source-output sync)
- **Story File:** `_bmad-output/implementation-artifacts/1-13-token-quality-gates-schema-validation-wcag-aa-contrast-source-output-sync.md`
- **Story State:** `validated` — FR14n matrix rows 1 (iter-67) + 2 (iter-68) complete; row 3 pending at iter-69 `/bmad-testarch-atdd` (SEVENTH cumulative ATDD-skip CANDIDATE).
- **GitHub Issue:** Story 1.13 issue TBD (ralph.py resolves at iter-69 start if not already at iter-68 start — depends on parse-lag). Parent Epic 1 at **#9** (OPEN — closes at EPIC_DONE halt). Stories 1.11 / 1.12 issues closed on PR merge.
- **PR:** #226 (Draft, MERGEABLE, no-CI-checks-reported — stays Draft through Epic 1; Draft→Open at EPIC_DONE after Stories 1.13–1.16 all complete FR14n lifecycle).

## Notes

- **Story 1.13 forecast: 7-iteration floor CANDIDATE** (matches Story 1.11 iter-53..iter-59 + Story 1.12 iter-60..iter-66 each at exactly 7 iters). Target: iter-67 drafted → iter-68 validated → iter-69 atdd-scaffolded → iter-70 in-dev → iter-71 traced → iter-72 sm-verified → iter-73 done. Risk-weighted alternative (if dev-story partials): +1 iter for `in-dev (partial) → in-dev` re-queue, reaching iter-74 done.
- **Carry-forward rules INTO Story 1.13 + downstream (iter-68+) — COMPOUNDING PRIOR ART (unchanged from iter-66):**
  - **Seven preventative audit layers** (iter-53/54/56/59/60). Applied at drafting; re-apply at SM review + CR.
  - **Story 1.11/1.12 ZERO-PATCH doctrine** — "structurally impossible + fully documented + does not violate AC text = ACCEPT"; three-prong test scales to SM + CR + command-literal-vs-intent disputes.
  - **Multi-layer-corroborated authoring-choice findings are DEFER, not PATCH** (iter-59 + iter-66 at 10-DEFER ceiling). Story 1.13 CR-target: 10-15 DEFER at gate-authoring scale.
  - **Single-layer BH/EH findings default DISMISS unless corroborated** (iter-36 + iter-52 + iter-59 + iter-66 at 26 dismisses).
  - **Structural trace inheritance ~90% savings** (iter-46 + iter-57 + iter-64; scales 4-AC → 6-AC). Story 1.13 6-AC scale — inheritable at iter-71.
  - **Scoped-build optimisation** — after manifest mutation, `pnpm --filter @keel/keel-invariants build` before `pnpm keel-invariants:check`. Applies at Task 5 manifest growth 14→17 + 4 hash PATCHes; scoped-build + umbrella `keel-invariants:check-all` recompose cleanly.
  - **Drift-smoke revert patterns** (iter-45 / iter-56 / iter-63). Task 7 three negative smokes (schema / contrast / sync) each follow this pattern.
  - **Ground-(c) hybrid variant-(ii)+(iii)** unlocks ATDD-skip + trace-WAIVED for substrate+tooling stories with runtime-probe deferral + spec-declared CR-substitution clause. Story 1.13 qualifies: no runtime probes at 1.0; CR is the substitute-for-test-runner gate. Target: 7th cumulative ATDD-skip + 7th cumulative WAIVED trace.
  - **Compound-ZERO-PATCH achievable under 5 conditions** (iter-66): (a) seven preventative audit layers; (b) pre-dev SM ≤2 PATCHes; (c) single-pass dev; (d) clean trace; (e) CR defenses pre-staged. Story 1.13 drafting pre-applied all seven layers; conditions (b)-(e) land at iter-68+.
  - **CR DEFER ceiling scales** (iter-66 NEW): populator ≈5 DEFER; emitter/gate ≈8-12 DEFER. Story 1.13 forecast: 10-15 DEFER (gate-authoring + 3-gate surface × manifest cross-cuts).
  - **`/bmad-code-review (args: "2")` arg `2` = "Leave as action items"**; vacuous in ZERO-PATCH case but required to force the non-interactive branch.
- **Story 1.13 DEFER absorption roadmap (FROM iter-66 deferred-work.md):** 7 defers absorbed — AA1 spec amendment (Task 4) / AA2 spec amendment + .prettierignore contract (Task 4) / AA3 manifest description (Task 3+5) / resolveSourceSha hoist (Task 3) / resolveSourceSha failure-mode distinction (Task 3) / alias-cycle diamond-DAG guard (Task 3) / walkLeaves non-leaf via schema-gate ordering (§ AC 1 carve-out) / parseInt breakpoint via schema regex pin (Task 4). 3 defers re-routed: REPO_ROOT → reliability follow-up/Epic 3; fontSize lineHeight → Epic 7/3; contrast-retune completeness via Story 1.11 iter-59 defer absorption at Task 2 (info.fg/warning.fg/5×status.fg dark overlays/text.accent+border.accent dark remaps to accent.400/gamut-map before AA math).
- **Story 1.13 NOT absorbing (route elsewhere):**
  - BH+EH REPO_ROOT brittle → reliability follow-up story OR Epic 3 (`packages/ralph` install-boundary scope).
  - BH+EH fontSize hardcoded lineHeight 1.5 → Epic 7 Story 7-1 (consumer) OR Epic 3 Story 3-X.
  - Shadow-machinery (iter-59 defer #4): Story 1.17-TBD or Epic 7 follow-up.
- **Issue Tracking carry-forward.** Parent Epic 1 at **#9** OPEN (never `Closes`; Ralph transitions at EPIC_DONE halt). Story 1.12 at **#36** OPEN-pending-PR-merge (closes on PR merge at EPIC_DONE). Story 1.13 issue TBD (ralph.py creates on next iter after `**Story:**` field set in IP Context — happens at iter-68 start). iter-67 commit trailer uses `Refs #9` (parent-epic fallback; `RALPH_ISSUE_NUMBER` env var likely still unset per ralph.py IP-Context-Story parse-lag since Story 1.12 done at iter-66 and Story 1.13 drafted fresh at iter-67).
