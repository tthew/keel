# Implementation Plan

## NOW

- [ ] Run `/bmad-create-story (args: "review")` — post-dev SM requirements-satisfaction verification for Story 2.2. State transition `traced → sm-verified` OR `sm-fixes-pending`. Pattern-match precedent: Story 2.1 iter-127 SM review (one ENHANCEMENT carve-out absorbed; 2.1 has operator-owned AC 3/4 backend-B carve-out — Story 2.2 has NONE because `docker compose config` is pre-daemon YAML-time + AC 5 sync-gate + lint rule fully in-iteration-executable). Expect ZERO-PATCH or ≤1-ENHANCEMENT scope (hybrid infrastructure-smoke + configuration-surface class; five AC substrate probes all verifiable structurally at trace time already). ~small (skill runs in its own fresh-context iteration; Ralph's iteration is orient + queue + commit + push).

## QUEUE (Story 2.2 `traced → sm-verified → done`)

- [ ] Run `/bmad-code-review (args: "2")` — post-SM CR adversarial action-items gate. State transition `sm-verified → done` OR `sm-verified → fixes-pending` (multi-cycle envelope bounded by Story 2.1's iter-128 → iter-138 → iter-144 three-cycle precedent; Story 2.2 forecast: TIGHTER envelope than 2.1 because (a) no operator-owned carve-out to adjudicate, (b) five AC substrate probes ALL live-verified at trace time, (c) check-no-committed-dotfiles.ts is 36 lines of Node-only code — minimal adversarial attack surface vs 2.1's image + compose + shell + markdown + manifest cross-cutting surface).
- [ ] _(conditional, post-Story-2.17 done)_ Transition PR #230 Draft→Open — final CI gate after all Epic 2 stories done; monitor CI; merge.
- [ ] _(conditional, post-Epic-2 merged)_ § Cross-epic transition → Epic 3 Story 3.1 via `/bmad-create-story` OR halt EPIC_DONE awaiting merge.

## BLOCKED

_(none — Story 2.2 trace clean WAIVED per twelfth cumulative Epic precedent; lifecycle continues via SM-review → CR gates.)_

## DONE (Story 2.2 — iter-145 draft, iter-146 review-with-fixes, iter-147 ATDD-skip, iter-148 dev-story, iter-149 trace)

- [x] iter-149: **`/bmad-testarch-trace (args: "yolo")` WAIVED — TWELFTH cumulative Epic WAIVED precedent; Story State `in-dev → traced`.** Four artifacts authored in `_bmad-output/test-artifacts/traceability/`: `2-2-coverage-matrix.json` (Phase 1 matrix — 5 P2 ACs, 0% automated, STRONG substrate evidence per-AC), `2-2-e2e-trace-summary.json` (Phase 2 schema-v1 portable summary), `2-2-gate-decision.json` (slim gate JSON), `2-2-envrc-parameterisation-contract.md` (full trace report matching Story 2.1 iter-126 template shape with ~95% structural inheritance). Decision rationale: hybrid ground-(a)-(b)-(c)-(d) variant-(ii)+(iii) — (a) substrate-verification-covers-ACs at CLI-exit-code level LIVE at iter-149 [AC 1: `.envrc.example` 3601 bytes + 14-line header + 5-section grouping + 6th substrate-knob section; AC 2: `docker compose config` exit 0 with defaults (cpus 8, mem_limit 12884901888 = 12 GB, shm_size 2147483648 = 2 GB, platform linux/arm64, nofile 65536, 4 ports host_ip 127.0.0.1) + knob-alignment diff compose ⊆ `.envrc.example` empty; AC 3: `KEEL_DEVBOX_MEMORY_GB=16 KEEL_DEVBOX_CPUS=4` override renders cpus 4 + mem_limit 17179869184 (16 GB) at YAML-substitution time; AC 4: `.secrets.example` 1595 bytes + 6-key scaffold architecture.md:328 verbatim; AC 5: lint-rule positive (exit 0) + negative (exit 1 with stderr pointer) + `git check-ignore` schemas (exit 1 stageable) + payloads (exit 0 ignored) + sync-gate (exit 0; 4 contentHash updates accepted)]; (b) no-runner — framework prerequisite unmet per iter-147 ATDD-skip; (c) HYBRID variant-(ii)+(iii) Epic 13 test-runner landing + § Testing Standards spec-declared-CR-substitution delegates AC 1-5 adversarial coverage to iter-CR; (d) upstream-provenance-precedent inherited from Story 2.1 iter-98 (upstream cc-devbox has no test suite). **CRITICAL DIFFERENCE from Story 2.1 iter-126**: Story 2.2 has NO operator-owned carve-out. Story 2.1 AC 3 + AC 4 required a running container (compose run pnpm test/lint + benchmark.sh cold+warm) which hit backend-B bind-mount denial; Story 2.2 AC 1-5 are ALL pre-daemon YAML-time operations + static file checks + sync-gate check + regex-match lint rule + `git check-ignore` — fully in-iteration-executable. Residual risk NEGLIGIBLE (substantially lower than Story 2.1's iter-126 LOW). Only residual item: prek-level end-to-end hook exercise deferred to operator workstation (prek binary not installed; node-direct substitute covers script-layer identically). Budget: ~25K tokens (orient ~5K + 4 artifacts authored ~15K + IP/RALPH updates + Change Log + live-verification smokes ~5K — substantially leaner than iter-126 ~30K because 2.2 has no operator-carve-out to adjudicate). PR #230 CI: no checks reported (pre-Epic-13). Next iter: `/bmad-create-story (args: "review")` for `traced → sm-verified`.

- [x] iter-148: **`/bmad-dev-story` landed Story 2.2 implementation in a single iteration (all 8 Tasks complete; AC 1–AC 5 green); Story State `atdd-scaffolded → in-dev`.** Full detail in `ff8121a` commit message.

- [x] iter-147: **`/bmad-testarch-atdd` ATDD-SKIP — TWELFTH cumulative Epic precedent** — full detail in `44e4ce2` commit message.

- [x] iter-146: **`/bmad-create-story (args: "review")` pre-dev SM validation applied 9 fixes** — full detail in `cc9aee8` commit message + story file v1.1 Change Log.

- [x] iter-145: **`/bmad-create-story` produced story file draft (Story State: `_(no story) → drafted`; sprint-status `2-2: backlog → ready-for-dev`).** Full detail in `fd03e1d` commit message.

## Context

- **Phase:** 4-implementation — Epic 2 open at 1/17 stories done (2.1 done at iter-144); Story 2.2 `traced` as of iter-149.
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available` § Backend contract). Iteration-context bind-mount denial persists — but Story 2.2's substrate evidence is NOT bind-mount-dependent (unlike Story 2.1 AC 3/4), so the denial is non-blocking at trace time. Operator-workstation resolves prek binary install via native prek + `pnpm install && pnpm exec prek run` when needed.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Version matrix in `packages/devbox/VERSIONS.md § Bake log`.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; Story 2.1 done; Story 2.2 `traced` (implementation landed iter-148; trace landed iter-149; SM + CR gates remain); Stories 2.3..2.17 in `backlog`.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2; PR #230 transitions to Open at epic completion).
- **Story:** 2.2 — `.envrc` parameterisation contract.
- **Story File:** `_bmad-output/implementation-artifacts/2-2-envrc-parameterisation-contract.md`.
- **Story State:** `traced` — iter-149 `/bmad-testarch-trace (args: "yolo")` authored 4 artifacts + WAIVED gate per twelfth cumulative precedent; all 5 ACs substrate-verified LIVE; no operator-owned carve-out. Next iteration: `/bmad-create-story (args: "review")`.
- **GitHub Issue:** Story 2.2 issue **#42** (https://github.com/tthew/ralph-bmad/issues/42). Parent Epic 2 → #10.
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2; no CI runners pre-Epic-13; transitions to Open at Story 2.17 done).

## Notes

- **DinD is fork-time substrate, not transitional.** `INV-devbox-dind-available` — every fork's cc-devbox-equivalent must provide Docker. Podman/rootless-Docker conformant if daemon reachability holds (backend C extension per iter-144 defer).
- **Backend B is the reference environment at 2026-04-21.** Host socket-passthrough. Broad-state-mutation scripts MUST detect backend + refuse destructive ops by default (`benchmark.sh` reference from iter-122). Bind-mount compose runtime requires host-shared File Sharing allowlist coverage — operator-owned from iteration context. **Story 2.2 trace surfaces the first "NO operator-owned carve-out" infrastructure-smoke story** — `docker compose config` is pre-daemon YAML-time, not bind-mount-dependent.
- **NFR2 authority unchanged.** AC 4 cold ≤ 300s / warm ≤ 30s remains authoritative on M4-Pro native. Story 2.2 is infrastructure-parameterisation — no runtime-performance impact on NFR2.
- **Story 2.2 parameterises knobs for Story 2.5 (tmpfs) + Story 2.11 (shared) + Story 2.12 (loopback ports / SSH).** Those downstream stories activate the tmpfs / shared / SSH knobs that Story 2.2 adds to `.envrc.example`.
- **Twelfth cumulative Epic WAIVED precedent validated** (iter-149). **Cumulative chain: Stories 1.7 iter-4 + 1.8 iter-3 + 1.9 iter-3 + 1.10 iter-46 + 1.11 iter-57 + 1.12 iter-64 + 1.13 iter-71 + 1.14 iter-78 + 1.15 iter-85 + 1.16 iter-92 + 2.1 iter-126 → 2.2 iter-149.** Twelfth cumulative ATDD-skip precedent validated (iter-147; second Epic 2). Exhaustive-context-engine story-creation pattern validated (iter-145). Pre-dev SM validation with parallel-subagent-review pattern validated (iter-146). Single-iteration dev-story landing pattern validated (iter-148). **Pre-daemon YAML-time substrate evidence pattern validated** (iter-149; first no-carve-out infrastructure-smoke story — distinct from Story 2.1's bind-mount-dependent carve-out class). Cross-epic auto-advance pattern validated (iter-96). Partial-completion pattern validated (iter-99). Backend-B safety-gate pattern established (iter-122). Review-with-operator-owned-carve-out pattern validated (iter-125). SM-verified-with-operator-owned-carve-out pattern validated (iter-127). CR-fixes-pending-adversarial-default pattern validated (iter-128). CR-re-run-adversarial-default + action-item-bundling pattern validated (iter-138). Doc-drift two-row bundle closure pattern validated (iter-141). Story-artefact spec-vs-code drift closure pattern validated (iter-142). Operator-UX polish pattern validated (iter-143). ZERO-PATCH third-gate close pattern validated (iter-144; three-cycle upper envelope for Epic-2 infrastructure-smoke stories).
