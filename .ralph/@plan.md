# Implementation Plan

## NOW

- [ ] `/bmad-create-story (args: "review")` — pre-dev SM requirements-satisfaction verification of Story 2.10 per § Story Lifecycle Decision Matrix row `drafted → validated`. Fresh context window. Story 2.10 drafted at iter-245 (`/bmad-create-story` fresh-context, 5 ACs + 6 Tasks + 17 SCs; file `_bmad-output/implementation-artifacts/2-10-prerequisite-check-docker-runtime-claude-auth-gh-auth-with-pointer-errors.md`; sprint-status flipped `2-10-...: backlog → ready-for-dev`). Forecast: 2-5 PATCHes at SM gate per iter-221 LESSON `~5-PATCH` ceiling, wider band than prior Epic-2 stories because Story 2.10 has novel surfaces (multi-shim refactor SC-11; first Epic-2 new-INV entry SC-12; alpine:3.19 probe image SC-4/SC-7; tier-dispatch argv). Budget forecast: ~60K (orient + 4 parallel Sonnet subagents + triage + surgical Edits + v1.1 Change Log + IP/RALPH.md updates + commit).

## QUEUE (Story 2.10..2.17 → Epic 2 close-out)

- [ ] _(post-Story-2.10-validated)_ Iterate Story 2.10 through remaining lifecycle: validated → atdd-scaffolded (FR14n ATDD-skip forecast under ground-(a)+(b)+(c) conjunction — TWENTIETH cumulative precedent candidate) → in-dev (impl landed: new prereq-check.sh + 17 shim edits + INV-devbox-prereq-check manifest+doc + README/AGENTS) → traced (trace WAIVED forecast — TWENTIETH cumulative precedent candidate) → sm-verified (post-dev SM; substrate-citation drift two-subagent verification pattern per iter-235 LESSON) → done (CR closure; candidate FIFTH one-pass ZERO-PATCH precedent if pattern holds under novel multi-shim-edit surface).
- [ ] _(post-Story-2.10-done)_ Iterate Stories 2.11..2.17 through full lifecycle — 7 remaining substrate stories spanning shared mode + port-publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-23 per iter-219..244 repeated pre-push confirmation — `gh pr view 230` shows `statusCheckRollup: []`).

## BLOCKED

_(none)_

## ATDD Red Phase

_(none — Story 2.9 closed done at iter-244; Story 2.10 ATDD-skip forecast for iter-247 under ground-(a)+(b)+(c) — TWENTIETH cumulative precedent candidate.)_

## DONE (iter-245 Story 2.10 DRAFT — Story State `_(no story)_ → drafted`; sprint-status `2-10-...: backlog → ready-for-dev`)

- [x] iter-245: **STORY 2.10 DRAFTED via `/bmad-create-story` fresh-context per § Story Lifecycle matrix row `_(no story) → drafted` intra-epic auto-advance** (Epic 2 has 7 more backlog stories after 2.10). Deliverable: new story file `_bmad-output/implementation-artifacts/2-10-prerequisite-check-docker-runtime-claude-auth-gh-auth-with-pointer-errors.md` (~480 lines) with **5 ACs + 6 Tasks + 17 SCs** + full Dev Notes + References + Dev Agent Record template + Change Log v1.0. **Novel surfaces vs Stories 2.7/2.8/2.9** (wider forecast band at pre-dev SM + CR gates): (a) **multi-shim refactor** — SC-11 wires `prereq-check.sh --tier1` into 15 existing host-side shims + `--tier2` into 2 ralph shims (17 total shims touched; first such story in Epic 2); (b) **first new Epic-2 manifest entry** — `INV-devbox-prereq-check` at `packages/keel-invariants/src/invariants.manifest.ts` + new `docs/invariants/devbox-prereq-check.md` authoritative doc (Story 2.3 iter-156 multi-faceted-invariant-doc LESSON applied — doc has 6 H2 sections so each sub-contract hashes into `contentHash`); (c) **first non-`<verb>-host.sh` host-side primitive** — `prereq-check.sh` naming follows Story 2.3/2.4 primitive pattern (`reload-egress.sh`, `whitelist.sh`), NOT `<verb>-host.sh` which is reserved for pnpm-verb wrappers per Story 2.6 SC-2 naming convention; (d) **alpine:3.19 probe image** — first story using a Docker image as runtime probe dependency (SC-4/SC-7); Renovate `docker` manager tracking + egress whitelist coverage (`*.docker.io` baseline) need cross-verification at impl time; (e) **tier-dispatch argv** — first host-side shim with multi-tier argument surface (`--tier1` / `--tier2` / default tier2). **Design decisions pinned in SCs:** SC-2 upstream Docker Desktop owns install path (URL `https://docs.docker.com/desktop/install/` hardcoded at 1.0); SC-3 filesystem-based token probe (existence only via `test -e /vol/.claude/.credentials.json` + `/vol/.config/gh/hosts.yml`; not validity — validity is upstream's concern); SC-5 composite-message aggregation within Tier 2 + Tier 1 fails fast + exit-code schema `0`/`2`/`8`/`12` extends Story 2.6 uniform; SC-10 volume-name derivation `${KEEL_DEVBOX_COMPOSE_PROJECT:-keel-devbox}_keel_home_dev`; SC-12 Story 2.10 IS the first story-scoped entry in the emerging `_lib.sh` refactor trajectory (iter-239 retrofit was non-story-scoped); SC-13 no compose/Dockerfile/entrypoint edits; SC-14 ralph.py TUI unchanged (Epic 3 scope); SC-15 no token content inspection; SC-17 read-only on Stories 2.6/2.7/2.8/2.9 README + AGENTS.md sections. **Sprint-status flipped** `2-10-...: backlog → ready-for-dev`; two comment-trail lines + live `last_updated` updated to `Story-2-10-ready-for-dev-iter-245`. **PR #230 stays Draft** across Epic 2 per IP § Context (unchanged). **No RALPH.md lesson append at drafting gate** — create-story is deterministic skill-dispatch with research digest + template mirror; no novel Ralph-flavored surprise surfaced. **Next iter per § Story Lifecycle matrix row `drafted → validated`:** `/bmad-create-story (args: "review")` fresh-context — pre-dev SM PATCH forecast 2-5 (wider band than Stories 2.7/2.8/2.9's 2-5 due to novel multi-shim + new-INV + probe-image surfaces). Budget iter-245: ~100K (orient + single Explore subagent research digest + template read + story file write ~480 lines + sprint-status edits + IP update + commit + push).

_(iter-234..244 Story 2.9 lifecycle gates pruned per Guardrail 2 — see Story 2.9 file § Change Log v1.0–v1.6 for per-gate details; iter-244 CR closure was the final Story 2.9 marker.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **9/17 stories done** (2.1-2.9) + 8/17 backlog (2.10 `ready-for-dev` at iter-245, 2.11..2.17 still backlog).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live OAuth AC 1-4 smokes operator-workstation-deferred per ground-(c) carry-forward (Stories 2.8/2.9 precedent; Story 2.10 applies ground-(c) to Docker-reachability + Stories 2.8/2.9 token-persistence-substrate).
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:119). Host-side shim count: **17 verbs** at Story 2.9 closure; Story 2.10 does NOT add a `<verb>-host.sh` but adds **1 shared primitive** `prereq-check.sh` (non-verb-pattern) → directory count 18 host-side files + 6 in-container primitives = 24 under `packages/devbox/scripts/` at Story 2.10 landing.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **9 done** (2.1-2.9); 1 in-progress drafting (2.10); 7 backlog (2.11..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.10 — Prerequisite check (Docker runtime + Claude auth + gh auth) with pointer errors.
- **Story File:** `_bmad-output/implementation-artifacts/2-10-prerequisite-check-docker-runtime-claude-auth-gh-auth-with-pointer-errors.md`
- **Story State:** `drafted` — next iter action per § Story Lifecycle Decision Matrix row `drafted → validated` is `/bmad-create-story (args: "review")` fresh-context window; skill does NOT flip sprint-status (pre-dev SM gate is Ralph-internal state, not sprint-row state — mirrors trace gate + post-dev SM gate precedent).
- **GitHub Issue:** Story 2.10 likely unwired (`RALPH_ISSUE_NUMBER` unset at iter-245 check; skip `Refs #<story-issue>` in commits). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2; `statusCheckRollup: []` at iter-242..244 pre-push confirmation; CLEAN merge-state; `gh pr view` at iter-245 orient attempt timed out with `api.github.com` network I/O timeout — transient network hiccup, IP state authoritative from iter-244 check).

## Notes

- **iter-245 Story 2.10 drafting — `_(no story)_ → drafted` transition complete per § Cross-epic transition within-epic route (not cross-epic halt).** Novel surfaces relative to Stories 2.7/2.8/2.9 forecast 2-5 PATCHes at pre-dev SM gate (wider band than prior 2-5). Causal chain from Stories 2.6/2.7/2.8/2.9 one-pass ZERO-PATCH precedent: dense pre-dev SM density (≥2 PATCHes) → verbatim spec-template impl → zero-drift trace-WAIVED → ZERO-PATCH post-dev SM → ONE-PASS ZERO-PATCH CR — pattern chain-length 4 at iter-244. Story 2.10 candidate for FIFTH precedent if pattern holds under novel multi-shim-edit + new-INV surface. **Wider-band forecast rationale:** novel surfaces at Story 2.10 are (a) multi-shim refactor edit scope (17 shims), (b) first Epic-2 new-INV entry (manifest + doc + sync-gate), (c) alpine probe image dependency (Renovate + whitelist coverage cross-verify), (d) tier-dispatch argv — each introducing new pre-dev-SM attack vectors.
- **iter-243/244 carry-forward — post-dev SM substrate-citation drift pattern continues to apply.** The iter-235 LESSON's prospective framing ("drift can be introduced by implementation edits to substrate files between SM gates") validated at iter-243 (13 drifts surfaced + deferred per SC-17 polish convention) + reconfirmed at iter-244 CR (Auditor verified content resolves at all cited locations despite line-number drift). Forecast: Story 2.10 post-dev SM will surface comparable drifts as iter-238..241 retrofits accumulate more substrate changes on the branch; resolution policy uniform — defer line-number reconciliation to Story 2.17 Epic 2 close-out per SC-17.
- **iter-244 CR triage yielded 11 deferred items (AR-60..AR-70) all pointing at post-Epic-2 `_lib.sh` refactor.** Story 2.10 is the first story-scoped entry in the emerging `_lib.sh` refactor trajectory — iter-239 retrofit extracted `lib/main-repo-resolver.sh` + `lib/check-mount-source.sh` as non-story-scoped first slice; Story 2.10 ships `prereq-check.sh` as the first story-scoped primitive. Forecast: Story 2.17 Epic 2 close-out may include a broader `_lib.sh` consolidation task to collapse remaining duplicated pre-flight boilerplate across all 17 host-side shims (container-state inspect, `log()` function, `CONTAINER_NAME` fallback) before Epic 3 adds further shims on top. Story 2.10 is NOT that refactor — it is narrowly scoped to adding the prereq-check primitive + wiring.
- **iter-235 carry-forward — two-subagent verification pattern IS the reference implementation for post-dev SM substrate-citation audits.** Source-file claims + planning-artefact claims in parallel via `Explore` subagents returning VERIFIED / DRIFTED / MISSING verdicts. Applied at iter-243 post-dev SM with full effect (14 VERIFIED + 13 DRIFTED surfaced). Story 2.9's FOURTH ZERO-PATCH precedent suggests the pattern is substrate-stable; Story 2.10 SHOULD re-apply verbatim at its post-dev SM gate.
- **iter-234 carry-forward — killed-before-commit skill recovery: inspect disk state before re-invoking.** Unchanged from iter-243/244 carry-forward; valid recovery primitive for fresh-context skill invocations whose primary output is committed artefacts. iter-245 entry confirmed clean (no residual iter-244 artefacts; `git status` only showed untracked `design_handoff_tthew_website/` — orthogonal, ignored).
- **iter-221 carry-forward — pre-dev SM `~5-PATCH` ceiling is independent of draft-SC density.** Story 2.9 landed at low-end 2 PATCHes (iter-235) vs forecast 3-7 band; the narrower novel surface drove the lower count. Story 2.10 has WIDER novel surface than 2.9 (see Notes first bullet) → forecast 2-5 PATCH band is appropriate, not tight like 2.9.
- **TTY-detect at docker-exec shim (iter-214 LESSON) — Story 2.10 NON-APPLICATION:** prereq-check.sh does NOT `docker exec` into the running container — it `docker run --rm` for the alpine probe (non-interactive, no TTY, short-lived). Tier dispatch is argv-based, not TTY-based. SC-11 wire-ins preserve each shim's own TTY posture unchanged.
- **`<verb>-host.sh` naming convention (iter-199 EXTENDED through iter-244) — Story 2.10 DEVIATION (by design):** Story 2.10 ships `prereq-check.sh` (NOT `<verb>-host.sh`) because it's a shared primitive called by other shims, not a pnpm-verb wrapper. The `pnpm devbox:prereq:check` standalone verb IS wired (Task 2) but the script itself is the primitive. Naming follows Story 2.3/2.4 primitive pattern (`reload-egress.sh`, `whitelist.sh`). Post-Story 2.10 the host-side-file family is 17 `<verb>-host.sh` + 1 primitive (`prereq-check.sh`) + 6 Story 2.3/2.4 in-container primitives + 2 iter-239 lib helpers (`lib/main-repo-resolver.sh`, `lib/check-mount-source.sh`) under `scripts/lib/`.
