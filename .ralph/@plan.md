# Implementation Plan

## NOW

- [ ] Run `/bmad-testarch-atdd` for Story 1.19 — FULL red-phase MANDATORY per SC-11 (FR14n state transition `validated → atdd-scaffolded`; first reopen-arc story to re-introduce ATDD post-Stories-1.17/1.18 ATDD-skip era; AC class is "test backfill of untested impl" — exactly what ground-(b) was hiding per SCP § risk assessment)

## QUEUE (Epic 1 reopen — Stories 1.19–1.21 bootstrap arc per issue #233)
- [ ] Story 1.19 lifecycle: `atdd-scaffolded → in-dev` (`/bmad-dev-story` — 6 ACs / 11 Tasks / ~38 subtasks; budget 4–6 CR iterations per IP § Notes line 54)
- [ ] Story 1.19 lifecycle: `in-dev → traced` (`/bmad-testarch-trace (args: "yolo")`)
- [ ] Story 1.19 lifecycle: `traced → sm-verified` (`/bmad-create-story (args: "review")` post-dev)
- [ ] Story 1.19 lifecycle: `sm-verified → done` (`/bmad-code-review (args: "2")` — anticipated 4–6 CR iterations vs 1–2 typical; pre-existing impl bugs in `keel-invariants` may surface)
- [ ] Run `/bmad-create-story` for Story 1.20 (Activate FR14i — register `INV-fr14i-ci-workflow-presence`; **expand `.github/workflows/ci.yml` trigger filter to cover `feat/epic-*` PR bases per RALPH.md iter-371 gotcha — current `branches: [main]` filter excludes PR #236-style stacked PRs**; address pre-existing 3× `INV-git-hooks-preservation` drifts before close-out per RALPH.md iter-358)
- [ ] Story 1.20 lifecycle
- [ ] Run `/bmad-create-story` for Story 1.21 (audit + sweep prior ATDD-skip into test-debt.md)
- [ ] Story 1.21 lifecycle
- [ ] Re-close Epic 1 (`epic-1: in-progress → done` after Story 1.21 closes); transition PR Draft→Open final CI gate

## BLOCKED

_(none)_

## ATDD Red Phase

_(empty — Story 1.18 ATDD-skipped at iter-365 via FR14n § ATDD-skip ground-(a) substrate-verification per Story 1.17 iter-358 precedent. Three Python smoke scaffolds (`tests/test_ralph.py` + `scripts/tests/test_bootstrap_bmad_agents.py` + `packages/devbox/tui/tests/test_theme.py`) authored at iter-366 dev-story landed GREEN on first run by construction — 4 tests passed (the second `test_format_duration_hour_plus_ends_with_s` was the optional-second-assertion per Subtask 3.1). 30th cumulative project ATDD-skip / 2nd post-(b)-sunset / 3rd course-correction-origin.)_

## DONE (Epic 1 reopen pass — Stories 1.17–1.21)

- [x] iter-1 (prior iter): `/bmad-correct-course` on issue #233 — Sprint Change Proposal authored autonomously per Ralph build-mode batch flow. Outputs: `_bmad-output/planning-artifacts/sprint-change-proposal-issue-233.md` + PRD/architecture/epics/sprint-status amendments per § 4.1–4.4. Epic 1 REOPENED (`epic-1: done → in-progress`); Stories 1.17–1.21 appended to epics.md + sprint-status as backlog. Branch: `chore/correct-course-test-runner-233` (based on `feat/epic-2-packaged-devbox`).
- [x] iter-356 → iter-362: Story 1.17 lifecycle complete (`drafted → validated → atdd-scaffolded → in-dev → traced → sm-verified → done`). Cumulative pre-merge PATCH count: 22 (16 SM-validate + 4 SM-verify + 2 CR; 0 dev-story; 0 trace; 0 ATDD-skip). Vitest 3.2.4 pinned; smoke test GREEN; `.github/workflows/ci.yml` shipped (first workflow file in repo); CR iter-362 added top-level `permissions: contents: read` + `concurrency: cancel-in-progress` blocks. Epic 1 REOPEN-ARC story 1 of 5 done.
- [x] iter-363: `/bmad-create-story` for Story 1.18 — autonomous discovery from sprint-status first-backlog row (`1-18-bootstrap-python-test-runner-pytest-under-uv`). FR14n state transition `_(no story) → drafted`. Sprint-status row `backlog → ready-for-dev`. **0 SCP-side drifts** at substrate verification. 10 Tasks / ~22 subtasks scaffolded; SC-1 through SC-11 pinned.
- [x] iter-364: `/bmad-create-story (args: "review")` for Story 1.18 — pre-dev SM-validate. FR14n state transition `drafted → validated`. 14 PATCHes applied inline at gate (within iter-363 forecast envelope 8–14). 2 deferred (LLM-OPT). Cumulative pre-merge PATCH count Story 1.18 lifecycle to date: 14.
- [x] iter-365: Story 1.18 ATDD-skipped via FR14n § ATDD-skip clause; FR14n state transition `validated → atdd-scaffolded`. Bare ground-(a) substrate-verification sufficiency. **30th cumulative project ATDD-skip / 2nd post-(b)-sunset / 3rd course-correction-origin.** Cumulative pre-merge PATCH count Story 1.18 lifecycle: 14 (unchanged from SM-validate).
- [x] iter-366: Story 1.18 dev-story landed via `/bmad-dev-story` (Change Log v1.3); FR14n state transition `atdd-scaffolded → in-dev → review` (single-iter same-iteration per RALPH.md iter-344 substrate-extension class — third confirmation after Story 2.18 iter-350 + Story 1.17 iter-359). All 10 Tasks / 22 subtasks complete; all 5 ACs satisfied. **Substrate-probe gap surfaced + corrected at dev-story time:** SM-validate Subtask 3.1 missed `ralph.py:66` top-level `from textual.app import ...` (verified at dev-story via Grep — 5 textual imports at lines 66/67/68/71/72, all top-level). Resolution: added `textual==8.2.4` as 5th dev dep (SC-3 wording was "four"; +1 deviation justified by AC1 satisfaction; SC-3 policy ["exact-version pinning per I7"] preserved on all 5). **Resolved-highest version drift vs spec forecast:** pytest 9.0.3 (vs 8.x), pytest-asyncio 1.3.0 (vs 0.x); ruff 0.15.12 + mypy 1.20.2 within forecast. `pnpm typecheck/lint/format:check` GREEN; sync-gate PARTIAL (3 pre-existing `INV-git-hooks-preservation` drifts per RALPH.md iter-358 — out of scope per SC-9, AC2 satisfied because not attributable to this story); actionlint unavailable (GH ingestion-side fallback per AC3 + Story 1.17 Subtask 10.4 precedent). 0 fix-task QUEUE entries → direct promotion to `review`. Cumulative pre-merge PATCH count Story 1.18 lifecycle: 14 (unchanged — clean dev-story landing, 0 PATCHes at gate; matches Story 1.17 iter-359 zero-PATCH dev-story baseline).
- [x] iter-367: Story 1.18 trace landed via `/bmad-testarch-trace (args: "yolo")` (Change Log v1.4); FR14n state transition `in-dev → traced`. Gate **WAIVED** — 30th cumulative trace-WAIVED + 2nd Epic-1-reopen-arc + 3rd course-correction-origin. Coverage 1/5 FULL (20%); P0 50% (AC1 FULL via 4 pytest smokes; AC2 PARTIAL — sync-gate green for new drift, manifest registration deferred per SC-8 + SC-9); P1 0% (AC3 PARTIAL — `actionlint` unavailable, GH ingestion fallback); P2 0% (AC4 + AC5 doc-substrate PARTIAL). Hybrid grounds (a)+(c) variant-(ii)+(iii) per IP § NOW directive. 0 fix-task QUEUE entries → direct promotion to `traced`. Trace artefacts at `_bmad-output/test-artifacts/traceability/1-18-bootstrap-python-test-runner-pytest-under-uv.md` + `…-e2e-trace-summary.json` + `…-gate-decision.json`. Cumulative pre-merge PATCH count Story 1.18 lifecycle: 14 (unchanged — WAIVED applies no patches; matches Story 1.17 iter-360 zero-PATCH trace baseline).
- [x] iter-368: Story 1.18 post-dev SM-verified via `/bmad-create-story (args: "review")` (Change Log v1.5); FR14n state transition `traced → sm-verified`. Two-subagent SM-review (technical-correctness + prose-density per RALPH.md iter-235 narrow-surface; iter-352 post-dev recipe). Subagent A GREEN (every AC verified line-by-line; 0 MUST-FIX + 1 SHOULD-FIX); Subagent B YELLOW-LEAN-GREEN (convergent on SC-3 SHOULD-FIX + 3 additional SHOULD-FIX + 1 NIT + 1 LLM-OPT). **4 PATCHes applied inline at gate** (within iter-352 envelope 1–4; matches Story 1.17 iter-361 4-PATCH datapoint exactly): (1) SC-3 line 122 "four" → "five" + textual rationale + policy-preservation note; (2) Subtask 1.1 line 60 dev list 4-element → 5-element with substrate-probe-gap correction note; (3) § Forecast envelope appended empirical-vs-forecast deltas block (iter-368 SM-verify retrospective per iter-361 residual class); (4) Change Log block reordered v1.4→v1.0 (reverse-chrono) → v1.0→v1.5 (ASCENDING) per iter-361 PATCH M1 + Story 1.17 sibling convention. **Pattern carry-rule extension (NEW, supersedes iter-361 carry-rule):** post-dev SM-verify probe order = (1) dev-story-time deviations not folded back into spec wording (NEW class — iter-368 textual=5th-dep + pytest version drift cases); (2) line-range drift on table/section anchors (iter-361 case); (3) Forecast empirical-vs-forecast historical block missing (iter-361 + iter-368 confirmed); (4) Change Log block-reorder vs sibling convention (iter-361 + iter-368 confirmed). 2 NIT/LLM-OPT deferred (AC4 line-range parenthetical + textual rationale 2× redundancy across Completion Notes + Change Log). 0 fix-task QUEUE entries → direct promotion to `sm-verified`. Story file `Status: review` unchanged; sprint-status row unchanged (SM-verify is Ralph-internal). Cumulative pre-merge PATCH Story 1.18 lifecycle: 18 (14 SM-validate + 4 SM-verify; 0 dev / 0 trace / 0 ATDD-skip).
- [x] iter-372 (this iter): Story 1.19 SM-validate landed via `/bmad-create-story (args: "review")` (Change Log v1.1); FR14n state transition `drafted → validated`. **21 PATCHes applied inline at gate** (within iter-370 forecast envelope SC-12 SM-validate band 14–22; lands at upper-band consistent with 6-AC / ~33-it()-block scope vs Story 1.18's 5-AC / 14-PATCH baseline). Three-subagent triage (substrate ledger ground-truth + spec/source alignment + LLM-dev-agent optimization per RALPH.md iter-352 narrow-surface recipe) — raw 36 findings → 21 PATCH + 5 DEFER (LLM-OPT) after dedup/triage. **4 MUST-FIX substrate-ledger corrections:** test count `8 → 7` (`hook-settings-tamper.test.ts` `test(...)` callsite count = 7, not 8); Subtask 1.2 vitest.config.ts edit-shape tightened (REPLACE 8-line block, not REMOVE+REPLACE); Subtask 1.1 assert-mapping table extended with `expect(value, message)` custom-message form; Subtask 9.3 INVARIANTS.md insertion-point fixed (between line 88 Story 1.16 + line 90 Story 2.1, NOT line 40-44 Story 1.6→Ralph as v1.0 claimed). **12 SHOULD-FIX:** package.json bin count 5→6; ESLint dual registration sites; denylist anchored-regex exemption mechanism; Node 20.11+ baseline lock; cc-devbox bash+dash availability lock; Strategy A/B contradiction resolved per lock-don't-defer (4 sites: Subtasks 4.3/5.1/6.2/8.3); 3× "SM-validate may dispute/absorb" replaced with explicit lock statements (AC2/AC3/AC4); SC-5 lock-points enumerated (4 not 2); SC-12 forecast restructured as table + ATDD-scaffold N/A reconciled with SC-11; **manifest-reader.ts substrate-ledger correction — 119 lines of crypto+fs helpers, NOT a re-export shim; pure-unit-test backfill of 7 helpers explicitly out of Story 1.19 scope (Story 1.21 deferral)**; Subtask 11.3 lifecycle plan extended to v1.11; Subtask 8.1 fs.readdir recursive locked + multi-violation NDJSON wire format pinned; Subtask 10.4 "hold" pass-condition explicitly locked; AC5 EXEMPT_LIST source-of-truth + standalone-CLI scope clarification. **5 NIT/LLM-OPT:** § Project Structure 5-vs-3 enforcer clarification; nfr5a-minimum two stderr emit sites; Subtasks 6.4/7.3 "optional defer-able" tags resolved (INCLUDED-not-optional with defer-on-blocker fallback). **5 DEFER (LLM-OPT, with rationale):** AC4→SC-13 elevation, heavy subtask prose restructure, Task 11 compact, AC5 sub-bullet (subsumed in P18), subtask sub-headings. **Pattern carry-rule (NEW):** SM-validate substrate-ledger probes must Read every cited file (not just Glob) — a "re-export shim" claim verified by Read at SM-validate iter-371/372 caught the 119-line manifest-reader gap that would have cascaded into AC3 scope confusion at dev-story. Carry-rule for Story 1.20 + Story 1.21 + Epic 4 substrate-extension stories: claims about "shim" / "re-export" / "thin wrapper" require explicit Read-then-line-count verification. Cumulative pre-merge PATCH Story 1.19 lifecycle: 21 (within forecast band 14–22). NEXT iteration: `/bmad-testarch-atdd` for Story 1.19 — FULL red-phase MANDATORY per SC-11.
- [x] iter-371 (prior iter): Monitor PR #236 CI evaluated as **PASS / no-op** — `gh pr view 236 --json statusCheckRollup` = `[]`; `gh pr checks 236` = `"no checks reported on the 'chore/correct-course-test-runner-233' branch"`. Root cause: `.github/workflows/ci.yml:3-7` triggers `pull_request: branches: [main]` + `push: branches: [main]` only; PR #236 base = `feat/epic-2-packaged-devbox` (stacked-epic shape per issue #233 SCP) is excluded BY DESIGN. CI Monitoring section "Pass → mark done, move next QUEUE to NOW, exit" applied. **RALPH.md updated:** (a) iter-370 Gotcha line 101 SUPERSEDED with iter-371 entry (corrected: empty rollup is design, not flake; skip Monitor for stacked-epic PRs); (b) iter-371 Lessons-learned entry added on CI workflow trigger filter blast radius. **IP § QUEUE updated:** Story 1.20 entry extended with sub-task "expand `.github/workflows/ci.yml` trigger filter to cover `feat/epic-*` PR bases" (folded into FR14i activation scope). **No code change** — bookkeeping + journal-correction only. Cumulative pre-merge PATCH Story 1.18 lifecycle final unchanged: 21.
- [x] iter-370 (prior iter): `/bmad-create-story` for Story 1.19 — autonomous discovery from sprint-status first-backlog row (`1-19-backfill-keel-invariants-test-coverage`). FR14n state transition `_(no story) → drafted`. Sprint-status row `backlog → ready-for-dev`; Epic 1 status unchanged (`in-progress` per SCP reopen — Story 1.19 is intra-epic continuation, NOT first-story-in-epic flip). **0 SCP-side drifts** at substrate verification (ledger built via Glob + Read against `packages/keel-invariants/src/**/*` + `vitest.config.ts` + `eslint.config.keel-invariants.js` + `package.json` + `INVARIANTS.md` + `invariants.manifest.ts`). 6 ACs / 11 Tasks / ~38 subtasks scaffolded; SC-1 through SC-12 pinned. **Two AC-vs-spec deliberate reinterpretations recorded per SC-2:** AC2 honours per-enforcer stderr shape AS-SHIPPED (NOT normalised JSON across all three — `check-no-committed-dotfiles.ts:35-38` + `check-claude-hook-syntax.ts:88-97` ship prose; `check-nfr5a-minimum.ts:42-44` ships JSON); AC4 reinterprets "non-existent sourcePath" as "sourcePath SHAPE violation" (Zod path-shape `.refine(...)`; file-existence covered by AC3's `removed-from-source-only` drift fixture). SM-validate may dispute either. **ATDD red-phase MANDATORY (SC-11)** — first reopen-arc story to re-introduce ATDD post-Stories-1.17/1.18 ATDD-skip era; the runner now exists (Story 1.17 + 1.18 substrate); Story 1.19's AC class is "test backfill of untested impl" — exactly the case ground-(b) was hiding per SCP § risk assessment. **Pre-existing INV-git-hooks-preservation drifts (3×) carved out per SC-9** (Subtask 10.3); Story 1.19 edits do NOT touch `.git/hooks` or that invariant's source. **Forecast envelope SC-12:** SM-validate 14–22 PATCH (vs Story 1.18 final 14 — wider because 5+5+4+5+3+4=26 distinct test surfaces vs Story 1.18's 5 ACs); ATDD-scaffold N/A (red-phase via testarch-atdd, not PATCH count); dev-story 0–4; trace 0–2 (FULL coverage IS the deliverable); SM-verify 1–4 (matches Story 1.18 datapoint); CR 4–12 across 4–6 CR iterations (per IP § Notes line 54 / SCP § risk Medium). Cumulative pre-merge PATCH count target 18–44 (vs Story 1.18 final 21 — wider per adversarial-backfill-of-untested-code class). NEXT iteration: `/bmad-create-story (args: "review")` for Story 1.19 — pre-dev SM-validate.
- [x] iter-369 (prior iter): Story 1.18 CR landed via `/bmad-code-review (args: "2")` (Change Log v1.6); FR14n state transition `sm-verified → done`. Three-subagent triage (Blind Hunter — diff-only adversarial; Edge Case Hunter — diff + project read access; Acceptance Auditor — diff + spec). Raw 18 findings → **3 PATCH + 4 DEFER + 11 DISMISS** after dedup/triage. Auditor: all 5 ACs SATISFIED + all 11 SCs honoured (3 deferrals by-design). **3 PATCHes applied inline at gate per RALPH.md iter-342 mid-arc narrow-band recipe** (PATCH band ≤3 + no decision_needed + L1-safe — all conditions met; matches Story 1.17 iter-362 inline-bundle-close + iter-362 first-CI-workflow-job hardening residual class extended to job-execution surface): (1) Blind B7 — `tests/test_ralph.py:8-9` tighten 2nd assertion `endswith("s")` → `== "1h02m05s"` + rename `_ends_with_s` → `_exact` (Edge Case Hunter verified return shape); (2) Blind B10 — `pyproject.toml [tool.pytest.ini_options]` append `asyncio_mode = "strict"` (pytest-asyncio 1.x defaults to strict but unconfigured leaves future async-test contributors to silent-no-op); (3) Edge E7 — `pyproject.toml` prepend `[tool.uv]\npackage = false` block (uv.lock implicitly virtual; explicit declaration prevents future-trap when `[project].dependencies` accidentally triggers a build). **Verified post-edit:** `uv sync --frozen --extra dev` exit 0 (no lock regen); `uv run pytest` 4/4 GREEN with `asyncio: mode=Mode.STRICT` confirmed; `pnpm format:check` exit 0. **4 DEFERs** logged to `_bmad-output/implementation-artifacts/deferred-work.md § code review of story-1-18 (2026-04-26)`: hardcoded `setup-uv` version pin (renovate territory), CI runner Python-version drift risk (Story 1.21 audit), `pythonpath = ["."]` future basename-collision risk (Story 1.21 audit), future top-level `tui/` shadow risk (Story 1.21 audit). 11 DISMISSes (false positives + by-design / SC-pinned). **CR yield empirical (course-correction-author origin, narrow-substrate-extension class, post-clean-dev-landing — third datapoint after Story 2.18 iter-353 = 0 PATCH + Story 1.17 iter-362 = 2 PATCH):** **Story 1.18 CR = 3 PATCH at TOP of envelope 0–3** — confirms first-CI-workflow-job hardening residual class fires not just on workflow YAML itself but also on the new job's execution surface (pytest config, build-system explicitness, assertion signal-vs-coverage). **Pattern carry-rule (NEW, extends iter-362):** for stories that introduce a new CI workflow JOB (vs adding the workflow file from scratch), CR-time hardening probes target (1) test runner config defaults that flip to footgun-mode in newer dep versions; (2) build-system explicitness for tools that auto-detect virtual vs package projects; (3) assertion signal-vs-coverage tightening when ATDD-skipped via ground-(a) substrate verification. 0 fix-task QUEUE entries — direct promotion to `done`. Sprint-status row `review → done`; last_updated bumped to iter-369. **Cumulative pre-merge PATCH count Story 1.18 lifecycle final: 21** (14 SM-validate + 4 SM-verify + 3 CR; 0 dev / 0 trace / 0 ATDD-skip — within iter-363 forecast envelope 10–24; vs Story 1.17 final 22 = 1 PATCH below per narrower-scope discount per RALPH.md iter-344 substrate-extension class held on net). Epic 1 REOPEN-ARC story 2 of 5 done (1.17 + 1.18); 3 stories remaining (1.19 keel-invariants backfill, 1.20 FR14i activation, 1.21 audit + sweep). NEXT iteration: `/bmad-create-story` for Story 1.19 — Story State `_(no story)` per intra-epic continuation per § Cross-epic transition step 3 sprint-status-has-next-story branch (Epic 1 still in-progress; no halt).

## Context

- **Phase:** 4-implementation — Story 1.19 drafted at iter-370 (FR14n `drafted` state; Story file `Status: ready-for-dev`). Epic 1 REOPENED (`epic-1: in-progress` per issue #233 SCP) for Stories 1.17–1.21 bootstrap arc; 3 stories remaining in lifecycle (1.19 keel-invariants backfill IN-FLIGHT, 1.20 FR14i activation, 1.21 audit + sweep). Epic re-close at Story 1.21 done.
- **Runtime:** cc-devbox iteration env. `uv 0.11.7` available at `/usr/local/bin/uv` (confirmed at substrate probe). Python 3.12.3 at `/usr/bin/python3` (satisfies `requires-python = ">=3.10"`). `actionlint` not available (GH ingestion-side fallback per AC3). github.com / api.github.com network access intermittent (DNS-rotation; PR #235's fix in container image: TBD).
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (REOPENED for Stories 1.17–1.21).
- **Epic Branch (parent):** `feat/epic-2-packaged-devbox` — this work targets it as the PR base, not `main`.
- **Working Branch:** `chore/correct-course-test-runner-233` (based on `origin/feat/epic-2-packaged-devbox`).
- **Story:** Story 1.19 — Backfill `keel-invariants` test coverage (drafted at iter-370 via `/bmad-create-story`).
- **Story File:** `_bmad-output/implementation-artifacts/1-19-backfill-keel-invariants-test-coverage.md`.
- **Story State:** `validated` (FR14n state transition `drafted → validated` at iter-372 via `/bmad-create-story (args: "review")` pre-dev SM-validate; 21 PATCHes applied inline at gate per SC-12 SM-validate band 14–22). **Next-NOW per FR14n matrix `validated` row:** `/bmad-testarch-atdd` for Story 1.19 — FULL red-phase MANDATORY per SC-11. Cumulative pre-merge PATCH Story 1.19 lifecycle to date: 21 (within forecast envelope iter-370 SC-12). Forecast remaining: dev-story 0–4, trace 0–2, SM-verify 1–4, CR 4–12 across 4–6 iterations; cumulative target 19–44 (vs Story 1.18 final 21). ATDD red-phase MANDATORY (SC-11) — first reopen-arc story to re-introduce ATDD.
- **GitHub Issue:** Issue #233 ("Course Correction: Bootstrap test runner + CI"). PR #236 (Draft, OPEN, base: `feat/epic-2-packaged-devbox`) carries `Refs #233`; `Closes #233` lands at Story 1.21 close-out per stacked-arc plan.
- **PR:** #236 (Draft, OPEN, base: `feat/epic-2-packaged-devbox`).

## Notes

- **Iter-369 push deferred to next iteration:** `git push` failed twice with `ssh: connect to host github.com port 22: Connection timed out` per IP § Context line 38 cc-devbox network-intermittency notice (DNS-rotation; PR #235's container-image fix TBD). Local commit `4f7c84b` (Story 1.18 done CR) carries forward; next iteration's Ralph step 5 will pick it up alongside the create-story commit and push both. NO commit re-attempt needed at iteration start; just `git push` again at step 5. Per Ralph step-5 + iter-365 ATDD-skip-iter precedent (also pushed in single shot at next iter): not a halt, just a deferred push.
- **Course-correction precedent stack 3 deep:** issue #231 (PR #234, doc-budget, MERGED) → issue #232 (PR #235, devbox network, MERGED) → issue #233 (PR #236, test runner bootstrap, in-flight). Issue #233 is the first to REOPEN a closed epic.
- **Story 1.17 → 1.18 inheritance:** Story 1.18 inherits the `.github/workflows/ci.yml` substrate (Story 1.17 shipped it) and the AGENTS.md `## Testing` section (Story 1.17 inserted it with a forward-pointer). Story 1.18 EXTENDED both additively per SC-5 + AC5 — both edits landed cleanly at iter-366.
- **Pre-existing INV-git-hooks-preservation drifts:** 3× drifts persist on `feat/epic-2-packaged-devbox` head (RALPH.md iter-358 gotcha — sync-gate hardcodes `<repoRoot>/.git/hooks` which is empty in worktree mode). Out of scope for Story 1.18 per SC-9 carve-out; address before Story 1.20 close-out.
- **Story 1.19 budget warning:** keel-invariants backfill has 4–6 CR-iteration budget exposure (vs 1–2 typical). Pre-existing impl bugs in `keel-invariants` will surface for the first time under test.
- **Epic 4 hard-precondition:** Epic 4 (per-iteration security verification, FR35–FR40) extends `packages/keel-invariants/` with secret/SAST/prompt-injection scanners. Story 1.19 backfill MUST land before Story 4.1 starts.
- **Iter-371 CI-workflow-trigger gotcha (NEW):** PR #236 returns `statusCheckRollup: []` BY DESIGN per `.github/workflows/ci.yml:3-7` `pull_request: branches: [main]` filter (PR #236 base = `feat/epic-2-packaged-devbox`). Future Ralph iterations encountering "Monitor PR #236 CI" in NOW or QUEUE auto-advance per RALPH.md iter-371 gotcha (treat empty rollup as pass; do NOT re-queue Monitor at step 5b for this PR). Ralph step 5 (pre-push CI gate) push proceeds normally — `gh pr checks` returns no in-progress checks → no block. Trigger-filter expansion deferred to Story 1.20 FR14i activation scope per QUEUE entry.
- **Substrate-probe gap class identified at iter-366:** SM-validate substrate probes can miss top-level imports inside otherwise-guarded modules (e.g. `ralph.py:66` `from textual.app import` evaluated unconditionally despite `if __name__ == "__main__":` at `:2015-2016` guarding only App instantiation). Future story SM-validate substrate probes that cite "module-level import has no side-effects" must explicitly grep for ALL top-level imports of any non-stdlib package; the `__main__` guard alone is insufficient evidence. Carry-rule for Story 1.19 + Story 1.20 + Epic 4 stories that test/load substrate Python modules.
- **Substrate-ledger "shim" verification carry-rule (NEW iter-372):** SM-validate iter-372 caught a critical create-story claim that `manifest-reader.ts` is a "re-export shim" — actual file is 119 lines with 7 crypto+fs helper functions (`computeSha256`, `computeSubtreeHash`, `computeAnchorRangeHash`, `computeNamesAndShebangsHash`, `loadExpectedHooks`, `listDirIfExists`, `readSourceFile`). The Glob-only probe at create-story didn't catch this; only Read-then-line-count verification did. Carry-rule for Story 1.20 + Story 1.21 + Epic 4: claims about "shim" / "re-export" / "thin wrapper" / "infrastructure-only" must be verified by Read (not Glob alone) and the actual line count + helper inventory pinned in Dev Notes. Without this carry-rule, the ledger will repeat the AC3-vs-manifest-reader-coverage scope ambiguity in future substrate-extension stories.
