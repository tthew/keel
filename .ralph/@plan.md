# Implementation Plan

## NOW

- [ ] **Story 2.15 `/bmad-testarch-atdd`** (`validated → atdd-scaffolded`). Iter-296 landed `/bmad-create-story (args: "review")` pre-dev SM with 5 PATCH bundled close (2 line-range drift fixes + CLAUDE.md two-bullet wording rewrite + Task 6 README stub-state clarification + NFR5a `~/.ssh/**` pinned DEFER to Story 2.17) — within iter-281 NOVEL LESSON drift-band for narrow-diff doc-heavy stories (1-3 forecast, 5 actual — above upper band, consistent with "2-6 PATCH for doc-heavy stories at moderate-novelty" re-baseline). Per § Story Lifecycle Decision Matrix row `validated`: invoke `/bmad-testarch-atdd`. Forecast per iter-295 signpost + iter-296 post-pre-dev SM: SKIP-WITH-GROUNDS-(ii) single-ground waiver — FIRST single-ground precedent in the 24-chain (would be 25th cumulative ATDD-skip precedent). Story 2.15 is configuration-only wholesale (no executable branch); (c) "mixed-class ACs" from Story 2.14's (ii)+(iii) no longer applies. META guard watch: if /bmad-testarch-atdd suggests grounds-(ii) alone insufficient, fall back to (ii)+(iii) (AC 5 deny-rule-pointer behavioural check is operator-workstation-deferred per Dev Notes § ATDD-applicability predicate — that's the (iii) anchor).

## QUEUE (Story 2.15 lifecycle + 2.16..2.17 substrate queue)

- [ ] _(after Story 2.15 atdd-scaffolded)_ Story 2.15 `/bmad-dev-story` — JSON authoring (copy-ready from Task 1) + 3 doc touches (AGENTS.md H3 append + CLAUDE.md two-bullet replace + packages/devbox/README.md H2 append) + 1 seed (byte-identical copy to packages/keel-templates/src/seeds/.claude/settings.json + keel-templates README stub update per iter-296 PATCH-3 clarification) + sprint-status flip. Forecast single-iter landing (no novel surface; no invariant doc; no manifest entry).
- [ ] _(after Story 2.15 in-dev)_ Story 2.15 `/bmad-testarch-trace (args: "yolo")` — likely WAIVED outcome per Story 2.14 iter-292 precedent (configuration-only delta; ACs 1-4 + 7 substrate-covered by static smokes; AC 5 operator-workstation-deferred; AC 6 forward-ref to Story 2.16). Would be 25th cumulative trace-WAIVED precedent; narrower-grounds per iter-292 LESSON carry-forward.
- [ ] _(after Story 2.15 traced)_ Story 2.15 `/bmad-create-story (args: "review")` post-dev SM (`traced → sm-verified`) — two-subagent pattern per iter-235/iter-270 LESSON; forecast 0-2 PATCH per iter-270 drift-band re-baseline (post-dev SM narrower when pre-dev SM absorbs substantive findings; iter-296 absorbed 5 PATCH on prose/drift polish — post-dev SM expected narrow).
- [ ] _(after Story 2.15 sm-verified)_ Story 2.15 `/bmad-code-review (args: "2")` CR — three-layer adversarial fan-out (Blind + Edge Case + Acceptance Auditor) per iter-271/iter-277/iter-294 pattern; forecast 0-2 PATCH (narrow diff; bundled close per iter-264/iter-294 precedent if fixes are trivial cross-doc lockstep prose edits).
- [ ] _(after Story 2.15 done)_ Story 2.16 Claude PreToolUse hooks for secret-file denylist — full lifecycle (drafted → validated → atdd-scaffolded → in-dev → traced → sm-verified → done). Story 2.16 adds `.claude/hooks/block-secret-access.sh` + `hooks` block registration in `.claude/settings.json` + `INV-claude-hook-secret-denylist` manifest entry + N=3 halt-threshold wiring.
- [ ] _(after Story 2.16 done)_ Story 2.17 hook-settings bypass-resistance + Epic 2 SC-17 close-out polish pass. SC-17 reconciliation absorbs cumulative **47 DEFERs at iter-294 Story 2.14 CR landing** (31 pre-CR + 16 iter-294 CR additions) PLUS Stories 2.15..2.16 accrued DEFERs. Story 2.15 accrued 0 first-class DEFER at iter-296 pre-dev SM (NFR5a `~/.ssh/**` + `~/.aws/credentials` extension pinned as Story 2.17 revisit — counts as `revisit-at-SC-17` marker, not a DEFER-queue entry since the decision is "don't add" not "add later"; Story 2.17 SC-17 may keep-deferred or re-pin add).
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured; `statusCheckRollup: []` carried unchanged iter-219..296).

## BLOCKED

_(none — Story 2.15 pre-dev SM landed cleanly at iter-296; all 5 PATCHes applied bundled; no push-transport or prereq issues encountered.)_

## ATDD Red Phase

_(none — Story 2.14 ATDD-skipped at iter-289 with grounds-(ii)+(iii); Story 2.15 ATDD forecast to skip with grounds-(ii) at iter-297. No red-phase tests owed.)_

## DONE (iter-296 `/bmad-create-story (args: "review")` pre-dev SM — Story State `drafted → validated`)

- [x] iter-296: **STORY 2.15 PRE-DEV SM LANDING** (`drafted → validated`). Two-subagent pattern per iter-235/iter-288 precedent — technical-correctness reviewer (AC↔Task traceability + 14-site source-ref verification + 13-deny + 6-allow byte-identity + 8-item scope-boundary accuracy + honour-system claim sanity) + prose-density reviewer (AGENTS.md H3 density vs peer norm + cross-ref density + CLAUDE.md two-bullet wording + voice coherence + forward-ref citation consistency + References count trim). **5 bundled PATCHes applied** (single commit per iter-264/iter-294 bundled-close precedent for trivial prose fixes): PATCH-1 source-range drift `epics.md:1722-1800 → :1722-1773` (Story 2.17 actually ends at line 1773; lines 1776-1800 are Epic 3 preamble); PATCH-2 source-range drift `epics.md:6214-6232 → :6214-6230` (line 6232 is Story 15a.5 header, not Story 15a.4); PATCH-3 Task 6 keel-templates/README.md bullet clarified to acknowledge current 5-line stub state ("Empty shell in Story 1.1 — populated in Epic 12") and instruct dev to REPLACE that sentence + CREATE seeded-assets list as first producer; PATCH-4 CLAUDE.md Task 4 bullet 1 rewritten to future-tense "Story 2.17's content-hash sync-gate will flag tampering once landed" (avoids misleading present-tense claim at Story 2.15 landing); PATCH-5 CLAUDE.md Task 4 bullet 2 shortened to match peer-bullet voice in § Claude Code specifics block — drops embedded `deny`-wins-over-`allow` explanation (pointer to AGENTS.md H3 suffices); PATCH-6 Dev Notes § NFR5a mapping pinned DEFER for `~/.ssh/**` + `~/.aws/credentials` to Story 2.17 bypass-resistance close-out (removes dev decision-load; aligns with peer-story 2.13 SC-4/SC-6 + 2.14 SC-14 pin-not-MAY posture). 4 OPTIMIZATION findings DISMISSED (cross-ref trims in Task 3 bullets d/f; References list trim 22→19) — not worth PATCH-cost at pre-dev SM stage. E4 (upstream Claude Code docs `deny`-wins-over-`allow` claim UNVERIFIED) noted but NOT PATCHed (low-risk; subagent itself flagged as optional + industry convention aligns). All 14 source-ref verifications PASSED (`epics.md:1629-1668` / `prd.md:1075,1076,832,104,910` / `epics.md:1165,1670-1720` / `CLAUDE.md:74` / `.gitignore:20` / `Dockerfile:121` / manifest.ts 34-entries / 13-deny + 6-allow byte-identity ALL CONFIRMED ACCURATE). Commit candidate: `docs(story-2-15): iter-296 /bmad-create-story (args: "review") pre-dev SM — drafted → validated; 5 PATCH bundled close (2 line-range + 2 CLAUDE.md wording + 1 Task 6 + 1 NFR5a pin); 4 DISMISS; 0 first-class DEFER`.

_(iter-287..295 Story 2.14 full lifecycle + Story 2.15 draft pruned per Guardrail 2; full detail in commit trail + RALPH.md signposts + story-file Change Log.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **14/17 stories done** (2.1-2.14) + 2.15 VALIDATED at iter-296 + 2.16-2.17 backlog.
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live smokes operator-workstation-deferred.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:121). Host-side shim count: **18** at iter-296 (unchanged — pre-dev SM iter; no substrate code edits).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **14 done** (2.1-2.14); **1 ready-for-dev, validated** (2.15); **2 backlog** (2.16-2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.15 — Committed `.claude/settings.json` with deny/allow permission policies.
- **Story File:** `_bmad-output/implementation-artifacts/2-15-committed-claude-settings-json-with-deny-allow-permission-policies.md`
- **Story State:** `validated` — next iter invokes `/bmad-testarch-atdd` per § Story Lifecycle Decision Matrix row `validated`. Forecast SKIP-WITH-GROUNDS-(ii) single-ground waiver (FIRST in 24-chain if it holds).
- **GitHub Issue:** Story 2.15 issue unknown; `RALPH_ISSUE_NUMBER` unset (no GitHub Project configured for this repo). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` unchanged iter-272..296).

## Notes

- **iter-296 observation (5-PATCH above 1-3 forecast band, within 2-6 re-baseline for doc-heavy):** iter-295 forecast 1-3 PATCH concentrated on AGENTS.md H3 length + cross-ref density + CLAUDE.md two-bullet wording. Actual: 5 PATCH across line-range drift (2) + CLAUDE.md wording (2) + Task 6 README stub (1) + NFR5a pin (1 — NFR5a pin counts as PATCH-6 above in DONE entry). Sub-surface rebalance from forecast: AGENTS.md H3 was confirmed peer-band (no PATCH); CLAUDE.md two-bullet was the hot zone (2 PATCH). Consistent with iter-281 LESSON re-baseline "doc-heavy moderate-novelty pre-dev SM band = 2-6 PATCH". No novel lesson this iter.
- **iter-296 observation (subagent line-range verification value):** Subagent A caught two line-range drifts (`:1800` → `:1773`; `:6232` → `:6230`) by actually reading the cited epics.md ranges and comparing to actual Story boundaries. Neither drift would have been caught by a prose-only pass. Pattern reinforcement: pre-dev SM subagents SHOULD do at least one source-range sanity probe per audit to catch drift class (line-range, file-path, section-header). Existing iter-281 LESSON "citation-audit false-positives hazard" already flags the inverse (enumerate-from-IP risk); iter-296 reinforces "verify cited ranges actually span what's claimed".
- **iter-296 observation (CLAUDE.md peer-voice coherence as first-class finding):** Subagent B's PR-E2 (CLAUDE.md bullet 2 duplicates AGENTS.md H3 content) is NOT a drift class — it's a knowledge-file-contract class finding (CLAUDE.md supposed to be pointer-to-AGENTS.md, not a mirror). Per CLAUDE.md:45-51 knowledge-file contract table. Carry-forward for Story 2.16 + 2.17 CLAUDE.md touches: keep bullets terse; link to AGENTS.md H3 rather than duplicating.
- **Cumulative DEFER forecast for Stories 2.15..2.17 Epic-2 close-out:** 47 pending at iter-294 carry-forward. Story 2.15 iter-296 pre-dev SM added 0 first-class DEFER (NFR5a `~/.ssh/**` + `~/.aws/credentials` pinned as "revisit at Story 2.17 SC-17" but this is a decision-boundary marker, not a new DEFER). Story 2.15 dev + post-dev SM + CR may accrue 0-4 more. Story 2.16 + 2.17 remaining accruals unknown. Absorption at Story 2.17 SC-17 close-out.
- **iter-292 NOVEL LESSON (narrower-grounds trace-WAIVED precedent) carry-forward:** Story 2.14 applied grounds-(ii)+(iii) dropping (c). Story 2.15 ATDD forecast extends to grounds-(ii) alone (FIRST single-ground waiver in the 24-chain if it holds) — watch for META guard firing at iter-297.
- **iter-291 NOVEL LESSON (initial-connection fetch-timeout budget under post-outage recovery) carry-forward:** default 120s `timeout` wrapping `git fetch` INSUFFICIENT; 240s required. Story 2.15 iter-296 pre-dev SM did NOT require network fetches; LESSON retains readiness posture for any transient surface that emerges.
