# Implementation Plan

## NOW

- [ ] **Story 2.17 `/bmad-create-story (args: "review")`** (`drafted → validated`). Per § Story Lifecycle Decision Matrix row `drafted`. Pre-dev SM validation of readiness — adversarial pass over the iter-309 draft at `_bmad-output/implementation-artifacts/2-17-hook-settings-bypass-resistance-git-layer-manifest-s4-halt.md` (475 lines; 8 ACs; 17 Tasks with ~70 subtasks; 36-entry primary DEFER absorption plan + 15-25 SC-17 polish plan). Expected PATCH band 3-5 (Story 2.16's pre-dev SM at iter-303 applied 3; Story 2.17's XL scope + 3-layer defense + sub-tree-hash design decision broaden drift surface). Forecast: if < 4 PATCH, "narrow-after-SM-absorb CR-drift-separate-class" 6-data-point threshold met → STABLE LESSON promotion eligible (iter-308 pattern refinement).

## QUEUE (Story 2.17 lifecycle + Epic 2 close-out)

- [ ] _(after Story 2.17 `validated`)_ `/bmad-testarch-atdd` — `validated → atdd-scaffolded`. Forecast: SKIP-WITH-GROUNDS likely (Epic 13 horizon variant-(ii) from Story 2.16 iter-304 pattern — CI test framework is Epic 13 scope) PAIRED WITH impl-time fixture smokes substrate-coverage substitute (Story 2.16 iter-306 NOVEL LESSON confirmed). If SKIP-WITH-GROUNDS applies, 27th cumulative ATDD-skip.
- [ ] _(after atdd-scaffolded)_ `/bmad-dev-story (args: "{story_file_path}")` — `atdd-scaffolded → in-dev`. Forecast XL (~80-120K) given 17 Tasks + schema extension + 3 new manifest entries + 3 S4 rules + 25+ impl-time smokes + 2 seed syncs + ~15 doc updates. Dev-story iter MAY need to be split across 2 iters if budget-exhausted; flag pre-emptively.
- [ ] _(after in-dev / review)_ `/bmad-testarch-trace (args: "yolo")` — `in-dev → traced`. 27th cumulative trace; likely WAIVED per Epic-2 16-story chain continuation + impl-time-smokes substitute.
- [ ] _(after traced)_ `/bmad-create-story (args: "review")` — post-dev SM `traced → sm-verified`. Forecast per Story 2.16 iter-307 ZERO-PATCH baseline; refinement of "narrow-after-SM-absorb" 5-data-point → 6-data-point STABLE LESSON promotion at iter-309-SM-gate.
- [ ] _(after sm-verified)_ `/bmad-code-review (args: "2")` — `sm-verified → done`. Forecast 2-6 PATCH absorbed inline (Story 2.16 CR hit 2; Story 2.15 hit 4; Story 2.17's wider surface suggests upper band). Sub-tree-hash design + schema extension + 3-layer defense = larger adversarial surface than prior Stories' single-layer deltas. Expect 25-40 new DEFERs if Epic-2 SC-17 close-out polish pass is exhaustive; target absorbed-in-2.17 count: 15-25 of those; remainder carry-forward to Epic 3+ substrate code.
- [ ] _(after Story 2.17 `done`)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured; `statusCheckRollup: []` carried unchanged iter-219..309).

## BLOCKED

_(none — iter-309 draft committed cleanly; research subagent completed in ~450s with 5,700-word comprehensive report; no `gh pr view` retries required this iter.)_

## ATDD Red Phase

_(none — Story 2.17 is at `drafted`. If pre-dev SM iter-310 ships + `/bmad-testarch-atdd` iter-311 triggers SKIP-WITH-GROUNDS, this section remains empty. If ATDD produces red-phase scaffolds, entries land here at iter-311.)_

## DONE (iter-309 `/bmad-create-story` drafting — Story State `_(no story) → drafted`)

- [x] iter-309: **STORY 2.17 `/bmad-create-story` DRAFTING** (`_(no story) → drafted`). 475-line story file authored at `_bmad-output/implementation-artifacts/2-17-hook-settings-bypass-resistance-git-layer-manifest-s4-halt.md`. Research subagent dispatched for comprehensive context (Story 2.16 intel + PRD/arch/deferred-work inventory + substrate surface citations) — 5,700-word report returned in ~451s / 203K subagent-internal tokens / 46 tool_uses; parent-context consumption ~30K for the report itself. **8 ACs verbatim from `epics.md:1722-1773`; 17 Tasks with ~70 subtasks covering 3-layer defense** (Layer 1 in-session hook from Story 2.16; Layer 2 pre-merge sync-gate; Layer 3 S4 prompt-injection scan) + L1 install-boundary protection rule addition + `.claude/settings.local.json` pre-commit rejection + halt-threshold consumer contract pin for Epic 3 Story 3.7. **Sub-tree-hash design decision:** `hashScope` schema extension with three `kind` variants (`jq-subtree` for `.claude/settings.json` `.hooks.PreToolUse`; `anchor-range` reserved; `names-and-shebangs` for `.git/hooks/` via `packages/keel-invariants/src/prek-hook-manifest.ts` enumeration). Back-compat preservation: all 35 pre-existing manifest entries continue at whole-file sha256 (absent `hashScope` field). Net manifest delta: 35 → 39 entries (4 new: `INV-claude-settings-deny-rules`, `INV-git-hooks-preservation`, `INV-git-hooks-preservation-enumeration`, `INV-claude-hook-secret-denylist-doc` via the existing-entry rename-split). **DEFER absorption plan:** 36 primary-scope (25 Story 2.16 D-12..D-36 + 8 Story 2.15 carry-forward + 3 Story 2.16 trace SC-17 candidates) via Tasks 7/8/10/11/12; 15-25 secondary-scope SC-17 polish via Task 16 (audit; absorb citation-lockstep class; defer substrate-code-change class). Sprint-status `2-17-…: backlog → ready-for-dev` flipped; last_updated appended `Story-2-17-ready-for-dev-iter-309 UTC`. Commit candidate: `docs(story-2-17): iter-309 /bmad-create-story DRAFTING — _(no story) → drafted; 475-line XL draft + 17 Tasks + 3-layer defense + sub-tree-hash design + 36-DEFER absorption plan`.

_(iter-302 / iter-303 / iter-304 / iter-305 / iter-306 / iter-307 / iter-308 LANDING detail pruned per Guardrail 2; retained in commits `d9e031b` / `441d710` / `7c89ff1` / `128e1b7` / `efde582` / `dfe14fd` / `1fa09ff` + story-file Status HTML comment chain + story-file Change Log 0.1-0.7 rows.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **16/17 stories done** (2.1-2.16) + **Story 2.17 `drafted` at iter-309** (Epic-2 final story).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live smokes operator-workstation-deferred.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:121; bash + jq baked at :36-37 + :48). Host-side shim count: **18** at iter-309 (unchanged — drafting iter; no shim substrate code edits).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **16 done** (2.1-2.16); **1 drafted** (2.17). Epic 2 closes at Story 2.17 done.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.17 — Hook + settings bypass-resistance (git-layer + manifest + S4 + halt).
- **Story File:** `_bmad-output/implementation-artifacts/2-17-hook-settings-bypass-resistance-git-layer-manifest-s4-halt.md` (475 lines; iter-309 draft).
- **Story State:** `drafted` — iter-310 NOW = pre-dev SM validation via `/bmad-create-story (args: "review")` per § Story Lifecycle row `drafted`.
- **GitHub Issue:** no GitHub Project configured; `RALPH_ISSUE_NUMBER` unset. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` unchanged iter-272..309).

## Notes

- **iter-309 NOVEL LESSON CANDIDATE — comprehensive-research-subagent for XL drafting iter:** Story 2.17 drafting dispatched a single `general-purpose` research subagent with a ~700-word brief covering five context areas (previous-story intel + sibling-story intel + deferred-work inventory + PRD/arch citations + substrate surface). Subagent returned a 5,700-word structured report in ~451s using 203K internal tokens / 46 tool_uses (well above iter-308 BH's 57K "enumerate touched files" optimisation; research is scope-unbounded by design). **Parent-context consumption ~30K for the report itself**, enabling the 475-line story file to be drafted in a single subsequent `Write` call. **Pattern generalises:** for XL-class drafting iters (Story 2.15/2.16/2.17-class with 10+ Tasks + wide artefact-surface), dispatch ONE comprehensive research subagent up-front instead of doing 20-30 sequential in-context Reads. Budget arithmetic: comprehensive-research pattern consumes ~30K parent-context for ~6K-word report; in-context approach would consume ~60-80K for equivalent coverage across N sequential tool-use rounds. Net parent-context savings: ~30-50K per XL drafting iter. **Promotion candidate at iter-312+ (after Story 2.17 dev-story iter confirms the pattern under XL implementation scope — not just drafting).** Track: if dev-story iter reuses a comprehensive research subagent with similar budget profile, promote to § Lessons as "comprehensive-research-subagent pattern".
- **iter-309 NOVEL architectural decision — `hashScope` schema extension (Story 2.17 Task 1.1):** the three `kind` variants (`jq-subtree` / `anchor-range` / `names-and-shebangs`) cover three distinct fork-extension semantics — JSON with fork-extension-slots (settings.json), non-JSON with comment-delimited regions (reserved; no current consumer), and directory-enumeration-with-byte-body-drift (git hooks regenerated by prek). This is an invariants-manifest architectural pattern that Story 2.17 dev-story will implement; if Epic 3+ stories encounter similar substrate files with fork-extension semantics, the three-variant extension subsumes them. Architectural decision lives in story file § Content-hash scoping strategy (Dev Notes) — NOT promoted to AGENTS.md at iter-309 (substrate-invariant pattern is machine-enforced; belongs in `packages/keel-invariants/` substrate docs once Task 1 lands, promoted from story Dev Notes to `docs/invariants/fork.md` § Content-hash scoping at dev-story iter close).
- **iter-309 PRESERVED carry-forward — iter-301 NOVEL LESSON CANDIDATE (SSH :22 egress retry budget extension) reaches seven-data-point threshold:** iter-302..308 first-try successes + iter-309 `gh pr view 230` NOT attempted this iter (drafting iter; no PR checks). 7-iter baseline window with 6 first-try + 1 one-retry at iter-308. **PROMOTION to § Lessons STABLE eligible at next iter where `gh pr view` is exercised.** Carry-forward posture: budget 4-5 retries with exponential pacing for `gh pr view` / `git push` under DinD backend B; do NOT escalate to halt after three.
- **iter-309 PRESERVED carry-forward — iter-305 NOVEL LESSON (hook self-immolation + pre-install `bash -n` discipline):** Story 2.17 Task 12 codifies this as a pre-commit invariant gate (`check-claude-hook-syntax.ts`) — transforming the iter-305 LESSON from Ralph-private journal entry to machine-enforced substrate contract. If Task 12 lands cleanly at Story 2.17 dev-story, the lesson graduates from RALPH.md (private) to `docs/invariants/claude-hook-denylist.md § Pre-install discipline` (substrate) — maximally strong promotion class.
- **iter-309 PRESERVED carry-forward — iter-301 NOVEL LESSON CANDIDATE (doc-heavy narrow-diff CR drift) fate pending Story 2.17 CR:** Story 2.16 CR PATCH = 2 (undercut hypothesis). Story 2.17 CR will be the 6-data-point threshold for "narrow-after-SM-absorb ZERO-PATCH + CR separate drift class" STABLE LESSON promotion. If Story 2.17 CR < 5 PATCH despite wider surface + sub-tree-hash-design drift-risk, the lesson STABILIZES: CR drift magnitude correlates with pre-dev SM absorption completeness, not story class. If ≥ 5 PATCH, pattern is story-class-dependent and does not generalise.
- **Cumulative DEFER forecast for Story 2.17 Epic-2 close-out:** 83 cumulative Epic-2 DEFERs at iter-308 close (55 carry-forward + 25 new + 3 SC-17 candidates). Primary-scope absorption at Story 2.17 dev-story: 36 (25 hook-layer + 8 permissions-layer + 3 trace). Secondary SC-17 polish absorption at CR or dev-story Task 16: 15-25. Expected carry-forward to Epic 3+ at Epic-2 close: **<30 DEFERs** (all Epic-1 doc-drift + early-Epic-2 devbox-substrate-code-changes out of Story 2.17 touch-set).
