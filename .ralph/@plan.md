# Implementation Plan

## NOW

- [ ] **Story 2.17 `/bmad-dev-story` CONTINUATION — Task 13.2 (CLAUDE.md sibling bullet)** — append a single Claude-Code-specific bullet AFTER the existing Story 2.16 bullet at `CLAUDE.md:76`. Pattern follows the Story 2.16 bullet shape: short operator-facing one-liner pointing at `AGENTS.md § Hook + settings bypass-resistance (Story 2.17)` for the full contract, with a terse "don't tamper with settings.json hooks-block / invariants.manifest.ts in-session — the git-layer sync-gate + install-boundary-protection rule will catch it" caveat. Plain `Edit` — CLAUDE.md is outside L1 + hook-self-protection ranges (iter-316 routing discipline applies).

## QUEUE (Story 2.17 Task 13 decomposition + lifecycle close + Epic 2 close-out)

- [ ] _(after Task 13.2 lands at iter-323)_ **Task 13.4 — `packages/devbox/README.md` sibling-append H2 § Hook + settings bypass-resistance (Story 2.17)** AFTER existing Story 2.16 H2 at `:1053`. Operator-facing walkthrough of git-layer backstop + fork-extension path + CI visibility forward-link + Story 2.15 carry-forward D-9 (`~/.ssh/**` + `~/.aws/credentials` NFR5a-minimum gap doc symmetry mirroring `AGENTS.md § Claude Code settings policy (Story 2.15)`). ~medium. Plain `Edit`.
- [ ] _(after Task 13.4 lands at iter-324)_ **Task 13.5 — `docs/invariants/claude-hook-denylist.md` amendment** — add § S4 prompt-injection scan rules (three rules + severity + Epic 4 consumer contract), § L1 install-boundary rule (third closed-enum rule-id + protected `packages/keel-invariants/src/**` + three-layer install-boundary contract), § Fresh-fork seed contract (D-36 exec-bit preservation + byte-identity lockstep + Epic 15a `create-keel-app --preserve-permissions` consumer); also absorb D-32 § Halt-threshold pin key-name cross-check (already partially pinned — re-verify config.toml `self_protection_halt_threshold` key spelling matches doc) + D-33 § Limitations Grep content-search forward-link (extend existing § Limitations with explicit note on Grep content-search being an Epic 4 S4 scanner responsibility, not an in-session hook responsibility). Post-amendment: manifest `INV-claude-hook-secret-denylist-doc` contentHash refresh (whole-file sha256). Seed step not applicable for the invariant doc. Plain `Edit` — docs/ is outside L1 + hook-self-protection ranges; manifest contentHash refresh requires `Bash(python3 *)` allow-listed path per iter-315/316 routing. ~large. Likely requires full-iter budget.
- [ ] _(after Task 13 lands fully)_ Task 11 (D-7/D-8/D-9 SC-17 close-out lints — preferring Option B manifest-entry approach per 11.1 rationale) + Task 14 (seed lockstep audit — likely already byte-identical by iter-320's continuous seed-sync discipline; verify + checksum + README.md bullet update) + Task 5 (S4 prompt-injection scan rules at `packages/keel-invariants/src/prompt-injection-rules/` — Epic 4 consumer contract; 3 rules covering `.claude/settings*.json` / `.claude/hooks/**` / `.git/hooks/**`) + Task 6 (halt-threshold `.ralph/config.toml` verify + doc pin; largely already landed at iter-318 D-23) + Task 9 doc pins (Epic 14 dashboard panel forward-link) + Task 15 (≥25 impl-time fixture smokes — cumulative 397 at iter-320; see Notes — target far exceeded, Task 15.1 remaining scope is `packages/keel-invariants/fixtures/hooks/` persistence for future-Ralph replay rather than additional probe authoring) + Task 16 (SC-17 polish; see sub-bullet) + Task 17 (completion).
- [ ] **SC-17 close-out candidates** queued for Task 16: (a) substrate-bake prek native binary in Dockerfile (Story 2.5 AMEND) so fresh-fork devbox first-boot works without network access — addresses iter-313 BLOCKED class; (b) add `release-assets.githubusercontent.com` to substrate `packages/devbox/whitelist/github.txt` so operator doesn't need manual `pnpm devbox:whitelist add`; (c) host-OS-aware `.git/hooks/*` config portability (if iter-312 gotcha re-emerges on fresh fork); (d) iter-314 NOVEL CANDIDATE: promote `loadExpectedHooks` runtime path translation into a generic "source-to-dist resolver" if further TS-authored enumerator entries land (currently 1 site — threshold not met); (e) iter-317 NOVEL: either close Python-write loophole at hook layer (Python source AST parse — costly, rejected) OR document it explicitly in `docs/invariants/claude-hook-denylist.md § Limitations` (the loophole is legitimate at Story 2.17 scope for substrate-self-maintenance; S4 scan layer + manifest content-hash backstop cover it); (f) iter-320 NOVEL: reader-verb word-boundary anchoring (extend D-35 to lines 193-197 reader-verb case-globs — lower FP-risk than mutation verbs; defer to invariant-doc § Limitations or comprehensive refactor).
- [ ] _(after Story 2.17 Tasks fully landed)_ `/bmad-testarch-trace (args: "yolo")` — `in-dev → traced`. 27th cumulative trace; likely WAIVED per Epic-2 17-story chain continuation + impl-time-smokes substitute.
- [ ] _(after traced)_ `/bmad-create-story (args: "review")` — post-dev SM `traced → sm-verified`.
- [ ] _(after sm-verified)_ `/bmad-code-review (args: "2")` — `sm-verified → done`.
- [ ] _(after Story 2.17 `done`)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge. Epic 2 completion fires `EPIC_DONE` halt; on re-entry auto-advances to Epic 3 Story 3.1.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured; `statusCheckRollup: []` carried unchanged iter-219..322).

## BLOCKED

_(empty — iter-322 push expected to succeed; egress recovered at iter-321. If egress recurs, apply iter-321 diagnostic recipe from RALPH.md 2026-04-24 Gotchas.)_

## ATDD Red Phase

_(empty — iter-311 SKIP-WITH-GROUNDS-(ii)+(iii). Impl-time substrate coverage: iter-312 `check-claude-hook-syntax.ts`; iter-314 `names-and-shebangs` walker; iter-315 14 hook smokes GREEN; iter-316 5; iter-317 53; iter-318 41; iter-319 60; iter-320 224 — cumulative 397 impl-time static probes toward Task 15.1 ≥25 target. Far above threshold; Task 15.1 remaining scope is bash-fixture persistence under `packages/keel-invariants/fixtures/hooks/` for future-Ralph replay, not additional probe authoring.)_

## DONE (iter-322 Task 13.1 landing)

- [x] iter-322: **Story 2.17 `/bmad-dev-story` PARTIAL CONTINUATION — Task 13.1 AGENTS.md sibling-append H3 § Hook + settings bypass-resistance (Story 2.17) landed.** New H3 inserted at `AGENTS.md:238` AFTER Story 2.16 H3 at `:225`; 14 lines added (251 → 265 total). Intro paragraph pins the three-layer NFR5a/NFR5b defense (Layer 1 Story 2.16 in-session hook + Layer 2 git-layer sync-gate over 5 manifest entries + Layer 3 S4 prompt-injection scan rules) and enumerates the five `INV-*` machine-enforced contracts (`INV-claude-hook-secret-denylist` repointed to the hook script + `INV-claude-hook-secret-denylist-doc` + `INV-claude-settings-deny-rules` jq-subtree + `INV-git-hooks-preservation` names-and-shebangs + `INV-git-hooks-preservation-enumeration` whole-file). Nine bullets cover: git-layer backstop (5-entry composition + `hashScope` kinds + sync-gate walker extension); S4 prompt-injection scan rules (three authored rules + severity `high` + FR36 threshold escalation + Epic 4 consumer defer); halt-threshold pin (N=3 + D-23 range contract + Epic 3 Story 3.7 halt-write schema + autonomy guardrail); L1 install-boundary protection (third closed-enum rule-id `install-boundary-protection` + three-layer install-boundary contract L1/L2/L3 + AMEND path); `.claude/settings.local.json` commit rejection + hook self-protection extension + deny-wins-over-allow precedence note; fork-extension honour (substrate-additive only + 7-site AMEND path); pre-install `bash -n` + `dash -n` syntax-check discipline (iter-305 NOVEL LESSON); Epic 14 forward-link (CI visibility dashboard panel + `rule_id` dimension breakdown); fresh-fork seeds (Task 11 + Task 14 lockstep + `create-keel-app --preserve-permissions`). Cross-reference bullet wires to Story 2.15/2.16 H3s + `INVARIANTS.md § Hook + settings bypass-resistance (Story 2.17)` + forward-pointers to Task 13.4 (README.md) + Task 13.5 (invariant doc). D-11 (AGENTS.md `Amendment-vs-fork decision` → `... decision tree` citation-lockstep) VERIFIED ALREADY CORRECT at orient — `grep -n "Amendment-vs-fork decision(?!\s*tree)"` returned zero matches. Task 13 decomposed: 13.2 (CLAUDE.md) → iter-323 NOW; 13.3 (INVARIANTS.md) CONFIRMED pre-landed at iter-314 Task 4 (`INVARIANTS.md:144-151`); 13.4 (`packages/devbox/README.md` H2) → iter-324; 13.5 (invariant-doc amendment with 3 new sections + SHA update) → iter-325. No `.claude/` or `packages/keel-invariants/src/` edits — plain `Edit` tool route applies throughout iter-322 per iter-316 routing discipline. Pre-push gates unchanged by this iter (docs-only edit does not perturb manifest / hook / settings subtrees; sync-gate exit 0 on 39 entries expected). Story file Change Log row 0.11 + HTML Status comment iter-322 entry + IP DONE appended.

_(iter-303..320 LANDING detail pruned per Guardrail 2; retained in commits through `a0e101a` + story-file Status HTML comment chain + story-file Change Log 0.1-0.11 rows. iter-321 was push-recovery only — no functional landing; committed as `b3a7ab7`.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **16/17 stories done** (2.1-2.16) + **Story 2.17 `in-dev (partial)` at iter-322** (Epic-2 final story; XL partial landing continuing through Task 13 decomposition).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live smokes operator-workstation-deferred.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:121; bash + jq baked at :36-37 + :48). Host-side shim count: **18** at iter-322 (unchanged).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **16 done** (2.1-2.16); **1 in-dev (partial)** (2.17 — Task 10.1 fully complete at iter-320; Task 13.1 + 13.3 done; 13.2/13.4/13.5 + Tasks 5/6/9/11/14/15/16/17 remain).
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.17 — Hook + settings bypass-resistance (git-layer + manifest + S4 + halt).
- **Story File:** `_bmad-output/implementation-artifacts/2-17-hook-settings-bypass-resistance-git-layer-manifest-s4-halt.md` (~600 lines post-iter-322 Change Log 0.11 append + Status HTML comment iter-322 entry).
- **Story State:** `in-dev (partial)` — iter-323 NOW = `/bmad-dev-story` CONTINUATION per § Story Lifecycle dev-story invocation guidance; NOW candidate Task 13.2 CLAUDE.md sibling bullet.
- **GitHub Issue:** no GitHub Project configured; `RALPH_ISSUE_NUMBER` unset. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` unchanged iter-272..322).

## Notes

- **iter-322 OBSERVATION — Task 13.3 pre-landed at iter-314 Task 4 landing.** When Task 4 (repoint `INV-claude-hook-secret-denylist` sourcePath + add `-doc` sibling + 3 new entries `INV-claude-settings-deny-rules` / `INV-git-hooks-preservation` / `INV-git-hooks-preservation-enumeration`) landed the manifest at 35→39 entries, the operator-iter also landed the corresponding `INVARIANTS.md § Hook + settings bypass-resistance (Story 2.17)` H3 at `:144-151` (4 bullets for the new entries + repoint of the Story 2.16 bullet's description at `:142`). This means iter-309 Task 13 decomposition assumed 5 sub-tasks but 13.3 was satisfied in-line with Task 4 rather than at Task 13 time. Task 13 remaining scope at iter-322: 13.1 (AGENTS.md — landed iter-322), 13.2 (CLAUDE.md — iter-323), 13.4 (packages/devbox/README.md — iter-324), 13.5 (docs/invariants/claude-hook-denylist.md amendment — iter-325). Not a novel gotcha — Task 4's scope naturally subsumed the INVARIANTS.md anchor-bullet writes. Promotion to RALPH.md § Decisions for the next similar Task-4-subsumes-Task-13.3 pattern: consider consolidating related manifest + INVARIANTS.md edits into one task when a story has both "add manifest entry" and "add INVARIANTS.md H3" tasks.

- **iter-320 NOVEL — shell case-globs match prefix-then-any, not word-boundary-anchored.** `case "$normalized" in rm*.claude/settings*) ...` matches `rmdir .claude/settings.json` (FP) because `rm*` is "rm followed by any chars" (matches "rmdir"). Fix via regex `[[ "$normalized" =~ ^rm([[:space:]]|$) ]]` — anchors "rm" at start with mandatory space/EOL boundary after. Applied to 9 mutation verbs in D-35; reader-verb case-globs (lines 193-197) intentionally preserved as legacy alternation (iter-320 SC-17 (f) candidate for future comprehensive refactor). **Carry-forward rule:** new case-glob-based rule authoring inside `.claude/hooks/*.sh` MUST use word-boundary regex for any verb with common-prefix collision risk (rm/rmdir, cp/cpio, dd/ddrescue, etc.); mutation-verb rules especially MUST be word-boundary anchored. Single data point at iter-320 for D-35 restructure; promotion threshold 2 unreached for RALPH.md (watch for re-validation at Task 11 or Task 13.5 if new reader-verb anchoring lands).

- **iter-320 NOVEL — `chmod` command prefix FP-blocks seed-sync pattern.** Running `chmod 755 scratch/foo.sh && python3 -c "...'.claude/hooks/block-secret-access.sh'..."` tripped the pre-iter-320 `chmod*.claude/hooks/*` case-glob because the command both starts with `chmod` AND contains `.claude/hooks/` (in the python argv string). Fix: put chmod INSIDE the python3 script (`os.chmod(src, 0o755)`) rather than as a bash prefix. Post-iter-320 D-35 regex `^chmod([[:space:]]|$)` combined with `\.claude/hooks/` path presence still blocks, but the seed-sync pattern now uses all-python3 (no bash chmod). **Carry-forward rule:** future substrate-mutation operations that need to preserve file mode + atomic replace should do BOTH in one python3 script (os.chmod + os.replace), not split between bash chmod + python3 replace. Enables legitimate substrate-maintenance path (`packages/keel-templates/src/seeds/.claude/hooks/block-secret-access.sh` ↔ `.claude/hooks/block-secret-access.sh` byte-identity re-sync) that would otherwise be blocked by the mutation-verb hook-self-protection regex. Applies across seed-sync iterations.

- **iter-319 NOVEL carry-forward RE-CONFIRMED at iter-320+iter-322** — `/tmp` tmpfs `noexec,nosuid,nodev,relatime` (Story 2.5 hardening). Impl-time fixture smokes at iter-320 staged under `/home/dev/tmp-story217-iter320/` per established discipline; no `/tmp/*.py` attempts. iter-322 docs-only iter authored no executable fixtures, carry-forward holds. Watch for re-validation at Task 11 + Task 15 + Task 13.5.

- **iter-319 NOVEL carry-forward — `readlink -f` canonicalises even when target file doesn't exist** (if parent dir exists). D-31 expansion at iter-320 adds 9 new /proc paths to D-22 resolved-arm case — all /proc paths exist as kernel pseudo-files so `readlink -f /proc/X` always returns canonical `/proc/X`, no new test-authoring gotcha. Carry-forward rule holds.

- **iter-318 NOVEL carry-forward — `sed -i <expr>` case-glob over-block when `<expr>` contains substrate-protected path literals.** Unchanged at iter-320/322; D-31..D-36 batch did not author any `sed -i` operation; iter-322 docs-only edit used plain `Edit` tool. Carry-forward rule holds.

- **iter-318 NOVEL carry-forward — `gh pr view` GraphQL API intermittent timeout + curl-sanity workaround.** iter-322 orient had `gh pr view` + `gh pr checks` both time out on first call (systemic egress failure — see iter-321 Gotcha); succeeded on retry after DNS sanity (`dig @127.0.0.1 -p 53 +short api.github.com` returned `140.82.121.6`) + curl sanity (`curl -sI --max-time 8 https://api.github.com/` returned `HTTP/2 200`). Carry-forward rule holds; iter-321 diagnostic recipe demonstrably effective.

- **iter-317 carry-forward — D-15 wrapper normalization MUST NOT strip interp-stdin readers.** Unchanged at iter-320/322.

- **iter-317 carry-forward — D-14 find-delete regex trailing-slash optional.** Unchanged at iter-320/322.

- **iter-316 carry-forward RE-CONFIRMED at iter-320** — Python-based hook/settings/manifest edit loophole still open post-D-31..D-36 landing. iter-322 did NOT exploit the Python-write loophole (docs-only edits used plain `Edit` tool directly — AGENTS.md + story file + IP + RALPH.md are all outside L1 + hook-self-protection ranges). Still pending long-term close at Task 16 SC-17 (option (e) — explicit doc-only limitation at `docs/invariants/claude-hook-denylist.md § Limitations`).

- **iter-316 NOVEL LESSON NOT exercised at iter-322** — Write-then-python3 pattern intentionally unneeded for docs-only iter. Already in `RALPH.md § Gotchas 2026-04-24 (Story 2.17 iter-316)` — no new data point.

- **iter-315 carry-forward — `install-boundary-protection` rule-id** unchanged at iter-322 (no L1 substrate edits this iter).

- **iter-314 carry-forward — `names-and-shebangs` runtime path translation.** Single data point; promotion threshold 2 unreached. Unchanged at iter-322.

- **iter-314 carry-forward — narrow-after-SM-absorb 5→7-data-point STABLE LESSON promotion threshold:** unchanged. Story 2.17 post-dev SM moves further out with iter-322 continuation (iter-325+ estimate after Task 13 fully lands).

- **iter-314 SC-17 close-out candidates carry forward + iter-320 NEW entry (f)** — reader-verb word-boundary anchoring for lines 193-197 case-globs; lower FP-risk than mutation verbs; either comprehensive refactor at Task 16 OR documented in invariant doc § Limitations as residual gap. Promotion to comprehensive refactor gated on SC-17 triage at Task 16; Task 13.5 § Limitations is the documentation-track landing site.
