# Implementation Plan

## NOW

- [ ] **AI-10 — `whitelist-host.sh:43` + `monitor-host.sh:55` TTY-detect `[[ -t 0 ]] && tty_flag="-it" || tty_flag="-i"`** so non-TTY CI / subprocess invocation doesn't fail with "the input device is not a TTY". Top-of-QUEUE per § Story Lifecycle row `fixes-pending`. Evidence: both shims hardcode `-it`; the `-t` flag requires a TTY, so `pnpm devbox:whitelist add foo.com` from CI / hooks / `sh -c` errors before reaching the in-container script. Fix: detect stdin-is-tty with `[[ -t 0 ]]`, set `tty_flag` accordingly, substitute into the `docker exec` call. Scope: line-level edits on whitelist-host.sh + monitor-host.sh. Note: AI-9 iter-213 added `--user dev` to whitelist-host.sh but kept `-it` hardcoded (CASE 2 backward compat); AI-10 is the corresponding TTY-detect follow-up. ~small.

## QUEUE (Story 2.6 lifecycle → Epic 2 Story 2.7..2.17 → Epic 2 close-out)
- [ ] AI-11 `restart.sh:18-21` poll `docker inspect ... .State.Status` until `exited` (or container disappears) before invoking start.sh, bounded by 10s timeout — otherwise `docker compose stop`-returns-when-SIGTERM-sent race causes start.sh to see lingering OLD container and report "started" prematurely.
- [ ] _(post-AI-11; QUEUE empty)_ `/bmad-code-review (args: "2")` — CR-closure gate (forecast: ZERO-PATCH per Epic-2 3-story precedent: 2.3 iter-171, 2.4 iter-183, 2.5 iter-197). 2 PATCH remain (AI-10..AI-11) after iter-213.
- [ ] _(post-Story-2.6-done; through Story 2.17-done)_ Iterate Story 2.7..2.17 through the full lifecycle — 11 remaining substrate stories spanning ralph-auto-start + claude OAuth + gh OAuth + prereq-check + shared mode + port-publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-22 per iter-197..iter-204 pre-flight — "no checks reported on the 'feat/epic-2-packaged-devbox' branch").

## BLOCKED

_(none)_

## ATDD Red Phase

_(none — Story 2.6 ATDD-skipped at iter-200 per § Story Lifecycle `validated → atdd-scaffolded` (skip branch). 16th-cumulative FR14n ATDD-skip precedent. iter-202 trace gate WAIVED confirms the ATDD-skip-trace-WAIVED pairing — SEVENTEENTH cumulative pairing across Epic 1 + 2.)_

## DONE (Story 2.6 lifecycle — keep CR-drain chain, iter-211 pruned at iter-213)

- [x] iter-213: **AI-9 RESOLVED — `whitelist-host.sh:37` `--user dev` pin lands, matching `shell.sh:39,42` precedent.** The `exec docker exec` call is now `exec docker exec -it --user dev "${CONTAINER_NAME}" /workspace/packages/devbox/scripts/whitelist.sh "$@"` — `--user dev` inserted between `-it` and container positional (docker CLI requires flags BEFORE positional). 6-line comment block at `whitelist-host.sh:37-42` anchors WHY: image default USER is `dev` per Story 2.5 Dockerfile so today it's an explicit-pin, not a posture change; guard activates when fork's `docker-compose.override.yml` flips `user: root`, at which point whitelist mutations would otherwise write `/run/keel-whitelist-mutate.lock` (SC-8) + `whitelist.local.txt` (SC-4) with root ownership — any subsequent `dev`-user mutation then fails (flock + atomic-rename both EPERM). Scope-bounded to whitelist-host.sh: monitor-host.sh NOT touched — monitor is tail-only read-only, no write-state footprint; AI-9 text explicitly targets whitelist's mutation semantics. Did NOT add `-w /workspace` — whitelist.sh resolves paths via `BASH_SOURCE[0]`+`DEVBOX_DIR` arithmetic, cwd not load-bearing. Iteration-env-safe shim smoke (13 assertions, `.ai9-smoke/run.sh` under workspace bypassing /tmp tmpfs noexec per Story 2.5 NFR8a, auto-cleaned): stub `docker` logs argv on `exec`, responds `running` to `inspect`, `exit 0` to `info`; PATH-prepended; invokes `whitelist-host.sh list` + `add foo.example`. 6 integration cases (--user dev present in argv / -it retained backward-compat / container+target-path / subcommand forwarding preserves --user dev / --user dev precedes container per flag-parse order / pre-fix `-it`-immediately-followed-by-`keel-devbox` pattern absent) + 2 buggy-before regressions via awk-reconstructed pre-fix single-line exec (buggy parses clean + buggy log has NO --user dev substring) + 4 source-level drift-guards (literal `exec docker exec -it --user dev` / AI-9 iter-213 anchor / AI-8 unset retained / `bash -n` clean). `whitelist-host.sh` NOT in `invariants.manifest.ts` sourcePath → no sync-gate refresh. Quality gates: lint ✓, format:check ✓, typecheck ✓, keel-invariants:check-all ✓. 2 PATCH remaining (AI-10..AI-11); iter-214 drains AI-10.
- [x] iter-212: **AI-8 RESOLVED — `unset COMPOSE_PROJECT_NAME` landed across all 9 host-side scripts (start / stop / restart / shell / attach / status / logs / monitor-host / whitelist-host).** Uniform 4-line block placed immediately after `set -euo pipefail` + before any `SCRIPT_DIR`/`CONTAINER_NAME` assignment, ensuring the env var is absent by the time every `docker info` / `docker inspect` / `docker compose` invocation runs. Chose `unset` over runtime-discovery (`docker compose -f … ps -q devbox`) because unset is a uniform one-line fix AND preserves the `keel-devbox_keel_home_dev` volume path pinned by INV-devbox-homedev-named-volume; runtime-discovery would leak the overridden project name into the volume FQ-name. Restart.sh gets unset too (with a comment noting stop.sh + start.sh each unset defensively) covering any future direct compose call. Scope-bounded to the 9 CR-flagged scripts; clean.sh + build.sh + rebuild.sh recorded as AR-20/AR-21 carry-forward defers in `deferred-work.md § Deferred from: Story 2.6 AI-8 iter-212` (not a defect — AI-8 was scoped to container-discovery scripts; clean has a destructive-path drift that is ironically safer than discovery-path drift, build/rebuild are cosmetic-log-drift-only). Iteration-env-safe env-shim smoke (stub `docker` under `.ai8-smoke/` inside workspace to bypass `/tmp` tmpfs noexec per Story 2.5 NFR8a, auto-cleaned): 9 test cases with `COMPOSE_PROJECT_NAME=my-fork-ai8` exported + `PATH` pre-pended; control probe confirms unshielded `docker info` sees `CPN=my-fork-ai8 set=yes`; each of the 9 scripts' first shim call logs `CPN=<unset> set=` (empty `set` marker proves truly absent not merely empty). Source-level ordering guard: `unset` line precedes first executable `docker` invocation in every script (start=37<48, stop=20<28, restart=18<no-direct, shell=20<26, attach=21<27, status=20<29, logs=19<27, monitor-host=28<36, whitelist-host=20<26). `bash -n` × 9 ✓. No invariant sourcePath touched → no sync-gate contentHash refresh. Quality gates: lint ✓, format:check ✓, typecheck ✓, keel-invariants:check-all ✓. 3 PATCH remaining (AI-9..AI-11); iter-213 drains AI-9.
## Context

- **Phase:** 4-implementation — Epic 2 at 5/17 stories `done` (2.1 + 2.2 + 2.3 + 2.4 + 2.5) + 12/17 remaining; Story 2.6 fixes-pending (in-progress; 9/11 PATCH drained) + 11 backlog (2.7..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.6 live lifecycle smokes operator-workstation-deferred per Story 2.4 SC-17 + Story 2.5 iter-187 precedent. PATCH-drain quality chain for shell-script edits: `bash -n` per touched script + iteration-env-safe behavioural smoke per AI fix + sync-gate (when invariant sourcePaths touched) + typecheck + lint + format:check + docker compose parse-smoke (for compose-touching fixes only).
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Story 2.5 substrate live. Story 2.6 PATCH drain is host-side script-only — no Dockerfile changes (SC-25 scope-boundary forbids; AR-9 escalation path conditional + not triggered).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; 5 done (2.1-2.5); Story 2.6 fixes-pending (in-progress); 11 backlog (2.7..2.17). Epic 2 closes at Story 2.17 (PR #230 Draft→Open transition at that point).
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.6 — host-side `pnpm devbox:*` CLI surface. Scope-owner for AR-7 `/run` relocation (SC-19, conditional — NOT triggered) + AR-9 `/etc/*` chown (SC-20, conditional — NOT triggered) + AR-10 pre-Story-2.5 volume migration docs (SC-21, LANDED iter-201) + AR-11 envvar shape validation (SC-14, LANDED iter-201) + AR-12 COMPOSE_PROJECT_NAME docs polish (SC-22, LANDED iter-201). NEW at iter-204: scope-owner for AI-1..AI-11 PATCH drain.
- **Story File:** `_bmad-output/implementation-artifacts/2-6-host-side-pnpm-devbox-cli-surface.md`
- **Story State:** `fixes-pending` — transitioned at iter-204 via `/bmad-code-review (args: "2")` opener. 11 PATCH action items recorded; 9/11 drained (AI-1 iter-205, AI-2 iter-206, AI-3 iter-207, AI-4 iter-208, AI-5 iter-209, AI-6 iter-210, AI-7 iter-211, AI-8 iter-212, AI-9 iter-213); 2 remaining (AI-10..AI-11). Iter-214+ continues one PATCH per iteration per § Story Lifecycle row `fixes-pending` top-of-QUEUE. On QUEUE drain, re-run `/bmad-code-review (args: "2")` for closure gate (forecast: ZERO-PATCH per Epic-2 3-story precedent → direct `done`).
- **GitHub Issue:** Story 2.6 TBD (RALPH_ISSUE_NUMBER unset in this iter env). Epic 2 → #10 (Ralph closes on EPIC_DONE halt).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **Quoting-aware shell-parse `first_char` probe generalises (iter-210 LESSON):** The `first_char="${value:0:1}"` + `${rest%%${first_char}*}` pattern cleanly extracts quoted content without regex + avoids greediness pitfalls of `[[ =~ ^\"(.*)\"$ ]]`. Works identically for `"` and `'` via variable dispatch. Carry-forward: any future shell-parse work that needs quoting-aware value extraction can reuse this 3-line pattern (first-char probe → capture-to-matching-quote → trailing-ws strip). Applies to potential future parsers in `whitelist.sh` (rule-file values) and `start.sh` (status format strings) if the need arises.
- **CR opener PATCH-count scales linearly with script-count, not story complexity (iter-204 LESSON):** Story 2.1 = 1 PATCH, Story 2.4 = 5 PATCH (1 in-container script), Story 2.5 = 6 PATCH, Story 2.6 = **11 PATCH (13 host-side scripts)**. Carry-forward rule for Stories 2.7+ user-facing-CLI class: if the surface adds ≥10 new scripts, expect 8-12 PATCH at CR opener. Drain-iter forecast scales similarly (Story 2.6 forecast 12-iter drain: 11 PATCH iters + 1 closure re-run iter).
- **Acceptance Auditor ZERO-FINDING holds even at upper-PATCH-count CR (iter-204 LESSON):** Acceptance Auditor returned ZERO PATCH-or-higher AC deviations despite 11 PATCH from Blind + Edge Hunters. CR-opener PATCH counts are code-quality issues (shell-script edge cases, error-handling gaps), NOT requirement-satisfaction issues. Carry-forward: when pre-dev + post-dev SM both close ZERO-FINDING, expect Acceptance Auditor at CR-opener to also close ZERO.
- **Cross-source semantic-drift audit rule (iter-199 LESSON, VALIDATED across lifecycle gates 201/202/203/204):** SC-15 pinned PRD `:494` as authoritative for `pnpm devbox:monitor`. Pattern works end-to-end across 4 lifecycle gates. Carry-forward rule for Stories 2.7/2.8/2.9/2.12/2.15 remains live.
- **`<verb>-host.sh` host-side shim naming convention (iter-199 EXTENDED, iter-204 CR-VERIFIED):** Story 2.6 ships `monitor-host.sh` + `whitelist-host.sh` as canonical host-side shim naming. iter-204 PATCH drain (AI-3, AI-9, AI-10) all touch the shim layer. Stories 2.7 (Ralph TUI wrapper), 2.8 (claude wrapper), 2.9 (gh wrapper), 2.12 (ssh wrapper) candidates for the same pattern.
- **AR-12 docs-drift was only HALF the story (iter-204 LESSON):** iter-201 landed COMPOSE_PROJECT_NAME docs polish; iter-204 surfaced AI-8 — same override breaks RUNTIME container polling in start.sh + siblings. Carry-forward rule: when an Edge Hunter flags an env-override drift in static docs, ALSO check whether the override breaks runtime script behaviour.
- **Within-epic § Story Lifecycle drain (iter-204 fixes-pending → iter-205+ AI-1..AI-11 → closure re-run):** Story 2.6 `fixes-pending` + 11 PATCH QUEUE → no halt required. Iter-213 drains AI-9. Each PATCH = one iteration. After QUEUE drain, re-run `/bmad-code-review (args: "2")` for closure gate. Epic 2 stays `in-progress`.
