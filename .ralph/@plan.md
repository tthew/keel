# Implementation Plan

## NOW

- [ ] **Story 2.14 trace — `/bmad-testarch-trace (args: "yolo")`.** Forecast WAIVED via ground-(a)+(b) hybrid — twenty-fifth+ cumulative WAIVED precedent; Story 2.14 narrower grounds per iter-289 ATDD-skip observation (drops (c) since ACs are static-smoke-testable: AC 1 verifiable via `git ls-remote origin refs/heads/legacy-devbox` + `git show origin/legacy-devbox:README.md | head -20`; AC 2-4 verifiable via doc-content grep against `docs/invariants/devbox-legacy-branch-retention.md` + sha256 sync-gate). Pattern reaffirmed at iter-291 post Task 1 landing: policy-class stories resolve to pure documentation + git-branch-state + substrate-synchrony. Transition on success: Story State `in-dev → traced`.

## QUEUE (Story 2.14 lifecycle + 2.15..2.17 substrate queue)
- [ ] _(after Story 2.14 traced)_ Story 2.14 post-dev SM — `/bmad-create-story (args: "review")` post-dev. Forecast 0-2 PATCH per iter-270 LESSON; iter-288 pre-dev SM landed 6 PATCH + 1 DEFER, so post-dev absorption band narrow.
- [ ] _(after Story 2.14 sm-verified)_ Story 2.14 CR — `/bmad-code-review (args: "2")`. Forecast 0-2 first-class PATCH per iter-279 + iter-286 ZERO-PATCH precedent for narrow-diff stories.
- [ ] _(after Story 2.14 done)_ Story 2.15 committed Claude settings.json deny/allow — full lifecycle.
- [ ] _(after Story 2.15 done)_ Story 2.16 Claude PreToolUse hooks for secret-file denylist — full lifecycle.
- [ ] _(after Story 2.16 done)_ Story 2.17 hook-settings bypass-resistance + Epic 2 SC-17 close-out polish pass. SC-17 reconciliation absorbs cumulative **31 DEFERs at iter-291 Story 2.14 Task 1 landing** (unchanged from iter-290; Task 1 retry adds zero DEFERs — pure spec-execution) PLUS cumulative Story 2.14 remaining lifecycle (trace + post-dev SM + CR) + Stories 2.15..2.16 accrued DEFERs.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured; `statusCheckRollup: []` carried unchanged iter-219..291).

## BLOCKED

_(none — iter-290 github.com apex-host outage self-resolved by iter-291 orient; Task 1 landed at iter-291 `/bmad-dev-story` re-entry.)_

## ATDD Red Phase

_(none — Story 2.14 ATDD-skipped at iter-289 with grounds-(ii)+(iii); no red-phase tests owed. Carry-forward from iter-289.)_

## DONE (iter-291 `/bmad-dev-story` re-entry — Task 1 LANDED; Story State `in-dev (partial) → in-dev → review`; all four ACs satisfied)

- [x] iter-291: **STORY 2.14 TASK 1 LANDING** (`in-dev (partial) → in-dev → review` at workflow Step 9). Four Tasks now complete; workflow Step 9 completion gate satisfied; sprint-status row `in-progress → review`; story file Status `ready-for-dev → review`. Commit candidate: `feat(story-2-14): iter-291 /bmad-dev-story Task 1 retry — in-dev (partial) → review; origin/legacy-devbox@cfdf011 materialized (cc-devbox@8ea5131)`.
  - **Task 1 executed end-to-end** (single-task re-entry; Tasks 2-4 already landed at iter-290). Pre-flight probe `timeout 10 curl -sI https://github.com` → `HTTP/2 200` (outage self-resolved within ≤1 iter of iter-290; contra iter-281 multi-iter carry-forward precedent). `timeout 120 git fetch https://github.com/tthew/cc-devbox.git main:legacy-devbox` exit `124` (first-attempt timeout insufficient under initial-connection post-outage latency); retry with `timeout 240 git fetch --progress …` exit `0` (125 objects, 112.25 KiB, 38 deltas). Scratch worktree at `.claude/worktrees/legacy-devbox-bootstrap-2-14` (gitignored); retention banner prepended via `cat banner README > new && mv` with `<UPSTREAM_SHA>` placeholder; `sed -i` substituted `UPSTREAM_SHA=8ea5131eecbbfe0d0eb063c55f170cce6915af90`; `grep UPSTREAM_SHA README.md` guard passed. Commit `cfdf011` (`docs(legacy-devbox): Story 2.14 retention banner over cc-devbox@8ea5131`); **prek hooks did NOT fire** (upstream cc-devbox orphan-toolchain; no `.pre-commit-config.yaml`, no keel `package.json`; natural no-op; NO `PRE_COMMIT_ALLOW_NO_CONFIG=1` bypass needed — SCOPE GUARDRAIL preserved); `git push -u origin legacy-devbox` exit `0`; scratch worktree removed. Verification: `git ls-remote origin refs/heads/legacy-devbox` → `cfdf011006d44f52e36f461eacd8395e7f54ac0e`; `git show origin/legacy-devbox:README.md | head -20` confirmed banner + upstream ASCII-art preserved; main working tree `git status` unchanged.
  - **AGENTS.md META-guard grep (iter-286 NOVEL LESSON #3 discipline) satisfied at Task 1 exec time.** Upstream `$WT/docker-compose.yml` grep confirmed `- "3000:3000"` (`:40`) + `healthcheck: test: ["CMD", "sh", "-c", "curl -f http://localhost:3000/api/health || exit 1"]` (`:70-71`) — upstream `main@8ea5131` still ships the BROKEN `curl :3000/api/health` healthcheck as anticipated. Known-divergence, NOT a cherry-pick candidate per § Cherry-pick workflow OUT-OF-SCOPE list.
  - **Cumulative PATCH forecast stable.** Story 2.14 count at `in-dev → review` = 6 (iter-288 pre-dev SM) + 0 (iter-290 partial) + 0 (iter-291 Task 1 retry; pure spec-execution) = on-track for iter-286 NOVEL LESSON ~6-10 lifecycle band. Projected remaining: 0 at trace (WAIVED-forecast) + 0-2 at post-dev SM + 0-2 at CR.

- [x] iter-291 orient: state reconciliation — (a) iter-290 commit `1e600be` verified on `origin/feat/epic-2-packaged-devbox` via `git branch -vv` (HEAD == upstream; `git log @{u}..HEAD` empty) — forecast "expected push FAILURE" did NOT materialize; the outage must have had a push-window between iter-290 dev-story landing and orient probe. (b) `timeout 10 curl -sI https://github.com` returned `HTTP/2 200` at iter-291 orient (confirming iter-290's scoped-outage observation self-resolved within ≤1 iter rather than iter-281's multi-iter carry-forward pattern). (c) `git ls-remote origin refs/heads/legacy-devbox` returned empty — Task 1 branch materialization still owed. (d) PR #230 state re-verified: `{"isDraft":true,"mergeable":"MERGEABLE","state":"OPEN","statusCheckRollup":[]}` unchanged. No halt sentinel present. NOW promoted from stale step-5-push-attempt (no-op; already pushed) to Task 1 retry per § Story Lifecycle Decision Matrix row `in-dev (partial) → /bmad-dev-story`.

- [x] iter-290: Story 2.14 DEV-STORY PARTIAL LANDING — Tasks 2+3+4 ON-MAIN (invariant-doc 167 lines + manifest 33→34 + INVARIANTS.md + README + AGENTS pointers); Task 1 DEFERRED on github.com :443+:22 outage. Commit `1e600be`. Full detail in story file v1.0 Change Log.
- [x] iter-287..289: Story 2.14 DRAFTING → PRE-DEV SM → ATDD-SKIP (`_(no story) → drafted → validated → atdd-scaffolded`); 6 PATCH + 1 DEFER at pre-dev; 24th-cumulative ATDD-skip precedent. Commits `ae169c8`/`3681250`/`ef10b5a`.
- [x] iter-272..286: Story 2.12+2.13 full lifecycles. Commits pruned per Guardrail 2; full detail in story-file Change Logs.

_(iter-253..271 pruned at iter-290; iter-272..289 deep-detail pruned at iter-291 per Guardrail 2.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **13/17 stories done** (2.1-2.13) + Story 2.14 at `review` post iter-291 `/bmad-dev-story` Task 1 landing + 2.15-2.17 backlog.
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live smokes operator-workstation-deferred; substrate smokes covered through iter-291 (`pnpm keel-invariants:check` GREEN at 34 manifest entries — unchanged from iter-290).
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:120). Host-side shim count: **18** at iter-291 (unchanged — Task 1 retry landed git-branch only; zero substrate-code edits).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **13 done** (2.1-2.13); **1 at review** (2.14); **3 backlog** (2.15-2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.14 — Legacy-devbox branch retention policy.
- **Story File:** `_bmad-output/implementation-artifacts/2-14-legacy-devbox-branch-retention-policy.md`
- **Story State:** `in-dev` at iter-291 landing (Task 1 retry complete; Story file Status `ready-for-dev → review` at workflow Step 9; sprint-status row `in-progress → review`). Next per § Story Lifecycle Decision Matrix row `in-dev`: `/bmad-testarch-trace (args: "yolo")`. `origin/legacy-devbox` materialized at SHA `cfdf011` (upstream `main@8ea5131` + 1 commit).
- **GitHub Issue:** Story 2.14 issue unknown; `RALPH_ISSUE_NUMBER` unset. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` unchanged iter-272..291; iter-291 orient `gh pr view 230` returned `{"isDraft":true,"mergeable":"MERGEABLE","state":"OPEN","statusCheckRollup":[]}`).

## Notes

- **iter-291 NOVEL LESSON (initial-connection fetch-timeout budget under post-outage recovery):** default 120s `timeout` wrapping `git fetch` from upstream is INSUFFICIENT under post-outage recovery initial-connection latency; 240s required. `curl -sI` apex probe can be HTTP/2 200 green while pack-transfer fetches still time out at 120s. LESSON: future upstream-fetch tasks under post-outage conditions default to 240s+ timeout; 120s fine for warm-connection operations (subsequent fetches, ls-remote probes) but tight for cold-start pack transfers. Pinned in RALPH.md § Lessons.
- **iter-291 observation (github.com-outage self-resolution within ≤1 iter):** iter-290's scoped-outage pattern (github.com apex :443 + :22 blocked; codeload.github.com reachable) self-resolved between iter-290 push attempt and iter-291 orient. Contra iter-281 multi-iter carry-forward precedent; NOT a reliable expectation — next outage may persist longer. Keep iter-290's NOVEL LESSON (invert-the-defer-order) as the substantive pattern; outage-duration is noise.
- **iter-291 observation (iter-286 NOVEL LESSON #3 META-guard applied AT Task 1 exec):** upstream docker-compose.yml grep at Task 1 exec confirmed expected `curl :3000/api/health` healthcheck — no surprise upstream state. META-guard discipline satisfied (grep performed, recorded in story file Completion Notes + Change Log v1.1). Worth noting: the META-guard pre-empted a potential silent inaccuracy in AGENTS.md cross-reference — the pattern holds as a forcing-function for any spec-endorsed claim.
- **iter-291 observation (no hook-bypass materialized on orphan-toolchain branch):** story file Task 1 § Commit + push anticipated `PRE_COMMIT_ALLOW_NO_CONFIG=1` bypass as a possible worst-case on legacy-devbox branch commits. Empirically, prek hooks naturally no-oped on the upstream cc-devbox layout (no `.pre-commit-config.yaml`, no keel `package.json`) — bypass not needed. SCOPE GUARDRAIL preserved: no hook-bypass precedent established for `main`-targeting commits; Story 1.6's `INV-no-verify-bypass` posture intact.
- **iter-286 NOVEL LESSON forecast-band stable at iter-291:** Story 2.14 cumulative PATCH = 6 (iter-288 pre-dev SM) + 0 (iter-290 partial) + 0 (iter-291 Task 1 retry) = 6. On-track for ~6-10 lifecycle band. Projected remaining: 0 trace (WAIVED-forecast) + 0-2 post-dev SM + 0-2 CR.
- **Cumulative DEFER forecast for Stories 2.14..2.17 Epic-2 close-out:** 31 pending at iter-291 (unchanged from iter-290; Task 1 retry adds zero DEFERs — pure spec-execution). Remaining Story 2.14 lifecycle + Stories 2.15-2.17 accumulate additional. Absorption at Story 2.17 SC-17 close-out.
