# Implementation Plan

## NOW

- [ ] **Story 2.17 `/bmad-dev-story` CONTINUATION — Task 16 (story-spec) `deferred-work.md` triage.** Re-read `_bmad-output/implementation-artifacts/deferred-work.md` in full per story-file Task 16.1-16.4. Audit 47 Epic-1 (Stories 1.8-1.16) + early-Epic-2 (Stories 2.1-2.14) DEFERs; categorise each as **Absorb-in-2.17** (citation-lockstep one-liner touching files Story 2.17 already edits — AGENTS.md / CLAUDE.md / INVARIANTS.md / packages/devbox/README.md / docs/invariants/* — apply inline), **Defer-to-Epic-3+** (substrate code change outside Story 2.17 touch-set; annotate `(SC-17 triage 2026-04-24 / iter-337: out-of-touch-set; defer to <next Story owning that file>)`), or **Obsolete** (file renamed/deleted in subsequent story; remove with rationale `(obsolete: <what happened>)`). Target absorption: **15-25 citation-lockstep DEFERs**; cumulative Epic-2 DEFER balance at close **< 30** (from 83 entry count; 36 substrate-absorbed via Tasks 10-11 + 15-25 polish-absorbed via Task 16 + 0-N triage-carry-forward). Update `deferred-work.md § Story 2.17` section with absorbed + deferred + obsolete final counts. Close with commit-note: `Epic 2 SC-17 close-out: absorbed <N> substrate + <M> polish + carry-forward <K>; Epic 2 DEFER queue at close = <K>`. Distinct from prior iter-333..337 "SC-17 polish bundle" sub-items (a)-(g) — this is the story-spec scope proper. ~medium-large own-iter item per Guardrail 13 budget discipline; no other Tasks in same iter.

## QUEUE (Story 2.17 lifecycle close + Epic 2 close-out)
- [ ] Task 17 (completion): close out Story 2.17 `in-dev (partial) → in-dev` once Task 16 fully lands + verify sub-tasks 15.2/15.3/15.4/15.5 (re-run `pnpm keel-invariants:check-all` 41 entries + `pre-commit run --all-files` + `pnpm --filter @keel/keel-invariants test` 7/7 GREEN + populate Completion Notes with per-AC evidence). Ensure story DoD complete + Dev Agent Record populated + Change Log final row.
- [ ] _(after Story 2.17 Tasks fully landed)_ `/bmad-testarch-trace (args: "yolo")` — `in-dev → traced`. 27th cumulative trace; likely WAIVED per Epic-2 17-story chain continuation + impl-time-smokes substitute. Exception: iter-329 Task 5 authored 7 genuine `node:test` unit tests (the first keel-invariants tests in-repo), so SM review can cite them as AC-3 coverage evidence and trace does not have to waive for Task 5.
- [ ] _(after traced)_ `/bmad-create-story (args: "review")` — post-dev SM `traced → sm-verified`.
- [ ] _(after sm-verified)_ `/bmad-code-review (args: "2")` — `sm-verified → done`.
- [ ] _(after Story 2.17 `done`)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge. Epic 2 completion fires `EPIC_DONE` halt; on re-entry auto-advances to Epic 3 Story 3.1.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured; `statusCheckRollup: []` carried unchanged iter-219..333).

## BLOCKED

_(empty — iter-337 Task 16 sub-items (d)+(e) marker-only defer-record bundle; only IP + story-file Status / lifecycle HTML comment / Change Log row 0.26 mutated; pre-push gates expected GREEN — no manifest / hook / settings / seed perturbation.)_

## ATDD Red Phase

_(empty — iter-311 SKIP-WITH-GROUNDS-(ii)+(iii). Impl-time substrate coverage: 397 cumulative smokes GREEN across iter-315..320 runs + 7 genuine `node:test` unit tests GREEN at iter-329 for Task 5 S4 rules + 74 persisted replay fixtures GREEN at iter-332 (`packages/keel-invariants/fixtures/hooks/run-all.sh` — 55 positive + 19 negative). Total persisted/reproducible at iter-337: 7 unit tests + 74 replay fixtures.)_

## DONE (iter-337 Task 16 sub-items (d)+(e) marker-only defer-record bundle landed)

- [x] iter-337: **Story 2.17 `/bmad-dev-story` PARTIAL CONTINUATION — Task 16 sub-items (d)+(e) marker-only defer-record bundle landed.** Sub-item (d) iter-314 NOVEL `.ts → .js` / `/src/ → /dist/` runtime translation in `loadExpectedHooks` (`packages/keel-invariants/src/manifest-reader.ts:93-108`; single-site fix at iter-314 enabling `names-and-shebangs` walker to read the compiled dist artefact via Node's dynamic `import()` while preserving sourcePath===enumeratorPath convention) recorded `defer: threshold-not-met` — single data point with no second re-validation site to-date; promotion to invariant-doc § Limitations awareness-level guidance (mirroring iter-336 (f)+(g) bundle pattern) deferred until a second site exercises the same translation. Sub-item (e) iter-317 NOVEL D-14/D-15 minor-regex + Python-write loophole carry-forward recorded `complete: pre-landed iter-325` — the L1 install-boundary rule § Substrate-maintenance loophole narrative already landed in `docs/invariants/claude-hook-denylist.md` at iter-325 Task 13.5 amendment (the `python3 <abs-path-to-/tmp-script>` atomic-replace route for L1-protected manifest edits is documented in-doc with cross-reference to `os.replace` vs `shutil.move` cross-fs handling per iter-329 RALPH.md Gotcha entry). Bookkeeping-only iter mirroring iter-335 (Task 16(c) DEFERRED) and iter-330 (Task 6 verify-pass); only IP + story-file Status / lifecycle HTML comment / Change Log row 0.26 mutated; no substrate / hook / settings / manifest / seed perturbation; no manifest contentHash refresh route (IP + story-file outside every hashed path); pre-push gates expected GREEN as iter-330 / iter-335 (typecheck 16/16 + lint 16/16 + format:check + `pnpm keel-invariants:check-all` exit 0 on 41 entries unchanged). **Task 16 sub-items (a)-(g) FULLY RESOLVED** — (a) iter-333 substrate-bake prek; (b) iter-334 release-assets whitelist; (c) iter-335 deferred conditional-on-re-emergence; (d) iter-337 deferred threshold-not-met; (e) iter-337 complete pre-landed iter-325; (f)+(g) iter-336 invariant-doc § Limitations one-bullet bundle. Net Story 2.17 post-iter-337: **3 top-level Tasks remain — 15 (15.2-15.5 fold into Task 17), 16 (story-spec `deferred-work.md` Epic-1 + early-Epic-2 DEFER audit only — sub-items (a)-(g) FULLY RESOLVED), 17 (completion DoD + Dev Agent Record + Change Log final row)**. No novel gotcha / lesson / decision — pure marker-only defer-record bookkeeping; no RALPH.md promotion. Sub-novel observation: defer-record bookkeeping iters now cluster at the closing edge of multi-decomposed Tasks (one-iter-per-decomposed-sub-item or one-iter-per-bundled-pair when sub-items share a docs-amend target — iter-336 bundled (f)+(g) by shared `claude-hook-denylist.md § Limitations` target; iter-337 bundled (d)+(e) by shared marker-only-no-mutation framing; iter-335 ran (c) solo for the unique substrate-restart-stale-state rationale); single data point at iter-337, promotion to RALPH.md as a Ralph-iter-density best-practice on second re-validation.

- [x] iter-336: Task 16 sub-items (f)+(g) bundle — one-bullet append to `docs/invariants/claude-hook-denylist.md § Limitations` (iter-320 case-glob + iter-329 ESLint self-exclusion-glob); manifest `INV-claude-hook-secret-denylist-doc` contentHash refresh `cb32195f…` → `90812335…`. Detail in commit `50851e3`.

- [x] iter-335: Task 16 sub-item (c) DEFERRED `defer: conditional-on-re-emergence`. Detail in commit `ba695d3`.

- [x] iter-334: Task 16 sub-item (b) — `release-assets.githubusercontent.com` whitelisted in `packages/devbox/whitelist/github.txt`. Detail in commit `8eb6817`.

- [x] iter-333: Task 16(a) substrate-bake prek native Rust binary in `packages/devbox/Dockerfile` (Story 2.5 AMEND; closes iter-313 BLOCKED class). Detail in commit `353b9f0`.

- [x] iter-332: Task 15.1 (bash-fixture persistence under `packages/keel-invariants/fixtures/hooks/` — 74/0/74 GREEN). Detail in commit `cdf4d79`.

- [x] iter-331: Task 9 (CI visibility contract stub for Epic 14) — invariant-doc § CI visibility contract H3 section + manifest contentHash refresh `486e6ee1ec6e…` → `cb32195fc573…`. Detail in commit `420f8c1`.

_(iter-303..330 LANDING detail pruned per Guardrail 2; retained in commits through `718276d` + story-file Status HTML comment chain + story-file Change Log 0.1-0.24 rows.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **16/17 stories done** (2.1-2.16) + **Story 2.17 `in-dev (partial)` at iter-337** (Epic-2 final story; XL partial landing continuing through Task 16 story-spec `deferred-work.md` triage + remaining Tasks 15/17 — sub-items (a)-(g) FULLY RESOLVED at iter-337).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live smokes operator-workstation-deferred.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:121; bash + jq baked at :36-37 + :48). Host-side shim count: **18** at iter-337 (unchanged). iter-333 Dockerfile edit + iter-334 whitelist edit await operator-workstation rebake to materialize (image stays at pre-iter-333 baseline until rebake; whitelist edit takes effect on next devbox container restart since whitelist is read at startup, not baked into image).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **16 done** (2.1-2.16); **1 in-dev (partial)** (2.17 — Task 10.1 iter-320 complete; Task 13 iter-325 complete; Task 11 iter-327 complete; Task 14 iter-328 complete; Task 5 iter-329 complete; Task 6 iter-330 complete; Task 9 iter-331 complete; Task 15.1 iter-332 complete; Task 16(a) iter-333 complete; Task 16(b) iter-334 complete; Task 16(c) iter-335 DEFERRED; Task 16(f)+(g) iter-336 complete; Task 16(d)+(e) iter-337 marker-only defer-recorded; 3 top-level Tasks remain: 15 (15.2-15.5 fold into Task 17), 16 (story-spec `deferred-work.md` triage only — sub-items (a)-(g) FULLY RESOLVED), 17).
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.17 — Hook + settings bypass-resistance (git-layer + manifest + S4 + halt).
- **Story File:** `_bmad-output/implementation-artifacts/2-17-hook-settings-bypass-resistance-git-layer-manifest-s4-halt.md` (~577 lines post-iter-337 Change Log 0.26 append + Status iter-337 segment append + lifecycle HTML comment iter-337 entry).
- **Story State:** `in-dev (partial)` — iter-338 NOW = `/bmad-dev-story` CONTINUATION per § Story Lifecycle dev-story invocation guidance; NOW item Task 16 (story-spec) `deferred-work.md` triage (sub-tasks 16.1-16.4; medium-large own-iter item).
- **GitHub Issue:** no GitHub Project configured; `RALPH_ISSUE_NUMBER` unset. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` unchanged iter-272..337).

## Notes

- **iter-337 NOVEL — none (pure-bookkeeping defer-record iter; precedent iter-335 Task 16(c) DEFERRED + iter-330 Task 6 pure-verify pass).** Sub-novel observation (single data point — promotion threshold 2 unreached): defer-record bookkeeping iters cluster at the closing edge of multi-decomposed Tasks (Task 16 sub-items (a)-(g) decomposed across iter-333..337 — pattern is one-iter-per-decomposed-sub-item or one-iter-per-bundled-pair when sub-items share a docs-amend target). iter-336 bundled (f)+(g) by shared `claude-hook-denylist.md § Limitations` target; iter-337 bundled (d)+(e) by shared marker-only-no-mutation framing; iter-335 ran (c) solo because the defer-record had a unique substrate-restart-stale-state rationale. Carry-forward to next-Story's similar SC-N polish bundle: prefer pair-bundling marker-only items in the same iter to amortise per-iter overhead (orient + commit + push) when no substrate mutation is involved. Promotion to RALPH.md as a Ralph-iter-density best-practice on second re-validation.

- **iter-336 NOVEL — none (promotion threshold unreached).** Sub-novel: **Limitations-bullet bundling iter-320 case-glob + iter-329 self-exclusion-glob items at single-data-point promotion-threshold-unreached level** is a documented Ralph practice — bundling related observations that share an "awareness, not formal rule" framing in one Limitations bullet keeps the doc's bullet count growth bounded while still surfacing future-Ralph triggers (each item documented enough that a second re-validation can promote independently). Single data point at iter-336; promotion to RALPH.md as "iteration-class taxonomy: bundled-Limitations-append" on second re-validation.

- **iter-334 NOVEL — none (promotion threshold unreached).** Pure additive whitelist entry following the file's existing one-host-per-line convention. Sub-novel observation: **the GitHub release-asset CDN host migration `objects.githubusercontent.com` → `release-assets.githubusercontent.com` (2024)** is a single data point worth noting in case future-Ralph encounters another release-asset 302-redirect that fails the runtime egress policy — the pattern is "look at the redirect target host, not the github.com URL". Single data point; promotion to RALPH.md as a Gotcha entry on second re-validation. Also reaffirmed: whitelist files (`packages/devbox/whitelist/{github,npm,python,anthropic}.txt`) are intentionally NOT contentHash-pinned in `invariants.manifest.ts` — they are operator-extensible by design.

- **iter-333 NOVEL — none (promotion threshold unreached).** Mechanical substrate-bake Dockerfile AMEND following established arch-aware release-asset fail-fast pattern (iter-128 CR AI-6 baseline) — precedent in same Dockerfile for supabase-cli, git-delta, and aws-cli.

- **iter-332 NOVEL — single data point promoted to RALPH.md § Gotchas (2026-04-24 iter-332).** Hook-contract tests MUST align to the REACHABLE branch in the runtime, not to source-order case precedence. Four devbox-runtime reachability learnings: (a) D-22 resolved-path branch dominates direct-path case-globs; (b) `__unknown__` fires only on jq-parse-fail or missing `.tool_name`; (c) L1 `find -delete` regex requires specific filenames, not dir; (d) Grep/Glob `search_path` case-glob requires trailing content. Full detail in `packages/keel-invariants/fixtures/hooks/README.md § Dominance notes`. Single data point; promotion to rule-type on second re-validation.

- **iter-331 NOVEL — none (promotion threshold unreached).** Mechanical invariant-doc amendment + manifest contentHash refresh following established iter-325/329 docs-amend-plus-python3-heredoc discipline.

- **iter-329 NOVEL — `shutil.move` vs `os.replace` for /tmp-to-L1 new-file authoring (recorded in RALPH.md § Gotchas 2026-04-24 iter-329).** `os.replace` requires same-fs atomic rename; when `/tmp` is tmpfs and the repo is on a different mount, the call raises `[Errno 18] Invalid cross-device link`. `shutil.move` handles cross-fs automatically via copy2+unlink fallback.

- **iter-320 NOVEL carry-forward — shell case-globs match prefix-then-any, not word-boundary-anchored.** **NOW DOCUMENTED at iter-336** in `docs/invariants/claude-hook-denylist.md § Limitations` as awareness-level guidance (single data point unmet for formal rule promotion). New case-glob-based rule authoring inside `.claude/hooks/*.sh` should use word-boundary regex for any verb with common-prefix collision risk; the L1 install-boundary mutation-verb list already follows this. Documentation target hit at iter-336 Task 16 sub-item (f).

- **iter-320 NOVEL carry-forward — `chmod` command prefix FP-blocks seed-sync pattern.** Bash command `chmod 755 <src> && python3 -c "...'.claude/hooks/...'..."` tripped hook case-glob. **Rule:** future substrate-mutation ops needing both file-mode + atomic replace MUST be one python3 script (os.chmod + os.replace). Still unreached at iter-336.

- **iter-319 NOVEL carry-forward RE-CONFIRMED iter-320..336** — `/tmp` tmpfs `noexec,nosuid,nodev,relatime` (Story 2.5 hardening). iter-336 wrote py-script to `/tmp/story217-iter336-manifest-hash-refresh.py` and ran `python3 <abs>` — `/tmp` is `noexec` for direct exec, but `python3 <abs-path-to-script>` works because Python interprets the file rather than execve-ing it; no chmod needed.

- **iter-318 carry-forward — `gh pr view` GraphQL timeout recipe.** iter-336 reconfirmed at orient: `gh pr checks 230` timed out via `Post "https://api.github.com/graphql": dial tcp 140.82.121.5:443: i/o timeout`. Per iter-318 carry-forward, three retries is the bound; proceeded with IP-recorded baseline (`statusCheckRollup: []` unchanged through iter-336; no in-progress checks; safe to push).

- **iter-317/316 carry-forward — D-15 + D-14 minor regex + Python-write loophole.** **NOW RECORDED `complete: pre-landed iter-325` at iter-337 Task 16 sub-item (e).** The L1 install-boundary rule § Substrate-maintenance loophole narrative in `docs/invariants/claude-hook-denylist.md` (landed iter-325 Task 13.5 amendment) documents the python3-atomic-replace route (Write py-script to /tmp; run `python3 <abs>`) for L1-protected manifest edits with cross-reference to `os.replace` vs `shutil.move` cross-fs handling per iter-329 RALPH.md Gotcha entry. Carry-forward closed; routing pattern remains in active use (most recent: iter-336 manifest contentHash refresh).

- **iter-329 NOVEL carry-forward — `eslint.config.keel-invariants.js` self-exclusion-glob requirement.** **NOW DOCUMENTED at iter-336** in `docs/invariants/claude-hook-denylist.md § Limitations` as awareness-level guidance (single data point unmet for formal rule promotion). Rule-authoring modules detecting their own literal needle (`--no-verify`, `--dangerously-skip-permissions`) require file-scoped rule override (`files: ['**/eslint-rules/**', '**/keel-invariants/test/**', '**/prompt-injection-rules/**']` → rule `off`); `**/prompt-injection-rules/**` was added at iter-329 alongside the existing `**/eslint-rules/**` + `**/keel-invariants/test/**`. Documentation target hit at iter-336 Task 16 sub-item (g).

- **iter-319 + earlier carry-forward pool — low-activity holds.** `readlink -f` canonicalises even when target file doesn't exist; `install-boundary-protection` rule-id live-enforcing; **iter-314 NOVEL `.ts→.js` / `/src/→/dist/` runtime translation in `loadExpectedHooks` recorded `defer: threshold-not-met` at iter-337 Task 16 sub-item (d)** — single-site fix at `manifest-reader.ts:93-108`; promotion to invariant-doc § Limitations awareness-level guidance deferred until a second site exercises the same translation pattern; SC-17 candidates unchanged at iter-337.

- **iter-314 SM-review promotion threshold: 5→7 data points** unchanged; Story 2.17 post-dev SM estimate iter-338+ after remaining Tasks 16/17 land (2 QUEUE items: story-spec `deferred-work.md` triage + completion — sub-items (a)-(g) FULLY RESOLVED at iter-337).
