# Implementation Plan

## NOW

- [ ] `/bmad-create-story` for Story 2.4 (whitelist.sh sync CLI + per-fork override + diff summary + invocation of `reload-egress.sh`) per § Story Lifecycle Decision Matrix row `done → _(no story) → drafted` (within-epic auto-advance via § Cross-epic transition — Epic 2 has Story 2.4 next in sprint-status; no halt). Budget: ~35K tokens.

## QUEUE (Story 2.4 → Story 2.17 → Epic 2 close-out)

- [ ] _(conditional, post-Story-2.4 drafted)_ `/bmad-create-story (args: "review")` — pre-dev SM validation per FR14n row `drafted → validated`.
- [ ] _(conditional, per Stories 2.1/2.2/2.3 ATDD-skip precedent)_ ATDD-skip at `validated → atdd-scaffolded` if Story 2.4's ACs reduce to substrate-commitment class (CLI surface + shell-script plumbing + contract-pinning docs) with no test-runner yet configured. Story 2.4 is infrastructure-CLI class — may break the ATDD-skip pattern if it introduces a `scripts/whitelist-test.sh` harness. Decision deferred to SM validation.
- [ ] _(conditional, post-Story-2.17 done)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge. Epic 2 completion.
- [ ] _(conditional, post-Epic-2 merged)_ § Cross-epic transition → Epic 3 Story 3.1 OR halt EPIC_DONE awaiting merge.

## BLOCKED

_(none — Story 2.3 fix-drain complete + CR re-run closure gate passed iter-171 with 0 PATCH verdict.)_

## ATDD Red Phase

_(none — ATDD-skipped at iter-157; no red-phase failures to carry.)_

## DONE (current phase — Story 2.3 closure; prune deeper history as Story 2.4 begins)

- [x] iter-171: **`/bmad-code-review (args: "2")` re-run closure gate — Story 2.3 `fixes-pending → done` (ZERO-PATCH close).** Three-layer Ralph-hosted adversarial fan-out (Blind + Edge Case + Acceptance Auditor) against drain-only diff `bfcd500..HEAD -- packages/devbox/ docs/invariants/ packages/keel-invariants/` (8 files / 251+/70− / 436 diff lines — AR-1..AR-9 drain only). Raw: Blind 26 + Edge 17 + Auditor 1 = ~44 → triage: **0 PATCH + 0 DECISION_NEEDED + 8 DEFER + ~36 DISMISS**. Zero bug-class defects unambiguously fixable without human input — matches Story 2.2 single-cycle drain-close precedent. Acceptance Auditor surfaced exactly 1 SC-text deviation (SC-13 literal "DO NOT write 1.1.1.1" prose was not amended when iter-161 elected AR-1's bootstrap-detour; steady-state pin via `restore_resolv_pin()` holds on every exit) — deferred as D1 (future `/bmad-edit-prd` spec-amendment pass; material SC-text changes out-of-scope for code-review drain). Blind + Edge Case surfaced 7 defensive-hardening convergent findings: domain-regex validation D2 (pre-existing from iter-161 dismiss list → formalised as defer; carry to Story 2.4 user-facing CLI); SIGTERM-during-flock-trap-gap D3 (low-likelihood race, fd 200 auto-released on process exit); tailer post-launch liveness D4 (observability-hardening mirror of the iter-161 tailer-supervisor defer); getent NODATA diagnostic D5; mv cross-fs exit-7 mapping D6; drain-regression test coverage D7 (Epic 13 carry); manifest anchor enrichment D8. **All 9 iter-161 PATCH items transitioned `[x]` DRAINED inline in § Review Findings with fix-commit SHAs** (AR-1 `df31b88` + AR-2 `7985dc4` + AR-3 `e1cad0e` + AR-4 `9a5d73e` + AR-5 `262a572` + AR-6 `528537f` + AR-7 `d15e4da` + AR-8 `ab2a3cb` + AR-9 `e37cf3a`). **Story file updated:** § Review Findings (9 PATCH flip + 8 defer insert + iter-171 re-review source para); Status `in-progress → done`; Change Log v1.8 prepended (iter-171 closure gate rationale + cumulative Epic-2 CR metrics final snapshot). **sprint-status.yaml** flipped `2-3-egress-policy-…: in-progress → done` + `Story-2-3-done` timestamp comment appended. **deferred-work.md** appended new section `## Deferred from: code review of 2-3-egress-policy-… (2026-04-21 re-run)` with all 8 defer items + carry-to annotations matching iter-128/138/144/151/154/161 convention. **FR14n Story State `fixes-pending → done`**. **Cumulative Epic-2 CR metrics final:** Story 2.1 = 9 patches × 3 gates (16-iter three-cycle 9→5→0); Story 2.2 = 2 patches × 1 gate (4-iter single-cycle 2→0); Story 2.3 = 9 patches × 1 gate (10-iter single-cycle 9→0). Story 2.3's 9/0 ratio matches Story 2.2's 2/0 slope — drain discipline held across the 9 fixes with no compounding regressions. **Re-review findings-density observation** (promoted to RALPH.md): drain-scoped re-run diff ~0.10 findings/line vs opener substrate diff ~0.05 findings/line = +100% density on the new surface; signals adversarial pressure-test quality, not regression risk (classification distribution shifted DOWN — 9P+9D+~9X opener → 0P+8D+~36X re-run). Budget: ~55K tokens (orient + diff prep + 3 parallel subagents + substrate re-verification + triage + story file + Change Log + deferred-work + sprint-status + IP + RALPH.md + commit + push). PR #230 stays Draft across Epic 2 (transitions to Open at Story 2.17). **Next:** iter-172 `/bmad-create-story` for Story 2.4 — infrastructure-CLI class (whitelist.sh user-facing CLI; inherits Story 2.3's `reload-egress.sh` primitive as downstream caller; first validation of AR-1's "future callers inherit first-boot safety" rationale).
- [x] iter-170: **AR-9 fix-drain landed — replaced unquoted `$(printf … | sort)` command-substitution in `packages/devbox/scripts/start-egress.sh:76` with `mapfile -t fragments < <(LC_ALL=C find "${WHITELIST_FRAGMENTS_DIR}" -maxdepth 1 -type f -name '*.txt' -print | sort)` + `for fragment in "${fragments[@]}"`.** Pre-fix form word-split on IFS (`$' \t\n'`) — a fragment filename `team A.txt` expanded into two tokens. Post-fix: `mapfile -t` splits on newlines only; 7-probe isolated harness verified happy + whitespace-fragment + empty-dir + scoping + LC_ALL=C sort. Drain closed 9/9.

## Context

- **Phase:** 4-implementation — Epic 2 at 3/17 stories `done` (2.1 + 2.2 + 2.3) + 14/17 `backlog` (2.4..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.3 live smokes (Task 12.1 – 12.8) operator-workstation deferred; closed-out successfully without any live-runtime iterations.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Story 2.3 Task 1 appended `dnsmasq` + `nftables` to image; re-bake needed at operator-workstation close-out.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; 2.1 + 2.2 + 2.3 done; 2.4..2.17 in `backlog`.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2; PR #230 transitions to Open at Story 2.17 completion).
- **Story:** _(no story — auto-advance to Story 2.4 via `/bmad-create-story` at iter-172)_.
- **Story File:** `_bmad-output/implementation-artifacts/2-4-whitelist-source-of-truth-pnpm-devbox-whitelist-atomic-reload-cli.md` _(not yet drafted; will be created by iter-172 `/bmad-create-story`)_.
- **Story State:** `_(no story)_` — next iteration transitions `_(no story) → drafted` via `/bmad-create-story`.
- **GitHub Issue:** Story 2.4 → _(to be created)_; Epic 2 → #10.
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **Story 2.3 closure validates the single-cycle drain-close precedent for a 9-PATCH opener.** Story 2.1 (9-PATCH opener → 5 → 0, three-cycle) and Story 2.3 (9-PATCH opener → 0, single-cycle) are same opener-size but different close slopes; Story 2.3's cleaner close reflects (a) the more-targeted drain-only diff scope on re-run (436 vs ~650 LOC implementation) concentrating reviewer attention + (b) each AR fix landed with explicit isolated-harness verification during the drain (iter-162..iter-170) that caught regressions in-iteration. Lesson: a 9-PATCH opener with strict isolation-harness discipline closes in 1 cycle; Story 2.1's 3-cycle close reflects the earlier Epic-2 drain pattern without the harness discipline (promoted to RALPH.md).
- **Acceptance Auditor convergence from iter-161 to iter-171 = 1 drift finding.** At iter-161 the Auditor returned 0 spec-fidelity violations (SM ZERO-PATCH verdict upheld). At iter-171 the Auditor returned 1 SC-13 text-drift finding introduced by the AR-1 drain election — this is the **first drain-induced spec-text deviation** in Epic 2 (2.1 and 2.2 drains did not touch spec-pinned prose). Material spec-text amendments are out-of-scope for a code-review drain; deferred to `/bmad-edit-prd` pass. Lesson (promoted to RALPH.md): when a CR drain elects a fix that technically deviates from a pinned SC "DO NOT" list, the drain should queue a follow-up `/bmad-edit-prd` iteration OR record an explicit spec-amendment carry-over at drain landing rather than waiting for the re-run to surface it as a defer.
- **DinD is fork-time substrate, not transitional** per `INV-devbox-dind-available`; Story 2.3 closure validates the first infrastructure-security-class story shipped under the backend-B operator-workstation-defer acknowledgment.
- **NFR2 authority unchanged.** Cold ≤ 300s / warm ≤ 30s remains authoritative on M4-Pro native; DinD runs are indicative only.
- **Cumulative Epic-2 CR metrics final:** Story 2.1 = 9 patches × 3 gates (16-iter three-cycle); Story 2.2 = 2 patches × 1 gate (4-iter single-cycle); Story 2.3 = 9 patches × 1 gate (10-iter single-cycle). Story 2.3's 9/0 ratio matches Story 2.2's 2/0 slope — drain discipline held.
