# Implementation Plan

## NOW

- [ ] **PATCH-1 — Extend SC-4 opinionated-shared-mode posture to `REPO_NAME` in resolver + three-site doc lockstep.** Story 2.11 fixes-pending per iter-260 CR findings (convergent Blind #5 + Edge E6). Code: `packages/devbox/scripts/lib/main-repo-resolver.sh:164` — change `REPO_NAME="${KEEL_DEVBOX_REPO_NAME:-$shared_parent_name}"; export REPO_NAME` to `REPO_NAME="$shared_parent_name"; export REPO_NAME` (drop operator override in shared branch for AC 2 correctness under multi-fork-with-override scenario). Doc lockstep (three sites): (1) resolver docstring § Shared mode paragraph — add `KEEL_DEVBOX_REPO_NAME` ignored clause mirroring existing `KEEL_DEVBOX_CONTAINER_NAME` IGNORED language; (2) `AGENTS.md § Per-fork vs shared devbox mode § Operator override` — name BOTH env vars; (3) `docs/invariants/devbox-mode.md § Mode signal` (or adjacent § SC-4-equivalent section) — cite BOTH env vars as hardcoded-in-shared. Story file § Change Log v1.2 note updated to reflect corrected operator-override posture. Manifest contentHash re-computed + sync-gate verified post-rebuild per iter-257 LESSON. Sprint-status row unchanged at `review`. Next iter re-runs `/bmad-code-review (args: "2")` for `fixes-pending → done | fixes-pending` confirmation gate.

## QUEUE (Story 2.11 CR closure → Stories 2.12..2.17 → Epic 2 close-out)

- [ ] _(after PATCH-1 lands)_ Re-run `/bmad-code-review (args: "2")` — Story State `fixes-pending → done | fixes-pending`. Expected closure pattern per iter-253 CR re-run precedent: ZERO new PATCH + PATCH-1 closure confirmed → `done`. If NEW convergent findings surface, re-enter `fixes-pending` with new QUEUE entries.
- [ ] _(after Story 2.11 `done`)_ Iterate Stories 2.12..2.17 through full lifecycle — 6 remaining substrate stories spanning loopback-bound port publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-24 per iter-219..259 repeated pre-push confirmation — `gh pr view 230` returns `statusCheckRollup: []`; iter-260 pre-flight `gh pr view` hit HTTPS :443 timeout — transient, not a CI gate).

## BLOCKED

_(none — iter-260 landing anticipates SSH :22 / HTTPS :443 fluctuation per iter-249..259 precedent; recovery-banking pattern remains load-bearing across iter-249..260. PR #230 still has no CI configured (statusCheckRollup: []); HTTPS :443 timeout on `gh pr view` at iter-260 orient is diagnostic-only, not a push gate.)_

## ATDD Red Phase

_(none — Story 2.11 ATDD-skipped at iter-256 per grounds (c)+(ii)+(iii); iter-258 trace gate WAIVED; iter-259 post-dev SM ZERO-PATCH + 2 MINOR drift DEFERs; iter-260 CR closure 1-PATCH + 16 DEFER + 44 DISMISS.)_

## DONE (iter-260 Story 2.11 `/bmad-code-review (args: "2")` — Story State `sm-verified → fixes-pending`)

- [x] iter-260: **STORY 2.11 CR CLOSURE — Story State `sm-verified → fixes-pending`; 1-PATCH + 16-DEFER + 44-DISMISS triage; MATCHES iter-253 LESSON forecast band (0-1 first-class PATCH).** Three-layer Ralph-hosted adversarial fan-out dispatched in parallel per § Story Lifecycle Decision Matrix row `sm-verified → done | fixes-pending`: (1) **Blind Hunter** (`bmad-agent-architect`/Winston, diff-only, `bmad-review-adversarial-general` protocol) — 18 raw findings; (2) **Edge Case Hunter** (`bmad-tea`/Murat, diff + project read, `bmad-review-edge-case-hunter` protocol) — 36 raw JSON findings; (3) **Acceptance Auditor** (`bmad-agent-dev`/Amelia, diff + spec + context docs) — 7 new + 2 pre-existing (iter-259 D1/D2) confirmed deferred. Diff range `aa17c03..HEAD` (Story 2.11 branch lifetime) = 2024 lines / 31 files / 1536 insertions / 35 deletions. **PATCH-1 convergent (Winston #5 + Edge E6 across 2-of-3 layers)** — extends SC-4 opinionated-shared-mode posture to `REPO_NAME` in resolver line 164; ignores operator `KEEL_DEVBOX_REPO_NAME` override in shared branch for AC 2 correctness under multi-fork-with-override setup (Fork A `KEEL_DEVBOX_REPO_NAME=fork-a-custom` + Fork B `fork-b-custom` would produce silent ENOENT at `docker exec -w` against second fork). Fix: one-liner resolver change + three-site doc lockstep (resolver docstring + AGENTS.md + INV doc). **16 DEFERs** recorded to `_bmad-output/implementation-artifacts/deferred-work.md § Deferred from: code review of story-2.11 (2026-04-24)` as AR-75..AR-90 (Amelia F1-F5 SC-17 cross-artifact drift class + 11 Blind/Edge second-class operator-edge + UX-polish findings per iter-253 LESSON SECOND-class pattern). **44 DISMISSED** — highlights: Blind #1 / Edge E15 FALSE POSITIVE (`parsed[KEEL_DEVBOX_SHARED]` guarded by REQUIRED_VARS at L77 + missing-check at L179-191 exit-before-L208); Blind #14 theoretical-only (verified only start.sh reaches bind-interpolation path); F6 + F7 INFO-only. Review Findings section appended to Story 2.11 spec (`_bmad-output/implementation-artifacts/2-11-per-fork-vs-shared-devbox-mode-keel-devbox-shared.md`) under Tasks/Subtasks; Change Log v1.4 entry authored; Status HTML comment updated. Sprint-status row UNCHANGED (per iter-202 precedent: only CR-closure `review → done` flips sprint rows; interim states hold `review`). Budget consumed iter-260: ~75K tokens (orient ~17K + 3 parallel adversarial subagents ~45K [Winston 96K agent budget, Murat 72K agent budget, Amelia 154K agent budget — all within 200K per-agent context] + grounding reads + triage + three file writes ~13K). Well within ~117K execution budget.

_(iter-253..259 Story 2.10 closure + Story 2.11 create-story / pre-dev SM / ATDD-skip / dev-story / trace / post-dev SM iters pruned per Guardrail 2 — see Story 2.11 § Change Log v1.0/v1.1/v1.2/v1.3/v1.4 + RALPH.md Signposts iter-253..260 for per-gate detail.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **10/17 stories done** (2.1-2.10) + 1/17 in review (2.11 at state `fixes-pending` — sprint row unchanged at `review`) + 6/17 backlog (2.12..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered at iter-248 9/9 + iter-252 9/9 + iter-257 6/6 + iter-259 adversarial SM + iter-260 three-layer adversarial CR.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:119). Host-side shim count: **18** at iter-260 (unchanged — Story 2.11 extends 18 existing shims with resolver invocation).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **10 done** (2.1-2.10); 1 in-review at state `fixes-pending` (2.11); 6 backlog (2.12..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.11 — Per-fork vs shared devbox mode (`KEEL_DEVBOX_SHARED`)
- **Story File:** `_bmad-output/implementation-artifacts/2-11-per-fork-vs-shared-devbox-mode-keel-devbox-shared.md`
- **Story State:** `fixes-pending` — Story 2.11 CR closure landed at iter-260 (1-PATCH + 16-DEFER + 44-DISMISS). Next iter executes PATCH-1 (resolver one-liner + three-site doc lockstep) per Guardrail 5 one-task-per-iter. After PATCH-1 lands + commit + push, next+1 iter re-runs `/bmad-code-review (args: "2")` for `fixes-pending → done | fixes-pending` confirmation gate.
- **GitHub Issue:** Story 2.11 likely unwired (`RALPH_ISSUE_NUMBER` unset at iter-260 orient; skipping `Refs #<story-issue>` in commits per § Issue Tracking guard). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out, NOT iter-260).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — last confirmed `{"isDraft":true,"mergeStateStatus":"CLEAN","state":"OPEN","statusCheckRollup":[]}` at iter-259 orient; iter-260 pre-flight `gh pr view 230` hit HTTPS :443 timeout (`dial tcp 140.82.121.5:443: i/o timeout`). Transient network fluctuation per iter-249..259 pattern; no CI configured so not a push gate.

## Notes

- **iter-260 NOVEL LESSON — Three-layer adversarial fan-out convergence signal.** Convergence across TWO OF THREE layers (Winston #5 Blind + Edge E6 on same silent-override class around SC-4 extension) = high-confidence PATCH signal. Pattern: Winston (adversarial blind) identifies code-level hazards; Murat (edge-case-exhaustive) identifies path gaps; Amelia (acceptance-auditor) validates spec fidelity. Single-layer-only findings default to DEFER or DISMISS unless AC-critical. Rule for future CR iterations: look for 2-of-3 layer overlap as the primary PATCH-promotion signal, not Amelia's verdict alone (Amelia's lens is AC-satisfaction which rarely surfaces code-hazards that don't violate an AC explicitly). Promote to RALPH.md § Lessons at commit. Distinct from iter-259 two-subagent post-dev SM pattern (which was AC-satisfaction + drift-detector, both non-adversarial); the CR three-layer is strictly adversarial.
- **iter-260 PATCH-1 forecast carry-forward.** Per iter-253 LESSON "CR re-run closure — SECOND-class operator-edge DEFER pattern", after PATCH-1 lands the CR re-run is highly likely ZERO new PATCH + closure confirmed. Next+1 iter should execute PATCH-1 and prepare for one-iter closure. If CR re-run surfaces unexpected new PATCH, treat as Story 2.10 iter-251 precedent (5th-cumulative-ZERO-PATCH-CR falsified) and continue fixes-pending with next PATCH.
- **iter-260 LESSON — Blind Hunter false-positive class #1: unset-array-access under `set -u` without verifying REQUIRED_VARS fence.** Winston's #1 finding "parsed[KEEL_DEVBOX_SHARED] aborts under set -u" was falsified by grounding: KEEL_DEVBOX_SHARED ∈ REQUIRED_VARS at env-check.sh:77, and the missing-check at L179-191 exits with code 2 BEFORE reaching L208. Blind Hunter has no project-read access (by design) so couldn't verify the fence. Future-Ralph rule: Blind Hunter findings citing "X aborts under set -u" REQUIRE ground-verification against the script's REQUIRED_VARS fence + preceding exit gates before promotion. Dismiss if fence exists; promote only if X is reached under some input. Promote to RALPH.md § Lessons.
- **iter-259 LESSONS carry-forward unchanged at iter-260:** (1) post-dev SM two-subagent pattern (AC-verifier + drift-detector) vs CR three-layer (Blind + Edge + Auditor); (2) D2 architecture.md:547 7-site cascade pattern — pre-existing deferred; (3) partial-waive trace-gate vocabulary absence; (4) manifest rebuild required after new InvariantSchema entry.
- **iter-258 partial-waive trace-gate LESSON + iter-257 shared-mode operator-override INTENTIONAL ignore (SC-4)** remain relevant at iter-260 — SC-4 posture being extended to REPO_NAME in PATCH-1 is the load-bearing correctness carry-forward.
- **iter-244/246 audit-findings carry-forward** unchanged from iter-247..259: (1) restart.sh transitive-delegate prepend-only; (2) benchmark.sh OUT-OF-SCOPE clarification (SC-11); (3) Manifest entry count **31** post-Story-2.11 (corrected from off-by-one "30" in story file per Amelia F1 / AR-76 defer); (4) Alpine egress non-applicability doctrine. Manifest count will not move until next story adds a new invariant.
- **Substrate-citation drift cumulative forecast for Stories 2.11..2.17 — updated at iter-260:** iter-259 2 MINOR drifts + iter-260 added 5 new SC-17 close-out deferrals (F1 manifest-count, F2 Task 6 (f) contradiction, F3 start.sh mechanism, F4 `:39` citation, F5 three-site inconsistency) — new carry-forward total for Story 2.11 alone: **7 items** to sweep at Story 2.17 landing. Cumulative Epic 2 forecast updated: ~23-34 cumulative line-number drifts + 2-3 substantive citation-claim misplacements (D2 architecture.md:547 + F3 start.sh mechanism now confirmed + F1 manifest-count as second substantive cross-artifact drift). Prior estimate ~18-27 at iter-259 → now ~23-34 at iter-260. SC-17 scope holds: single reconciliation pass at Story 2.17 landing. Deferred-work.md now at 562 lines with AR-75..AR-90 added.
