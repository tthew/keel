# Implementation Plan

## NOW

- [ ] CR fix T/4 [MEDIUM]: trace.md:112 + :205 AC-3 template-prefix symmetry — (a) :112 § AC-3 Coverage header `NONE ❌ — structural only.` → `NONE ❌ (no automated test) — structural-only realisation via fs.readFile rejection → removed-from-source-only Drift branch; CR adversarial pass is the agreed backstop at 1.9.` (template-prefix + backstop-clause surfacing; AC-3 claim `structural only` preserved); (b) :205 § High Priority Gaps item-3 (AC-3 enumeration entry) align to the same post-Fix-M/N template-prefix + structural-claim-preserved form. Single-commit two-site edit. AC-3 IS spec-accurately structural-only per spec:152 (fs.readFile rejection); only the template-prefix is added for cross-AC header symmetry. Pure prose — zero substantive-claim change. ~small

## QUEUE (Story 1.9 — CR re-run #6 outcome processed: 2-of-3 layers NEW_FINDINGS [15 Blind Hunter + 15 Edge Case Hunter raw], Acceptance Auditor PASS; triage 4 PATCH + 6 DEFER + 13 DISMISS; halving hypothesis decisively falsified again; Story State stays `fixes-pending`)

- [ ] CR fix U/4 [MEDIUM]: trace.md:105 + :145 AC-2 + AC-5 backstop-vocabulary reframe — (a) :105 AC-2 Gaps `schema-level uniqueness refine + CR adversarial pass (Blind Hunter) is the agreed backstop per § Testing Standards` → `schema-level uniqueness refine (load-bearing complement per Fix O iter-33) + CR adversarial pass (Blind Hunter) per § Testing Standards`; (b) :145 AC-5 Recommendation `CR adversarial pass (Blind Hunter) backstops` vocabulary reframed to align with Fix O's coverage-matrix.json:41 reframing. Preserves CR-adversarial-pass layer (distinct from schema-refine evidence layer). Pre-edit grep: 2 sites. Post-edit grep forward-sweep for `agreed backstop` in AC-2/AC-5 scope must return 0. Pure prose. ~small
- [ ] CR re-run #7: `/bmad-code-review (args: "2")` adversarial triage against post-Fix-R/S/T/U diff. Expected outcome per FR14n matrix row 10 single-transition edge: zero findings across all 3 layers → `fixes-pending → sm-verified → done`. If new findings surface (eighth round), queue fix tasks and stay `fixes-pending`. Halving hypothesis DECISIVELY falsified across 6 rounds (iter-14 4 → iter-18 2 → iter-21 2 → iter-24 5 → iter-30 5 → iter-36 30 raw → 4 post-triage); residual-risk at CR re-run #7 is EIGHTH-ROUND residue — either (a) DEFER-SET re-surfaces as convergent findings (requiring promotion to fix), or (b) broader-corpus SIBLING-VOCABULARY-CLUSTER residue from Fix R/S/T/U's vocabulary introductions (`load-bearing complement` spreading, `(no automated test)` template-prefix expanding), or (c) convergent zero-out signalling story closure (low-probability given prior 6-round non-halving trend). ~large

## BLOCKED

_(none)_

## DONE (Story 1.9 — CR iter-7 through iter-38)

- [x] iter-38: Fix S/4 [MEDIUM] landed — `_bmad-output/test-artifacts/traceability/1-9-invariant-sync-gate-runtime-tooling-reader-walker-drift-detector.md` three-site surgical swap of bare `runner-hosted` → `test-runner-hosted`: (a) :175 AC-7 Gaps `No runner-hosted performance regression test` → `No test-runner-hosted performance regression test`; (b) :236 § HP Gaps AC-7 Missing Tests `runner-hosted performance-regression test` → `test-runner-hosted performance-regression test`; (c) :484 § Verification `a runner-hosted test` → `a test-runner-hosted test`. Closes the AC-7 unbolded vocabulary cluster that Fix Q iter-35 pre-admitted as out-of-scope (bolded-only) and that iter-36 Edge Case Hunter two-layer convergence surfaced for promotion. Pre-edit grep (`(?<!test-)runner-hosted` anchored to exclude already-canonical form): 3 hits at the three target sites. Post-edit grep: 0 hits of bare `runner-hosted` + 13 hits of canonical `test-runner-hosted` (10 pre-existing + 3 newly converted). Mechanical application of iter-36's SIBLING-VOCABULARY-CLUSTER carry-forward rule. Pure prose — zero substantive-claim change, zero code changes. 2 fixes remain (T → U → CR re-run #7 at iter-41 earliest).

- [x] iter-37: Fix R/4 [MEDIUM] landed — `_bmad-output/test-artifacts/traceability/1-9-coverage-matrix.json:39` AC-2 `substrate_verification` one-leaf surgical swap `Schema-level backstop: InvariantsSchema.superRefine id-uniqueness refine (Story 1.9 Task 3, story file line 75).` → `Schema-level load-bearing complement: InvariantsSchema.superRefine id-uniqueness refine (Story 1.9 Task 3, story file line 75).`. Syncs the body-leaf vocabulary at :39 with Fix O iter-33's `level_notes` reframing at sibling :41 (same JSON object, adjacent fields). Pre-edit grep across `_bmad-output/test-artifacts/traceability/`: 1 hit of pre-fix form at coverage-matrix.json:39, 0 hits of canonical form. Post-edit grep: 0 hits of pre-fix form, 1 hit of canonical form at :39. JSON re-parses clean. Pure prose — zero substantive-claim change. 3 fixes remain (S → T → U → CR re-run #7). Filename discovery: trace artefact under `_bmad-output/test-artifacts/traceability/` is `1-9-invariant-sync-gate-runtime-tooling-reader-walker-drift-detector.md` (NOT `1-9-trace.md`) — recorded in IP NOW note for Fix S.

- [x] iter-36: `/bmad-code-review (args: "2")` CR RE-RUN #6 — 3-layer fan-out against post-Fix-M/N/O/P/Q diff (`bcd18e2..HEAD`, 121 lines across 4 artefacts, 2 Story-1.9 substantive: trace.md + coverage-matrix.json). **Verdicts:** Blind Hunter **NEW_FINDINGS** (15 raw — 2 HIGH + 6 MEDIUM + 5 LOW + 2 borderline); Acceptance Auditor **PASS** (zero AC violations; 9 positive checks — byte-identical smoke-evidence match at trace.md:98+:138 vs story lines 180+183 + AC-2 scope carve-out preserved + Fix Q vocabulary swap surgical + coverage-matrix level_notes faithful reduction of substrate_verification bodies + Fix O schema-refine clause consistent with Task 3 + line-citation drift audit clean + sibling-vocabulary inflation check clean + AC-scope boundary clean + JSON validity preserved); Edge Case Hunter **NEW_FINDINGS** (15 items — heavy SIBLING-FIELD-PARALLEL + SYMMETRIC-AC-PARALLEL on AC-3 Coverage header at :112 + AC-3 § HP Gaps item-3 at :205 + in-trace.md § Gaps/§ Recommendation paragraphs for AC-2 at :105/:106 + AC-5 at :145 that Fix M/N/O/P did not reach + AC-1/AC-3/AC-5/AC-6 level_notes shape asymmetries at coverage-matrix.json :31/:51/:71/:81 + 3 unbolded runner-hosted sites at trace.md :175/:236/:484 + AC-2 substrate_verification body at coverage-matrix.json:39 `Schema-level backstop` out-of-sync with Fix O's `load-bearing complement` at :41). **Triage outcome: 4 PATCH + 6 DEFER + 13 DISMISS.** **PATCH (Fix R/S/T/U):** Fix R (coverage-matrix.json:39 `Schema-level backstop:` → `load-bearing complement:` sync with Fix O iter-33) + Fix S (trace.md :175/:236/:484 bare `runner-hosted` × 3 sweep → `test-runner-hosted`) + Fix T (trace.md:112 + :205 AC-3 Coverage header + HP Gaps item-3 template-prefix symmetry — add `(no automated test)` parenthetical + surface backstop clause; preserve `structural only` claim) + Fix U (trace.md:105 + :145 AC-2 + AC-5 backstop→load-bearing-complement vocabulary reframe). **DEFER (6):** BH2 (scope_notes vs level_notes posture mix at coverage-matrix.json:40-41 — end-to-end + DEFERRED describe orthogonal evidence paths), BH3 (coverage-matrix.json :41 vs :71 asymmetry — AC-2 legitimately has schema-refine complement, AC-5 doesn't), EH2 (coverage-matrix.json:51 AC-3 level_notes bare `structural` — spec-accurate per AC-3 evidence class), EH3 (coverage-matrix.json:31 AC-1 level_notes `runner-hosted` — coverage-matrix.json-side sweep expands Fix S scope), EH4 (coverage-matrix.json:81 AC-6 level_notes lacks `smoke (shell)` prefix — AC-6 is CLI-contract-only per spec:41 carve-out, per-AC evidence class differs), EH10 (trace.md:106 AC-2 Recommendation WAIVED posture — spec-accurate scope carve-out; retagging conflates trace-gate outcome with schema-refine classifier). **DISMISS (13):** BH1/BH5 (editorial — `(no automated test)` parenthetical does the work; cardinal "5 gaps" preamble explicitly reconciles), BH7 ("parallel of" template-not-byte-identical — Acceptance Auditor PASS Check 1 confirmed M/N byte-identical), BH8 (level_notes field-overloading — schema-design meta out-of-scope), BH9/BH10/BH11/BH13/BH14 (bookkeeping — plan.md + RALPH.md artefacts, non-normative), BH12 (SUBSTRATE_VERIFIED bolding — purposeful header-vs-prose convention), EH13/EH14/EH15 (§ Happy-Path-Only/§ Long-term Actions/§ Sign-Off reference AC-3 as structural-only — spec-accurate, not stale). **SIXTH-ROUND halving hypothesis DECISIVELY falsified AGAIN — FIFTH-ROUND NON-HALVING:** iter-14 4 → iter-18 2 → iter-21 2 → iter-24 5 → iter-30 5 → iter-36 30 raw (4 PATCH post-triage). Fix-count is now INCREASING per round; Fix M/N/O/P/Q's scope-narrowing choices introduced more surface than they closed. **NEW LESSON — seventh-round residue class: SIBLING-VOCABULARY-CLUSTER.** Fix M/N/O/P introduced NEW literals (`SUBSTRATE_VERIFIED`, `load-bearing complement`, `smoke (shell) — end-to-end evidence`, `test-runner-hosted`) that did NOT propagate uniformly across all structurally-adjacent sibling paragraphs in the broader corpus: (a) `Schema-level backstop:` in coverage-matrix.json:39 out of sync with `load-bearing complement` at :41 (same JSON object); (b) bare `runner-hosted` × 3 at trace.md:175/:236/:484 (AC-7 unbolded cluster); (c) AC-3 Coverage header + HP Gaps item-3 missing `(no automated test)` template-prefix; (d) trace.md:105/:145 AC-2/AC-5 `agreed backstop`/`backstops` stale vocabulary. Orthogonal to prior residue classes: (1) iter-14..21 vocabulary-variant within-same-AC-same-field, (2) iter-24 SYMMETRIC-AC-PARALLEL cross-AC same-field, (3) iter-30 SIBLING-FIELD-PARALLEL within-AC cross-field, (4) iter-36 SIBLING-VOCABULARY-CLUSTER broader-corpus-vocabulary-propagation — each CR round reveals a new propagation axis. **NEW CARRY-FORWARD RULE (iter-37..iter-40):** when refreshing an AC-scoped vocabulary literal, ALSO grep the broader corpus (trace.md + coverage-matrix.json + story file Dev Agent Record) for the pre-fix form, record all hits, commit the fix with pre-edit + post-edit grep attestation (`pre-edit: N hits in [file:line, ...]; post-edit: 0 hits of pre-fix form + N hits of canonical form`). Combined with iter-22 sibling-artefact rule + iter-24 SYMMETRIC-AC-PARALLEL rule + iter-30 SIBLING-FIELD-PARALLEL rule, this forms a four-layer carry-forward enumeration. **DEFER-HEAVY TRIAGE RATIONALE:** 6 of 30 raw findings address "level_notes field shape / AC-distinct evidence class" questions that would expand Story 1.9 scope to normalise all 7 ACs × 3-4 field types × 2 files (~20+ sites). Ralph PROMPT guardrail 12 (do-one-thing + bias-toward-exiting) + guardrail 2 (single-sources-of-truth + no-adapters) argue DEFER on broader surface; narrow PATCH on genuine in-file contradictions (Fix R/S/T/U, 8 sites total). If CR re-run #7 surfaces deferred items as two-layer convergent (not single-layer adversarial overreach), promote to fixes. **5 Story-1.9 § Review Findings sub-blocks in story file now: iter-18 (Fix D/E) + iter-21 (Fix F/G) + iter-24 (Fix H/I/J/K/L) + iter-30 (Fix M/N/O/P/Q) + iter-36 (Fix R/S/T/U queued, 6 defers + 13 dismisses recorded).** iter-30 block checkboxes flipped `[ ] → [x]` for M/N/O/P/Q to signal landed. Pure prose across all artefacts — zero code changes.

- [x] iter-35: Fix Q/5 [LOW] landed — `trace.md:190` vocabulary normalise: swap `**runner-hosted**` → `**test-runner-hosted**` at Fix L preamble (§ HP Gaps intro sentence). Single-line surgical change; zero substantive claim change. All 5 MEDIUM + LOW fixes (M/N/O/P/Q) from iter-30 CR re-run #5 now discharged. Story State stays `fixes-pending` — CR re-run #6 is the next transition trigger. Bolded `**runner-hosted**` no longer appears in trace.md; unbolded `runner-hosted` at :175 + :236 + :484 remains out-of-scope (AC-7 Gaps / § Missing Tests / § Verification — all three unbolded AC-7 sites form their own vocabulary cluster; if CR re-run #6 flags them, queue as new fix tasks).
- [x] iter-34: Fix P/5 [MEDIUM] landed — `_bmad-output/test-artifacts/traceability/1-9-coverage-matrix.json:71` AC-5 `level_notes` one-line surgical upgrade from `"structural"` → `"smoke (shell) — end-to-end evidence"`. Parallelises AC-4 `level_notes` at :61 (identical smoke-evidence vocabulary). Closes the last SIBLING-FIELD-PARALLEL drift site within coverage-matrix.json — body prose at :69 already cited `iter-8 Task-5 docs-side orphan-anchor smoke` end-to-end evidence since iter-8; Fix P propagates body→level_notes, zero regression risk. JSON re-parses clean. SIBLING-FIELD-PARALLEL carry-forward rule (iter-30) fully discharged across both AC-2 + AC-5 × both trace.md + coverage-matrix.json (4-way sweep complete: Fix M AC-2 trace.md :98 ✓ + Fix N AC-5 trace.md :138 ✓ + Fix O AC-2 coverage-matrix.json :41 ✓ + Fix P AC-5 coverage-matrix.json :71 ✓). 1 fix remains (Q → CR re-run #6).
- [x] iter-33: Fix O/5 [MEDIUM] landed — `_bmad-output/test-artifacts/traceability/1-9-coverage-matrix.json:41` AC-2 `level_notes` one-line surgical upgrade from `"schema + structural; adversarial CR pass is agreed backstop"` → `"smoke (shell) — end-to-end evidence; schema-level uniqueness refine is the load-bearing complement"`. Parallelises AC-4 `level_notes` at :61 (smoke-evidence vocabulary) while preserving schema-refine backstop clause (acknowledges InvariantsSchema.superRefine id-uniqueness as load-bearing complement per substrate_verification body at :39). Two-layer CR convergence (Blind Hunter + Edge Case Hunter both flagged :41) confirmed iter-24 scope-defer is now discharged. JSON re-parses clean. SIBLING-FIELD-PARALLEL carry-forward rule (iter-30) propagating AC-2 substrate_verification vocabulary → AC-2 level_notes. 2 fixes remain (P → Q → CR re-run #6).
- [x] iter-32: Fix N/5 [MEDIUM] landed — `trace.md:138` AC-5 Coverage header one-line surgical upgrade from `NONE ❌ — structural only.` → `NONE ❌ (no automated test) — **SUBSTRATE_VERIFIED end-to-end** via iter-8 Task-5 docs-side orphan-anchor smoke.`. Parallelises AC-4 header at :124 (and mirrors iter-31's Fix M landing at :98 for AC-2). SIBLING-FIELD-PARALLEL carry-forward rule (iter-30): AC-5 Coverage-header now reads in same smoke-evidence vocabulary as AC-5 substrate_verification body at :143 ("iter-8 Task-5 docs-side orphan-anchor smoke" already present there — Fix N propagates body→header, zero regression risk). 3 fixes remain (O → P → Q → CR re-run #6).
- [x] iter-31: Fix M/5 [MEDIUM] landed — `trace.md:98` AC-2 Coverage header one-line surgical upgrade from `NONE ❌ — schema-level + structural realisation only.` → `NONE ❌ (no automated test) — **SUBSTRATE_VERIFIED end-to-end** via iter-8 Task-5 manifest-side missing-anchor smoke.`. Parallelises AC-4 header at :124. SIBLING-FIELD-PARALLEL carry-forward (AC-2 Coverage-header → AC-2 substrate_verification at :103) applied. Zero regression risk: body prose already says "iter-8 Task-5 manifest-side missing-anchor smoke" at :103 — Fix M propagates that verbiage from body to header.


- [x] iter-7..iter-13: `/bmad-code-review (args: "2")` initial Ralph-hosted three-layer fan-out + 6 patches landed (CRITICAL `ANCHOR_REGEX` uppercase→lowercase + symmetric `added-to-source-only` emitter + 2 new Task-5 smokes + spec echoes; HIGH spec line 41 stdout→stderr; HIGH count 7→8 sourcePath files; HIGH count 2→5 runtime smokes; MEDIUM trace-bundle line citations re-anchored at 31 sites; MEDIUM Dev Agent Record AC-2 status prose upgraded schema→end-to-end).
- [x] iter-14: CR RE-RUN #1 — Blind Hunter NEW_FINDINGS (3 HIGH + 1 MEDIUM → Fix A/B/C queued); Edge Case Hunter MINOR_ONLY (defers); Acceptance Auditor PASS. **New pattern: trace-bundle semantic-drift from impl-upgrade.**
- [x] iter-15..iter-17: Fix A/B/C landed (HIGH regex echo swap; HIGH AC-5 framing at 3 sites; HIGH+MEDIUM cardinality 3→5 + AC enumeration {1,4,7}→{1,2,4,5,7} at 14+ sites).
- [x] iter-18: CR RE-RUN #2 — Blind Hunter PASS; Acceptance Auditor PASS; Edge Case Hunter NEW_FINDINGS (1 MEDIUM Fix D + 1 LOW Fix E).
- [x] iter-19..iter-20: Fix D/E landed (broad-scope literal swap at 6 steady-state-recommendation sites; single-line prose refinement at trace.md:319).
- [x] iter-21: CR RE-RUN #3 — Blind Hunter dismissed (ordinal drift consistent with prior convention); Acceptance Auditor PASS; Edge Case Hunter NEW_FINDINGS (2 MEDIUM Fix F + G).
- [x] iter-22..iter-23: Fix F/G landed (single-line YAML waiver.reason smoke-count 3→5; single-line AC-5 drop from remediation bullet).
- [x] iter-24: CR RE-RUN #4 — Blind Hunter PASS; Acceptance Auditor PASS + 1 MEDIUM out-of-scope (trace.md:103 AC-2 substrate_verification); Edge Case Hunter NEW_FINDINGS (3 MEDIUM + 1 LOW). **Fifth-round halving FALSIFIED (iter-24 5 items).** Root cause: SYMMETRIC-AC-PARALLEL drift, a new class.
- [x] iter-25..iter-29: Fix H/I/J/K/L landed (AC-2 substrate_verification bullet; § HP Gaps AC-2 + AC-5 entries; coverage-matrix.json AC-2 substrate_verification leaf; § HP Gaps preamble cardinal). SYMMETRIC-AC-PARALLEL carry-forward rule discharged.
- [x] iter-30: `/bmad-code-review (args: "2")` CR RE-RUN #5 — 3-layer fan-out against post-Fix-H/I/J/K/L diff (`6447720..HEAD`, 181 lines across 5 artefacts, 3 substantive Story-1.9 artefacts). **Verdicts:** Blind Hunter **NEW_FINDINGS** (3 MEDIUM + 1 LOW — 2 MEDIUMs dismissed as paper-trail audit-record residue [iter-24 Review-Findings description prose is frozen history — "per spec:163" / "per story file line 163" citations preserved verbatim, landings at trace.md:103 + coverage-matrix.json:39 use currently-correct "story file line 180"; Fix K sourcePath bare form matches Dev Agent Record canonical posture at story line 180, Auditor-confirmed]; promoted Fix O + Fix Q). Acceptance Auditor **PASS** (smoke-evidence phrasings exactly match story line 180 + 183 Dev Agent Record canonical forms; AC-2 scope carve-out preserved; Fix L preamble/body enumeration internally consistent). Edge Case Hunter **NEW_FINDINGS** (5 items — 4 MEDIUM SIBLING-FIELD-PARALLEL drift at trace.md:98 AC-2 Coverage header + trace.md:138 AC-5 Coverage header + coverage-matrix.json:41 AC-2 level_notes + coverage-matrix.json:71 AC-5 level_notes; 1 LOW dismissed as paper-trail residue). **Combined: 5 QUEUE fix tasks (Fix M/N/O/P MEDIUM + Fix Q LOW).** **NEW LESSON — sixth-round residue class: SIBLING-FIELD-PARALLEL drift.** Fix H/I/J/K/L refreshed the substantive `substrate_verification` prose for AC-2 + AC-5 but did NOT propagate to the structurally-adjacent sibling fields within the SAME AC's scope: (a) one-line `Coverage:` headers in trace.md (two sites at :98 AC-2 + :138 AC-5); (b) `level_notes` string leaves in coverage-matrix.json (two sites at :41 AC-2 + :71 AC-5). Orthogonal to iter-24's SYMMETRIC-AC-PARALLEL class (which governed AC-2↔AC-5 parallels across the SAME field type) — SIBLING-FIELD-PARALLEL is WITHIN-AC across DIFFERENT field types. **Halving hypothesis DECISIVELY falsified across five rounds:** iter-14 4 → iter-18 2 → iter-21 2 → iter-24 5 → iter-30 5. Fix-count is NOT halving; each CR re-run surfaces a NEW residue class born from the prior round's scope-narrowing choices. **NEW CARRY-FORWARD RULE (iter-31..iter-35):** when refreshing an AC's `substrate_verification` field, ALSO update every structurally-sibling categorical descriptor within the SAME AC's scope in the SAME commit: (a) the one-line `Coverage:` header that introduces the AC subsection in trace.md; (b) the sibling `level_notes` string leaf in coverage-matrix.json; (c) scope_notes / Recommendation paragraphs if they encode the same posture. Combined with iter-24's rule, this closes both the cross-AC and within-AC variants of the "narrow-scope fix leaves asymmetric residue" pattern.

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1–1.8 done; 1.9 fixes-pending — iter-36 CR re-run #6 triage surfaced 4 PATCH fix tasks [R/S/T/U]; iter-37 Fix R landed; iter-38 Fix S landed; 2 remain [T/U] + CR re-run #7; 7 more stories 1.10–1.16 remain in Epic 1 before EPIC_DONE)
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (continues — PR #226 stays Draft until Epic 1 EPIC_DONE per PROMPT_build.md step 5c)
- **Story:** `1-9-invariant-sync-gate-runtime-tooling-reader-walker-drift-detector`
- **Story File:** `_bmad-output/implementation-artifacts/1-9-invariant-sync-gate-runtime-tooling-reader-walker-drift-detector.md`
- **Story State:** `fixes-pending` (Ralph FR14n matrix row 10) — sprint-status + story-file Status remain `review`; flips to `done` only on CR re-run #7 zero-findings outcome per matrix row 10 single-transition edge. 2 PATCH fixes remain (T iter-39, U iter-40); iter-41 runs CR re-run #7.
- **PR:** #226 (Draft, MERGEABLE, no-CI-checks-reported — expected pre-Story-1.16; stays Draft through Epic 1; Draft→Open at EPIC_DONE)

## Notes

- **iter-36 CR re-run #6 surfaced SEVENTH-ROUND residue — SIBLING-VOCABULARY-CLUSTER drift.** Expected zero-findings outcome did NOT materialize; 2-of-3 layers returned NEW_FINDINGS (30 raw items: 15 Blind Hunter + 15 Edge Case Hunter), Acceptance Auditor PASS (9 positive checks — byte-identical smoke-evidence match, AC carve-out preservation, faithful reduction, JSON validity, scope boundary, sibling-vocabulary-inflation-internal-consistency-check). Four genuine propagation-axes in the SIBLING-VOCABULARY-CLUSTER class: (a) `Schema-level backstop:` at coverage-matrix.json:39 out-of-sync with Fix O's `load-bearing complement` reframing at sibling :41 (same JSON object, two adjacent fields); (b) 3 unbolded `runner-hosted` sites at trace.md:175/:236/:484 (AC-7 unbolded cluster, pre-admitted out-of-scope by Fix Q iter-35 but two-layer convergence [Blind Hunter + Edge Case Hunter] discharges the deferral); (c) AC-3 Coverage header at trace.md:112 + HP Gaps item-3 at :205 retain bare `NONE ❌ — structural only.` template missing `(no automated test)` parenthetical that 6 sibling AC headers now carry post-Fix-M/N; (d) trace.md:105 AC-2 Gaps + :145 AC-5 Recommendation retain pre-Fix-O `agreed backstop` / `backstops` vocabulary that Fix O's coverage-matrix.json reframing purged. This is NOT SIBLING-FIELD-PARALLEL drift (iter-30 class, within-AC cross-field) — it is SIBLING-VOCABULARY-CLUSTER, broader-corpus-vocabulary-propagation: the narrow fix introduced NEW vocabulary (`load-bearing complement`, `test-runner-hosted`, `SUBSTRATE_VERIFIED`, `(no automated test)` template-prefix) that did not grep-sweep through every structurally-adjacent paragraph in the broader trace.md + coverage-matrix.json corpus.
- **Pattern — SIX CR re-runs (iter-14/18/21/24/30/36) confirm NON-HALVING cumulative compound-drift as the story's defining residue family.** iter-14 4 → iter-18 2 → iter-21 2 → iter-24 5 → iter-30 5 → iter-36 30 raw (4 PATCH post-triage). Fix-count is NOT halving — after round 5 it INCREASED to round 6's 30 raw items. Four residue classes identified: (1) iter-14..21 vocabulary-variant within-same-AC-same-field; (2) iter-24 SYMMETRIC-AC-PARALLEL cross-AC same-field; (3) iter-30 SIBLING-FIELD-PARALLEL within-AC cross-field; (4) iter-36 SIBLING-VOCABULARY-CLUSTER broader-corpus-vocabulary-propagation. New carry-forward rule (iter-37..iter-40): when refreshing any AC-scoped vocabulary literal, ALSO grep the broader corpus (trace.md + coverage-matrix.json + story file Dev Agent Record) for the pre-fix form; commit the fix with pre-edit + post-edit grep attestation.
- **Fix R/S/T/U specifics:** Fix R at coverage-matrix.json:39 (AC-2 substrate_verification `backstop` → `load-bearing complement`). Fix S at trace.md:175+:236+:484 (AC-7 unbolded `runner-hosted` × 3 sweep). Fix T at trace.md:112+:205 (AC-3 Coverage header + HP Gaps item-3 template-prefix). Fix U at trace.md:105+:145 (AC-2 + AC-5 backstop vocabulary reframe). All MEDIUM severity. Order in QUEUE: R → S → T → U → CR re-run #7.
- **Dismissed this round (13 items):** (a) BH1/BH5 editorial — `(no automated test)` parenthetical does the load-bearing work at :98 + :138; preamble cardinal "5 gaps" explicitly reconciles with SUBSTRATE_VERIFIED framing. (b) BH7 "parallel of" template-vs-byte-identical — Acceptance Auditor PASS Check 1 confirmed M/N byte-identical to story-file Dev Agent Record canonical forms at story lines 180 + 183. (c) BH8 level_notes field-overloading — schema-design meta out-of-scope for Story 1.9 hygiene. (d) BH9/BH10/BH11/BH13/BH14 bookkeeping — plan.md + RALPH.md are non-normative process artefacts. (e) BH12 SUBSTRATE_VERIFIED bolding — purposeful header-vs-prose stylistic convention. (f) EH13/EH14/EH15 § Happy-Path-Only / § Long-term Actions / § Sign-Off AC-3 structural-only references — spec-accurate descriptions of AC-3's distinct evidence class.
- **Deferred this round (6 items — recorded in `_bmad-output/implementation-artifacts/deferred-work.md § Deferred from: code review RE-RUN #6 of story-1.9 (2026-04-20)`):** BH2 (coverage-matrix.json scope_notes vs level_notes posture), BH3 (AC-2 vs AC-5 level_notes asymmetry), EH2 (AC-3 level_notes bare `structural`), EH3 (AC-1 level_notes `runner-hosted` — coverage-matrix.json side), EH4 (AC-6 level_notes missing `smoke (shell)` prefix — spec-distinct CLI-contract-only class), EH10 (AC-2 Recommendation WAIVED posture). All defers cite spec-accuracy-per-evidence-class, orthogonal-evidence-paths, or scope-inflation rationale; revisit at Story 1.16 test-runner landing or Epic 13 workflow-wiring.
- **Post-iter-36 EPIC-done readiness UNCHANGED.** Even after all 4 Fix R/S/T/U patches + CR re-run #7 zero-out, Story 1.9 is only the first Epic-1 story in review. Sprint-status shows stories 1.10–1.16 still `backlog` (7 more stories × full FR14n lifecycle). EPIC_DONE halt many iterations away; PR #226 stays Draft.
- **Issue Tracking:** Story 1.9 issue number still unset at iter-36. Parent Epic 1 issue **#9** — `Refs #9` only.

- **iter-30 CR re-run #5 surfaced SIXTH-ROUND residue — SIBLING-FIELD-PARALLEL drift.** Expected zero-findings outcome did NOT materialize; Edge Case Hunter found 4 sibling structurally-adjacent sites (trace.md:98 + :138 Coverage headers + coverage-matrix.json:41 + :71 level_notes) all carrying pre-iter-8 categorisations that the Fix H/I/J/K/L `substrate_verification`-only scope never propagated to. Blind Hunter independently flagged coverage-matrix.json:41 (level_notes adjacent-field contradiction) confirming two-layer convergence on Fix O. This is NOT SYMMETRIC-AC-PARALLEL drift (iter-24 class, cross-AC) — it is SIBLING-FIELD-PARALLEL drift, within-AC across different structural field types. A NEW residue class.
- **Pattern — five CR re-runs (iter-14, 18, 21, 24, 30) confirm cumulative compound-drift as the story's defining residue family.** iter-14 4 (Fix A/B/C) → iter-18 2 (Fix D/E) → iter-21 2 (Fix F/G) → iter-24 5 (Fix H/I/J/K/L) → iter-30 5 (Fix M/N/O/P/Q). Three classes identified: (1) iter-14..21 vocabulary-variant drift within same-AC; (2) iter-24 SYMMETRIC-AC-PARALLEL drift cross-AC same-field; (3) iter-30 SIBLING-FIELD-PARALLEL drift within-AC cross-field. New carry-forward rule: refreshing any AC-scoped assertion mandates an in-commit sweep across the AC's full structural cluster (Coverage header + substrate_verification + level_notes + scope_notes + Recommendation) in both trace.md and coverage-matrix.json.
- **Fix M/N/O/P/Q specifics:** Fix M at trace.md:98 (AC-2 Coverage header). Fix N at trace.md:138 (AC-5 Coverage header). Fix O at coverage-matrix.json:41 (AC-2 level_notes). Fix P at coverage-matrix.json:71 (AC-5 level_notes). Fix Q at trace.md:190 (preamble vocabulary "runner-hosted" → "test-runner-hosted"). All MEDIUM except Fix Q (LOW). Order in QUEUE: M → N → O → P → Q → CR re-run #6.
- **Dismissed this round (3 items):** (a) Blind Hunter flagged Fix H "per spec:163" + Fix K "per story file line 163" in frozen Review-Findings description prose — paper-trail audit-record residue; landings at trace.md:103 + coverage-matrix.json:39 use currently-correct "story file line 180"; editing the descriptions would corrupt audit trail. (b) Blind Hunter flagged Fix K bare `sourcePath` landing vs description-prescribed `sourcePath packages/keel-invariants/commitlint.config.keel-invariants.js` — Auditor confirmed bare form aligns with Dev Agent Record canonical posture at story line 180; canonical posture wins. (c) Edge Case Hunter flagged `spec:163` dangling citation — merged with dismissed Fix H paper-trail item.
- **Post-iter-30 EPIC-done readiness UNCHANGED.** Even after all 5 fixes + CR re-run #6 zero-out, Story 1.9 is only the first Epic-1 story in review. Sprint-status shows stories 1.10–1.16 still `backlog` (7 more stories × full FR14n lifecycle). EPIC_DONE halt is many iterations away; PR #226 stays Draft.
- **Issue Tracking:** Story 1.9 issue number still unset at iter-30. Parent Epic 1 issue **#9** — `Refs #9` only.
