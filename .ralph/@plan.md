# Implementation Plan

## NOW

- [ ] **Story 2.17 `/bmad-code-review (args: "2")` — `sm-verified → done` per Story Lifecycle Decision Matrix.** ~medium-to-large own-iter item (final-story CR at Epic 2 close usually accretes 2-4 PATCH + ~15-25 DEFER triage; precedent Story 2.15 iter-301 + Story 2.16 iter-308). Forecast PATCH band: 2-4 absorbed inline; ~15-25 DEFER deferred to `_bmad-output/implementation-artifacts/deferred-work.md` per Story 2.16 / Story 2.15 trace pattern. Pre-selects "Create action items" per the args="2" contract — every CR action item becomes a QUEUE fix task unless IP records `defer: <reason>` per item. Lifecycle transition `sm-verified → done` (no findings) OR `sm-verified → fixes-pending` (action items queued).

## QUEUE (Story 2.17 lifecycle close + Epic 2 close-out)

- [ ] _(after Story 2.17 `done`)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge. Epic 2 completion fires `EPIC_DONE` halt; on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured; `statusCheckRollup: []` carried unchanged iter-219..341).

## BLOCKED

_(iter-341 SM-verified bookkeeping landed cleanly at commit `c3417ec` — but **push to remote FAILED at step-5** with `ssh: connect to host github.com port 22: Connection timed out` on both initial push + one retry, mirroring iter-340 SSH :22 egress incident; per Ralph rules step 5 "If push fails, document in IP and exit cleanly" — commit carries to next iter; iter-342 step-5 will retry the push first thing per `git log @{u}..` showing 1 unpushed commit at `c3417ec`. Pre-push gates GREEN at iter-341: typecheck 16/16 cached + lint 16/16 cached + format:check clean + commitlint passed + `pnpm keel-invariants:check-all` exit 0 on 41 entries unchanged. Story 2.17 Story State `traced → sm-verified` post-commit. iter-341 forecast confirmed: CLEAN 0-PATCH SM-verified. **SSH :22 egress flake recurrence — second consecutive iter** (iter-340 + iter-341); promotion-threshold-class observation for next-Ralph at iter-342 orient: if push retry succeeds first attempt, single-incident class; if fails again, persistent egress-side issue requiring operator-workstation diagnosis.)_

## ATDD Red Phase

_(empty — iter-311 SKIP-WITH-GROUNDS-(ii)+(iii). Impl-time substrate coverage at Story 2.17 close: 397 cumulative smokes GREEN across iter-315..320 runs + 7 genuine `node:test` unit tests GREEN at iter-329 for Task 5 S4 rules + 74 persisted replay fixtures GREEN at iter-332 / re-replayed at iter-339 / re-re-replayed at iter-340 (`packages/keel-invariants/fixtures/hooks/run-all.sh` — 55 positive + 19 negative). Total persisted/reproducible at iter-341: 7 unit tests + 74 replay fixtures.)_

## DONE (iter-341 SM-verified CLEAN landed — Story 2.17 `traced → sm-verified`)

- [x] iter-341: **Story 2.17 `/bmad-create-story (args: "review")` post-dev SM verification CLEAN — 0 PATCH absorbed, 0 fix tasks queued; FR14n Story State `traced → sm-verified`.** Per-AC evidence matrix at story file Dev Agent Record § Completion Notes List (iter-339 entry, lines 491-498) verified ACCURATE for all 8 ACs against the iter-340 trace re-verification: **1 FULL** (AC 3 — 7/7 `node:test` GREEN ~36 ms via `pnpm --filter @keel/keel-invariants test`) + **4 PARTIAL** (AC 1 manifest 41 entries; AC 2 sync-gate walker via `check-all` exit 0; AC 6 L1 install-boundary 74-fixture replay corpus + iter-315 end-to-end runtime block; AC 7 settings.local.json rejection via 3 hook-self-protection fixtures + dotfile lint + gitignore + hook case-glob) + **3 NONE** (AC 4 → Epic 3 Story 3.7 halt-write runtime; AC 5 → fork-AMEND PR operator-class via `docs/invariants/fork.md § Amendment-vs-fork decision tree`; AC 8 → Epic 14 dashboard pipeline). Downstream-class carving (AC 4/5/8) consistent with Story 2.17 scope (pin contracts + forward-link; runtime/dashboard implementation owned downstream). Story-file mutations (all via plain-`Edit`; story file outside L1 + hook-self-protection ranges per iter-316 routing discipline): (a) Status line preamble extended with iter-341 entry; (b) Status line lifecycle HTML comment terminal `→ sm-verified → done.` placeholder filled in with `→ sm-verified (iter-341 …)` segment; (c) Dev Agent Record § Completion Notes List extended with iter-341 block (SM-review verdict + Per-AC evidence verification + sprint-status invariance + pre-push gates); (d) Change Log row 0.29 appended. Sprint-status row UNCHANGED at `2-17-…: review` (sprint-status flips at `done` after CR; lifecycle-internal `sm-verified` is below sprint-status threshold per iter-339 precedent). Pre-push gates iter-341 all GREEN: `pnpm -w typecheck` 16/16 cached (140 ms) + `pnpm -w lint` 16/16 cached (108 ms) + `pnpm -w format:check` clean (story-file MD outside prettier scope this iter) + `pnpm keel-invariants:check-all` exit 0 on 41 entries unchanged. Manifest contentHash sub-tree unchanged (story file outside every hashed path; no manifest/hook/settings/seed perturbation). **No novel gotcha / lesson / decision** — pure SM-review bookkeeping iter following iter-310 (pre-dev SM CLEAN-after-3-PATCH) + iter-330 (Task 6 verify-pass) + iter-335 / iter-337 (defer-record bundles) + iter-340 (trace bookkeeping) narrow-iter discipline. Reinforcement-class observation (carry-forward, single data point): iter-341 SM-verified CLEAN at zero-PATCH despite the story being THE Epic-2 close-out story with 17/17 Tasks + 8 ACs spanning 3-layer defense (in-session hook + git-layer manifest sync-gate + S4 prompt-injection scan) — ground-(a) FIRST `node:test` runner + FIRST persisted replay corpus posture from iter-340 carried through to iter-341 post-dev SM verification, indicating that the Story 2.17 implementation chain (iter-309 draft → iter-340 trace) was sufficiently disciplined that the post-dev SM had no surface to find. Brief signpost entry appended to RALPH.md § Signposts for next-Ralph orientation.

- [x] iter-340: **Story 2.17 `/bmad-testarch-trace (args: "yolo")` GATE WAIVED — FR14n Story State `in-dev → traced`; TWENTY-SEVENTH cumulative trace-WAIVED; SEVENTEENTH Epic-2 trace-WAIVED; STRONGEST ground-(a) posture in chain.** Detail in commit `055f8e1`. Trace artifact trio at `_bmad-output/test-artifacts/traceability/2-17-{slug}.md|e2e-trace-summary.json|gate-decision.json`. Coverage stats: 1 FULL (AC 3) + 4 PARTIAL (AC 1/2/6/7) + 3 NONE (AC 4/5/8 downstream-class) = 13% < 80% deterministic FAIL → WAIVED via FR14n Epic-2-substrate precedent. Ground-(a) FURTHER-HARDENED to wired-`node:test`-runner-plus-persisted-replay-corpus (NOVEL — Story 2.16 iter-306's previous-strongest had impl-time-only smokes that were not persistent test files; Story 2.17 has 7 persistent unit tests + 74 persistent replay fixtures that survive iterations and reproduce on every run). Pre-push gates GREEN.

- [x] iter-339: **Story 2.17 `/bmad-dev-story` CONTINUATION CLOSE — Task 17 (Story file completion + sprint-status flip) landed. 17/17 top-level Tasks DONE.** Detail in commit `f448f78`.

- [x] iter-338: Task 16 (story-spec) `deferred-work.md` Epic-1 + early-Epic-2 DEFER triage; 693 → 763 lines (+70); 45 absorbed / ~173 carry-forward / 0 obsolete; 2 inline citation-lockstep absorbs (`packages/devbox/README.md:966` dev-sudo + `INVARIANTS.md:134` Four → Five workflow contracts). Detail in commit `b11db59`.

- [x] iter-337: Task 16 sub-items (d)+(e) marker-only defer-record bundle; (d) iter-314 NOVEL runtime translation `defer: threshold-not-met`; (e) iter-317 NOVEL loophole `complete: pre-landed iter-325`. Detail in commit `c2bf002`.

- [x] iter-336: Task 16 sub-items (f)+(g) bundle — one-bullet append to `docs/invariants/claude-hook-denylist.md § Limitations`. Manifest `INV-claude-hook-secret-denylist-doc` contentHash refresh. Detail in commit `50851e3`.

_(iter-303..335 LANDING detail pruned per Guardrail 2; retained in commits through `ba695d3` + story-file Status HTML comment chain + story-file Change Log 0.1-0.29 rows.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **16/17 stories done** (2.1-2.16) + **Story 2.17 `sm-verified` at iter-341** (Epic-2 final story; dev-phase FULLY COMPLETE post-iter-339 Task 17 close; trace-phase WAIVED at iter-340; post-dev SM-phase CLEAN at iter-341; next iter = `/bmad-code-review (args: "2")` → `sm-verified → done`).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live smokes operator-workstation-deferred.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:121; bash + jq baked at :36-37 + :48). Host-side shim count: **18** at iter-341 (unchanged). iter-333 Dockerfile edit + iter-334 whitelist edit await operator-workstation rebake to materialize (image stays at pre-iter-333 baseline; whitelist edit takes effect on next devbox container restart since whitelist is read at startup, not baked into image).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **16 done** (2.1-2.16); **1 sm-verified** (2.17 — dev-phase + trace-phase + post-dev SM-phase complete at iter-341; lifecycle transitioning `sm-verified → done` via Story Lifecycle Decision Matrix on `/bmad-code-review`).
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.17 — Hook + settings bypass-resistance (git-layer + manifest + S4 + halt).
- **Story File:** `_bmad-output/implementation-artifacts/2-17-hook-settings-bypass-resistance-git-layer-manifest-s4-halt.md` (~679 lines post-iter-341 Status iter-341 segment + lifecycle HTML comment iter-341 entry + Completion Notes iter-341 block + Change Log row 0.29).
- **Story State:** `sm-verified` — iter-342+ NOW = `/bmad-code-review (args: "2")` per § Story Lifecycle row `sm-verified → done`; forecast PATCH band 2-4 + ~15-25 DEFER triage at final-story Epic-2 close.
- **GitHub Issue:** no GitHub Project configured; `RALPH_ISSUE_NUMBER` unset. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` unchanged iter-272..341).

## Notes

- **iter-341 NOVEL — none (pure SM-review bookkeeping iter; promotion threshold unreached).** Reinforcement-class observation (carry-forward, single data point): post-dev SM CLEAN at zero-PATCH for the Epic-2 close-out story (17/17 Tasks + 8 ACs spanning 3-layer defense) corroborates that the Story 2.17 implementation chain (iter-309 draft → iter-310 pre-dev SM 3-PATCH → iter-311 ATDD-skip → iter-312..339 dev → iter-340 trace WAIVED) was disciplined enough that the post-dev SM has no surface to find — a counterpoint to the typical Epic-close-story SM accretion observed at smaller stories. Promotion to RALPH.md § Lessons as iter-density best-practice noted on second precedent re-validation. The Story 2.17 chain is the canonical reference for the bypass-resistance class story dev-flow at Epic 2 close.

- **iter-340 NOVEL — none (pure trace bookkeeping iter; promotion threshold unreached).** Reinforcement-class observation (carry-forward, single data point): the FIRST repo story with WIRED `node:test` runner (Story 2.17 AC 3 via `pnpm --filter @keel/keel-invariants test`) coexists with FIRST persisted replay corpus (`packages/keel-invariants/fixtures/hooks/run-all.sh` 74-fixture); both substrates SURVIVE iterations and reproduce on every run, surpassing Story 2.16 iter-306's previous-strongest ground-(a) (impl-time-only smokes that were not persistent test files). Promotion to RALPH.md § Signposts as iter-density best-practice noted on second precedent re-validation. The Story 2.17 trace markdown serves as the canonical reference for the bypass-resistance class story trace template at Epic 2 close.

- **iter-339 NOVEL — none (promotion threshold unreached).** Pure story-close bookkeeping iter (Task 17 completion DoD + Dev Agent Record populate + sprint-status flip + Change Log final row). No substrate / hook / settings / manifest / seed mutation. Precedent: iter-301 Story 2.15 Task-17-equivalent CR-close vs. iter-308 Story 2.16 Task-17-equivalent CR-close; Story 2.17's Task 17 is a dev-phase close (not CR-close) because ATDD was SKIP-WITH-GROUNDS at iter-311 so the lifecycle chain is `dev-story → trace → post-dev SM → CR → done` with Task 17 at the `dev-story → trace` seam rather than at CR landing.

- **iter-338 NOVEL — none (promotion threshold unreached).** Sub-novel observation (single data point — carry-forward): SC-N triage iters can co-land 2-3 citation-lockstep one-line absorbs alongside the audit write-up when target files are outside every hashed sub-tree (no manifest contentHash refresh route required; pure plain-`Edit` route on `INVARIANTS.md` + `packages/devbox/README.md` + `deferred-work.md`). Promotion to RALPH.md as iter-density best-practice on second re-validation. The 763-line `deferred-work.md` itself is now the principal SC-N close-out artefact for Story 2.17; future-Story analogous SC-N close-out tasks should target the same H2 append pattern.

- **iter-332 NOVEL — promoted to RALPH.md § Gotchas (2026-04-24).** Hook-contract tests MUST align to the REACHABLE branch in the runtime: (a) D-22 resolved-path branch dominates direct-path case-globs; (b) `__unknown__` fires only on jq-parse-fail or missing `.tool_name`; (c) L1 `find -delete` regex requires specific filenames; (d) Grep/Glob `search_path` case-glob requires trailing content. Full detail in `packages/keel-invariants/fixtures/hooks/README.md § Dominance notes`.

- **iter-329 NOVEL — `shutil.move` vs `os.replace` for /tmp-to-L1 new-file authoring (RALPH.md § Gotchas 2026-04-24 iter-329).** `os.replace` requires same-fs atomic rename; cross-fs raises `[Errno 18]`. `shutil.move` handles cross-fs via copy2+unlink fallback.

- **iter-320 NOVEL carry-forwards (now DOCUMENTED at iter-336)** — shell case-globs prefix-then-any matching + ESLint self-exclusion-glob requirement at `eslint.config.keel-invariants.js`; both in `docs/invariants/claude-hook-denylist.md § Limitations` as awareness-level guidance (Task 16 (f)+(g) bundle).

- **iter-319 NOVEL carry-forward RE-CONFIRMED iter-320..341** — `/tmp` tmpfs `noexec,nosuid,nodev,relatime` per Story 2.5 hardening; `python3 <abs>` works because Python interprets the file rather than execve-ing it.

- **iter-318 carry-forward — `gh pr view` GraphQL timeout recipe.** Three retries is the bound per iter-318; PR #230 baseline `statusCheckRollup: []` unchanged iter-272..341; iter-341 did NOT exercise `gh pr view` at orient (per IP `statusCheckRollup: []` discipline carried — no CI checks to wait on; step-5 push expected clean).

- **iter-317/316 carry-forward — D-15 + D-14 minor regex + Python-write loophole** — recorded `complete: pre-landed iter-325` at iter-337 Task 16 sub-item (e). Routing pattern remains in active use.

- **iter-314 carry-forward** — `.ts→.js` / `/src/→/dist/` runtime translation in `loadExpectedHooks` recorded `defer: threshold-not-met` at iter-337 Task 16 sub-item (d); single-site fix at `manifest-reader.ts:93-108`; promotion deferred until a second site exercises the same translation.

- **iter-314 SM-review promotion threshold: 5→7 data points** unchanged. Story 2.17 post-dev SM CLEAN at zero-PATCH at iter-341 confirms the band 0-3 forecast and serves as the seventh data point absorbed by the Story Lifecycle Decision Matrix `traced → sm-verified` row.
