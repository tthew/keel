# Implementation Plan

## NOW

- [ ] `/bmad-testarch-atdd` — FR14n row 3 `validated → atdd-scaffolded`. Sixth cumulative ATDD-skip EXPECTED (no test runner at Story 1.12 time; emitter substrate+tooling verified via § Task 6 determinism smokes; Story 1.13 owns the formal gate suite + test-runner integration). If Task 1's `tsx` devDep lands at Story 1.12 dev time, this opens a door for emitter-unit-tests via `tsx`, but a test framework (vitest/mocha) is still not installed — skip rationale holds. Apply the conjunction-of-three grounds from the Story 1.7/1.8/1.9/1.10/1.11 precedent: (a) substrate-verification-covers-ACs (Task 6 determinism smokes 1 + 2 exercise AC 2 byte-identical round-trip + source-change propagation end-to-end at shell level) + (b) no test runner wired at substrate level yet (Story 1.16 scope — no `vitest.config.*` / `jest.config.*` / `playwright.config.*`) + (c) downstream-story-covers-integration (Story 1.13 owns the pre-commit schema-validation + WCAG-AA contrast + source-output sync gates that formally exercise emitter correctness per the FR14n ground-(c) catalogue variant (ii)). Skill preflight also fails independently (TEA `test_framework: auto` autodetects nothing). State advances `validated → atdd-scaffolded` (NOT directly `→ in-dev`); record rationale in IP § ATDD Skip Rationale. Budget: ~15-20K (skip-iteration is pure IP + RALPH.md upkeep).

## QUEUE (Story 1.12 — token emitter pipeline)
- [ ] `/bmad-dev-story (args: "{story_file_path}")` — FR14n row 4 `atdd-scaffolded → in-dev`. **Budget-intensive iteration** (3 output stages + manifest + quality gates + 2 determinism smokes + sprint-status bump); multi-pass plausible with `in-dev (partial)` re-queue per iter-56 pattern. Candidate sequencing if partial: (a) pass 1 = emitter skeleton + Task 2 CSS stage + Task 5 manifest + Task 6 subset (determinism smoke 1); (b) pass 2 = Task 3 Tailwind preset + Task 4 TUI theme.py + Task 6 determinism smoke 2 + sprint-status bump. Prefer single-pass if execution budget permits (Story 1.11 iter-56 was single-pass with ~106 leaves + manifest + 2 smokes — Story 1.12 is more code but fewer value literals).
- [ ] `/bmad-testarch-trace (args: "yolo")` — FR14n row 5 `in-dev → traced`. Sixth WAIVED precedent CANDIDATE (matches Stories 1.7 + 1.8 + 1.9 + 1.10 + 1.11 cumulative pattern for substrate+tooling stories). Alternative: if emitter has edge-case surfaces (alias-cycle-detection, uncommitted-state SHA fallback, optional gamut-mapping if implemented), partial coverage may warrant `trace-fixes-pending` — assess at trace time. Structural trace-artefact inheritance from Story 1.11 trace artefact EXPECTED (~90% reasoning-cost savings per iter-46 + iter-57 lesson — mechanical adaptation of substrate_verification blocks).
- [ ] `/bmad-create-story (args: "review")` — FR14n row 7 post-dev SM `traced → sm-verified`. Target: ZERO-PATCH first-round pass per iter-58 precedent (preventative-audit + pre-dev SM compounding through the FR14n lifecycle).
- [ ] `/bmad-code-review (args: "2")` — FR14n row 9 `sm-verified → done` (or `fixes-pending`). Target: ZERO-PATCH + 5-10 DEFER class outcome per iter-59 precedent. Emitter stories may surface MORE Blind Hunter findings than populator stories due to emit-logic complexity (alias walker, SHA resolver, three-target translation rules, digit-segment handling, mode-overlay divergence across targets) vs Story 1.11's pure data-population simplicity. Realistic median: 1-2 PATCH rounds before zero-out. Expected CR defer absorption: (a) shadow-machinery may resurface per iter-59 CR defer #4 — carry-to a new Story 1.13+ or Story-1.17-TBD; (b) gamut-mapping may resurface per iter-59 CR defer #5 — either close via opt-in implementation at dev-story OR re-defer to Story 1.13.

## BLOCKED

_(none)_

## DONE (Story 1.12 SM-verified at iter-61; Story 1.11 complete at iter-59)

- [x] iter-61: **`/bmad-create-story (args: "review")` pre-dev SM review; FR14n state `drafted → validated` per matrix row 2.** 2 in-place PATCHes applied + 1 DEFER recorded (matches Story 1.11 iter-54/iter-58 ≤2-fix precedent). (SM-1 PATCH) Manifest entry count drift at 3 sites — claimed "14 (post-Story-1.11) to 15 entries"; actual post-1.11 count is 13 via `grep id: invariants.manifest.ts` enumeration; corrected all 3 sites to `13 → 14`. Layer-5 audit-rule carry-forward: future audits must count discrete entries, not just verify range bounds. (SM-2 PATCH) Python identifier edge-case for segment-local dashes — `color.state.in-progress` (tokens.json:88; Story-1.11 frozen) would emit as `state_in-progress` (Python SyntaxError) under the pre-patch Task 4 rule. Added sub-bullet specifying `.replace('-', '_')` post-join transform for Python target only; CSS + Tailwind preserve dashes. Audited tokens.json: sole dash-containing segment is `in-progress` (grep verified). (SM-3 DEFER) UX-spec source-tree diagram variance at `ux-design-specification.md:362` (places `tailwind.preset.ts` under `src/`) — resolved by existing architecture.md:916-917 variance-reconciliation pattern at § AC 1 scope carve-out; redundant enumeration avoided. Story 1.12 file v1.1 Change Log row added documenting all 3 finding classes with root-cause + carry-forward. Story State advances to `validated`; no un-addressed findings; next iter-62 runs `/bmad-testarch-atdd` (sixth cumulative ATDD-skip expected).
- [x] iter-60: **`/bmad-create-story` drafted `_bmad-output/implementation-artifacts/1-12-token-emitter-pipeline-web-css-tailwind-preset-tui-theme.md`; FR14n state `_(no story) → drafted` per matrix row 1.** 6 ACs (emitter command + 3 output paths + provenance header; determinism; CSS var enumeration + mode overlays; Tailwind preset flat-kebab; TUI theme.py snake_case + Python-stdlib-only; FR67-adapted purity contract). 6 Tasks (emitter skeleton + `tsx` devDep; CSS emitter; Tailwind preset emitter; TUI theme.py emitter; `INV-tokens-emitter` manifest registration; quality gates + 2 determinism smokes + sprint-status bump). Seven preventative-audit layers pre-applied at drafting (iter-53 three-point + iter-54 two-point + iter-56 sixth + iter-59 seventh-candidate). File-format ratification: `packages/ui/scripts/generate-tokens.ts` per Story 1.12 AC text + UX-DR3 (ratifies architecture.md:916-917 + :1217-1218 variance per W1 party-mode amendment precedent). Emitter tooling: `tsx` devDep (Option A default; Options B tsc-compile / C plain `.mjs` documented fallbacks). Alias-resolution strategy: flatten-at-emit (not `var()` chains). Output naming conventions: CSS kebab full-path (`--color-surface-raised`); Tailwind flat-kebab group-stripped (`'surface-raised'`); Python snake_case + digit-segment handling (`accent_500`, `type_xl2`). Mode overlays: CSS both `:root` + `[data-theme="dark"]`; Tailwind base-only; Python base + `theme.dark` nested. Sprint-status: `1-12-...: backlog → ready-for-dev`; `last_updated: 2026-04-20 Story-1-12-ready-for-dev UTC` (both header-comment `:2` + data-field `:38`). Change Log v1.0 row.

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1–1.11 done; 1.12 drafted; 1.13–1.16 backlog → 4 more stories × full FR14n lifecycle remaining before EPIC_DONE)
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (continues through Epic 1; PR #226 stays Draft until EPIC_DONE per PROMPT_build.md step 5c)
- **Story:** Story 1.12 — Token emitter pipeline → web CSS + Tailwind preset + TUI theme
- **Story File:** `_bmad-output/implementation-artifacts/1-12-token-emitter-pipeline-web-css-tailwind-preset-tui-theme.md`
- **Story State:** `validated` — FR14n matrix row 2 complete at iter-61 (pre-dev SM review passed with 2 in-place PATCHes + 1 DEFER); next iter-62 runs `/bmad-testarch-atdd` for `validated → atdd-scaffolded` (sixth cumulative ATDD-skip expected — no test runner).
- **GitHub Issue:** Story 1.12 GitHub issue to be surfaced via `gh issue list --search "Story 1.12 in:title"` at iter-62 or iter-63 (ralph.py IP-Context-Story parse lag pattern per Story 1.11 iter-53 + Story 1.10 iter-42); `RALPH_ISSUE_NUMBER` env var unset at iter-61. Parent Epic 1 tracked at **#9** (OPEN — closes at EPIC_DONE halt).
- **PR:** #226 (Draft, MERGEABLE, no-CI-checks-reported — stays Draft through Epic 1; Draft→Open at EPIC_DONE after stories 1.12–1.16 all complete FR14n lifecycle).

## Notes

- **Story 1.11 7-iteration floor held EXACTLY; Story 1.12 target same floor.** Compounding preventative-audit discipline through 7 layers at iter-60 drafting + ZERO-PATCH SM doctrine (iter-58) + multi-layer-corroborated-DEFER doctrine (iter-59) + single-layer-BH-DISMISS doctrine (iter-36 + iter-52) creates a trajectory-minimum hypothesis: iter-60 drafted → iter-61 validated → iter-62 atdd-scaffolded → iter-63 in-dev → iter-64 traced → iter-65 sm-verified → iter-66 done = **7 iterations (floor)**. Realistic median: 9 iterations (1 SM PATCH round + 1 CR PATCH round). Maximum plausible: 11 iterations (matches Story 1.10 ceiling for contract-plus-tooling stories with multi-pass dev + 2 SM rounds + 2 CR rounds).
- **Carry-forward rules INTO Story 1.12 dev + downstream (iter-61+) — COMPOUNDING PRIOR ART:**
  - **Seven preventative audit layers codified (iter-53/54/56/59 + new iter-60 additions).** iter-53 three-point (stable-IDs / Task-vs-schema-leaf diff / sprint-status wording) + iter-54 two additions (internal-consistency drift / cross-file line-number staleness) + iter-56 sixth (schema-permission diff on story-mandated ADDITIONS) + iter-59 seventh (gamut-mapping surface check for emitter stories) — ALL PRE-APPLIED AT iter-60 DRAFTING. Apply at Story 1.13+ drafting time.
  - **Story 1.11 ZERO-PATCH discipline doctrine** (iter-58 + iter-59): "structurally impossible + fully documented + does not violate AC text = ACCEPT"; applies equally at SM and CR; compounds through the FR14n lifecycle. Pre-staged defenses in § AC scope carve-outs for iter-65 + iter-66 at drafting time.
  - **Multi-layer-corroborated authoring-choice findings are DEFER, not PATCH (iter-59 lesson).** When ECH flags an authoring decision that the story-task text explicitly mandates, route to the downstream gate story via DEFER with concrete carry-to target — do NOT inline retune. Applies to Story 1.12 CR: shadow-machinery + gamut-mapping findings route to Story 1.13+ / follow-up-story; dark-mode contrast findings route to Story 1.13.
  - **Single-layer BH findings default DISMISS unless corroborated (iter-36 + iter-52).** Holds through iter-59: 17 single-layer BH findings DISMISSED; 0 BH PATCHes applied to Story 1.11. Apply to Story 1.12 CR triage.
  - **Structural trace inheritance ~90% reasoning-cost savings (iter-46 + iter-57).** Re-apply to Story 1.12 trace iter (iter-64) when that story reaches `in-dev → traced`.
  - **Scoped-build optimisation** (iter-51 + iter-56): after manifest mutation, run `pnpm --filter @keel/keel-invariants build` before `pnpm keel-invariants:check` (NOT `pnpm -w build`). Pre-pinned in Task 5.
  - **Drift-smoke revert patterns** (iter-45 new-untracked-file gotcha + iter-56 tracked-uncommitted-edit gotcha): pre-pinned in Task 6 OPTIONAL drift-smoke.
- **Story 1.12 DEFER absorption roadmap (from iter-59 deferred-work.md):**
  - Shadow-machinery (defer #4): Story 1.12 **EXPLICITLY OUT OF SCOPE** per § AC 6 scope carve-out — requires source mutation of `packages/ui/tokens.json` which is Story-1.11-frozen. Defers to a new follow-up story (Story 1.13-adjacent or Story-1.17-TBD) that first adds shadow tokens to source.
  - Gamut-mapping (defer #5): Story 1.12 **OPTIONAL OPT-OUT by default** per § AC 6 scope carve-out — dev may implement at `/bmad-dev-story` time if trivial; otherwise re-DEFER at iter-66 CR to Story 1.13 contrast gate.
  - Dark-mode `status.<X>.fg` / light-mode `status.info/warning` / `text.accent`/`border.accent` (defers #1/#2/#3): Story 1.13 scope — Story 1.12 emits source values as-is; WCAG-AA retune is Story 1.13's gate.
- **Issue Tracking carry-forward.** Parent Epic 1 at **#9** OPEN (never `Closes`; Ralph transitions at EPIC_DONE halt). Story 1.11 at **#35** OPEN (closes on PR merge at EPIC_DONE). Story 1.12 GitHub issue to be surfaced via `gh issue list --search "Story 1.12 in:title"` at iter-61 (RALPH_ISSUE_NUMBER env var parse-lag pattern). iter-60 commit trailer uses `Refs #9` (parent epic) since story issue number not yet resolved.
