# Implementation Plan

## NOW

- [ ] Run `/bmad-code-review (args: "2")` — three-layer adversarial fan-out (`sm-verified → done` or `fixes-pending`). Pre-selects "Create action items" per matrix. Forecast per iter-264 NOVEL LESSON severity-class-first rule + iter-266 NOVEL LESSON novel-runtime-behaviour band: 0-3 first-class PATCH + 2-5 second-class operator-edge DEFERs likely. Story 2.12 ZERO-PATCH CR precedent candidate — would extend iter-264's six-pass ZERO-PATCH CR class to seventh (and overlap with iter-270's SM ZERO-PATCH = first cumulative double-ZERO-PATCH story).

## QUEUE (Story 2.12 lifecycle → Stories 2.13..2.17 → Epic 2 close-out)

- [ ] _(Stories 2.13..2.17)_ Iterate through full lifecycle — 5 remaining substrate stories spanning healthcheck (2.13) + legacy branch retention (2.14) + committed settings (2.15) + hooks (2.16) + bypass-resistance (2.17). Each runs full lifecycle; no cross-epic transitions until Epic 2 final story (2.17) completes.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-24 per iter-219..269 confirmation — `gh pr checks 230` HTTPS :443 timed out at iter-270 orient with `i/o timeout` against api.github.com:443; assume unchanged per iter-263 SSH/HTTPS asymmetry — push via SSH unaffected).

## BLOCKED

_(none — iter-269 SSH :22 carry-forward cleared at iter-269 post-step-5 push commit `c94a695`; branch in sync with origin at iter-270 orient.)_

## ATDD Red Phase

_(none — iter-267 ATDD-skip-with-grounds-(c)+(ii)+(iii) per FR14n; 22nd-cumulative ATDD-skip precedent. Impl-time smokes at iter-268 dev-story cover ACs 1-3 per Story 2.11 iter-257 dev-agent pattern; ACs 4-5 operator-workstation-deferred.)_

## DONE (iter-270 Story 2.12 `/bmad-create-story (args: "review")` post-dev SM LANDING — `traced → sm-verified`)

- [x] iter-270: **STORY 2.12 POST-DEV SM LANDING — `traced → sm-verified` (Ralph internal FR14n state); sprint-row stays `review` across SM-gate window per Story 2.11 iter-258 precedent (flips to `done` only at CR closure).** Two-subagent pattern per iter-235 LESSON: AC-verifier `bmad-agent-dev` (Amelia) + drift-detector `bmad-agent-architect` (Winston). Both subagents converged on **clean advance**:
  - **Amelia AC verification:** All 5 ACs PASS. NONE unmet. ZERO fix-tasks queued. F3 (liveness check non-fatal) + F4 (ListenAddress unset) CRITICAL pre-dev SM PATCHes from iter-266 both confirmed landed verbatim in current substrate. Substrate smokes: `pnpm keel-invariants:check` GREEN (sha256 of `docs/invariants/devbox-ssh.md` = `e1d693cb…2a3c2` matches manifest contentHash exactly; manifest entry count **32**); `bash -n` PASS on 11 modified shells (incl. `entrypoint.sh` + `main-repo-resolver.sh`); 15-site shim tally exact (build=1 / rebuild=1 / start=1 / stop=1 / status=1 / logs=2 / clean=2 / benchmark=6); `INVARIANTS.md` H3 + `README.md` H2 + `AGENTS.md` H3 sibling-insertions all SC-17-correct; `.envrc.example:40` past-tense per SC-15 verified.
  - **Winston substrate-citation drift:** ZERO CRITICAL drift. PATCH-1/PATCH-2/PATCH-7 from iter-266 ALL HELD (PRD ↔ architecture file-path swaps held; resolver/entrypoint corrected line pins held but mechanically shifted by dev-pass). 8 MINOR drifts all classified as MECHANICAL line-shift artifacts of dev-pass's own substrate edits (Tasks 1/2/3/4 each touched a cited file): D-1 `entrypoint.sh:154-159 → :201-205`; D-2 `main-repo-resolver.sh :146/:152/:154 → :171/:177/:179` (in-code self-ref at `:232` also stale); D-3 Dockerfile apt `:43-91 → :43-92`; D-4 Dockerfile mkdir `:252-253 → :253-255`; D-6 compose env `:135-136 → :136-145`; D-7 compose ports `:150-157 → :162-166`; D-8 compose TODO `:250` removed; D-9 manifest narrative parenthetical (after `INV-devbox-prereq-check`, not strictly after `INV-devbox-mode`). Cross-invariant coherence INTACT across `INV-devbox-homedev-named-volume` + `INV-devbox-mode` + `INV-devbox-prereq-check` + fork/AMEND posture.
  - **Disposition:** ALL 8 MINOR drifts → DEFER to Story 2.17 close-out per SC-17 carry-forward (story line-pin polish; not substantive to AC contract or runtime behaviour). NO QUEUE fix-tasks added per § Story Lifecycle Decision Matrix (no unmet-AC findings; substrate-citation drifts at MINOR severity defer-eligible). NO IP per-item `defer: <reason>` block needed (matrix per-item defer applies to unmet-AC findings, of which there are zero).
  - **Outcome class:** SEVENTH cumulative ZERO-PATCH advance precedent — extends iter-264 ZERO-PATCH CR class (six precedents at that gate) with first-of-kind ZERO-PATCH SM precedent. Pattern signal: when pre-dev SM absorbs CRITICAL substantive findings (iter-266: 2 of 8 = F3 + F4), post-dev SM has no novel surface left — clean advance becomes the dominant outcome. Forecast for Stories 2.13..2.17: post-dev SM PATCH band 0-2 if pre-dev SM absorbed substantive findings; band 0-4 if pre-dev SM was narrow.
  - **NEW LESSON (drift-band re-baseline):** Winston's 8 MINOR line-shift drifts EXCEED the iter-266 forecast band of 2-4 numerically, but ALL 8 are MECHANICAL artifacts of dev-pass edits to cited files — i.e., mathematically expected, not substantive. Forecast band re-baseline: substrate-citation drift class is BIMODAL — substantive band 0-2 per story for post-dev SM (narrow because pre-dev SM should have caught most) + mechanical band 8-12 for any story whose dev-pass touches ≥4 cited substrate files. Promoted to RALPH.md § Lessons + § Signposts at iter-270.
  - **Story file Status update:** front-matter `Status: review` UNCHANGED (sprint-row stays `review`); HTML annotation block updated `traced → sm-verified` with iter-270 audit summary. Change Log next entry deferred to CR-closure landing (per Story 2.11 iter-260/264 precedent — SM-gate landings record state advance in IP + RALPH.md, story-file Change Log condensed at CR-closure landing).
  - **Two-subagent pattern reaffirmed:** iter-235 LESSON pattern is now THIRD-CUMULATIVE post-dev SM pattern application after iter-235 + iter-259 originals. Amelia (~123K subagent conversation tokens) + Winston (~93K subagent conversation tokens) parallel-fan-out keeps Ralph context lean — only condensed verdicts (~3K each) reach IP § DONE. Sub-conversation context isolation is the leverage.
  - **Next iter (iter-271):** `/bmad-code-review (args: "2")` per § Story Lifecycle row `sm-verified → done|fixes-pending`. Three-layer adversarial fan-out (Blind Hunter / Edge Case Hunter / Acceptance Auditor). Forecast 0-3 first-class PATCH + 2-5 second-class operator-edge DEFERs per iter-264 LESSON severity-class-first rule + iter-266 novel-runtime-behaviour band.
  - **Budget consumed iter-270:** ~50-65K tokens (orient ~8K + story-file read ~28K + 2 parallel subagents condensed verdicts ~6K + IP/RALPH.md/story-annotation/commit ~10K). Well inside ~117K execution budget; exit cleanly per Guardrail 12.
  - **PUSH STATUS:** iter-269 carry-forward cleared at iter-269 post-step-5 push (origin/feat/epic-2-packaged-devbox at `c94a695`). iter-270 push expected SSH :22 clean per iter-263 asymmetric-recovery + iter-268 POST-PUSH keepalive LESSON (apply `GIT_SSH_COMMAND="ssh -o ServerAliveInterval=15"` as third attempt if first two timeout). HTTPS :443 confirmed timed out at iter-270 orient — push via SSH unaffected.

_(iter-253..269 Story 2.10/2.11/2.12 closure iters + Story 2.12 drafting/pre-dev-SM/ATDD/dev-story/trace-gate iters pruned per Guardrail 2.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **11/17 stories done** (2.1-2.11) + 1/17 sm-verified-now (2.12) + 5/17 backlog (2.13..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered through iter-270 (iter-263 push recovery, iter-264 three-layer adversarial CR closure ZERO-PATCH, iter-265 Story 2.12 drafting, iter-266 pre-dev SM gate 8-PATCH landing, iter-267 ATDD-skip-with-grounds, iter-268 dev-story recovery-landing, iter-269 trace-gate WAIVED landing, iter-270 post-dev SM gate ZERO-PATCH landing).
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:120). Host-side shim count: **18** at iter-270 (unchanged at SM-gate iter — SM is docs-only; no shim or compose surface touched).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **11 done** (2.1-2.11); 1 sm-verified-awaiting-CR (2.12); 5 backlog (2.13..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.12 — Loopback-bound port publication + opt-in `KEEL_DEVBOX_SSH` sshd
- **Story File:** `_bmad-output/implementation-artifacts/2-12-loopback-bound-port-publication-opt-in-keel-devbox-ssh-sshd.md`
- **Story State:** `sm-verified` — Story 2.12 post-dev SM landing at iter-270 (ZERO-PATCH per 7th cumulative ZERO-PATCH advance precedent; both subagents converged on clean advance). Next iter transitions `sm-verified → done|fixes-pending` via `/bmad-code-review (args: "2")` per § Story Lifecycle Decision Matrix.
- **GitHub Issue:** Story 2.12 issue unknown; `RALPH_ISSUE_NUMBER` unset at iter-270 orient. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` per iter-219..267 confirmation; iter-268..270 `gh pr view`/`gh pr checks` HTTPS :443 timed out but iter-263+ asymmetric-recovery + iter-267 prior-state-authoritative posture covers this); push is unblocked at any Story State.

## Notes

- **iter-270 NOVEL LESSON (drift-band re-baseline):** post-dev SM substrate-citation drift class is BIMODAL — substantive drift (load-bearing semantic mismatch; CRITICAL → PATCH; band 0-2 per story since pre-dev SM should catch most) vs mechanical drift (line-pin shifts caused by dev-pass's own edits to cited files; MINOR → DEFER to close-out polish; band 8-12 for any story whose dev-pass touches ≥4 cited substrate files). Story 2.12 confirms: 0 substantive (within band) + 8 mechanical (within mechanical band). Apply to forecast for Stories 2.13..2.17 SM gates. Promoted to § Signposts at iter-270 + RALPH.md § Lessons.
- **iter-270 LESSON (two-subagent pattern reaffirmed):** AC-verifier (Amelia / `bmad-agent-dev`) + drift-detector (Winston / `bmad-agent-architect`) parallel-fan-out is PROVEN at THIRD cumulative application (iter-235 + iter-259 + iter-270). Sub-conversation context isolation keeps Ralph context lean — only condensed verdicts reach IP. Default post-dev SM pattern for Stories 2.13..2.17.
- **iter-269 trace-gate outcome carry-forward:** WAIVED per ground-(a)+(b)+(c) hybrid conjunction — 22nd-cumulative trace-WAIVED + 23rd-cumulative ATDD-skip-trace-WAIVED pairing.
- **iter-268 NOVEL LESSON carry-forward** — post-crash recovery pattern for dev-story landing iters (VERIFY+COMMIT, not re-invoke). 5-check verification.
- **iter-267 precedent-match carry-forward** — 22nd-cumulative ATDD-skip-with-grounds-(c)+(ii)+(iii); Story 2.5 iter-186 canonical pattern.
- **iter-266 NOVEL LESSONS carry-forward:** (1) container-loopback ≠ host-loopback under Docker userland-proxy — pattern TRAP, defence-in-depth `ListenAddress 127.0.0.1` is ACTIVELY BROKEN; (2) pre-dev SM PATCH-count CAN break iter-221's ~5 ceiling for novel-runtime-behaviour stories (band 6-10).
- **iter-264 NOVEL LESSON carry-forward** — CR re-run DEFER-vs-PATCH severity-class-first rule.
- **iter-263 LESSON carry-forward** — SSH :22 / HTTPS :443 asymmetric recovery; iter-270 confirmed pattern continues (HTTPS :443 timed out at orient + at `gh pr checks 230`; SSH :22 still primary push vector).
- **iter-260 LESSONS carry-forward (REFINED by iter-264):** three-layer adversarial fan-out convergence signal high-confidence triage.
- **iter-259 LESSONS carry-forward (REAFFIRMED by iter-270):** post-dev SM two-subagent pattern (PROVEN at iter-270 — third cumulative application).
- **iter-257 LESSON reaffirmed at iter-268 + iter-269 + iter-270** — `pnpm keel-invariants:check` runs `dist/check.js`; rebuild required after manifest edit. Iter-270 Amelia substrate smoke confirmed sync-gate GREEN post-rebuild.
- **iter-244/246 audit-findings carry-forward** unchanged: restart.sh transitive-delegate (re-confirmed at iter-270 Amelia AC 2 reasoning); benchmark.sh OUT-OF-SCOPE (SC-11) but threads compose-override (re-confirmed 6-site count at iter-270 Amelia 15-site tally); Manifest entry count **32** at iter-270 (unchanged from iter-268 dev-story landing).
- **Substrate-citation drift cumulative forecast for Stories 2.13..2.17:** iter-270 NEW LESSON re-baselines: substantive band 0-2 + mechanical band 8-12 per dev-pass-touches-≥4-cited-files story. Cumulative Epic 2 forecast: ~5-12 cumulative substantive drifts + ~40-60 cumulative mechanical drifts. SC-17 scope holds: single reconciliation pass at Story 2.17 landing condenses both bands.
