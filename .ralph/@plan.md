# Implementation Plan

## NOW

- [ ] `/bmad-create-story (args: "review")` per § Story Lifecycle row `drafted → /bmad-create-story (args: "review")` for pre-dev SM validation of Story 2.14. Two-subagent or three-subagent pattern (depends on novel-runtime surface estimate — Story 2.14 is policy/git-branch-creation class; two-subagent likely sufficient per iter-285 LESSON). Forecast band per iter-286 NOVEL LESSON narrow-diff documentation-heavy: 3-6 PATCH at pre-dev SM (wider than runtime-code-heavy at same novelty per iter-281 NOVEL LESSON; pre-dev SM is the absorbing gate for documentation-heavy stories per iter-270 NOVEL LESSON). On `validated` transition: `Story State = drafted → validated`; sprint-status row stays `ready-for-dev` (lifecycle-internal state).

## QUEUE (Story 2.14 lifecycle + 2.15..2.17 substrate queue)
- [ ] _(after Story 2.14 validated)_ Story 2.14 ATDD step — `/bmad-testarch-atdd` likely SKIP-WITH-GROUNDS-(c)+(ii)+(iii) per Story 2.12/2.13 cumulative 23rd+ precedent (no test runner wired; git-branch state inspectable via `git ls-remote` static smokes; substrate verification covers AC 1-4). Twenty-fourth+ ATDD-skip-trace-WAIVED pairing forecast.
- [ ] _(after Story 2.14 atdd-scaffolded)_ Story 2.14 dev-story — `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-14-legacy-devbox-branch-retention-policy.md")`. Tasks 1-4: Task 1 git-branch creation + push (one-time `git fetch upstream cc-devbox + worktree-bootstrap + README banner + push`); Task 2 invariant doc author; Task 3 manifest entry + INVARIANTS.md anchor (33 → 34); Task 4 README + AGENTS pointers + optional RALPH.md pre-record.
- [ ] _(after Story 2.14 in-dev)_ Story 2.14 trace — `/bmad-testarch-trace (args: "yolo")` likely WAIVED via ground-(a)+(b)+(c) hybrid (twenty-fourth+ cumulative WAIVED precedent; structural inheritance from Story 2.13 iter-284 template).
- [ ] _(after Story 2.14 traced)_ Story 2.14 post-dev SM — `/bmad-create-story (args: "review")` post-dev. Forecast 0-2 PATCH per iter-270 LESSON.
- [ ] _(after Story 2.14 sm-verified)_ Story 2.14 CR — `/bmad-code-review (args: "2")`. Forecast 0-2 first-class PATCH per iter-279 + iter-286 ZERO-PATCH precedent for narrow-diff stories.
- [ ] _(after Story 2.14 done)_ Story 2.15 committed Claude settings.json deny/allow — full lifecycle.
- [ ] _(after Story 2.15 done)_ Story 2.16 Claude PreToolUse hooks for secret-file denylist — full lifecycle.
- [ ] _(after Story 2.16 done)_ Story 2.17 hook-settings bypass-resistance + Epic 2 SC-17 close-out polish pass. SC-17 reconciliation absorbs cumulative **30 DEFERs at Story 2.13 landing** (24 carried from Story 2.12 + 6 new iter-286 CR defers: D-8 UID-empirical, D-9 probe robustness, D-10 exit-2 edge, D-11 Dockerfile lockstep lint scope, D-12 README sudo disclaimer, D-13 start_period cold-boot + AGENTS.md cross-ref polish); PLUS cumulative Epic 2 Story 2.14..2.16 DEFERs accrued during those stories.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-24 per iter-219..287 confirmation; `statusCheckRollup: []` continues; iter-287 orient REST fallback first-try GREEN per iter-263 asymmetric-recovery LESSON after `gh pr view 230` GraphQL :443 timeout; SSH :22 expected clean at step-5 push).

## BLOCKED

_(none — iter-287 `/bmad-create-story` landed Story 2.14 ready-for-dev cleanly; sprint-status row `2-14-…: backlog → ready-for-dev`; story file v1.0 authored at ~270 lines mirroring Story 2.13's structure scaled for policy/branch-creation class; no manifest delta this iter — manifest +1 lands in dev-story per Task 3.)_

## ATDD Red Phase

_(none — Story 2.13 ATDD-skipped at iter-282 with grounds-(c)+(ii)+(iii); no red-phase tests owed. Story 2.14 ATDD-skip-likely per § QUEUE forecast.)_

## DONE (iter-287 Story 2.14 `/bmad-create-story` — `_(no story) → drafted`; first POLICY+BRANCH-CREATION story in Epic 2; story file v1.0 authored from Story 2.13 template)

- [x] iter-287: **STORY 2.14 DRAFTING LANDING.** FR14n Story State `_(no story) → drafted` under § Story Lifecycle row "_(no story) → /bmad-create-story". Sprint-status row `2-14-legacy-devbox-branch-retention-policy: backlog → ready-for-dev` flipped this iter (preserving append-only audit-trail comment ledger per iter-269/iter-279 convention; body `last_updated` advanced to iter-287). Story file `_bmad-output/implementation-artifacts/2-14-legacy-devbox-branch-retention-policy.md` authored at ~270 lines.
  - **Story shape — first POLICY+BRANCH-CREATION story in Epic 2.** Stories 2.1-2.13 all touched runtime substrate code under `packages/devbox/`; Story 2.14 introduces NO runtime substrate code on `main` — substrate impact is one new invariant doc + one new git branch (`origin/legacy-devbox`) + AGENTS.md/README.md pointers + INVARIANTS.md anchor + manifest entry. Closest precedent: Story 1.16 (fork-extension config-pattern, also documentation-heavy with one template scaffold). The "running code" is the GIT BRANCH itself, hosted at `origin/legacy-devbox` — Ralph creates one-time in dev-story Task 1; Story 15b.1 retires at 1.0 cut ritual (`scripts/major-cut.sh` per epics.md:6293-6314).
  - **Branch-creation method baked into Task 1 spec — `git fetch <upstream>` not orphan-snapshot.** Two viable methods existed: (a) `git fetch https://github.com/tthew/cc-devbox.git main:legacy-devbox` brings ~3500 upstream commits into local pack-files (preserves history; supports `git bisect` on the canary per AC 4 triage path); (b) orphan branch with single-snapshot commit. Decision: method (a). Pack-file bloat is ~30MB one-time cost for the lifetime of the branch (ends at Story 15b.1 retirement); upstream commit history is load-bearing for AC 4's `git bisect` posture on the canary AND for any future archaeology against `legacy-devbox-final` tag post-retirement. Egress whitelist already covers `github.com` + `codeload.github.com` per Story 2.3 default-whitelist substrate; no whitelist amendment required.
  - **AC mapping captured (4 ACs verbatim from epics.md:1602-1627):**
    - AC 1 → Task 1 (branch creation + README banner) + Task 2 (invariant doc § Branch creation contract).
    - AC 2 → Task 2 (§ Cherry-pick workflow with `git format-patch | sed | git am` recipe + minimal-drift scope discipline).
    - AC 3 → Task 2 (§ Retirement gate; documents the procedure executed by Story 15b.1 — tag `legacy-devbox-final` + `git push origin --delete legacy-devbox` + RALPH.md decision entry per FR33 + AGENTS.md flip).
    - AC 4 → Task 2 (§ Triage path with canary-then-bisect recipe anchored at commit `5278738` Story 2.1 absorption) + Task 4 (README + AGENTS pointers).
  - **Substrate impact summary (lands in dev-story, NOT this iter):** create `docs/invariants/devbox-legacy-branch-retention.md`; edit `packages/keel-invariants/src/invariants.manifest.ts` (manifest 33 → 34); edit `INVARIANTS.md` (new H3 + anchor between Story 2.13 and Story 2.2 H3s); edit `packages/devbox/README.md` (new H2 after § Healthcheck); edit `AGENTS.md § Devbox iteration environment` (new H3 after § Healthcheck); on `legacy-devbox` branch — PREPEND retention banner to upstream README.md.
  - **Forecast band (iter-286 NOVEL LESSON #1 applied to narrow-diff documentation-heavy):** cumulative ~6-10 PATCH lifecycle (3-6 at pre-dev SM per iter-281 NOVEL LESSON wider-band-for-doc-heavy; 0-2 at post-dev SM per iter-270 NOVEL LESSON pre-dev-SM-absorbs-novel-surface; 0-2 at CR per iter-279 + iter-286 ZERO-PATCH precedent for narrow-diff stories). Twenty-fourth+ ATDD-skip + trace-WAIVED pairing forecast.
  - **Pre-dev SM trigger queued for iter-288:** `/bmad-create-story (args: "review")` per § Story Lifecycle row `drafted → /bmad-create-story (args: "review")`. Two-subagent pattern (AC-satisfaction audit + spec-fidelity audit) per iter-285 LESSON sufficient for narrow-surface; escalate to three-subagent if novel-runtime estimate surfaces.
  - **Budget consumed:** ~50K tokens (orient ~10K + epics.md/prd.md/architecture.md selective reads ~12K + Story 2.13 template study ~12K + INVARIANTS.md read ~6K + Story 2.14 file authoring ~10K). Well within ~117K execution budget; exit cleanly per Guardrail 12.
  - **PR:** #230 **Draft** — `state=open, draft=true, mergeable=clean` confirmed via REST fallback at iter-287 orient (GraphQL :443 i/o timeout cleared first-try via REST per iter-263 asymmetric-recovery LESSON); `statusCheckRollup: []` carries unchanged from iter-272..286; clean push expected at step 5.

- [x] iter-286: **STORY 2.13 CR CLOSURE LANDING.** FR14n Story State `sm-verified → done` under § Story Lifecycle row `sm-verified → /bmad-code-review (args: "2")` — three-layer Ralph-hosted adversarial fan-out per iter-271/iter-277 pattern. **0 first-class PATCH + 6 DEFER + ~14 DISMISS** — BELOW iter-264 LESSON forecast band 1-3 PATCH; matches iter-270 NOVEL LESSON (pre-dev SM absorbs novel surface → post-dev gates clean-advance dominant) + iter-279 ZERO-PATCH CR closure precedent extending from closure-re-run class to first-pass class. Story sprint-status flipped `review → done` at iter-286 per iter-279 Story 2.12 CR closure precedent. Epic 2 progress: 12/17 → 13/17 done; Story 2.14 becomes next backlog story (4 backlog remaining — 2.14/2.15/2.16/2.17 — no § Cross-epic transition trigger).
  - **Subagent dispatch:** Blind Hunter (`general-purpose` diff-only, 15 raw findings) + Edge Case Hunter (`general-purpose` diff+project-read, 12 raw findings) + Acceptance Auditor (`general-purpose` diff+spec+invariant, spec-fidelity PASS across AC 1/2/3/5 with AC 4 operator-workstation-deferred per Dev Notes:149 — unchanged from iter-283/iter-284/iter-285). Diff scope narrowed to substrate files only (`packages/devbox/docker-compose.yml`, `packages/devbox/Dockerfile`, `docs/invariants/devbox-healthcheck.md`, `packages/keel-invariants/src/invariants.manifest.ts`, `INVARIANTS.md`, `AGENTS.md`, `packages/devbox/README.md`, `_bmad-output/implementation-artifacts/sprint-status.yaml`; story file + trace artefacts excluded per CR-is-gate-on-code-not-on-spec discipline).
  - **Triage pivot — Blind+Edge convergent UID-claim finding DEFERRED not PATCHed:** Blind + Edge Case Hunters independently flagged that `docs/invariants/devbox-healthcheck.md:45` + `packages/devbox/docker-compose.yml:265-266` + `packages/keel-invariants/src/invariants.manifest.ts:299` all claim "probes run as USER `dev`" whereas compose `user: '0:0'` at `:220` sets `Config.User` which Docker HEALTHCHECK may inherit (running probes as root). Convergent 2-of-3 hunter-class finding. HOWEVER — story Dev Notes:145 explicitly takes the OPPOSING position ("compose `user: '0:0'` scopes only PID 1 (entrypoint); separate exec surfaces default to image USER") without empirical validation. Per iter-277 NOVEL LESSON #2 META guard (empirical-test override when Acceptance Auditor verdict CONTRADICTS Blind+Edge convergent), the disciplined move when Ralph cannot empirically disconfirm from DinD backend B under cap-dropped semantics is DEFER-to-operator-workstation-verify (D-8), NOT blind-PATCH. Capability-posture conclusion unchanged either way; deferred to Story 2.17 close-out with explicit `docker exec ps -eo pid,user,comm` smoke protocol.
  - **6 DEFERs all polish-class for Story 2.17 SC-17 close-out** (full detail pinned in story file v1.4 Change Log + `deferred-work.md § code review of story-2.13`):
    - **D-8:** Healthcheck runtime UID empirical verification (4-site spec-endorsed claim vs compose `user:'0:0'` override interaction — operator smoke required).
    - **D-9:** `nc -z` / `dig` probe robustness polish (`-w 2` + `2>/dev/null` belt-and-braces hardening).
    - **D-10:** Exit-code `2` POSIX-syntax-error edge case + invariant doc note.
    - **D-11:** Dockerfile line-number citations lockstep lint scope expansion (extends iter-281 D-5 from `api.github.com` domain lockstep to include Dockerfile symbol lockstep `:61`/`:64`/`:360`).
    - **D-12:** README § Opt-in sshd walkthrough disclaimer polish (dev lacks sudo per SC-13; rewrite to acknowledge + point at host-side `docker exec -u root`).
    - **D-13:** `start_period: 30s` cold-boot RSA-4096-keygen marginal edge + AGENTS.md § Healthcheck ↔ § Opt-in SSH "Mode-flip discipline" cross-ref polish.
  - **~14 DISMISS** (reasoning pinned in story file v1.4): dig REFUSED/NXDOMAIN as "dnsmasq responsive" is by-design (invariant doc acknowledges); SSH-branch inversion case-folded away by Story 2.12 resolver; probes during start_period FR37-expected; nc -z no-pubkey-auth intentional; probe-domain=whitelist-cyclical by design (escape path = whitelist restoration); YAML folded-scalar verified via iter-283 `dash -n`; restart:'no' dev-substrate philosophy; host-side env toggle teardown-requirement documented in AGENTS.md § Opt-in SSH; fork /var/log bind-mount hypothetical; manifest sourcePath existence via ENOENT; KEEL_DEVBOX_SSH_PORT vs container-port orthogonal; COMPOSE_FILE_SSH vs SSH_RESOLVED hypothetical refactor-hazard; dig upstream-flake documented tradeoff; sprint-status ledger append-only convention.
  - **Cumulative Epic-2 DEFER queue for Story 2.17 SC-17 close-out:** 24 (at iter-285 post-dev SM) + 6 (iter-286 CR) = **30 DEFERs** pending.
  - **AC coverage at iter-286 CR closure:** AC 1 (no curl :3000) PASS via static grep + Acceptance Auditor negative-assertion; AC 2 (dnsmasq probe) PASS via compose:281 probe-string match; AC 3 (conditional sshd probe) PASS via POSIX-sh branch analysis + iter-281 PATCH-4 sshd ListenAddress-unset; AC 4 UNCHANGED operator-workstation-deferred; AC 5 (timing documented) PASS via three-site lockstep verification.
  - **Cumulative Story 2.13 lifecycle PATCH count:** 6 (iter-281 pre-dev SM) + 2 (iter-285 post-dev SM) + 0 (iter-286 CR) = **8 PATCH total**. Novel lesson candidate (pinned in v1.4 Change Log): narrow-diff moderate-novelty story cumulative PATCH ~6-10; novel-runtime story (2.12-class) cumulative ~10-14. Apply to Stories 2.14..2.17 forecasting.
  - **Budget consumed:** ~65K tokens (orient ~12K + story file deep read ~10K + three parallel subagent dispatches ~35K + Ralph triage synthesis + verification greps + edits + commit-prep ~18K). Well within ~117K execution budget; exit cleanly per Guardrail 12.
  - **PR:** #230 **Draft** — `statusCheckRollup: []` carries unchanged at iter-286 orient (REST fallback first-try GREEN; GraphQL timeout at orient per iter-263 asymmetric-recovery LESSON cleared on REST probe). Clean push expected at step 5.

- [x] iter-285: **STORY 2.13 POST-DEV SM LANDING.** FR14n Story State `traced → sm-verified` under § Story Lifecycle row `traced → /bmad-create-story (args: "review")` post-dev. Two-subagent pattern (iter-235 LESSON) — AC-satisfaction audit + implementation-fidelity audit; Ralph-synthesis triage. **2 NARROW PATCH + 0 new DEFER** within iter-270 NOVEL LESSON forecast band 0-2 PATCH.
  - **PATCH-1 (NARROW; 9 sites):** `Dockerfile:347` → `Dockerfile:360` self-inflicted same-commit citation drift. iter-283 Task 5 D-1 absorption inserted 13-line block at `Dockerfile:318-330` (pre-create `/var/log/sshd.log`), shifting `USER dev` directive from what would have been `:347` to `:360`. Dev Notes + References + Completion Notes + invariant doc § Probe tooling + compose-file comment + manifest description all cited pre-absorption `:347`. Corrected across 9 sites in one commit: story file 6 occurrences + `docker-compose.yml:266` + `docs/invariants/devbox-healthcheck.md:45` + `invariants.manifest.ts:299` description. Invariant doc sha256 refreshed `665174a3…d18623` → `ae0ac4b3da589de4958eb118df334e57a55c44823456440960430d4bf1470c48`; manifest `contentHash:301` updated in lockstep. `pnpm --filter @keel/keel-invariants build && check && check-all` GREEN post-rebuild.
  - **PATCH-2 (NARROW; File List `:224` bookkeeping):** H2 count prose "7 H2 contract sections + § Intent" → "8 H2 contract sections + § Intent". Actual doc is § Intent + 8 contract sections = 9 H2. Completion Notes line 216 "8 total including § Intent plus 7 contract H2s" prose retained as historical record; structural fix at File List only (matches iter-281 posture — Dev Agent Record prose is historical, not normative).
  - **DEFERs unchanged:** cumulative Epic-2 DEFER queue for Story 2.17 SC-17 close-out = **24** (no new DEFER at iter-285; 2 surfaced items (automated lockstep lint D-5-equivalent + AC 4 live-probe operator-workstation-deferred) are pre-existing carry-forwards).
  - **DISMISSes:** ~3 findings DISMISSed (README:966 operator-sudo advisory correct-as-written; AGENTS.md vs README log-path split intentional audience design; Completion Notes off-by-one is historical-record-only). Reasoning pinned in v1.3 Change Log.
  - **Budget consumed:** ~45K tokens (orient ~10K + story file read ~8K + 2-subagent dispatch ~15K + triage synthesis + PATCH edits + rebuild + sync-gate + Change Log + IP + RALPH.md + commit-prep ~12K). Well within ~117K execution budget; exit cleanly per Guardrail 12.
  - **PR:** #230 **Draft** — `statusCheckRollup: []` carries unchanged at iter-285 orient (GraphQL GH outage at orient; REST fallback GREEN per iter-263 LESSON carry-forward + iter-281 network-outage precedent); clean push expected at step 5.

- [x] iter-284: **STORY 2.13 TRACE LANDING.** FR14n Story State `in-dev → traced` under § Story Lifecycle row `in-dev → /bmad-testarch-trace`. **Gate decision: WAIVED** (TWENTY-THIRD cumulative trace-WAIVED precedent extending Story 2.12 iter-269 twenty-second; TWENTY-FOURTH ATDD-skip-trace-WAIVED pairing overall). Ground-(a)+(b)+(c) hybrid conjunction applied: (a) substrate-verification covers ACs 1, 2, 3, 5 via iter-283 recovery-landing impl-time smokes (sha256 contentHash `665174a3…d18623` + sync-gate GREEN `pnpm --filter @keel/keel-invariants build && check && check-all` + dash -n POSIX parse + three-site `api.github.com` lockstep + probe-tooling-baked dnsutils/netcat-openbsd + sshd_config ListenAddress-unset + timing-values lockstep + AC-1 negative-assertion + Story-2.12 env-propagation canonical stream); (b) no test runner wired (Epic 13 scope; zero test configs under repo root); (c) HYBRID variant-(ii) operator-workstation-deferred-AC-completion for AC 4 (mid-run SIGKILL + retry accumulator + State.Health.Status transition infeasible under DinD backend B cap-dropped semantics per Dev Notes:149).
  - **Trace artefacts landed:** `_bmad-output/test-artifacts/traceability/2-13-healthcheck-on-dnsmasq-sshd-replaces-upstream-s-broken-curl-3000-healthcheck.md` (~500 lines; full five-phase matrix + gate decision + residual risks + waiver details + CI/CD yaml snippet) + `2-13-coverage-matrix.json` (Phase 1 temp; 5 requirements × P2 × NONE coverage) + `2-13-e2e-trace-summary.json` (portable summary + gate_status WAIVED) + `2-13-gate-decision.json` (slim gate surface). Structural inheritance from Story 2.12 iter-269 template (~85% reasoning-cost savings; mechanical adaptation to 2.13's 5-AC healthcheck-compose-probe class).
  - **Story file v1.3:** Status HTML comment updated `in-dev → traced` with full annotation covering hybrid grounds + trace artefact paths + next-iter forecast.
  - **No code changes.** Trace is gate-only; no manifest or substrate edit. `pnpm --filter @keel/keel-invariants build && pnpm keel-invariants:check` GREEN confirms no regression from inherited iter-283 landing (manifest count stays at 33 entries).
  - **Budget consumed:** ~35K tokens (orient ~10K + story file + IP + RALPH.md reads ~8K + Story 2.12 precedent read (markdown + 3 JSONs) ~8K + artefact authoring (markdown + 3 JSONs) ~15K + sync-gate verification + commit prep ~5K). Well within ~117K execution budget; exit cleanly per Guardrail 12.
  - **PR:** #230 **Draft** — `statusCheckRollup: []` carries unchanged at iter-284 orient; clean push expected at step 5.



- [x] iter-283: **STORY 2.13 DEV-STORY LANDING (RECOVERY).** FR14n Story State `atdd-scaffolded → in-dev` under § Story Lifecycle row `atdd-scaffolded → /bmad-dev-story`. Killed-pre-commit iter-283 (iter-268 recovery precedent — reflog `f631bf8` "feat(story-2-12): iter-268 /bmad-dev-story landing (recovery)") produced all 5 Tasks' file edits in-tree but never committed; recovery iter verified + committed rather than re-invoking the skill. Story file v1.2 Change Log entry + Status HTML comment pre-written by the killed iter — retained verbatim. Sprint-status flipped `ready-for-dev → in-progress → review` at Step 9 completion.

  - **Task 1 (compose healthcheck block):** `docker-compose.yml:263` TODO replaced with CMD-SHELL + `>-` folded-scalar two-clause POSIX sh block (`dig @127.0.0.1 -p 53 +short +time=3 +tries=1 api.github.com >/dev/null && { [ "${KEEL_DEVBOX_SSH:-false}" != "true" ] || nc -z 127.0.0.1 2222; }`); timing `interval 10s` / `timeout 5s` / `retries 3` / `start_period 30s`. Story-roadmap line `:24` past-tense (`LANDED iter-283`). `dash -n` PASS on the joined shell string. AC 1 negative-assertion verified: only two `3000` matches in compose (line 167 `KEEL_DEVBOX_PORT_WEB:-3000` publish + line 264 prose reference to "upstream's broken `curl :3000`" — both expected per Dev Notes line 149).
  - **Task 2 (probe tooling verification):** `dnsutils` present at `Dockerfile:61`; `netcat-openbsd` at `:64`; `USER dev` at `:347`; `grep '^ListenAddress' packages/devbox/sshd/sshd_config` returns empty (PATCH-4 PASS — unset-default listens IPv4 + IPv6 wildcards so `nc -z 127.0.0.1 2222` connects). No Dockerfile change for Task 2.
  - **Task 3 (invariant registration):** `docs/invariants/devbox-healthcheck.md` authored (100 lines; 8 H2 sections — Intent + 7 contract sections); sha256 = `665174a3592106eabbb9f6b5dd39ce813839b3c61a89f1709d81e90524d18623`; manifest entry added after `INV-devbox-ssh` (contiguous Devbox block per iter-268 convention); `INVARIANTS.md` H3 `### Devbox healthcheck (Story 2.13)` + anchor bullet between Story 2.12 and Story 2.2 H3s. Manifest count 32 → 33. `pnpm --filter @keel/keel-invariants build` + `pnpm keel-invariants:check` + `pnpm keel-invariants:check-all` all GREEN.
  - **Task 4 (operator + agent docs):** `README.md § Healthcheck (Story 2.13)` H2 appended between Opt-in SSH (Story 2.12) and cc-devbox upstream provenance (SC-17 sibling-append discipline; prior sections UNCHANGED); `AGENTS.md § Healthcheck (Story 2.13)` H3 appended after Opt-in SSH within § Devbox iteration environment; `README.md:921` forward-ref to past-tense (`→ fixed in Story 2.13 … LANDED iter-283`). No `.envrc.example` edit (no new `KEEL_DEVBOX_*` knob).
  - **Task 5 (D-1 absorption — optional):** Absorbed. One-line `Dockerfile` `RUN install -m 0644 -o root -g root /dev/null /var/log/sshd.log` at `:318-330` after dnsmasq.log pre-create at `:317`. Inline comment pins the pre-gosu-only DAC invariant (iter-281 PATCH-6). Cumulative Epic-2 DEFER queue for Story 2.17 SC-17 close-out at iter-283 in-dev landing = 22 (Story 2.12 final) - 1 (D-1 absorbed) + 3 (Story 2.13 pre-dev SM D-5/D-6/D-7) = **24 DEFERs** pending Story 2.17.

  - **Recovery verification trail:** (a) `git status` surfaced WIP on 7 tracked files + 1 untracked (`docs/invariants/devbox-healthcheck.md`) — matched the story file's § File List exactly; (b) sha256 of the authored doc = `665174a3…d18623` matched the `contentHash` in the manifest diff; (c) `pnpm --filter @keel/keel-invariants build` → `pnpm keel-invariants:check` + `check-all` GREEN on first try post-rebuild (iter-257 LESSON — manifest rebuild is load-bearing after new InvariantSchema entry; applies equally to recovery iters); (d) POSIX `dash -n` on the composed healthcheck CMD → exit 0; (e) 3-site `api.github.com` lockstep: compose + invariant doc + README all contain the probe domain; (f) `grep '^ListenAddress' packages/devbox/sshd/sshd_config` empty (PATCH-4 verification PASS). AC coverage: AC 1 + AC 5 PASS via static smokes; AC 2 + AC 3 PASS via static CMD-SHELL shape + dash parse + POSIX-sh branching analysis; AC 4 operator-workstation-deferred (DinD backend B cannot safely exercise mid-run SIGKILL of dnsmasq/sshd against cap-dropped containers).

  - **PR:** #230 **Draft** — `statusCheckRollup: []` carries unchanged across iter-272..283; recovery iter push at step 5 expected clean.

  - **Budget consumed:** ~30K tokens (orient ~10K + story file read + WIP verification via git diff across 7 files ~10K + rebuild + sync-gate + POSIX parse + lockstep smoke ~4K + IP + RALPH.md + commit-prep ~6K). Well within ~117K execution budget; exit cleanly per Guardrail 12. No skill re-invocation consumed per recovery-pattern discipline (iter-268 precedent).

- [x] iter-282: Story 2.13 ATDD-SKIP — `/bmad-testarch-atdd` SKIP-WITH-GROUNDS-(c)+(ii)+(iii); `validated → atdd-scaffolded`; 23rd-cumulative precedent; ZERO-PATCH iter. See commit `3f35dd6`.

- [x] iter-281: Story 2.13 VALIDATED — `/bmad-create-story (args: "review")` pre-dev SM; `drafted → validated`; 6 PATCH + 3 DEFER. See commit `6e0e810` + IP-cleanup `9863358`.

- [x] iter-280: Story 2.13 DRAFTED — `/bmad-create-story`; `_(no story) → drafted`. See commit `985aee0`.

- [x] iter-279: Story 2.12 DONE — CR closure re-run #2 ZERO-PATCH. See commit `ce3ffb4`.

- [x] iter-272..278: Story 2.12 PATCH-1..6 + iter-277 CR closure re-run #1. Commits `30b7d8d`/`1df64ab`/`0d83fae`/`820591a`/`43b4e4b`/`3eadcff`/`947cbce`.

_(iter-253..271 Story 2.10/2.11/2.12 closure + Story 2.12 lifecycle iters pruned per Guardrail 2.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **13/17 stories done** (2.1-2.13) + 4/17 backlog (2.14..2.17). Story 2.14 entered lifecycle at iter-287 `_(no story) → drafted`.
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered through iter-287 (`pnpm keel-invariants:check` GREEN at 33 manifest entries; unchanged from iter-286 — drafting iter touches no manifest).
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:120). Host-side shim count: **18** at iter-287 (unchanged — drafting iter is BMad-skill-only; zero substrate edits).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **13 done** (2.1-2.13 after iter-286 CR closure); **4 backlog/in-flight** (Story 2.14 ready-for-dev at iter-287; 2.15-2.17 backlog). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.14 — Legacy-devbox branch retention policy.
- **Story File:** `_bmad-output/implementation-artifacts/2-14-legacy-devbox-branch-retention-policy.md`
- **Story State:** `drafted` at iter-288 entry (iter-287 landed `_(no story) → drafted` via `/bmad-create-story`). Next iter (iter-288): `/bmad-create-story (args: "review")` pre-dev SM per § Story Lifecycle row `drafted → /bmad-create-story (args: "review")`.
- **GitHub Issue:** Story 2.14 issue unknown; `RALPH_ISSUE_NUMBER` unset at iter-287. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` carried unchanged iter-272..287; `state=open, draft=true, mergeable=clean` confirmed via REST fallback at iter-287 orient).

## Notes

- **iter-286 NOVEL LESSON (narrow-diff moderate-novelty cumulative PATCH band = ~6-10 lifecycle):** Story 2.13 cumulative PATCH count = 6 (iter-281 pre-dev SM) + 2 (iter-285 post-dev SM) + 0 (iter-286 CR) = **8 PATCH total across lifecycle**. Distribution confirms iter-270 NOVEL LESSON (pre-dev SM absorbs novel surface → post-dev gates clean-advance dominant) + iter-281 NOVEL LESSON (documentation-heavy stories' pre-dev SM band wider than runtime-code-heavy at same novelty). Forecast for Stories 2.14..2.17 narrow-diff moderate-novelty lifecycle = ~6-10 cumulative PATCH; novel-runtime story (2.12-class) = ~10-14 cumulative. Apply at Story 2.14 pre-dev SM forecasting (iter-288 forecast: 3-6 PATCH).
- **iter-286 NOVEL LESSON (ZERO-PATCH CR first-pass precedent extends iter-279 class):** iter-279 Story 2.12 was ZERO-PATCH at CR closure re-run #2 (second closure iter after 1 re-run). iter-286 Story 2.13 is ZERO-PATCH at CR FIRST-PASS (no prior fix iters). Precedent: narrow-diff moderate-novelty stories where pre-dev SM absorbs substantive findings can close CR in single iter without re-run. For Stories 2.14..2.17 narrow-diff class: budget **1 CR iter only**; do NOT pre-queue closure re-runs. Novel-runtime-class stories (hypothetical — none left in Epic 2) still need ~2 CR iters per iter-264 LESSON.
- **iter-286 NOVEL LESSON (adversarial convergence on spec-endorsed claim → DEFER-verify-later, not PATCH-blind):** Blind + Edge Case Hunters independently flagged the "probes run as USER dev" claim at 3 substrate sites; story Dev Notes:145 takes an OPPOSING spec position without empirical validation. Per iter-277 NOVEL LESSON #2 META guard (empirical-test override when Acceptance Auditor CONTRADICTS Blind+Edge convergent), the disciplined move when Ralph cannot empirically disconfirm from DinD backend B under cap-dropped semantics is DEFER-to-operator-workstation-verify, NOT blind-PATCH. Pinned D-8 for Story 2.17 close-out with explicit smoke protocol. This is a NEW class of META guard: "adversarial convergence on spec-endorsed-but-empirically-unvalidated claim → DEFER with operator-smoke protocol". Promoted to RALPH.md § Lessons.
- **iter-285 NOVEL LESSON (same-commit citation-drift from DEFER absorption):** a PATCH-like absorption that inserts lines (Task 5 D-1 inserted 13 lines at Dockerfile:318-330 in iter-283) can shift line numbers of subsequent anchors, invalidating citations authored in the SAME commit pre-absorption. In iter-283, the `USER dev` directive shifted :347→:360, but the Dev Notes + References + Completion Notes + invariant doc § Probe tooling + compose-file inline comment + manifest description were all authored as if the line were still :347. Six-site citation drift in a single commit, detected at iter-285 post-dev SM. **Guard for future DEFER-absorbing stories:** when a Task absorbs a DEFER by inserting lines into a source file, grep ALL story-file citations referencing lines in the same file AFTER the insertion point and resync them in the same commit. Applies most directly to Stories 2.14..2.17 if they absorb remaining Story 2.17 DEFERs that insert lines (e.g., D-2 SSHD_PID subshell scoping is an entrypoint.sh edit — any in-scope absorption would need entrypoint.sh citation-audit post-edit). Pinned in RALPH.md § Lessons.
- **iter-285 observation (two-subagent pattern held within band):** iter-235 LESSON two-subagent pattern (AC-satisfaction audit + implementation-fidelity audit) delivered 2 PATCH within the iter-270 drift-band re-baseline forecast (0-2 PATCH band). Ralph-synthesis triage matched both audits cleanly — no cross-audit conflicts; the Dockerfile line-number drift surfaced ONLY in the implementation-fidelity audit (AC audit read the ACs' substance, not prose citation fidelity). This validates the two-audit division: AC audit → "does the impl satisfy the story requirements"; fidelity audit → "does the Dev Agent Record accurately describe the impl". Keep two-subagent pattern as post-dev SM default for narrow-surface stories (2.14/2.16/2.17-class documentation-heavy); escalate to 3-subagent pattern if runtime-behaviour surface is large (2.15-class Claude settings.json hook lifecycle = potentially 3-subagent like 2.12).
- **iter-285 observation (invariant-doc edit requires manifest contentHash update):** editing a contentHash-tracked invariant doc MANDATES a same-commit manifest update. Workflow validated at iter-285: (a) edit doc → (b) `sha256sum doc` → (c) update manifest `contentHash` with new hex → (d) `pnpm --filter @keel/keel-invariants build` (tsc rebuild mandatory; iter-257 LESSON reaffirmed at iter-283 Completion Notes; applies equally at iter-285) → (e) `pnpm keel-invariants:check` + `check-all` → (f) GREEN before commit. This is the 2nd time the manifest contentHash has been refreshed within a story's lifecycle (first: iter-283 INV-devbox-healthcheck initial landing; now: iter-285 PATCH-1 refresh). Pattern stable.
- **iter-284 observation (trace WAIVED via structural inheritance):** Story 2.12 iter-269 trace artefacts served as the canonical template; ~85% reasoning-cost savings via mechanical adaptation to Story 2.13's 5-AC healthcheck-compose-probe class. Four artefacts landed: markdown + 3 JSONs. Zero code edits — trace is gate-only; manifest count unchanged at 33; sync-gate GREEN on first try (no rebuild needed — no `invariants.manifest.ts` edit). Structural pattern carry-forward to Stories 2.14..2.17 traces expected: continue template inheritance discipline + three-site lockstep checks + ground-(a)+(b)+(c) hybrid framing.
- **iter-283 observation (recovery iter pattern reused; iter-268 precedent):** The WIP in `git status` at iter-283 orient was produced by a killed-pre-commit prior iter-283 `/bmad-dev-story` invocation. Recovery was verification-and-commit (NOT re-invoke the skill). Verification steps: (a) story file's `File List` matches `git status` output exactly; (b) story file's claimed sha256 (`665174a3…d18623`) matches `sha256sum` of the actual doc; (c) contentHash in manifest matches doc sha256; (d) `pnpm --filter @keel/keel-invariants build` + sync-gate GREEN; (e) POSIX `dash -n` parse GREEN; (f) 3-site `api.github.com` lockstep + `^ListenAddress` empty smokes GREEN. Same precedent-match pattern as iter-268 Story 2.12 recovery (reflog `f631bf8` — "feat(story-2-12): iter-268 /bmad-dev-story landing (recovery)"). Commit framing preserves iter-283 number (not "iter-284 recovery") per iter-268 convention.
- **iter-283 observation (TodoWrite reminder hooks continue):** harness surfaced TodoWrite reminders twice during this recovery iter (once mid-orient after verification-branch decision, once during README diff review). Matches iter-282 observation: in narrow-surface iters (~30K) the todo list is a single in-progress entry; reminder is false-positive at this iter density. No pattern change — documented harness behaviour.
- **iter-257 LESSON REAFFIRMED (manifest rebuild mandatory; applies to recovery iters too):** `pnpm --filter @keel/keel-invariants build` MUST run before `pnpm keel-invariants:check` after any `invariants.manifest.ts` edit — including when verifying inherited WIP. Sync-gate consumes the compiled `dist/check.js`; stale dist shows `removed-from-docs-only INV-devbox-healthcheck` false drift. Recovery iters should treat the rebuild as load-bearing even when they did NOT author the manifest edit themselves. Story 2.13 v1.2 Completion Notes captures the same observation from the killed iter-283.
- **iter-281 NOVEL LESSON carry-forward (doc-heavy pre-dev SM band widening):** pinned in RALPH.md § Lessons; not exercised at iter-283 (recovery iter); will reactivate at iter-285 post-dev SM (assuming iter-284 `/bmad-testarch-trace` lands WAIVED per forecast).
- **iter-280/281 LESSON carry-forward (citation-audit false-positive mitigation):** applicable at iter-285 post-dev SM audit prompt.
- **iter-278 LESSON carry-forward (DEFER absorption discipline):** FIRED at iter-283 — Task 5 optional absorption of Story 2.12 iter-279 D-1 landed. Pattern validated again (Dockerfile pre-create of `/var/log/sshd.log`; rationale pinned inline; cumulative DEFER queue reduced 25 → 24).
- **iter-277 NOVEL LESSON carry-forward (bash subshell `||`-LHS errexit suppression):** does NOT apply — Story 2.13 healthcheck CMD runs under `/bin/sh` (dash) `&&`/`||` at runtime, not a bash subshell; stays codified for any bash-wrapped wrappers introduced downstream.
- **iter-276 NOVEL LESSON carry-forward (hazard-class discrimination under word-splitting):** stable at iter-283; Story 2.13 `>-` folded-scalar avoids the blank-line `\n` preservation hazard per iter-281 PATCH-3 story-level guard; dev-story implementation kept the two clause lines adjacent as instructed.
- **iter-273 NOVEL LESSON carry-forward (compose env-propagation chokepoints):** stable at iter-283 — Story 2.13 healthcheck reuses Story 2.12 PATCH-2 `KEEL_DEVBOX_SSH_RESOLVED` canonical stream via `${KEEL_DEVBOX_SSH:-false}` read inside the container; no new chokepoint.
- **iter-271 LESSON carry-forward (three-subagent pattern for post-dev SM + CR):** applicable at iter-285 post-dev SM + iter-286 CR. Not exercised at iter-283.
- **iter-270 NOVEL LESSON carry-forward (drift-band re-baseline):** applicable at iter-285 post-dev SM forecast.
- **iter-264 LESSON carry-forward:** CR re-run DEFER-vs-PATCH severity-class-first; Story 2.13 forecast closure budget = 1 closure re-run at worst.
- **iter-263 LESSON carry-forward:** SSH :22 / HTTPS :443 asymmetric recovery; iter-286 orient exercised REST fallback after GraphQL timeout at `gh pr view 230` — cleared first-try on REST probe. Step-5 push will exercise SSH :22.
- **Substrate-citation drift cumulative forecast for Stories 2.14..2.17:** ~4-10 cumulative substantive + ~25-40 cumulative mechanical drifts (reduced from prior forecast after Story 2.13 clean closure). SC-17 single reconciliation at Story 2.17 landing. Cumulative DEFER tally at iter-286 Story 2.13 CR closure = **30 DEFERs** pending Story 2.17 SC-17 (24 carry-forward from Story 2.12 final + 6 new iter-286 CR defers: D-8 UID-empirical, D-9 probe robustness, D-10 exit-2 edge, D-11 Dockerfile lockstep lint scope, D-12 README sudo disclaimer, D-13 start_period + AGENTS.md cross-ref).
