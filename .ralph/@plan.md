# Implementation Plan

## NOW

- [ ] Run `/bmad-create-story` — pick up the next Epic 2 story from `sprint-status.yaml` backlog (2-2 envrc-parameterisation-contract is the next entry; skill auto-advances `2-2: backlog → ready-for-dev` and produces the story file). Story 2.1 is `done` (iter-144 third CR gate ZERO-PATCH verdict); all Epic 2 stories 2.2..2.17 remain in `backlog`. Per § Story Lifecycle Decision Matrix row `done`, the next iteration's NOW is `/bmad-create-story` for the next story in the current epic — within-epic advancement, not cross-epic transition (Epic 2 is not yet complete; PR #230 stays Draft across the full epic per the Epic 2 § Cross-epic transition plan at "Transition PR Draft→Open — final CI gate after all Epic 2 stories done"). State transitions: `Epic = Epic 2 - Sandboxed Execution Environment (devbox)` unchanged; `Story = 2.2 - envrc-parameterisation-contract`; `Story File = _bmad-output/implementation-artifacts/2-2-envrc-parameterisation-contract.md` (new); `Story State = _(no story) → drafted` after skill runs. One-workflow-per-iteration guardrail: `/bmad-create-story` IS this iteration's full task; do NOT bundle with `/bmad-create-story (args: "review")` (pre-dev SM review is next iteration's NOW per matrix row `drafted`). ~medium

## QUEUE (Story 2.2 `_(no story) → drafted → validated → atdd-scaffolded → in-dev → traced → sm-verified → done`)

- [ ] Run `/bmad-create-story (args: "review")` — pre-dev SM validation of Story 2.2 readiness. State transition `drafted → validated`.
- [ ] Run `/bmad-testarch-atdd` — red-phase scaffolds for Story 2.2 (or record FR14n ATDD-skip rationale if no test runner configured + no testable runtime ACs). State transition `validated → atdd-scaffolded`.
- [ ] Run `/bmad-dev-story (args: "{story_file_path}")` — implement Story 2.2. State transition `atdd-scaffolded → in-dev`.
- [ ] Run `/bmad-testarch-trace (args: "yolo")` — AC→test coverage gate + traceability matrix for Story 2.2. State transition `in-dev → traced`.
- [ ] Run `/bmad-create-story (args: "review")` — post-dev SM requirements-satisfaction verification. State transition `traced → sm-verified`.
- [ ] Run `/bmad-code-review (args: "2")` — post-SM CR adversarial action-items gate. State transition `sm-verified → done` OR `sm-verified → fixes-pending` (depending on adversarial-triage verdict; multi-cycle envelope bounded by Story 1.9's iter-7 → iter-14 → iter-17 precedent).
- [ ] _(conditional, post-Story-2.17 done)_ Transition PR #230 Draft→Open — final CI gate after all Epic 2 stories done; monitor CI; merge.
- [ ] _(conditional, post-Epic-2 merged)_ § Cross-epic transition → Epic 3 Story 3.1 via `/bmad-create-story` OR halt EPIC_DONE awaiting merge.

## BLOCKED

_(none — Story 2.1 `fixes-pending → done` transition cleared all action items; Story 2.2 backlog entry has no prerequisites beyond the `/bmad-create-story` invocation.)_

## DONE (Story 2.1 — iter-144 third-gate close)

- [x] iter-144: **Third CR gate — `/bmad-code-review (args: "2")` RE-RUN-2 ZERO-PATCH verdict. Story State `fixes-pending → done`; Status `in-progress → done`; sprint-status `2-1: in-progress → done`.** Twenty-eighth cumulative Epic post-SM CR / CR-re-run iteration across Stories 1.7..1.16 + Story 2.1. Three-layer Ralph-hosted adversarial fan-out against `git diff main...HEAD` (2,704 lines / 20 files) using parallel subagents (each no-conversation-context): **Blind Hunter** (diff-only) 20 findings; **Edge Case Hunter** (diff + project read) 16 findings; **Acceptance Auditor** (diff + spec + context) ZERO substantive findings + ALL 9 AI-1..AI-9 (iter-128 first-gate) and ALL 5 AR-1..AR-5 (iter-138 second-gate) closures VERIFIED byte-identical to landed code. AC verdicts: AC 1 SATISFIED + AC 2 SATISFIED + AC 3 PARTIAL under WAIVED operator-owned carve-out from iter-126 + AC 4 PARTIAL under WAIVED operator-owned carve-out; scope-creep CLEAN; defer-list discipline CLEAN; context-doc consistency CLEAN across devbox-dind.md / architecture.md / prd.md; sprint-status + trace-matrix CLEAN. Deduplicated across Blind + Edge layers (5 converged dupes: Blind F1 = Edge E-1 numeric UID regex; Blind F20 = Edge E-8 empty-arg; Blind F4/F5/F16 = iter-138 defer duplicates) → **33 unique findings** triaged per FR14n adversarial default into **0 patch findings** + **7 defer items** (carried to `deferred-work.md § Deferred from: code review RE-RUN-2 of story-2.1 (2026-04-21)`) + **26 dismissed** as noise / intentional / by-design / duplicates-of-existing-defers. **ZERO-PATCH THIRD GATE** — first zero-patch CR on Story 2.1 at its third gate; matches Story 1.8 iter-7 precedent ("first zero-patch CR on Epic 1 substrate-stage") but with a multi-cycle extension: Story 2.1 required THREE CR cycles vs Story 1.8's single pass, confirming that Story 1.9's iter-7 → iter-14 → iter-17 three-cycle envelope scales to Epic 2 infrastructure-smoke stories. **Artifacts updated:** story file `### Review Findings` gains new `#### iter-144 RE-RUN-2` subsection (0 patch + 7 defer + 26 dismiss enumeration + ~2k-word context paragraph + landing evidence); Status header `in-progress → done`; Change Log v2.12 entry prepended (descending-v2.x convention); `deferred-work.md § Deferred from: code review RE-RUN-2 of story-2.1 (2026-04-21)` new section with 7 defer entries (numeric UID regex → Story 2.5; VERSIONS.md primary-table pin propagation → Story 2.6; SIGPIPE uncovered → Epic 13; ERR-trap diagnosis missing → Epic 13; `/.dockerenv` misclassifies podman → Story 2.11/Epic 15a; Ubuntu:24.04 digest pin → Epic 4 Story 4.10; detect_backend case-variant Finch/Rancher → Story 2.11/Epic 15a); `sprint-status.yaml` `2-1: in-progress → done` with `last_updated: 2026-04-21 Story-2-1-done UTC`. **Next iteration:** `/bmad-create-story` for Story 2.2 per § Story Lifecycle Decision Matrix row `done` → next-story-in-current-epic path (Epic 2 has 16 remaining backlog stories 2.2..2.17; not yet cross-epic). **Pattern lesson:** Epic-2-infrastructure-smoke stories can require multi-cycle CR (three cycles is the upper envelope); the ZERO-PATCH third gate is the expected verdict when prior cycles' bundled action items drained cleanly; third-gate findings are dominated by class-extensions of prior-gate fixes (iter-144's SIGPIPE / ERR-diagnosis extend AR-1; case-variant + Finch extend iter-138 defer F6) + defensive nits that don't reach the patch bar + re-raises of existing defers. Adversarial-default correctly routes these as defers/dismissals; Acceptance Auditor's ZERO-substantive-findings verdict is the signal that the story is CR-complete. Multi-cycle upper envelope for Epic-2: three cycles (iter-128 + iter-138 + iter-144); beyond that, a fourth cycle indicates churn without progress and should escalate to IP § BLOCKED.

- [x] iter-128..iter-143 (summarised; full detail in git log + story file Change Log v1.7..v2.11 + prior IP versions): **Twelve cumulative fix iterations** across two CR cycles. Iter-128 first CR gate: 9 AI-1..AI-9 patch action items from 49 raw findings (3 High + 3 Medium + 3 Low; 12 defers + ~11 dismissed). Iter-129..iter-137: AI-1 Dockerfile @2.1.116 pin; AI-2 container_name parameterisation + Compose v2.20+ annotation; AI-3 detect_backend fail-safe-B; AI-4 benchmark.sh safety bundle (trap + probe + combo-warn + flag-text); AI-5 entrypoint.sh bundle (chown stderr-capture + regex + terminator + empty-CMD fallback); AI-6 supabase-cli fail-closed; AI-7 INV-devbox-dind-available heading + manifest contentHash regen; AI-8 README heading demotion; AI-9 VERSIONS.md upstream-apt-extras section. Iter-138 second CR gate: 5 AR-1..AR-5 patch action items bundled from 9 findings (2 Medium + 3 Low; 18 defers + 6 dismissed). Iter-139..iter-143: AR-1 benchmark.sh trap-completeness (INT=130/TERM=143/HUP=129/QUIT=131 + EXIT-trap cleanup); AR-2 entrypoint.sh concurrency+charset (mktemp + widened regex); AR-3 VERSIONS.md doc-drift (Supabase + uv rows); AR-4 story Task 3 wording (chown -R removal + cross-ref); AR-5 benchmark.sh --help/-h flag. First successful baked image iter-123 (`keel-devbox:local`, 848 MB, linux/arm64, ~4.5 min). Two operator-owned carve-outs (iter-124 Tasks 4.2/4.3/5.2 backend-B bind-mount denial; iter-126 trace gate WAIVED; iter-127 SM-verified with PARTIAL; iter-128/138/144 CR chain). Twenty-seven cumulative Epic-wide post-SM CR / CR-re-run-fix iterations culminating in this iter-144 third-gate ZERO-PATCH close.

## Context

- **Phase:** 4-implementation — Epic 2 open at 1/17 stories done (2.1); Story 2.1 is `done` as of iter-144 (third CR gate ZERO-PATCH verdict). Next action: `/bmad-create-story` for Story 2.2 per § Story Lifecycle Decision Matrix row `done` → next-story-in-current-epic path.
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available` § Backend contract). Iteration-context bind-mount denial persists as an operational constraint; operator-workstation resolves via M4-Pro native Docker Desktop (backend A equivalent) with worktree under host-shared `/Users/...` OR backend-A isolated DinD harness.
- **Baked image:** `keel-devbox:local` (first bake iter-123 2026-04-21; Story 2.1 complete). Version matrix in `packages/devbox/VERSIONS.md § Bake log` (iter-123 entry authoritative; Story 2.6's `pnpm devbox:rebuild` will auto-append future bakes per iter-144 defer).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; Story 2.1 (image + compose + entrypoint foundation) is done; Stories 2.2..2.17 remain in `backlog`.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2; PR #230 transitions to Open at epic completion).
- **Story:** _(no story)_ — next iteration's `/bmad-create-story` will produce Story 2.2 file + transition `2-2: backlog → ready-for-dev` in sprint-status.
- **Story File:** n/a (Story 2.1 done; Story 2.2 file not yet produced — `/bmad-create-story` is the next iteration's task).
- **Story State:** _(no story)_ — Story 2.1 finished at `done` iter-144; next iteration's first action per matrix is `/bmad-create-story`.
- **GitHub Issue:** Story 2.1 → #41 (https://github.com/tthew/ralph-bmad/issues/41) — closes on PR #230 merge (`Closes #41` will be added to PR body this iteration). Parent Epic 2 → #10. Commit trailer `Refs #41` this iteration.
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2; transitions to Open at Story 2.17 done; no CI runners pre-Epic-13).

## Notes

- **DinD is fork-time substrate, not transitional.** Every fork's cc-devbox-equivalent environment must provide Docker per `INV-devbox-dind-available`. Forks substituting Podman/rootless Docker remain conformant if daemon reachability + function hold (with a backend C extension per iter-144 defer on podman misclassification).
- **Backend B is the reference environment at 2026-04-21.** Host socket-passthrough to macOS Docker Desktop. Broad-state-mutation scripts MUST detect the backend + refuse destructive ops by default (`benchmark.sh` is the reference from iter-122). Bind-mount-based compose runtime requires host-shared File Sharing allowlist coverage of the workspace path — operator-owned from iteration context.
- **NFR2 authority unchanged.** AC 4 cold ≤ 300s / warm ≤ 30s remains authoritative on M4-Pro native. Post iter-124, BOTH cold AND warm runtime measurement are operator-owned (waived with operator-owned carve-out iter-126 through iter-144 chain).
- **Cross-epic auto-advance pattern validated** (iter-96). **Partial-completion pattern validated** (iter-99). **Backend-B safety-gate pattern established** (iter-122). **Review-with-operator-owned-carve-out pattern validated** (iter-125). **SM-verified-with-operator-owned-carve-out pattern validated** (iter-127). **CR-fixes-pending-adversarial-default pattern validated** (iter-128). **CR-re-run-adversarial-default + action-item-bundling pattern validated** (iter-138). **Doc-drift two-row bundle closure pattern validated** (iter-141). **Story-artefact spec-vs-code drift closure pattern validated** (iter-142). **Operator-UX polish pattern validated** (iter-143). **ZERO-PATCH third-gate close pattern validated** (iter-144; first zero-patch CR for Story 2.1; extends Story 1.8 iter-7 single-pass precedent to Story 1.9 multi-cycle precedent for Epic-2-infrastructure-smoke-stories; three cycles is the upper envelope for multi-cycle CR chains; beyond that escalate to IP § BLOCKED; ZERO-PATCH Acceptance Auditor verdict is the signal that the story is CR-complete).
- **Story 2.1 scope bounded by AC1-AC4 literally.** Substrate's pre-existing `packages/devbox/` TS scaffold preserved; Story 2.1 ADDED runtime-infrastructure siblings (Dockerfile + docker-compose.yml + entrypoint.sh + scripts/benchmark.sh + VERSIONS.md + README.md expansion + docs/invariants/devbox-dind.md + manifest entry + INVARIANTS.md anchor + architecture.md I5a cross-ref + AGENTS.md ops notes).
