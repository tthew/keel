# Implementation Plan

## NOW

- [ ] `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-14-legacy-devbox-branch-retention-policy.md")` per § Story Lifecycle row `atdd-scaffolded → /bmad-dev-story`. Tasks 1-4: Task 1 git-branch creation + push (one-time `git fetch upstream cc-devbox + worktree-bootstrap + README banner + push`); Task 2 invariant doc author; Task 3 manifest entry + INVARIANTS.md anchor (33 → 34); Task 4 README + AGENTS pointers + optional RALPH.md pre-record. Forecast: XL-class iter due to git-branch bootstrap novelty; may land partial with `in-dev (partial)` requiring follow-up. Guardrail 13 pre-flight: ~117K budget − 25K push buffer = ~92K exec budget; dev-story precedent is 40-80K depending on task decomposition.

## QUEUE (Story 2.14 lifecycle + 2.15..2.17 substrate queue)
- [ ] _(after Story 2.14 in-dev)_ Story 2.14 trace — `/bmad-testarch-trace (args: "yolo")` likely WAIVED via ground-(a)+(b)+(c) hybrid (twenty-fourth+ cumulative WAIVED precedent; structural inheritance from Story 2.13 iter-284 template).
- [ ] _(after Story 2.14 traced)_ Story 2.14 post-dev SM — `/bmad-create-story (args: "review")` post-dev. Forecast 0-2 PATCH per iter-270 LESSON.
- [ ] _(after Story 2.14 sm-verified)_ Story 2.14 CR — `/bmad-code-review (args: "2")`. Forecast 0-2 first-class PATCH per iter-279 + iter-286 ZERO-PATCH precedent for narrow-diff stories.
- [ ] _(after Story 2.14 done)_ Story 2.15 committed Claude settings.json deny/allow — full lifecycle.
- [ ] _(after Story 2.15 done)_ Story 2.16 Claude PreToolUse hooks for secret-file denylist — full lifecycle.
- [ ] _(after Story 2.16 done)_ Story 2.17 hook-settings bypass-resistance + Epic 2 SC-17 close-out polish pass. SC-17 reconciliation absorbs cumulative **31 DEFERs at Story 2.14 pre-dev SM landing** (24 carried from Story 2.12 + 6 iter-286 Story 2.13 CR defers + 1 iter-288 Story 2.14 pre-dev SM defer: D-14 Task 1 push-failure recovery + idempotent re-bootstrap); PLUS cumulative Epic 2 Story 2.14 (post-dev SM + CR forecast) + Story 2.15..2.16 DEFERs accrued during those stories.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-24 per iter-219..287 confirmation; `statusCheckRollup: []` continues; iter-287 orient REST fallback first-try GREEN per iter-263 asymmetric-recovery LESSON after `gh pr view 230` GraphQL :443 timeout; SSH :22 expected clean at step-5 push).

## BLOCKED

_(none — iter-289 `/bmad-testarch-atdd` SKIP-WITH-GROUNDS-(ii)+(iii) landed `validated → atdd-scaffolded`; twenty-fourth cumulative ATDD-skip precedent; ZERO-PATCH iter. Sprint-status row unchanged (atdd-scaffolded is lifecycle-internal). Cumulative Epic-2 DEFER queue stays at 31 — no new DEFER at ATDD-skip iter.)_

## ATDD Red Phase

_(none — Story 2.14 ATDD-skipped at iter-289 with grounds-(ii)+(iii); no red-phase tests owed. Narrower grounds than Story 2.13 iter-282 (c)+(ii)+(iii) — Story 2.14 drops (c) since ALL ACs are static-smoke-testable via git-state inspection (no operator-workstation-deferred live ACs).)_

## DONE (iter-289 Story 2.14 `/bmad-testarch-atdd` SKIP-WITH-GROUNDS-(ii)+(iii) — `validated → atdd-scaffolded`; ZERO-PATCH iter; twenty-fourth cumulative ATDD-skip precedent)

- [x] iter-289: **STORY 2.14 ATDD-SKIP LANDING.** FR14n Story State `validated → atdd-scaffolded` under § Story Lifecycle row `validated → /bmad-testarch-atdd`. **Twenty-fourth cumulative FR14n ATDD-skip precedent** (10 Epic-1 + 14 Epic-2 incl. Story 2.14). ZERO-PATCH iter per Story 2.5 iter-186 canonical pattern (IP + RALPH.md + story-file Status HTML comment only; no story-file Change Log v1.1 entry; no separate ATDD checklist artefact generated — skill HALTed cleanly at Step 1 § 2 Prerequisites on missing test framework).
  - **Grounds narrower than Story 2.13 iter-282 (c)+(ii)+(iii) — Story 2.14 applies (ii)+(iii) ONLY:** NEW observation. Story 2.14 drops ground (c) "Mixed-class ACs" because AC 1/2/3/4 are ALL static-smoke-testable via git-state inspection (`git ls-remote origin legacy-devbox` + `git show origin/legacy-devbox:README.md` content-grep + `git log 5278738..HEAD -- packages/devbox/` bisect-scope smoke + manifest `contentHash` sync-gate on new invariant doc); NO operator-workstation-deferred live AC exists. This is the FIRST ATDD-skip story in Epic 2 where (c) doesn't apply (Stories 2.1-2.13 all carried at least one mixed-class / operator-deferred AC). Pattern candidate (NOVEL LESSON for RALPH.md § Lessons): policy-class stories drop ground (c) cleanly when ACs resolve to pure documentation + git-branch-state + substrate-synchrony — (ii)+(iii) grounds suffice.
  - **Grounds load-bearing:** (ii) No live test runner at 1.0 — verified at iter-289 via `ls playwright.config.* cypress.config.* pyproject.toml pom.xml build.gradle go.mod Cargo.toml Gemfile` = empty + `find . -maxdepth 3 -name "*_test.go" -o -name "conftest.py" -o -name ".rspec"` = empty; Epic 13 scope; all 4 ACs land as Task-internal impl-time smokes at `/bmad-dev-story` Tasks 1-4 per dev spec. (iii) Adversarial-coverage substitutes — post-dev SM two-subagent (iter-235 pattern) + CR three-layer fan-out (iter-271/277 pattern) + Story 1.9 sync-gate on `docs/invariants/devbox-legacy-branch-retention.md` sha256 (manifest 33 → 34) + INVARIANTS.md anchor parse (33 → 34 H3s) + `git ls-remote origin legacy-devbox` static smoke at trace + Epic 13 downstream regression (future test-runner exercises manifest/doc synchrony).
  - **Skill halt path:** `/bmad-testarch-atdd` workflow.md Step 1 § 2 Prerequisites dictates HALT when "Test framework configured" is missing for the detected stack. Auto-detection scans for frontend (`playwright.config.*`, `cypress.config.*`), backend (`pyproject.toml`, `pom.xml`, `build.gradle`, `go.mod`, `Cargo.toml`, `Gemfile`), or test-config (`*_test.go`, `conftest.py`, `.rspec`) — all absent at iter-289 repo-root scan. Clean HALT; no `{test_artifacts}/atdd-checklist-2-14-*.md` file created.
  - **Sprint-status row:** stays `ready-for-dev` per iter-282 precedent. `atdd-scaffolded` is lifecycle-internal (visible only in story file Status HTML comment + IP § Context `Story State` field). Sprint-status ledger NOT updated (no new `last_updated` comment line).
  - **Cumulative Epic-2 DEFER queue:** stays at 31 (no new DEFER at ATDD-skip iter; 24 carried from Story 2.12 + 6 iter-286 Story 2.13 CR + 1 iter-288 Story 2.14 pre-dev SM D-14 push-failure recovery). Story 2.17 SC-17 close-out absorbs.
  - **Budget consumed:** ~20K tokens (orient ~10K + skill workflow Step 1 read + preflight verification + IP + RALPH.md + Status HTML comment + commit-prep ~10K). Well within ~117K execution budget; narrow ATDD-skip iter density matches iter-282 ZERO-PATCH precedent.
  - **PR:** #230 **Draft** — `state=open, draft=true, mergeable=MERGEABLE, statusCheckRollup=[]` confirmed via single `gh pr view 230` at iter-289 orient (no GraphQL :443 timeout this iter; clean first-try). Unchanged since iter-272; no CI configured; clean push expected at step 5.

- [x] iter-288: **STORY 2.14 PRE-DEV SM LANDING.** FR14n Story State `drafted → validated` under § Story Lifecycle row `drafted → /bmad-create-story (args: "review")`. Two-subagent pattern (AC-completeness audit + spec-fidelity audit) per iter-285 LESSON sufficient for narrow-surface; Ralph-synthesis triage. **6 PATCH + 1 DEFER + ~5 DISMISS** — within iter-286 NOVEL LESSON forecast band for narrow-diff documentation-heavy stories (3-6 PATCH at pre-dev SM).
  - **PATCH-1 (FR33 citation correction):** References line 230 `prd.md:932 → prd.md:997`. Actual verification: `grep -n "^- \*\*FR33\*\*\|^- \*\*FR6\*\*" prd.md` returned `932:FR6` and `997:FR33`. Path-convention sourcing note added ("Path convention `docs/research/checkpoints/YYYY-Q#.{md,json}` is established by architecture.md:361 — FR33 itself specifies the capability, not the path") to disambiguate conflation between prd.md:143 (which mentions `docs/checkpoints/`) and architecture.md:361 (which specifies the quarterly `YYYY-Q#.{md,json}` convention).
  - **PATCH-2 (README EOF / H2-position drift):** Project Structure Notes line 210 + References line 242 — `~923 → ~1001` with H2-name anchoring preferred over line-number hints. Actual verification: `wc -l packages/devbox/README.md` = 1001; `grep -n "^## "` shows `:909 Healthcheck (Story 2.13)` + `:985 cc-devbox upstream provenance`. H2-name anchoring is drift-resilient; numeric hints are stale from pre-Story-2.13 count (drift ~78 lines).
  - **PATCH-3 (`<UPSTREAM_SHA>` placeholder resolution):** Task 1 § README retention banner — explicit `sed -i` substitution step + grep-verification guard added. Rationale: original text had "Substitute `<UPSTREAM_SHA>` with the captured `$UPSTREAM_SHA`" as prose; a dev agent literally reading the banner template might land `<UPSTREAM_SHA>` verbatim without performing the substitution. The grep guard fails-loudly if placeholder remains.
  - **PATCH-4 (hook-bypass scope annotation):** Task 1 § Commit + push bash block — inline SCOPE GUARDRAIL comment added asserting hook-bypass is ONLY acceptable for legacy-devbox branch commits; on `main`-targeting commits it is FORBIDDEN per Story 1.6's `INV-no-verify-bypass`. Prevents pattern transfer. Dev Notes already had this context at line 187; PATCH brings it inline with the recipe to prevent context-splitting.
  - **PATCH-5 (devbox-mode.md length target correction):** Task 2 § Length target `~120 → ~237`. Actual verification: `wc -l docs/invariants/devbox-mode.md` = 237 (2x off from claimed `~120`). Reframed comparison: "on par with devbox-mode.md (~237; multi-section recipe-heavy)" and "denser than devbox-healthcheck.md (~100; single-probe contract)". Length target widened to `~150-250 lines`.
  - **PATCH-6 (AGENTS.md (e) curl:3000 claim softening per iter-286 NOVEL LESSON #3):** Task 4 § AGENTS.md cross-reference (e) — softened the assertion "legacy-devbox carries upstream's broken `curl :3000` healthcheck — that's a known-divergence" to "legacy-devbox inherits UPSTREAM's healthcheck as-fetched — at time of Story 2.14 drafting upstream ships the broken `curl :3000` healthcheck, but dev agent MUST grep `$WT/docker-compose.yml` at Task 1 execution to record the actual state in Completion Notes". Per iter-286 NOVEL LESSON #3 (adversarial-convergence-on-spec-endorsed-claim → DEFER-verify-later → OR promote to empirical-verify-at-impl): the claim is spec-endorsed but unvalidated; promoting to "grep at impl time and record in Completion Notes" converts it from a static assertion to an empirical verification, closing the iter-286 META-guard loop without a DEFER.
  - **D-14 (push-failure recovery + idempotent re-bootstrap; DEFER to Story 2.17 SC-17 close-out):** Task 1 has no guidance for resumption if `git push -u origin legacy-devbox` fails mid-execution (auth, network). Also no guidance on whether force-pushing over an existing `origin/legacy-devbox` is acceptable if Task 1 runs twice. Defer to SC-17 close-out (polish-class, not a blocker; Task 1 is a one-time operation). Cumulative Epic-2 DEFER queue 30 → 31.
  - **~5 DISMISSes:** (1) manifest count 33 claim — verified correct via `grep -c "^  {" invariants.manifest.ts` = 33. (2) AGENTS.md `### Healthcheck (Story 2.13)` last-H3 under § Devbox iteration environment claim — verified at AGENTS.md:190 (§ Ralph loop begins at :202). (3) PRD/architecture/epics line-number citations `:617`, `:676`, `:668`, `:143`, `architecture:131-134`, `architecture:361`, `epics:1602-1627`, `epics:6293-6314` — all verified correct. (4) sync-gate regex at sync-gate.ts:24 verbatim `/^-\s+\*\*\`(INV-[a-z0-9]+(?:-[a-z0-9]+)+)\`\*\*/gm`, lowercase-after-`INV-` mandated. (5) AC 3 vs Task 2 retirement-ordering ambiguity — Task 2 correctly orders (1) tag → (2) push tag → (3) delete branch → (4) RALPH.md; AC 3 is a requirements-set not a procedure; dev agent will execute correctly from Task 2. (6) iter-286 NOVEL LESSON #1 forecast-band self-reference — not in spec-fidelity audit scope.
  - **AC coverage at iter-288 pre-dev SM closure:** AC 1 (branch creation + README banner) covered by Task 1 (creation method + banner template + `sed` substitution + verification); AC 2 (cherry-pick workflow + minimal-drift scope) covered by Task 2 § Cherry-pick workflow (full recipe + explicit "CVE-class/egress/secret-leakage only" scope); AC 3 (retirement procedure + Story 15b.1 binding) covered by Task 2 § Retirement gate (4-step sequence + lockstep cross-reference to epics.md:6293-6314); AC 4 (triage path + AGENTS/README pointers) covered by Task 2 § Triage path + Task 4 § packages/devbox/README.md + § AGENTS.md pointers (full recipe + TL;DR variants + `INV-devbox-legacy-branch-retention` citation).
  - **Two-subagent pattern observation:** AC-completeness audit (Subagent A) surfaced 7 findings (0 CRITICAL / 5 NON-CRITICAL / 1 DEFER / 1 DISMISS); spec-fidelity audit (Subagent B) surfaced 8 findings (1 CRITICAL / 6 NON-CRITICAL / 1 DISMISS). Both audits converged on the README EOF drift (PATCH-2) and FR33 citation (PATCH-1). Non-convergent findings from AC-audit: `<UPSTREAM_SHA>` placeholder (PATCH-3), hook-bypass scope (PATCH-4), curl:3000 claim (PATCH-6), push-failure recovery (D-14). Non-convergent findings from spec-audit: devbox-mode.md length (PATCH-5), AC 3 vs Task 2 ordering (DISMISS). No cross-audit conflicts. Two-subagent pattern validated for narrow-surface documentation-heavy stories; runtime-heavy stories (none left in Epic 2) would still warrant three-subagent per iter-271 LESSON.
  - **iter-286 NOVEL LESSON #3 application confirmed:** PATCH-6 is the first in-Epic-2 application of the "adversarial-convergence-on-spec-endorsed-claim → empirical-verify-at-impl" META guard. The story file does NOT DEFER this; instead it PROMOTES the claim to an impl-time grep + Completion Notes record. This is a non-DEFER resolution path that the iter-286 NOVEL LESSON left open (the LESSON offered DEFER as the default; at iter-288 we demonstrate PROMOTE-TO-EMPIRICAL-VERIFY is equally valid for claims where the verification is trivially mechanical).
  - **Cumulative Story 2.14 lifecycle PATCH count (projected):** 6 (iter-288 pre-dev SM) + forecast 0-2 (post-dev SM per iter-270 LESSON) + 0-2 (CR per iter-279/286 precedent) = **~6-10 PATCH total**. Matches iter-286 NOVEL LESSON forecast for narrow-diff moderate-novelty stories.
  - **Budget consumed:** ~72K tokens (orient ~15K + 2-subagent parallel dispatch ~40K + triage synthesis + 3 verification bash greps ~5K + 7 PATCH edits + Status HTML comment + IP + RALPH.md + commit-prep ~12K). Well within ~117K execution budget; exit cleanly per Guardrail 12.
  - **PR:** #230 **Draft** — `draft=true, state=open, mergeable_state=clean` confirmed via REST fallback at iter-288 orient (GraphQL :443 i/o timeout cleared first-try via REST per iter-263 asymmetric-recovery LESSON + iter-287 precedent carry-forward); `statusCheckRollup: []` continues unchanged iter-272..288; clean push expected at step 5.

- [x] iter-287: **STORY 2.14 DRAFTING LANDING.** FR14n Story State `_(no story) → drafted` under § Story Lifecycle row "_(no story) → /bmad-create-story". Sprint-status row `2-14-legacy-devbox-branch-retention-policy: backlog → ready-for-dev` flipped this iter (preserving append-only audit-trail comment ledger per iter-269/iter-279 convention; body `last_updated` advanced to iter-287). Story file `_bmad-output/implementation-artifacts/2-14-legacy-devbox-branch-retention-policy.md` authored at ~270 lines.
  - **Story shape — first POLICY+BRANCH-CREATION story in Epic 2.** Stories 2.1-2.13 all touched runtime substrate code under `packages/devbox/`; Story 2.14 introduces NO runtime substrate code on `main` — substrate impact is one new invariant doc + one new git branch (`origin/legacy-devbox`) + AGENTS.md/README.md pointers + INVARIANTS.md anchor + manifest entry. Closest precedent: Story 1.16 (fork-extension config-pattern, also documentation-heavy with one template scaffold). The "running code" is the GIT BRANCH itself, hosted at `origin/legacy-devbox` — Ralph creates one-time in dev-story Task 1; Story 15b.1 retires at 1.0 cut ritual (`scripts/major-cut.sh` per epics.md:6293-6314).
  - **Branch-creation method baked into Task 1 spec — `git fetch <upstream>` not orphan-snapshot.** Two viable methods existed: (a) `git fetch https://github.com/tthew/cc-devbox.git main:legacy-devbox` brings ~3500 upstream commits into local pack-files (preserves history; supports `git bisect` on the canary per AC 4 triage path); (b) orphan branch with single-snapshot commit. Decision: method (a). Pack-file bloat is ~30MB one-time cost for the lifetime of the branch (ends at Story 15b.1 retirement); upstream commit history is load-bearing for AC 4's `git bisect` posture on the canary AND for any future archaeology against `legacy-devbox-final` tag post-retirement. Egress whitelist already covers `github.com` + `codeload.github.com` per Story 2.3 default-whitelist substrate; no whitelist amendment required.
  - **AC mapping captured (4 ACs verbatim from epics.md:1602-1627):**
    - AC 1 → Task 1 (branch creation + README banner) + Task 2 (invariant doc § Branch creation contract).
    - AC 2 → Task 2 (§ Cherry-pick workflow with `git format-patch | sed | git am` recipe + minimal-drift scope discipline).
    - AC 3 → Task 2 (§ Retirement gate; documents the procedure executed by Story 15b.1 — tag `legacy-devbox-final` + `git push origin --delete legacy-devbox` + RALPH.md decision entry per FR33 + AGENTS.md flip).
    - AC 4 → Task 2 (§ Triage path with canary-then-bisect recipe anchored at commit `5278738` Story 2.1 absorption) + Task 4 (README + AGENTS pointers).
  - **Substrate impact summary (lands in dev-story, NOT this iter):** create `docs/invariants/devbox-legacy-branch-retention.md`; edit `packages/keel-invariants/src/invariants.manifest.ts` (manifest 33 → 34); edit `INVARIANTS.md` (new H3 + anchor between Story 2.13 and Story 2.2 H3s); edit `packages/devbox/README.md` (new H2 after § Healthcheck); edit `AGENTS.md § Devbox iteration environment` (new H3 after § Healthcheck); on `legacy-devbox` branch — PREPEND retention banner to upstream README.md.
  - **Forecast band (iter-286 NOVEL LESSON #1 applied to narrow-diff documentation-heavy):** cumulative ~6-10 PATCH lifecycle (3-6 at pre-dev SM per iter-281 NOVEL LESSON wider-band-for-doc-heavy; 0-2 at post-dev SM per iter-270 NOVEL LESSON pre-dev-SM-absorbs-novel-surface; 0-2 at CR per iter-279 + iter-286 ZERO-PATCH precedent for narrow-diff stories). Twenty-fourth+ ATDD-skip + trace-WAIVED pairing forecast.
  - **Pre-dev SM trigger queued for iter-288:** `/bmad-create-story (args: "review")` per § Story Lifecycle row `drafted → /bmad-create-story (args: "review")`. Two-subagent pattern (AC-satisfaction audit + spec-fidelity audit) per iter-285 LESSON sufficient for narrow-surface; escalate to three-subagent if novel-runtime estimate surfaces.
  - **Budget consumed:** ~50K tokens (orient ~10K + epics.md/prd.md/architecture.md selective reads ~12K + Story 2.13 template study ~12K + INVARIANTS.md read ~6K + Story 2.14 file authoring ~10K). Well within ~117K execution budget; exit cleanly per Guardrail 12.
  - **PR:** #230 **Draft** — `state=open, draft=true, mergeable=clean` confirmed via REST fallback at iter-287 orient (GraphQL :443 i/o timeout cleared first-try via REST per iter-263 asymmetric-recovery LESSON); `statusCheckRollup: []` carries unchanged from iter-272..286; clean push expected at step 5.

- [x] iter-286: Story 2.13 DONE — `/bmad-code-review (args: "2")` CR closure ZERO-PATCH first-pass; 6 DEFERs added (D-8..D-13) to Story 2.17 SC-17 close-out; Epic 2 progress 12/17 → 13/17. See commit `d2c49d7`.
- [x] iter-285: Story 2.13 SM-VERIFIED — `/bmad-create-story (args: "review")` post-dev SM; `traced → sm-verified`; 2 NARROW PATCH (Dockerfile :347→:360 9-site citation-drift from iter-283 D-1 absorption + File List H2-count bookkeeping). See commit `bb3f0b3`.
- [x] iter-284: Story 2.13 TRACED — `/bmad-testarch-trace (args: "yolo")`; `in-dev → traced` WAIVED ground-(a)+(b)+(c) hybrid; 23rd-cumulative trace-WAIVED precedent. See commit `964f494`.
- [x] iter-283: Story 2.13 DEV-STORY LANDING (RECOVERY) — `/bmad-dev-story`; `atdd-scaffolded → in-dev`; killed-pre-commit recovery iter; all 5 Tasks verified + committed; INV-devbox-healthcheck registered (manifest 32→33); Task 5 absorbed D-1; DEFER queue 24. See commit `1fb2fde`.
- [x] iter-282: Story 2.13 ATDD-SKIP — `/bmad-testarch-atdd` SKIP-WITH-GROUNDS-(c)+(ii)+(iii); `validated → atdd-scaffolded`; 23rd-cumulative precedent; ZERO-PATCH iter. See commit `3f35dd6`.
- [x] iter-280..281: Story 2.13 DRAFTED + VALIDATED — `/bmad-create-story` + pre-dev SM review; `_(no story) → drafted → validated`; 6 PATCH + 3 DEFER at pre-dev. Commits `985aee0`/`6e0e810`/`9863358`.
- [x] iter-272..279: Story 2.12 PATCH-1..6 + iter-277 CR closure re-run #1 + iter-279 DONE ZERO-PATCH closure re-run #2. Commits `30b7d8d`/`1df64ab`/`0d83fae`/`820591a`/`43b4e4b`/`3eadcff`/`947cbce`/`ce3ffb4`.

_(iter-253..271 Story 2.10/2.11/2.12 closure + Story 2.12 lifecycle iters pruned per Guardrail 2. iter-285/286 deep-detail sections pruned at iter-289 per Guardrail 2 — full detail retained in commits + story file v1.3/v1.4 Change Log + `deferred-work.md § code review of story-2.13`.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **13/17 stories done** (2.1-2.13) + 4/17 backlog (2.14..2.17). Story 2.14 at `atdd-scaffolded` after iter-289 ATDD-SKIP (validated → atdd-scaffolded).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered through iter-289 (`pnpm keel-invariants:check` GREEN at 33 manifest entries; unchanged iter-286..289 — ATDD-skip iter touches no manifest).
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:120). Host-side shim count: **18** at iter-289 (unchanged — ATDD-skip iter is BMad-skill-only; zero substrate edits).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **13 done** (2.1-2.13 after iter-286 CR closure); **4 backlog/in-flight** (Story 2.14 ready-for-dev at iter-289; 2.15-2.17 backlog). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.14 — Legacy-devbox branch retention policy.
- **Story File:** `_bmad-output/implementation-artifacts/2-14-legacy-devbox-branch-retention-policy.md`
- **Story State:** `atdd-scaffolded` at iter-290 entry (iter-289 landed `validated → atdd-scaffolded` via `/bmad-testarch-atdd` SKIP-WITH-GROUNDS-(ii)+(iii); twenty-fourth cumulative ATDD-skip precedent; ZERO-PATCH iter). Next iter (iter-290): `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-14-legacy-devbox-branch-retention-policy.md")` per § Story Lifecycle row `atdd-scaffolded → /bmad-dev-story`. Tasks 1-4 decomposed in story file; Task 1 git-branch bootstrap novel surface may require partial-landing + follow-up iter per iter-268/iter-283 recovery precedent.
- **GitHub Issue:** Story 2.14 issue unknown; `RALPH_ISSUE_NUMBER` unset at iter-289. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` carried unchanged iter-272..289; `state=OPEN, draft=true, mergeable=MERGEABLE` confirmed via single `gh pr view 230` at iter-289 orient — no network flake this iter).

## Notes

- **iter-286 NOVEL LESSON (narrow-diff moderate-novelty cumulative PATCH band = ~6-10 lifecycle):** Story 2.13 cumulative PATCH count = 6 (iter-281 pre-dev SM) + 2 (iter-285 post-dev SM) + 0 (iter-286 CR) = **8 PATCH total across lifecycle**. Distribution confirms iter-270 NOVEL LESSON (pre-dev SM absorbs novel surface → post-dev gates clean-advance dominant) + iter-281 NOVEL LESSON (documentation-heavy stories' pre-dev SM band wider than runtime-code-heavy at same novelty). Forecast for Stories 2.14..2.17 narrow-diff moderate-novelty lifecycle = ~6-10 cumulative PATCH; novel-runtime story (2.12-class) = ~10-14 cumulative. Apply at Story 2.14 pre-dev SM forecasting (iter-288 forecast: 3-6 PATCH).
- **iter-286 NOVEL LESSON (ZERO-PATCH CR first-pass precedent extends iter-279 class):** iter-279 Story 2.12 was ZERO-PATCH at CR closure re-run #2 (second closure iter after 1 re-run). iter-286 Story 2.13 is ZERO-PATCH at CR FIRST-PASS (no prior fix iters). Precedent: narrow-diff moderate-novelty stories where pre-dev SM absorbs substantive findings can close CR in single iter without re-run. For Stories 2.14..2.17 narrow-diff class: budget **1 CR iter only**; do NOT pre-queue closure re-runs. Novel-runtime-class stories (hypothetical — none left in Epic 2) still need ~2 CR iters per iter-264 LESSON.
- **iter-286 NOVEL LESSON (adversarial convergence on spec-endorsed claim → DEFER-verify-later, not PATCH-blind):** Blind + Edge Case Hunters independently flagged the "probes run as USER dev" claim at 3 substrate sites; story Dev Notes:145 takes an OPPOSING spec position without empirical validation. Per iter-277 NOVEL LESSON #2 META guard (empirical-test override when Acceptance Auditor CONTRADICTS Blind+Edge convergent), the disciplined move when Ralph cannot empirically disconfirm from DinD backend B under cap-dropped semantics is DEFER-to-operator-workstation-verify, NOT blind-PATCH. Pinned D-8 for Story 2.17 close-out with explicit smoke protocol. This is a NEW class of META guard: "adversarial convergence on spec-endorsed-but-empirically-unvalidated claim → DEFER with operator-smoke protocol". Promoted to RALPH.md § Lessons.
- **iter-285 NOVEL LESSON (same-commit citation-drift from DEFER absorption):** a PATCH-like absorption that inserts lines (Task 5 D-1 inserted 13 lines at Dockerfile:318-330 in iter-283) can shift line numbers of subsequent anchors, invalidating citations authored in the SAME commit pre-absorption. In iter-283, the `USER dev` directive shifted :347→:360, but the Dev Notes + References + Completion Notes + invariant doc § Probe tooling + compose-file inline comment + manifest description were all authored as if the line were still :347. Six-site citation drift in a single commit, detected at iter-285 post-dev SM. **Guard for future DEFER-absorbing stories:** when a Task absorbs a DEFER by inserting lines into a source file, grep ALL story-file citations referencing lines in the same file AFTER the insertion point and resync them in the same commit. Applies most directly to Stories 2.14..2.17 if they absorb remaining Story 2.17 DEFERs that insert lines (e.g., D-2 SSHD_PID subshell scoping is an entrypoint.sh edit — any in-scope absorption would need entrypoint.sh citation-audit post-edit). Pinned in RALPH.md § Lessons.
- **iter-285 observation (two-subagent pattern held within band):** iter-235 LESSON two-subagent pattern (AC-satisfaction audit + implementation-fidelity audit) delivered 2 PATCH within the iter-270 drift-band re-baseline forecast (0-2 PATCH band). Ralph-synthesis triage matched both audits cleanly — no cross-audit conflicts; the Dockerfile line-number drift surfaced ONLY in the implementation-fidelity audit (AC audit read the ACs' substance, not prose citation fidelity). This validates the two-audit division: AC audit → "does the impl satisfy the story requirements"; fidelity audit → "does the Dev Agent Record accurately describe the impl". Keep two-subagent pattern as post-dev SM default for narrow-surface stories (2.14/2.16/2.17-class documentation-heavy); escalate to 3-subagent pattern if runtime-behaviour surface is large (2.15-class Claude settings.json hook lifecycle = potentially 3-subagent like 2.12).
- **iter-285 observation (invariant-doc edit requires manifest contentHash update):** editing a contentHash-tracked invariant doc MANDATES a same-commit manifest update. Workflow validated at iter-285: (a) edit doc → (b) `sha256sum doc` → (c) update manifest `contentHash` with new hex → (d) `pnpm --filter @keel/keel-invariants build` (tsc rebuild mandatory; iter-257 LESSON reaffirmed at iter-283 Completion Notes; applies equally at iter-285) → (e) `pnpm keel-invariants:check` + `check-all` → (f) GREEN before commit. This is the 2nd time the manifest contentHash has been refreshed within a story's lifecycle (first: iter-283 INV-devbox-healthcheck initial landing; now: iter-285 PATCH-1 refresh). Pattern stable.
- **iter-284 observation (trace WAIVED via structural inheritance):** Story 2.12 iter-269 trace artefacts served as the canonical template; ~85% reasoning-cost savings via mechanical adaptation to Story 2.13's 5-AC healthcheck-compose-probe class. Four artefacts landed: markdown + 3 JSONs. Zero code edits — trace is gate-only; manifest count unchanged at 33; sync-gate GREEN on first try (no rebuild needed — no `invariants.manifest.ts` edit). Structural pattern carry-forward to Stories 2.14..2.17 traces expected: continue template inheritance discipline + three-site lockstep checks + ground-(a)+(b)+(c) hybrid framing.
- **iter-283 observation (recovery iter pattern reused; iter-268 precedent):** The WIP in `git status` at iter-283 orient was produced by a killed-pre-commit prior iter-283 `/bmad-dev-story` invocation. Recovery was verification-and-commit (NOT re-invoke the skill). Verification steps: (a) story file's `File List` matches `git status` output exactly; (b) story file's claimed sha256 (`665174a3…d18623`) matches `sha256sum` of the actual doc; (c) contentHash in manifest matches doc sha256; (d) `pnpm --filter @keel/keel-invariants build` + sync-gate GREEN; (e) POSIX `dash -n` parse GREEN; (f) 3-site `api.github.com` lockstep + `^ListenAddress` empty smokes GREEN. Same precedent-match pattern as iter-268 Story 2.12 recovery (reflog `f631bf8` — "feat(story-2-12): iter-268 /bmad-dev-story landing (recovery)"). Commit framing preserves iter-283 number (not "iter-284 recovery") per iter-268 convention.
- **iter-283 observation (TodoWrite reminder hooks continue):** harness surfaced TodoWrite reminders twice during this recovery iter (once mid-orient after verification-branch decision, once during README diff review). Matches iter-282 observation: in narrow-surface iters (~30K) the todo list is a single in-progress entry; reminder is false-positive at this iter density. No pattern change — documented harness behaviour.
- **iter-257 LESSON REAFFIRMED (manifest rebuild mandatory; applies to recovery iters too):** `pnpm --filter @keel/keel-invariants build` MUST run before `pnpm keel-invariants:check` after any `invariants.manifest.ts` edit — including when verifying inherited WIP. Sync-gate consumes the compiled `dist/check.js`; stale dist shows `removed-from-docs-only INV-devbox-healthcheck` false drift. Recovery iters should treat the rebuild as load-bearing even when they did NOT author the manifest edit themselves. Story 2.13 v1.2 Completion Notes captures the same observation from the killed iter-283.
- **iter-281 NOVEL LESSON carry-forward (doc-heavy pre-dev SM band widening):** pinned in RALPH.md § Lessons; not exercised at iter-283 (recovery iter); will reactivate at iter-285 post-dev SM (assuming iter-284 `/bmad-testarch-trace` lands WAIVED per forecast).
- **iter-280/281 LESSON carry-forward (citation-audit false-positive mitigation):** applicable at iter-285 post-dev SM audit prompt.
- **iter-278 LESSON carry-forward (DEFER absorption discipline):** FIRED at iter-283 — Task 5 optional absorption of Story 2.12 iter-279 D-1 landed. Pattern validated again (Dockerfile pre-create of `/var/log/sshd.log`; rationale pinned inline; cumulative DEFER queue reduced 25 → 24).
- **iter-277 NOVEL LESSON carry-forward (bash subshell `||`-LHS errexit suppression):** does NOT apply — Story 2.13 healthcheck CMD runs under `/bin/sh` (dash) `&&`/`||` at runtime, not a bash subshell; stays codified for any bash-wrapped wrappers introduced downstream.
- **iter-276 NOVEL LESSON carry-forward (hazard-class discrimination under word-splitting):** stable at iter-283; Story 2.13 `>-` folded-scalar avoids the blank-line `\n` preservation hazard per iter-281 PATCH-3 story-level guard; dev-story implementation kept the two clause lines adjacent as instructed.
- **iter-273 NOVEL LESSON carry-forward (compose env-propagation chokepoints):** stable at iter-283 — Story 2.13 healthcheck reuses Story 2.12 PATCH-2 `KEEL_DEVBOX_SSH_RESOLVED` canonical stream via `${KEEL_DEVBOX_SSH:-false}` read inside the container; no new chokepoint.
- **iter-271 LESSON carry-forward (three-subagent pattern for post-dev SM + CR):** applicable at iter-285 post-dev SM + iter-286 CR. Not exercised at iter-283.
- **iter-270 NOVEL LESSON carry-forward (drift-band re-baseline):** applicable at iter-285 post-dev SM forecast.
- **iter-264 LESSON carry-forward:** CR re-run DEFER-vs-PATCH severity-class-first; Story 2.13 forecast closure budget = 1 closure re-run at worst.
- **iter-263 LESSON carry-forward:** SSH :22 / HTTPS :443 asymmetric recovery; iter-286 orient exercised REST fallback after GraphQL timeout at `gh pr view 230` — cleared first-try on REST probe. Step-5 push will exercise SSH :22.
- **Substrate-citation drift cumulative forecast for Stories 2.14..2.17:** ~4-10 cumulative substantive + ~25-40 cumulative mechanical drifts (reduced from prior forecast after Story 2.13 clean closure). SC-17 single reconciliation at Story 2.17 landing. Cumulative DEFER tally at iter-286 Story 2.13 CR closure = **30 DEFERs** pending Story 2.17 SC-17 (24 carry-forward from Story 2.12 final + 6 new iter-286 CR defers: D-8 UID-empirical, D-9 probe robustness, D-10 exit-2 edge, D-11 Dockerfile lockstep lint scope, D-12 README sudo disclaimer, D-13 start_period + AGENTS.md cross-ref).
