# Implementation Plan

## NOW

- [ ] **Run `/bmad-create-story`** for Story 2.5 (container hardening: non-root `dev` user + `cap_drop: [ALL]` preserving `cap_add: [NET_ADMIN, NET_RAW]` + `security_opt: [no-new-privileges:true]` + tmpfs + named volumes for OAuth) per § Story Lifecycle `_(no story)_` row + § Cross-epic transition within-epic auto-advance (Story 2.4 closed ZERO-PATCH iter-183; 13 Epic 2 backlog stories remaining 2.5..2.17). Fresh context; the skill auto-produces the story file + flips sprint-status row `2-5` `backlog → ready-for-dev`. FR14n Story State transitions `_(no story)_ → drafted` at that iteration's close. ~medium.

## QUEUE (Story 2.5 lifecycle → Epic 2 Story 2.6..2.17 → Epic 2 close-out)

- [ ] _(after 2.5 drafted)_ `/bmad-create-story (args: "review")` pre-dev SM validation per § Story Lifecycle `drafted → validated`.
- [ ] _(after 2.5 validated)_ `/bmad-testarch-atdd` red-phase scaffolds per § Story Lifecycle `validated → atdd-scaffolded` (skip rationale recorded inline if hybrid ground-(c)+(ii)+(iii) applies — Epic-2 precedent at Stories 2.1..2.4).
- [ ] _(after 2.5 atdd-scaffolded)_ `/bmad-dev-story` per § Story Lifecycle `atdd-scaffolded → in-dev`.
- [ ] _(after 2.5 in-dev)_ `/bmad-testarch-trace (args: "yolo")` per § Story Lifecycle `in-dev → traced`.
- [ ] _(after 2.5 traced)_ `/bmad-create-story (args: "review")` post-dev SM per § Story Lifecycle `traced → sm-verified`.
- [ ] _(after 2.5 sm-verified)_ `/bmad-code-review (args: "2")` CR opener per § Story Lifecycle `sm-verified → (fixes-pending | done)`.
- [ ] _(conditional, post-Story-2.5 done)_ `/bmad-create-story` for Story 2.6 (host-side `pnpm devbox:*` CLI surface) per § Story Lifecycle + § Cross-epic within-epic path. 12 backlog stories remaining in Epic 2 (2.6..2.17) after 2.5 done.
- [ ] _(conditional, post-Story-2.17 done)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge. Epic 2 completion.
- [ ] _(conditional, post-Epic-2 merged)_ § Cross-epic transition → Epic 3 Story 3.1 OR halt EPIC_DONE awaiting merge.

## BLOCKED

_(none — Story 2.4 iter-183 CR re-run closed ZERO-PATCH cleanly. All 5 PATCH items from iter-177 opener checked off; 13 DEFERs logged to `deferred-work.md § Deferred from: code review RE-RUN of story-2.4 (2026-04-22)`; 4 DISMISSes retained in story file § Review Findings → Re-run dismissal paragraph including the empirically-verified FALSE-POSITIVE bash `=~` multi-line regex claim. FR14n Story State `fixes-pending → done`; Status `fixes-pending → done`; sprint-status row `2-4`: `review → done`; Epic 2 counter 3/17 → 4/17 `done`.)_

## ATDD Red Phase

_(none — Story 2.4 ATDD-skipped at `validated → atdd-scaffolded` per 14th-cumulative-precedent iter-173; no tests-to-green tracking. With Story 2.4 closed, this section resets for Story 2.5 which will make its own ATDD decision at iter-185+ per § Story Lifecycle row `validated → atdd-scaffolded`.)_

## DONE (Story 2.4 lifecycle — keep current story chain, prune after Story 2.5 starts)

- [x] iter-183: **`/bmad-code-review (args: "2")` CR re-run closed ZERO-PATCH — Story 2.4 `fixes-pending → done`.** Three-layer Ralph-hosted adversarial fan-out against post-drain HEAD (Story 2.4-scoped diff `770e4e7^..84fff37`, 5 substrate files, 625 patch lines). **Acceptance Auditor: CR RE-RUN CLOSES ZERO-PATCH** — all 5 ACs SATISFIED byte-identical to iter-177 opener; all 5 AI-N drain commits PRESENT AND CLOSE opener findings (AI-1 cross-fs mv at whitelist.sh:300; AI-2 grep rc-capture at :493-502; AI-3 chmod fail-loud at :344+:360; AI-4 mktemp at :404+:489; AI-5 README exit-code table at :263-268); no regressions; no new HIGH findings outside opener's scope. **Blind Hunter + Edge Case Hunter triage: ZERO PATCH + 13 DEFER + 4 DISMISS.** All 13 DEFERs are defensive-hardening post-1.0 CLI refinement logged to `deferred-work.md § Deferred from: code review RE-RUN of story-2.4 (2026-04-22)`. **4 DISMISSes:** (1) Blind Hunter self-downgraded trap-shadowing; (2) Edge Case Hunter "cmd_sync lacks MUTATE_LOCK" architecture-sanctioned per SC-8; (3) Edge Case Hunter "DOMAIN_REGEX multi-line bypass via bash `=~`" **FALSE POSITIVE** empirically verified — bash POSIX ERE `$` = end-of-string; multi-line NO_MATCH; validate_sources reads per-line independently; (4) validate_sources docstring aggregation applies to exit-2 not exit-3. **Updates landed in single commit:** story file Status `fixes-pending → done` + 5 PATCH checkoffs (`[x]`) with commit-SHA annotations + new `#### Re-run (iter-183 CR)` subsection with 13 DEFER checkoffs + 4 DISMISS paragraph + Change Log v2.2 entry; deferred-work.md new section appended; sprint-status row `2-4: review → done` + matching `# last_updated: ... Story-2-4-done UTC` comment; IP advances NOW to `/bmad-create-story` for Story 2.5; RALPH.md iter-183 signpost appended. **§ Story Lifecycle Decision Matrix within-epic auto-advance:** iter-184 NOW = `/bmad-create-story` for Story 2.5 in fresh context; FR14n `Story State: done → _(no story)_` at this iteration's close + `_(no story)_ → drafted` at iter-184's close. PR #230 stays Draft (Epic 2 closes at Story 2.17). **6-iter Story 2.4 chain closure at forecast:** iter-175 trace + iter-176 SM + iter-177 CR opener + iter-178..182 5-PATCH drain + iter-183 CR re-run = exactly 6 iterations. Tighter than Story 2.3's 10-iter chain (user-facing-CLI class surgical-Edit drain vs infrastructure-security class multi-hunk algorithmic rewrites). Cumulative Epic-2 ZERO-PATCH CR-re-run closures: 4/4 (Story 2.1 iter-133 / 2.2 iter-154 / 2.3 iter-171 / 2.4 iter-183). **First iter-183-specific empirical-bash-validation closure** — first Story 2.4 lifecycle iter where an Edge Case Hunter HIGH finding was dismissed via iteration-env executable empirical test (bash `[[ =~ ]]` POSIX-ERE end-of-string semantics), rather than via spec-reading or architecture-sanction; adds live-bash smoke-testing to Ralph's orient-phase triage toolkit for regex-class findings.
- [x] iter-182: AI-5 README per-subcommand exit-code table completeness fix landed — CR drain 5/5 (final PATCH). Docs-only surgical `Edit` at `packages/devbox/README.md:263-268` — widened Exit codes column (51→77 chars) + populated full transitive exit-code surface. Commit `84fff37`.
- [x] iter-181: AI-4 PID-predictable tempfile fix landed — CR drain 4/5. Surgical 2-hunk `Edit` at `packages/devbox/scripts/whitelist.sh:301,385` — `${WHITELIST_LOCAL}.tmp.$$` → `mktemp "${WHITELIST_LOCAL}.XXXXXX.tmp"`. Commit `c0c795d`.
- [x] iter-180: AI-3 silent-`chmod 0644` fix landed — CR drain 3/5. Surgical 2-hunk `Edit` at `packages/devbox/scripts/whitelist.sh:242,258` — removed ` 2>/dev/null || true` suffix. Commit `2459f34`.
- [x] iter-179: AI-2 `grep -Fxv` rc-capture fix landed — CR drain 2/5. Surgical 1-hunk `Edit` at `packages/devbox/scripts/whitelist.sh:389-398` — rc-capture idiom replaces `|| true`. Commit `ef524ac`.
- [x] iter-178: AI-1 cross-filesystem `mv` fix landed — CR drain 1/5. Surgical 1-hunk `Edit` at `packages/devbox/scripts/whitelist.sh:198` — `mktemp /tmp/keel-whitelist-sync.XXXXXX` → `mktemp /run/keel-whitelist-sync.XXXXXX`. Commit `0bed9c3`.
- [x] iter-177: `/bmad-code-review (args: "2")` CR opener — FR14n Story State `sm-verified → fixes-pending`; 5 PATCH + 4 DEFER + ~30 DISMISS against cumulative Story 2.4 substrate diff.
- [x] iter-176: `/bmad-create-story (args: "review")` Story 2.4 post-dev SM verification — FR14n Story State `traced → sm-verified`; ZERO-PATCH + ZERO-DEFER + 1-DOCUMENTATION-DISMISS.
- [x] iter-175: `/bmad-testarch-trace (args: "yolo")` Story 2.4 — FR14n Story State `in-dev → traced`; Gate WAIVED (14th cumulative trace-WAIVED precedent).
- [x] iter-174: `/bmad-dev-story` Story 2.4 landed single-iteration; FR14n Story State `validated → in-dev`.
- [x] iter-173: `/bmad-create-story (args: "review")` pre-dev SM validation of Story 2.4; FR14n Story State `drafted → validated`.
- [x] iter-172: `/bmad-create-story` drafted Story 2.4; Status `backlog → ready-for-dev`.

## Context

- **Phase:** 4-implementation — Epic 2 at 4/17 stories `done` (2.1 + 2.2 + 2.3 + 2.4) in sprint-status enum + 13/17 `backlog` (2.5..2.17) after iter-183 CR re-run ZERO-PATCH closure flipped row `2-4: review → done`.
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.4 live smokes (Task 10.1–10.8 + 9.2) operator-workstation-deferred per Subtask 10.10; iter-177 CR opener + iter-183 CR re-run re-verified deferral acceptance via Acceptance Auditor ALL-5-ACs-SATISFIED verdict.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Story 2.4 required NO Dockerfile edits; Story 2.5 is the first Epic-2 story to touch `docker-compose.yml` (user:/cap_drop:/security_opt:/tmpfs:/volumes: hardening) + entrypoint.sh (non-root `dev` user).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; 2.1 + 2.2 + 2.3 + 2.4 done; 2.5..2.17 in `backlog`. Epic 2 closes at Story 2.17 (PR #230 transitions Draft→Open at that point).
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2; PR #230 stays Draft until Story 2.17 completion triggers the final-CI-gate + Draft→Open transition).
- **Story:** _(no story)_ — iter-184 NOW will create Story 2.5 via `/bmad-create-story`.
- **Story File:** n/a (Story 2.5 file not yet created; `/bmad-create-story` skill will produce `_bmad-output/implementation-artifacts/2-5-container-hardening-non-root-user-capabilities-tmpfs-noexec-named-volume.md` at iter-184).
- **Story State:** `_(no story)_` — at iter-184's close transitions to `drafted` upon `/bmad-create-story` completion.
- **GitHub Issue:** Story 2.4 → _(not yet created; RALPH_ISSUE_NUMBER unset through iter-183)_; Epic 2 → #10 (stays open; Ralph closes epic issues on EPIC_DONE halt only).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **iter-183 CR-re-run closure pattern (ZERO-PATCH + 13 DEFER + 4 DISMISS).** Epic-2 CR-re-run closures follow a consistent envelope: Story 2.1 iter-133 ZERO-AI + ~5 DEFER; Story 2.2 iter-154 ZERO-AI + ~3 DEFER; Story 2.3 iter-171 ZERO-PATCH + ~8 DEFER; Story 2.4 iter-183 ZERO-PATCH + 13 DEFER. Defer count trends with story complexity: infrastructure-security (2.3) > user-facing-CLI (2.4) > hybrid-configuration (2.2) > infrastructure-smoke (2.1) at the DEFER axis, but all four close ZERO-PATCH on re-run because the drain iter-178..iter-182 surgical Edits addressed the OPENER's in-scope PATCH set while Blind/Edge Case Hunter re-run passes re-surface only post-1.0 refinement-scope findings. Future-Ralph rule: budget CR-re-run iters at ~60K tokens for user-facing-CLI class + 3 parallel general-purpose subagents + triage + file updates + commit/push; reserve empirical-bash verification as orient-phase triage technique for regex/parser-class findings.
- **iter-183 empirical-bash-validation technique (new lesson).** Edge Case Hunter surfaced a DOMAIN_REGEX multi-line bypass HIGH finding claiming bash `[[ =~ ]]` matches `^...$` per-LINE (via REG_NEWLINE-like semantics). Rather than dismissing by spec-reading alone, iter-183 ran a live bash smoke-test: `bash -c 'd=$'\''foo.com\nbar_baz'\''; regex="^[a-zA-Z0-9]...$"; [[ $d =~ $regex ]] && echo MATCH || echo NO_MATCH'` → NO_MATCH. This empirically verifies bash POSIX ERE's default `$` = end-of-string semantics and DISMISSES the finding with high confidence. Future-Ralph rule: for regex/parser-class adversarial findings where the claim is testable in ~30 seconds, run the empirical smoke-test BEFORE writing triage prose. Cheap, defensible, and resolves the "bash vs Perl-class regex semantics" ambiguity that adversarial reviewers sometimes conflate.
- **Story 2.4 6-iter chain closure at forecast.** iter-175 trace WAIVED + iter-176 ZERO-PATCH SM + iter-177 5-PATCH opener + iter-178..iter-182 5-PATCH drain + iter-183 ZERO-PATCH CR re-run = 6 iterations exactly, matching iter-175 trace forecast envelope (`0 carve-out + backend-B live-smoke defer (+3) + ~360 LOC impl (+3.6) = ~6.6 ceiling`). Confirms the fix-chain equation `(carve-out × 3) + (live-smoke-defer × 3) + (impl-surface-LOC / 100)` as a reliable CR-opener + chain-length forecaster for Epic-2 class stories. Cumulative fix-chain: Story 2.1 = 16-iter (9-AI infrastructure-smoke) + Story 2.2 = 4-iter (2-PATCH hybrid-config) + Story 2.3 = 10-iter (9-PATCH infrastructure-security) + Story 2.4 = 6-iter (5-PATCH user-facing-CLI).
- **Story 2.5 scope.** Non-root `dev` user + `cap_drop: [ALL]` preserving `cap_add: [NET_ADMIN, NET_RAW]` for nftables management + `security_opt: [no-new-privileges:true]` + tmpfs for ephemeral state + named volumes for OAuth token persistence across devbox restarts. Touches `docker-compose.yml` (first Epic-2 story to do so in a structural way), `entrypoint.sh` (user-switching via su-exec / gosu), `Dockerfile` (may require USER directive refinement or dev-user creation at image layer), and potentially `whitelist.sh` / `reload-egress.sh` (signal handling + fd cleanup now matters more under non-root posture — see RALPH.md iter-180 cross-subcommand fail-loud lesson and iter-181 mktemp TOCTOU lesson). Story 2.5 is architecture-security class per Epic-2 taxonomy; expect CR-opener envelope 6-10 PATCH based on security-hardening surface breadth.
- **NFR2 authority unchanged.** Cold ≤ 300s / warm ≤ 30s remains authoritative on M4-Pro native; DinD runs indicative only. Story 2.5 hardening SHOULD NOT measurably change cold-start (dev-user creation is ~negligible; cap_drop is an enforcement-at-start switch, not a runtime op). Warm-start unaffected.
- **Substrate sanity check (post-iter-183):** Story 2.4 substrate stable. Files touched iter-174..iter-182 (per `git log` on Story 2.4 commits): `packages/devbox/scripts/whitelist.sh` (main impl + 4 drain fixes), `packages/devbox/scripts/start-egress.sh` (compose_whitelist extension), `packages/devbox/whitelist.default.txt` (header refresh), `packages/devbox/README.md` (subcommand table + exit-code fix), `packages/devbox/package.json` (pnpm script wiring), `AGENTS.md` (per-fork whitelist section), `.gitignore` (whitelist.local.txt entry), story doc + sprint-status + IP + RALPH.md + traceability artefacts. No invariant-manifest touch (SC-15 consumer-only); no Dockerfile / docker-compose.yml / entrypoint.sh touch (reserved for Story 2.5). `INV-devbox-egress-contract` contentHash `cd1b5da1…` unchanged through iter-183.
