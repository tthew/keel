# Implementation Plan

## NOW

- [ ] `Run /bmad-create-story` — picks Story 2.7 (`ralph-auto-start-tui-attach-detach-via-pnpm-ralph-build-pnpm-ralph-plan`) from sprint-status per § Story Lifecycle row `_(no story) → drafted` + § Cross-epic within-epic auto-advance (Story 2.6 `done` at iter-219, Epic 2 still `in-progress`). Fresh context window per one-skill-per-context rule.

## QUEUE (Story 2.7 lifecycle → Epic 2 Stories 2.8..2.17 → Epic 2 close-out)

- [ ] _(post-Story-2.7-drafted)_ Run /bmad-create-story (args: "review") — pre-dev SM validation per § Story Lifecycle row `drafted → validated`.
- [ ] _(post-Story-2.7-validated)_ Run /bmad-testarch-atdd OR skip-to-in-dev per ATDD-skip precedent (Story 2.6 iter-200 was 16th-cumulative FR14n skip).
- [ ] _(post-Story-2.7-atdd-scaffolded|skipped)_ Run /bmad-dev-story (args: "{story_file_path}") per § Story Lifecycle row `atdd-scaffolded → in-dev`.
- [ ] _(post-Story-2.7-in-dev)_ Run /bmad-testarch-trace (args: "yolo") per § Story Lifecycle row `in-dev → traced | trace-fixes-pending`.
- [ ] _(post-Story-2.7-traced)_ Run /bmad-create-story (args: "review") — post-dev SM verification per § Story Lifecycle row `traced → sm-verified | sm-fixes-pending`.
- [ ] _(post-Story-2.7-sm-verified)_ Run /bmad-code-review (args: "2") per § Story Lifecycle row `sm-verified → done | fixes-pending`.
- [ ] _(on-Story-2.7-fixes-pending)_ Drain PATCH action items one-per-iter per § Story Lifecycle row `fixes-pending`; re-run /bmad-code-review on QUEUE empty.
- [ ] _(post-Story-2.7-done; through Story 2.17-done)_ Iterate Story 2.8..2.17 through the full lifecycle — 10 remaining substrate stories spanning claude OAuth + gh OAuth + prereq-check + shared mode + port-publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-22 per iter-219 pre-flight — "no checks reported on the 'feat/epic-2-packaged-devbox' branch").

## BLOCKED

_(none)_

## ATDD Red Phase

_(none — Story 2.6 ATDD-skipped at iter-200 per § Story Lifecycle `validated → atdd-scaffolded` (skip branch). 16th-cumulative FR14n ATDD-skip precedent. iter-202 trace gate WAIVED confirms the ATDD-skip-trace-WAIVED pairing — SEVENTEENTH cumulative pairing across Epic 1 + 2.)_

## DONE (iter-219 CR closure ZERO-PATCH — Story 2.6 done; within-epic auto-advance queued)

- [x] iter-219: **CR CLOSURE ZERO-PATCH ACHIEVED — Story 2.6 `fixes-pending → done`.** `/bmad-code-review (args: "2")` 2nd closure re-run against cumulative Story 2.6 substrate diff (`65ef3a3..HEAD` / 14 packages/devbox/ scripts + README.md + root package.json / 1162 diff lines). Three-layer adversarial fan-out: **Acceptance Auditor ZERO-FINDING** (3rd consecutive pass: iter-204 / iter-216 / iter-219 — AC-to-code mapping clean across 9 ACs + 25 SCs; 13-verb CLI intact; uniform exit-code schema intact; NFR10 volume-preservation posture honored; monitor semantic pinned to FR1a JSONL per PRD `:494` + SC-15). **Edge Case Hunter 3 pre-existing env-check.sh parser edges → ALL DEFER** (AR-29 bash 3.x preflight / AR-30 `.envrc`-as-directory / AR-31 UTF-8 BOM strip; operator workarounds documented; iter-216 AR-22..AR-28 defensive-parser-edge DEFER posture precedent). **Blind Hunter 22 findings → 9 duplicates + 5 false-positives + 4 new DEFER (AR-32 README `pnpm devbox:shell -c` `--`-form / AR-33 clean.sh docstring "rejected" wording polish / AR-34 README raw `docker volume inspect` FR1-exception annotation / AR-35 `.envrc.example` ↔ `REQUIRED_VARS` drift-risk Story 2.17 scope) + ~4 dismissed-as-noise.** Final triage: 0 PATCH + 7 DEFER + ~11 DISMISS. Story file Status `in-progress → done` at `2-6-host-side-pnpm-devbox-cli-surface.md:3`. Sprint-status `2-6-host-side-pnpm-devbox-cli-surface: in-progress → done` + `last_updated: 2026-04-22 Story-2-6-done-CR-closure-ZERO-PATCH-iter-219 UTC`. Story file § Review Findings § Code Review Closure Re-Run #2 (iter-219, v1.16) appended. Change Log v1.16 appended. deferred-work.md § Deferred from: Story 2.6 CR closure iter-219 (2026-04-22) appended with 7 AR entries. § Cross-epic transition within-epic route: Epic 2 has open backlog (2.7..2.17) → next iter runs `/bmad-create-story` for Story 2.7 per § Story Lifecycle row `_(no story) → drafted`. **No halt required** — PR #230 stays Draft across Epic 2; EPIC_DONE halt fires at Story 2.17 Draft→Open transition. Epic 2 progresses 5 → **6/17 stories done**. Cumulative Story 2.6 CR cost: 11 PATCH opener + 2 PATCH closure re-run + 0 PATCH closure re-run #2 = 13 drains (upper-middle band of iter-204 forecast "8-12 PATCH opener + 0-3 PATCH closure re-run"). **LESSON — 2-round CR closure is the cost envelope for ≥10-scripts host-side-CLI-class stories.** Round 1 (opener) surfaces implementation PATCHes; round 2 (closure re-run) surfaces drain scope gaps; round 3+ surfaces only pre-existing parser edge cases + design-posture disagreements (all DEFER-appropriate). Carry-forward rule for Stories 2.7+ at ≥10-scripts class: budget 2 CR closure re-runs; 3rd re-run is noise unless new surface area lands between passes. **LESSON — Acceptance Auditor ZERO-FINDING streak through 3 CR passes is a strong signal that AC-to-code contract is load-bearing + well-drafted.** Story 2.6 spec had 9 ACs + 25 SCs (high density of pinned constraints); Auditor had rich material to cross-check against. Pattern: dense SC pinning at draft time → clean Auditor finding through multiple CR passes. Carry-forward for Stories 2.7+: invest SC-pin density at draft time; it amortises across all downstream CR passes.

## Context

- **Phase:** 4-implementation — Epic 2 at **6/17 stories done** (2.1 + 2.2 + 2.3 + 2.4 + 2.5 + 2.6) + 11/17 remaining (2.7..2.17 backlog).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.7+ live lifecycle smokes operator-workstation-deferred per Story 2.4 SC-17 + Story 2.5 iter-187 + Story 2.6 iter-201/204/216/219 precedent cluster.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Story 2.5 substrate live. Story 2.6 host-side CLI live (iter-201 first landing + iter-205..iter-218 drain + iter-219 ZERO-PATCH closure).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **6 done** (2.1-2.6); **11 backlog** (2.7..2.17). Epic 2 closes at Story 2.17 (PR #230 Draft→Open transition at that point).
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** _(no story — Story 2.6 closed iter-219; Story 2.7 to be created next iter via `/bmad-create-story` per § Cross-epic within-epic auto-advance route).
- **Story File:** n/a (next story to be drafted at iter-220).
- **Story State:** `_(no story)_` — next action per § Story Lifecycle Decision Matrix is `/bmad-create-story`.
- **GitHub Issue:** Story 2.7 TBD (RALPH_ISSUE_NUMBER unset in this iter env). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **CR closure round-budget rule (iter-219 LESSON):** 2 CR closure re-runs is the cost envelope for ≥10-scripts host-side-CLI-class stories (Story 2.6 actual: 11 PATCH opener + 2 PATCH closure re-run + 0 PATCH closure re-run #2 = 13 drains, upper-middle of iter-204 forecast band). Round 1 surfaces implementation PATCHes; round 2 surfaces drain scope gaps; round 3+ surfaces pre-existing parser edge cases + design-posture disagreements (all DEFER-appropriate). Carry-forward rule for Stories 2.7+ at ≥10-scripts class: budget 2 CR closure re-runs; DO NOT plan for a 3rd re-run unless new surface area lands between passes. Dense SC pinning at draft time amortises across all downstream CR passes (Story 2.6 spec had 9 ACs + 25 SCs → Acceptance Auditor ZERO-FINDING streak through 3 passes).
- **Within-epic auto-advance works cleanly for Story 2.6 → Story 2.7 transition (iter-219 CONFIRMATION):** Prior execution (Story 2.5 → Story 2.6 at iter-198) validated the "Story State `done` + Epic has backlog → `/bmad-create-story` fresh context" sequence. iter-219 re-confirms: no halt needed when sprint-status has backlog rows AND PR stays Draft across the epic. Carry-forward for Story 2.7..2.16 transitions (all within-epic) + Story 2.17 Epic-close transition (cross-epic, will need EPIC_DONE halt for PR Draft→Open).
- **Defer-with-rationale > PATCH-everything for pre-existing parser edges (iter-219 LESSON):** Edge Case Hunter at CR-closure round 3+ tends to surface pre-existing parser edge cases (not drain regressions). The workflow's "conservative = PATCH" rule can pull toward over-fitting; the precedent-following alternative is DEFER with well-documented rationale + operator workaround + Carry-to story pointer. iter-216 AR-22..AR-28 established this posture; iter-219 AR-29..AR-35 extended it. Heuristic: if the fix is 1-2 lines AND the trigger requires an unusual operator configuration AND the operator workaround is documented → DEFER with Carry-to pointer. If the fix is drain-introduced regression OR a load-bearing safety defect → PATCH.
- **CR-closure forecast reliability caveat (iter-216 LESSON, iter-219 REFINED):** The 3-story ZERO-PATCH closure precedent (2.3 iter-171 / 2.4 iter-183 / 2.5 iter-197) was distributional. Story 2.6 was the counter-example (11 PATCH opener + 2 PATCH closure re-run). iter-219 refines: once a story has drained ≥ 10 PATCH, the 2nd closure re-run is highly likely to return ZERO-PATCH (the drain-tightening has exhausted the Blind + Edge Case Hunter discovery surface). Round-budget rule (above) operationalises this. Stories 2.7+ with < 10 PATCH at opener likely hit the 3-story ZERO-PATCH precedent at round 2; stories with ≥ 10 PATCH at opener need the 2-round-closure budget.
- **AR-20 promotion precedent (iter-216 LESSON, closed iter-217):** Closure re-run CAN promote a previously-deferred item to PATCH if the blast-radius analysis shifts. iter-212 deferred clean.sh COMPOSE_PROJECT_NAME as "low-severity no-op"; iter-216 Edge Case Hunter weighted `--remove-orphans` collateral damage above that analysis; iter-217 drained it as AI-12. Promotion is legitimate triage, not triage-reversal; the audit trail updates the defer entry with `PROMOTED TO AI-N iter-M` suffix so the chain closes cleanly. Carry-forward rule applied successfully in iter-219 triage: AR-29..AR-35 kept DEFER (no blast-radius shift vs iter-201 landing); only re-promote to PATCH if downstream-story context (e.g., Story 2.10 prerequisite-check absorbing AR-29 naturally) makes it the efficient path.
- **Counter-file stub pattern needs empty-string guard (iter-215 LESSON):** When writing a state-machine docker stub that progresses through values via a counter file, `: > counter` truncates to empty (not "0"); `cat ""` then yields empty string; `case "" in 0)` does NOT match (matches `*)` instead) → stub returns the default-arm value on first call, not the 0-indexed first value. Fix: `count=$(cat counter 2>/dev/null || true); count=${count:-0}` + initial `rm -f counter` (no file = default 0). Carry-forward for any future stub-based smoke that uses persistent counter state across invocations.
- **Soft-gate poll + fall-through pattern (iter-215 LESSON):** restart.sh's AI-11 fix uses a SOFT gate — on timeout, log the timeout AND proceed to start.sh regardless. This preserves forward progress under unreachable state (start.sh has its own 120s healthcheck bound), vs a hard-abort which would fail the whole restart. General rule for "wait-for-X-before-Y" gates in host-side scripts: if Y has its own downstream validation, the upstream gate can be soft. Applies to potential future poll-gates in Stories 2.7+ (Ralph auto-start readiness, claude OAuth handshake, gh auth status).
- **TTY-detect at docker-exec shim (iter-214 LESSON):** The `if [[ -t 0 ]]; then tty_flag="-it"; else tty_flag="-i"; fi` gate generalises — any host-side shim that invokes `docker exec` must TTY-detect when it wants to remain scriptable (CI / hooks / `sh -c` / `ssh host …`). Carry-forward rule for Stories 2.7+ (Ralph TUI wrapper / claude wrapper / gh wrapper / ssh wrapper): if the shim is expected to be callable from non-interactive contexts, apply the same pattern. Interactive-only shims (shell.sh, attach.sh) can keep hardcoded `-it` — interactive is the AC. **`script -qc '…' /dev/null` (util-linux) is the iteration-env-safe pty allocator** for smoking the TTY leg.
- **Regex-guarded progressive strip (iter-218 LESSON):** Three-line idiom for "strip keyword + any following whitespace" — `=~ ^keyword[[:space:]]` matches-or-bypass, `#keyword` strips literal token, `#"${…%%[![:space:]]*}"` progressively strips every whitespace byte. Works under any common locale (C, en_US, UTF-8); no external tools; `bash -n` + `shellcheck`-clean. Carry-forward for any future shell-parse work that needs "strip leading keyword + variable whitespace" — whitelist.sh rule-file value-side parsing (future hardening), start.sh `STATE_RE` parsing if it adds `Status: <state>` handling, generic CLI-option parsing where `--flag<WS>value` needs tolerant handling.
- **Cross-source semantic-drift audit rule (iter-199 LESSON, VALIDATED across lifecycle gates 201/202/203/204/216/219):** SC-15 pinned PRD `:494` as authoritative for `pnpm devbox:monitor`. Pattern held clean through 3 CR passes (iter-204 opener + iter-216 closure + iter-219 closure #2 — Acceptance Auditor confirmed AC 7 monitor-semantic satisfied at all three). Carry-forward rule for Stories 2.7+ remains live.
- **`<verb>-host.sh` host-side shim naming convention (iter-199 EXTENDED, iter-219 3-PASS-CR-VERIFIED):** Story 2.6 shipped `monitor-host.sh` + `whitelist-host.sh` as canonical host-side shim naming. All 3 CR passes (iter-204/216/219) validated the shim naming pattern. Stories 2.7 (Ralph TUI wrapper), 2.8 (claude wrapper), 2.9 (gh wrapper), 2.12 (ssh wrapper) candidates for the same pattern.
