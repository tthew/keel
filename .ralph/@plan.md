# Implementation Plan

## NOW

- [ ] Run `/bmad-create-story (args: "review")` (fresh context) per § Story Lifecycle Decision Matrix row `drafted → validated | (fixes-pending)`. Story 2.8 landed `drafted` at iter-227 (`_bmad-output/implementation-artifacts/2-8-claude-code-oauth-via-pnpm-claude.md`; 17 SCs; 5 ACs; 6 tasks; sprint-status `backlog → ready-for-dev`). Pre-dev SM adversarial validation will surface PATCH/DEFER/DISMISS findings against the 17 SCs + 5 ACs + 6 tasks. Forecast per Story 2.7 iter-221 precedent: 3–5 PATCHes expected; ZERO-PATCH is a degenerate case suggesting SM under-engagement (the whole pattern hinges on SM density upstream of one-pass ZERO-PATCH CR closure at the downstream gate). After SM review, Story State `drafted → validated`, sprint-status UNCHANGED (pre-dev SM gate does not flip sprint-status per iter-156 + iter-221 precedent).

## QUEUE (Story 2.8 lifecycle → Story 2.9..2.17 → Epic 2 close-out)
- [ ] _(post-Story-2.8-validated)_ `/bmad-testarch-atdd` per § Story Lifecycle row `validated → atdd-scaffolded`. FR14n matrix row 3 skip forecast (eighteenth cumulative precedent); new ground-(c) variant "external-service-owns-behavior-under-test" documented in story file § Testing Standards (Anthropic CLI + OAuth endpoint own AC 1/2/4 behavior; Story 2.5 named-volume + Docker own AC 3/5). Sprint-status UNCHANGED at skip-marker endpoint.
- [ ] _(post-Story-2.8-atdd-scaffolded)_ `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-8-claude-code-oauth-via-pnpm-claude.md")` per § Story Lifecycle row `atdd-scaffolded → in-dev`. Task 1 authors `packages/devbox/scripts/claude-host.sh` verbatim from § Shim structure template; Task 2 wires `"claude"` into root package.json after `ralph:plan`/before `prepare`; Task 3 appends README H2 sibling after `## Ralph loop (Story 2.7)` at line 505; Task 4 appends AGENTS.md H3 sibling after `### Ralph loop (Story 2.7)` at line 114; Task 5 iteration-env-safe stub-docker smokes (4 assertions: syntax parse + pnpm wiring + info-fail exit 8 + not-running exit 9 + running exec line + args passthrough + no-TTY-gate grep). Story State `atdd-scaffolded → in-dev`; sprint-status `ready-for-dev → in-progress → review`.
- [ ] _(post-Story-2.8-in-dev)_ `/bmad-testarch-trace (args: "yolo")` per § Story Lifecycle row `in-dev → traced`. WAIVED forecast per § Testing Standards three-ground conjunction (substrate-smoked envelope + no runner + external-service ground-(c) variant). 4 artefacts per Story 2.7 iter-224 template: `2-8-coverage-matrix.json` + `2-8-e2e-trace-summary.json` + `2-8-gate-decision.json` + `2-8-claude-code-oauth-via-pnpm-claude.md` traceability report. Story State `in-dev → traced`; sprint-status UNCHANGED.
- [ ] _(post-Story-2.8-traced)_ `/bmad-create-story (args: "review")` per § Story Lifecycle row `traced → sm-verified` — post-dev SM requirements-satisfaction verification. Story 2.7 iter-225 pattern: ZERO-PATCH if dense pre-dev SM PATCHes already tightened spec to zero-latitude-for-drift + dev-story landed verbatim. Forecast: ZERO unmet-AC findings (first-data-point for single-script Epic-2-auth-class story). Story State `traced → sm-verified`; sprint-status UNCHANGED.
- [ ] _(post-Story-2.8-sm-verified)_ `/bmad-code-review (args: "2")` per § Story Lifecycle row `sm-verified → done | fixes-pending`. Forecast: 1–3 PATCH opener + 0 closure re-run; candidate for SECOND one-pass ZERO-PATCH precedent given narrower novel surface than Story 2.7 (1 script vs 2) + 100% composition on Story 2.6 + 2.7 patterns + upstream/substrate-owned AC behaviors. Story State `sm-verified → done` (no findings) OR `fixes-pending` (if CR surfaces any PATCH). Sprint-status `review → done` at closure.
- [ ] _(post-Story-2.8-done; through Story 2.17-done)_ Iterate Stories 2.9..2.17 through full lifecycle — 9 remaining substrate stories spanning gh OAuth + prereq-check + shared mode + port-publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-22 per iter-219 pre-flight — "no checks reported on the 'feat/epic-2-packaged-devbox' branch").

## BLOCKED

_(none)_

## ATDD Red Phase

_(none — Story 2.7 closed ZERO-PATCH one-pass at iter-226; Story 2.8 drafted at iter-227 without a runner. ATDD skip will be applied at Story 2.8 `validated → atdd-scaffolded` gate per FR14n matrix row 3 eighteenth cumulative precedent.)_

## DONE (iter-227 Story 2.8 DRAFTED — `_(no story) → drafted`, Change Log v1.0)

- [x] iter-227: **STORY 2.8 DRAFTED — Story State `_(no story) → drafted`; sprint-status `backlog → ready-for-dev`; Status field `ready-for-dev`.** `/bmad-create-story` ran fresh-context per one-skill-per-context rule. Auto-discovered Story 2.8 as first backlog row in sprint-status (`2-8-claude-code-oauth-via-pnpm-claude`). Exhaustive artefact analysis: epics.md:1417-1447 (Story 2.8 AC verbatim + Epic 2 preamble :1142-1165); prd.md:929 (FR3 verbatim) + :1082 (NFR10 verbatim); architecture.md:74-79 (host surface contract + one-time auth prerequisites) + :489 (pnpm claude CLI table row) + :506 (Claude auth persistence contract); Dockerfile:111-120 (`@anthropic-ai/claude-code@2.1.116` install) + :246-252 (`/home/dev/.claude/` placeholder create) + :255-270 (non-root dev user UID 1000); docker-compose.yml:86-92 + :170-171 (named volume `keel_home_dev` at `/home/dev` — subsumes `/home/dev/.claude/` persistence); shell.sh + attach.sh Story 2.6 interactive-exec + fail-closed-exit-9 precedents; Story 2.7 full lifecycle (iter-220..226) for CR-ZERO-PATCH + stub-docker-harness + SC-17 read-only patterns + ATDD-skip ground-(c) variant scaffolding. **Story file artefacts:** 5 ACs verbatim from epics; 6 tasks (claude-host.sh + package.json + README H2 + AGENTS.md H3 + smoke tests + housekeeping); 17 SCs (SC-1 composability + SC-2 upstream-CLI-owns-auth + SC-3 file placement + SC-4 no-auto-start + SC-5 exit-code-schema + SC-6 no-TTY-gate + SC-7 compose prereqs + SC-8 unset-COMPOSE + SC-9 args passthrough + SC-10 no-signal-trap + SC-11 container-name + SC-12 no-compose-edits + SC-13 Dockerfile-placeholder + SC-14 lib-deferral + SC-15 no-token-inspection + SC-16 no-help + SC-17 read-only-prior-sections). Shim structure template: 41-line bash with `unset COMPOSE_PROJECT_NAME` + dual pre-flight (docker info → exit 8; container-state → exit 9 no-auto-start) + `exec docker exec -it --user dev -w /workspace ${CONTAINER_NAME} claude "$@"` (args passthrough). **Draft density target (17 SCs) matches Story 2.7 post-PATCH 17-SC density** — per Story 2.7 iter-226 LESSON, this is the upstream investment for one-pass ZERO-PATCH CR closure. CR forecast: 1–3 PATCH opener + 0 closure re-run; candidate for SECOND one-pass ZERO-PATCH precedent given narrower novel surface than Story 2.7 (1 script vs 2; 100% composition pattern). **New FR14n matrix row 3 ground-(c) variant introduced** — "external-service-owns-behavior-under-test" (upstream CLI + external endpoint) extending Story 2.7's "downstream-epic-owns-behavior-under-test" (Epic-3-scope) variant. Applies to Story 2.8 (Claude) + Story 2.9 (gh) auth-flow class. Sprint-status flipped `backlog → ready-for-dev` with `last_updated: 2026-04-22 Story-2-8-ready-for-dev-iter-227 UTC`. Story 2.8 **SC carry-forward to Story 2.9:** identical pattern (host-side shim + pnpm-verb wiring + README H2 + AGENTS.md H3 + external-service-owns-auth carve-out) — Story 2.9 drafting will be near-mechanical adaptation of Story 2.8's template. Budget consumed iter-227: ~70K (orient ~15K; epics + prd + architecture + previous-story + Dockerfile + compose + shell/attach.sh reads ~25K; story file authoring ~25K; sprint-status edits + IP update + commit ~5K remaining).

- [x] iter-226: **STORY 2.7 CR CLOSURE ZERO-PATCH ONE-PASS — Story State `sm-verified → done`; sprint-status `review → done`; Status field `review → done`.** `/bmad-code-review (args: "2")` ran fresh-context per one-skill-per-context rule. 3-layer adversarial triage via 3 parallel Sonnet subagents: **Blind Hunter 17 findings + Edge Case Hunter 20 findings + Acceptance Auditor 0 findings** = 37 raw → deduplicated + classified → **0 PATCH / 0 DECISION / 15 DEFER / 22 DISMISS**. Acceptance Auditor verified all 17 SCs respected (SC-2 + SC-6 + SC-9 + SC-10 + SC-11 + SC-12 + SC-14 + SC-15 + SC-16 + SC-17 with per-SC evidence from grep + line-number inspection). 15 DEFER items registered as AR-36 through AR-50 in `_bmad-output/implementation-artifacts/deferred-work.md § Deferred from: Story 2.7 CR iter-226`, each with defer reason + carry-to (primary destinations: `_lib.sh` refactor at Story 2.6 AR-19 substrate-wide threshold; Epic 3 mode-signal consumer integration AR-36; Story 2.10 prerequisite-check AR-41; Story 2.13 healthcheck AR-44; future README/AGENTS.md polish passes AR-39/40/50). Story file § Review Findings subsection written with all 15 bullets marked `[x] [Review][Defer]`; Change Log v1.6 landed at top; Status field `review → done`. Sprint-status flipped `review → done` with `last_updated: 2026-04-22 Story-2-7-done-CR-closure-ZERO-PATCH-one-pass-iter-226 UTC`. **SECOND cumulative CR-gate ZERO-PATCH precedent (Stories 2.6 + 2.7); FIRST one-pass ZERO-PATCH precedent — Story 2.6 required 3 CR passes + 13 AI drains to reach ZERO-PATCH; Story 2.7 achieved it in a single pass.** **NEW LESSON — dense pre-dev SM PATCHes (iter-221: 5 PATCHes) amortise through THREE downstream gates: post-dev SM (iter-225 ZERO-PATCH) + CR (iter-226 ZERO-PATCH one-pass) + (trace WAIVED iter-224).** Spec-template verbatim implementation + pre-dev-SM zero-drift-latitude is the mechanism. Carry-forward for Stories 2.8+: monitor pre-dev SM PATCH density as a leading indicator of CR outcome.

## Context

- **Phase:** 4-implementation — Epic 2 at **7/17 stories done** (2.1 + 2.2 + 2.3 + 2.4 + 2.5 + 2.6 + 2.7 as of iter-226 ZERO-PATCH one-pass CR closure) + 9/17 backlog (2.9..2.17) + 1/17 ready-for-dev (2.8 iter-227).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.8 AC 1-5 live OAuth flow smokes operator-workstation-deferred per Story 2.4 SC-17 + Story 2.5 iter-187 + Story 2.6 iter-201/204/216/219 + Story 2.7 iter-223/225/226 precedent cluster.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` baked at Dockerfile:119). Story 2.5 substrate live. Story 2.6 host-side CLI live (13 verbs). Story 2.7 ralph-*-host.sh shims live (2 verbs; iter-223 landing). Story 2.8 claude-host.sh queued (iter-227 drafted → iter-228+ implementation).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **7 done** (2.1-2.7); 1 ready-for-dev (2.8); 9 backlog (2.9..2.17). Epic 2 closes at Story 2.17 (PR #230 Draft→Open transition at that point).
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.8 — `claude-code-oauth-via-pnpm-claude` (drafted iter-227; 5 ACs + 6 tasks + 17 SCs).
- **Story File:** `_bmad-output/implementation-artifacts/2-8-claude-code-oauth-via-pnpm-claude.md`.
- **Story State:** `drafted` — next iter action per § Story Lifecycle Decision Matrix is `/bmad-create-story (args: "review")` (fresh context; pre-dev SM adversarial validation against 17 SCs + 5 ACs + 6 tasks).
- **GitHub Issue:** Story 2.8 TBD (GH issue wire may/may-not exist — `RALPH_ISSUE_NUMBER` env inspection at next iter start will resolve; if unset, skip `Refs #<story-issue>` in commits per § Issue Tracking block). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **iter-227 draft-density target holds (17 SCs match Story 2.7 post-PATCH density):** Per Story 2.7 iter-226 LESSON, dense SC pinning at draft time amortises through all three downstream gates (post-dev SM + CR + trace-WAIVED artefact density). Story 2.8 ships 17 SCs at draft time — same density as Story 2.7 post-PATCH endpoint. Pre-dev SM review at next iter expected to generate 3+ PATCHes to maintain the upstream-investment pattern that drives one-pass ZERO-PATCH CR closure. Carry-forward: if Story 2.8 pre-dev SM produces <3 PATCHes, it signals SM under-engagement — CR at iter-228+n may regress to Story 2.6 multi-pass pattern rather than Story 2.7 one-pass outcome.
- **iter-227 new FR14n matrix row 3 ground-(c) variant "external-service-owns-behavior-under-test":** Extends Story 2.7's "downstream-epic-owns-behavior-under-test" (Epic-3-scope) with a sibling variant for upstream-CLI + external-endpoint-scope auth-flow class. Applies to Story 2.8 (Claude; Anthropic CLI + OAuth endpoint) + Story 2.9 (gh; GitHub CLI + OAuth endpoint). Cumulative FR14n ground-(c) variant stack: (c-1) substrate-artefact-hash-sync (Story 1.8/1.9); (c-2) downstream-story-covers-integration (Story 1.12); (c-3) spec-declared-CR-substitution (Story 1.9); (c-4) downstream-epic-owns-behavior-under-test (Story 2.7); (c-5) external-service-owns-behavior-under-test (Story 2.8 NEW). Ground-(c) stack grows as novel story classes emerge; Story 2.10 prereq-check + Story 2.13 healthcheck are candidates for (c-6) "host-interactive-owns-behavior-under-test" if they surface a new variant.
- **iter-227 ralph-host-shim count extension (16 host-side shims total at Story 2.8 landing):** 13 Story 2.6 verbs (attach + build + clean + env-check + logs + monitor-host + rebuild + restart + shell + start + status + stop + whitelist-host) + 2 Story 2.7 ralph wrappers (ralph-build-host + ralph-plan-host) + 1 Story 2.8 claude-host = 16. `_lib.sh` refactor threshold (Story 2.6 AR-19) still not tripped — Stories 2.9 + 2.10 + 2.13 are candidates to push toward substrate-wide threshold. Story 2.8 deliberately does NOT extract (SC-14 carries AR-19 deferral).
- **iter-227 compose edits avoided (SC-12 carries Story 2.7 SC-16 posture):** Story 2.5's `/home/dev` named-volume mount (compose lines 90-92 + 170-171) already subsumes Story 2.8's `/home/dev/.claude/` persistence requirement. No compose edits needed. The stale `TODO(Story 2.8 / 2.9)` comment at compose:156 is DEFER'd to Story 2.17 Epic 2 close-out or standalone polish pass (not Story 2.8 scope per SC-17 read-only spirit). Carry-forward: Story 2.9 will mirror this posture — `/home/dev/.config/gh/` is also under the named-volume mount.
- **iter-226 CR-ZERO-PATCH-one-pass extension pattern carries to Story 2.8 draft (iter-227 APPLICATION):** Story 2.7's iter-221 pre-dev SM 5-PATCH density → iter-223 verbatim implementation → iter-225 post-dev SM ZERO-PATCH → iter-226 CR ZERO-PATCH one-pass. Story 2.8 draft at iter-227 ships 17 SCs matching Story 2.7 post-PATCH density; pre-dev SM review at iter-228 is the critical upstream for maintaining the pattern. Per Story 2.7 iter-226 LESSON: dense pre-dev SM PATCHes are the mechanism; SM under-engagement breaks the pattern.
- **Within-epic auto-advance works cleanly for Story 2.7 → 2.8 transition (iter-227 CONFIRMATION):** Prior execution validates "Story State `done` + Epic has backlog → `/bmad-create-story` fresh context" sequence. Third cumulative within-epic auto-advance precedent (1.6 → 1.7; 2.5 → 2.6; 2.6 → 2.7; 2.7 → 2.8). Carry-forward for Story 2.8 → 2.9 transition + subsequent stories.
- **iter-223 stub-harness trap caveat still applies (iter-227 CARRY-FORWARD):** Story 2.7 iter-223 LESSON: `trap 'rm -rf <dir>' EXIT` in setup shell fires when THAT shell exits, not when subsequent Bash-call shells finish. Story 2.8 Task 5 smoke-3 codifies explicit `rm -rf` in final assertion call; no EXIT-trap-at-setup. Applies to all future stub-harness smokes.
- **Defer-with-rationale > PATCH-everything for pre-existing parser edges (iter-219 CARRY-FORWARD):** Edge Case Hunter at CR-closure rounds tends to surface pre-existing parser edge cases. Heuristic: if fix is 1-2 lines AND trigger requires unusual operator config AND operator workaround is documented → DEFER with Carry-to pointer. If fix is drain-introduced regression OR load-bearing safety defect → PATCH. Story 2.7 iter-226 applied this to all 15 findings → 0 PATCH / 15 DEFER.
- **TTY-detect at docker-exec shim (iter-214 LESSON, Story 2.8 NON-APPLICATION CONFIRMED iter-227):** Story 2.8 does NOT apply the TTY-detect gate to its `docker exec` call because the OAuth flow IS operator-interactive (SC-6 explicit). Story 2.8's wrapper is operator-interactive by contract — same posture as Story 2.7's `docker attach` (SC-8). TTY-detect pattern applies ONLY to `docker exec` verbs that must remain scriptable (shell.sh with `-c` args, which Story 2.8 does NOT adopt — no trailing scriptable-branch).
- **Cross-source semantic-drift audit rule (iter-199 LESSON, VALIDATED across Story 2.1..2.7 lifecycle gates):** Story 2.8 SC-15 pins "wrapper does NOT inspect or parse token file" — similar posture to Story 2.7 SC-15 ("wrapper does NOT read PROMPT file"). Carry-forward: audit at implementation time (iter-228+n) via grep — `grep -c '/home/dev/.claude\|\.credentials\|token' packages/devbox/scripts/claude-host.sh` should return 1 hit (the log line referencing the directory for operator comprehension, not any inspection/parsing op).
- **`<verb>-host.sh` host-side shim naming convention (iter-199 EXTENDED, iter-219 3-PASS-CR-VERIFIED, iter-223 LANDING CONFIRMATION, iter-227 EXTENSION):** Story 2.8 ships `claude-host.sh` under `packages/devbox/scripts/` at iter-228+n, extending the pattern to 16 host-side shims total (13 Story 2.6 + 2 Story 2.7 + 1 Story 2.8). Stories 2.9/2.12 remain candidates for the same pattern.
