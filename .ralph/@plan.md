# Implementation Plan

## NOW

- [ ] **CR fix #1/6 [CRITICAL]** — Fix `ANCHOR_REGEX` at `packages/keel-invariants/src/sync-gate.ts:24`: replace `/^-\s+\*\*\`([A-Z][A-Z0-9-]+)\`\*\*/gm` with `/^-\s+\*\*\`(INV-[a-z0-9]+(?:-[a-z0-9]+)+)\`\*\*/gm` (matches the manifest id-schema verbatim; lowercase after `INV-` prefix). Also fix spec echo at `_bmad-output/implementation-artifacts/1-9-…md:52` carrying the same uppercase-class typo. Add symmetric `added-to-source-only` emitter to the `invariants` loop in `sync-gate.ts:58-87` so a manifest entry without a matching anchor triggers that drift kind (closes AC 2's anchor-side realisation currently missing). Extend Task 5 smoke to docs-side induction (append `- **\`INV-fake-orphan\`**` line to `INVARIANTS.md` → exit 1 + `removed-from-docs-only`; revert). Re-run 6 quality gates. ~medium

## QUEUE (Story 1.9 — CR-iter-7 fix pass; 6 patches)

- [ ] **CR fix #2/6 [HIGH]** — AC 6 spec stream-channel correction: `_bmad-output/implementation-artifacts/1-9-…md:41` currently says "structured JSON report on stdout" / "structured JSON report on stdout"; impl at `packages/keel-invariants/src/check.ts:9` writes `process.stderr.write`, and Dev Agent Record + File List + trace md + coverage-matrix.json all say "stderr" (8 occurrences). Fix spec line 41 `stdout → stderr` (two occurrences in that one prose bullet) to align spec with reality — minimal-edit over the alternative of flipping impl + 8 trace citations.
- [ ] **CR fix #3/6 [HIGH]** — "7 distinct source files" count correction in § Performance posture of story spec `1-9-…md:128` + trace md "all 7 distinct `sourcePath` files exist on baseline" (enumerates 8 paths in the same sentence) + coverage-matrix.json at `Baseline substrate = 7 distinct source files (10 invariants dedupe to 7)` + any RALPH.md iter-1/iter-4 narrative mentioning "7". Actual count: **8 distinct `sourcePath` files** across 10 manifest entries (Task 2 added `docs/invariants/ralph-execute.md` as the 8th). The "7" figure predated Task-2's manifest addition and never refreshed.
- [ ] **CR fix #4/6 [HIGH]** — "two runtime smoke tests (clean + drift + performance path)" → "three" in story spec `1-9-…md:118` § Testing Standards rationale (b) + trace md + coverage-matrix.json verbatim quotes. Parenthetical enumerates three; cardinality word says two. RALPH.md iter-6 pre-flagged this as expected CR finding.
- [ ] **CR fix #5/6 [MEDIUM]** — Off-by-one line citations in trace bundle. `1-9-coverage-matrix.json` + `1-9-e2e-trace-summary.json` + trace md cite story lines 94, 95, 81, 117, 137, 140 but actual lines are 95, 96, 82, 118, 138, 141 respectively (consistently +1). Grep all `story file line NNN` / `line:NNN` citations across all four trace files and re-anchor.
- [ ] **CR fix #6/6 [MEDIUM]** — Dev Agent Record AC-matrix entry for AC 2 at story `1-9-…md:138-139` currently reads "AC 2 (addition drift) — ✅ schema-level (manifest-side unique-id refine + anchor-walker's manifest-ID-membership check)". The id-uniqueness refine catches duplicate manifest IDs — NOT addition drift. Addition drift's anchor-side realisation (per § Scope Carve-Out) is the `added-to-source-only` emitter added in fix #1. Update entry to: "AC 2 (addition drift) — ✅ anchor-side-only realisation (per carve-out) — emits `added-to-source-only` when a manifest `entry.id` is absent from the anchor set; exercised end-to-end in Task 5 drift smoke (remove a manifest entry → expect `added-to-source-only`)".
- [ ] After QUEUE empties, re-run `/bmad-code-review (args: "2")` adversarial triage against post-fix diff. Expected outcome per matrix row 10: zero findings → `fixes-pending → sm-verified → done` (single-transition edge per Story 1.7 iter-20 precedent). If new findings surface, queue new fix tasks; do NOT advance state.

## BLOCKED

_(none)_

## DONE (Story 1.9 — CR iter-7 adversarial triage)

- [x] `/bmad-code-review (args: "2")` Ralph-hosted three-layer fan-out (sixth cumulative confirmation across Stories 1.7 iter-7/12/15/18/20, 1.8 iter-7, 1.9 iter-7) against `/tmp/story-1-9-review-diff.txt` (2903 lines, 23 files, diff = `git diff origin/main..HEAD -- . ':!.ralph/@plan.md' ':!.ralph/logs/'`). Three parallel `Agent` calls: Blind Hunter as `bmad-agent-architect` (diff-only; 13 findings = 0 CRITICAL + 6 HIGH + 3 MEDIUM + 4 LOW); Edge Case Hunter as `bmad-tea` (diff + project read; 10 findings = 1 CRITICAL + 2 HIGH + 3 MEDIUM + 4 LOW); Acceptance Auditor as `bmad-agent-dev` (diff + spec; overall BLOCKED — AC 1 PARTIAL, AC 2 FAIL, AC 3 PASS, AC 4 PASS, AC 5 FAIL, AC 6 PASS-with-caveat, AC 7 PASS). **Convergent CRITICAL across Edge + Auditor, live-reproduced twice independently:** `ANCHOR_REGEX` at `sync-gate.ts:24` uses uppercase character class `[A-Z][A-Z0-9-]+` but real IDs are lowercase — `readAnchors()` returns empty `Set()` against current INVARIANTS.md; the `for (const anchor of anchors)` loop at line 80 iterates over zero elements; AC 5 (`removed-from-docs-only`) is structurally unreachable; AC 2's anchor-side carve-out is also unreachable. Edge reproduced via direct `readAnchors(process.cwd())` call; Auditor reproduced via adding orphan `- **\`INV-fake-orphan\`** …` anchor to INVARIANTS.md → exit 0, no drift reported. Spec `1-9-…md:52` echoes the same uppercase typo — spec-encoded defect faithfully reproduced in impl. Triage: **6 patches + 10 defers + 3 dismissed**. Patches queued one-per-iteration per FR14n matrix row 10. Story State `sm-verified → fixes-pending`. Per iter-6 note + iter-2 validated→in-dev convention, sprint-status + story-file Status remain `review` (they flipped to `review` at iter-4 dev-story sealing; `fixes-pending` is an IP-local state, not a YAML-surface state; flips to `done` only on CR zero-findings terminal outcome per matrix row 9).

### CR triage detail

**Patches (6, queued in priority order above):** (#1 CRITICAL regex-case + missing-symmetric-branch; #2 HIGH AC 6 stdout→stderr spec correction; #3 HIGH 7→8 source-file count; #4 HIGH two→three smoke-test count; #5 MEDIUM trace line-citation ±1 drift; #6 MEDIUM Dev Agent AC 2 framing correction).

**Defers (10, absorbed into `deferred-work.md § Deferred from: code review of story-1.9 (2026-04-20)`):**
- Shallow `Object.freeze` vs "readonly + deep-freeze" claim (spec over-advertises guarantee; amend text OR deep-freeze in 1.10+).
- `check.ts` lacks try/catch around `runSyncGate` — raw ENOENT stacktrace instead of structured `error`-status report on missing INVARIANTS.md.
- Traversal refine gaps (URL-encoded `%2e%2e`, null-byte, newline, symlink-follow, Windows-drive `D:/`).
- UTF-8 binary-file mangling in `manifest-reader.ts` — `readFile(path, 'utf-8')` diverges from `sha256sum` byte-hash on non-text sources (all current sources are text; vacuous today).
- `Promise.all` error-swallow at `sync-gate.ts:47-57` — non-ENOENT errors collapse silently into `removed-from-source-only`.
- Anchor regex O(n·m) at scale (linear in entries × doc lines; sub-second at 500+ entries; watch but no action for 1.0).
- Duplicate-id refine message shape — triplicate IDs show 2 messages, not one-per-pair (cosmetic UX).
- Whitespace-in-anchor-backtick normalisation (regex doesn't allow spaces today, but principled hardening for future syntax-drift).
- `<500ms internal target` (prose at story line 128) exceeded at 0.77s measured — the AC threshold <2s is met; internal target is prose not AC. Amend spec language OR re-benchmark with node-fresh cache baseline.
- Provisional-ID bullet leftover "(Actually — Task 2 edits `invariants.manifest.ts`, not `INVARIANTS.md`; …)" scratch-commentary at spec line 130 — cosmetic cleanup.

**Dismissed (3):**
- `architecture.md:922` hyphen→dot normalisation drift (Story 1.8 artefact bookkeeping, not 1.9 scope).
- Sprint-status `1-8: backlog → done` direct transition in this commit range (by-design per Ralph post-halt orphan-prevention precedent; Story 1.8's sprint-status bump rode in Story 1.9's iteration span — not a drift).
- Story 1.8 spec wording ambiguity ("4 shared-source pairs" vs "2 shared files"; not Story 1.9's subject directly).

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1–1.8 done; 1.9 fixes-pending)
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (continues — PR #226 stays Draft until Epic 1 EPIC_DONE per PROMPT_build.md step 5c)
- **Story:** `1-9-invariant-sync-gate-runtime-tooling-reader-walker-drift-detector`
- **Story File:** `_bmad-output/implementation-artifacts/1-9-invariant-sync-gate-runtime-tooling-reader-walker-drift-detector.md`
- **Story State:** `fixes-pending` (Ralph FR14n matrix row 10) — sprint-status + story-file Status remain `review`; flips to `done` only on CR zero-findings outcome per matrix row 9.
- **PR:** #226 (Draft, MERGEABLE, empty CI rollup — expected pre-Story-1.16; stays Draft through Epic 1; Draft→Open at EPIC_DONE)

## Notes

- **First CR on Story 1.9 surfaced a CRITICAL spec-encoded defect reproduced verbatim in impl** — the ANCHOR_REGEX uppercase-class bug is the first time an Epic 1 substrate story's CR pass has converged two independent adversarial layers (Edge Case Hunter + Acceptance Auditor) on the same live-reproduced runtime failure. Prior Stories 1.3–1.8 CR passes were either zero-finding (1.3–1.6, 1.8) or wording/content-accuracy drift (1.7). This is the lower-envelope test of the "behaviour-introducing stories trend multi-pass CR" guidance from Story 1.8 iter-7 Decisions 2026-04-20: Story 1.9 lands 1 critical impl bug + 3 HIGH content-accuracy drifts + 10 defensive-hardening defers. Budget ~6 iterations to EPIC_DONE halt (6 fix iterations + 1 CR re-run + 1 PR-transition — but fixes #3/#4/#5/#6 are pure docs and can land alongside fix #1's re-verification if Ralph compresses; conservative ceiling ~8 iterations).
- **CR-hosting pattern re-confirmed third-iteration-class (after Stories 1.7 + 1.8):** Ralph-hosted three-layer fan-out via three parallel `Agent` calls with role-semantic subagent types (architect/tea/dev) remains the canonical realisation of `/bmad-code-review (args: "2")` without nesting a `Skill()` call (which would HALT at step-01 / step-04 HITL checkpoints). Diff scoping via `git diff origin/main..HEAD -- . ':!.ralph/@plan.md' ':!.ralph/logs/'` load-bearing. First time the scoped-diff has been a 2903-line corpus covering 23 files (Story 1.9 is the richest runtime-behaviour story in Epic 1 — 3 new TS files + CLI + schema hardening + INVARIANTS.md expansion + 4-file trace bundle + architecture.md touch + sprint-status bump).
- **Convergence signal:** a CRITICAL finding reproduced independently by two of three adversarial layers (Edge Case Hunter via direct `readAnchors()` call against live INVARIANTS.md; Acceptance Auditor via orphan-anchor injection + exit-code observation) is the strongest-possible triage evidence for `patch` vs `defer` — DEFINITE patch, no debate. Auditor PASS→FAIL transitions for AC 2 + AC 5 are the consequence, not the root: the root is the regex character class.
- **Spec-echoes-impl-bug pattern (new for Story 1.9):** when a spec lists a verbatim regex literal like `/^-\s+\*\*\`([A-Z][A-Z0-9-]+)\`\*\*/gm` as authoring guidance AND the dev-story implementation copies it character-for-character, an impl bug becomes a spec bug too. Fix BOTH (impl + spec) in the same commit — otherwise the next trace/SM-review/CR re-run surfaces the spec-side un-synchronised copy as fresh drift. Prior analogue: Story 1.7 iter-18 drift-survey-scope rule for sibling wording in YAML blocks; this is a cross-artefact sibling rule (code-vs-spec). **Carry-forward rule:** when a spec-authoring story declares an implementation-guidance literal (regex, schema, type, constant), Ralph MUST grep the fix across BOTH the source file AND every spec / trace / doc artefact echoing that literal.
- **Status-label convention clarified:** this is the FIRST Epic-1 CR-fixes-pending iteration where story-file Status + sprint-status are BOTH at `review` (not `done`) at triage-time, because the iter-6 SM audit flipped story-file Status `in-progress → review` at dev-story seal (not `in-progress → done`). Consequence for Ralph-loop ledger: YAML-surface status stays `review` through the entire fix-pass arc; only IP Story State tracks `fixes-pending`. When QUEUE empties and CR re-run emits zero findings, matrix row 9 "done (no findings)" edge bumps story-file Status AND sprint-status `review → done` simultaneously in the same commit. Distinct from Story 1.7 iter-7/iter-20 pattern (which went `done → in-progress → done` in sprint-status because SM was inline-in-dev-story sealed as `done`, not `review`).
- **Adversarial-triage-is-default signal-strength ranking:** Edge Case Hunter's CRITICAL > Blind Hunter's HIGH count-drift > Auditor's FAIL verdicts (in this triage, the ordering is: Edge CRITICAL regex-bug → Auditor AC 2/5 FAIL (same root) → Blind HIGH count drifts → Blind/Auditor MEDIUM framing + line-citation → Defers). Future-Ralph carry-forward: when layers converge on the same root (different symptoms), deprecate the symptom findings from individual fix-task queueing (they auto-resolve when the root lands) — track them as the root-fix's verification checklist instead. Example in this iter: fix #1's verification checklist must include "run AC 5 orphan-anchor smoke" (Auditor's FAIL evidence) AND "re-run `readAnchors()` smoke" (Edge's CRITICAL evidence); after fix #1 lands, Auditor AC 2/5 should flip FAIL→PASS automatically — no separate fix tasks for AC 2/5 needed.
- **Deferred-work.md § story-1.9 growth:** 10 defers absorbed this iteration — the largest single-CR defer batch yet (Story 1.8 shipped 6 defers). Most defers (9 of 10) are defensive-hardening candidates for Story 1.10+ substrate refactors. One defer (`<500ms internal target`) is prose-only and can be amended in a cleanup iteration alongside fix #3 if Ralph chooses; left as defer to preserve the "CR action items = QUEUE fix OR explicit defer" matrix-row-10 contract and avoid scope creep on fix iterations.
- **Issue Tracking:** Story 1.9 issue number still unset at iter-7 (no GH project auto-creation for stories). Parent Epic 1 issue **#9** — `Refs #9` only; closed by ralph.py EPIC_DONE halt handler at Epic 1 completion (still ~11 stories away — 1.10–1.16 + any 1.x inserts).
