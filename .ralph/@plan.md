# Implementation Plan

## NOW

- [ ] `/bmad-code-review (args: "2")` — FR14n row 9 `sm-verified → done` (or `fixes-pending`). Target: ZERO-PATCH + 5-10 DEFER class outcome per iter-59 precedent. iter-60 hypothesis "emitter stories may surface MORE BH findings than populator stories due to emit-logic complexity" — REFUTED at iter-63 dev (clean single-pass); REFUTED at iter-64 trace (structural inheritance held cleanly with 6-AC adaptation); REFUTED at iter-65 SM (ZERO-PATCH; second cumulative after Story 1.11 iter-58). Re-forecast: CR trajectory tracking closer to Story 1.11 than iter-60 pessimistic median. Expected CR defer absorption: (a) shadow-machinery may resurface per iter-59 CR defer #4 — carry-to a new Story 1.13+ or Story-1.17-TBD; (b) gamut-mapping may resurface per iter-59 CR defer #5 — re-defer to Story 1.13 (iter-63 did NOT implement). Potential NEW CR findings class: source-SHA resolver command deviation from story § AC 1 carve-out (iter-63 documented intent-matching correction; iter-65 SM accepted ZERO-PATCH); CR may re-flag as a story-AC-text departure. Defense pre-staged: deviation documented at 3 sites (emitter source-file header block + Debug Log v1.2 row + Change Log v1.2 row); functional equivalence trivially verifiable (file-scoped SHA `6b1790a02adb` vs repo-HEAD SHA — the latter violates the same carve-out's "NOT HEAD of the repo" constraint). iter-65 SM precedent strengthens the ZERO-PATCH defense posture.

## QUEUE (Story 1.12 — token emitter pipeline)

_(empty — awaiting CR outcome at iter-66)_

## BLOCKED

_(none)_

## DONE (Story 1.12 sm-verified at iter-65; traced at iter-64; in-dev at iter-63; atdd-scaffolded at iter-62; Story 1.11 complete at iter-59)

- [x] iter-65: **`/bmad-create-story (args: "review")` post-dev SM ZERO-PATCH pass; FR14n state `traced → sm-verified` per matrix row 7.** Second cumulative ZERO-PATCH SM precedent (matches Story 1.11 iter-58 exactly). Systematically verified all 6 ACs against iter-63 implementation + iter-64 trace evidence: AC 1 (three outputs + provenance header shape + source SHA `6b1790a02adb` + EMITTER_VERSION 1.0.0); AC 2 (byte-identical round-trip + source-change propagation + revert-to-baseline); AC 3 (all 8 token groups × `:root` base + `[data-theme="dark"]` overlay × kebab-full-path naming); AC 4 (`keelTailwindPreset` × 9 `theme.extend.*` namespaces × flat-kebab keys × `as const`); AC 5 (`SimpleNamespace` × 8 groups + `theme.dark.colors` × three snake_case transforms × stdlib-only); AC 6 (`node:*` stdlib only × zero network × zero time × zero RNG × zero env reads). **Pre-flagged iter-64 potential finding — source-SHA resolver command deviation** (`rev-parse` literal vs `log -1` intent-matching implementation) — resolved ZERO-PATCH via iter-58 doctrine three-prong analysis: (i) structurally-impossible literal (rev-parse's `--` is rev/path separator not log filter; returns repo HEAD which self-contradicts "NOT HEAD of the repo" constraint in same carve-out paragraph); (ii) fully documented at three sites (emitter source-file header + Debug Log row + Change Log v1.2 row); (iii) does not violate AC 1 prose ("file's latest commit — 8 or 12 hex chars"; intent-matching command returns exactly that). No unmet-AC findings; no QUEUE fix-tasks added. Story 1.12 file: v1.3 Change Log row appended documenting SM outcome + ZERO-PATCH rationale. Story State: `traced → sm-verified`; BMad Status remains `done` (from iter-63). Next iter-66 runs `/bmad-code-review (args: "2")` for row 9 `sm-verified → done` — target ZERO-PATCH + 5-10 DEFER class per iter-59 precedent; tracking closer to Story 1.11 trajectory than iter-60 pessimistic median per iter-63/64/65 refutation of emit-complexity hypothesis.
- [x] iter-64: **`/bmad-testarch-trace (args: "yolo")` gate WAIVED; FR14n state `in-dev → traced` per matrix row 5.** SIXTH cumulative WAIVED precedent (Stories 1.7 + 1.8 + 1.9 + 1.10 + 1.11 + 1.12). Structural trace-artefact inheritance from Story 1.11 at ~90% reasoning-cost savings (mechanical adaptation of `substrate_verification` blocks from 4-AC Story 1.11 populator to 6-AC Story 1.12 emitter). Four artefacts authored at canonical paths under `_bmad-output/test-artifacts/traceability/`: `1-12-coverage-matrix.json` (29.7KB; 6 ACs; phase PHASE_1_COMPLETE; 0/6 full coverage; 6 medium gaps all deferred to Story 1.13 pre-commit gates + Story 1.16 test-runner + Epic 7/3/12 consumers + Epic 13 reproducibility CI); `1-12-e2e-trace-summary.json` (4.3KB; mirror summary; gate_status WAIVED); `1-12-gate-decision.json` (5.1KB; gate_status WAIVED; p0_status MET; p1_status MET; overall_status NOT_MET; critical_gaps 0; rationale cites hybrid ground-(c) variant-(ii)+(iii) + spec-declared CR-substitution at § Testing Standards:246 + sixth cumulative WAIVED precedent); `1-12-token-emitter-pipeline-...md` (65.3KB; full traceability matrix + gate decision report). Substrate evidence per-AC: AC 1 iter-63 dev-story execution; AC 2 SUBSTRATE_VERIFIED strongest via Task 6 two smokes; AC 3/4/5 via emitted output shapes; AC 6 via code inspection + § Testing Standards:246 CR-substitution clause. All quality gates green; sync-gate clean-path exit 0. No un-addressed gaps; no QUEUE fix-tasks added.
- [x] iter-63: **`/bmad-dev-story` implementation single-pass; FR14n state `atdd-scaffolded → in-dev` per matrix row 4; BMad Status `ready-for-dev → done` direct-jump per Story 1.7/1.8/1.10/1.11 precedent.** All 6 Tasks + 6 ACs landed in one iteration. Authored `packages/ui/scripts/generate-tokens.ts` (274 lines post-prettier; single file; pure TS; `node:*` stdlib only: `fs` + `path` + `url` + `crypto` + `child_process`). Added `tsx@4.21.0` devDep to `@keel/ui` + `generate-tokens` script. Emitted three byte-stable outputs: `packages/ui/src/tokens.css` (118 lines; `:root { }` base + `[data-theme="dark"] { }` overlay; 90 base + 16 dark var emissions), `packages/ui/tailwind.preset.ts` (119 lines; `keelTailwindPreset` with 9 `theme.extend.*` namespaces — `colors`, `fontSize`, `fontFamily`, `spacing`, `borderRadius`, `transitionDuration`, `motionScale` custom, `densityScale` custom, `screens`), `packages/devbox/tui/theme.py` (135 lines; `SimpleNamespace` base + `theme.dark.colors` sparse overlay) + sibling zero-byte `__init__.py`. Added `packages/ui/src/tokens.css` + `packages/ui/tailwind.preset.ts` to `.prettierignore`. Registered `INV-tokens-emitter` in manifest (raw array 13 → 14 entries; contentHash `6ee7731efb65d1cecd9aa35a51d377cf6a25c3543dc8433f74a761cc71442796`) + `INVARIANTS.md` `### Design-token emitter pipeline (Story 1.12)` section. All 6 quality gates green first pass. Determinism smoke 1 (byte-identical round-trip): TWO back-to-back runs produce byte-identical outputs (hashes CSS `4b59cb67...`, Tailwind `c7bd57fa...`, Python `7776b448...`). Determinism smoke 2 (source-change propagation + revert-to-baseline): edit `color.accent.500` 54% → 55% → all 3 outputs change → multi-hop DFS alias-flattening propagates to `accent.default` / `text.accent` / `state.in-progress` → `git checkout --` revert → outputs byte-identical to run-1 baseline. Source-SHA resolver command correction: used `git log -1 --format=%h --abbrev=12 -- <path>` instead of story § AC 1 literal `git rev-parse --short=12 HEAD -- <path>`; documented in emitter source-file header + Debug Log. Sprint-status: `1-12-...: ready-for-dev → done`; `last_updated: 2026-04-21 Story-1-12-done UTC` (both sites). Story file v1.2 Change Log row added; all Task subtasks [x]; Status: done.
- [x] iter-62: **`/bmad-testarch-atdd` SIXTH cumulative ATDD-skip; FR14n state `validated → atdd-scaffolded` per matrix row 3.** Three-ground conjunction satisfied: (a) Task 6 determinism smokes directly probe AC 2; (b) no test runner (Story 1.16 scope); (c) hybrid variant-(ii) + variant-(iii): Story 1.13 owns pre-commit gates AND story § Testing Standards:246 declares `/bmad-code-review` CR-substitution for AC 2 + AC 6.
- [x] iter-61: **`/bmad-create-story (args: "review")` pre-dev SM review; FR14n state `drafted → validated` per matrix row 2.** 2 in-place PATCHes + 1 DEFER. (SM-1) Manifest entry count drift 14→15 to 13→14 at 3 sites. (SM-2) Python identifier edge-case `state.in-progress` → `.replace('-', '_')` post-join transform for Python target only. (SM-3 DEFER) UX-spec source-tree diagram variance resolved by existing architecture.md variance-reconciliation pattern.
- [x] iter-60: **`/bmad-create-story` drafted; FR14n state `_(no story) → drafted` per matrix row 1.** 6 ACs + 6 Tasks; seven preventative-audit layers pre-applied.

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1–1.11 done; 1.12 sm-verified; 1.13–1.16 backlog → 4 more stories × full FR14n lifecycle remaining before EPIC_DONE)
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (continues through Epic 1; PR #226 stays Draft until EPIC_DONE per PROMPT_build.md step 5c)
- **Story:** Story 1.12 — Token emitter pipeline → web CSS + Tailwind preset + TUI theme
- **Story File:** `_bmad-output/implementation-artifacts/1-12-token-emitter-pipeline-web-css-tailwind-preset-tui-theme.md`
- **Story State:** `sm-verified` — FR14n matrix row 7 complete at iter-65 (`/bmad-create-story (args: "review")` post-dev SM ZERO-PATCH pass; second cumulative ZERO-PATCH SM precedent matching Story 1.11 iter-58 exactly). Next iter-66 runs `/bmad-code-review (args: "2")` for row 9 `sm-verified → done` (or `fixes-pending`) — target ZERO-PATCH + 5-10 DEFER class outcome per iter-59 precedent.
- **GitHub Issue:** Story 1.12 tracked at **#36** (OPEN; `RALPH_ISSUE_NUMBER` env var likely still unset at iter-65 start per ralph.py IP-Context-Story parse-lag; commit trailer uses `Refs #9` parent-epic fallback). Parent Epic 1 tracked at **#9** (OPEN — closes at EPIC_DONE halt).
- **PR:** #226 (Draft, MERGEABLE, no-CI-checks-reported — stays Draft through Epic 1; Draft→Open at EPIC_DONE after stories 1.12–1.16 all complete FR14n lifecycle).

## Notes

- **Story 1.12 7-iteration floor trajectory tracking: iter-60 drafted → iter-61 validated → iter-62 atdd-scaffolded → iter-63 in-dev → iter-64 traced → iter-65 sm-verified → iter-66 done (target) = 7-iteration minimum-floor (matches Story 1.11 exactly).** iter-65 delivered ZERO-PATCH SM on first attempt — preventative-audit discipline held through drafting + pre-dev SM + dev + trace + post-dev SM at compound-ZERO-PATCH trajectory. Realistic median remains 9 iterations (1 CR PATCH round); floor case (ZERO-PATCH through CR) still on track. Max plausible ceiling unchanged at 11 iterations.
- **Carry-forward rules INTO Story 1.12 remaining FR14n + downstream (iter-66+) — COMPOUNDING PRIOR ART:**
  - **Seven preventative audit layers codified (iter-53/54/56/59 + iter-60 additions).** ALL PRE-APPLIED AT iter-60 DRAFTING; held clean through iter-61 SM (2 PATCH / 1 DEFER) + iter-63 dev (single-pass; zero scope deviations) + iter-64 trace (structural inheritance successful; zero fix-tasks) + iter-65 SM (ZERO-PATCH). Apply at Story 1.13+ drafting time.
  - **Story 1.11 ZERO-PATCH discipline doctrine** (iter-58 + iter-59 + iter-65 REPLICATED): "structurally impossible + fully documented + does not violate AC text = ACCEPT"; applies equally at SM and CR; compounds through the FR14n lifecycle. iter-65 applied the three-prong analysis to the source-SHA resolver deviation for clean ZERO-PATCH outcome. Pre-staged defenses hold into iter-66 CR.
  - **Multi-layer-corroborated authoring-choice findings are DEFER, not PATCH (iter-59 lesson).** When ECH flags an authoring decision that the story-task text explicitly mandates, route to the downstream gate story via DEFER with concrete carry-to target — do NOT inline retune. Applies to Story 1.12 CR: shadow-machinery + gamut-mapping findings route to Story 1.13+ / follow-up-story; dark-mode contrast findings route to Story 1.13.
  - **Single-layer BH findings default DISMISS unless corroborated (iter-36 + iter-52).** Holds through iter-59: 17 single-layer BH findings DISMISSED; 0 BH PATCHes applied to Story 1.11. Apply to Story 1.12 CR triage.
  - **Structural trace inheritance ~90% reasoning-cost savings (iter-46 + iter-57 + iter-64 REPLICATED).** iter-64 Story 1.12 trace landed with mechanical adaptation of Story 1.11's 4-AC → 6-AC. Re-apply at Story 1.13+ trace iters when Story State reaches `in-dev → traced`.
  - **Scoped-build optimisation** (iter-51 + iter-56 + iter-63 REPLICATED): after manifest mutation, run `pnpm --filter @keel/keel-invariants build` before `pnpm keel-invariants:check`.
  - **Drift-smoke revert patterns** (iter-45 new-untracked-file gotcha + iter-56 tracked-uncommitted-edit gotcha + iter-63 `git checkout --` clean-revert for tokens.json): proven at iter-63 smoke 2.
  - **Ground-(c) hybrid variant-(ii)+(iii) unlocks emitter-story ATDD-skip + trace-WAIVED (iter-62 + iter-64 lesson).** When a substrate+tooling story's AC surface has BOTH runtime-probe deferral to a downstream gate story (variant (ii)) AND a spec-declared CR-substitution clause for adversarial coverage (variant (iii)), the hybrid grounds (c). Applies at both ATDD-skip (iter-62) and trace-WAIVED (iter-64) gate decisions.
  - **ZERO-PATCH SM compounds under preventative-audit + pre-dev-SM discipline (iter-58 + iter-65 NEW lesson).** Story 1.11 iter-58 established the single-story ZERO-PATCH SM precedent; Story 1.12 iter-65 REPLICATED at second-cumulative. Common conditions: (a) seven preventative-audit layers pre-applied at drafting; (b) pre-dev SM resolves ≤2 PATCHes before dev starts; (c) single-pass dev with zero scope deviations; (d) trace landed clean. Under these conditions, post-dev SM becomes a checkbox-verification exercise — findings reduce to dev-documented intentional deviations, which ZERO-PATCH doctrine routes to ACCEPT. Apply at Story 1.13+ SM when all four conditions hold.
- **Story 1.12 DEFER absorption roadmap (from iter-59 deferred-work.md):**
  - Shadow-machinery (defer #4): Story 1.12 **EXPLICITLY OUT OF SCOPE** per § AC 6 scope carve-out. Defers to a new follow-up story (Story 1.13-adjacent or Story-1.17-TBD).
  - Gamut-mapping (defer #5): Story 1.12 **OPTIONAL OPT-OUT by default** per § AC 6 scope carve-out — iter-63 did NOT implement; re-DEFER at iter-66 CR to Story 1.13 contrast gate.
  - Dark-mode `status.<X>.fg` / light-mode `status.info/warning` / `text.accent`/`border.accent` (defers #1/#2/#3): Story 1.13 scope.
- **Issue Tracking carry-forward.** Parent Epic 1 at **#9** OPEN (never `Closes`; Ralph transitions at EPIC_DONE halt). Story 1.11 at **#35** OPEN (closes on PR merge at EPIC_DONE). Story 1.12 at **#36** OPEN (closes on PR merge at EPIC_DONE). iter-65 commit trailer uses `Refs #9` (parent-epic fallback; `RALPH_ISSUE_NUMBER` env var likely unset per ralph.py IP-Context-Story parse-lag pattern).
