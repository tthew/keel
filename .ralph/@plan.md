# Implementation Plan

## NOW

- [ ] **Run `/bmad-testarch-atdd` fresh-context for Story 2.11** (Ralph-internal `Story State` = `validated → atdd-scaffolded` per § Story Lifecycle Decision Matrix). Story 2.11 passed pre-dev review at iter-255 with Change Log v1.1 folding 1 CRITICAL (composite Task 3 + Task 5) + 6 ENHANCEMENT review-findings (see Story 2.11 § Change Log v1.1 for the full list). ATDD-forecast per § Testing Standards: **NON-SKIP for ACs 1-3** (resolver smokes + compose-config assertion + env-check warning stderr-grep all feasible as bash functional tests); **AC 4 concurrency is doc-only** → trace partial-waive candidate (no cross-fork harness exists). Red-phase failures go to IP § ATDD Red Phase and do NOT block the `atdd-scaffolded → in-dev` transition (Guardrail 4 exception). Forecast: ~40-55K (orient + skill dispatch + ATDD scaffold authorship if non-skip + IP + commit); if ATDD-skip branch fires (`bmad-testarch-atdd` halts on missing test framework — Story 2.5 iter-186 precedent ground (c)+(ii)+(iii)), the iteration collapses to ~20K "skip-with-grounds" IP+RALPH entry.

## QUEUE (Story 2.11..2.17 → Epic 2 close-out)
- [ ] _(after Story 2.11 `atdd-scaffolded`)_ `Run /bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-11-per-fork-vs-shared-devbox-mode-keel-devbox-shared.md")` — impl of Tasks 1-6 in a fresh context window. Story State `atdd-scaffolded → in-dev`. Forecast: ~70-100K (6 tasks; 18-shim wire-in is mechanical composition on Stories 2.6/2.8/2.9/2.10 precedent; resolver-function extension + compose edit + env-check warning + INV doc authorship are the novel surface). May split across iters (`in-dev (partial)` re-queue per FR14n).
- [ ] _(after Story 2.11 `in-dev`)_ `Run /bmad-testarch-trace (args: "yolo")` — AC→test coverage gate + traceability matrix. AC 4 partial-waive candidate (doc-only concurrency decision). Story State `in-dev → traced` OR `trace-fixes-pending` (fix tasks per coverage gap).
- [ ] _(after Story 2.11 `traced`)_ `Run /bmad-create-story (args: "review")` post-dev SM verification. Story State `traced → sm-verified` OR `sm-fixes-pending`. Two-subagent verification pattern per iter-235 LESSON.
- [ ] _(after Story 2.11 `sm-verified`)_ `Run /bmad-code-review (args: "2")` adversarial CR via three-layer Ralph-hosted triage (Blind Hunter `bmad-agent-architect` diff-only + Edge Case Hunter `bmad-tea` diff+project-read + Acceptance Auditor `bmad-agent-dev` diff+spec+INV). Story State `sm-verified → done` OR `fixes-pending`. Chain-reset forecast: Stories 2.11..2.17 begin fresh ZERO-PATCH-CR forecast cycle per iter-253 LESSON (0-1 PATCH + 2-4 second-class DEFERs band-aligned).
- [ ] _(after Story 2.11 `done`)_ Iterate Stories 2.12..2.17 through full lifecycle — 6 remaining substrate stories spanning loopback-bound port publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-23 per iter-219..254 repeated pre-push confirmation — `gh pr view 230` returns `statusCheckRollup: []`).

## BLOCKED

_(none — iter-255 step 5 push RECOVERED: `git push origin feat/epic-2-packaged-devbox` succeeded with range `d9199cf..a15845f` — full backlog flushed in one round-trip. SSH :22 intermittent-timeout class cleared at iter-255. `gh pr view 230` HTTPS GraphQL still intermittent at iter-255 orient — `dial tcp 140.82.121.5:443: i/o timeout` — but `git push` over SSH recovered independently. Note for next iter: carry-forward "intermittent network" expectation across iter-219..255; no persistent block.)_
  - Attempted: push attempts at iter-249/250/251/252/253 all failed with SSH :22 timeout to github.com.
  - Error/Issue: persistent egress block on SSH :22 to github.com from iter env across 5 consecutive iters; HTTPS :443 to api.github.com intermittent (succeeded iter-252 orient + iter-254 orient, failed iter-253 orient + pre-push); git push hard-wired to SSH remote URL.
  - Next: iter-254+ continues banking on intermittent SSH :22 recovery. **Operator escalation now strongly recommended after 5 consecutive iter failures** — one-off `git remote set-url --push origin https://github.com/tthew/ralph-bmad.git` on the host (or environmental egress unblock for SSH :22 to github.com `140.82.121.4/5/3/6/22`) to unblock the iter-254 backlog. Ralph SHOULD NOT autonomously reconfigure the remote URL (shared-state mutation beyond single-iter scope per § Executing actions with care). Commits are safe in local branch — no force-push, no amend, preserved intact across iter-249..254; on operator unblock, `git push` flushes all 9 commits in a single round-trip with no conflict (each commit non-destructive).

## ATDD Red Phase

_(none — Story 2.11 ATDD not yet queued; lifecycle at `drafted`. Red-phase scaffolds land at `/bmad-testarch-atdd` step later; forecast non-skip per Story 2.11 § Testing Standards.)_

## DONE (iter-255 Story 2.11 pre-dev review — Story State `drafted → validated`)

- [x] iter-255: **STORY 2.11 PRE-DEV `/bmad-create-story (args: "review")` GATE — 1 CRITICAL (composite Task 3 + Task 5) + 6 ENHANCEMENT review-findings folded into Change Log v1.1 + HTML status-comment flipped `drafted → validated`.** Fresh-context `bmad-agent-architect` (Winston) dispatch per `.claude/skills/bmad-create-story/checklist.md` produced: 1 CRITICAL (composite spanning multiple tasks) + 6 ENHANCEMENT + 5 OPTIMISATION findings with file:line anchors. **Ralph (orchestrator) applied all CRITICAL + 6 highest-value ENHANCEMENT edits autonomously** (Guardrail 3; 5 OPTIMISATION deferred to impl-time dev-agent discretion — non-blocking wording/citation polish). **Substrate-truth corrections applied** (see Story 2.11 Change Log v1.1 for full detail): (1) Task 3 bullet 1 shim enumeration — transitive "17 from Story 2.10" replaced with explicit 18-shim split (8 already-sourcing resolver + 10 not-sourcing; `clean.sh`/`stop.sh`/`restart.sh`/`status.sh`/`logs.sh` under shared mode would target wrong compose project without resolver wiring — correctness regression, not cosmetic); (2) Task 3 bullet 3 `CONTAINER_NAME` inline-recompute inventory corrected from 7-files-with-errors to canonical 11-file grep output; (3) Task 5 anchor format "backtick-wrapped" replaced with bare-ID-string verified against `invariants.manifest.ts:54,62,70,78`; (4) **AC 4 "docker attach single-writer semantics — second attach detaches first" factually WRONG** — Docker permits concurrent attaches with I/O corruption, NOT auto-detach; corrected to "single-operator-at-a-time BY CONVENTION, NOT Docker enforcement" + explicit dev-agent guardrail against implementing an auto-detach feature; (5) Task 4 three-site lockstep warning template expanded to BOTH Case A (current-shared / orphan-per-fork) + Case B (current-per-fork / orphan-shared) concrete renderings; (6) env-check.sh process-env / parsed-`.envrc` reconciliation pinned (direnv-lag divergence protection); (7) Task 1 shared-mode `CONTAINER_WORKDIR` re-derivation case-block made verbatim; (8) Task 6 `.envrc.example:41` comment update promoted to dedicated bullet (SC-15); (9) Dev Notes rc-capture caveat strengthened ("future extension with rc-meaningful commands MUST adopt Story 2.10 PATCH-1 pattern"). Story-file Status HTML-comment flipped `drafted → validated`. **NOVEL LESSON — "substrate-truth corrections" are the dominant review-finding class on Stories N≥11 with 10+ precedents** — Epic 1 + Epic 2 Stories 2.1-2.5 review-finding class was "add missing context" (enhancement); Stories 2.6-2.10's shifted to "fix transitive citations"; Story 2.11's shifted to "verify substrate inventory" (exact shim counts, grep-derived file lists, verified manifest conventions). Forecast for Stories 2.12..2.17: 1-3 CRITICAL "inventory correction" findings per pre-dev review; ENHANCEMENT class reduces as writing pattern stabilises. **NOVEL LESSON — AC claims about Docker-primitive behaviour MUST be runtime-or-doc-verified BEFORE create-story landing**; AC 4's claim conflated `docker run -a` (single-client) with `docker attach` against a running container (concurrent-attach-permitted) — if dev-agent implemented to the prior wording, they would build an auto-detach feature that Docker does not expose, a substantive misimplementation. Budget iter-255: ~55K (orient ~10K + checklist + story read + Winston-subagent dispatch ~22K + 9 targeted edits + Change Log v1.1 + IP + RALPH.md signpost + commit ~12K). Push status: `gh pr view 230` orient at iter-255 not re-tested; SSH :22 retry at step 5 (carry-forward of iter-249..254 intermittent-timeout class). +1 commit at iter-255 → 10 unpushed commits locally.

- [x] iter-254: **STORY 2.11 CREATE-STORY LANDING — `/bmad-create-story` fresh-context auto-advance from Story 2.10 closure.** Story file landed at `_bmad-output/implementation-artifacts/2-11-per-fork-vs-shared-devbox-mode-keel-devbox-shared.md` (~30KB; 4 ACs + 6 Tasks + 17 SCs + Dev Notes); sprint-row `2-11-…: backlog → ready-for-dev`; Story State `_(no story) → drafted`. See RALPH.md iter-254 Signpost for full per-step detail. _(Pruned from IP at iter-255 per Guardrail 2 — Story 2.11 create-story landing detail carried in RALPH.md; Change Log v1.0 in story file.)_

_(iter-243..253 Story 2.9 closure + Story 2.10 lifecycle iters pruned per Guardrail 2 — see Story 2.10 § Change Log v1.0–v1.7 + RALPH.md Signposts for per-gate detail.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **10/17 stories done** (2.1-2.10) + 1/17 drafted (2.11) + 6/17 backlog (2.12..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered at iter-248 9/9 stub-docker harness + iter-252 9/9 mock-docker PATH-shim harness.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:119). Host-side shim count: **18** at iter-254 (unchanged from iter-253 — Story 2.11 does NOT add new shims; it extends the 18 existing ones with a resolver invocation).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **10 done** (2.1-2.10); 1 drafted (2.11); 6 backlog (2.12..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.11 — Per-fork vs shared devbox mode (`KEEL_DEVBOX_SHARED`)
- **Story File:** `_bmad-output/implementation-artifacts/2-11-per-fork-vs-shared-devbox-mode-keel-devbox-shared.md`
- **Story State:** `validated` — Story 2.11 pre-dev review gate cleared at iter-255 via `/bmad-create-story (args: "review")`; Change Log v1.1 records 1 CRITICAL + 6 ENHANCEMENT findings folded in; next iter `/bmad-testarch-atdd` transitions `validated → atdd-scaffolded`.
- **GitHub Issue:** Story 2.11 likely unwired (`RALPH_ISSUE_NUMBER` unset at iter-254 check; skip `Refs #<story-issue>` in commits). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out, NOT iter-254; Story 2.11 draft ≠ Epic 2 close).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — last confirmed `{"isDraft":true,"mergeStateStatus":"CLEAN","state":"OPEN","statusCheckRollup":[]}` at iter-254 orient; PR state unchanged (no merge events expected during BLOCKED push window).

## Notes

- **iter-255 Story 2.11 pre-dev review gate — NOVEL LESSON on "substrate-truth corrections" as the dominant review-finding class for Stories N≥11** (carry-forward for Stories 2.12..2.17). Winston's review focused on inventory-verification (exact shim counts, grep-derived file lists, verified manifest conventions) — NOT "add missing context" (Epic 1 class) or "fix transitive citations" (Stories 2.6-2.10 class). Pattern: every enumeration claim (shim list, recompute inventory, anchor format, Docker primitive behaviour) MUST be grep/ls/doc-verified at create-story OR pre-dev-review time. **Forecast for Stories 2.12..2.17:** 1-3 CRITICAL "inventory correction" findings per pre-dev review; ENHANCEMENT class reduces as writing pattern stabilises.
- **iter-255 LESSON — AC claims about Docker-primitive behaviour MUST be runtime-or-doc-verified BEFORE create-story landing.** AC 4's "docker attach single-writer" conflated `docker run -a` (attach-at-create; single-client) with `docker attach` against a running container (concurrent-attach-permitted). Applies to any future AC referencing Docker primitives — verify behavioural claims against Docker docs at create-story time, NOT at pre-dev-review time where corrections cost a v1.x Change Log entry.
- **iter-253 Story 2.10 CR re-run closure — NOVEL LESSON on PATCH-iter SECOND-class DEFER pattern** (carry-forward, applies to Story 2.11 CR forecast). PATCH iterations close FIRST-class adversarial findings but commonly surface SECOND-class operator-edges in the same surface the prior bug had hidden. Forecast-actionable: Story 2.11's resolver-function + compose parameterisation + env-check orphan warning constitute "multi-site novel runtime probe" surface; expect 2-4 second-class DEFERs at CR re-run (Docker Desktop wake-race, Podman-compat semantics, compound-failure short-circuit doctrine carry-forward forecast classes).
- **iter-252 Story 2.10 PATCH-1 — `set -e`+`if cmd; then` rc-suppression LESSON** (carry-forward). Story 2.11's resolver function does NOT invoke multi-step runtime probes (assignment-only branching on `KEEL_DEVBOX_SHARED`); not a recurrence risk. BUT env-check orphan-container probe (Task 4) DOES invoke `docker inspect` with rc capture — impl MUST use the canonical `rc=0; docker inspect … || rc=$?; case $rc in …` pattern per Story 2.10 PATCH-1 to avoid re-surfacing the rc-suppression class. iter-255 review strengthened Task 1 Dev Notes rc-capture caveat (applies to future extensions, not Task 1 dirname/basename which are rc=0 invariant).
- **iter-252 GOTCHA — iter-env `/tmp` is `noexec`** (carry-forward, applies to Story 2.13 healthcheck emitter + Story 2.14 legacy-branch cleanup; NOT applicable to Story 2.11 which does not use mock-binary PATH shims).
- **iter-244/246 audit-findings carry-forward** unchanged from iter-247..254: (1) restart.sh transitive-delegate prepend-only; (2) benchmark.sh OUT-OF-SCOPE clarification (SC-11); (3) Manifest entry count drift (29→30 baseline at iter-248 → 30→31 at Story 2.11 landing after `INV-devbox-mode` entry); (4) Alpine egress non-applicability doctrine. Story 2.11 adds `INV-devbox-mode` → manifest count becomes 31 at dev-story landing.
- **Substrate-citation drift forecast for Stories 2.11..2.17:** Story 2.11 touches `docker-compose.yml:39` (`name:` parameterisation is L-stable; surrounding comment-block amendments are L-stable within their paragraph). Forecast unchanged at iter-255 review: Stories 2.11..2.17 accumulate 1-3 drifts each, deferred to Story 2.17 SC-17 polish pass. Total Epic 2 close-out reconciliation forecast: ~10-20 cumulative line-number drifts unchanged.
