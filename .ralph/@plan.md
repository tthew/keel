# Implementation Plan

## NOW

- [ ] iter-83: `/bmad-testarch-atdd` — FR14n row 3 `validated → atdd-scaffolded` via hybrid ground-(c) variant-(ii)+(iii) ATDD-SKIP (NINTH cumulative precedent — Stories 1.7-1.14 → 1.15). Three-ground rationale pre-staged in story § Testing Standards lines 215-225: (a) substrate-verification-covers-ACs at CLI-exit-code level (Task 6 six smokes); (b) no-runner (test-runner still Epic 13 / post-Epic-1); (c) HYBRID variant-(ii)+(iii) — Renovate runtime + Epic 13 CI gate own AC 2 + AC 3 runtime halves; adversarial coverage of AC 1 + AC 4 delegated to iter-87 CR. Expected: 1 in-situ Change Log row (v1.2) mirroring Story 1.14 iter-76 pattern; Story State `validated → atdd-scaffolded`.

## QUEUE (Story 1.15 — renovate config i7)
- [ ] iter-84: `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/1-15-renovate-config-with-version-pinning-rules-i7.md")` — FR14n row 4 `atdd-scaffolded → in-dev`. SINGLE-PASS CANDIDATE at configuration-surface scale (matches Story 1.14 iter-77 single-pass precedent). Expected deliverables: 2 NEW files (`.github/renovate.json` + `docs/invariants/renovate.md`), 2 MODIFIED files (`packages/keel-invariants/src/invariants.manifest.ts` +2 entries + `INVARIANTS.md` +1 new H3 + 2 bullets). Six Task-6 substrate smokes expected PASS: static JSON parse, extends-+-schema check, 4-packageRules + automerge-false enumeration, I7 pin-mode consistency, manifest-load (22 invariants), sync-gate clean. Remember iter-77 Gotcha: `pnpm --filter @keel/keel-invariants build` before runtime manifest-load smoke (dist/ cache staleness).
- [ ] iter-85: `/bmad-testarch-trace (args: "yolo")` — FR14n row 5 `in-dev → traced`. NINTH cumulative WAIVED precedent CANDIDATE (Stories 1.7-1.14 → 1.15). Four trace artefacts authored: `1-15-coverage-matrix.json` + `1-15-e2e-trace-summary.json` + `1-15-gate-decision.json` + `1-15-renovate-config-with-version-pinning-rules-i7.md` trace report. Structural inheritance from 1.11-1.14 (~90% reasoning-cost savings); mechanical 4-AC template adaptation (vs Story 1.14's 6-AC — Story 1.15 has FEWER ACs). All 6 Task-6 smokes re-verified LIVE byte-identical to iter-84 dev-story record. Adversarial coverage delegated to iter-87 CR per variant-(iii).
- [ ] iter-86: `/bmad-create-story (args: "review")` — FR14n row 7 `traced → sm-verified` post-dev SM requirements-satisfaction verification. FIFTH cumulative ZERO-PATCH post-dev SM precedent CANDIDATE (Stories 1.11/1.12/1.13/1.14 → 1.15). Independent fresh-context Sonnet Explore subagent audits all 4 ACs against committed iter-84 implementation + live filesystem state. AC 1 + AC 4 substrate-verified (ZERO unmet-AC findings expected); AC 2 + AC 3 SCOPE-CARVED to downstream runtime per story scope-carve-outs.
- [ ] iter-87: `/bmad-code-review (args: "2")` — FR14n row 9 `sm-verified → done`. FIFTH cumulative ZERO-PATCH CR precedent CANDIDATE (Stories 1.11/1.12/1.13/1.14 → 1.15). Three-layer adversarial fan-out (Blind Hunter + Edge Case Hunter + Acceptance Auditor) on iter-84 dev-story commit. Triage per convergence-doctrine + ZERO-PATCH-doctrine + adversarial-triage default. Expected outcome: 0 PATCH / 0 DECISION-NEEDED / 5-8 DEFER / ~10-13 DISMISS (lower DEFER vs Story 1.14's 10, due to smaller 4-packageRules surface + no `.release-please-manifest.json` hash-cascade concerns + no "release-please WORKFLOW = Story 13.5" consumer-deferral ambiguity — Renovate App install is a cleaner ops carve-out). After CR: Story State `sm-verified → done`; BMad Status `review → done`; sprint-status `1-15-...: review → done` + `last_updated: 2026-04-21 Story-1-15-done UTC`. Next: iter-88 starts Story 1.16 (`_(no story) → drafted` for `1-16-fork-extension-config-pattern-growth-tier-invariants-fork-md-scaffold`). Epic 1 NOT yet complete; PR #226 stays Draft through Story 1.16 completion.

## BLOCKED

_(none)_

## DONE (current epic — trailing iterations only)

- [x] iter-80: **FR14n row 9 `sm-verified → done` via `/bmad-code-review (args: "2")` — FOURTH cumulative ZERO-PATCH CR precedent** (Stories 1.11 iter-59 + 1.12 iter-66 + 1.13 iter-73 + 1.14 iter-80). Three-layer adversarial fan-out: 0 PATCH / 0 DECISION-NEEDED / 10 DEFER / ~13 DISMISS. 7-iteration `drafted → done` lifecycle held exactly. Story 1.14 complete.
- [x] iter-81: **FR14n row 1 `_(no story) → drafted` via `/bmad-create-story` for Story 1.15.** Story file authored (~350 lines). FOUR ACs + SIX Tasks. TWO new invariants pre-staged: `INV-deps-version-pinning` (AC 4 epic-verbatim) + `INV-renovate-rationale`. Manifest grows 20 → 22. Seven preventative audit layers L1-L7 applied at drafting. Hybrid ATDD-skip ground-(c) variant-(ii)+(iii) PRE-STAGED at § Testing Standards. Sprint-status `1-15-...: backlog → ready-for-dev`. Story State: `_(no story) → drafted`.
- [x] iter-82: **FR14n row 2 `drafted → validated` via `/bmad-create-story (args: "review")` pre-dev SM.** Three parallel fresh-context Explore-subagent audits covering L1-L7. **1 PATCH applied** (L4-class cross-file citation drift): 4 sites cited `architecture.md:3984` but actual source is `epics.md:3984` (Epic 7 Story 7.2 AC delegating Radix pinning to Story 1.15). Fixed all 4 sibling sites per RALPH.md 2026-04-20 drift-survey-scope rule (AC 1 Given-clause narrative + Task 1 JSON packageRules entry 3 description + Dev Notes § Architecture references + References section). Architecture.md § F1 Component library line 240 noted as vendoring-posture source; delegation literal lives in epics.md only. L1/L2/L3/L5/L6/L7 all clean: regex-conformance of both IDs PASS; Task↔AC bidirectional coverage complete (Task 5 explicit no-op protective against cascade-hash); sprint-status wording pinned; mechanical counters (`ls -d apps/*/ packages/*/ | wc -l = 16`; `grep -cE "^\s*id: '" invariants.manifest.ts = 20`; NINTH ATDD-skip / NINTH WAIVED-trace / FIFTH ZERO-PATCH-CR-candidate sequence counter verified); iter-81 git diff shows zero cascade-hash-update sites (only 3 bookkeeping files); Renovate config-shape contract (`$schema` + `extends` + `packageRules` + `groupName` + `rangeStrategy` + `lockFileMaintenance` + regex well-formedness with no overmatching) all clean. Residual count 1 PATCH BELOW 2-PATCH 4-story precedent average Stories 1.11/1.12/1.13/1.14 — configuration-surface + epic-verbatim-ID reduced drafting-time drift. Story State: `drafted → validated`. Sprint-status `last_updated: 2026-04-21 Story-1-15-validated UTC` (both header-comment + data-field sites). Change Log v1.1 row added.

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1-1.14 done; Story 1.15 drafted iter-81; Story 1.16 backlog — 1 more full FR14n lifecycle × Story 1.15 + 1 full × Story 1.16 before EPIC_DONE halt).
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (continues through Epic 1; PR #226 stays Draft until EPIC_DONE per PROMPT_build.md step 5c).
- **Story:** Story 1.15 — Renovate config with version-pinning rules (I7).
- **Story File:** `_bmad-output/implementation-artifacts/1-15-renovate-config-with-version-pinning-rules-i7.md`.
- **Story State:** `validated` — FR14n matrix row 2 complete at iter-82 (1 PATCH; citation drift `architecture.md:3984` → `epics.md:3984` at 4 sites); row 3 (`validated → atdd-scaffolded`) next at iter-83.
- **GitHub Issue:** Story 1.15 at **#39** (https://github.com/tthew/ralph-bmad/issues/39). Parent Epic 1 at **#9** OPEN (closes at EPIC_DONE halt). Story 1.14 at #38 expected CLOSED on PR merge (ralph.py wiring; PR merge waits for EPIC_DONE).
- **PR:** #226 (Draft, empty statusCheckRollup — stays Draft through Epic 1; Draft→Open at EPIC_DONE after Stories 1.15 + 1.16 complete FR14n lifecycle).

## Notes

- **Story 1.15 iter-82 pre-dev SM closed with 1 PATCH** — below 2-PATCH 4-story precedent average (Stories 1.11/1.12/1.13/1.14 all 2). Residual class: L4 cross-file citation drift (`architecture.md:3984` → `epics.md:3984`). Drafting-time audit hit was tight on L1/L2/L3/L5/L6/L7 but L4 missed because the "Architecture line 3984" citation was in the drafted text — a mental-echo of architecture.md being the usual source; actual `@radix-ui/*` pin delegation-to-Story-1.15 literal is in epics.md only. **NEW LESSON carry-forward:** cross-file citations over line-specific content should be verified at drafting-time by `Grep pattern=<literal> path=<cited-file>` — not copy-verified from surrounding context. Applies to every future story where a cross-file citation is given line-specific precision.
- **Carry-forward rules INTO iter-83 `/bmad-testarch-atdd` ATDD-skip:**
  - Skip rationale pre-staged in story § Testing Standards lines 215-225. Three-ground conjunction: (a) substrate-verification-covers-ACs (Task 6 six smokes); (b) no-runner; (c) HYBRID variant-(ii)+(iii) — Renovate runtime + Epic 13 CI gate own AC 2 + AC 3 runtime halves; CR pass owns AC 1 + AC 4 adversarial coverage.
  - Expected: 1 in-situ Change Log row (v1.2) mirroring Story 1.14 iter-76 pattern; Story State `validated → atdd-scaffolded`.
  - Skill preflight will HALT at Step 1.2 (no test runner); Ralph runs autonomously so ATDD-skip is the only autonomous path.
- **Configuration-surface CR forecast (Story 1.15 iter-87):** 0 PATCH / 0 DECISION-NEEDED / 5-8 DEFER / ~10-13 DISMISS expected. Lower DEFER vs Story 1.14's 10 due to: (a) smaller 4-packageRules surface; (b) no manifest-file hash-cascade concerns; (c) no "release-please WORKFLOW = Story 13.5" consumer-deferral ambiguity.
- **Issue Tracking carry-forward.** Story 1.15 at **#39** — current iteration commit trailer uses `Refs #39`. Parent Epic 1 at **#9** OPEN.
