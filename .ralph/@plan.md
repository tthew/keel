# Implementation Plan

## NOW

- [ ] `/bmad-testarch-atdd` — red-phase scaffolds for Story 2.10 per § Story Lifecycle Decision Matrix row `validated → atdd-scaffolded`. Fresh context window. **FR14n three-ground ATDD-skip forecast applies** — ground-(a) substrate-verification envelope covers AC 1 + AC 5 composite-message surface (stub-docker smokes); ground-(b) no test runner wired at substrate level yet (Story 1.16 deferred); ground-(c) external-service-owns-behavior — AC 1 owned by Docker Desktop, AC 2/3 owned by Stories 2.8/2.9 OAuth substrate + Story 2.5 named-volume persistence, AC 4 silent-pass verified by stub-docker, AC 5 composite by fresh-fork-smoke. Forecast: ATDD-skip verdict → TWENTIETH cumulative precedent candidate. Budget: ~50K (orient + skill dispatch + IP/sprint-status update + commit).

## QUEUE (Story 2.10..2.17 → Epic 2 close-out)

- [ ] _(post-Story-2.10-atdd-scaffolded)_ Iterate Story 2.10 through remaining lifecycle: atdd-scaffolded → in-dev (impl landed: new `prereq-check.sh` primitive + 14 shim block-swaps + 3 shim prepend-only + new INV manifest entry + new `docs/invariants/devbox-prereq-check.md` + README/AGENTS.md append + Change Log v1.2) → traced (trace WAIVED forecast — TWENTIETH cumulative precedent candidate; rationale "wrapper envelope substrate-smoked; three-check behaviors external-service-owned") → sm-verified (post-dev SM; two-subagent substrate-citation verification per iter-235 LESSON; v1.3 Change Log entry) → done (CR closure; candidate FIFTH one-pass ZERO-PATCH precedent if pattern holds under novel multi-shim-edit + new-INV + alpine-probe surface; v1.4 Change Log entry).
- [ ] _(post-Story-2.10-done)_ Iterate Stories 2.11..2.17 through full lifecycle — 7 remaining substrate stories spanning shared mode + port-publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-23 per iter-219..246 repeated pre-push confirmation — `gh pr view 230` shows `statusCheckRollup: []`).

## BLOCKED

_(none)_

## ATDD Red Phase

_(none — Story 2.9 closed done at iter-244; Story 2.10 ATDD-skip forecast for iter-247 under ground-(a)+(b)+(c) — TWENTIETH cumulative precedent candidate.)_

## DONE (iter-246 Story 2.10 pre-dev SM — Story State `drafted → validated`; sprint-row unchanged)

- [x] iter-246: **STORY 2.10 PRE-DEV SM REVIEW via `/bmad-create-story (args: "review")` fresh-context per § Story Lifecycle matrix row `drafted → validated`.** Four PATCHes applied against v1.0 (LOW END of 2-5 forecast band — novel surfaces surfaced exactly the predicted categories; the SM review is engaged per iter-219/226/233/244 LESSON "dense pre-dev SM PATCHes → reliable one-pass ZERO-PATCH CR" pattern). **PATCH 1 — SC-4 + SC-7 alpine egress + Renovate posture correction:** removed incorrect `*.docker.io` whitelist-coverage claim (no such entry exists in composed whitelist per Audit D; `prereq-check.sh` runs host-side via `/var/run/docker.sock` passthrough → alpine pull uses host-daemon network, NOT devbox in-container egress namespace → whitelist is irrelevant); removed incorrect "Renovate auto-discovers shell-script image refs" claim (default `docker` manager fileMatch per Story 1.15 baseline scopes Dockerfile + docker-compose.yml only); documented manual `alpine:3.19` tracking at 1.0 with deferred `customManagers` regex-manager FR44 AMEND trajectory. **PATCH 2 — manifest entry count:** corrected "19 → 20 entries" to "29 → 30 entries at 2026-04-23 baseline" per Audit C (actual manifest holds 29 entries spanning Stories 1.3-1.15 substrate + Epic-2 devbox + ralph-halt — NOT only devbox). Applied in Task 5 first bullet + Dev Notes § Project Structure Notes. **PATCH 3 — Task 5 anchor regex precision:** corrected quoted regex from `/^-\s+\*\*\`([A-Z][A-Z0-9-]+)\`\*\*/gm` to `/^-\s+\*\*\`(INV-[a-z0-9]+(?:-[a-z0-9]+)+)\`\*\*/gm` per Audit C reading of `packages/keel-invariants/src/sync-gate.ts:24` (lowercase regex-enforced, not just convention per Story 1.9 iter-7 LESSON); added example compliant anchor bullet. **PATCH 4 — restart.sh + benchmark.sh shim discipline:** `restart.sh` special case documented in Task 4 (no inline `docker info` block — transitively delegates via stop.sh/start.sh per 2026-04-23 Audit B verification L47 comment; prepend-only wire-in for consistency); `benchmark.sh` explicitly flagged OUT-OF-SCOPE under SC-11 (not a pnpm verb per Audit B stray-shim finding; exit-127 command-not-found semantic load-bearing to NFR2 contract; reconciliation deferred as FR44 AMEND); clarifies refactor shape is 14-block-swap + 3-prepend-only (restart.sh + env-check.sh + ralph-*-host.sh via Task 3) NOT 15+2-all-block-swap. Change Log v1.1 recorded. Story header status unchanged ("ready-for-dev" is sprint-row status; Ralph-internal `Story State` flip to `validated` annotated via HTML comment per precedent). Sprint-status unchanged (pre-dev SM gate is Ralph-internal per iter-167/220 precedent). **Forecast calibration vs actuals:** iter-221 `~5-PATCH` ceiling held at 4 (low end of 2-5 forecast band); novel surfaces surfaced exactly predicted categories (egress-whitelist-vs-host-side-runtime confusion; manifest-count staleness 19→29 baseline drift; anchor-regex precision; multi-shim refactor shape 14+3). No RALPH.md lesson append at this gate — audit-driven PATCHes are the expected outcome of the pre-dev SM substrate-citation verification pattern (iter-235 LESSON + Audit C anchor-regex + Audit D egress-whitelist applied); no novel Ralph-flavored gotcha surfaced. **Next iter per matrix row `validated → atdd-scaffolded`:** `/bmad-testarch-atdd` fresh-context — FR14n ground-(a)+(b)+(c) conjunction expected to produce ATDD-skip verdict (TWENTIETH cumulative precedent candidate); ATDD skill outputs skip-rationale annotation rather than red-phase scaffolds. Budget iter-246: ~80K (orient + 4 parallel Explore subagents + story read + 5 surgical Edits + IP/sprint-status-unchanged update + commit + push).

_(iter-234..245 Story 2.9 lifecycle + Story 2.10 drafting iters pruned per Guardrail 2 — see Story 2.9 file § Change Log v1.0–v1.6 for Story 2.9 per-gate details; Story 2.10 § Change Log v1.0 + v1.1 for Story 2.10 drafting + pre-dev SM.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **9/17 stories done** (2.1-2.9) + 8/17 backlog (2.10 `ready-for-dev` at iter-245, `Story State = validated` at iter-246; 2.11..2.17 still backlog).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live OAuth AC 1-4 smokes operator-workstation-deferred per ground-(c) carry-forward (Stories 2.8/2.9 precedent; Story 2.10 applies ground-(c) to Docker-reachability + Stories 2.8/2.9 token-persistence-substrate).
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:119). Host-side shim count: **17 verbs** at Story 2.9 closure; Story 2.10 does NOT add a `<verb>-host.sh` but adds **1 shared primitive** `prereq-check.sh` (non-verb-pattern) → directory count 18 host-side files + 6 in-container primitives + 2 iter-239 `lib/*.sh` retrofits = 26 under `packages/devbox/scripts/` at Story 2.10 landing.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **9 done** (2.1-2.9); 1 in pre-dev-SM-validated (2.10); 7 backlog (2.11..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.10 — Prerequisite check (Docker runtime + Claude auth + gh auth) with pointer errors.
- **Story File:** `_bmad-output/implementation-artifacts/2-10-prerequisite-check-docker-runtime-claude-auth-gh-auth-with-pointer-errors.md`
- **Story State:** `validated` — next iter action per § Story Lifecycle Decision Matrix row `validated → atdd-scaffolded` is `/bmad-testarch-atdd` fresh-context; ATDD skill does NOT flip sprint-status (state is Ralph-internal; mirrors pre-dev SM + trace + post-dev SM gate precedent).
- **GitHub Issue:** Story 2.10 likely unwired (`RALPH_ISSUE_NUMBER` unset at iter-245/246 checks; skip `Refs #<story-issue>` in commits). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2; `statusCheckRollup: []` + `mergeStateStatus: CLEAN` at iter-246 pre-push confirmation; `isDraft: true`).

## Notes

- **iter-246 Story 2.10 pre-dev SM — `drafted → validated` transition complete.** 4 PATCHes applied; forecast 2-5 band held at low end. Novel surfaces confirmed exactly as forecast: (1) egress-vs-host-side-runtime confusion at SC-4/SC-7 (alpine-pull context mental-model drift); (2) manifest-count staleness (19→29 baseline drift from earlier drafting fixed); (3) anchor-regex precision (uppercase-quoted vs lowercase-actual per sync-gate.ts:24); (4) multi-shim refactor shape (14-block-swap + 3-prepend-only vs 15+2-all-block-swap). Candidate trajectory FIFTH one-pass ZERO-PATCH CR precedent intact: v1.1 density now refined (5 ACs + 6 tasks + 17 SCs unchanged in count + narrative updates to SC-4/SC-7/SC-11 + Task 4/5 edits) — matches Stories 2.7/2.8/2.9 post-PATCH density precedent for reliable one-pass CR forecasts.
- **iter-243/244 carry-forward — post-dev SM substrate-citation drift pattern continues to apply at iter-249 post-dev SM gate.** The iter-235 LESSON's prospective framing validated at iter-243/246 (substrate-file + planning-artefact two-subagent verification caught 4 PATCHable claims at iter-246 pre-dev SM — continued application at iter-249 post-dev SM expected). Resolution policy uniform — defer line-number reconciliation (e.g. `docker-compose.yml:170-171` currently at L257 per Audit A) to Story 2.17 Epic 2 close-out per SC-17.
- **iter-244 CR triage yielded 11 deferred items (AR-60..AR-70) all pointing at post-Epic-2 `_lib.sh` refactor.** Story 2.10 is the first story-scoped entry in the emerging `_lib.sh` refactor trajectory — iter-239 retrofit extracted `lib/main-repo-resolver.sh` + `lib/check-mount-source.sh` as non-story-scoped first slice; Story 2.10 ships `prereq-check.sh` as the first story-scoped primitive. Forecast: Story 2.17 Epic 2 close-out may include a broader `_lib.sh` consolidation task to collapse remaining duplicated pre-flight boilerplate across all 17 host-side shims (container-state inspect, `log()` function, `CONTAINER_NAME` fallback) before Epic 3 adds further shims on top.
- **iter-235 carry-forward — two-subagent verification pattern validated AT pre-dev SM gate (iter-246) too.** Originally framed for post-dev SM substrate-citation drift; iter-246 applied 4 parallel Explore subagents for: (A) substrate-citation line-numbers, (B) shim inventory + inline-docker-info block presence, (C) InvariantSchema + anchor-regex + manifest-count + doc-structure precedent, (D) alpine egress + Renovate coverage. All four returned actionable findings (PATCH 1-4). Pattern is substrate-stable ACROSS gate types; Story 2.10 SHOULD re-apply verbatim at its post-dev SM gate.
- **iter-234 carry-forward — killed-before-commit skill recovery: inspect disk state before re-invoking.** Unchanged from iter-243/244/246 carry-forward; valid recovery primitive for fresh-context skill invocations whose primary output is committed artefacts. iter-246 entry confirmed clean (no residual iter-245 artefacts; `git status` only showed untracked `design_handoff_tthew_website/` — orthogonal, ignored).
- **iter-221 carry-forward — pre-dev SM `~5-PATCH` ceiling is independent of draft-SC density.** Story 2.9 landed at low-end 2 PATCHes (iter-235); Story 2.10 landed at 4 PATCHes (iter-246); both within the ceiling. Story 2.10's wider novel-surface forecast (iter-245) → 4 vs 2 vs Story 2.9, confirming wider-novel-surface → slightly higher PATCH density within the same ~5 ceiling.
- **TTY-detect at docker-exec shim (iter-214 LESSON) — Story 2.10 NON-APPLICATION:** prereq-check.sh does NOT `docker exec` into the running container — it `docker run --rm` for the alpine probe (non-interactive, no TTY, short-lived). Tier dispatch is argv-based, not TTY-based. SC-11 wire-ins preserve each shim's own TTY posture unchanged.
- **`<verb>-host.sh` naming convention (iter-199 EXTENDED through iter-246) — Story 2.10 DEVIATION (by design):** Story 2.10 ships `prereq-check.sh` (NOT `<verb>-host.sh`) because it's a shared primitive called by other shims, not a pnpm-verb wrapper. The `pnpm devbox:prereq:check` standalone verb IS wired (Task 2) but the script itself is the primitive. Naming follows Story 2.3/2.4 primitive pattern (`reload-egress.sh`, `whitelist.sh`). Post-Story 2.10 the host-side-file family is 17 `<verb>-host.sh` + 1 primitive (`prereq-check.sh`) + 6 Story 2.3/2.4 in-container primitives + 2 iter-239 lib helpers (`lib/main-repo-resolver.sh`, `lib/check-mount-source.sh`).
- **iter-246 audit-findings carry-forward to post-dev SM + CR gates:** (1) restart.sh transitive-delegate pattern (prepend-only; no block-swap) may surface CR Acceptance-Auditor attention; dev-agent should commit restart.sh with an inline comment referencing the Task-4 special case. (2) benchmark.sh OUT-OF-SCOPE clarification must survive CR adversarial triage; if CR Blind Hunter flags benchmark.sh as "missing prereq-check coverage", the SC-11 OUT-OF-SCOPE text is the canonical defense. (3) Manifest entry count drift (19→29 baseline) is a cumulative signal — future Epic 2 stories (2.11..2.17) MUST verify count at pre-dev SM via Audit C equivalent pattern; drafters using cached "last count" will land stale. (4) Alpine egress non-applicability doctrine crystallizes a Story 2.10 LESSON: host-side-script `docker run` operations are orthogonal to devbox in-container egress rules; only in-container `docker exec`-initiated traffic (or processes running INSIDE the devbox namespace) are subject to the whitelist. This should generalize to Epic 3 Ralph-in-container tools that may spawn docker-subprocesses — document in RALPH.md if Story 3.x surfaces the same confusion.
