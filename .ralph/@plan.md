# Implementation Plan

## NOW

- [ ] **AI-4 — `clean.sh:80-88` backend detection over/under-classification.** Top-of-QUEUE per § Story Lifecycle row `fixes-pending`. Evidence: line 86 `if [[ -f /.dockerenv ]]; then echo B; return; fi` — `/.dockerenv` exists in BOTH true DinD (backend A) and host-socket-passthrough (backend B); using its presence alone over-classifies a backend-A container as B → false-positive `--with-volumes` lock-out. Conversely, when `docker info --format '{{.Name}}'` at line 82 fails (transient daemon hiccup), the case-statement falls through, `/.dockerenv` is absent on host, and detection defaults to `A` at line 87 — the LESS-protective branch on a backend-B host. Fix (both directions): drop the `/.dockerenv` arm OR treat it as "unknown — require explicit `--force-backend-b`"; on `docker info` failure, exit 8 OR fail-safe to B. Destructive-adjacent — use iter-205 extract-and-mirror harness pattern. ~medium.

## QUEUE (Story 2.6 lifecycle → Epic 2 Story 2.7..2.17 → Epic 2 close-out)
- [ ] AI-5 `env-check.sh:24-26` fragile relative-path resolution — use `git rev-parse --show-toplevel` with fallback or honour explicit `KEEL_DEVBOX_REPO_ROOT` envvar.
- [ ] AI-6 `env-check.sh:82-87` inline-`#`-strip order corrupts quoted values containing ` #` — strip inline-comment BEFORE quote-removal OR track quoting state.
- [ ] AI-7 `env-check.sh:70-91` strip `\r` from CRLF `.envrc` (`value="${value%$'\r'}"` after line 80) so tmpfs shape-regex doesn't fail with cryptic invisible-`\r` message.
- [ ] AI-8 `start.sh:28,71` `unset COMPOSE_PROJECT_NAME` (or use `docker compose ps -q devbox` for container ID) so polling doesn't break under operator-shell `COMPOSE_PROJECT_NAME` override. Apply same fix to stop/restart/shell/attach/status/logs/monitor-host/whitelist-host.
- [ ] AI-9 `whitelist-host.sh:32` add `--user dev` matching `shell.sh:34` pattern so mutations don't run as root in container.
- [ ] AI-10 `whitelist-host.sh:32` TTY-detect (`[[ -t 0 ]] && tty_flag="-it" || tty_flag="-i"`) so non-TTY CI invocation doesn't fail with "the input device is not a TTY". Apply same fix to `monitor-host.sh:45`.
- [ ] AI-11 `restart.sh:18-21` poll `docker inspect ... .State.Status` until `exited` (or container disappears) before invoking start.sh, bounded by 10s timeout — otherwise `docker compose stop`-returns-when-SIGTERM-sent race causes start.sh to see lingering OLD container and report "started" prematurely.
- [ ] _(post-AI-11; QUEUE empty)_ `/bmad-code-review (args: "2")` — CR-closure gate (forecast: ZERO-PATCH per Epic-2 3-story precedent: 2.3 iter-171, 2.4 iter-183, 2.5 iter-197). 8 PATCH remain (AI-4..AI-11) after iter-207.
- [ ] _(post-Story-2.6-done; through Story 2.17-done)_ Iterate Story 2.7..2.17 through the full lifecycle — 11 remaining substrate stories spanning ralph-auto-start + claude OAuth + gh OAuth + prereq-check + shared mode + port-publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-22 per iter-197..iter-204 pre-flight — "no checks reported on the 'feat/epic-2-packaged-devbox' branch").

## BLOCKED

_(none)_

## ATDD Red Phase

_(none — Story 2.6 ATDD-skipped at iter-200 per § Story Lifecycle `validated → atdd-scaffolded` (skip branch). 16th-cumulative FR14n ATDD-skip precedent. iter-202 trace gate WAIVED confirms the ATDD-skip-trace-WAIVED pairing — SEVENTEENTH cumulative pairing across Epic 1 + 2.)_

## DONE (Story 2.6 lifecycle — keep CR-drain chain, iter-199..iter-203 pre-`fixes-pending` pruned iter-207)

- [x] iter-207: **AI-3 RESOLVED — `monitor-host.sh:41` misleading exit-3 hint rephrased + docstring honesty.** Runtime log-line rephrased from `"in-container primitive 'monitor.sh' missing — rebuild via 'pnpm devbox:build'"` → `"monitor.sh missing from packages/devbox/scripts/ — restore via 'git checkout packages/devbox/scripts/monitor.sh'"` (iter-204 AI-3 CR spec, primary path). Docstring exit-3 entry (`monitor-host.sh:12-18`) rewritten to catalog the bind-mount rationale — `monitor.sh` is not baked into the image; `docker compose build` does NOT restore a missing repo file; `git checkout` does. Inline comment at `monitor-host.sh:42-44` mirrors the docstring rationale at the call site. Iteration-env-safe extract-and-mirror behavioural smoke (14 assertions): 6 drift-guards (new hint present verbatim / old rebuild hint removed / docstring mentions `git checkout` / docstring disclaims `docker compose build` / exit-3 code preserved / file-check branch still exits 3) + 6 harness cases (absent→rc 3 + new hint + no "rebuild" leak + no "pnpm devbox:build" leak; present→rc 0 + no log emitted) + 1 buggy-before distinctness check + 1 sibling-unchanged regression guard (exit-9 hint untouched). `bash -n monitor-host.sh` ✓. No invariant sourcePath touched (monitor-host.sh is not in `keel-invariants` manifest) — no sync-gate refresh. Story file v1.5 Change Log appended; § Review Findings AI-3 marked `[x]` RESOLVED. 8 PATCH remaining (AI-4..AI-11); iter-208 drains AI-4 (`clean.sh:80-88` backend detection over/under-classification).
- [x] iter-206: **AI-2 RESOLVED — `start.sh:84-87` fatal-state vs unhealthy hint drift + docstring honesty.** Rephrased-hint path (SC-5 exit-code set preserved). Case arm split at `start.sh:93-107`: `unhealthy)` keeps the "still running" log + logs hint; `exited|dead|removing|paused)` gets a new log line explicitly saying "not running" + "may show last output before exit". Docstring lines 20-28 rewritten to honestly catalog the three exit-11 sub-cases (timeout / unhealthy / fatal) with line-anchor pointers. Iteration-env-safe behavioural smoke — 12 assertions all green. `bash -n start.sh` ✓. No invariant sourcePath touched.
- [x] iter-205: **AI-1 RESOLVED — `clean.sh:105` non-TTY `read` EOF handler.** Option A applied: `if ! IFS= read -r answer; then log "no input — aborting (stdin closed / non-interactive); use --yes to auto-confirm"; exit 0; fi`. Iteration-env-safe behavioural smoke (4 cases) all green. `bash -n clean.sh` ✓.
- [x] iter-204: **`/bmad-code-review (args: "2")` — Story 2.6 CR opener, `sm-verified → fixes-pending`.** Three-layer adversarial fan-out. Acceptance Auditor: ZERO PATCH-or-higher AC deviations. Code-quality triage: 11 PATCH (AI-1..AI-11) + 7 DEFER (AR-13..AR-19) + 6 DISMISS. PATCH fan-out across 6 of 13 scripts (clean ×2, start ×2, env-check ×3, monitor-host ×1, whitelist-host ×2, restart ×1).

## Context

- **Phase:** 4-implementation — Epic 2 at 5/17 stories `done` (2.1 + 2.2 + 2.3 + 2.4 + 2.5) + 12/17 remaining; Story 2.6 fixes-pending (in-progress) + 11 backlog (2.7..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.6 live lifecycle smokes operator-workstation-deferred per Story 2.4 SC-17 + Story 2.5 iter-187 precedent. PATCH-drain quality chain for shell-script edits: `bash -n` per touched script + iteration-env-safe behavioural smoke per AI fix + sync-gate (when invariant sourcePaths touched) + typecheck + lint + format:check + docker compose parse-smoke (for compose-touching fixes only).
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Story 2.5 substrate live. Story 2.6 PATCH drain is host-side script-only — no Dockerfile changes (SC-25 scope-boundary forbids; AR-9 escalation path conditional + not triggered).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; 5 done (2.1-2.5); Story 2.6 fixes-pending (in-progress); 11 backlog (2.7..2.17). Epic 2 closes at Story 2.17 (PR #230 Draft→Open transition at that point).
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.6 — host-side `pnpm devbox:*` CLI surface. Scope-owner for AR-7 `/run` relocation (SC-19, conditional — NOT triggered) + AR-9 `/etc/*` chown (SC-20, conditional — NOT triggered) + AR-10 pre-Story-2.5 volume migration docs (SC-21, LANDED iter-201) + AR-11 envvar shape validation (SC-14, LANDED iter-201) + AR-12 COMPOSE_PROJECT_NAME docs polish (SC-22, LANDED iter-201). NEW at iter-204: scope-owner for AI-1..AI-11 PATCH drain.
- **Story File:** `_bmad-output/implementation-artifacts/2-6-host-side-pnpm-devbox-cli-surface.md`
- **Story State:** `fixes-pending` — transitioned at iter-204 via `/bmad-code-review (args: "2")` opener. 11 PATCH action items recorded; 3/11 drained (AI-1 iter-205, AI-2 iter-206, AI-3 iter-207); 8 remaining (AI-4..AI-11). Iter-208+ continues one PATCH per iteration per § Story Lifecycle row `fixes-pending` top-of-QUEUE. On QUEUE drain, re-run `/bmad-code-review (args: "2")` for closure gate (forecast: ZERO-PATCH per Epic-2 3-story precedent → direct `done`).
- **GitHub Issue:** Story 2.6 TBD (RALPH_ISSUE_NUMBER unset in this iter env). Epic 2 → #10 (Ralph closes on EPIC_DONE halt).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **CR opener PATCH-count scales linearly with script-count, not story complexity (iter-204 LESSON):** Story 2.1 = 1 PATCH (Dockerfile + compose), Story 2.4 = 5 PATCH (1 in-container script), Story 2.5 = 6 PATCH (compose + entrypoint + Dockerfile), Story 2.6 = **11 PATCH (13 host-side scripts)**. Carry-forward rule for Stories 2.7+ user-facing-CLI class: if the surface adds ≥10 new scripts, expect 8-12 PATCH at CR opener (not the 4-6 baseline). Drain-iter forecast scales similarly: Story 2.6 forecast 12-iter drain (11 PATCH iters + 1 closure re-run iter), longer than Story 2.4's 4-iter or Story 2.5's 7-iter.
- **Acceptance Auditor ZERO-FINDING holds even at the upper-PATCH-count CR (iter-204 LESSON):** Acceptance Auditor returned ZERO PATCH-or-higher AC deviations despite 11 PATCH from Blind + Edge Hunters. Strong evidence that pre-dev SM ZERO-FINDING + post-dev SM ZERO-FINDING reliably catches AC-class issues; CR-opener PATCH counts are code-quality issues (shell-script edge cases, error-handling gaps), NOT requirement-satisfaction issues. Carry-forward rule: when pre-dev + post-dev SM both close ZERO-FINDING, expect Acceptance Auditor at CR-opener to also close ZERO; CR PATCH count then reflects code-quality surface size.
- **Single-iter landing precedent — user-facing-CLI class (iter-201 formalises, iter-203 carries forward, iter-204 RE-VALIDATES):** Story 2.4 iter-174 + Story 2.5 iter-187 + Story 2.6 iter-201 all single-iter landings. iter-204 CR confirms the single-iter landing did NOT compromise quality at the AC level (Acceptance Auditor ZERO) — only the code-quality CR layers surfaced PATCH backlog. Carry-forward: future stories MAY single-iter-land but should expect proportional PATCH-drain after.
- **Cross-source semantic-drift audit rule (iter-199 LESSON, iter-201 VALIDATED, iter-202 TRACE-RECORDED, iter-203 SM-VERIFIED, iter-204 CR-VERIFIED):** SC-15 pinned PRD `:494` as authoritative for `pnpm devbox:monitor` semantic. iter-204 Acceptance Auditor explicitly verified the SC-15 reconciliation — "almost flagged AC 7 monitor semantic, verified intentional via SC-15 PRD-authoritative pin". Pattern works end-to-end across 4 lifecycle gates. Carry-forward rule for Stories 2.7 + 2.8 + 2.9 + 2.12 + 2.15 remains live.
- **`<verb>-host.sh` host-side shim naming convention (iter-199 LESSON, iter-201 EXTENDED, iter-202 TRACED, iter-203 SM-VERIFIED, iter-204 CR-VERIFIED):** Story 2.6 ships `monitor-host.sh` + `whitelist-host.sh` as the canonical host-side shim naming pattern. iter-204 PATCH drain (AI-3, AI-9, AI-10) all touch the shim layer — the convention exposes consistent fix surface. Stories 2.7 (Ralph TUI wrapper), 2.8 (claude wrapper?), 2.9 (gh wrapper?), 2.12 (ssh wrapper?) candidates for the same pattern when in-container primitives exist.
- **AR-12 docs-drift was only HALF the story (iter-204 LESSON):** Story 2.5 iter-197 deferred AR-12 as "cosmetic, COMPOSE_PROJECT_NAME only changes verification-command volume FQN prefix". iter-201 landed the docs polish. iter-204 surfaced AI-8: the SAME `COMPOSE_PROJECT_NAME` override breaks RUNTIME container polling in start.sh + sibling scripts (not just docs). Carry-forward rule: when an Edge Hunter flags an env-override drift in static docs, ALSO check whether the override breaks runtime script behaviour — the runtime equivalent often surfaces one CR cycle later.
- **Within-epic § Story Lifecycle drain (iter-204 fixes-pending → iter-205+ AI-1..AI-11 → closure re-run):** Story 2.6 `fixes-pending` + 11 PATCH QUEUE → no halt required. iter-205 drains AI-1 (`clean.sh:105` non-TTY read abort). Each PATCH = one iteration. After QUEUE drain, re-run `/bmad-code-review (args: "2")` for closure gate. Epic 2 stays `in-progress`.
