# Implementation Plan

## NOW

- [ ] `Run /bmad-code-review (args: "2")` adversarial CR via three-layer Ralph-hosted triage for Story 2.11 — Blind Hunter (`bmad-agent-architect` diff-only) + Edge Case Hunter (`bmad-tea` diff+project-read) + Acceptance Auditor (`bmad-agent-dev` diff+spec+INV). Per § Story Lifecycle Decision Matrix row `sm-verified → done | fixes-pending`. Forecast per iter-253 LESSON (chain-reset post-Story-2.10): 0-1 first-class PATCH + 2-4 second-class operator-edge DEFERs; Story 2.11's THREE novel vectors (resolver-function extension + compose parameterisation + env-check orphan warning) align with forecast band. Diff range `aa17c03..HEAD` across Story 2.11 branch lifetime iter-254..iter-259 (~600 lines across 30 files — lighter than Story 2.10's iter-245..iter-250 ~2035 lines due to smaller novel surface).

## QUEUE (Story 2.11..2.17 → Epic 2 close-out)

- [ ] _(after Story 2.11 `done`)_ Iterate Stories 2.12..2.17 through full lifecycle — 6 remaining substrate stories spanning loopback-bound port publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-24 per iter-219..259 repeated pre-push confirmation — `gh pr view 230` returns `statusCheckRollup: []`).

## BLOCKED

_(none — iter-258 step-5 push RECOVERED all 5 previously-banked commits. iter-259 anticipates similar SSH :22 fluctuation; recovery-banking pattern remains load-bearing across iter-249..258 precedent. Post-push noise: GitHub reports 2 moderate-severity Dependabot vulnerabilities on default branch (unrelated to Epic 2 work; tracked at https://github.com/tthew/ralph-bmad/security/dependabot). PR #230 still has no CI configured (statusCheckRollup: []); HTTPS :443 gate not applicable.)_

## ATDD Red Phase

_(none — Story 2.11 ATDD-skipped at iter-256 per grounds (c)+(ii)+(iii); iter-258 trace gate WAIVED; iter-259 post-dev SM verification closed `traced → sm-verified` with ZERO-PATCH AC-satisfaction + 2 MINOR citation DEFERs per SC-17.)_

## DONE (iter-259 Story 2.11 post-dev `/bmad-create-story (args: "review")` — Story State `traced → sm-verified`)

- [x] iter-259: **STORY 2.11 POST-DEV SM VERIFICATION — Story State `traced → sm-verified`; ZERO-PATCH AC-satisfaction + 2 MINOR citation-drift DEFERs (both SC-17 close-out reconciliation deferred).** Two-subagent verification pattern per iter-235 LESSON dispatched in parallel: **(1) AC-satisfaction verifier** (`bmad-agent-dev`, Amelia) — verdict `ZERO-PATCH` (AC 1–4 all SATISFIED; evidence pinned to resolver L148-155 per-fork branch + L157-192 shared branch, compose L51/L64 interpolation, env-check.sh L194-233 orphan probe + verbatim Case A/B strings, AGENTS.md L163-173 H3 + guardrails at L169-171); **(2) substrate-citation drift detector** (`bmad-agent-architect`, Winston) — verdict `2-NEW-DRIFT` (both MINOR): D1 `prereq-check.sh:34 → actual L65` ~31-line drift (content intact; iter-257 resolver-source block top-insertion shifted lines); D2 `architecture.md:547 → § Project organisation source-tree block` (content for "upstream cc-devbox `/Users/tthew/Development:/workspace:delegated` pattern" does NOT resolve at L547 under any phrasing in current `architecture.md`; cited at AC 2 + 6 other story sites L17/L132/L181/L188/L247/L259/L339; story-file authoring drift from create-story iter-254 phase — AC 2 impl intent satisfied via AGENTS.md + INV doc + README in-tree; not a behavioural-satisfaction regression). **Both drifts DEFERRED per SC-17 Epic 2 close-out reconciliation policy** (within iter-250 carry-forward forecast band 1-3 drifts per iter; pre-declared deferred class per iter-252/254/256/258 precedent). Story file Change Log v1.3 authored (~90-line entry) documenting AC-satisfaction ZERO-PATCH + 2-NEW-DRIFT-DEFERRED + Story State transition; Status HTML comment updated with sm-verified state + next-iter CR pointer. Sprint-status.yaml row UNCHANGED (per iter-202 precedent: post-dev SM verification does NOT flip sprint-row; only pre-dev `drafted → ready-for-dev`, dev-story `ready-for-dev → review`, and CR `review → done` transitions flip). Budget consumed iter-259: ~55K tokens (orient ~17K + 2 parallel adversarial subagents ~20K [Amelia 46K agent budget, Winston 37K agent budget] + spot-check architecture.md + Change Log v1.3 write ~5K + IP rewrite + RALPH.md signpost + commit ~13K). Well inside ~117K execution budget.

_(iter-253..258 Story 2.10 closure + Story 2.11 create-story / pre-dev SM / ATDD-skip / dev-story / trace iters pruned per Guardrail 2 — see Story 2.11 § Change Log v1.0/v1.1/v1.2/v1.3 + RALPH.md Signposts iter-253..259 for per-gate detail.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **10/17 stories done** (2.1-2.10) + 1/17 in review (2.11 at state `sm-verified`) + 6/17 backlog (2.12..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered at iter-248 9/9 stub-docker harness + iter-252 9/9 mock-docker PATH-shim harness + iter-257 6/6 resolver functional smokes; iter-259 adversarial SM verification covered static-read AC-satisfaction + citation drift.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:119). Host-side shim count: **18** at iter-259 (unchanged — Story 2.11 extends 18 existing shims with resolver invocation).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **10 done** (2.1-2.10); 1 in-review at state `sm-verified` (2.11); 6 backlog (2.12..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.11 — Per-fork vs shared devbox mode (`KEEL_DEVBOX_SHARED`)
- **Story File:** `_bmad-output/implementation-artifacts/2-11-per-fork-vs-shared-devbox-mode-keel-devbox-shared.md`
- **Story State:** `sm-verified` — Story 2.11 post-dev `/bmad-create-story (args: "review")` gate landed at iter-259 (ZERO-PATCH AC-satisfaction + 2 MINOR citation-drift DEFERs). Next iter `/bmad-code-review (args: "2")` transitions `sm-verified → done` (or `fixes-pending`).
- **GitHub Issue:** Story 2.11 likely unwired (`RALPH_ISSUE_NUMBER` unset at iter-259 orient; skipping `Refs #<story-issue>` in commits per § Issue Tracking guard). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out, NOT iter-259).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — confirmed `{"isDraft":true,"mergeStateStatus":"CLEAN","state":"OPEN","statusCheckRollup":[]}` at iter-259 orient. `gh pr checks 230` confirms no CI configured (`statusCheckRollup: []`) so HTTPS-timeout is not a CI gate.

## Notes

- **iter-259 Story 2.11 post-dev SM verification — ZERO-PATCH AC-satisfaction + 2 MINOR drift DEFERs.** Two-subagent parallel fan-out (`bmad-agent-dev` AC-verifier + `bmad-agent-architect` drift-detector) per iter-235 LESSON. Pattern holds: post-dev SM gate at states `traced → sm-verified` is structurally lighter than pre-dev `drafted → validated` (Winston solo fan-out for pre-dev per iter-255; Amelia+Winston two-subagent fan-out for post-dev per iter-235+259). NOVEL LESSON candidate: the two-subagent pattern is strictly ADDITIVE not overlapping — AC-verifier reads impl-vs-ACs; drift-detector reads story-file citation anchors vs HEAD; neither substitutes for the CR three-layer adversarial fan-out (Blind + Edge + Auditor) at state `sm-verified → done`. Future-Ralph rule: at post-dev SM gate, always dispatch the TWO-subagent pattern (NOT the THREE-layer CR pattern — overkill for non-adversarial AC-satisfaction lens); at CR gate, escalate to THREE-layer adversarial pattern.
- **iter-259 NOVEL LESSON — D2 architecture.md:547 drift has 7 sites in the story file (L17/L132/L181/L188/L247/L259/L339).** Defer-vs-fix decision: defer per SC-17 Epic 2 close-out reconciliation policy (consistent with iter-252/254/256/258 pre-declared deferred class; iter-250 carry-forward forecast band 1-3 drifts per iter). But the 7-site count is notable — single citation claim drifted across many sites, not isolated. Pattern carry-forward for Stories 2.12..2.17 pre-dev + post-dev review gates: grep substrate anchors (`architecture.md:NNN`, `prd.md:NNN`, `epics.md:NNN`) appearing ≥3 times in a story file and re-verify ONCE at post-dev SM review; single-verification cascades to N-site coverage.
- **iter-258 partial-waive trace-gate LESSON carry-forward unchanged:** future-Ralph rule that partial-waive at trace gate requires coverage-class vocabulary (not present at 1.0) holds — full-waive preferred for consistency with N-th-cumulative-precedent pattern until Epic 13 lands `packages/devbox/tests/`.
- **iter-257 NOVEL LESSON carry-forward — manifest rebuild required after adding new InvariantSchema entry.** `pnpm keel-invariants:check` runs against compiled `dist/check.js`; MUST invoke `pnpm --filter @keel/keel-invariants build` before sync-gate reflects new entry. Pattern carry-forward for Stories 2.12..2.17 + any future story adding a manifest entry: ALWAYS rebuild before verifying sync-gate. Promoted to RALPH.md § Lessons at iter-258; preserved unchanged at iter-259 post-dev SM (no manifest edits this iter).
- **iter-257 NOVEL LESSON carry-forward — shared-mode operator-override INTENTIONAL ignore (SC-4) is load-bearing.** Verified at iter-259 AC-satisfaction evidence: resolver L157-192 shared branch hardcodes `KEEL_DEVBOX_CONTAINER_NAME_RESOLVED=keel-devbox-shared` regardless of operator `KEEL_DEVBOX_CONTAINER_NAME`. Without this SC-4 enforcement, AC 2's "fork A + fork B attach to SAME container" would fail silently when operator A customises the per-fork container name. Promoted to RALPH.md § Lessons at iter-258; preserved unchanged at iter-259.
- **iter-255 Story 2.11 pre-dev review gate — "substrate-truth corrections" as the dominant review-finding class for Stories N≥11** (carry-forward). Pattern: every enumeration claim (shim list, recompute inventory, anchor format, Docker primitive behaviour) MUST be grep/ls/doc-verified at create-story OR pre-dev-review time. Forecast for Stories 2.12..2.17: 1-3 CRITICAL "inventory correction" findings per pre-dev review; ENHANCEMENT class reduces as writing pattern stabilises.
- **iter-253 Story 2.10 CR re-run closure — SECOND-class operator-edge DEFER pattern** (carry-forward). PATCH iterations close FIRST-class adversarial findings but commonly surface SECOND-class operator-edges in the same surface the prior bug had hidden. Story 2.11's resolver-function + compose parameterisation + env-check orphan warning constitute "multi-site novel runtime probe" surface; expect 2-4 second-class DEFERs at CR re-run (Docker Desktop wake-race, Podman-compat semantics, compound-failure short-circuit doctrine carry-forward forecast classes).
- **iter-252 Story 2.10 PATCH-1 — `set -e`+`if cmd; then` rc-suppression LESSON applied at iter-257.** Task 4 env-check orphan-container probe uses canonical `rc=0; docker inspect … || rc=$?; case "${rc}" in …` pattern; preserved unchanged at iter-258 trace + iter-259 post-dev SM (rc-capture mechanics re-verified by AC-satisfaction Amelia + drift-detector Winston at their respective lenses — neither surfaced a rc-suppression hazard, consistent with iter-257 landing).
- **iter-252 GOTCHA — iter-env `/tmp` is `noexec`** (carry-forward, applies to Story 2.13 healthcheck emitter + Story 2.14 legacy-branch cleanup; NOT applicable to Story 2.11 which does not use mock-binary PATH shims).
- **iter-244/246 audit-findings carry-forward** unchanged from iter-247..258: (1) restart.sh transitive-delegate prepend-only (applied at iter-257: restart.sh invokes resolver for fail-fast inspect consistency even though stop.sh + start.sh each re-run it downstream); (2) benchmark.sh OUT-OF-SCOPE clarification (SC-11); (3) Manifest entry count drift (29 → **30** at iter-257 after `INV-devbox-mode` landing); (4) Alpine egress non-applicability doctrine. Manifest count will not move again until next story adds a new invariant.
- **Substrate-citation drift cumulative forecast for Stories 2.11..2.17 — updated at iter-259:** iter-259 confirmed 2 MINOR drifts for Story 2.11 (D1 `prereq-check.sh:34 → L65` + D2 `architecture.md:547 → 7-site misplaced-content`). Cumulative Epic 2 close-out reconciliation forecast updated: ~18-27 cumulative line-number drifts + 1-2 substantive citation-claim misplacements (e.g., D2 architecture.md:547). Prior estimate ~15-25 at iter-258 → now ~18-27 including D2's 7-site cascade. SC-17 scope holds: single reconciliation pass at Story 2.17 landing sweeps cumulative drift across Epic 2 story files + architecture.md + prd.md. Deferred-work.md may gain AR-75 (architecture.md § Workspace mount location verification) as a separate entry if D2 persists at Story 2.17.
