# Implementation Plan

## NOW

- [ ] `/bmad-create-story (args: "review")` — post-dev SM requirements-satisfaction verification of Story 2.4 per FR14n `traced → sm-verified` transition. Expected ZERO-PATCH-with-operator-workstation-defer-acknowledgment variant (mirrors Story 2.3 iter-160 precedent on infrastructure-class stories with deferred live smokes). Three-layer parallel fresh-context Sonnet fan-out against the 5 ACs × ~360 LOC substrate diff: each Sonnet verifies AC-to-implementation reduction with explicit acknowledgement of the backend-B live-smoke defer for Subtasks 10.1–10.8 + 9.2. May produce `sm-fixes-pending` entries if any of the 5 ACs don't reduce to substrate commitments — expected outcome: ZERO-PATCH because (a) iter-175 trace already verified each AC has STRONG substrate evidence with file-presence + content-signal + line numbers + iteration-env executable smokes for AC 1/2/3/4/5 (parity + dispatcher + regex + multi-error + gitignore); (b) the only non-substrate-covered ACs are the live container reload smokes which are explicitly deferred per Subtask 10.10. Budget projection: ~20K tokens (template inheritance from 2.3 iter-160 ~95%; mechanical adaptation).

## QUEUE (Story 2.4 → Story 2.5..2.17 → Epic 2 close-out)

- [ ] _(conditional, post-Story-2.4 `sm-verified`)_ `/bmad-code-review (args: "2")` — pre-selects "Create action items"; FR14n `sm-verified → done` OR `fixes-pending`. Three-layer Ralph-hosted adversarial fan-out (Blind + Edge + Auditor) against cumulative Story 2.4 substrate diff. Per iter-175 forecast envelope `(0 carve-out × 3) + (backend-B defer × 3) + (~360 LOC / 100) = ~6.6 ceiling` → 2–4 PATCH opener projected; chain length 2–4 iter (tighter than 2.3's 10-iter chain because SC-14 parity PASS in iteration env de-risks byte-identity).
- [ ] _(conditional, post-Story-2.4 done)_ `/bmad-create-story` for Story 2.5 (container hardening: non-root user `dev` + `cap_drop: [ALL]` preserving `cap_add: [NET_ADMIN, NET_RAW]` + `security_opt: [no-new-privileges:true]` + tmpfs + named volumes for OAuth) per § Cross-epic transition within-epic path. 13 backlog stories remaining in Epic 2 (2.5..2.17) after 2.4 done.
- [ ] _(conditional, post-Story-2.17 done)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge. Epic 2 completion.
- [ ] _(conditional, post-Epic-2 merged)_ § Cross-epic transition → Epic 3 Story 3.1 OR halt EPIC_DONE awaiting merge.

## BLOCKED

_(none — Story 2.4 trace landed iter-175 WAIVED; SM review queued iter-176; no blockers.)_

## ATDD Red Phase

_(none — Story 2.4 ATDD-skipped at `validated → atdd-scaffolded` per 14th-cumulative-precedent (carried iter-173). Red-phase scaffolds not produced; no tests-to-green tracking. Downstream Epic 13 harness + CR adversarial coverage substitute per hybrid ground-(c)+(ii)+(iii).)_

## DONE (current story — Story 2.4 lifecycle; prune deeper when landing)

- [x] iter-175: **`/bmad-testarch-trace (args: "yolo")` Story 2.4 — FR14n Story State `in-dev → traced`; Gate: WAIVED (FOURTEENTH cumulative trace-WAIVED precedent + FIFTEENTH ATDD-skip-trace-WAIVED co-application pairing).** Four trace artefacts produced under `_bmad-output/test-artifacts/traceability/`: `2-4-coverage-matrix.json` (Phase 1 — 5 P2 ACs; 0% automated; STRONG substrate evidence per AC with file + line-number + iteration-env smoke citations), `2-4-e2e-trace-summary.json` (Phase 2 schema-v1 rationale), `2-4-gate-decision.json` (WAIVED grounds (a)+(b)+(c)+(d) variant-(ii)+(iii)), `2-4-whitelist-source-of-truth-…-cli.md` (full report). **Class distinction vs Story 2.3:** unlike Story 2.3's ZERO iteration-env runtime assertions (daemon + kernel + log-tailer all require live container), Story 2.4 has MORE iteration-env executable evidence — SC-14 dual-composer parity smoke PASSED byte-identical + dispatcher exit-2 contract + validate_domain single-arg regex rejection + validate_sources 4-error multi-collection + cmd_list source-attribution + `git check-ignore -v` rule-fires. Only Subtask 10.1–10.8 full reload chain against running container + Subtask 9.2 `pnpm --filter @keel/devbox run` listing remain operator-workstation-deferred per backend-B. Story file Status `in-dev → traced`; Change Log v1.4 appended (iter-175 row). RALPH.md iter-175 signpost appended (precedent-holder entry for 14th trace-WAIVED + user-facing-CLI class distinction). Budget consumed iter-175: ~35K tokens. PR #230 stays Draft (Epic 2 closes at Story 2.17).
- [x] iter-174: `/bmad-dev-story` Story 2.4 landed in single iteration; FR14n Story State `validated → in-dev`; 8 files touched per SC-19 boundary (NEW whitelist.sh ~360 LOC + start-egress.sh extension + whitelist.default.txt header + package.json + .gitignore + README.md + AGENTS.md H3 + sprint-status); SC-14 parity smoke PASSED; iteration-env safe smokes all pass; live container smokes 10.1–10.8 + 9.2 deferred.
- [x] iter-173: `/bmad-create-story (args: "review")` pre-dev SM validation of Story 2.4; FR14n Story State `drafted → validated`; 1 CRITICAL + 4 ENHANCEMENTS applied; ATDD-skip adopted as 14th-cumulative precedent.
- [x] iter-172: `/bmad-create-story` drafted Story 2.4 — whitelist.sh user-facing CLI; Status `backlog → ready-for-dev`; within-epic auto-advance.

## Context

- **Phase:** 4-implementation — Epic 2 at 3/17 stories `done` (2.1 + 2.2 + 2.3) + 1/17 `review` (2.4 iter-174; traced iter-175) + 13/17 `backlog` (2.5..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.4 live smokes (Task 10.1–10.8 + 9.2) operator-workstation-deferred per Subtask 10.10 + Story 2.1 iter-127 precedent; Task 10.9 dual-composer parity smoke ran in iteration env (PASSED, byte-identical).
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Story 2.4 required NO Dockerfile edits (consumes existing baked tooling — `bash`, `flock`, `diff`, `grep`, `sed`, `awk`, `sort`, `mv`, `mktemp`).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; 2.1 + 2.2 + 2.3 done; 2.4 traced (review for sprint-status); 2.5..2.17 in `backlog`.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2; PR #230 transitions to Open at Story 2.17 completion).
- **Story:** Story 2.4 — Whitelist source-of-truth + `pnpm devbox:whitelist` atomic-reload CLI.
- **Story File:** `_bmad-output/implementation-artifacts/2-4-whitelist-source-of-truth-pnpm-devbox-whitelist-atomic-reload-cli.md`.
- **Story State:** `traced` — next iteration transitions `traced → sm-verified` via `/bmad-create-story (args: "review")` OR `sm-fixes-pending` if ACs don't reduce.
- **GitHub Issue:** Story 2.4 → _(not yet created; ralph.py creates on iter-176 entry via In Progress transition)_; Epic 2 → #10.
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **iter-175 trace-artefact template inheritance ~95% from Story 2.3 iter-159.** Four-artefact bundle (coverage-matrix.json + e2e-trace-summary.json + gate-decision.json + full-report.md) shape reuses 2.3's exact schema; mechanical adaptation of 5-AC narratives from infrastructure-security (dnsmasq + nftables + JSONL-emitter) to user-facing-CLI (compose_whitelist_into + validate_sources + cmd_* dispatcher + mutation-lock fd-201). Four-artefact fan-out takes ~35K tokens per trace iteration at this template-inheritance rate. Apply at every future Epic-2 story where the class-transition is "first of class" — the evidence BLOCK per AC changes, but the JSON schema + markdown phase headers + gate-decision rationale structure all carry over verbatim.
- **iter-175 class-distinction (Story 2.4 vs Story 2.3) — iteration-env-evidence-density axis.** Story 2.3 had ZERO iteration-env runtime assertions because the full stack (dnsmasq daemon + nftables kernel rules + JSONL log tailer process + atomic-reload script) requires a live container. Story 2.4's CLI composition + validation paths are shell-level-verifiable in iteration env: SC-14 parity smoke + dispatcher + regex + multi-error + gitignore. Only the live reload chain (add → compose → validate → mv → reload-egress.sh against running dnsmasq + nft) + `pnpm` listing defer to operator workstation. Class-transition forecast: each Epic-2 story after 2.3 should have MORE iteration-env evidence, not less — consumer-class stories (2.4 CLI, 2.6 host-side wrappers) vs substrate-authoritative stories (2.3 primitive, 2.5 hardening) cluster toward different evidence-density profiles; the trace-WAIVED rationale becomes progressively stronger as more iteration-env evidence accumulates, but the overall coverage % stays 0% until Epic 13 landing.
- **iter-175 CR-forecast de-risking via SC-14 parity PASS.** Story 2.3 had ZERO parity smokes (no dual-composer to verify); Story 2.4's SC-14 dual-composer byte-identity contract was the single highest-risk invariant (if `compose_whitelist_into` diverged from `start-egress.sh`'s `compose_whitelist()`, the CLI and the at-boot composer would produce different runtime state → intermittent bug). iter-174 iteration-env parity smoke PASSED byte-identical against 5-domain fixture with deliberate overlap to exercise `sort -u` dedup. This single smoke de-risks the SC-14 invariant substantially — CR-forecast envelope narrows from "4-iter chain per Story 2.2 LOC-scaling" to "2–4 PATCH opener" projected; the baseline-fragments-override composition semantic is locked.
- **NFR2 authority unchanged.** Cold ≤ 300s / warm ≤ 30s remains authoritative on M4-Pro native; DinD runs are indicative only.
- **Substrate sanity check (post-iter-175):** `docker-compose.yml`, `entrypoint.sh`, `reload-egress.sh`, `egress-log-tailer.sh`, `monitor.sh`, `nftables/egress.nft`, `dnsmasq/dnsmasq.conf`, `docs/invariants/devbox-egress.md` unchanged by iter-175 (trace-only, no substrate touched). Files modified iter-175: `_bmad-output/test-artifacts/traceability/2-4-*` (4 NEW trace artefacts), `_bmad-output/implementation-artifacts/2-4-whitelist-source-of-truth-…-cli.md` (Status flip + Change Log v1.4), `.ralph/@plan.md`, `RALPH.md` (signpost append).
- **Cumulative Epic-2 fix-chain forecast tracking:** Story 2.1 = 16-iter chain (carve-out × 2 + LOC ~2700 → ceiling 33; landed 9+5+ZERO); Story 2.2 = 4-iter chain (zero carve-out + LOC ~500 → ceiling 5; landed 2 PATCH + re-run); Story 2.3 = 10-iter chain (defer × 1 + LOC ~650 → ceiling 9; landed 5+1+ZERO); Story 2.4 forecast = 2–4 iter chain (defer × 1 + LOC ~360 + SC-14 parity PASS de-risks → ceiling ~6.6; expected 1 trace (iter-175 DONE) + 1 SM + 1 CR opener + 0–1 CR re-run). Trend: smaller surface + iteration-env evidence density = tighter chain.
