# Implementation Plan

## NOW

- [ ] **Run `/bmad-create-story (args: "review")`** — pre-dev SM review of the iter-53-drafted Story 1.11 file; FR14n matrix row 2 (`drafted → validated`). Apply the **three-point preventative audit** that has already been built into the draft (per iter-43 + iter-47 + iter-51 carry-forward rules): (a) stable-ID cross-check (motion/density two-segment; status fg/bg three-segment; every AC-named ID resolves to a schema leaf AND a tokens.md bullet); (b) Task-enumeration-vs-schema-enumeration diff (Task 1 subtasks enumerate every schema-required leaf — ~86 leaves; any gap is a pre-dev fix); (c) sprint-status transition-wording correction — already applied at drafting (`ready-for-dev → done` direct-jump); (d) anchor-regex landmines — already applied (column-0 anchor bullet required in new `INVARIANTS.md` section; inline backticks in rationale prose, never triple-backtick fences containing `INV-*` bullets); (e) Direction A value-precedence rule — AC 1 scope carve-out ratifies `§ Design Direction Decision wins` when ux-spec sections disagree. Expected outcome: single-round pass (`drafted → validated`) if the preventative audit is effective; partial-falsification with ≤1 MEDIUM finding if any of the five preventative layers has a miss (budget 1 fix iteration in that case per iter-47 two-round SM closure precedent). ~medium

## QUEUE (Story 1.11 — design-token source, Direction A baseline with motion + density scales)

- [ ] `/bmad-testarch-atdd` — FR14n row 3 `validated → atdd-scaffolded`. Fifth cumulative ATDD-skip application expected (no `vitest.config.*` / `jest.config.*` / `playwright.config.*` present at Story 1.11 time; test runner lands Story 1.16 per architecture.md:650-652 + story § Testing Standards; ATDD-skip hybrid ground-(c) rationale re-applicable: (c.ii) Story 1.13 pre-commit schema-validation gate owns AC 1 integration coverage + (c.iii) `/bmad-code-review (args: "2")` adversarial pass is AC 2 grep-based backstop per story § Testing Standards).
- [ ] `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/1-11-design-token-source-direction-a-baseline-with-motion-density-scales.md")` — FR14n row 4 `atdd-scaffolded → in-dev`. ~large budget (Task 1 author `packages/ui/tokens.json` ~86-leaf DTCG population ≈ medium-large + Task 2 schema-validate one-shot ≈ small + Task 3 manifest+INVARIANTS 1-entry register ≈ small + Task 4 quality gates + 2-3 sync-gate smokes + sprint-status bump ≈ medium). If partial completion, re-queue with `Story State = in-dev (partial)` per iter-45 Lesson. Expected dev ordering: Task 1 (authoring) BEFORE Task 2 (validation) BEFORE Task 3 (`sha256sum` compute requires final canonical byte-form — run prettier first per Task 4 ordering lesson) BEFORE Task 4 (smokes require Task 3's manifest+anchor pairing).
- [ ] `/bmad-testarch-trace (args: "yolo")` — FR14n row 5 `in-dev → traced`. Fifth cumulative WAIVED precedent expected (Stories 1.7/1.8/1.9/1.10 + now 1.11 — contract-populator stories at substrate stage inherit the same gate posture; mirror Story 1.10's trace artefact structure per iter-46 structural-inheritance Lesson — ~90% less reasoning authoring cost).
- [ ] `/bmad-create-story (args: "review")` — FR14n row 7 `traced → sm-verified`. Expected: first-round pass if the iter-53 preventative audit held through dev (three-point audit + Direction A precedence pre-pinned). Mechanical-fix round-trip budget = 1 iter if any structural gap surfaces (Story 1.10 iter-47..49 precedent).
- [ ] `/bmad-code-review (args: "2")` — FR14n row 9 `sm-verified → done` (or `sm-verified → fixes-pending` if action items surface; budget 1-3 CR rounds per iter-7 contract-only-single-pass hypothesis + iter-52 halving-to-zero-at-round-2 trajectory for contract-populator stories with ≤1 convergent PATCH at round 1).

## BLOCKED

_(none)_

## DONE (Story 1.11 authoring; Story 1.10 closure at iter-52)

- [x] iter-53: **`/bmad-create-story` drafted `_bmad-output/implementation-artifacts/1-11-design-token-source-direction-a-baseline-with-motion-density-scales.md`; FR14n state transition `_(no story)_ → drafted` per matrix row 1.** 4 ACs (schema-valid Direction A population / Directions B+C excluded / motion + density tier numerics / `INV-tokens-source` manifest registration + sync-gate smokes). 4 Tasks (~86-leaf DTCG JSON authoring / schema-validate one-shot via `pnpm dlx ajv-cli@5` evidence-only / 1-entry manifest register + INVARIANTS anchor / quality gates + 2-3 sync-gate smokes + sprint-status direct-jump bump). **File-format ratification (AC 1 scope carve-out):** `packages/ui/tokens.json` in DTCG JSON is the 1.0 substrate source — resolves Story 1.10 § Project Structure Notes:169 variance vs architecture.md:915/:1149/:1216 typed-TS-variant naming. Rationale: UX-spec:358 primary recommendation + Story 1.10's schema built for DTCG + Story 1.12's emitter consumes DTCG + W3C-CG open-standard interop. Typed-TS path not closed off (Growth-tier option). **Direction A value-precedence rule pinned (AC 1 scope carve-out):** when ux-spec § Visual Design Foundation and § Design Direction Decision disagree, Chosen Direction wins (ux-spec:621). Concrete known case: `accent.500` = `oklch(54% 0.18 245)` per :613 table, NOT `oklch(62% 0.16 245)` per :492 foundation. **`leafFontFamily` string-vs-array scope carve-out (AC 1):** schema as-shipped rejects arrays; Story 1.11 uses comma-separated strings (`"Inter, system-ui, ..."`); array support deferred to a later story per deferred-work.md iter-52 entry. **Preventative three-point audit applied at drafting** (iter-43 + iter-47 + iter-51 carry-forward rules): stable-IDs cross-checked (motion/density two-segment forms; status fg/bg three-segment modifiers per `<category>.<semantic-name>.<modifier>` pattern architecture.md:693); Task 1 subtask list mirrors every schema-required leaf (~170 lines enumerating 11 neutral + 6 accent + 4 surface + 5 text + 3 border + 10 status + 4 severity + 4 state + 8 type + 2 font + 13 space + 5 radius + 6 motion + 4 density + 5 breakpoint + 2 mode overlays = ~86 leaves); sprint-status transition wording pre-corrected to direct-jump `ready-for-dev → done`; anchor-regex column-0 + inline-backtick rules pre-applied in Task 3; scoped-build-before-check optimisation pre-pinned in Task 3 (`pnpm --filter @keel/keel-invariants build` before `pnpm keel-invariants:check`). **Value inventory summary in the story:** 11-step true-neutral OKLCH ramp with dev-specified lightness mapping (50→99%, 950→8%); accent.500 = `oklch(54% 0.18 245)` (Direction A); full motion tiers (60/120/200/320/500 ms) + density factors (0.875/1.0/1.125) + space (4px-base Tailwind) + radius (0/4/8/12/9999px) + type scale (1.125 ratio; 12→36px) + breakpoints (640→1536px) + font stacks (Inter + JetBrains Mono comma-separated strings). Dark-mode overlay re-maps surface/text/border slots via neutral-ramp inversion (`surface.default = {color.neutral.950}` in dark, etc.). **Schema validation strategy:** one-shot `pnpm dlx ajv-cli@5` evidence capture at Task 2 (no dep add, no committed script — Story 1.13 owns formal pre-commit gate + validator choice). Sprint-status: `1-11-...: backlog → ready-for-dev`; `last_updated: 2026-04-20 Story-1-11-ready-for-dev UTC` (both header-comment at `:2` + data-field at `:38`). § Change Log v1.0 row appended to story file. Pure prose + YAML edit; zero code changes; quality gates not re-run (no TS/JS touched). Story 1.11 GitHub issue **#35** (OPEN) surfaced via `gh issue list` at iter-53 time — `Refs #35` included in commit trailer preemptively; `RALPH_ISSUE_NUMBER` env var unset at iter-53 start due to ralph.py IP-Context-Story parse lag (same pattern as Story 1.10 iter-42 where #34 didn't surface until iter-44's PROMPT Issue Tracking block). Parent Epic 1 `Refs #9` optional. PR #226 CI: `no checks reported`; push safe.

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1–1.10 done; 1.11 drafted + next-up for validate; 1.12–1.16 backlog → 5 more stories × full FR14n lifecycle remaining after 1.11 closes before EPIC_DONE)
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (continues through Epic 1; PR #226 stays Draft until EPIC_DONE per PROMPT_build.md step 5c)
- **Story:** Story 1.11 — Design-token source, Direction A baseline with motion + density scales
- **Story File:** `_bmad-output/implementation-artifacts/1-11-design-token-source-direction-a-baseline-with-motion-density-scales.md`
- **Story State:** `drafted` — FR14n matrix row 1 (`_(no story)_ → drafted`) landed at iter-53; next iter-54 runs `/bmad-create-story (args: "review")` for row 2 `drafted → validated`.
- **GitHub Issue:** Story 1.11 tracked at **#35** (OPEN); parent Epic 1 tracked at **#9** (OPEN).
- **PR:** #226 (Draft, MERGEABLE, no-CI-checks-reported — stays Draft through Epic 1; Draft→Open at EPIC_DONE after stories 1.11–1.16 all complete FR14n lifecycle).

## Notes

- **Story 1.11 sizing projection (conservative — apply iter-52's halving-hypothesis-at-round-2 trajectory for contract-populator stories):** 7–11 iterations end-to-end, FR14n lifecycle. Best case (clean preventative-audit payoff): iter-53 drafted → iter-54 validated → iter-55 ATDD-skip-scaffolded → iter-56 in-dev (single-pass large budget — ~86 DTCG leaves; if partial, +1 iter) → iter-57 traced → iter-58 sm-verified → iter-59 CR-done = **7 iterations** (matches Story 1.8 contract-only floor). Realistic case (one-round SM fix + one-round CR fix): **9–10 iterations** (Story 1.10's 11-iter trajectory with one preventative-audit layer held is the realistic median). Pessimistic case (structural gap + convergent CR PATCH): **11 iterations** (Story 1.10 replay). Sizing informs Epic 1 close-out timing: 5 more stories (1.12–1.16) × avg 9 iter/story ≈ 45 more iterations to EPIC_DONE halt on PR #226 Draft→Open.
- **Carry-forward rules INTO Story 1.11 dev-time + SM/CR layers** (iter-43 + iter-47 + iter-51 + iter-52):
  - **Scoped-build optimisation** (iter-51): when mutating only `packages/keel-invariants/src/invariants.manifest.ts`, run `pnpm --filter @keel/keel-invariants build` BEFORE `pnpm keel-invariants:check` — ~10× faster than full-workspace `pnpm -w build`. Pre-pinned in Story 1.11 Task 3.
  - **Two-layer CR convergence doctrine** (iter-36 + iter-52 confirmation): single-layer Blind-Hunter findings (especially adversarial-prose overreach, forward-compat-reframed-as-dead, section-ID-vs-publishable-slug conflation) are the default DISMISS class unless corroborated by Edge Case Hunter AND/OR Acceptance Auditor. Apply aggressively at Story 1.11's CR triage iterations (iter-59 onwards).
  - **Trace artefact structural inheritance** (iter-46): author Story 1.11's trace report (iter-57) by mirroring Story 1.10's `_bmad-output/test-artifacts/traceability/1-10-...md` section-by-section (YAML frontmatter → PHASE 1 Coverage Summary → Detailed Mapping per-AC subsections → Gap Analysis → Coverage Heuristics → PHASE 2 Evidence Summary → Gate Decision → Rationale → Waiver Details → Gate Recommendations → Next Steps → Integrated YAML Snippet → Related Artifacts → Sign-Off). Saves ~90% of reasoning cost.
  - **Hybrid ground-(c) ATDD-skip rationale** (iter-44 + Story 1.10 precedent): Story 1.11's ATDD-skip (iter-55) applies BOTH (c.ii) downstream-story-covers-integration (Story 1.13 pre-commit schema-validation gate owns AC 1; Story 1.16 test runner owns per-AC unit tests) AND (c.iii) spec-declared-CR-substitution (story § Testing Standards Dev Notes defer adversarial coverage of AC 1 value-correctness + AC 3 tier-enumeration to the CR pass). Cite both for defense-in-depth.
  - **Drift-smoke filter precision** (iter-45 Lesson): when Task 4's added-to-source-only smoke (anchor deletion) needs a `sed`/`node -e` filter, match the column-0 anchor bullet pattern (`- **\`INV-tokens-source\`**`) NOT the bare ID (which may appear in prose elsewhere — e.g. rationale doc's Companion reference). Pre-pinned in Task 4 subtask.
  - **New-untracked-file revert pattern** (iter-45 Lesson): for files that were created within the iteration, `git checkout --` fails (`pathspec did not match`); use `pnpm exec prettier --write <path>` to restore canonical byte-form (sha256 matches manifest). For Story 1.11, `packages/ui/tokens.json` will be committed before the Task 4 drift-smoke runs (Task 4 executes after Task 3 registers the manifest entry), so `git checkout --` works for the drift-revert; noted in Task 4 subtask.
- **Next iteration (iter-54) expected outcome:** `/bmad-create-story (args: "review")` runs against the Story 1.11 file. If all five preventative-audit layers held, expect **first-round pass** (zero § Review Findings; state `drafted → validated`). If any layer had a miss (most likely class: a schema-required leaf not enumerated in Task 1; secondary class: an OKLCH value citation that contradicts ux-spec), expect **single MEDIUM finding** (state `drafted → validated-fixes-pending` — but per FR14n matrix, this collapses to `drafted → validated` with a carry-forward fix to iter-55 or deferred to iter-58 SM-review; the FR14n matrix does not define a `drafted-fixes-pending` state, so any pre-dev-review finding is applied in-place via Edit by the iter-54 run itself). Sprint-status stays `ready-for-dev` through iter-54 (BMad convention — `ready-for-dev` is the BMad default `Status:` value from `/bmad-create-story` completion through `/bmad-dev-story` start; Ralph's FR14n `drafted / validated / atdd-scaffolded` states track orthogonally in IP § Context `**Story State:**`).
- **Issue Tracking:** Story 1.11 tracked at GitHub issue **#35** (OPEN at iter-53 time; `gh issue list --search "Story 1.11 in:title"` returns it). `Refs #35` in iter-53 commit trailer (pre-emptive — same pattern as Story 1.10 iter-42 where the issue was #34 but `RALPH_ISSUE_NUMBER` env var didn't surface until iter-44's PROMPT Issue Tracking block). iter-54 onwards: `RALPH_ISSUE_NUMBER=35` should be injected by ralph.py once IP § Context's `**Story:**` field is parsed at iteration start. `Closes #35` deferred to PR body when PR #226 transitions Draft→Open at EPIC_DONE halt (not at individual story closure — GitHub native automation closes the issue on PR merge; ralph.py then transitions to Done). Parent Epic 1 issue **#9** — `Refs #9` optional; never `Closes` it (ralph.py transitions epic issues to Done on EPIC_DONE halt).
