# Implementation Plan

## NOW

- [ ] Run `/bmad-testarch-trace (args: "yolo")` — AC→test coverage gate + traceability matrix for Story 2.2. State transition `in-dev → traced` (or `trace-fixes-pending` if Ralph-addressable coverage gaps surface; expect WAIVED per Story 2.1 iter-126 precedent for hybrid infrastructure-smoke + configuration-surface class — twelfth cumulative). ~small (skill runs in its own fresh-context iteration; Ralph's iteration is orient + queue + commit + push).

## QUEUE (Story 2.2 `in-dev → traced → sm-verified → done`)

- [ ] Run `/bmad-create-story (args: "review")` — post-dev SM requirements-satisfaction verification. State transition `traced → sm-verified` OR `sm-fixes-pending`.
- [ ] Run `/bmad-code-review (args: "2")` — post-SM CR adversarial action-items gate. State transition `sm-verified → done` OR `sm-verified → fixes-pending` (multi-cycle envelope bounded by Story 2.1's iter-128 → iter-138 → iter-144 three-cycle precedent).
- [ ] _(conditional, post-Story-2.17 done)_ Transition PR #230 Draft→Open — final CI gate after all Epic 2 stories done; monitor CI; merge.
- [ ] _(conditional, post-Epic-2 merged)_ § Cross-epic transition → Epic 3 Story 3.1 via `/bmad-create-story` OR halt EPIC_DONE awaiting merge.

## BLOCKED

_(none — Story 2.2 implementation landed green in iter-148; lifecycle continues via trace → SM-review → CR gates.)_

## DONE (Story 2.2 — iter-145 draft, iter-146 review-with-fixes, iter-147 ATDD-skip, iter-148 dev-story)

- [x] iter-148: **`/bmad-dev-story` landed Story 2.2 implementation in a single iteration (all 8 Tasks complete; AC 1–AC 5 green); Story State `atdd-scaffolded → in-dev`.** Sprint-status flipped `ready-for-dev → in-progress → review` at dev-story Step 4 + Step 9 per skill contract; story-file `Status: ready-for-dev → review`. **Deliverables (8 new files + 8 modified):** NEW `packages/devbox/.envrc.example` (14-line block-comment header + 5 grouped knob sections covering every AC 1 knob + 6th substrate-knob section covering Story 2.1 carryovers so `.envrc.example` is a complete fork-retune surface); NEW `packages/devbox/.secrets.example` (6-key `act`-consumer schema per architecture.md:328 verbatim); NEW `packages/keel-invariants/src/check-no-committed-dotfiles.ts` (36-line Node-only anchored-regex denylist + stderr pointer + exit 1 per-match); NEW `docs/invariants/gitignored-secret-commit-deny.md` (AI-7-pattern front-matter + § Intent + § Mechanism + § Verification); NEW `dist/check-no-committed-dotfiles.{js,js.map,d.ts,d.ts.map}` (tsc output). MODIFIED `packages/devbox/docker-compose.yml` (non-swarm `cpus:` / `mem_limit:` / `shm_size:` / `ulimits.nofile.{soft,hard}:` / `platform:` / 4 loopback-bound `ports:` entries — all `${KEEL_DEVBOX_*:-<default>}`; pre-existing Story 2.1 contract surfaces preserved); `packages/devbox/README.md` (new `## Retuning` section + `### Secrets` subsection between NFR2 budget and cc-devbox provenance; Story 2.2 struck-through in "does NOT deliver" table); `.gitignore` (extended `# Environment / secrets` block with `.envrc`, `.envrc.local`, `.secrets` + 4 bang-suffix negations); `.pre-commit-config.yaml` (new `no-committed-dotfiles` prek hook between `format-check` and `tokens-schema` with `pass_filenames: true` + `files:` filter); `package.json` (root forwarding script); `packages/keel-invariants/package.json` (bin + scripts entry); `packages/keel-invariants/src/invariants.manifest.ts` (new `INV-gitignored-secret-commit-deny` entry with doc-first sourcePath + 3 contentHash updates for touched files `.pre-commit-config.yaml` + `package.json`); `INVARIANTS.md` (new `### Gitignored-secret commit-deny (Story 2.2)` section + bullet anchor). **Verification results:** `docker compose config` → exit 0 with defaults rendered (cpus 8, mem_limit 12884901888 / 12 GB, shm_size 2147483648 / 2 GB, platform linux/arm64, nofile 65536, 4 port entries with `host_ip: 127.0.0.1`); `KEEL_DEVBOX_MEMORY_GB=16 KEEL_DEVBOX_CPUS=4` override → `cpus: 4` + `mem_limit: "17179869184"` / 16 GB; `node packages/keel-invariants/dist/check.js` → exit 0 (sync-gate after 4 contentHash updates); lint-rule smokes positive `.envrc.example` → exit 0 + negative `.envrc` → exit 1 with pointer "Refusing to commit gitignored secret file: packages/devbox/.envrc (matches .envrc)."; knob-alignment diff `comm -23 compose envrc-example` → empty (every compose-referenced knob enumerated in `.envrc.example`); reverse direction returns only the expected tmpfs trio (Story 2.5 consumer); entrypoint forbidden-pattern sweep → exit 1 (no runtime-install regression; preserves Story 2.1 Task 7.1); workspace `pnpm -w typecheck` → 16/16 successful; `pnpm -w lint` → 16/16 successful; `pnpm -w format:check` → green after Prettier auto-format pass on `docker-compose.yml` + `README.md` (no semantic changes). **Dev-time notes:** (a) task spec's `/tmp/fake.envrc` negative case inconsistent with AC 5 regex (basename-only match); corrected smoke uses `/tmp/.envrc`; (b) git 2.43 `check-ignore -v` exits 0 on negation-matched paths (contradicts task spec's "exit 1 no output" — functional check via `git add --dry-run` + `git check-ignore` no-flag confirms negation rules work; schemas stageable); (c) prek-level end-to-end hook exercise deferred to operator workstation (prek binary not installed in iteration env; node-direct + pnpm-script invocations substitute with identical semantics). Budget: ~75K tokens (orient ~40K + 8 tasks ~30K + IP / RALPH.md updates ~5K — within 117K execution budget). **Next iteration:** `/bmad-testarch-trace (args: "yolo")` for `in-dev → traced`.

- [x] iter-147: **`/bmad-testarch-atdd` ATDD-SKIP — TWELFTH cumulative Epic precedent (Stories 1.7..1.16 + 2.1 → 2.2); Story State `validated → atdd-scaffolded`.** Four-ground rationale variant-(ii)+(iii) substitution; no test-plan artefacts authored; Change Log v1.2 appended in-situ. Full detail in `44e4ce2` commit message.

- [x] iter-146: **`/bmad-create-story (args: "review")` pre-dev SM validation applied 9 fixes (5 CRITICAL + 2 ENHANCEMENT + 2 polish); Story State `drafted → validated`.** Three-layer parallel subagent fan-out. Full detail in `cc9aee8` commit message + story file v1.1 Change Log.

- [x] iter-145: **`/bmad-create-story` produced `_bmad-output/implementation-artifacts/2-2-envrc-parameterisation-contract.md` (Story State: `_(no story) → drafted`; sprint-status `2-2: backlog → ready-for-dev`).** Exhaustive context-engine pass. Full detail in `fd03e1d` commit message.

## Context

- **Phase:** 4-implementation — Epic 2 open at 1/17 stories done (2.1 done at iter-144); Story 2.2 `in-dev` as of iter-148 (`review` sprint-status; story `Status: review`).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available` § Backend contract). Iteration-context bind-mount denial persists; operator-workstation resolves via native Docker Desktop under host-shared `/Users/...` OR backend-A isolated DinD harness.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Version matrix in `packages/devbox/VERSIONS.md § Bake log`.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; Story 2.1 done; Story 2.2 `in-dev` (implementation landed iter-148; trace + SM + CR gates remain); Stories 2.3..2.17 in `backlog`.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2; PR #230 transitions to Open at epic completion).
- **Story:** 2.2 — `.envrc` parameterisation contract.
- **Story File:** `_bmad-output/implementation-artifacts/2-2-envrc-parameterisation-contract.md`.
- **Story State:** `in-dev` — iter-148 `/bmad-dev-story` completed all 8 tasks; AC 1–AC 5 green; sync-gate green; workspace quality gates green. Next iteration: `/bmad-testarch-trace (args: "yolo")`.
- **GitHub Issue:** Story 2.2 issue **#42** (https://github.com/tthew/ralph-bmad/issues/42). Parent Epic 2 → #10.
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2; no CI runners pre-Epic-13; transitions to Open at Story 2.17 done).

## Notes

- **DinD is fork-time substrate, not transitional.** `INV-devbox-dind-available` — every fork's cc-devbox-equivalent must provide Docker. Podman/rootless-Docker conformant if daemon reachability holds (backend C extension per iter-144 defer).
- **Backend B is the reference environment at 2026-04-21.** Host socket-passthrough. Broad-state-mutation scripts MUST detect backend + refuse destructive ops by default (`benchmark.sh` reference from iter-122). Bind-mount compose runtime requires host-shared File Sharing allowlist coverage — operator-owned from iteration context.
- **NFR2 authority unchanged.** AC 4 cold ≤ 300s / warm ≤ 30s remains authoritative on M4-Pro native. Story 2.2 is infrastructure-parameterisation — no runtime-performance impact on NFR2; AC3 retunability verified via `docker compose config` static YAML expansion (no daemon required).
- **Story 2.2 parameterises knobs for Story 2.5 (tmpfs) + Story 2.11 (shared) + Story 2.12 (loopback ports / SSH).** Those downstream stories activate the tmpfs / shared / SSH knobs that Story 2.2 adds to `.envrc.example`. Story 2.2 did NOT partially implement those — TODO markers in compose + commented substrate knobs in `.envrc.example` make the handoffs explicit.
- **Twelfth cumulative Epic ATDD-skip pattern validated** (iter-147; second Epic 2). **Exhaustive-context-engine story-creation pattern validated** (iter-145). **Pre-dev SM validation with parallel-subagent-review pattern validated** (iter-146; nine fixes on an already-exhaustive draft proved the post-draft review pass catches cross-file drift the single-threaded drafter can't internally audit). **Single-iteration dev-story landing pattern validated** (iter-148; Story 2.2's 8 tasks + 5 ACs + infrastructure-smoke + configuration-surface hybrid class landed in ~75K budget via tight task-graph decomposition by the iter-145 drafter — grep-friendly verification literals + pre-resolved sourcePath decisions + mapping tables at story-authoring time minimized dev-agent decision surface). **Cross-epic auto-advance pattern validated** (iter-96). **Partial-completion pattern validated** (iter-99). **Backend-B safety-gate pattern established** (iter-122). **Review-with-operator-owned-carve-out pattern validated** (iter-125). **SM-verified-with-operator-owned-carve-out pattern validated** (iter-127). **CR-fixes-pending-adversarial-default pattern validated** (iter-128). **CR-re-run-adversarial-default + action-item-bundling pattern validated** (iter-138). **Doc-drift two-row bundle closure pattern validated** (iter-141). **Story-artefact spec-vs-code drift closure pattern validated** (iter-142). **Operator-UX polish pattern validated** (iter-143). **ZERO-PATCH third-gate close pattern validated** (iter-144; three-cycle upper envelope for Epic-2 infrastructure-smoke stories).
