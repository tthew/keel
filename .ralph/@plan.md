# Implementation Plan

## NOW

- [ ] Fix A/4 [HIGH] — stale UPPERCASE regex echo in trace bundle (iter-8 residue missed by prior passes). Edit 2 sites to swap `[A-Z][A-Z0-9-]+` → `INV-[a-z0-9]+(?:-[a-z0-9]+)+`: `_bmad-output/test-artifacts/traceability/1-9-invariant-sync-gate-runtime-tooling-reader-walker-drift-detector.md:141` + `_bmad-output/test-artifacts/traceability/1-9-coverage-matrix.json:69` (AC-5 substrate_verification). Same fix class as iter-8 spec-echoes-impl-bug rule but for TRACE artefacts which iter-8 missed. Pure prose — zero code changes. ~small

## QUEUE (Story 1.9 — CR iter-14 re-run surfaced 3 new fix tasks + 4 defers; stays `fixes-pending` until QUEUE empties → re-run CR)

- [ ] Fix B/4 [HIGH] — stale "Not exercised in smoke" AC-5 framing (iter-8 smoke upgrade not propagated to trace narrative). Edit 3 sites: trace md:143 (AC-5 mapping bullet) + trace md:260 (Happy-Path-Only section) + coverage-matrix.json:69 AC-5 substrate_verification (has both the uppercase regex of Fix A AND the "Not exercised in smoke" of Fix B — may combine with Fix A commit if surgical). Leave AC-3 equivalents unchanged (trace md:116 + coverage-matrix.json:49) — iter-8 did NOT add an AC-3 smoke; only AC-2 + AC-5. Pure prose — zero code changes. ~small
- [ ] Fix C/4 [HIGH + MEDIUM] — count drift "three smokes" → "five smokes" + "AC 1+4+7 only" → "AC 1+2+4+5+7" across ~13 sites (iter-11 narrow-scope pre-announced). Target inventory:
  - spec:120 (trailing "AC 1 / AC 4 / AC 7 fully covered at the shell-invocation level" — iter-11 patched opening "five" but left closing AC-list stale = internal self-contradiction)
  - spec:149 ("three smoke tests materially cover 4 of 7 ACs end-to-end; remaining 3 are schema-level" — iter-4 dev-story phrasing, now 5 smokes cover 5 of 7 ACs; AC-3 + AC-6 remain schema/structural)
  - trace md:307 + 349 + 436 + 465 + 526 + 555 + 583 + 614 (8 sites with "three" / "3 runtime smoke tests" / "AC-1 + AC-4 + AC-7 end-to-end")
  - coverage-matrix.json:160 + e2e-trace-summary.json:73 (2 sites with "Task 5 shell-invocation smoke tests already cover AC-1 + AC-4 + AC-7 end-to-end")
  - gate-decision.json:12 (rationale: "Task 5's three shell-invocation smoke tests exercised AC 1 + AC 4 + AC 7 end-to-end … AC 2 / AC 3 / AC 5 realised schema-level" — AC-2 + AC-5 now end-to-end post-iter-8; only AC-3 remains schema+structural). Pure prose — zero code changes. ~medium
- [ ] Re-run `/bmad-code-review (args: "2")` CR adversarial triage against post-Fix-A/B/C diff. Expected outcome per FR14n matrix row 10: zero Blind Hunter findings → `fixes-pending → sm-verified → done` (single-transition edge). If new findings surface, queue fix tasks; stay `fixes-pending`. ~medium

## BLOCKED

_(none)_

## DONE (Story 1.9 — CR iter-7 through iter-14)

- [x] iter-7: `/bmad-code-review (args: "2")` Ralph-hosted three-layer fan-out — 6 patches + 10 defers + 3 dismissed. CRITICAL: `ANCHOR_REGEX` uppercase-class bug reproduced by Edge Case Hunter + Acceptance Auditor.
- [x] iter-8 through iter-13: 6 CR fix patches landed — (1) CRITICAL `ANCHOR_REGEX` uppercase→lowercase + symmetric `added-to-source-only` emitter + 2 new Task-5 smokes + spec echoes; (2) HIGH spec line 41 stdout→stderr; (3) HIGH count 7→8 sourcePath files; (4) HIGH count 2→5 runtime smokes (narrow-scope per iter-11); (5) MEDIUM trace-bundle line citations re-anchored (31 edits); (6) MEDIUM Dev Agent Record AC 2 status prose upgraded schema→end-to-end.
- [x] **iter-14: `/bmad-code-review (args: "2")` CR RE-RUN — 3-layer Ralph-hosted fan-out against post-fix diff (2862 lines, 23 files at `/tmp/story-1-9-cr-rerun-diff.txt`).** Verdicts: (a) **Blind Hunter NEW_FINDINGS** — 3 HIGH trace-bundle residue + 1 MEDIUM spec self-contradiction → queued as Fix A/B/C + absorbed into Fix C; (b) **Edge Case Hunter MINOR_ONLY** — 4 LOW new-edge-cases (regex column-0 binding; regex matches inside code fences; `!p.includes('..')` over-broad; UTF-8 byte-lossy hashing) → 2 new defers appended to `deferred-work.md` § story-1.9 CR-rerun, 2 reconfirmed from iter-7's defer list; (c) **Acceptance Auditor PASS** — all 7 AC validated end-to-end with LIVE reproduction: AC 1 clean-path `pnpm keel-invariants:check` exit 0 + no stderr; AC 2 upgraded FAIL→PASS via live delete `INV-commitlint-shared` anchor → exit 1 + JSON stderr `{kind:"added-to-source-only", id:"INV-commitlint-shared", sourcePath:"packages/keel-invariants/commitlint.config.keel-invariants.js"}` + revert byte-identical; AC 3 structural PASS; AC 4 drift-path PASS; AC 5 upgraded FAIL→PASS via live append `INV-fake-orphan` orphan anchor → exit 1 + JSON stderr `{kind:"removed-from-docs-only", anchor:"INV-fake-orphan"}` + revert byte-identical; AC 6 stderr+exit-code contract materially PASS (no longer hollow for AC 2/5 branches); AC 7 1.647s total wall-clock vs 2s budget. Impl is CLEAN; trace-narrative is STALE. **New pattern established: trace-bundle semantic-drift from impl-upgrade** — when a later iter upgrades AC coverage (here iter-8's Task-5 smoke additions), the trace bundle's "Happy-Path-Only" + "Not exercised in smoke" + smoke-count + AC-coverage-list semantics must be refreshed at the SAME commit as the upgrade. iter-12's line-citation re-anchor was narrow-scope and missed these semantic echoes; iter-13's Dev Agent Record prose fix only touched the spec's AC-matrix, not the trace bundle. Carry-forward analogous to iter-9's prose-echoes-impl-rule but for derived-artefact coverage-claims. Adversarial triage: 3 patch (A/B/C) + 4 defer (2 new + 2 reconfirmed) + 0 dismissed.

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1–1.8 done; 1.9 fixes-pending — iter-14 CR re-run surfaced 3 new fix patches; 3 fix iters + 1 final CR re-run remaining)
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (continues — PR #226 stays Draft until Epic 1 EPIC_DONE per PROMPT_build.md step 5c)
- **Story:** `1-9-invariant-sync-gate-runtime-tooling-reader-walker-drift-detector`
- **Story File:** `_bmad-output/implementation-artifacts/1-9-invariant-sync-gate-runtime-tooling-reader-walker-drift-detector.md`
- **Story State:** `fixes-pending` (Ralph FR14n matrix row 10) — sprint-status + story-file Status remain `review`; flips to `done` only on NEXT CR re-run zero-findings outcome per matrix row 9 single-transition edge.
- **PR:** #226 (Draft, MERGEABLE, no-CI-checks-reported — expected pre-Story-1.16; stays Draft through Epic 1; Draft→Open at EPIC_DONE)

## Notes

- **iter-14 CR re-run did NOT zero-out as hoped.** Impl is CLEAN (Acceptance Auditor PASS + Edge Case Hunter MINOR_ONLY LOWs all deferrable + new-edge-case), but Blind Hunter surfaced 4 trace-bundle/spec residue findings the iter-8..13 fix pass missed. Root cause: iter-8's CRITICAL fix upgraded AC 2 + AC 5 from FAIL → PASS-end-to-end by adding 2 new Task-5 smokes, but the trace bundle's narrative (written at iter-5 trace-gen, never refreshed) still carries pre-iter-8 semantics: "AC-5 Not exercised in smoke", "three smoke tests", "AC 1+4+7 only", UPPERCASE regex echoes. iter-12's line-citation re-anchor was narrow-scope and didn't touch content semantics; iter-13's Dev Agent Record AC 2 upgrade only touched the spec's AC-matrix, not the trace bundle's mirror claims.
- **Pattern corollary established iter-14: trace-bundle semantic-drift from impl-upgrade.** When iteration N+k upgrades AC coverage (new smoke, fixed unreachable branch, new end-to-end evidence), the trace bundle's three claim classes drift: (i) "Not exercised in smoke" (now false for the upgraded AC); (ii) smoke-count cardinals ("three"→"five"); (iii) AC-coverage lists ("AC-1 + AC-4 + AC-7" → "AC-1 + AC-2 + AC-4 + AC-5 + AC-7"). Generalised carry-forward: every AC-coverage-upgrading iteration MUST update trace bundle's mirror claims in the same commit. Analogous to iter-9's prose-echoes-impl rule but for derived-artefact coverage-claims instead of syntax literals. Add to post-dev trace-gen + CR-fix checklist: grep-sweep `"Not exercised"` + `"three smoke"` + `"AC-1 + AC-4 + AC-7"` + uppercase-regex-literals across `_bmad-output/test-artifacts/traceability/{story}-*` whenever AC coverage is materially upgraded.
- **Fix A + Fix B may combine into a single commit.** coverage-matrix.json:69 AC-5 substrate_verification contains BOTH the stale uppercase regex literal (Fix A target) AND the stale "Not exercised in smoke" framing (Fix B target). Next Ralph iteration MAY combine them into one commit if narrow-scope discipline holds; keeping them as separate NOW+QUEUE entries preserves the option of splitting for safer revert granularity. Either approach is OK; IP DONE should record which choice was made.
- **Fix C scope is ~13 target sites across 5 artefacts** — single mechanical prose-swap like iter-10's 6-site swap + iter-12's 31-site re-anchor. Do NOT split into Fix C1/C2/C3 by artefact — the underlying change is one semantic ("post-iter-8 reality: 5 smokes covering AC 1+2+4+5+7 end-to-end; only AC-3 + AC-6 remain schema/structural"), and splitting would fragment a single coherent edit across iterations.
- **Acceptance Auditor live reproductions are load-bearing evidence** — both the AC 2 manifest-side missing-anchor smoke (delete `INV-commitlint-shared` → exit 1 + `added-to-source-only` + sourcePath → revert byte-identical) AND the AC 5 docs-side orphan-anchor smoke (append `INV-fake-orphan` → exit 1 + `removed-from-docs-only` → revert byte-identical) were exercised live in the auditor layer, giving iter-8's Task-5 additions the strongest possible end-to-end validation. The iter-13 Dev Agent Record AC 2 rewrite "end-to-end via iter-8 Task-5 manifest-side missing-anchor smoke" is now substantiated by a second independent reproduction.
- **Edge Case Hunter 4 LOWs appended to `deferred-work.md` § story-1.9 CR-rerun (2026-04-20)** — 2 new (regex column-0 binding; regex matches inside code fences) + 2 reconfirmations of iter-7 defers (traversal refine `..` over-broad; UTF-8 byte-lossy). All natural-scope for Story 1.10+ schema-hardening OR Story 1.16 test-runner landing; none exploitable in current single-consumer linux-only text-sources-only baseline.
- **Issue Tracking:** Story 1.9 issue number still unset at iter-14. Parent Epic 1 issue **#9** — `Refs #9` only.
