# Implementation Plan

## NOW

- [ ] **Run `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-6-host-side-pnpm-devbox-cli-surface.md")`** per § Story Lifecycle `atdd-scaffolded → in-dev`. Fresh context; dev-agent executes 10 Tasks (~50 subtasks) — 13 bash scripts under `packages/devbox/scripts/` + 13 `devbox:*` entries in root `package.json` + AR-10 operator-migration docs (SC-21) + AR-12 COMPOSE_PROJECT_NAME docs polish (SC-22) + new H2 `## Host-side CLI (Story 2.6)` in `packages/devbox/README.md` (SC-23) + new H3 `### Host-side CLI (Story 2.6)` in `AGENTS.md` (SC-24). Scope-owner for conditional AR-7 `/run` relocation (SC-19; only if empirical smoke surfaces breakage) + AR-9 `/etc/*` chown (SC-20; same conditional gate). SC-17 invariant sync-gate fires only if SC-22 docs polish touches `docs/invariants/devbox-hardening.md`. Story 2.4 precedent: single-iter landing for 1-script surface (iter-174); Story 2.6 is broader (13 scripts + 4 docs surfaces) — may require 1-2 dev iterations depending on AR-7/AR-9 conditional-escalation outcome. Live AC 2..7 smokes operator-workstation-deferred per Story 2.4 SC-17 + Story 2.5 iter-187 backend-B precedent. If dev-story returns partial, re-queue next iter per `in-dev (partial)` protocol. ~medium (single-iter) to ~large (2-iter if SC-19/SC-20 escalation triggers).

## QUEUE (Story 2.6 lifecycle → Epic 2 Story 2.7..2.17 → Epic 2 close-out)

- [ ] _(post-`in-dev`)_ `/bmad-testarch-trace (args: "yolo")` — AC→test coverage gate per § Story Lifecycle row `in-dev`. Likely WAIVED or minimal (8 ACs against 13 host-side scripts; most ACs will be operator-workstation-smoke-covered at the recipe level per Story 2.4 iter-175 + Story 2.5 iter-188 precedent).
- [ ] _(post-`traced`)_ `/bmad-create-story (args: "review")` — post-dev SM verification per § Story Lifecycle row `traced` (same skill as pre-dev validation; state governs intent).
- [ ] _(post-`sm-verified`)_ `/bmad-code-review (args: "2")` — CR opener per § Story Lifecycle row `sm-verified`. Forecast: 4-6 PATCH opener (user-facing-CLI class, matches Story 2.4 5-PATCH envelope; Story 2.6 is broader surface but simpler per-script so ~4-6 is reasonable).
- [ ] _(post-CR-drain; QUEUE empty)_ `/bmad-code-review (args: "2")` — CR-closure gate (expected ZERO-PATCH per Epic-2 3-story precedent: 2.3 iter-171, 2.4 iter-183, 2.5 iter-197).
- [ ] _(post-Story-2.6-done; through Story 2.17-done)_ Iterate Story 2.7..2.17 through the full lifecycle — 11 remaining substrate stories spanning ralph-auto-start + claude OAuth + gh OAuth + prereq-check + shared mode + port-publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-22 per iter-197 + iter-198 + iter-199 pre-flight — "no checks reported on the 'feat/epic-2-packaged-devbox' branch").

## BLOCKED

_(none)_

## ATDD Red Phase

_(none — Story 2.6 ATDD-skipped at iter-200 per § Story Lifecycle `validated → atdd-scaffolded` (skip branch). 16th-cumulative FR14n ATDD-skip precedent (Stories 1.7..1.16 = 10 Epic-1 skips; 2.1 iter-98 = 11; 2.2 iter-147 = 12; 2.3 iter-157 = 13; 2.4 iter-174 lifecycle-bypass dev-advance = 14; 2.5 iter-186 = 15; 2.6 iter-200 = 16). Grounds (c)+(ii)+(iii): (c) Story 2.6's 8 ACs are all CLI-smoke-class (`pnpm devbox:<verb>` → exit-code + message assertions over 13 host-side bash scripts) — same class as Story 2.4 `pnpm devbox:whitelist` (iter-173/174 lifecycle-bypass); (ii) no live test runner configured at 1.0 — repo-root `pnpm test` is `turbo run test` with NO devbox-package test target and no Playwright/Cypress/Vitest/Jest/pytest configs; (iii) Task 8 iteration-env-safe smokes (bash -n syntax + pnpm-run-listing + `env:check` exit-code + dispatcher-usage) + Task 9 operator-workstation full-lifecycle recipe are both SHIPPED AS IMPLEMENTATION by dev-story (SC-23 + Task 8.6 + Task 9.1 record them as README verification recipes) — a pre-written ATDD scaffold file would duplicate that source of truth. Downstream Epic 13 harness + post-dev SM review + CR opener substitute for adversarial coverage.)_

## DONE (Story 2.6 lifecycle — keep current story chain, prune after Story 2.7 starts)

- [x] iter-200: **`/bmad-testarch-atdd` → ATDD-skip (FR14n Story State `validated → atdd-scaffolded`).** 16th-cumulative FR14n ATDD-skip precedent (Stories 1.7..1.16 = 10 Epic-1 skips; 2.1 iter-98 = 11; 2.2 iter-147 = 12; 2.3 iter-157 = 13; 2.4 iter-174 lifecycle-bypass dev-advance = 14; 2.5 iter-186 = 15; 2.6 iter-200 = 16). **Grounds (c)+(ii)+(iii):** (c) Story 2.6's 8 ACs are all CLI-smoke-class (`pnpm devbox:<verb>` → exit-code + message + container-state assertions over 13 host-side bash scripts), identical class to Story 2.4 `pnpm devbox:whitelist` which skipped via lifecycle-bypass at iter-173→iter-174 without a dedicated ATDD iter; (ii) no live test runner configured at 1.0 — repo-root `pnpm test` is `turbo run test` with NO devbox-package test target and no Playwright/Cypress/Vitest/Jest/pytest configs in the monorepo; ad-hoc shell-level smokes are operator-workstation-deferred per Story 2.4 SC-17 + Story 2.1 iter-127 backend-B precedent; (iii) Task 8 iteration-env-safe smokes (bash -n syntax × 13 scripts + pnpm-run-listing smoke + `env:check` exit-code smoke + dispatcher-usage smokes for `clean.sh` / `whitelist-host.sh`) + Task 9 operator-workstation full-lifecycle recipe are both SHIPPED AS IMPLEMENTATION by dev-story (SC-23 + Task 8.6 + Task 9.1 record them as README verification recipes per Story 2.4 iter-174 + Story 2.5 iter-187 precedent) — a pre-written ATDD scaffold file would duplicate that source of truth. Downstream Epic 13 harness + post-dev SM review (`/bmad-create-story (args: "review")` at `traced → sm-verified`) + CR opener (`/bmad-code-review (args: "2")` at `sm-verified`) substitute for adversarial coverage. **Substrate touchpoints:** story file UNTOUCHED (no v0.3 Change Log row per Story 2.4 iter-173 + Story 2.5 iter-186 canonical pure-IP-operation precedent); IP + RALPH.md only. Sprint-status unchanged (`2-6: ready-for-dev` — dev-story skill auto-flips to `in-progress` on commencement per Task 10.6b). Invariant manifest untouched (no sourcePath edits); contentHash stability for `INV-devbox-homedev-named-volume` at v1.8 value `f34cb62feea03eb0d3ef80d29221fc85fa1d1ee3ba01e7e26ef06ee5c9715a5e` carried from Story 2.5 iter-196. **Forward-plan envelope:** dev-story iter-201 1-2-iter landing forecast — base envelope ~100-150 patch LOC + 4-docs-surface edits (README H3 + README H2 + AGENTS.md H3 + optional invariant-doc qualifier) across 14-18 files; AR-7/AR-9 conditional-escalation paths add +3 LOC (Dockerfile chown) or ~50 LOC (whitelist.sh/dnsmasq.conf `/run`→`/tmp/keel-state/` relocation) IF operator-workstation smoke surfaces breakage. iter-201 NOW = `/bmad-dev-story`. Budget consumed iter-200: ~35-40K (orient ~10K + story-file read ~20K + IP edits ~3K + RALPH.md signpost ~5K + commit + push).
- [x] iter-199: **Ran `/bmad-create-story (args: "review")` — pre-dev SM validation of Story 2.6.** 8 findings applied as v0.2 Change Log entry (AI-1..AI-8): **AI-1 CRITICAL** — SC-2 monitor-naming self-contradiction resolved (pinned `monitor-host.sh` per Story 2.4 `whitelist-host.sh` precedent; dropped "rename existing `monitor.sh`" path). SC-2 13-verb table + SC-3 package.json example + Task 1.2 + Task 2.11 + Dev Notes File List targets all updated consistently. **AI-2 CRITICAL** — PRD `:494` vs epics AC 7 semantic drift reconciled (`pnpm devbox:monitor` = FR1a JSONL DNS-event tail, NOT `docker stats`; architecture `:1003` reinforces). SC-15 now cites PRD `:494` as authoritative. **AI-3 ENHANCEMENT** — Task 10.6 split (10.6a `[x]` backlog→ready-for-dev completed-at-draft iter-198; 10.6b + 10.6c dev-story-flip `[ ]`) per iter-173 carry-forward rule. **AI-4..AI-8 OPTIMIZATION** — SC-3 typo, Task 1.2 simplification (HEAD state known; dropped (b)/(c) decision paths), Task 2.11 consistency, File List targets clarification, Change Log v0.2 entry. Story State `drafted → validated` (IP-internal; sprint-status stays `ready-for-dev`). RALPH.md § Lessons gained two carry-forward entries (cross-source semantic drift audit rule + `<verb>-host.sh` host-side shim convention rule). Budget: ~60-65K tokens (orient ~8K + full story file read ~20K + parallel cross-ref verification ~12K + 9 targeted Edit calls ~15K + IP + RALPH.md + commit ~5K). Higher envelope than typical pre-dev SM (~30-45K) — AI-1 + AI-2 were both root-cause issues requiring multi-SC reconciliation, not surface polish.
- [x] iter-198: **Ran `/bmad-create-story` (no args) — drafted Story 2.6 host-side `pnpm devbox:*` CLI surface.** Story file landed at `_bmad-output/implementation-artifacts/2-6-host-side-pnpm-devbox-cli-surface.md`. Drafted 13-verb host-side CLI surface (build/rebuild/clean/status/logs/shell/attach/start/stop/restart/whitelist/monitor/env:check) with 25 scope clarifications (SC-1..SC-25) + 10 tasks + full Dev Notes + File List targets + Change Log v0.1. **Story 2.5 CR DEFER absorption:** AR-10 pre-Story-2.5 volume-migration docs (SC-21; unconditional), AR-11 `KEEL_DEVBOX_TMPFS_{TMP,VARTMP}_MB` envvar shape validation (SC-14; unconditional), AR-12 COMPOSE_PROJECT_NAME docs polish (SC-22; unconditional); AR-7 `/run` relocation + AR-9 `/etc/*` chown escalations conditional on operator-workstation smoke outcome per SC-19 + SC-20. Sprint-status `2-6: backlog → ready-for-dev`; Story State `_(no story) → drafted`. Epic 2 counter 5/17 → story file exists for 6th story (still 5/17 done). PR #230 stays Draft (Epic 2 closes at Story 2.17). Budget: ~65-70K tokens (orient ~8K + 3-subagent parallel context extraction ~40K + file write ~15K + sprint-status + IP + commit ~5K). Higher than Story 2.5 iter-184 envelope (~45-55K) — larger surface (13 scripts vs 3 files) + thicker DEFER-absorption surface (5 DEFERs carried vs 2 from 2.4 opener) warranted the expanded subagent fan-out.

## Context

- **Phase:** 4-implementation — Epic 2 at 5/17 stories `done` (2.1 + 2.2 + 2.3 + 2.4 + 2.5) + 12/17 remaining; Story 2.6 atdd-scaffolded (ready-for-dev).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.6 live smokes operator-workstation-deferred per Story 2.4 SC-17 + Story 2.5 iter-187 precedent. 5-gate quality chain for docs-surface iters: sync-gate + typecheck + lint + format:check + docker compose parse-smoke.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Story 2.5 substrate live; post-drain HEAD builds cleanly. Story 2.6 is a host-side CLI layer — no Dockerfile changes at SM review (SC-25 scope-boundary forbids; AR-9 escalation path is the only exception).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; 5 done (2.1-2.5); Story 2.6 atdd-scaffolded; 11 backlog (2.7..2.17). Epic 2 closes at Story 2.17 (PR #230 Draft→Open transition at that point).
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.6 — host-side `pnpm devbox:*` CLI surface. Scope-owner for AR-7 `/run` relocation (SC-19, conditional on operator-workstation smoke) + AR-9 `/etc/*` chown (SC-20, conditional on operator-workstation smoke) + AR-10 pre-Story-2.5 volume migration docs (SC-21, unconditional) + AR-11 envvar shape validation (SC-14, unconditional) + AR-12 COMPOSE_PROJECT_NAME docs polish (SC-22, unconditional).
- **Story File:** `_bmad-output/implementation-artifacts/2-6-host-side-pnpm-devbox-cli-surface.md`
- **Story State:** `atdd-scaffolded` (skip) — transitioned at iter-200 via `/bmad-testarch-atdd` ATDD-skip decision (16th-cumulative FR14n ATDD-skip precedent; grounds (c)+(ii)+(iii) recorded in IP § DONE iter-200 + RALPH.md iter-200 signpost). Story file untouched; no v0.3 Change Log row per Story 2.4 iter-173 + Story 2.5 iter-186 precedent. iter-201 runs `/bmad-dev-story` to transition `atdd-scaffolded → in-dev` with 13-script + 4-docs-surface substrate landing.
- **GitHub Issue:** Story 2.6 TBD (will be surfaced by ralph.py at next iter if the GH project is configured). Epic 2 → #10 (Ralph closes on EPIC_DONE halt).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **Epic-2 story-class prediction (iter-198 + iter-199 validated):** Story 2.6 is **user-facing-CLI class** — parallel to Story 2.4 (`pnpm devbox:whitelist` 4-verb surface, 5-PATCH opener). Story 2.6 is broader (13 verbs vs 4) but per-script simpler (most are ~20-40 line `docker compose …` wrappers; no `compose_whitelist()` dual-composer complexity). CR-opener forecast unchanged from iter-198: 4-6 PATCH. Pre-dev SM review outcome: 2 CRITICAL + 1 ENHANCEMENT + 5 OPTIMIZATION applied — matches the user-facing-CLI SM-review envelope at the higher end (Story 2.4 iter-173 was 1 CRITICAL only; Story 2.6's wider-surface + epic-vs-PRD semantic-drift discovery pushed it higher).
- **Cross-source semantic-drift audit (iter-199 NEW LESSON):** the pre-dev SM review caught that epics AC 7 "cpu/memory/network" language drifted from PRD `:494` "JSONL DNS-event tail". Dev-agent reading AC 7 literally would have implemented `docker stats` — wrong semantic. Resolution: SC-15 + Dev Notes cite PRD `:494` as authoritative. **Carry-forward rule:** every future user-facing-CLI pre-dev SM (Stories 2.7 Ralph-wrapper, 2.8 claude OAuth, 2.9 gh OAuth, 2.10 prereq-check, 2.12 ssh, 2.15 hooks) MUST spot-check each AC verb against PRD § CLI-Tool Surface before `drafted → validated`. See RALPH.md § Lessons iter-199 entry.
- **`<verb>-host.sh` host-side shim convention (iter-199 formalised):** Story 2.4 established the pattern; Story 2.6 extends to `monitor-host.sh`. Future host-side shims adopt the `-host` suffix as a canonical naming convention — see RALPH.md § Lessons iter-199 entry. Candidate stories: 2.7 (Ralph TUI wrapper), 2.8 (claude-host.sh?), 2.9 (gh-host.sh?), 2.12 (ssh-host.sh?). Pattern: in-container primitive keeps its canonical unsuffixed name; host-side dispatcher gets the `-host` suffix.
- **SC-14 env-check var list is regenerate-at-impl-time:** seed list in story draft is NOT authoritative — dev-agent runs `rg 'KEEL_DEVBOX_[A-Z_]+' packages/devbox/ .envrc.example | grep -oE 'KEEL_DEVBOX_[A-Z_]+' | sort -u` at impl time to get HEAD-current list. Drift between draft and impl is expected (Story 2.11 + 2.12 may add knobs before Story 2.6 lands).
- **SC-19 + SC-20 conditional escalation paths:** AR-7 `/run` relocation + AR-9 `/etc/*` chown are opt-in IF operator-workstation smoke surfaces the breakage. Pre-emptive landing would scope-creep Story 2.6 into Story 2.3 primitive edits (SC-25 forbids). Deferred decision pending empirical data — operator runs SC-23 README verification recipes on M4-Pro native and reports findings back to impl iter.
- **Within-epic § Cross-epic transition (atdd-scaffolded iter-200 → dev-story iter-201):** Story 2.6 `atdd-scaffolded` + Epic 2 has 12/17 backlog stories (2.6..2.17; 2.6 at ready-for-dev with Story State `atdd-scaffolded`, 2.7..2.17 remain backlog) → NO halt required. Next iter runs `/bmad-dev-story` per lifecycle matrix row `atdd-scaffolded → in-dev`. Epic 2 stays `in-progress`.
- **Invariant-sourcePath contentHash stability at ATDD-skip (iter-200):** no invariant docs edited during ATDD-skip iter; `INV-devbox-homedev-named-volume` contentHash remains `f34cb62feea03eb0d3ef80d29221fc85fa1d1ee3ba01e7e26ef06ee5c9715a5e` (Story 2.5 iter-196 v1.8 value). SC-17 three-step sync-gate fires only if dev-story Task 5.3 (AR-12 docs polish) touches `docs/invariants/devbox-hardening.md`.
- **Sprint-status unchanged (iter-200):** ATDD-skip does NOT flip sprint-status — the `atdd-scaffolded` state is IP-internal only; sprint-status stays `ready-for-dev` until dev-story flips it to `in-progress`. Matches Story 2.4 iter-173 + Story 2.5 iter-186 canonical pure-IP-operation precedent.
