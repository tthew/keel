# Implementation Plan

## NOW

- [ ] **Re-run `/bmad-code-review (args: "2")` fresh-context** per § Story Lifecycle matrix row `fixes-pending → done | fixes-pending`. Scope: verify iter-252's PATCH-1 closes the iter-251 HIGH finding (`prereq-check.sh` exit 12 reachability) with no new surfaces introduced. Invoke in a fresh context (one workflow per iteration; Guardrail 5). Three-layer Ralph-hosted fan-out per iter-251 pattern (Blind Hunter `bmad-agent-architect` diff-only against `930b912..HEAD` range post-PATCH-1; Edge Case Hunter `bmad-tea` diff+project-read; Acceptance Auditor `bmad-agent-dev` diff+spec+INV doc). Likely outcome: ZERO-PATCH re-run → `fixes-pending → done`; `/bmad-code-review` → no new adversarial findings since the fix is a narrow ~15-line rc-capture refactor with no downstream shim changes. If the re-run surfaces novel findings on the patched surface (unlikely), another iter of `fixes-pending` before `done`. After `done`: mark Story 2.10 sprint-row `done`; NOW becomes `/bmad-create-story` for Story 2.11 per § Cross-epic within-epic auto-advance (sprint-status has next-in-epic backlog).

## QUEUE (Story 2.10..2.17 → Epic 2 close-out)

- [ ] _(after Story 2.10 `done`)_ Run `/bmad-create-story` fresh-context for Story 2.11 (Docker Compose shared mode — devbox substrate) per § Story Lifecycle row `_(no story) → drafted`. Sprint-status auto-marks Epic 2 unchanged (already `in-progress`); Story 2.11 sprint-row flips `backlog → drafted`.
- [ ] _(post-Story-2.10-done)_ Iterate Stories 2.11..2.17 through full lifecycle — 7 remaining substrate stories spanning shared mode + port-publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows. Stories 2.11..2.17 CR-gate forecast (iter-251 LESSON + iter-252 meta-LESSON): when a story introduces a novel multi-step runtime probe chain, forecast ≥1 PATCH at CR gate; pre-emptively audit for `set -e` + `if cmd; then` rc-suppression patterns at impl time to reduce CR-gate surprise.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-23 per iter-219..252 repeated pre-push confirmation — `gh pr view 230` returns `statusCheckRollup: []`).

## BLOCKED

- [ ] iter-249+250+251+252 push to `origin feat/epic-2-packaged-devbox` — REASON: SSH :22 to github.com STILL timing out at iter-252 step 5 push attempt (`ssh: connect to host github.com port 22: Connection timed out` — identical symptom to iter-249/250/251). HTTPS GraphQL :443 recovered at iter-252 orient (`gh pr view 230` returns valid JSON). SSH/HTTPS split: push traffic → SSH → BLOCKED; PR-probe queries → HTTPS → OK. 5 commits locally on `feat/epic-2-packaged-devbox` (5 ahead of upstream): `aefb382` (iter-249 push-fail) + `d908d6f` (iter-249 trace-WAIVED) + `447bd84` (iter-250 post-dev SM) + `b701e97` (iter-251 CR) + `1e67b84` (iter-252 PATCH-1).
  - Attempted: push attempts at iter-249/250/251/252 all failed with SSH :22 timeout to github.com.
  - Error/Issue: persistent egress block on SSH :22 to github.com from iter env across 4 consecutive iters; HTTPS :443 to api.github.com partially recovered but git push is hard-wired to the SSH remote URL.
  - Next: iter-253 attempts push again at step 5 (bank on intermittent recovery). If iter-253+ still fails, escalate to operator — **recommended operator action**: one-off `git remote set-url --push origin https://github.com/tthew/ralph-bmad.git` on the host to unblock. Ralph SHOULD NOT autonomously reconfigure the remote URL (shared-state mutation beyond single-iter scope per § Executing actions with care). Commits are safe in local branch — no force-push, no amend, preserved intact across iter-249..252.

## ATDD Red Phase

_(none — Story 2.10 ATDD-skip applied at iter-247; mock-docker smokes at iter-252 are ephemeral + torn down inside the same iter per iter-223/230/237/248 LESSON; no persisted red-phase artefacts.)_

## DONE (iter-252 Story 2.10 PATCH-1 landed — Story State stays `fixes-pending` pending CR re-run; sprint-row `review` unchanged)

- [x] iter-252: **STORY 2.10 PATCH-1 impl landed — `prereq-check.sh` rc-capture refactor makes exit 12 reachable across 3 docker-daemon-error paths.** Scope-bounded direct edit (NOT `/bmad-dev-story` — single-primitive ~15-line bash refactor) to `packages/devbox/scripts/prereq-check.sh:83-118` replacing three `if docker … ; then … ; fi` wrappers (volume-inspect L84 + Claude probe L95 + gh probe L99 per pre-patch numbering) with explicit `rc=0; docker … || rc=$?; case "${rc}" in 0) present=1 ;; 1) present=0 ;; *) log "docker daemon error during <site> (rc=${rc})"; exit 12 ;; esac`. Exit 12 now surfaces `rc!∈{0,1}` classes (125 image-pull-fail, 126 exec-fail, 127 cmd-not-found-in-stub, etc.) with distinct pointer per probe site ("volume inspect" / "Claude token probe" / "gh token probe"). **Nine mock-docker smokes 9/9 green** (PATH-shimmed mock docker from `/home/dev` since `/tmp` is `noexec` — minor iter-env gotcha surfaced): (1) tier-1 unreachable → exit 8 + install pointer; (2) fresh-fork first-run volume-missing → exit 2 + both auth pointers; (3) volume-inspect rc=125 → exit 12 + volume-daemon-error pointer; (4) Claude probe rc=125 → exit 12 + Claude-token-probe pointer; (5) gh probe rc=125 → exit 12 + gh-token-probe pointer; (6) both tokens present → exit 0 silent; (7) both missing → exit 2 + both pointers; (8) `--tier1` → exit 0 silent (token probes skipped); (9) `--bogus` → exit 2 + usage. **INV doc + README + manifest contentHash UNCHANGED** — iter-251 triage already identified that impl would now match the existing `"propagated via docker's exit"` design intent verbatim; no drift on any of the 4 SOT sites (`prereq-check.sh` header L25-26, INV doc § Exit-code schema L41, README L748, manifest description). `pnpm keel-invariants:check` EXIT=0 green (Story 1.9 sync-gate). **Story State stays `fixes-pending`** — PATCH-1 impl is complete but CR closure only flips via a re-run `/bmad-code-review (args: "2")` in fresh context (queued NOW for iter-253). Story file Change Log entry NOT added at iter-252 per iter-201 precedent: narrow-scope PATCH impl lands with RALPH.md signpost but no Change Log bump until CR re-run confirms closure (keeps the Change Log entries at gate-transitions, not intra-gate). Sprint-status UNCHANGED per iter-202 precedent (CR gate does NOT flip sprint-row until `done` transition). **NOVEL LESSON (RALPH.md signpost):** `set -euo pipefail` + `if cmd; then …; fi` silently discards cmd's non-zero rc because `set -e` does NOT fire inside the `if` test expression — for runtime probes where distinct non-zero rcs carry distinct operator-remediation semantics, use explicit `rc=0; cmd || rc=$?; case "${rc}" in 0) …; N) …; *) fail ;; esac` pattern. Mechanical-forecast for Stories 2.11..2.17: pre-emptively audit new multi-step runtime probe chains for this pattern at impl time. Also GOTCHA: iter-env `/tmp` is `noexec`; PATH-shimmed mock binaries must live under `/home/dev` / `/root` / `/workspace` (propagates to any story using mock-binary smoke harnesses). **Push probe recovery partial:** `gh pr view 230` GraphQL succeeded at iter-252 orient (iter-249/250/251 all failed with i/o timeout); SSH :22 not re-tested yet; will retry at step 5. Budget iter-252: ~32K (orient ~8K + Read prereq-check.sh + Edit 3-block + 9 mock-docker smokes + INV sync-gate + RALPH.md signpost + IP + commit).

_(iter-243..251 Story 2.9 closure + Story 2.10 lifecycle iters pruned per Guardrail 2 — see Story 2.10 § Change Log v1.0–v1.6 + RALPH.md Signposts for per-gate detail.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **9/17 stories done** (2.1-2.9) + 8/17 in-flight-or-backlog (2.10 `Story State = fixes-pending` sprint-row `review` at iter-252 post-PATCH-1; 2.11..2.17 still backlog).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered at iter-248 9/9 stub-docker harness + iter-252 9/9 mock-docker PATH-shim harness (two independent harness approaches, both green).
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:119). Host-side shim count: **18** at iter-252 (unchanged from iter-248).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **9 done** (2.1-2.9); 1 at sprint-row `review` Story State `fixes-pending` (2.10); 7 backlog (2.11..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.10 — Prerequisite check (Docker runtime + Claude auth + gh auth) with pointer errors.
- **Story File:** `_bmad-output/implementation-artifacts/2-10-prerequisite-check-docker-runtime-claude-auth-gh-auth-with-pointer-errors.md`
- **Story State:** `fixes-pending` — PATCH-1 impl landed iter-252; NOW is CR re-run to confirm `fixes-pending → done`. Per § Story Lifecycle row `fixes-pending → done | fixes-pending`.
- **GitHub Issue:** Story 2.10 likely unwired (`RALPH_ISSUE_NUMBER` unset at iter-250/251/252 check; skip `Refs #<story-issue>` in commits). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — `{"isDraft":true,"mergeStateStatus":"CLEAN","state":"OPEN","statusCheckRollup":[]}` confirmed at iter-252 orient (GraphQL back online after iter-249/250/251 timeouts).

## Notes

- **iter-252 Story 2.10 PATCH-1 — scope-bounded direct edit; NOVEL LESSON on `set -e`+`if cmd; then` rc-suppression.** The rc-capture pattern is the canonical substrate form for future multi-step runtime probe primitives; iter-251's HIGH finding + iter-252's fix together establish a precedent forecastable across Stories 2.11..2.17 (mechanical carry-forward).
- **iter-251 Story 2.10 CR — FIRST CR-gate patch on 4-story ZERO-PATCH chain FALSIFIED.** Adversarial triage (Blind+Edge+Auditor) surfaced the `if cmd` rc-suppression gap neither SM nor narrow-AC verification caught. LESSON: adversarial CR catches what narrow-AC verification cannot; CR-after-SM separation is load-bearing for multi-step runtime probe primitives. Chain continuity: Stories 1.13 iter-85 → 2.7 iter-226 → 2.8 iter-233 → 2.9 iter-244 remain valid (4 cumulative ZERO-PATCH CR precedents); Story 2.10 is FIRST CR-gate patch on the chain.
- **iter-250 Story 2.10 post-dev SM ZERO-PATCH — 5th cumulative precedent.** Two-subagent verification pattern (iter-235 LESSON) valid for post-dev SM gates but INSUFFICIENT for CR gates (iter-251 LESSON).
- **iter-249 Story 2.10 trace-WAIVED — 20th cumulative precedent.** Mechanical template inheritance.
- **iter-248 Story 2.10 impl — mechanical inheritance from Stories 2.6/2.8/2.9.** Impl smoke harness at iter-248 was stub-docker 9-smoke (workspace-based). Iter-252 extended the harness concept with mock-docker PATH-shim 9-smoke (home-dev-based under `/tmp` noexec constraint) — two independent harness approaches, both green.
- **iter-244/246 audit-findings carry-forward to post-PATCH-1 CR re-run** (unchanged from iter-247..251): (1) restart.sh transitive-delegate prepend-only pattern; (2) benchmark.sh OUT-OF-SCOPE clarification (SC-11); (3) Manifest entry count drift (29→30 baseline); (4) Alpine egress non-applicability doctrine (SC-4 + § Alpine probe image). None of these are iter-252 concerns.
- **Substrate-citation drift forecast for Stories 2.11..2.17 (unchanged from iter-251 Notes):** Story 2.10's 1-drift outcome confirms prepend-only edits shift fewer downstream lines. Forecast: Stories 2.11..2.17 accumulate 1-3 drifts each, deferred to Story 2.17 SC-17 polish pass. Total Epic 2 close-out reconciliation forecast: ~10-20 cumulative line-number drifts.
- **iter-252 GOTCHA — iter-env `/tmp` is `noexec`.** PATH-shimmed mock binaries fail with rc=126 "Permission denied" when dropped in `mktemp -d` default path (`/tmp/tmp.XXXXXXXXXX/docker`). Workaround: create mock dir under `/home/dev`, `/root`, or `/workspace`. Forecast-actionable for any future story using mock-binary smoke harnesses (Stories 2.13 healthcheck emitter + Story 2.14 legacy-branch cleanup are candidates).
