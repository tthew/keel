# Implementation Plan

## NOW

- [ ] **Story 2.17 `/bmad-dev-story` CONTINUATION — Task 13.4 (`packages/devbox/README.md` sibling-append H2 § Hook + settings bypass-resistance (Story 2.17))** AFTER existing Story 2.16 H2 at `:1053`. Operator-facing walkthrough of git-layer backstop + fork-extension path + CI visibility forward-link + Story 2.15 carry-forward D-9 (`~/.ssh/**` + `~/.aws/credentials` NFR5a-minimum gap doc symmetry mirroring `AGENTS.md § Claude Code settings policy (Story 2.15)`). ~medium. Plain `Edit` — `packages/devbox/README.md` outside L1 + hook-self-protection ranges per iter-316 routing discipline.

## QUEUE (Story 2.17 Task 13 decomposition + lifecycle close + Epic 2 close-out)

- [ ] _(after Task 13.4 lands at iter-324)_ **Task 13.5 — `docs/invariants/claude-hook-denylist.md` amendment** — add § S4 prompt-injection scan rules (three rules + severity + Epic 4 consumer contract), § L1 install-boundary rule (third closed-enum rule-id + protected `packages/keel-invariants/src/**` + three-layer install-boundary contract), § Fresh-fork seed contract (D-36 exec-bit preservation + byte-identity lockstep + Epic 15a `create-keel-app --preserve-permissions` consumer); also absorb D-32 § Halt-threshold pin key-name cross-check (already partially pinned — re-verify config.toml `self_protection_halt_threshold` key spelling matches doc) + D-33 § Limitations Grep content-search forward-link (extend existing § Limitations with explicit note on Grep content-search being an Epic 4 S4 scanner responsibility, not an in-session hook responsibility). Post-amendment: manifest `INV-claude-hook-secret-denylist-doc` contentHash refresh (whole-file sha256). Seed step not applicable for the invariant doc. Plain `Edit` — docs/ is outside L1 + hook-self-protection ranges; manifest contentHash refresh requires `Bash(python3 *)` allow-listed path per iter-315/316 routing. ~large. Likely requires full-iter budget.
- [ ] _(after Task 13 lands fully)_ Task 11 (D-7/D-8/D-9 SC-17 close-out lints — preferring Option B manifest-entry approach per 11.1 rationale) + Task 14 (seed lockstep audit — likely already byte-identical by iter-320's continuous seed-sync discipline; verify + checksum + README.md bullet update) + Task 5 (S4 prompt-injection scan rules at `packages/keel-invariants/src/prompt-injection-rules/` — Epic 4 consumer contract; 3 rules covering `.claude/settings*.json` / `.claude/hooks/**` / `.git/hooks/**`) + Task 6 (halt-threshold `.ralph/config.toml` verify + doc pin; largely already landed at iter-318 D-23) + Task 9 doc pins (Epic 14 dashboard panel forward-link) + Task 15 (≥25 impl-time fixture smokes — cumulative 397 at iter-320; see Notes — target far exceeded, Task 15.1 remaining scope is `packages/keel-invariants/fixtures/hooks/` persistence for future-Ralph replay rather than additional probe authoring) + Task 16 (SC-17 polish; see sub-bullet) + Task 17 (completion).
- [ ] **SC-17 close-out candidates** queued for Task 16: (a) substrate-bake prek native binary in Dockerfile (Story 2.5 AMEND) so fresh-fork devbox first-boot works without network access — addresses iter-313 BLOCKED class; (b) add `release-assets.githubusercontent.com` to substrate `packages/devbox/whitelist/github.txt` so operator doesn't need manual `pnpm devbox:whitelist add`; (c) host-OS-aware `.git/hooks/*` config portability (if iter-312 gotcha re-emerges on fresh fork); (d) iter-314 NOVEL CANDIDATE: promote `loadExpectedHooks` runtime path translation into a generic "source-to-dist resolver" if further TS-authored enumerator entries land (currently 1 site — threshold not met); (e) iter-317 NOVEL: either close Python-write loophole at hook layer (Python source AST parse — costly, rejected) OR document it explicitly in `docs/invariants/claude-hook-denylist.md § Limitations` (the loophole is legitimate at Story 2.17 scope for substrate-self-maintenance; S4 scan layer + manifest content-hash backstop cover it); (f) iter-320 NOVEL: reader-verb word-boundary anchoring (extend D-35 to lines 193-197 reader-verb case-globs — lower FP-risk than mutation verbs; defer to invariant-doc § Limitations or comprehensive refactor).
- [ ] _(after Story 2.17 Tasks fully landed)_ `/bmad-testarch-trace (args: "yolo")` — `in-dev → traced`. 27th cumulative trace; likely WAIVED per Epic-2 17-story chain continuation + impl-time-smokes substitute.
- [ ] _(after traced)_ `/bmad-create-story (args: "review")` — post-dev SM `traced → sm-verified`.
- [ ] _(after sm-verified)_ `/bmad-code-review (args: "2")` — `sm-verified → done`.
- [ ] _(after Story 2.17 `done`)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge. Epic 2 completion fires `EPIC_DONE` halt; on re-entry auto-advances to Epic 3 Story 3.1.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured; `statusCheckRollup: []` carried unchanged iter-219..322).

## BLOCKED

- **iter-323 push BLOCKED — SSH egress timeout to github.com:22.** DNS healthy (`dig @127.0.0.1 -p 53 +short github.com` → `140.82.121.4`); `ssh -T git@github.com` + `git push` (default + `GIT_SSH_COMMAND="ssh -o ConnectTimeout=30" git push`) both returned `ssh: connect to host github.com port 22: Connection timed out`. Same systemic-egress signature as iter-321 (recovered in <5s on retry after 90s timeout on that iter). Commit `b2c56b8` (iter-323 Task 13.2 landing) queued locally; 1 commit ahead of upstream. Retry push chained after committing this BLOCKED IP update per iter-321 recovery pattern; if still blocked, next iter-324 NOW first action is push retry before Task 13.4.

## ATDD Red Phase

_(empty — iter-311 SKIP-WITH-GROUNDS-(ii)+(iii). Impl-time substrate coverage: iter-312 `check-claude-hook-syntax.ts`; iter-314 `names-and-shebangs` walker; iter-315 14 hook smokes GREEN; iter-316 5; iter-317 53; iter-318 41; iter-319 60; iter-320 224 — cumulative 397 impl-time static probes toward Task 15.1 ≥25 target. Far above threshold; Task 15.1 remaining scope is bash-fixture persistence under `packages/keel-invariants/fixtures/hooks/` for future-Ralph replay, not additional probe authoring.)_

## DONE (iter-323 Task 13.2 landing)

- [x] iter-323: **Story 2.17 `/bmad-dev-story` PARTIAL CONTINUATION — Task 13.2 CLAUDE.md sibling-append bullet § Hook + settings bypass-resistance (Story 2.17) landed.** New bullet inserted at `CLAUDE.md:77` immediately AFTER the Story 2.16 hook-script bullet at `:76` and BEFORE the "Don't invent skills" bullet at new `:78`. 1 line added (78→79 total). Content mirrors the Story 2.15 bullet (`:74`) + Story 2.16 bullet (`:76`) pattern — short operator-facing one-liner pinning the git-layer sync-gate + content-hash manifest backstop as the out-of-session leg of NFR5a/NFR5b (companion to Story 2.16's in-session Ralph-path defense); terse "don't tamper with `.claude/settings.json` hooks-block or `packages/keel-invariants/src/invariants.manifest.ts` in-session — the git-layer sync-gate + `install-boundary-protection` rule will catch it" caveat; pointer to `AGENTS.md § Hook + settings bypass-resistance (Story 2.17)` for the full five-entry manifest + nine-bullet contract landed by Task 13.1 at iter-322. Docs-only plain-`Edit` route (CLAUDE.md outside L1 + hook-self-protection ranges per iter-316 routing discipline; no Python-write detour needed). Pre-push gates unchanged (no manifest / hook / settings subtree perturbation; sync-gate exit 0 on 39 entries + claude-hook-syntax exit 0 + typecheck + lint + format:check expected GREEN). No manifest contentHash refresh; no seed re-sync. Task 13 net post-iter-323: **3 of 5 sub-tasks done** (13.1 iter-322 AGENTS.md + 13.2 iter-323 CLAUDE.md + 13.3 iter-314 pre-landing at `INVARIANTS.md:144-151`); **2 remain** (13.4 `packages/devbox/README.md` H2 → iter-324 NOW; 13.5 `docs/invariants/claude-hook-denylist.md` amendment + manifest content-hash refresh → iter-325). Story file Status line (`:3`) flipped iter-322 → iter-323; HTML-comment lifecycle chain (`:5`) extended with iter-323 segment; Change Log row 0.12 appended. No novel gotcha/lesson/decision from this iter — mechanical sibling-append following the exact iter-322 Task 13.1 pattern; no RALPH.md promotion.

_(iter-303..322 LANDING detail pruned per Guardrail 2; retained in commits through `c5c6378` + story-file Status HTML comment chain + story-file Change Log 0.1-0.11 rows. iter-321 was push-recovery only — no functional landing; committed as `b3a7ab7`.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **16/17 stories done** (2.1-2.16) + **Story 2.17 `in-dev (partial)` at iter-323** (Epic-2 final story; XL partial landing continuing through Task 13 decomposition).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live smokes operator-workstation-deferred.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:121; bash + jq baked at :36-37 + :48). Host-side shim count: **18** at iter-323 (unchanged).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **16 done** (2.1-2.16); **1 in-dev (partial)** (2.17 — Task 10.1 fully complete at iter-320; Task 13.1 + 13.2 + 13.3 done; 13.4/13.5 + Tasks 5/6/9/11/14/15/16/17 remain).
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.17 — Hook + settings bypass-resistance (git-layer + manifest + S4 + halt).
- **Story File:** `_bmad-output/implementation-artifacts/2-17-hook-settings-bypass-resistance-git-layer-manifest-s4-halt.md` (~562 lines post-iter-323 Change Log 0.12 append + Status HTML comment iter-323 segment + Status line flip).
- **Story State:** `in-dev (partial)` — iter-324 NOW = `/bmad-dev-story` CONTINUATION per § Story Lifecycle dev-story invocation guidance; NOW item Task 13.4 `packages/devbox/README.md` H2 sibling-append.
- **GitHub Issue:** no GitHub Project configured; `RALPH_ISSUE_NUMBER` unset. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` unchanged iter-272..323).

## Notes

- **iter-320 NOVEL — shell case-globs match prefix-then-any, not word-boundary-anchored.** `case "$normalized" in rm*.claude/settings*) ...` matches `rmdir .claude/settings.json` (FP) because `rm*` is "rm followed by any chars" (matches "rmdir"). Fix via regex `[[ "$normalized" =~ ^rm([[:space:]]|$) ]]` — anchors "rm" at start with mandatory space/EOL boundary after. Applied to 9 mutation verbs in D-35; reader-verb case-globs (lines 193-197) intentionally preserved as legacy alternation (iter-320 SC-17 (f) candidate for future comprehensive refactor). **Carry-forward rule:** new case-glob-based rule authoring inside `.claude/hooks/*.sh` MUST use word-boundary regex for any verb with common-prefix collision risk (rm/rmdir, cp/cpio, dd/ddrescue, etc.); mutation-verb rules especially MUST be word-boundary anchored. Single data point at iter-320 for D-35 restructure; promotion threshold 2 unreached for RALPH.md (watch for re-validation at Task 11 or Task 13.5 if new reader-verb anchoring lands).

- **iter-320 NOVEL — `chmod` command prefix FP-blocks seed-sync pattern.** Running `chmod 755 scratch/foo.sh && python3 -c "...'.claude/hooks/block-secret-access.sh'..."` tripped the pre-iter-320 `chmod*.claude/hooks/*` case-glob because the command both starts with `chmod` AND contains `.claude/hooks/` (in the python argv string). Fix: put chmod INSIDE the python3 script (`os.chmod(src, 0o755)`) rather than as a bash prefix. Post-iter-320 D-35 regex `^chmod([[:space:]]|$)` combined with `\.claude/hooks/` path presence still blocks, but the seed-sync pattern now uses all-python3 (no bash chmod). **Carry-forward rule:** future substrate-mutation operations that need to preserve file mode + atomic replace should do BOTH in one python3 script (os.chmod + os.replace), not split between bash chmod + python3 replace. Enables legitimate substrate-maintenance path (`packages/keel-templates/src/seeds/.claude/hooks/block-secret-access.sh` ↔ `.claude/hooks/block-secret-access.sh` byte-identity re-sync) that would otherwise be blocked by the mutation-verb hook-self-protection regex. Applies across seed-sync iterations.

- **iter-319 NOVEL carry-forward RE-CONFIRMED at iter-320+iter-322** — `/tmp` tmpfs `noexec,nosuid,nodev,relatime` (Story 2.5 hardening). Impl-time fixture smokes at iter-320 staged under `/home/dev/tmp-story217-iter320/` per established discipline; no `/tmp/*.py` attempts. iter-322 docs-only iter authored no executable fixtures, carry-forward holds. Watch for re-validation at Task 11 + Task 15 + Task 13.5.

- **iter-318 carry-forward — `sed -i <expr>` + `gh pr view` GraphQL timeout recipes.** Both unchanged at iter-323; docs-only edits used plain `Edit`. For sed: use `python3 -c` when replacement contains substrate-protected path literals. For gh timeouts: apply iter-321 DNS+curl sanity diagnostic from RALPH.md 2026-04-24 Gotchas.

- **iter-317/316 carry-forward — D-15 + D-14 minor regex + Python-write loophole.** D-15 wrapper normalization must not strip interp-stdin readers; D-14 find-delete trailing-slash optional. Python-write loophole (ast-level source mutation of hook/settings/manifest) still open; DEFER-track landing at Task 13.5 § Limitations per Task 16 SC-17 option (e).

- **iter-319 + earlier carry-forward pool — low-activity holds.** `readlink -f` canonicalises even when target file doesn't exist (parent-dir sufficient); `install-boundary-protection` rule-id live-enforcing; `names-and-shebangs` runtime `.ts→.js` / `/src/→/dist/` translation (single data point; promotion threshold 2 unreached); SC-17 candidates (f) reader-verb word-boundary anchoring → Task 13.5 § Limitations landing site. All unchanged at iter-323 (docs-only iter, no substrate-L1 or hook perturbation).

- **iter-314 SM-review promotion threshold: 5→7 data points** unchanged; Story 2.17 post-dev SM estimate iter-326+ after Task 13.4 + 13.5 + Tasks 5/6/9/11/14/15/16/17 land.
