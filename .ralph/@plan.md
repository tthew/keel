# Implementation Plan

## NOW

- [ ] `/bmad-create-story (args: "review")` — FR14n row 2 `drafted → validated` for Story **1.15**. Fresh-context Sonnet pre-dev SM-review audits seven preventative audit layers L1-L7 applied at iter-81 drafting against `_bmad-output/implementation-artifacts/1-15-renovate-config-with-version-pinning-rules-i7.md`. Expected residual class per Stories 1.11 iter-54 + 1.12 iter-61 + 1.13 iter-68 + 1.14 iter-75 compound precedent: ≤2 PATCHes (L4/L5-class — internal-consistency, cross-file line-number, cardinality-drift). **Scope of SM audit:** (a) L1 regex-conformance of `INV-deps-version-pinning` + `INV-renovate-rationale`; (b) L2 Task↔AC bidirectional coverage (AC 1 → Tasks 1/3/4/6; AC 2 → Task 1 `automerge: false` + Task 6; AC 3 → Task 1 `groupName: "opentelemetry"` + Task 6; AC 4 → Tasks 2/3/4/6); (c) L3 sprint-status transition wording; (d) L4 cross-AC convergence (4-packageRules / rangeStrategy pin / `.github/renovate.json` location / `config:recommended` extends / `automerge: false` posture); (e) L5 cardinality + cumulative-counter sequences (NINTH ATDD-skip / NINTH WAIVED-trace / FIFTH ZERO-PATCH-CR mechanically-derived); (f) L6 schema-permission diff clean (zero cascade-hash-update sites); (g) L7 Renovate config-shape contract. FIFTH cumulative ZERO-PATCH post-dev SM precedent CANDIDATE (Stories 1.11 iter-58 + 1.12 iter-65 + 1.13 iter-72 + 1.14 iter-79 + 1.15 iter-82?).

## QUEUE (Story 1.15 — renovate config i7)

- [ ] iter-83: `/bmad-testarch-atdd` — FR14n row 3 `validated → atdd-scaffolded` via hybrid ground-(c) variant-(ii)+(iii) ATDD-SKIP (NINTH cumulative precedent — Stories 1.7-1.14 → 1.15). Three-ground rationale pre-staged in story § Testing Standards lines 215-225: (a) substrate-verification-covers-ACs at CLI-exit-code level (Task 6 six smokes); (b) no-runner (test-runner still Epic 13 / post-Epic-1); (c) HYBRID variant-(ii)+(iii) — Renovate runtime + Epic 13 CI gate own AC 2 + AC 3 runtime halves; adversarial coverage of AC 1 + AC 4 delegated to iter-87 CR. Expected: 1 in-situ Change Log row (v1.2) mirroring Story 1.14 iter-76 pattern; Story State `validated → atdd-scaffolded`.
- [ ] iter-84: `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/1-15-renovate-config-with-version-pinning-rules-i7.md")` — FR14n row 4 `atdd-scaffolded → in-dev`. SINGLE-PASS CANDIDATE at configuration-surface scale (matches Story 1.14 iter-77 single-pass precedent). Expected deliverables: 2 NEW files (`.github/renovate.json` + `docs/invariants/renovate.md`), 2 MODIFIED files (`packages/keel-invariants/src/invariants.manifest.ts` +2 entries + `INVARIANTS.md` +1 new H3 + 2 bullets). Six Task-6 substrate smokes expected PASS: static JSON parse, extends-+-schema check, 4-packageRules + automerge-false enumeration, I7 pin-mode consistency, manifest-load (22 invariants), sync-gate clean. Remember iter-77 Gotcha: `pnpm --filter @keel/keel-invariants build` before runtime manifest-load smoke (dist/ cache staleness).
- [ ] iter-85: `/bmad-testarch-trace (args: "yolo")` — FR14n row 5 `in-dev → traced`. NINTH cumulative WAIVED precedent CANDIDATE (Stories 1.7-1.14 → 1.15). Four trace artefacts authored: `1-15-coverage-matrix.json` + `1-15-e2e-trace-summary.json` + `1-15-gate-decision.json` + `1-15-renovate-config-with-version-pinning-rules-i7.md` trace report. Structural inheritance from 1.11-1.14 (~90% reasoning-cost savings); mechanical 4-AC template adaptation (vs Story 1.14's 6-AC — Story 1.15 has FEWER ACs). All 6 Task-6 smokes re-verified LIVE byte-identical to iter-84 dev-story record. Adversarial coverage delegated to iter-87 CR per variant-(iii).
- [ ] iter-86: `/bmad-create-story (args: "review")` — FR14n row 7 `traced → sm-verified` post-dev SM requirements-satisfaction verification. FIFTH cumulative ZERO-PATCH post-dev SM precedent CANDIDATE (Stories 1.11/1.12/1.13/1.14 → 1.15). Independent fresh-context Sonnet Explore subagent audits all 4 ACs against committed iter-84 implementation + live filesystem state. AC 1 + AC 4 substrate-verified (ZERO unmet-AC findings expected); AC 2 + AC 3 SCOPE-CARVED to downstream runtime per story scope-carve-outs.
- [ ] iter-87: `/bmad-code-review (args: "2")` — FR14n row 9 `sm-verified → done`. FIFTH cumulative ZERO-PATCH CR precedent CANDIDATE (Stories 1.11/1.12/1.13/1.14 → 1.15). Three-layer adversarial fan-out (Blind Hunter + Edge Case Hunter + Acceptance Auditor) on iter-84 dev-story commit. Triage per convergence-doctrine + ZERO-PATCH-doctrine + adversarial-triage default. Expected outcome: 0 PATCH / 0 DECISION-NEEDED / 5-8 DEFER / ~10-13 DISMISS (lower DEFER vs Story 1.14's 10, due to smaller 4-packageRules surface + no `.release-please-manifest.json` hash-cascade concerns + no "release-please WORKFLOW = Story 13.5" consumer-deferral ambiguity — Renovate App install is a cleaner ops carve-out). After CR: Story State `sm-verified → done`; BMad Status `review → done`; sprint-status `1-15-...: review → done` + `last_updated: 2026-04-21 Story-1-15-done UTC`. Next: iter-88 starts Story 1.16 (`_(no story) → drafted` for `1-16-fork-extension-config-pattern-growth-tier-invariants-fork-md-scaffold`). Epic 1 NOT yet complete; PR #226 stays Draft through Story 1.16 completion.

## BLOCKED

_(none)_

## DONE (current epic — trailing iterations only)

- [x] iter-79: **FR14n row 7 `traced → sm-verified` via `/bmad-create-story (args: "review")` post-dev SM — FOURTH cumulative ZERO-PATCH post-dev SM precedent** (Stories 1.11 iter-58 + 1.12 iter-65 + 1.13 iter-72 + 1.14 iter-79). Independent fresh-context Sonnet verifier. All 6 ACs MET / SCOPE-CARVED. Zero PATCHes applied.
- [x] iter-80: **FR14n row 9 `sm-verified → done` via `/bmad-code-review (args: "2")` — FOURTH cumulative ZERO-PATCH CR precedent** (Stories 1.11 iter-59 + 1.12 iter-66 + 1.13 iter-73 + 1.14 iter-80). Three-layer adversarial fan-out: 0 PATCH / 0 DECISION-NEEDED / 10 DEFER / ~13 DISMISS. 10 DEFER route: Story 13.5 (6) + spec amendments (3) + Epic 3 reliability (1). Compound-ZERO-PATCH SATISFIED. 7-iteration `drafted → done` lifecycle held exactly. Story 1.14 complete.
- [x] iter-81: **FR14n row 1 `_(no story) → drafted` via `/bmad-create-story` for Story 1.15 (`renovate-config-with-version-pinning-rules-i7`).** Story file authored at `_bmad-output/implementation-artifacts/1-15-renovate-config-with-version-pinning-rules-i7.md` (~350 lines vs Story 1.14's 414 — terser at smaller surface). **FOUR ACs + SIX Tasks** (vs Story 1.14's 6 ACs + 7 Tasks — smaller surface: 1 new JSON + 1 new markdown + 2 manifest entries vs Story 1.14's 2 new JSON + 1 new markdown + 3 manifest entries). **TWO new invariants pre-staged**: `INV-deps-version-pinning` (AC 4 EPIC-VERBATIM ID per epics.md:1107) + `INV-renovate-rationale` (companion doc mirroring Story 1.14 `INV-release-please-rationale`). Manifest will grow **20 → 22** entries. **Seven preventative audit layers L1-L7 applied at drafting** (NINTH cumulative apply Stories 1.7-1.14 → 1.15): L1 both IDs Zod-regex verified mentally; L2 task-AC bidirectional coverage 4 ACs × 6 Tasks mapped; L3 sprint-status transition pinned (`backlog → ready-for-dev`; `last_updated: 2026-04-21 Story-1-15-drafted UTC`); L4 cross-AC convergence ("4 packageRules" / "`config:recommended`" extends / "`.github/renovate.json`" location / "`rangeStrategy: pin`" / "`automerge: false`" posture — all 5+ sites each converge); L5 cardinality MECHANICALLY-DERIVED per iter-76 LESSON (16 workspace members verified via `ls -d apps/*/ packages/*/ | wc -l`; 20-entry manifest verified; cumulative counters: NINTH ATDD-skip / NINTH WAIVED-trace / FIFTH ZERO-PATCH-CR CANDIDATES); L6 schema-permission diff clean (zero `package.json` / `.pre-commit-config.yaml` / `tokens.json` / `release-please-*.json` / `docs/invariants/release.md` edits → zero cascade-hash-update sites); L7 Renovate config-shape contract (`$schema` + `extends: ["config:recommended"]` + `packageRules` array with `matchPackageNames` / `matchPackagePatterns` / `matchDatasources` + `groupName` + `rangeStrategy: pin` + `automerge: false` + `lockFileMaintenance`). **Hybrid ATDD-skip ground-(c) variant-(ii)+(iii) clause PRE-STAGED** at § Testing Standards lines 215-225 (ninth precedent candidate — Renovate GitHub App install + Epic 13 CI gate own AC 2 + AC 3 runtime halves; CR pass owns AC 1 + AC 4 adversarial coverage). **Architecture drift: NONE** (`.github/renovate.json` location converges across architecture.md:649 + architecture.md:810 + epics.md:1090 — contrast Story 1.14's `.github/release-please-config.json` vs architecture.md:807 drift that was inherited). **Sprint-status updated**: `1-15-renovate-config-with-version-pinning-rules-i7: backlog → ready-for-dev` + `last_updated: 2026-04-21 Story-1-15-drafted UTC` (both header-comment + data-field sites). Story State: `_(no story) → drafted`. Prettier-check PASS on new story file + sprint-status.

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1-1.14 done; Story 1.15 drafted iter-81; Story 1.16 backlog — 1 more full FR14n lifecycle × Story 1.15 + 1 full × Story 1.16 before EPIC_DONE halt).
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (continues through Epic 1; PR #226 stays Draft until EPIC_DONE per PROMPT_build.md step 5c).
- **Story:** Story 1.15 — Renovate config with version-pinning rules (I7).
- **Story File:** `_bmad-output/implementation-artifacts/1-15-renovate-config-with-version-pinning-rules-i7.md`.
- **Story State:** `drafted` — FR14n matrix row 1 complete at iter-81; row 2 (`drafted → validated`) next at iter-82.
- **GitHub Issue:** Story 1.15 at **#39** (https://github.com/tthew/ralph-bmad/issues/39). Parent Epic 1 at **#9** OPEN (closes at EPIC_DONE halt). Story 1.14 at #38 expected CLOSED on PR merge (ralph.py wiring; PR merge waits for EPIC_DONE).
- **PR:** #226 (Draft, empty statusCheckRollup — stays Draft through Epic 1; Draft→Open at EPIC_DONE after Stories 1.15 + 1.16 complete FR14n lifecycle).

## Notes

- **Story 1.15 drafted at iter-81 — TERSER than Story 1.14 (~350 lines vs 414).** Smaller surface: 2 new files + 2 manifest entries (vs 3 + 3). Sprint-decomposed Epic AC provides stable-ID `INV-deps-version-pinning` verbatim (AC 4) — first Epic-1 story where AC pins the ID by name rather than Ralph inventing it. This is a cleaner constraint (less drafting-time ambiguity) but constrains the companion-doc-ID naming to match (chose `INV-renovate-rationale` for clarity/symmetry).
- **Carry-forward rules INTO iter-82 `/bmad-create-story (args: "review")` pre-dev SM:**
  - Verify seven preventative audit layers L1-L7 held at iter-81 drafting; apply residual-class PATCHes (≤2 expected per 4-story precedent Stories 1.11/1.12/1.13/1.14).
  - L5c mechanical-sequence-counter discipline per Story 1.14 iter-76 LESSON: verify NINTH ATDD-skip + NINTH WAIVED-trace + FIFTH ZERO-PATCH-CR cumulative counters via shell count against prior-story Change Log rows (NOT mental-inference).
  - L5b mechanical-count discipline per Story 1.14 iter-75 LESSON: verify `ls -d apps/*/ packages/*/ | wc -l = 16` + `grep -c "^\s*id: '" packages/keel-invariants/src/invariants.manifest.ts = 20` at SM-review time via real shell command (NOT mental substitute).
  - Layer 4 enhancement per Story 1.13 iter-68 LESSON: scan for design-convergence residuals in AC text (e.g., "3 packageRules" vs "4 packageRules" — should be unified on 4; "single file" vs "one JSON file" — unify wording).
- **Configuration-surface CR forecast (Story 1.15 iter-87):** 0 PATCH / 0 DECISION-NEEDED / 5-8 DEFER / ~10-13 DISMISS expected. Lower DEFER vs Story 1.14's 10 due to: (a) smaller 4-packageRules surface (4 groups vs Story 1.14's 17 packages); (b) no manifest-file hash-cascade concerns (Story 1.14's `.release-please-manifest.json` would have post-Release-PR-merge hash drift; Renovate's renovate.json does NOT have an atomic-rewrite equivalent — it's static policy); (c) no "release-please WORKFLOW = Story 13.5" consumer-deferral ambiguity (Renovate runtime consumer is a CLEAN ops carve-out — install the Renovate GitHub App).
- **Issue Tracking carry-forward.** Story 1.15 at **#39** — current iteration commit trailer uses `Refs #39`. Parent Epic 1 at **#9** OPEN. Story 1.14 at #38 still OPEN (expected CLOSED at eventual PR merge; PR stays Draft through Story 1.16 completion).
