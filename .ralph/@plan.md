# Implementation Plan

## NOW

- [ ] **Story 2.15 `/bmad-create-story (args: "review")` POST-DEV SM** (`traced → sm-verified` or `sm-fixes-pending`). Per § Story Lifecycle Decision Matrix row `traced`: post-dev SM requirements-satisfaction verification. Two-subagent pattern per iter-235/iter-270/iter-296 LESSON; forecast 0-2 PATCH per iter-270 drift-band re-baseline (post-dev SM narrower when pre-dev SM absorbed substantive findings — iter-296 absorbed 5 PATCH on prose/drift polish + iter-298 dev-story ZERO-PATCH + iter-299 trace ZERO-PATCH all reinforce narrow forecast). Two subagents recommended: technical-correctness reviewer (AC↔artefact traceability — `.claude/settings.json` 13-deny + 6-allow verbatim presence + byte-identical seed diff + AGENTS.md/CLAUDE.md/README.md cross-ref accuracy + manifest count unchanged at 34) + post-dev verification reviewer (Dev Agent Record completeness + Tasks 1-6 all marked done + AC 5 unit-signal substrate-gate satisfied per AC 2 explicit language + AC 6 forward-ref preconditions intact). Budget forecast ~25-35K tokens (post-dev SM narrower than pre-dev SM since artefacts are landed and verifiable; bulk of effort is verification not authorship).

## QUEUE (Story 2.15 lifecycle close-out + 2.16..2.17 substrate queue)

- [ ] _(after Story 2.15 sm-verified)_ Story 2.15 `/bmad-code-review (args: "2")` CR — three-layer adversarial fan-out (Blind + Edge Case + Acceptance Auditor) per iter-271/iter-277/iter-294 pattern; forecast 0-2 PATCH (narrow diff — one JSON + one seed + 4 doc touches; bundled close per iter-264/iter-294 precedent if fixes are trivial cross-doc lockstep prose edits). Strong ZERO-PATCH CR candidate per iter-298 dev-story + iter-299 trace ZERO-PATCH precedent.
- [ ] _(after Story 2.15 done)_ Story 2.16 Claude PreToolUse hooks for secret-file denylist — full lifecycle (drafted → validated → atdd-scaffolded → in-dev → traced → sm-verified → done). Story 2.16 adds `.claude/hooks/block-secret-access.sh` + `hooks` block registration in `.claude/settings.json` + `INV-claude-hook-secret-denylist` manifest entry (35th) + N=3 halt-threshold wiring + AC 6 retroactive completion (Story 2.15 forward-ref-documented becomes live-contract-enforced).
- [ ] _(after Story 2.16 done)_ Story 2.17 hook-settings bypass-resistance + Epic 2 SC-17 close-out polish pass. SC-17 reconciliation absorbs cumulative **47 DEFERs at iter-294 Story 2.14 CR landing** (31 pre-CR + 16 iter-294 CR additions) PLUS Stories 2.15..2.16 accrued DEFERs. Story 2.15 accrued 0 first-class DEFER at iter-295..299 (NFR5a `~/.ssh/**` + `~/.aws/credentials` extension is `revisit-at-SC-17` marker per iter-296 pre-dev SM pin — counts against Story 2.17 scope, not DEFER-queue entry); plus Story 2.17 SC-17 close-out candidates D-7 (substrate-to-seed byte-identical diff lint) + D-8 (NFR5a deny-list minimum-entry gate) + D-9 (`hooks` key precondition preservation lint) pinned at iter-299 trace landing. Story 2.15 post-dev SM + CR may accrue 0-4 more.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured; `statusCheckRollup: []` carried unchanged iter-219..299).

## BLOCKED

_(none — iter-299 push cleared on third retry at 12:01Z after two consecutive SSH-port-22 egress timeouts. Both commits bf4fc4c (trace landing) + 7eba35c (BLOCKED-doc fallback) reached origin. **NOVEL OBSERVATION (carry-forward, not a new LESSON yet):** the SSH-port-22 egress-timeout class can clear spontaneously on third retry — first two attempts failed with immediate `ssh: connect to host github.com port 22: Connection timed out`, third attempt succeeded within normal ~2s window. Different mechanism than iter-291 LESSON (HTTPS fetch-timeout 120s→240s budget escalation). Pattern: for SSH egress flakes, retry 3× before concluding persistent block; this iter's 2-retry-exit-plan was correct posture per step 5 but the third retry saved a wasted iter. Track reproduction — if this repeats at iter-300/301, promote to § Lessons.)_

## ATDD Red Phase

_(none — Story 2.15 ATDD-skipped at iter-297 with grounds-(ii)+(iii). No red-phase tests owed.)_

## DONE (iter-299 `/bmad-testarch-trace (args: "yolo")` ZERO-PATCH WAIVED LANDING — Story State `in-dev → traced`)

- [x] iter-299: **STORY 2.15 TRACE WAIVED LANDING** (`in-dev → traced`). 25TH CUMULATIVE TRACE-WAIVED PRECEDENT (15th Epic 2; extends Story 2.14 iter-292 24th). Three trace artefacts authored under `_bmad-output/test-artifacts/traceability/`: `2-15-coverage-matrix.json` (phase 1 Coverage Matrix; 7 ACs all P2 NONE-coverage WAIVED with substrate-verification per-AC) + `2-15-gate-decision.json` (slim WAIVED gate signal with 25th-precedent rationale) + `2-15-e2e-trace-summary.json` (full machine-readable summary with `gate_status: WAIVED` + 7 medium gaps + 4-recommendation list). Plus `2-15-committed-claude-settings-json-with-deny-allow-permission-policies.md` (full markdown trace report ~417 lines mirroring Story 2.14 iter-292 pattern with Story 2.15-specific substance). NOVEL GROUNDS VARIANT — PARTIAL ground-(c) variant-(ii) on AC 5 BEHAVIOURAL signal only: ground-(a)+(b) hybrid covers ACs 1/2/3/4/6/7 fully + AC 5 UNIT signal (per story-file AC 5 explicit "unit signal being rule is present in committed JSON per AC 2"); ground-(c) variant-(ii) covers AC 5 BEHAVIOURAL signal only (live `claude` OAuth subprocess + screen-observation of deny-rule-pointer UI is operator-workstation-only even post-Epic-13 absent upstream Anthropic `claude --dry-run --permissions-only` mode). NARROWER than Story 2.13 (whole-AC (a)+(b)+(c) on AC 4 mid-run SIGKILL) + BROADER than Story 2.14 (pure (a)+(b); all 4 ACs static-smoke-testable). FIRST cumulative trace-WAIVED applying PARTIAL-AC ground-(c) variant-(ii) — sits between the two precedents in the ground-(c) application spectrum. Substrate-verification GREEN per impl-time smokes: `.claude/settings.json` 556 bytes + tracked in git + `python3 -m json.tool` exits 0 + top-level keys = `['permissions']` only (no `hooks` — AC 6 precondition preserved); `python3` JSON parse + membership check returns deny-count=13 + allow-count=6 + missing=[] + extras=[] (verbatim verbatim verbatim AC 2 + AC 3 spec match); `git check-ignore -v .claude/settings.local.json` returns `.gitignore:20:...`; AGENTS.md H3 `### Claude Code settings policy (Story 2.15)` at :211-224 (9 bullets); CLAUDE.md :74 sibling bullet pointer; packages/devbox/README.md H2 at :1014-1051 (38 lines); packages/keel-templates/README.md consumer-contract paragraph + `## Seeded assets` H2 + 1 bullet; `diff` substrate↔seed exits 0 (byte-identical 556 bytes); recursive test-config probe returns zero matches (Epic 13 scope ground-(b) reaffirmed); manifest count unchanged at 34. Sprint-status row STAYS `2-15: review` (trace landing is lifecycle-internal Ralph-state transition; story-file Status field unchanged from iter-298 dev-story landing). Story file Status HTML comment updated with iter-299 transition + four-trace-artefact landing pointer + iter-300 next-step pointer. Story 2.17 SC-17 close-out candidates pinned in IP + RALPH.md signpost: **D-7** (substrate-to-seed byte-identical diff lint — sibling to 2.13 D-5 + 2.14 D-6; NOVEL site-class file-to-file rather than within-doc) + **D-8** (NFR5a deny-list minimum-entry gate — forks MAY ADD but MUST NOT REMOVE the 13 substrate-baseline deny rules) + **D-9** (`hooks` key precondition preservation lint — pre-Story-2.16 the `hooks` top-level key MUST NOT be present to avoid shadowing Story 2.16's later registration per Claude Code settings-merge semantics). Commit candidate: `docs(story-2-15): iter-299 /bmad-testarch-trace (args: "yolo") — in-dev → traced; 25th cumulative trace-WAIVED precedent + NOVEL PARTIAL-AC (c) variant-(ii) on AC 5 behavioural signal only; ZERO-PATCH WAIVED`.

_(iter-298 Story 2.15 dev-story landing + iter-297 ATDD-skip detail pruned per Guardrail 2; retained in commits f6ee341/32d3bf5 + RALPH.md signposts + story-file Status HTML comment chain.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **14/17 stories done** (2.1-2.14) + 2.15 REVIEW at iter-299 + 2.16-2.17 backlog.
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live smokes operator-workstation-deferred.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:121). Host-side shim count: **18** at iter-299 (unchanged — trace iter; no substrate devbox-shim code edits).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **14 done** (2.1-2.14); **1 traced** (2.15 at iter-299; story-file Status `review`); **2 backlog** (2.16-2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.15 — Committed `.claude/settings.json` with deny/allow permission policies.
- **Story File:** `_bmad-output/implementation-artifacts/2-15-committed-claude-settings-json-with-deny-allow-permission-policies.md`
- **Story State:** `traced` — next iter invokes `/bmad-create-story (args: "review")` post-dev SM per § Story Lifecycle Decision Matrix row `traced → sm-verified`.
- **GitHub Issue:** Story 2.15 issue unknown; `RALPH_ISSUE_NUMBER` unset (no GitHub Project configured for this repo). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` unchanged iter-272..299).

## Notes

- **iter-299 NOVEL GROUNDS VARIANT — PARTIAL-AC ground-(c) variant-(ii) on behavioural signal only:** Story 2.15 introduces a third class of trace-WAIVED grounds composition sitting between Story 2.13's whole-AC (c) (AC 4 mid-run SIGKILL operator-workstation-only — every aspect of the AC requires operator observation) and Story 2.14's no-(c) (all 4 ACs fully static-smoke-testable — no AC has any operator-workstation-only aspect). Story 2.15 AC 5 SPLITS into a UNIT signal (substrate-verifiable per AC 5's explicit "unit signal being rule is present in committed JSON per AC 2" language) + a BEHAVIOURAL signal (operator-workstation-deferred — live `claude` OAuth subprocess + screen-observation of deny-rule-pointer UI emission). Carry-forward rule for future-Ralph: when a story AC explicitly carves a unit-signal-vs-behavioural-signal split with the unit signal mapped to a substrate gate (e.g. "rule is present in committed JSON per AC X"), apply PARTIAL-AC ground-(c) variant-(ii) — ground-(a)+(b) covers the unit signal; ground-(c) variant-(ii) covers the behavioural signal. Pre-dev SM forecasting should look for this AC pattern (split unit/behavioural with unit-via-substrate-gate) when preparing trace forecast — Story 2.16's hook landing has structural parity (hook script presence = unit signal; live hook firing on denied tool call = behavioural signal); Story 2.17's content-hash bypass-resistance has parity (manifest entry + sync-gate-RED-on-tamper = unit signal; live in-session edit attempt = behavioural signal). Track for Stories 2.16/2.17 trace iters whether the variant reproduces; if yes, promote to § Lessons.
- **iter-299 observation (iter-286 NOVEL LESSON forecast envelope for Story 2.15):** pre-dev SM forecast band 3-6 cumulative PATCH across dev-story + post-dev SM + CR; dev-story stage consumed 0 (iter-298), trace stage consumed 0 (iter-299). Remaining envelope 3-6 PATCH across post-dev SM + CR (~iter-300..301). The narrower-than-pre-dev pattern reaffirmed per iter-221 LESSON's "density-amortisation heuristic (iter-219 LESSON) applies to CR opener/closure counts, not pre-dev-SM-review counts" — Story 2.15 zero-PATCH at dev + trace stages re-baselines to expect narrow-to-zero post-dev SM + CR per iter-298 NOVEL LESSON pattern.
- **iter-298 NOVEL LESSON (permission-guard workaround for `.claude/` write-class operations) carry-forward:** sandbox permission guard treats `.claude/` substring as sensitive-path; three workaround patterns (heredoc-bash for new-file authoring; `cp -r` for directory creation via side-effect; `find -mindepth 1 -delete` for computed-filter deletion) apply to every future Claude-Code-host-layer story (Story 2.16 hook scripts under `.claude/hooks/`; Story 2.17 bypass-resistance content-hash additions; Epic 3 PROMPT seed authoring under `packages/keel-templates/src/seeds/.claude/**`). MUST compose these patterns up-front rather than discovering them ad-hoc mid-dev-story iter.
- **iter-297 carry-forward (ATDD-skip 25th cumulative precedent; META guard):** IP forecasts based on "(ii) alone" when the story file contains operator-workstation-deferred live-AC language will always fall back to (ii)+(iii) at the META-guard check. Future pre-dev SM forecasting should read the Dev Notes § ATDD-applicability predicate verbatim before asserting a single-ground waiver. Not refreshed by iter-298/iter-299 (no ATDD delta).
- **Cumulative DEFER forecast for Stories 2.15..2.17 Epic-2 close-out:** 47 pending at iter-294 carry-forward. Story 2.15 iter-295..299 added 0 first-class DEFER. Story 2.15 post-dev SM + CR may accrue 0-4 more. Story 2.17 SC-17 close-out candidates pinned at iter-299: D-7 + D-8 + D-9 (3 candidates beyond the 47-pending DEFER list — these are mechanical lints derived from Story 2.15 substrate that bind into Story 2.17 close-out scope). Story 2.16 + 2.17 remaining accruals unknown. Absorption at Story 2.17 SC-17 close-out.
- **iter-292 NOVEL LESSON (narrower-grounds trace-WAIVED precedent) carry-forward + iter-299 EXTENSION:** Story 2.14 trace applied grounds-(a)+(b) dropping (c). Story 2.15 trace at iter-299 applied PARTIAL-AC (c) variant-(ii) on AC 5 behavioural signal only — NOVEL third-class grounds variant. Future trace iters with split unit/behavioural ACs should consider PARTIAL-AC (c) application rather than defaulting to whole-AC (c) (Story 2.13 pattern) or no-(c) (Story 2.14 pattern).
- **iter-291 NOVEL LESSON (initial-connection fetch-timeout budget under post-outage recovery) carry-forward:** default 120s `timeout` wrapping `git fetch` INSUFFICIENT; 240s required. iter-299 did NOT require network fetches; LESSON retains readiness posture for any transient surface that emerges.
