# Implementation Plan

## NOW

- [ ] **Re-run `/bmad-code-review (args: "2")`** — CR re-verification gate against HEAD after AI-1..AI-5 drain landed (iter-178..iter-182). Expected ZERO-PATCH close if Acceptance Auditor confirms all 5 ACs still SATISFIED byte-identical + prior-gate AI-N closures verified + Blind/Edge Case layers produce only defensive-nit defers/dismisses (no new HIGH findings). On ZERO-PATCH close: FR14n Story State `fixes-pending → done`; `Status` `fixes-pending → done`; sprint-status row `2-4`: `review → done`; Epic 2 counter 3/17 → 4/17 `done`. If new HIGHs surface in CR re-run, re-enter fix drain with AR-1..AR-N from that cycle (per Story 2.3 iter-166..iter-170 AR-drain precedent). ~medium.

## QUEUE (Story 2.4 CR drain → Epic 2 Story 2.5..2.17 → Epic 2 close-out)
- [ ] _(conditional, post-Story-2.4 done)_ `/bmad-create-story` for Story 2.5 (container hardening: non-root user `dev` + `cap_drop: [ALL]` preserving `cap_add: [NET_ADMIN, NET_RAW]` + `security_opt: [no-new-privileges:true]` + tmpfs + named volumes for OAuth) per § Story Lifecycle `done` row + § Cross-epic transition within-epic path. 13 backlog stories remaining in Epic 2 (2.5..2.17) after 2.4 done.
- [ ] _(conditional, post-Story-2.17 done)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge. Epic 2 completion.
- [ ] _(conditional, post-Epic-2 merged)_ § Cross-epic transition → Epic 3 Story 3.1 OR halt EPIC_DONE awaiting merge.

## BLOCKED

_(none — CR opener produced 5 PATCH + 4 DEFER + ~30 DISMISS envelope; Acceptance Auditor verified all 5 ACs SATISFIED; no ambiguous `decision-needed` findings surfaced. AI-1..AI-5 drain closed at iter-182.)_

## ATDD Red Phase

_(none — Story 2.4 ATDD-skipped at `validated → atdd-scaffolded` per 14th-cumulative-precedent iter-173; CR patches AI-1..AI-5 are surgical shell-script + markdown edits with no tests-to-green tracking. Re-verified by iter-177 CR opener: Blind/Edge Case/Auditor layers substitute for absent test-runner per hybrid ground-(c)+(ii)+(iii) declaration in Story 2.4 § Testing Standards.)_

## DONE (Story 2.4 lifecycle — keep current story chain, prune after next-story starts)

- [x] iter-182: **AI-5 README per-subcommand exit-code table completeness fix landed — CR drain 5/5 (final PATCH).** Docs-only surgical `Edit` at `packages/devbox/README.md:263-268` — widened Exit codes column (51→77 chars between pipes; separator dashes 49→75) + populated full transitive exit-code surface. `add` row: `0 ok; 2 syntax; 4 lock-timeout; 5–7 reload` → `0 ok; 2 syntax; 3 unreadable; 4 lock-timeout; 5–7 reload`. `remove` row: `0 ok; 2 substrate-domain / syntax; 4 lock-timeout` → `0 ok; 2 substrate-domain / syntax; 3 unreadable; 4 lock-timeout; 5–7 reload`. Rationale: both subcommands transitively call `cmd_sync` (cmd_add → `whitelist.sh:327`; cmd_remove → `:407`) whose `check_readable` at `:128-151` exits 3 on unreadable sources + reload-egress.sh propagates 5/6/7 per SC-11; cmd_remove also exits 3 locally at `:399` (AI-2 iter-179). Pre-fix the table asserted a narrower surface than the implementation exposes; post-fix it aligns with SC-11 across all four subcommands. **Verification:** `Read packages/devbox/README.md:261-272` renders aligned 4-row table; `grep -c '3 unreadable'` → 3 hits (sync/add/remove); `grep -c '5–7 reload'` → 3 hits (sync/add/remove); `grep -c 'lock-timeout'` → 2 hits (add/remove — cmd_sync has no mutation lock). **Scope invariance:** confined to README subcommand table; AGENTS.md deliberately retains substrate-level narrative without the operator-facing exit-code table (SC-19 substrate-vs-docs boundary). No `whitelist.sh` / `invariants.manifest.ts` / `reload-egress.sh` / sprint-status changes. FR14n Story State stays `fixes-pending` until iter-183 CR re-run; `Status` unchanged; sprint-status row `2-4` stays `review`; PR #230 stays Draft. Budget consumed iter-182: ~6K tokens (orient ~3K + target-line read + exit-code verify in whitelist.sh + README reflow + verification greps + Change Log v2.1 + IP updates + RALPH.md signpost + commit + push ~3K) — ~2K tighter than iter-178..iter-181's ~8K-per-iter code-patch envelope. Files modified iter-182: `packages/devbox/README.md` (6-line table reflow), story doc (Change Log v2.1 append), `.ralph/@plan.md`, `RALPH.md`. **Pattern closure:** 5-iter AI-1..AI-5 surgical-Edit drain landed per iter-177 CR opener forecast (iter-178 AI-1 + iter-179 AI-2 + iter-180 AI-3 + iter-181 AI-4 + iter-182 AI-5); cumulative drain envelope ~38K tokens across 5 iter (4 code × ~8K + 1 doc × ~6K), inside budget headroom. Drain iter pattern affirmed for user-facing-CLI class at ~6-8K tokens per iter. Next iter (183): `/bmad-code-review (args: "2")` CR re-run.
- [x] iter-181: **AI-4 PID-predictable tempfile fix landed — CR drain 4/5.** Surgical 2-hunk `Edit` (replace_all over two byte-identical occurrences) at `packages/devbox/scripts/whitelist.sh:301,385` — `local tempfile="${WHITELIST_LOCAL}.tmp.$$"` → `local tempfile` + `tempfile="$(mktemp "${WHITELIST_LOCAL}.XXXXXX.tmp")"`. Split declaration matches existing script convention at `:197-198` (cmd_sync) + `:309-310` (cmd_add `local existing; existing="$(cat …)"`) and avoids shellcheck SC2155 exit-code-loss under `local x="$(…)"` with `set -e` in force (line 35). Adjacency to `WHITELIST_LOCAL` preserved → SC-9 same-fs rename semantics at `mv` sites `:320,402` intact. Trap safety invariant: `trap "rm -f '${tempfile}'" EXIT` at `:304,389` registered AFTER assignment at `:302,387` → captures expanded mktemp path. **mktemp template validity:** GNU coreutils 9.4 (cc-devbox) accepts `whitelist.local.txt.XXXXXX.tmp` (implicit `--suffix=.tmp`) — smoke-tested at `/tmp/testmk.XXXXXX.tmp` → `/tmp/testmk.nwHZfp.tmp` (6-char `[A-Za-z0-9]` random + `.tmp` preserved, 56^6 ≈ 30B). Defensive for Story 2.5 non-root `dev` hardening but applies even in pre-2.5 root posture. Files modified iter-181: `whitelist.sh` (2-hunk replace_all), story doc (Change Log v2.0), `.ralph/@plan.md`, `RALPH.md`. FR14n Story State stays `fixes-pending` (AI-5 remaining); Status unchanged; sprint-status row `2-4` stays `review`; PR #230 stays Draft. Budget: ~8K tokens.
- [x] iter-180: **AI-3 silent-`chmod 0644` fix landed — CR drain 3/5.** Surgical 2-hunk `Edit` at `packages/devbox/scripts/whitelist.sh:242,258` — removed ` 2>/dev/null || true` suffix from both `chmod 0644 "${COMPOSED_WHITELIST}"` (post-`mv` atomic-swap) and `chmod 0644 "${PREVIOUS_COMPOSED}"` (post-`cp -f` snapshot). Pre-fix: cmd_sync runs as root pre-Story-2.5 so chmod MUST succeed; suppressing stderr + rc hid genuine faults. Post-fix: `set -e` propagates rc with chmod's stderr visible; dispatcher terminates; reload-egress.sh NOT invoked for broken snapshot. Aligns with Story 2.3 iter-170 AR-9 "fail loud on I/O boundaries" precedent + in-script chmod 0644 posture at cmd_add:319 + cmd_remove:400.
- [x] iter-179: **AI-2 `grep -Fxv` rc-capture fix landed — CR drain 2/5.** Surgical 1-hunk `Edit` at `packages/devbox/scripts/whitelist.sh:389-398` — replaced `grep -Fxv -- "${domain}" … || true` with rc-capture idiom: `local rc=0; grep ... || rc=$?; if (( rc > 1 )); then log ERROR; exec 201>&-; exit 3; fi`. Preserves rc=0 (match) + rc=1 (every line matched → empty tempfile — legitimate "remove last domain" case) while surfacing rc≥2 (real I/O error) as fail-loud `exit 3`. Aligns with SC-11 (`3 = whitelist source file unreadable`).
- [x] iter-178: **AI-1 cross-filesystem `mv` fix landed — CR drain 1/5.** Surgical 1-hunk `Edit` at `packages/devbox/scripts/whitelist.sh:198` — `mktemp /tmp/keel-whitelist-sync.XXXXXX` → `mktemp /run/keel-whitelist-sync.XXXXXX`. SC-9 "same-fs rename" contract restored: `${COMPOSED_WHITELIST}` (`/run/keel-whitelist.composed.txt`) is on tmpfs; `mv "${tempfile}" "${COMPOSED_WHITELIST}"` at line 241 now stays within tmpfs → kernel `rename(2)` atomic.
- [x] iter-177: `/bmad-code-review (args: "2")` CR opener — FR14n Story State `sm-verified → fixes-pending`; 5 PATCH + 4 DEFER + ~30 DISMISS against cumulative Story 2.4 substrate diff.
- [x] iter-176: `/bmad-create-story (args: "review")` Story 2.4 post-dev SM verification — FR14n Story State `traced → sm-verified`; ZERO-PATCH + ZERO-DEFER + 1-DOCUMENTATION-DISMISS; 4th Epic-2 SM precedent; first user-facing-CLI class post-dev SM.
- [x] iter-175: `/bmad-testarch-trace (args: "yolo")` Story 2.4 — FR14n Story State `in-dev → traced`; Gate WAIVED (14th cumulative trace-WAIVED precedent; 15th cumulative ATDD-skip-trace-WAIVED co-application).
- [x] iter-174: `/bmad-dev-story` Story 2.4 landed single-iteration; FR14n Story State `validated → in-dev`.
- [x] iter-173: `/bmad-create-story (args: "review")` pre-dev SM validation of Story 2.4; FR14n Story State `drafted → validated`.
- [x] iter-172: `/bmad-create-story` drafted Story 2.4; Status `backlog → ready-for-dev`.

## Context

- **Phase:** 4-implementation — Epic 2 at 3/17 stories `done` (2.1 + 2.2 + 2.3) + 1/17 at `review` in sprint-status enum / `fixes-pending` in FR14n Story State (2.4 iter-174 dev-story; traced iter-175; sm-verified iter-176; CR opener iter-177 → 5 PATCH queued; AI-1 iter-178; AI-2 iter-179; AI-3 iter-180; AI-4 iter-181; AI-5 iter-182 — drain complete; iter-183 CR re-run next) + 13/17 `backlog` (2.5..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.4 live smokes (Task 10.1–10.8 + 9.2) operator-workstation-deferred per Subtask 10.10; iter-177 CR opener re-verified deferral acceptance via Acceptance Auditor ALL-5-ACs-SATISFIED verdict.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). AI-1..AI-5 patches required NO Dockerfile edits (pure shell-script + README edits).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; 2.1 + 2.2 + 2.3 done; 2.4 fixes-pending → awaiting iter-183 CR re-run for `fixes-pending → done`; 2.5..2.17 in `backlog`.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2; PR #230 transitions to Open at Story 2.17 completion).
- **Story:** Story 2.4 — Whitelist source-of-truth + `pnpm devbox:whitelist` atomic-reload CLI.
- **Story File:** `_bmad-output/implementation-artifacts/2-4-whitelist-source-of-truth-pnpm-devbox-whitelist-atomic-reload-cli.md`.
- **Story State:** `fixes-pending` — AI-1..AI-5 drain complete iter-178..iter-182; iter-183 CR re-run → ZERO-PATCH close → `fixes-pending → done`.
- **GitHub Issue:** Story 2.4 → _(not yet created; RALPH_ISSUE_NUMBER unset at iter-177)_; Epic 2 → #10.
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **iter-177 CR-opener pattern affirmation (defensive-boundary axis).** Epic-2 CR openers cluster at the defensive-boundary of the story's class: Story 2.1 (infrastructure-smoke) → 9 AI on build/bake/compose seams; Story 2.2 (hybrid configuration) → 2 PATCH on parameterisation asymmetry; Story 2.3 (infrastructure-security) → 9 PATCH on dnsmasq/nftables/flock/JSONL kernel-daemon boundaries; Story 2.4 (user-facing-CLI) → 5 PATCH on filesystem/redirect/rename/perm/tempfile CLI-infrastructure boundaries. Future-Ralph rule: when forecasting a CR opener envelope, sum the fix-chain equation `(carve-out × 3) + (live-smoke-defer × 3) + (impl-surface-LOC / 100)` AND add a class-boundary-density coefficient — user-facing-CLI class gets +1 defensive-boundary finding per 100 LOC on top of the base estimate (Story 2.4 actual: 0 carve-out + 3 backend-B defer + 5.44 LOC-div-100 + 5 class-boundary → fits 5 PATCH opener exactly).
- **iter-182 drain-envelope confirmation (doc vs code patch axis).** AI-1..AI-4 (code patches) held at ~8K tokens per iter (1- or 2-hunk Edits + `bash -n` + grep-verify + Change Log entry + IP + RALPH.md). AI-5 (doc patch) came in at ~6K tokens per iter (6-line table reflow + two `grep -c` verifications + Change Log + IP + RALPH.md). Pattern rule: doc-only AI-N drain iters run ~2K tighter than code AI-N iters; budget 5-7K for doc vs 8-12K for code vs 15-25K for algorithmic rewrites. Applies to future CR drain forecasting across all stories; doc-only patches are strictly cheaper even when character-count is wider (widening a 5-row markdown table is still cheaper than a 1-line shell-script edit because there's no runtime-verification ladder).
- **NFR2 authority unchanged.** Cold ≤ 300s / warm ≤ 30s remains authoritative on M4-Pro native; DinD runs are indicative only. Story 2.4 substrate changes (whitelist.sh + start-egress.sh + default-txt header + README table) do NOT affect cold-start footprint because they consume existing baked tooling; warm-start unaffected because the scripts are copied from workspace on every startup (no new image layer).
- **Substrate sanity check (post-iter-182):** AI-5 edit is pure doc + IP + RALPH.md update. Files modified iter-182: `packages/devbox/README.md` (6-line table reflow at `:263-268`), `_bmad-output/implementation-artifacts/2-4-whitelist-source-of-truth-…-cli.md` (Change Log v2.1 append), `.ralph/@plan.md`, `RALPH.md` (iter-182 signpost append). No shell script edits; no invariant manifest touch; no sprint-status flip.
- **Cumulative Epic-2 fix-chain tracking (through iter-182):** Story 2.1 = 16-iter chain (9+5+ZERO); Story 2.2 = 4-iter chain (2 PATCH + re-run); Story 2.3 = 10-iter chain (9→0 single-cycle drain); Story 2.4 = 6-iter chain projected (iter-175 trace + iter-176 SM + iter-177 CR opener + iter-178..182 5-PATCH drain COMPLETE + iter-183 CR re-run pending). Trend: Story 2.4 at 6-iter is tightest Epic-2 drain chain to date because per-patch LOC surface is ~10× smaller (user-facing-CLI ~small-Edit class vs infrastructure-security ~medium-Edit class). Cumulative Epic-2 SM metrics at iter-182: 4/4 ZERO-PATCH SM opener closes (2.1 iter-127 + 2.2 iter-150 + 2.3 iter-160 + 2.4 iter-176); CR openers = 9 / 2 / 9 / 5 PATCH.
