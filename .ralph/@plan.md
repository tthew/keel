# Implementation Plan

## NOW

- [ ] `/bmad-code-review (args: "2")` — Story 2.9 CR adversarial triage (pre-selects "Create action items") per § Story Lifecycle Decision Matrix row `sm-verified → done`. Fresh context window. Forecast: **ZERO-PATCH one-pass closure** — FOURTH cumulative precedent candidate (chain: 1.13 iter-85 → 2.7 iter-226 → 2.8 iter-233 → 2.9 projected). Stack contributing to forecast: 17 SCs + 2-PATCH pre-dev SM + zero-drift iter-237 impl + trace-WAIVED + ZERO-PATCH post-dev SM (iter-243 surfaced substrate-citation drift but deferred reconciliation to Epic 2 close-out per SC-17 polish convention; no unmet-AC findings). Budget forecast: ~90K (Blind Hunter + Edge Case Hunter + Acceptance Auditor fan-out + action-items synthesis + deferred-work.md append + IP update + commit).

## QUEUE (Story 2.9 lifecycle → Stories 2.10..2.17 → Epic 2 close-out)

- [ ] _(post-Story-2.9-done)_ Iterate Stories 2.10..2.17 through full lifecycle — 8 remaining substrate stories spanning prereq-check + shared mode + port-publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-23 per iter-219..242 repeated pre-push confirmation — `gh pr view 230` shows `statusCheckRollup: []`).

## BLOCKED

_(none)_

## ATDD Red Phase

_(none — Story 2.9 FR14n ATDD-skip applied at iter-236; NINETEENTH cumulative precedent under inherited ground-(c) "external-service-owns-behavior-under-test" variant. No red-phase scaffolds produced.)_

## DONE (iter-243 Story 2.9 POST-DEV SM VERIFIED — Story State `traced → sm-verified`; Change Log v1.5)

- [x] iter-243: **STORY 2.9 POST-DEV SM REQUIREMENTS-SATISFACTION VERIFIED via `/bmad-create-story (args: "review")` fresh-context — Story State `traced → sm-verified`; sprint-status UNCHANGED (post-dev SM gate is Ralph-internal state, not sprint-row state — matches trace gate + pre-dev SM gate precedent).** **ZERO PATCHes for unmet-AC findings** — all 4 ACs verified satisfied at iter-237 impl via iter-235 LESSON two-subagent verification pattern (source-file claims + planning-artefact claims in parallel via `Explore` subagents). **Subagent A (impl verification) — 11 VERIFIED / 2 DRIFTED** (both from iter-239 retrofit already acknowledged in v1.4; neither regresses AC behavior): A.5 SCRIPT_DIR present (lib-sourcing retrofit); A.8 `${CONTAINER_WORKDIR}` substitution (functionally identical; defaults to /workspace via entrypoint env). **Subagent B (citation drift) — 14 VERIFIED / 13 DRIFTED / 0 MISSING**: all planning artefacts + invariants VERIFIED (architecture.md:74/75/1239/991-1004, epics.md:1449-1475/1157, prd.md:929/931/FR14k:801, INVARIANTS.md:94/100/106, docs/invariants/ralph-execute.md:77-89, AGENTS.md H3 anchors 104/114/123/135, whitelist/github.txt 7 entries, claude-host.sh template); 13 substrate-file line-number drifts from iter-238..241 evolution (Dockerfile:246-252→252 + 255-270→275; compose:86-92→116-118 + 170-171→256-257 + 156→242 + 158-159→244-245 + 109-113→142-146; shell.sh:42→56; README.md:410→440 + 505→536 + 550→581 + 601→691; attach.sh:32-36→32-35 trivial) — content still exists at all cited locations; line-number reconciliation deferred to Story 2.17 Epic 2 close-out per SC-17 polish convention. **iter-235 LESSON applied successfully — FIRST explicit surface of substrate-citation drift at post-dev SM gate in project history**; the LESSON's prospective framing validated empirically (Story 2.8's `architecture.md:489` cautionary tale resolved prospectively at iter-235 PATCH 1 + retrospectively here at iter-243 v1.5). **AC satisfaction**: AC 1 wrapper envelope verified by Task 5 smokes 1-4 + iter-239 functional equivalence; AC 2/3 Story 2.5 substrate composition; AC 4 clauses 1+2 pointer + halt-able contract pinned in AGENTS.md + README.md. **Story file Change Log v1.5 appended** above v1.4 (reverse-chronological per Story 2.7/2.8 precedent). **No sprint-status.yaml flip** (post-dev SM gate is Ralph-internal state). **No RALPH.md lesson** — iter-235 LESSON already captured the pattern prospectively + validated here; no novel pattern surfaced. Budget iter-243: ~85K (orient + story-file read + parallel-subagent dispatch [A + B] + v1.5 append + IP update + commit).

_(iter-234 draft + iter-235 pre-dev SM + iter-236 ATDD-skip + iter-237 impl + iter-242 trace pruned per Guardrail 2 — see Change Log v1.0/1.1/1.2/1.3/1.4 for details.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **8/17 stories done** (2.1-2.8) + 1 in review (2.9 — post-dev SM verified iter-243, Story State `sm-verified`) + 8/17 backlog (2.10..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live OAuth AC 1-4 smokes operator-workstation-deferred per ground-(c) carry-forward. Post-iter-237 infrastructure retrofits (iter-238..241) landed path-mirroring + ralph-TUI + whitelist + build fixes on same branch; all orthogonal to Story 2.9 AC verification envelope per iter-243 post-dev SM subagent-A verdict.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:119). Host-side shim count: **17 verbs** at Story 2.9 landing (iter-237 impl; unchanged through post-dev SM gate).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **8 done** (2.1-2.8); 1 in review (2.9); 8 backlog (2.10..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.9 — `gh` CLI OAuth via `pnpm gh:auth`.
- **Story File:** `_bmad-output/implementation-artifacts/2-9-gh-cli-oauth-via-pnpm-gh-auth.md` (v1.5 post-iter-243 post-dev SM; Status `review`; all 6 Tasks + 36 subtasks `[x]`; Dev Agent Record filled; § Change Log v1.5 appended above v1.4).
- **Story State:** `sm-verified` — Status field at `review` (sprint-status row `2-9-...: review`); next iter action per § Story Lifecycle Decision Matrix is `/bmad-code-review (args: "2")` in fresh context window for the `sm-verified → done` gate (forecast ZERO-PATCH one-pass — FOURTH cumulative one-pass ZERO-PATCH CR precedent candidate; chain Story 1.13 + 2.7 + 2.8 + 2.9 projected).
- **GitHub Issue:** Story 2.9 unwired at iter-234 (`RALPH_ISSUE_NUMBER` unset per per-iteration Issue Tracking block; skip `Refs #<story-issue>` in commits). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2; `statusCheckRollup: []` at iter-242 pre-push confirmation; CLEAN merge-state).

## Notes

- **iter-243 post-dev SM pattern confirmation — citation-drift deferral at Epic-scope.** First explicit substrate-file line-drift surface at post-dev SM gate; 13 drifts across Dockerfile + compose + README + shell.sh from branch evolution iter-238..241. Resolution: defer reconciliation to Story 2.17 Epic 2 close-out rather than per-gate rewrite (per SC-17 polish convention). Rationale: (i) content still resolves at all cited locations, (ii) stable anchors (section headings, AC numbers, prose bullets) remain accurate, (iii) per-gate rewrite would amortize unfavorably across Stories 2.10..2.17 which will trigger the same drift cascade. Stories 2.10+ SHOULD re-apply the iter-235 LESSON two-subagent pattern at post-dev SM; when drift is ≤200 lines and content still exists, Change Log acknowledgement is sufficient (Story 2.9 v1.5 precedent). Reserved for explicit amendment: citations where content has actually been removed (MISSING verdict), not merely shifted.
- **iter-242 carry-forward — Story 2.8 template is reliable baseline for subsequent upstream-CLI-ships-runtime class stories.** The 4-artefact layout (main .md + coverage-matrix.json + gate-decision.json + e2e-trace-summary.json) + frontmatter schema + per-AC substrate-evidence + ground-(c) rationale block = stable template. Forecast: if Stories 2.10..2.17 introduce any further upstream-CLI-ships-runtime variants (unlikely from sprint-status scope — remaining stories are prereq-check + shared-mode + port-pub + healthcheck + legacy-branch + settings + hooks + bypass), trace gates inherit Story 2.8/2.9 template directly. Otherwise Epic 2 trace-WAIVED counter plateaus at NINETEEN cumulative until Epic 3+ introduces new substrate class.
- **iter-235 carry-forward — pre-dev SM + post-dev SM reviews MUST grep-verify every `<planning-file>:<line>` citation.** Two-subagent verification pattern (source-file claims + planning-artefact claims in parallel, each returning VERIFIED / DRIFTED / MISSING) is the reference implementation — dispatch via `Explore` subagent with terse-report instruction. Applied at iter-243 post-dev SM with full effect: 14 VERIFIED + 13 DRIFTED surfaced + resolution deferred per SC-17. Story 2.8's 6-gate ZERO-PATCH closure without surfacing the `architecture.md:489` drift (the cautionary tale) is now closed-loop — iter-235 prospective fix + iter-243 retrospective surface confirm the LESSON's load-bearing role for every future SM review.
- **iter-234 carry-forward — killed-before-commit skill recovery: inspect disk state before re-invoking.** Four durable signals indicate a prior-attempt-killed state: (i) untracked output files matching expected skill-output path, (ii) sprint-status.yaml modified with a `last_updated: <current-iter>` header advancing past last commit's iter, (iii) IP.md modified with partial updates, (iv) `git log` tip NOT matching sprint-status iter-marker. Any three present → recovery-commit mode, DO NOT re-run skill. Generalises to all fresh-context skill invocations whose primary output is committed artefacts (create-story, testarch-trace, testarch-atdd, code-review, dev-story).
- **iter-233 carry-forward — one-pass ZERO-PATCH CR compounds with dense pre-dev SM + zero-drift impl + zero-PATCH post-dev SM (THIRD cumulative precedent):** Story 1.13 iter-85 first; Story 2.7 iter-226 second; Story 2.8 iter-233 third. Forecast pattern: pre-dev SM PATCH density ≥ 2 AND verbatim spec-template impl AND ATDD-skip-trace-WAIVED under ground-(c) → reliable one-pass ZERO-PATCH CR closure. Story 2.9 candidate for FOURTH one-pass ZERO-PATCH precedent confirmed through post-dev SM gate iter-243 (2-PATCH pre-dev SM + zero-drift iter-237 impl + trace-WAIVED iter-242 + ZERO-PATCH post-dev SM iter-243). CR gate next.
- **iter-221 carry-forward — pre-dev SM `~5-PATCH` ceiling is independent of draft-SC density.** Story 2.9 achieved 2 PATCHes at iter-235 (low end of 3-7 forecast band). Types that reliably appear: (a) claims about EXISTING file state (H-levels, line numbers) drifting between draft + review time, (b) template-vs-schema fidelity gaps, (c) implicit read-only assumptions a dev-agent wouldn't infer, (d) gotchas buried in Dev Notes that belong in user-facing AC/Task content.
- **TTY-detect at docker-exec shim (iter-214 LESSON) — Story 2.9 CONFIRMED NON-APPLICATION across all lifecycle gates:** SC-6 explicitly pins hardcoded `-it` ("OAuth flow IS the AC"). Same deferral posture as Story 2.7's `docker attach` (SC-8) + Story 2.8 SC-6. Pattern holds for Story 2.9 gh OAuth (interactive-by-contract) verified at pre-dev SM + trace + post-dev SM gates.
- **`<verb>-host.sh` host-side shim naming convention (iter-199 EXTENDED through iter-243):** Story 2.9 `gh-auth-host.sh` ships substrate-quality under `packages/devbox/scripts/`, holding host-side shim count at 17 through post-dev SM gate. Remaining Epic 2 candidate: Story 2.12 (`ssh-host.sh` for loopback-bound port publication if the story adopts the same pattern). Forecast: Story 2.17 landing → 17+ total shims → post-Epic-2 `_lib.sh` refactor threshold triggered (iter-239 already extracted `lib/main-repo-resolver.sh` + `lib/check-mount-source.sh` shared helpers — first evidence of the refactor beginning organically under runtime-bug pressure).
