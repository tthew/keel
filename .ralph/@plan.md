# Implementation Plan

## NOW

- [ ] **Fix SM-1 [MEDIUM]** — add `### Font` section to `docs/invariants/tokens.md` between `### Type scale` and `### Spacing / radius / breakpoints`; one consolidated bullet `- **\`TOKEN-font-{name}\`** — *typographic font stack roles (sans / mono)*. Cross-runtime: Tailwind preset `font-sans` / `font-mono` utility classes; Textual TUI `$font-sans` / `$font-mono` style vars. Individual slugs: \`TOKEN-font-sans\`, \`TOKEN-font-mono\`. Value: see ux-design-specification.md § Visual Design Foundation; Story 1.11 populates.`; recompute `sha256sum docs/invariants/tokens.md`; update `INV-tokens-semantic-rationale.contentHash` at `packages/keel-invariants/src/invariants.manifest.ts:141`; re-run `pnpm -w typecheck` + `pnpm keel-invariants:check` clean-path → exit 0; check `[ ]` → `[x]` in story-file § Review Findings. ~small

## QUEUE (Story 1.10 — design-token schema, semantic + rationale contract)

- [ ] Run `/bmad-create-story (args: "review")` — post-dev SM re-verify. State `sm-fixes-pending → sm-verified` (if all § Review Findings checkboxes `[x]`). ~medium
- [ ] Run `/bmad-code-review (args: "2")` — adversarial CR with action items. State `sm-verified → done` (or `fixes-pending`). ~medium

## BLOCKED

_(none)_

## DONE (Story 1.10 — iter-47 `/bmad-create-story review` post-dev SM review)

- [x] iter-47: **`/bmad-create-story (args: "review")` post-dev SM review executed; FR14n state `traced → sm-fixes-pending` per matrix row 7.** Per-AC attestation produced in story-file § Change Log v1.3 row: **AC 1 satisfied** (JSON Schema draft-2020-12, `additionalProperties: false` at every level, 7 per-`$type` leaf defs, 8 required root groups, optional `$modes` overlay with sparse defs); **AC 2 satisfied** (3 AC-named exemplars present — `TOKEN-surface-raised` + `TOKEN-status-success-fg`/`-bg` + `TOKEN-text-primary`; motion/density two-segment IDs per iter-43 scope carve-out; Neutral + Accent groups inline-resolved at iter-45); **AC 3 NOT fully satisfied — SM-1 MEDIUM** (Font group cross-link parity gap); **AC 4 satisfied** (2 manifest entries at `invariants.manifest.ts:128-143` + 2 column-0 anchors at `INVARIANTS.md:50-51` + 3 sync-gate smoke branches proven at iter-45). Story-file § Review Findings block added with SM-1 enumeration + fix-plan; Change Log v1.3 row appended. Pure prose — zero source-code changes at iter-47; quality gates not re-run (no TS/JS touched). Root cause of SM-1: Task 2's subtask enumeration listed 11 required `###` sections but omitted Font; iter-45 dev-agent caught the structurally-parallel Neutral omission inline (same gap class) but did not continue the audit and missed Font (mirror-gap). Pre-dev SM review iter-43 should have caught both Task-2-vs-schema gaps (Neutral + Font) in one sweep; iter-43 scoped its drift-audit narrower. PR #226 CI: `no checks reported`; push safe. Tracked at GitHub issue #34 — `Refs #34` in commit trailer.

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1–1.9 done; 1.10 `sm-fixes-pending` at iter-47 via `/bmad-create-story review`; 1.11–1.16 backlog → 6 more stories × full FR14n lifecycle remaining after 1.10 closure before EPIC_DONE)
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (continues through Epic 1; PR #226 stays Draft until EPIC_DONE per PROMPT_build.md step 5c)
- **Story:** 1.10 — design-token schema, semantic + rationale contract
- **Story File:** `_bmad-output/implementation-artifacts/1-10-design-token-schema-semantic-rationale-contract.md`
- **Story State:** `sm-fixes-pending` — FR14n matrix row 8 next action: top QUEUE fix task (SM-1 font gap). Stays `sm-fixes-pending` until all § Review Findings checkboxes `[x]`, then re-run `/bmad-create-story (args: "review")` → `sm-verified` per matrix row 7.
- **PR:** #226 (Draft, MERGEABLE, no-CI-checks-reported — stays Draft through Epic 1; Draft→Open at EPIC_DONE)

## Notes

- **Story 1.10 positioning.** First of 4 design-token-pipeline stories in Epic 1 (1.10 schema+rationale contract → 1.11 source population Direction A → 1.12 emitter pipeline → 1.13 quality gates). Story 1.10 is analogous to Story 1.8's role (`invariants.manifest.ts` contract) — contract-only, zero runtime behaviour, two artefact files + 2 manifest entries + 1 new INVARIANTS.md section. Per the contract-only / data-only pattern observed in Stories 1.3–1.6 + 1.8, expected to trend single-pass CR rather than the multi-pass behaviour of Story 1.9 (runtime sync-gate) + Story 1.7 (docs prose). **Post-dev SM hypothesis partially falsified at iter-47** — Story 1.10 needed 1 SM fix round (Story 1.8 needed 0 at iter-5). Apply the four-layer carry-forward enumeration (sibling-artefact / SYMMETRIC-AC-PARALLEL / SIBLING-FIELD-PARALLEL / broader-corpus-vocabulary-propagation) from iter-22/24/30/36 RALPH Signposts + iter-36 two-layer-convergence-gates-PATCH-promotion rule + iter-47 Task-enumeration-vs-schema-group-enumeration cross-check rule to bound Story 1.10's CR loop tight.
- **Trace posture.** Gate WAIVED (fourth cumulative — 1.7 iter-5 + 1.8 iter-5 + 1.9 iter-8 + 1.10 iter-46). Rationale enumeration pinned to seven points: (1) stakeholder-approved waiver per § Testing Standards; (2) four ACs scoped-out to downstream stories (AC 1 → Story 1.13; AC 2 → Story 1.16; AC 3 → Story 1.13 + Story 1.16; AC 4 → Task 4 smokes + Story 1.9 runtime); (3) substrate verification strong (typecheck/lint/build/format:check/prek × 3 + 3 sync-gate smokes); (4) no test runner until Story 1.16; (5) FR14n ATDD-skip precedent is load-bearing (fourth cumulative); (6) Story 1.9 sync-gate is the AC-4 runtime integration test; (7) contract-only single-pass CR hypothesis (iter-7 Decisions).
- **SM-1 fix scope (iter-48).** Single prose edit to `docs/invariants/tokens.md` — add `### Font` section between `### Type scale` and `### Spacing / radius / breakpoints`. One consolidated bullet `TOKEN-font-{name}` listing `TOKEN-font-sans` + `TOKEN-font-mono` (primitive-scale group-level pattern permitted by Task 2 — mirrors Neutral). Post-edit cascade: recompute `sha256sum docs/invariants/tokens.md`; update manifest entry's `contentHash` at `invariants.manifest.ts:141`; re-run `pnpm -w typecheck` (Zod import-time validation) + `pnpm keel-invariants:check` clean-path → exit 0; flip story-file § Review Findings SM-1 checkbox `[ ]` → `[x]`. No drift-smokes needed (only clean-path — sync-gate smokes already proven comprehensively at iter-45 for both branches). ~small budget. Contract-only single-pass CR hypothesis still plausible IF CR re-run at iter-50 zero-outs.
- **Schema vs value separation.** Story 1.10 ships DTCG schema STRUCTURE + rationale PROSE only; Story 1.11 populates Direction A concrete values. This separation — contract-then-data — matches Story 1.8's posture (contract-then-enforcement in 1.9) and lets 1.10's ACs stay tight. Story 1.13's value-quality gates (schema-validation + WCAG AA contrast + source-output sync) run AFTER 1.11 source lands; Story 1.10 only needs to satisfy the structure-validates-DTCG claim of AC 1.
- **Two-entry manifest registration.** AC 4's epic phrasing at `epics.md:951` (`an entry INV-tokens-schema-contract registers tokens.schema.json + tokens.md`) conflicts with Story 1.8's single-`sourcePath`-per-`Invariant` shape. Resolution: two sibling manifest entries (`INV-tokens-schema-contract` → schema; `INV-tokens-semantic-rationale` → rationale doc), both anchored under a new `### Design-token schema + semantic rationale (Story 1.10)` section in `INVARIANTS.md`. Pattern matches `INV-eslint-shared` + `INV-eslint-import-boundary` (two IDs, shared `sourcePath`); here it's two IDs + two distinct `sourcePath` values. Documented as Story 1.10 § AC 4 scope carve-out in the story file.
- **AC 2 motion/density stable-ID carve-out (iter-43 pre-dev-review addition).** The epic's AC 2 shorthand `motion.scale.*` / `density.scale.*` is NOT a nested-group reference. Stable IDs resolve as: `TOKEN-motion-scale` (dial leaf) + `TOKEN-motion-{instant|snap|swift|smooth|drift}` (tier sibling leaves); `TOKEN-density-scale` (dial leaf) + `TOKEN-density-{compact|default|comfortable}` (tier sibling leaves). Three-segment IDs (`TOKEN-motion-scale-snap`) are ruled out — DTCG prohibits group nodes carrying `$type`, architecture.md:693 pins two-segment `<category>.<semantic-name>` pattern, and the story's Task 1 schema models the dial as a sibling of the tiers. Consistency enforced across the story's AC 2 + Task 1 schema + Task 2 rationale-doc sections.
- **Anchor regex landmines avoided** (Story 1.9 CR defer carry-forward — `deferred-work.md:37-38`): new `INVARIANTS.md` anchors use column-0 `- **\`INV-*\`**` form (regex binding at `packages/keel-invariants/src/sync-gate.ts:24`) and avoid fenced-code-block examples containing `INV-` bullets. Any rationale-doc example that needs to SHOW the anchor format uses inline backticks in prose, not triple-backtick fences.
- **Issue Tracking:** Story 1.10 tracked at GitHub issue **#34**. `Refs #34` in every commit trailer; `Closes #34` lands in PR body only when Story 1.10 reaches `done` (matrix row 11). Parent Epic 1 issue **#9** — `Refs #9` optional; never `Closes` it (ralph.py transitions epic issues to Done on EPIC_DONE halt).
