# Implementation Plan

## NOW

- [ ] **Story 2.10 PATCH-1 impl: `prereq-check.sh` exit-12 rc-capture refactor.** Scope-bounded direct edit (NOT a full `/bmad-dev-story` invocation — single-primitive ~15-line bash edit + smoke verify + no downstream shim changes). Change: replace `if docker volume inspect "${VOLUME_NAME}" >/dev/null 2>&1; then volume_present=1; fi` (L84-86) with explicit rc capture — `rc=0; docker volume inspect "${VOLUME_NAME}" >/dev/null 2>&1 || rc=$?; case $rc in 0) volume_present=1 ;; 1) volume_present=0 ;; *) log "docker daemon error during volume inspect (rc=$rc)"; exit 12 ;; esac`. Apply symmetric pattern to the two `docker run --rm … alpine:3.19 test -e …` probes at L95-98 and L99-102 (rc=0 token present; rc=1 test false → token missing; rc∈{125,126,127} → pull/exec failure → exit 12 with `"docker daemon error during <Claude|gh> token probe (rc=$rc)"` pointer). After patch: smoke `DOCKER_HOST=unix:///nonexistent pnpm devbox:prereq:check` → expect exit 12 + daemon-error pointer. Also verify `KEEL_DEVBOX_COMPOSE_PROJECT=nonexistent pnpm devbox:prereq:check --tier2` → still exits 2 with "not authed" pointers (volume-not-found is rc=1, fresh-fork first-run path intact). If INV doc § Exit-code schema wording changes (e.g., sharpen "other docker-daemon error" → "volume-inspect / image-pull / exec failure"), the manifest contentHash for `INV-devbox-prereq-check` MUST bump in the same commit (Story 1.9 sync-gate; `pnpm keel-invariants:check` EXIT=0 gate). If impl matches existing doc verbatim (likely — "propagated via docker's exit" is the design intent now actually realised), doc + manifest contentHash need not change. Post-patch: flip Story State `fixes-pending → fixes-pending` (stays until CR re-run confirms resolution); re-run `/bmad-code-review (args: "2")` in a fresh context to verify PATCH-1 closure; likely `fixes-pending → done` on re-run (ZERO-PATCH re-run possible if no adversarial triage surfaces new findings on the patched surface). Budget forecast: ~35K (orient + read prereq-check.sh + Edit 3 blocks + 2 smoke shells + Change Log v1.7 + IP + RALPH.md + commit).

## QUEUE (Story 2.10..2.17 → Epic 2 close-out)

- [ ] _(after PATCH-1 lands)_ Re-run `/bmad-code-review (args: "2")` fresh-context per § Story Lifecycle matrix row `fixes-pending → done | fixes-pending`. Forecast: ZERO-PATCH re-run IF the fix is narrow (rc-capture only; no new surfaces introduced); `done` transition likely. If re-run surfaces new findings on the patched surface (unlikely given ~15-line scope), another iter of fixes-pending before `done`.
- [ ] _(post-Story-2.10-done)_ Iterate Stories 2.11..2.17 through full lifecycle — 7 remaining substrate stories spanning shared mode + port-publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows. Stories 2.11..2.17 CR-gate forecast adjusted: if a story introduces a novel multi-step runtime probe (docker/alpine/inspect chain beyond Story 2.6 `<verb>-host.sh` single-shot template), forecast ≥1 PATCH at CR gate regardless of post-dev SM verdict (iter-251 LESSON).
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-23 per iter-219..250 repeated pre-push confirmation — `gh pr view 230` shows `statusCheckRollup: []`).

## BLOCKED

- [ ] iter-249+250+251 push to `origin feat/epic-2-packaged-devbox` — REASON: SSH :22 to github.com still timed out at iter-251 orient (identical to iter-249/250 symptoms); HTTPS :443 partially reachable (`curl https://api.github.com` returns 200; `gh pr view 230` fails GraphQL timeout). Remote is `git@github.com:tthew/ralph-bmad` (SSH).
  - Attempted: `ssh -T git@github.com` → `ssh: connect to host github.com port 22: Connection timed out`; `gh pr view 230 --json state,mergeStateStatus,isDraft,statusCheckRollup` → `Post "https://api.github.com/graphql": dial tcp 140.82.121.5:443: i/o timeout` (GraphQL specifically still failing even though bare HTTPS to api.github.com returns 200). Both probes confirm github.com partially unreachable from this iter env at 2026-04-23T23:2X.
  - Error/Issue: transient network / firewall blocking SSH + GraphQL outbound to github.com from this iter env. Four commits LOCALLY STAGED on `feat/epic-2-packaged-devbox` (4 ahead of upstream): `aefb382` (iter-249 push-fail BLOCKED) + `d908d6f` (iter-249 trace-WAIVED) + `447bd84` (iter-250 post-dev SM) + the iter-251 CR commit; no force-push, no amend — preserved intact for next iter.
  - Next: next iteration's first action is PATCH-1 impl (queued NOW). Per § Execute step 0h check unpushed commits at orient → push at step 5 alongside iter-252 commit. If SSH/HTTPS continue to fail, operator may need to investigate iter-env egress to github.com or reconfigure remote to HTTPS; Ralph cannot autonomously reconfigure the remote URL (beyond single-iteration scope). PR #230 pre-push CI status assumed unchanged from iter-247 confirmation (`statusCheckRollup: []`; no CI to wait on).

## ATDD Red Phase

_(none — Story 2.10 ATDD-skip applied at iter-247; stub-docker smokes at iter-248 are ephemeral + torn down inside the same iter per iter-223/230/237 LESSON; no persisted red-phase artefacts. Post-PATCH-1 verification smokes at iter-252 will be similarly ephemeral.)_

## DONE (iter-251 Story 2.10 CR 1-PATCH + 4-DEFER + 22-DISMISS — Story State `sm-verified → fixes-pending`; sprint-row `review` unchanged)

- [x] iter-251: **STORY 2.10 `/bmad-code-review (args: "2")` adversarial triage; 1 PATCH / 4 DEFER / 22 DISMISS; FIFTH cumulative one-pass ZERO-PATCH CR precedent candidate FALSIFIED; Story State `sm-verified → fixes-pending`.** Three-layer Ralph-hosted fan-out per iter-226/233/244 pattern (NOW sixth cumulative confirmation Stories 1.7/1.8/1.9 + 2.7/2.8/2.9 → 2.10): **Blind Hunter** as `bmad-agent-architect` diff-only against `/tmp/story-2-10-review-diff.txt` (2035 lines, 32 files, `930b912..HEAD` range — Story 2.10 branch lifetime); **Edge Case Hunter** as `bmad-tea` diff+project-read; **Acceptance Auditor** as `bmad-agent-dev` diff+spec+INV doc+substrate-citation spot-check. **Convergent HIGH** (Blind+Edge): `prereq-check.sh:84-103` documented exit `12` unreachable. Four SOT sites document `12 other docker-daemon error (volume-inspect crash, alpine pull failure under fail-closed egress; propagated via docker's exit)` — script header L25-26, INV doc § Exit-code schema, README L748, manifest description. Code wraps `docker volume inspect` at L84 and `docker run` at L95,L99 in `if … ; then … ; fi`, discarding docker's non-zero rc (`set -e` does NOT fire inside `if` conditions). Every docker-daemon failure during Tier-2 probing (daemon restart, corp proxy blocking Docker Hub, rate limit, registry outage, fresh-fork first-run over shaky network) silently collapses to `claude_present=0 gh_present=0` → composite "Claude Code not authed" + "gh CLI not authed" pointer + exit 2 with WRONG operator remediation. Exit 12 unreachable by any input. **Auditor was misled by narrow-AC lens** (5/5 ACs + 17/17 SCs VERIFIED + ZERO-PATCH confirmation) — adversarial triage overrode with documented-contract-vs-code-reality gap. **Four DEFERs** at `_bmad-output/implementation-artifacts/deferred-work.md § Deferred from: code review of story-2.10 (2026-04-23)`: AR-71 (`case "${1:-}"` extra-args silent accept; SC-17 polish); AR-72 (`docker-compose.yml:170-171` citation drift; pre-declared deferred per v1.1 PATCH + SC-17 Epic 2 close-out); AR-73 (rm-guard missing on 17-shim wire-in; `_lib.sh` refactor scope per AR-60..AR-70 chain); AR-74 (magic-strings 3-SOT lockstep via convention-only; Story 2.17 substrate-lint `packages/keel-invariants/src/substrate-string-lockstep.ts` candidate). **Twenty-two DISMISS** spans: spec-authorised design choices (SC-4/6/7/11/15/16); upstream-contracts (Story 3.7 halt-enum CI_BLOCKED; Dockerfile HOME/volume writability); narrow-race TOCTOU dups of AR-52/AR-60; verified-false-positives (Blind Hunter F2 "SCRIPT_DIR unbound in 9 shims" — repo-audit confirmed all 11 shims define SCRIPT_DIR at script-head; diff-only lens insufficient). **Chain continuity broken:** Stories 1.13 iter-85 → 2.7 iter-226 → 2.8 iter-233 → 2.9 iter-244 remain valid (4 cumulative one-pass ZERO-PATCH CR precedents); Story 2.10 is FIRST CR gate on chain to surface a patch. The gap was beneath both two-subagent post-dev SM verification (AC satisfaction only, not exit-code reachability) AND prior Stories' narrow impl surfaces (Stories 2.7-2.9 wrappers have no multi-step probe chain comparable to prereq-check.sh's Tier 2 volume-inspect + dual docker-run). **LESSON: adversarial CR catches what narrow-AC verification cannot; CR-after-SM separation is load-bearing precisely for this class of finding.** Story file landed: `### Review Findings` subsection with 1 PATCH + 4 DEFER action items; Change Log v1.6 prepended; Status HTML comment `sm-verified → fixes-pending` inline. Sprint-status UNCHANGED per iter-202 precedent (CR gate does NOT flip sprint-row until closure). `deferred-work.md` appended AR-71..AR-74 under new H2. RALPH.md signpost landed with one novel LESSON (CR-catches-what-SM-cannot for multi-step runtime probes). Push BLOCKED extension from iter-249/250 (SSH :22 + HTTPS GraphQL :443 still timing out at iter-251 orient; 4 commits unpushed locally on branch; preserved intact). Budget iter-251: ~80K (orient ~10K + 3 parallel adversarial subagents ~50K + triage ~5K + Review Findings ~5K + Change Log v1.6 prepend + deferred-work.md append + IP + RALPH.md + commit).

_(iter-243..250 Story 2.9 closure + Story 2.10 lifecycle iters pruned per Guardrail 2 — see Story 2.10 § Change Log v1.0–v1.6 + RALPH.md Signposts for per-gate detail.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **9/17 stories done** (2.1-2.9) + 8/17 in-flight-or-backlog (2.10 `Story State = fixes-pending` sprint-row `review` at iter-251 post-CR; 2.11..2.17 still backlog).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered at iter-248 9/9 stub-docker harness (workspace-based, not tmpfs).
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:119). Host-side shim count: **18** at Story 2.10 landing (17 Story 2.9 landing + 1 new `prereq-check.sh` primitive — not a `<verb>-host.sh`).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **9 done** (2.1-2.9); 1 at sprint-row `review` Story State `fixes-pending` (2.10); 7 backlog (2.11..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.10 — Prerequisite check (Docker runtime + Claude auth + gh auth) with pointer errors.
- **Story File:** `_bmad-output/implementation-artifacts/2-10-prerequisite-check-docker-runtime-claude-auth-gh-auth-with-pointer-errors.md`
- **Story State:** `fixes-pending` — next iter action per § Story Lifecycle Decision Matrix row `fixes-pending → done | fixes-pending` is PATCH-1 impl (direct edit; scope-bounded ~15-line bash) followed by re-run `/bmad-code-review (args: "2")` to verify closure. FIRST CR gate on the 4-story one-pass ZERO-PATCH chain to surface a patch at iter-251.
- **GitHub Issue:** Story 2.10 likely unwired (`RALPH_ISSUE_NUMBER` unset at iter-250/251 check; skip `Refs #<story-issue>` in commits). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2; iter-251 PR-state probe failed due to GraphQL timeout; assumed unchanged from iter-247 `statusCheckRollup: []` + `mergeStateStatus: CLEAN` + `isDraft: true`).

## Notes

- **iter-251 Story 2.10 CR: FIRST CR-gate patch on 4-story ZERO-PATCH chain.** Adversarial triage (Blind Hunter + Edge Case Hunter + Acceptance Auditor) surfaced convergent HIGH finding (`prereq-check.sh` exit 12 unreachable; 4-SOT documented but 0-code-path) that neither two-subagent post-dev SM verification (iter-250) nor prior narrower Stories' CR gates caught. Adversarial CR's value: catching documented-contract-vs-code-reality gaps that AC-satisfaction-lens misses. LESSON forecast-actionable for Stories 2.11..2.17: when a story introduces a novel multi-step runtime probe beyond the `<verb>-host.sh` single-shot template, forecast ≥1 PATCH at CR gate regardless of SM verdict.
- **iter-250 Story 2.10 post-dev SM ZERO-PATCH — mechanical inheritance from Story 2.9 iter-243 at ~95%.** Two-subagent verification pattern (iter-235 LESSON) empirically validated across two stories (2.9 iter-243 + 2.10 iter-250) on Epic 2 branch. Zero novel RALPH.md LESSON at SM gate — pattern stable. BUT the SM gate's narrow lens was superseded by the CR gate at iter-251 (above).
- **iter-249 Story 2.10 trace-WAIVED — mechanical template inheritance from Story 2.9 iter-242 at ~85%.** No novel LESSON. FR14n row `in-dev → traced` transition. 4 artefacts landed. Sprint-status unchanged per iter-202 precedent.
- **iter-248 Story 2.10 impl — mechanical inheritance from Stories 2.6/2.8/2.9; zero drift from v1.2 spec.** Two novel LESSONs in RALPH.md signposts: (a) collateral `INV-prek-prepare-lifecycle` contentHash bump discipline on root `package.json` edits; (b) stub-docker 9-smoke harness pattern generalises to any Epic-2 tier-dispatch primitive. Both forecast-actionable for next 7 stories on Epic 2 branch. **Note iter-251:** the 9-smoke harness did NOT probe docker-daemon error branch (all smokes run with a running stub-docker); this was the PATCH-1 blind spot at impl time. Future smoke harnesses SHOULD include a "docker-daemon-broken-mid-flight" variant.
- **iter-244/246 audit-findings carry-forward to post-PATCH-1 CR re-run** (per v1.1 PATCH notes; unchanged from iter-247/248/249/250/251): (1) restart.sh transitive-delegate prepend-only pattern (documented inline); (2) benchmark.sh OUT-OF-SCOPE clarification (SC-11); (3) Manifest entry count drift (29→30 baseline); (4) Alpine egress non-applicability doctrine (SC-4 + § Alpine probe image).
- **iter-235 carry-forward — two-subagent verification pattern:** valid for post-dev SM gates but INSUFFICIENT for CR gates; CR requires full 3-layer adversarial fan-out (iter-251 LESSON).
- **iter-234 carry-forward — killed-before-commit skill recovery pattern:** unchanged; valid recovery primitive.
- **iter-221 carry-forward — pre-dev SM `~5-PATCH` ceiling:** held at iter-246 (4 PATCHes at low end of 2-5 forecast band). Forecast holds for Stories 2.11..2.17.
- **TTY-detect at docker-exec shim (iter-214 LESSON) — Story 2.10 NON-APPLICATION confirmed at iter-248:** prereq-check.sh does NOT `docker exec` — it `docker run --rm` (non-interactive, no TTY, short-lived); SC-11 wire-ins preserved each shim's TTY posture unchanged.
- **`<verb>-host.sh` naming convention DEVIATION (iter-199 EXTENDED through iter-251):** Story 2.10 shipped `prereq-check.sh` per Story 2.3/2.4 primitive pattern (shared primitive called by other shims, not a pnpm-verb wrapper). `pnpm devbox:prereq:check` standalone verb IS wired (Task 2).
- **Substrate-citation drift forecast for Stories 2.11..2.17:** Story 2.10's 1-drift outcome (vs Story 2.9's 13-drift baseline) confirms prepend-only edits shift fewer downstream lines than block-swap edits. Forecast: Stories 2.11..2.17 may individually accumulate 1-3 drifts each, all deferred to Story 2.17 Epic 2 close-out reconciliation per SC-17 policy. Total forecast at Epic 2 close-out: ~10-20 cumulative line-number drifts to reconcile in a single SC-17 polish pass.
