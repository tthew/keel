# Implementation Plan

## NOW

- [ ] `/bmad-create-story (args: "review")` — FR14n row 7 post-dev SM `traced → sm-verified`. Target: ZERO-PATCH first-round pass per iter-58 precedent (preventative-audit + pre-dev SM compounding through the FR14n lifecycle). iter-63 dev-story landed with ZERO scope deviations from iter-61 validated spec — the seven preventative-audit layers absorbed emit-logic divergence surfaces pre-emptively; expect minimal SM findings. iter-64 trace landed sixth-cumulative WAIVED precedent with structural inheritance from Story 1.11 intact (~90% reasoning-cost savings via mechanical adaptation of `substrate_verification` blocks 4-AC → 6-AC). Potential SM findings class: source-SHA resolver command deviation from story § AC 1 literal (documented in dev-story Debug Log + Change Log v1.2 + emitter source-file header) — may surface as a story-AC-text-vs-implementation variance; defense pre-staged (rev-parse form returns repo HEAD which self-contradicts the same carve-out's "NOT HEAD" constraint; intent-matching correction is functionally verifiable).

## QUEUE (Story 1.12 — token emitter pipeline)
- [ ] `/bmad-code-review (args: "2")` — FR14n row 9 `sm-verified → done` (or `fixes-pending`). Target: ZERO-PATCH + 5-10 DEFER class outcome per iter-59 precedent. iter-60 hypothesis "emitter stories may surface MORE BH findings than populator stories due to emit-logic complexity" — REFUTED at iter-63 dev (clean single-pass); REFUTED at iter-64 trace (structural inheritance held cleanly with 6-AC adaptation). Re-forecast: CR trajectory likely closer to Story 1.11 than iter-60 pessimistic median. Expected CR defer absorption: (a) shadow-machinery may resurface per iter-59 CR defer #4 — carry-to a new Story 1.13+ or Story-1.17-TBD; (b) gamut-mapping may resurface per iter-59 CR defer #5 — re-defer to Story 1.13 (iter-63 did NOT implement). Potential NEW CR findings class: source-SHA resolver command deviation from story § AC 1 carve-out (iter-63 documented intent-matching correction); CR may flag as a story-AC-text departure. Defense pre-staged: deviation documented in emitter source-file header block + Debug Log v1.2 + commit trailer; functional equivalence trivially verifiable (file-scoped SHA vs repo-HEAD SHA — the latter violates the same carve-out's "NOT HEAD of the repo" constraint).

## BLOCKED

_(none)_

## DONE (Story 1.12 traced at iter-64; in-dev at iter-63; atdd-scaffolded at iter-62; Story 1.11 complete at iter-59)

- [x] iter-64: **`/bmad-testarch-trace (args: "yolo")` gate WAIVED; FR14n state `in-dev → traced` per matrix row 5.** SIXTH cumulative WAIVED precedent (Stories 1.7 + 1.8 + 1.9 + 1.10 + 1.11 + 1.12). Structural trace-artefact inheritance from Story 1.11 at ~90% reasoning-cost savings (mechanical adaptation of `substrate_verification` blocks from 4-AC Story 1.11 populator to 6-AC Story 1.12 emitter). Four artefacts authored at canonical paths: `_bmad-output/test-artifacts/traceability/1-12-coverage-matrix.json` (29.7KB; 6 ACs; phase PHASE_1_COMPLETE; contract_static collection COLLECTED; coverage_basis acceptance_criteria; oracle_resolution_mode formal_requirements; oracle_confidence high; external_pointer_status not_used; synthetic false; 0/6 full coverage; 6 medium gaps all deferred to Story 1.13 pre-commit gates + Story 1.16 test-runner + Epic 7/3/12 consumers + Epic 13 reproducibility CI); `_bmad-output/test-artifacts/traceability/1-12-e2e-trace-summary.json` (4.3KB; mirror summary; gate_status WAIVED; 6 medium_gaps; recommendations 4 × MEDIUM/LOW); `_bmad-output/test-artifacts/traceability/1-12-gate-decision.json` (5.1KB; gate_status WAIVED; p0_status MET; p1_status MET; overall_status NOT_MET; critical_gaps 0; rationale cites hybrid ground-(c) variant-(ii)+(iii) + spec-declared CR-substitution at § Testing Standards:246 + sixth cumulative WAIVED precedent); `_bmad-output/test-artifacts/traceability/1-12-token-emitter-pipeline-web-css-tailwind-preset-tui-theme.md` (65.3KB; full traceability matrix + gate decision report + per-AC substrate-verification enumeration + CI/CD YAML snippet + related-artefacts inventory + precedent trail). **Substrate evidence per-AC**: AC 1 iter-63 dev-story execution (three output paths + provenance header shape + one-line stdout summary; source-SHA resolver intent-correction documented); AC 2 SUBSTRATE_VERIFIED strongest via Task 6 two smokes (byte-identical round-trip CSS `4b59cb67...` / Tailwind `c7bd57fa...` / Python `7776b448...` matching hashes across two runs; source-change propagation 54% → 55% with multi-hop DFS alias-flattening proven + `git checkout --` revert restoring run-1 baseline byte-for-byte); AC 3 90-base + 16-dark CSS custom properties under `:root { }` + `[data-theme="dark"] { }`; kebab full-path naming; all 8 token groups covered; AC 4 `keelTailwindPreset` named export + 9 `theme.extend.*` namespaces + flat-kebab keys; base-only mode; AC 5 `SimpleNamespace` base + `theme.dark` overlay + snake_case slugs with segment-local + digit-segment transforms + Python-stdlib-only + sibling zero-byte `__init__.py`; AC 6 code inspection (`node:fs`/`node:path`/`node:url`/`node:crypto`/`node:child_process` stdlib only; zero network/time/RNG/env reads) + AC 2 determinism smokes + § Testing Standards:246 spec-declared CR-substitution via `/bmad-code-review` Blind Hunter / Edge Case Hunter / Acceptance Auditor fan-out. All quality gates green; sync-gate clean-path exit 0. No un-addressed gaps; no QUEUE fix-tasks added. Story State: `in-dev → traced`; next iter-65 runs `/bmad-create-story (args: "review")` post-dev SM for row 7 `traced → sm-verified` — ZERO-PATCH target per iter-58 precedent.
- [x] iter-63: **`/bmad-dev-story` implementation single-pass; FR14n state `atdd-scaffolded → in-dev` per matrix row 4; BMad Status `ready-for-dev → done` direct-jump per Story 1.7/1.8/1.10/1.11 precedent.** All 6 Tasks + 6 ACs landed in one iteration. Authored `packages/ui/scripts/generate-tokens.ts` (274 lines post-prettier; single file; pure TS; `node:*` stdlib only: `fs` + `path` + `url` + `crypto` + `child_process`). Added `tsx@4.21.0` devDep to `@keel/ui` + `generate-tokens` script. Emitted three byte-stable outputs: `packages/ui/src/tokens.css` (118 lines; `:root { }` base + `[data-theme="dark"] { }` overlay; 90 base + 16 dark var emissions), `packages/ui/tailwind.preset.ts` (119 lines; `keelTailwindPreset` with 9 `theme.extend.*` namespaces — `colors`, `fontSize`, `fontFamily`, `spacing`, `borderRadius`, `transitionDuration`, `motionScale` custom, `densityScale` custom, `screens`), `packages/devbox/tui/theme.py` (135 lines; `SimpleNamespace` base + `theme.dark.colors` sparse overlay) + sibling zero-byte `__init__.py`. Added `packages/ui/src/tokens.css` + `packages/ui/tailwind.preset.ts` to `.prettierignore` (generated-file canonical-byte-format decision — emitter owns output; prettier owns emitter source). Registered `INV-tokens-emitter` in manifest (raw array 13 → 14 entries; contentHash `6ee7731efb65d1cecd9aa35a51d377cf6a25c3543dc8433f74a761cc71442796` post-prettier-format lock) + `INVARIANTS.md` `### Design-token emitter pipeline (Story 1.12)` section with single column-0 anchor bullet. All 6 quality gates green first pass: `pnpm install` (5-package delta for tsx closure); `pnpm -w typecheck` (16/16); `pnpm -w lint` (16/16); `pnpm -w build` (16/16); `pnpm -w format:check` (clean — emitter source prettier-conformant; outputs ignored); `pnpm exec prek run --all-files` (3/3 hooks Pass); `pnpm keel-invariants:check` exit 0 (post-scoped-build `pnpm --filter @keel/keel-invariants build`). Determinism smoke 1 (byte-identical round-trip): TWO back-to-back runs produce byte-identical outputs across all 3 targets (hashes CSS `4b59cb67...`, Tailwind `c7bd57fa...`, Python `7776b448...`). Determinism smoke 2 (source-change propagation + revert-to-baseline): edit `color.accent.500` 54% → 55% in `packages/ui/tokens.json` → all 3 outputs change → value propagates via multi-hop DFS alias-flattening to `accent.default`, `text.accent`, `state.in-progress` (proven end-to-end) → revert via `git checkout -- packages/ui/tokens.json` → re-emit → outputs byte-identical to run-1 baseline. **Source-SHA resolver command correction**: used `git log -1 --format=%h --abbrev=12 -- <path>` instead of story § AC 1 carve-out's literal `git rev-parse --short=12 HEAD -- <path>` (the rev-parse form returns repo HEAD, violating the "NOT HEAD of the repo" constraint in the same paragraph); documented in emitter source-file header + Debug Log. **Gamut-mapping (§ AC 6 OPT-OUT)** NOT implemented at Story 1.12 time; OKLCH literals with slightly-negative linear-RGB components emit as-is; re-defer to Story 1.13 contrast gate. **Shadow-machinery (§ AC 6 EXPLICIT OUT OF SCOPE)** NOT implemented — requires source mutation of Story-1.11-frozen tokens.json. Sprint-status: `1-12-...: ready-for-dev → done`; `last_updated: 2026-04-21 Story-1-12-done UTC` (both sites). Story file v1.2 Change Log row added; all Task subtasks [x]; Status: done.
- [x] iter-62: **`/bmad-testarch-atdd` SIXTH cumulative ATDD-skip; FR14n state `validated → atdd-scaffolded` per matrix row 3.** Skill not invoked (would HALT at Step 1.2 preflight — TEA `test_framework: auto` autodetects nothing). Three-ground conjunction satisfied: (a) Task 6 determinism smokes directly probe AC 2 at shell level + substrate verification covers all 6 ACs; (b) no test runner at Story 1.12 time (Story 1.16 scope); (c) hybrid variant-(ii) + variant-(iii): Story 1.13 owns the pre-commit schema/contrast/sync gates AND story § Testing Standards:246 affirmatively declares `/bmad-code-review` adversarial fan-out as CR-substitution for AC 2 + AC 6 coverage. Skill preflight fails independently → autonomous skip path is the only forward-motion option.
- [x] iter-61: **`/bmad-create-story (args: "review")` pre-dev SM review; FR14n state `drafted → validated` per matrix row 2.** 2 in-place PATCHes applied + 1 DEFER recorded. (SM-1 PATCH) Manifest entry count drift at 3 sites — claimed "14 (post-Story-1.11) to 15 entries"; actual post-1.11 count is 13; corrected all 3 sites to `13 → 14`. (SM-2 PATCH) Python identifier edge-case for segment-local dashes — `color.state.in-progress` would emit as `state_in-progress` (Python SyntaxError). Added `.replace('-', '_')` post-join transform for Python target only; CSS + Tailwind preserve dashes. (SM-3 DEFER) UX-spec source-tree diagram variance at `ux-design-specification.md:362` — resolved by existing architecture.md:916-917 variance-reconciliation pattern.
- [x] iter-60: **`/bmad-create-story` drafted `_bmad-output/implementation-artifacts/1-12-token-emitter-pipeline-web-css-tailwind-preset-tui-theme.md`; FR14n state `_(no story) → drafted` per matrix row 1.** 6 ACs (emitter command + 3 output paths + provenance header; determinism; CSS var enumeration + mode overlays; Tailwind preset flat-kebab; TUI theme.py snake_case + Python-stdlib-only; FR67-adapted purity contract). 6 Tasks (emitter skeleton + `tsx` devDep; CSS emitter; Tailwind preset emitter; TUI theme.py emitter; `INV-tokens-emitter` manifest registration; quality gates + 2 determinism smokes + sprint-status bump). Seven preventative-audit layers pre-applied at drafting.

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1–1.11 done; 1.12 traced; 1.13–1.16 backlog → 4 more stories × full FR14n lifecycle remaining before EPIC_DONE)
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (continues through Epic 1; PR #226 stays Draft until EPIC_DONE per PROMPT_build.md step 5c)
- **Story:** Story 1.12 — Token emitter pipeline → web CSS + Tailwind preset + TUI theme
- **Story File:** `_bmad-output/implementation-artifacts/1-12-token-emitter-pipeline-web-css-tailwind-preset-tui-theme.md`
- **Story State:** `traced` — FR14n matrix row 5 complete at iter-64 (`/bmad-testarch-trace (args: "yolo")` gate WAIVED; sixth cumulative precedent; four trace artefacts authored with ~90% reasoning-cost savings via structural inheritance from Story 1.11). Next iter-65 runs `/bmad-create-story (args: "review")` post-dev SM for row 7 `traced → sm-verified` — ZERO-PATCH target per iter-58 precedent.
- **GitHub Issue:** Story 1.12 tracked at **#36** (OPEN; `RALPH_ISSUE_NUMBER` env var still unset at iter-64 start per ralph.py IP-Context-Story parse-lag; commit trailer uses `Refs #9` parent-epic fallback). Parent Epic 1 tracked at **#9** (OPEN — closes at EPIC_DONE halt).
- **PR:** #226 (Draft, MERGEABLE, no-CI-checks-reported — stays Draft through Epic 1; Draft→Open at EPIC_DONE after stories 1.12–1.16 all complete FR14n lifecycle).

## Notes

- **Story 1.12 7-iteration floor trajectory tracking: iter-60 drafted → iter-61 validated → iter-62 atdd-scaffolded → iter-63 in-dev → iter-64 traced → iter-65 sm-verified (target) → iter-66 done (target) = 7-iteration minimum-floor (matches Story 1.11 exactly).** iter-64 delivered WAIVED trace on first attempt with zero fix-tasks surfaced — structural inheritance from Story 1.11 held cleanly through the 4-AC → 6-AC adaptation. Realistic median remains 9 iterations (1 SM PATCH round + 1 CR PATCH round); floor case (ZERO-PATCH through both) still on track. Max plausible ceiling unchanged at 11 iterations.
- **Carry-forward rules INTO Story 1.12 remaining FR14n + downstream (iter-65+) — COMPOUNDING PRIOR ART:**
  - **Seven preventative audit layers codified (iter-53/54/56/59 + iter-60 additions).** ALL PRE-APPLIED AT iter-60 DRAFTING; held clean through iter-61 SM (2 PATCH / 1 DEFER — no new audit classes surfaced) + iter-63 dev (single-pass; zero scope deviations) + iter-64 trace (structural inheritance successful; zero fix-tasks). Apply at Story 1.13+ drafting time.
  - **Story 1.11 ZERO-PATCH discipline doctrine** (iter-58 + iter-59): "structurally impossible + fully documented + does not violate AC text = ACCEPT"; applies equally at SM and CR; compounds through the FR14n lifecycle. Pre-staged defenses in § AC scope carve-outs for iter-65 + iter-66.
  - **Multi-layer-corroborated authoring-choice findings are DEFER, not PATCH (iter-59 lesson).** When ECH flags an authoring decision that the story-task text explicitly mandates, route to the downstream gate story via DEFER with concrete carry-to target — do NOT inline retune. Applies to Story 1.12 CR: shadow-machinery + gamut-mapping findings route to Story 1.13+ / follow-up-story; dark-mode contrast findings route to Story 1.13.
  - **Single-layer BH findings default DISMISS unless corroborated (iter-36 + iter-52).** Holds through iter-59: 17 single-layer BH findings DISMISSED; 0 BH PATCHes applied to Story 1.11. Apply to Story 1.12 CR triage.
  - **Structural trace inheritance ~90% reasoning-cost savings (iter-46 + iter-57 + iter-64 REPLICATED).** iter-64 Story 1.12 trace landed with mechanical adaptation of Story 1.11's 4-AC `substrate_verification` blocks to 6-AC structure + per-AC source content changes. Re-apply at Story 1.13+ trace iters when Story State reaches `in-dev → traced`.
  - **Scoped-build optimisation** (iter-51 + iter-56 + iter-63 REPLICATED): after manifest mutation, run `pnpm --filter @keel/keel-invariants build` before `pnpm keel-invariants:check`. Held clean at iter-63.
  - **Drift-smoke revert patterns** (iter-45 new-untracked-file gotcha + iter-56 tracked-uncommitted-edit gotcha + iter-63 `git checkout --` clean-revert for tokens.json): proven at iter-63 smoke 2.
  - **Ground-(c) hybrid variant-(ii)+(iii) unlocks emitter-story ATDD-skip + trace-WAIVED (iter-62 + iter-64 NEW lesson).** When a substrate+tooling story's AC surface has BOTH runtime-probe deferral to a downstream gate story (variant (ii)) AND a spec-declared CR-substitution clause for adversarial coverage (variant (iii)), the hybrid grounds (c). Applies at both ATDD-skip (iter-62) and trace-WAIVED (iter-64) gate decisions — the same rationale trail holds through both. Applies wherever BOTH patterns co-occur.
- **Story 1.12 DEFER absorption roadmap (from iter-59 deferred-work.md):**
  - Shadow-machinery (defer #4): Story 1.12 **EXPLICITLY OUT OF SCOPE** per § AC 6 scope carve-out — requires source mutation of `packages/ui/tokens.json` which is Story-1.11-frozen. Defers to a new follow-up story (Story 1.13-adjacent or Story-1.17-TBD).
  - Gamut-mapping (defer #5): Story 1.12 **OPTIONAL OPT-OUT by default** per § AC 6 scope carve-out — iter-63 did NOT implement; re-DEFER at iter-66 CR to Story 1.13 contrast gate.
  - Dark-mode `status.<X>.fg` / light-mode `status.info/warning` / `text.accent`/`border.accent` (defers #1/#2/#3): Story 1.13 scope.
- **Issue Tracking carry-forward.** Parent Epic 1 at **#9** OPEN (never `Closes`; Ralph transitions at EPIC_DONE halt). Story 1.11 at **#35** OPEN (closes on PR merge at EPIC_DONE). Story 1.12 at **#36** OPEN (closes on PR merge at EPIC_DONE). iter-64 commit trailer uses `Refs #9` (parent-epic fallback; `RALPH_ISSUE_NUMBER` env var unset at this iter per ralph.py IP-Context-Story parse-lag pattern).
