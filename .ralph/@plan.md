# Implementation Plan — PR #230 Round-3 review-fix-arc

Detail: `.ralph/round3-fix-arc.md` (Round-2 closeout at `.ralph/round2-fix-arc.md`). The IP holds NOW + QUEUE + DONE + Context only.

## NOW

- [ ] **Round-3 thread-resolve sweep / decision** — live `gh api graphql ... reviewThreads` count = 2 unresolved (A5+A6 DEFER-by-design); no Round-3 reviewer threads have landed. **Decision:** if step-0h shows R3 threads posted (next-tthew-review or self-posted R3 summary), reply-with-commit-ref + `resolveReviewThread` mutation per Node-script pattern (RALPH.md `iter:pr-230-closeout` recipe). Otherwise self-post R3-self-review summary comment per Round-2 pattern (`iter:pr-230-round2-decompose`) listing FIX-12..16 + WONTFIX-doc landings + DEFER A5/A6 + NOFIX R3-I03..I06, then mark sweep done. ~small.

## QUEUE (Round-3 fix-arc; pull from `.ralph/round3-fix-arc.md`)
- [ ] **Final CI watch + EPIC_DONE halt** — step-0h CI gate clear → write `EPIC_DONE` per `.ralph/round3-fix-arc.md § Halt criterion`. (Carry-rule: **Monitor-bookkeeping-loop-break** at `iter:pr-review-4` — if CI clear at iter-N+1 start AND thread-resolve done, jump straight to halt.)

## DONE (Round-3 only — Round-1+2 archived in git log, RALPH.md `iter:pr-230-fix-1..11` + `iter:pr-230-wontfix-d1d2d3` + `iter:pr-230-thread-resolve-sweep` + `iter:pr-230-epic2-halt`)

- [iter-pr230-round3-decompose] Round-3 (4 HIGH + 1 LOW) decomposed FIX-12..16 + WONTFIX + DEFER-continue
- [iter-pr230-fix-12] FIX-12 (R3-Hook-A) verb-gate left-boundary widening landed via `verb_left_re`/`subshell_left_re` shared vars; +secret_left/right_re backtick admit; +env-dump-bare subshell-narrowed; 23/23 R3-Hook-A flip; 82/82 hook fixtures + 56/56 vitest preserved
- [iter-pr230-fix-12-ci] FIX-12 push `4420c3f` CI monitored GREEN (4/4 checks pass — node × 2, python × 2)
- [iter-pr230-fix-13] FIX-13 (R3-Hook-B + sub-agent C1/C2/C3/C4 broadening) landed; reader_verb_re widened 14→50 verbs, dotsource_re added (asymmetric verb_left_re/subshell_left_re for FP avoidance), wrapper-strip extended `eval` arm to also strip `builtin|command|exec`. example_read_re lockstep extension. substrate↔seed byte-parity + manifest contentHash lockstep substrate L370 + seed L407 (`27cfc8a9…` → `d6b5c6be…`). 77/77 adversarial pass + 82/82 hook fixtures + 56/56 vitest + typecheck + lint green
- [iter-pr230-fix-13-ci] FIX-13 push `d3c127a` CI monitored GREEN (4/4 checks pass — node × 2, python × 2 against HEAD `d3c127a`). FIX-14 promoted to NOW.
- [iter-pr230-fix-14-precheck] Chore IP commit `d1e6c4c` (NOW=FIX-14) CI monitored GREEN (4/4 checks pass — 2× node + 2× python at HEAD `d1e6c4c`). Pre-push gate clear; next iter commences FIX-14 (R3-Inv-I01 — wire `pnpm keel-invariants:check` to `.pre-commit-config.yaml` + `.github/workflows/ci.yml`).
- [iter-pr230-fix-14] FIX-14 (R3-Inv-I01) wired `pnpm keel-invariants:check` into BOTH legs (combined CI-monitor + work iter per `iter:pr-review-4` Monitor-bookkeeping-loop-break carry-rule; `8585a1b` precheck CI confirmed GREEN at iter start, then FIX-14 executed in same iter). **Leg A** `.pre-commit-config.yaml`: `keel-invariants-check` hook (always_run, pass_filenames=false, `bash -c 'pnpm --filter @keel/keel-invariants build && pnpm keel-invariants:check'`); cold-cache wall-clock ~0.7s (build 0.29s + check 0.39s) — well within ≤10s pre-commit budget. **Leg B** `.github/workflows/ci.yml`: node-job step inserted after `pnpm install --frozen-lockfile` and before `pnpm turbo run test lint typecheck` so cheap drift gate fails fast ahead of heavy turbo. **Surprise lockstep manifest bump** — FIX-14 plan claimed "No L1, no manifest bump" but BOTH YAML files are whole-file contentHash-tracked: 3 manifest entries (INV-prek-pre-commit-config + INV-prek-commit-msg-config sharing `.pre-commit-config.yaml`, INV-fr14i-ci-workflow-presence on `.github/workflows/ci.yml`) bumped via `node /tmp/fix14_update_manifest.js` (RALPH.md `iter:pr-230-fix-5/10` recipe). Empirical adversarial: real edit → 3 content-hash-mismatch drifts fired (exit 1); post-bump exit 0. Vitest sync-gate suite (9 tests including expected-id-missing + byte-parity-mismatch) passes. **Regression GREEN**: typecheck (16/16 turbo), lint (16/16 turbo), test (17/17 turbo / 56/56 vitest), format:check, prek run keel-invariants-check (--all-files).
- [iter-pr230-fix-15] FIX-15 (R3-Devbox-D01) three-leg awk-injection shape gates landed (env-vector + supply-chain + threat-model doc). Leg A `reload-egress.sh:46` + `env-check.sh` `SHAPE_IP_LITERAL` group reject non-`[0-9a-fA-F:.]` `KEEL_DEVBOX_DNS_UPSTREAM`. Leg B post-`mapfile` per-domain LDH gate. Leg C `docs/invariants/devbox-egress.md` § Threat-model section. Whole-file contentHash bump on `INV-devbox-egress-contract` (`764bb6f8…` → `cd755e53…`). 39/39 isolated case-pattern adversarial + end-to-end via `bash reload-egress.sh /tmp/wl-clean.txt`. Regression GREEN.
- [iter-pr230-fix-15-ci] FIX-15 push `e4bf2d5` CI monitored GREEN (4/4 checks pass at HEAD `e4bf2d5`). FIX-16 promoted to NOW.
- [iter-pr230-fix-16-precheck] Chore IP commit `26d8309` (NOW=FIX-16) CI monitored GREEN (4/4 checks pass — 2× node + 2× python at HEAD `26d8309`). Combined CI-monitor + FIX-16 work in same iter per `iter:pr-review-4` Monitor-bookkeeping-loop-break carry-rule.
- [iter-pr230-fix-16] FIX-16 (R3-D02) doc-drift housekeeping landed. Replaced 9 stale literal line-number cross-refs with stable section/SC-step refs across 2 files. **reload-egress.sh** (8 sites, 7 Edit calls): L54 `line ~307` → `§ "Render dnsmasq config (SC-5 step 4)"`; L149-150 `line ~307` + `lines ~316-317` → same section; L200-201 `line ~330` + `line ~370` → `§ "Render dnsmasq config (SC-5 step 4)"` + `§ "Reload or start dnsmasq (SC-5 step 5)"`; L219 `line ~225` → `§ "Resolve domains → IPv4/IPv6 allow-rules"`; L229+L240 `dnsmasq.conf:59` + `:60` → `dnsmasq.conf § "Fail-closed default (SC-12)"`. **Dockerfile** (1 site): L372 `entrypoint.sh:235 launch` → `entrypoint.sh launch` (next-line literal `gosu dev /usr/sbin/sshd …` command is its own stable anchor). Two reflow follow-ups for awkward line breaks. Sweep extended beyond IP-cited 4 audit sites to all same-class same-file siblings per Sweep-completion + IP-planner-trust-calibration carry-rules; out-of-scope cross-file refs in `dnsmasq.conf:33,48`, `reload-egress.sh:425`, `lib/main-repo-resolver.sh:164,229` left untouched (not currently drifted; preventive cleanup deferred). **No L1, no manifest bump** — affected files (`reload-egress.sh`, `Dockerfile`) are not in `BYTE_PARITY_PAIRS` and not sourcePath'd in `invariants.manifest.ts` (only `docs/invariants/*.md` are). Regression GREEN: typecheck (16/16 turbo cached), lint (16/16 turbo cached), vitest 56/56, format:check, keel-invariants:check.
- [iter-pr230-fix-16-ci] FIX-16 push `4a8642b` CI monitored GREEN (4/4 checks pass — 2× node + 2× python at HEAD `4a8642b`). WONTFIX-doc promoted to NOW.
- [iter-pr230-wontfix-r3-precheck] Chore IP commit `ec6b779` (NOW=WONTFIX-doc) CI monitored GREEN (4/4 checks pass — 2× node + 2× python at HEAD `ec6b779`). Combined CI-monitor + WONTFIX-doc work in same iter per `iter:pr-review-4` Monitor-bookkeeping-loop-break carry-rule.
- [iter-pr230-wontfix-r3] WONTFIX-doc landed (R3-H48 + R3-H49 + R3-D03 + R3-D04). 4 inline `# WONTFIX (PR #230 R3-<id>)` comment blocks. **Hook substrate** (`.claude/hooks/block-secret-access.sh`) + byte-identical seed: R3-H48 after `reader_verb_re` (verb-trailing `[[:space:]]` accepts whitespace only; `cat<.env`/`cat>.env` no-space form bypasses; ⊗ Do NOT loosen — would FP on `cat<<HEREDOC`); R3-H49 after `interp_verb_re` (static-vs-dynamic limit; runtime-decode `bash -c "$(echo …|base64 -d)"` evades regex; ⊗ Do NOT add runtime evaluator — egress firewall + OAuth named-volume isolation are load-bearing). Edits via `node /tmp/wontfix_update_hook.js` (L1 install-boundary workaround per `iter:pr-230-fix-5`). **Hardening doc** (`docs/invariants/devbox-hardening.md`) new § Defense-in-depth gaps section: R3-D03 sshd implicit-default residuals (`AllowTcpForwarding`/`AllowAgentForwarding`/`MaxAuthTries`/`LoginGraceTime`/`MaxStartups`/`PermitUserRC` enumerated) + R3-D04 `cap_add: NET_RAW` likely-unused note (operator-workstation removal verification deferred); both INFO/accepted-residual + load-bearing-layer note + AMEND-path opt-in. **Compose** (`packages/devbox/docker-compose.yml`) WONTFIX-D04 inline comment between three-cap-block + security_opt block (cross-references hardening-doc § Defense-in-depth gaps). **Manifest contentHash lockstep**: `INV-claude-hook-secret-denylist` (substrate L370) + `INV-claude-hook-secret-denylist-seed` (L407) shared `d6b5c6be7043…` → `42e5b5018cb7…` (byte-parity preserved); `INV-devbox-homedev-named-volume` (L310 sourcePath devbox-hardening.md) `7ba95e97c708…` → `aac19aa74d61…`. Bumped via `node /tmp/wontfix_update_manifest.js`. Pure-comment edit on hook substrate; zero semantic change. Regression GREEN: 82/82 hook fixtures + 56/56 vitest + 16/16 typecheck + 16/16 lint + format:check + sync-gate clean.

## Context

- **Phase:** 4-implementation — PR #230 Round-3 review-fix-arc landing complete (Round-1 + Round-2 + FIX-12..16 + WONTFIX-doc landed); remaining: thread-resolve sweep + final CI + EPIC_DONE halt.
- **Epic:** 2 — Sandboxed Execution Environment. All 18 stories `done` in sprint-status.yaml; PR #230 OPEN/CLEAN/MERGEABLE awaiting final closeout + merge.
- **Epic Branch:** `feat/epic-2-packaged-devbox`.
- **Story:** _(none — PR-fix-arc bypasses § Story Lifecycle per landing-summary intent.)_
- **Story File:** _(n/a)._
- **Story State:** _(no story)._
- **PR:** #230 OPEN, isDraft=false; HEAD `ec6b779` pre-push (next push will advance HEAD with WONTFIX-doc); CI 4/4 GREEN at `ec6b779`. Live unresolved-thread count: 2 (A5, A6 DEFER-by-design); no Round-3 reviewer threads posted — next iter's NOW decision tree handles self-post-vs-await per Round-2 pattern.
