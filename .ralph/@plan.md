# Implementation Plan

## NOW

- [ ] `/bmad-code-review (args: "2")` fresh-context per § Story Lifecycle matrix row `sm-verified → done | fixes-pending`. Adversarial triage (Blind Hunter / Edge Case Hunter / Acceptance Auditor; pre-selects "Create action items"). Novel surfaces for CR attention: (α) 17-shim refactor coverage completeness — did every shim get the right tier + correct block-swap vs prepend-only posture (15 block-swap + 2 prepend-only verified at iter-250 post-dev SM); (β) 8 H2 sections in `docs/invariants/devbox-prereq-check.md` (Three-check contract / Exit-code schema / Tier contract / No-partial-bypass / Fresh-fork first-run behavior / Alpine probe image / Consumption / Extension); (γ) alpine-probe image pin (SC-4 + SC-7) + no-bypass posture adversarial scrutiny; (δ) collateral `INV-prek-prepare-lifecycle` contentHash bump discipline (single commit contains both `package.json` edit + manifest bump per iter-248 LESSON). Forecast 0–3 PATCH opener; FIFTH cumulative one-pass ZERO-PATCH CR precedent candidate (chain: Story 1.13 iter-85 → 2.7 iter-226 → 2.8 iter-233 → 2.9 iter-244 → 2.10 iter-251). Budget forecast: ~40-50K (orient + 3 parallel adversarial layers + PATCH drafting + Change Log v1.6 + IP + commit).

## QUEUE (Story 2.10..2.17 → Epic 2 close-out)

- [ ] _(post-Story-2.10-done)_ Iterate Stories 2.11..2.17 through full lifecycle — 7 remaining substrate stories spanning shared mode + port-publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-23 per iter-219..249 repeated pre-push confirmation — `gh pr view 230` shows `statusCheckRollup: []`).

## BLOCKED

- [ ] iter-250 push to `origin feat/epic-2-packaged-devbox` — REASON: SSH :22 + HTTPS :443 to github.com both timed out at iter-250 orient (identical to iter-249 symptoms). Remote is `git@github.com:tthew/ralph-bmad` (SSH).
  - Attempted: `gh pr view 230 --json …` failed with `Post "https://api.github.com/graphql": dial tcp 140.82.121.5:443: i/o timeout`; `ssh -T git@github.com` failed with `ssh: connect to host github.com port 22: Connection timed out`. Both probes confirm github.com unreachable from this iter env at 2026-04-23T22:55Z.
  - Error/Issue: transient network / firewall blocking outbound connections to github.com from this iter env. Three commits LOCALLY STAGED on `feat/epic-2-packaged-devbox` (3 ahead of upstream): `aefb382` (iter-249 push-fail BLOCKED entry), `d908d6f` (iter-249 trace-WAIVED), and the iter-250 post-dev SM commit; no force-push, no amend — preserved intact for next iter.
  - Next: next iteration's first action is the queued `/bmad-code-review` task; per § Execute step 0h check unpushed commits at orient → push at step 5 alongside iter-251 commit. If SSH/HTTPS continue to fail, operator may need to investigate iter-env egress to github.com or reconfigure remote to HTTPS; Ralph cannot autonomously reconfigure the remote URL (beyond single-iteration scope). PR #230 pre-push CI status assumed unchanged from iter-247 confirmation (`statusCheckRollup: []`; no CI to wait on).

## ATDD Red Phase

_(none — Story 2.10 ATDD-skip applied at iter-247; stub-docker smokes at iter-248 are ephemeral + torn down inside the same iter per iter-223/230/237 LESSON; no persisted red-phase artefacts.)_

## DONE (iter-250 Story 2.10 post-dev SM ZERO-PATCH — Story State `traced → sm-verified`; sprint-row `review` unchanged)

- [x] iter-250: **STORY 2.10 POST-DEV SM ZERO-PATCH ONE-PASS via `/bmad-create-story (args: "review")` fresh-context — FIFTH cumulative post-dev SM ZERO-PATCH precedent (chain: Stories 2.7/2.8/2.9 → 2.10).** Two-subagent substrate-citation verification pattern (iter-235 LESSON) applied. Subagent A (AC-satisfaction verifier — parallel) confirmed all 5 ACs VERIFIED via source-file read of `packages/devbox/scripts/prereq-check.sh` + 13 cross-referenced substrate files: AC 1 install-URL `https://docs.docker.com/desktop/install/` verbatim at prereq-check.sh:68 + Tier 1 exit 8 before any container op; AC 2 `Claude Code not authed — run 'pnpm claude' first` verbatim at prereq-check.sh:109 (em-dash U+2014); AC 3 `gh CLI not authed — run 'pnpm gh:auth' first` verbatim at prereq-check.sh:113; AC 4 exit 0 silent + ralph shims propagate rc; AC 5 Claude-before-gh aggregation order (line 108 precedes line 112) + no `--skip-claude`/`--force`/`KEEL_PREREQ_BYPASS` code paths. File-surface audit clean across `package.json` (entry between devbox:env:check and ralph:build), ralph-build/plan-host.sh Tier 2 wire-in at L54, 15 block-swap + 2 prepend-only shims (restart.sh + env-check.sh), `INV-devbox-prereq-check` 5-field manifest entry at manifest.ts:272-279, `docs/invariants/devbox-prereq-check.md` 8 H2 sections, `INVARIANTS.md:112` anchor regex-compliant, README H2 + AGENTS.md H3 sibling appends, no recursion in prereq-check.sh, benchmark.sh not wired (SC-11 out-of-scope preserved). Subagent B (substrate-citation drift detector — parallel) verified 15 of 16 citation triples + identified 1 distinct DRIFT: `docker-compose.yml:170-171` cited 2× in story file (§ References line 380 + SC-10 prose line 129) — named-volume `keel_home_dev: {}` declaration now at L257 (comment block L247-256); cap bullets NET_ADMIN/NET_RAW now at L170-171. Drift is **pre-declared deferred** per v1.1 PATCH + § Notes carry-forward (SC-17 Epic 2 close-out reconciliation policy applies to ALL § References line-number drifts accumulated during Epic 2 branch lifecycle). NOT a sm-fixes-pending item — explicit deferral policy. **1-drift-count below Story 2.9 iter-243's 13-drift baseline** — Story 2.10 prepend-only edits shifted only 2 citation occurrences, both in same docker-compose.yml cluster. Story file Status HTML comment flipped `traced → sm-verified` inline + Change Log v1.5 prepended. Sprint-status UNCHANGED per iter-202 precedent. RALPH.md signpost landed with no novel LESSON (mechanical inheritance from Story 2.9 iter-243 + iter-235 two-subagent pattern empirically validated on second story). Push BLOCKED extension from iter-249 (SSH + HTTPS both timed out at iter-250 orient; 3 commits unpushed). Budget iter-250: ~45K (orient ~10K + 2 parallel verification subagents ~12K + Change Log v1.5 prepend ~10K + IP + RALPH.md + commit ~13K).

_(iter-243..249 Story 2.9 closure + Story 2.10 lifecycle iters pruned per Guardrail 2 — see Story 2.10 § Change Log v1.0–v1.5 + RALPH.md Signposts for per-gate detail.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **9/17 stories done** (2.1-2.9) + 8/17 in-flight-or-backlog (2.10 `Story State = sm-verified` sprint-row `review` at iter-250; 2.11..2.17 still backlog).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered at iter-248 9/9 stub-docker harness (workspace-based, not tmpfs).
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:119). Host-side shim count: **18** at Story 2.10 landing (17 Story 2.9 landing + 1 new `prereq-check.sh` primitive — not a `<verb>-host.sh`).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **9 done** (2.1-2.9); 1 at sprint-row `review` Story State `sm-verified` (2.10); 7 backlog (2.11..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.10 — Prerequisite check (Docker runtime + Claude auth + gh auth) with pointer errors.
- **Story File:** `_bmad-output/implementation-artifacts/2-10-prerequisite-check-docker-runtime-claude-auth-gh-auth-with-pointer-errors.md`
- **Story State:** `sm-verified` — next iter action per § Story Lifecycle Decision Matrix row `sm-verified → done | fixes-pending` is `/bmad-code-review (args: "2")` fresh-context. ZERO-PATCH FIFTH cumulative one-pass CR-precedent candidate forecast (chain: Story 1.13 iter-85 → 2.7 iter-226 → 2.8 iter-233 → 2.9 iter-244 → 2.10 iter-251) if pattern holds under novel multi-shim-edit + new-INV + alpine-probe surface.
- **GitHub Issue:** Story 2.10 likely unwired (`RALPH_ISSUE_NUMBER` unset at iter-250 check; skip `Refs #<story-issue>` in commits). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2; iter-250 PR-state probe failed due to network timeout; assumed unchanged from iter-247 `statusCheckRollup: []` + `mergeStateStatus: CLEAN` + `isDraft: true`).

## Notes

- **iter-250 Story 2.10 post-dev SM ZERO-PATCH — mechanical inheritance from Story 2.9 iter-243 at ~95% (highest inheritance ratio among Stories 2.7/2.8/2.9 → 2.10 chain).** Two-subagent verification pattern (iter-235 LESSON) now empirically validated across two stories (2.9 iter-243 + 2.10 iter-250) on the Epic 2 branch. Subagent A AC-satisfaction verifier returned 5/5 VERIFIED with verbatim string + em-dash + Claude-before-gh + exit-code audit; Subagent B substrate-citation drift detector returned 15-VERIFIED + 1-DRIFT (pre-declared deferred per SC-17 Epic 2 close-out policy). Zero novel RALPH.md LESSON generated — pattern is now stable.
- **iter-249 Story 2.10 trace-WAIVED — mechanical template inheritance from Story 2.9 iter-242 at ~85%.** No novel LESSON. FR14n row `in-dev → traced` transition. 4 artefacts landed. Sprint-status unchanged per iter-202 precedent.
- **iter-248 Story 2.10 impl — mechanical inheritance from Stories 2.6/2.8/2.9; zero drift from v1.2 spec.** Two novel LESSONs in RALPH.md signposts: (a) collateral `INV-prek-prepare-lifecycle` contentHash bump discipline on root `package.json` edits; (b) stub-docker 9-smoke harness pattern generalises to any Epic-2 tier-dispatch primitive. Both forecast-actionable for next 7 stories on Epic 2 branch.
- **iter-244/246 audit-findings carry-forward to iter-251+ CR gate** (per v1.1 PATCH notes; unchanged from iter-247/248/249/250): (1) restart.sh transitive-delegate prepend-only pattern (documented inline); (2) benchmark.sh OUT-OF-SCOPE clarification (SC-11); (3) Manifest entry count drift (29→30 baseline); (4) Alpine egress non-applicability doctrine (SC-4 + § Alpine probe image).
- **iter-235 carry-forward — two-subagent verification pattern** empirically validated on second story (Story 2.10 iter-250) — pattern now graduates from "candidate" to "established" for post-dev SM gates on multi-substrate-touching stories.
- **iter-234 carry-forward — killed-before-commit skill recovery pattern** unchanged; valid recovery primitive.
- **iter-221 carry-forward — pre-dev SM `~5-PATCH` ceiling** held at iter-246 (4 PATCHes at low end of 2-5 forecast band). Forecast holds for Stories 2.11..2.17.
- **TTY-detect at docker-exec shim (iter-214 LESSON) — Story 2.10 NON-APPLICATION confirmed at iter-248:** prereq-check.sh does NOT `docker exec` — it `docker run --rm` (non-interactive, no TTY, short-lived); SC-11 wire-ins preserved each shim's TTY posture unchanged.
- **`<verb>-host.sh` naming convention DEVIATION (iter-199 EXTENDED through iter-250):** Story 2.10 shipped `prereq-check.sh` per Story 2.3/2.4 primitive pattern (shared primitive called by other shims, not a pnpm-verb wrapper). `pnpm devbox:prereq:check` standalone verb IS wired (Task 2).
- **Substrate-citation drift forecast for Stories 2.11..2.17:** Story 2.10's 1-drift outcome (vs Story 2.9's 13-drift baseline) suggests prepend-only edits shift fewer downstream lines than block-swap edits. Forecast: Stories 2.11..2.17 may individually accumulate 1-3 drifts each, all deferred to Story 2.17 Epic 2 close-out reconciliation per SC-17 policy. Total forecast at Epic 2 close-out: ~10-20 cumulative line-number drifts to reconcile in a single SC-17 polish pass.
