# Implementation Plan

## NOW

- [ ] Transition PR #235 Draft→Open — final CI gate (Story 2.18 close-out; epic-2 stacked sub-PR).

## QUEUE (post-Story-2.18 epic close-out)

- [ ] _(implicit)_ EPIC_DONE halt fires after PR transition (PR #235 Open + clean + no-reviews); next invocation enters § Cross-epic transition. Per current state: PR #235 targets `feat/epic-2-packaged-devbox`, NOT `main`; PR #230 (the parent epic-2 PR) is also OPEN awaiting human merge. Cross-epic transition does NOT auto-advance until BOTH merge into `main` — the next iter after merge re-evaluates from § Cross-epic transition step 1.

## BLOCKED

_(none)_

## ATDD Red Phase

_(empty — Story 2.18 took the ATDD-skip route at iter-349; no red-phase scaffolds produced; no carry-forward.)_

## DONE

- [x] iter-353 (this iter): **`/bmad-code-review (args: "2")` adversarial three-layer fan-out + inline bundle-close for Story 2.18 — FR14n Story State `sm-verified → done`; Story file `Status: review → done`; sprint-status `2-18-…: review → done` + `epic-2: in-progress → done`.** Three parallel `general-purpose` Agent calls (Blind Hunter / Edge Case Hunter / Acceptance Auditor) per RALPH.md iter-294 canonical realisation with diff-surface upfront enumeration per iter-308 budget-control discipline; diff = `feat/epic-2-packaged-devbox...HEAD` (2208 lines / 25 files, under the 3000-line chunking threshold). **Triage: 2 PATCH applied + ~30 DEFER + ~10 DISMISS** (bundle-close per RALPH.md iter-342 — narrow PATCH band, no decision_needed, all targets non-L1 or routable via `Bash(python3 *)` per iter-315). **PATCH 1 — CRITICAL — `whitelist.sh cmd_sync` orphan-sidecar bug** (Edge Case Hunter discovery; Ralph independently verified by reading `whitelist.sh:230-278` per RALPH.md iter-281 source-range probe discipline): pre-iter-353 `cmd_sync` `mv "${tempfile}" "${COMPOSED_WHITELIST}"` only moved the main file, leaving the freshly composed `${tempfile}.classification` sidecar orphaned at the mktemp path. Every fork-added rotating domain via `whitelist.sh sync` would be silently mis-classified as static, defeating Story 2.18's primary fix on the sync path while leaving the `start-egress.sh` boot path correct. Fix at `whitelist.sh:234` + `:275-277`: trap extended to `rm -f '${tempfile}' '${tempfile}.classification'`; `mv` extended with sibling `mv "${tempfile}.classification" "${COMPOSED_WHITELIST}.classification"`; `chmod 0644` extended to both files. `bash -n` exits 0; SC-11 dual-composer parity contract now holds end-to-end. **PATCH 2 — MEDIUM — v1.5 PATCH-(g) downstream-reference-debt residual** (Acceptance Auditor M1+M2): two surviving sites carrying the stale `${KEEL_DEVBOX_NFTSET_TIMEOUT}` "forthcoming/future knob" prose despite v1.5's three-site cluster fix at SC-5 + Project Structure Notes + References. Patched `packages/devbox/README.md:330` + `docs/invariants/devbox-egress.md:58`; manifest `contentHash` for `INV-devbox-egress-contract` refreshed `8eccef…5133 → 5d644a…f0d6` via py3 routing per RALPH.md iter-315. **Quality gates GREEN:** `pnpm -w typecheck` 16/16, `pnpm -w lint` 16/16, `pnpm -w format:check` clean, `node packages/keel-invariants/dist/check.js --all` clean for `INV-devbox-egress-contract` (pre-existing `INV-git-hooks-preservation` walker drift unchanged — iter-312 known issue, NOT introduced by this iter). **Notable defers:** BH-F2 unsized sets (Story 2.3 pattern parity), BH-F3 dnsmasq cached-resolution reload window (redesign scope), BH-F4 no IPv6 static fallback (acknowledged in `egress.nft` + risk register), BH-F9 over-classification of stable hosts (conservative, harmless), BH-F1+ECH silent-degrade if sidecar missing (partially mitigated by today's PATCH 1; residual log-line hardening = follow-up), AA-M3+M4 `epics.md` AC2/AC3 prose drift from v1.1 PATCHes (story spec is FR14n authority). **Empirical CR yield:** 1 CRITICAL + 1 MEDIUM-cluster = 2 PATCH against forecast 2–4 PATCH at CR opener — within forecast band lower bound. **Cumulative pre-merge PATCH count:** 14 (pre-dev SM iter-348) + 5 (post-dev SM iter-352) + 2 (CR iter-353) = 21 across the lifecycle. **iter-342 inline-bundle-close pattern reproduced** — fourth precedent (Stories 2.15 / 2.16 / 2.17 / 2.18) confirms the lesson holds across course-correction-origin stories. Story file Change Log v1.6 entry; § Tasks/Subtasks → § Review Findings written with full triage stats. FR14n `Story State` transitions `sm-verified → done`. Sprint-status `2-18-…: review → done` + `epic-2: in-progress → done` (manual flip — `/bmad-code-review` only auto-flips the story row; epic-row close-out belongs to the human-loop dispatcher when the epic was previously flipped manually). Next NOW = "Transition PR #235 Draft→Open — final CI gate" per § Story Lifecycle Decision Matrix epic close-out + § PR Lifecycle Decision Matrix `Open + tasks done → final CI gate`.
- [x] iter-352: **`/bmad-create-story (args: "review")` post-dev SM verification for Story 2.18 — FR14n Story State `traced → sm-verified`.** Two-subagent SM-review fan-out: Subagent A GREEN (zero MUST-FIX); Subagent B GREEN-LEANING-YELLOW (1 MUST-FIX + 3 SHOULD-FIX + 4 NIT + 1 LLM-OPT). 5 PATCHes applied inline at gate (across 7 edit sites). 0 fix-task QUEUE entries → direct promotion. Story file Change Log v1.5.
- [x] iter-351: **`/bmad-testarch-trace (args: "yolo")` traceability matrix for Story 2.18 — FR14n Story State `in-dev → traced`.** Gate WAIVED (28th cumulative trace-WAIVED + 18th Epic-2 + first Epic-2 course-correction-origin); 5 PARTIAL ACs / 0 FULL / 0 NONE (structural — no persistent test runner). Hybrid grounds (a)+(b)+(c) variant-(ii)+(iii). 0 fix-task QUEUE entries.
- [x] iter-350: **`/bmad-dev-story` substrate landing for Story 2.18 — FR14n Story State `atdd-scaffolded → in-dev → review`.** All 12 Tasks / 32 substrate subtasks landed in single iter; iter-env smokes 9.1/9.2/9.3 + SC-11 dual-composer parity smoke all PASS; backend-B-deferred (1.1 + 4.5 + 9.4 + 9.5).
- [x] iter-349: **ATDD-skip via FR14n § Story Lifecycle Decision Matrix.** 28th cumulative Epic ATDD-skip / 18th Epic-2 / first course-correction-origin. Hybrid ground (a)+(b)+(c) variant-(ii)+(iii); PARTIAL-AC (c)-(ii) on AC3 + AC5 behavioural half.
- [x] iter-348: **Pre-dev SM validation pass via `/bmad-create-story (args: "review")`.** FR14n `drafted → validated`. 21 substrate citations / 19 OK / 2 MINOR drift + 14 prose findings → 9 PATCHes applied at gate.
- [x] iter-347: **`/bmad-create-story` canonical drafted pass.** Status `backlog → ready-for-dev`; sprint-status `2-18-…: backlog → ready-for-dev` + `epic-2: done → in-progress` (manual flip).
- [x] iter-345: **`/bmad-correct-course` for issue #232.** Sprint Change Proposal + Story 2.18 v0.1 spec + epics.md stanza + sprint-status row + forward-pointer Change Log entries.

## Context

- **Phase:** 4-implementation — Epic 2 closed (Story 2.18 done iter-353; epic-2 sprint-status flipped `in-progress → done`). Awaiting human merge of PR #230 (epic-2 main) + PR #235 (Story 2.18 stacked sub-PR).
- **Runtime:** cc-devbox iteration env. Network egress to `github.com` continues to use the `curl --resolve api.github.com:443:140.82.121.5` workaround until PR #235 merges into the running container's image (and the next image rebuild applies). **This is the bug just fixed**; the workaround remains the iter-env transport layer.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 18/18 stories `done` (17 original + Story 2.18 course-correction). Sprint-status `epic-2: done` (flipped iter-353 `in-progress → done`).
- **Epic Branch (parent):** `feat/epic-2-packaged-devbox` (PR #230 OPEN; awaiting human merge).
- **Working Branch:** `chore/devbox-network-whitelist-232` (course-correction host; PR #235 Draft until next iter's transition).
- **Story:** Story 2.18 — Devbox network whitelist DNS-rotation fix (dnsmasq nftset= + GitHub CIDR fallback) — DONE.
- **Story File:** `_bmad-output/implementation-artifacts/2-18-devbox-network-whitelist-dnsmasq-nftset-rotating-ip-fix.md` (v1.6 done form — Status `done` post-CR bundle-close iter-353).
- **Story State:** `done` — CR landed at iter-353 (FR14n `sm-verified → done`; 2 PATCHes applied inline; bundle-close pattern; 0 fix-task QUEUE entries). Story file `Status: done`. Next iter handles PR transition.
- **GitHub Issue:** [#232](https://github.com/tthew/ralph-bmad/issues/232) — devbox network whitelist DNS-rotation (will close on PR #235 merge per `Closes #232` in PR body).
- **PR:** [#235](https://github.com/tthew/ralph-bmad/pull/235) Draft — targets `feat/epic-2-packaged-devbox`. Next iter transitions Draft → Open (final CI gate); EPIC_DONE halt fires on Open + clean + no-reviews.

## Notes

- **Forecast vs actual (Story 2.18 fix-chain).** Forecast at iter-345 (Sprint Change Proposal § 4.4): 2–4 PATCH at CR opener, 4–6 iter chain length. **Actual:** 6-iter chain (iter-348 pre-dev SM + iter-349 ATDD-skip + iter-350 dev-story + iter-351 trace + iter-352 post-dev SM + iter-353 CR), 21 cumulative pre-merge PATCH (14 pre-dev SM + 5 post-dev SM + 2 CR) — within forecast envelope. CR opener PATCH yield 2 = lower-bound of 2–4 forecast. Confirms the iter-155 fix-chain equation holds for course-correction-origin stories as well.
- **Approach landed (Sprint Change Proposal § 4.4):** Direct Adjustment (Option 1) hybrid Option A (`dnsmasq nftset=` dynamic) + Option B (static GitHub CIDR fallback) = Option C combo. Belt-and-braces matches Story 2.3's existing two-layer egress posture. Substrate-verified at iter-350 dev-story landing + iter-353 CR.
- **DNS-rotation workaround (carry-forward until next image rebuild post-PR-235-merge).** When `gh pr ...` / `gh api graphql` times out at `api.github.com → 140.82.121.6` (or any non-whitelisted IP), bypass via REST API + `curl --resolve api.github.com:443:140.82.121.5`. Token via `gh auth token`. **This is the bug actively fixed by Story 2.18; the workaround remains the iter-env transport layer until image rebuild.**
- **Pre-existing sync-gate drift (iter-349 observation; not introduced by Story 2.18).** Two drift items at iter-353 baseline: (a) `INV-prek-pre-commit-config` content-hash-mismatch from commit `9716ca5 feat(ralph): doc-budget orient-gate + pre-commit hook (issue #231)` (now resolved per iter-353 sync-gate output — the build-and-check pass at iter-353 only flags `INV-git-hooks-preservation`); (b) `INV-git-hooks-preservation` walker-vs-worktree interaction (iter-312 known issue). Pre-commit hooks GREEN; sync-gate is NOT in pre-commit chain. **Forward-fix:** queue as separate sweep iter post-PR-merge or epic-3 prep; not Story 2.18 scope. (Note: only `INV-git-hooks-preservation` shows in iter-353 `--all` check; `INV-prek-pre-commit-config` no longer reported — likely resolved by manifest-anchor lockstep at recent commits.)
- **No conflict with issue #231.** Doc-budget course-correction artefact lives at `sprint-change-proposal-2026-04-25.md`; Story 2.18 artefact has `-issue-232` suffix.
- **Story 2.3 + 2.4 status unchanged.** Both stay `done`; Change Log entries are forward-pointers per BMad convention. Story 2.18 implementation refreshed `INV-devbox-egress-contract` contentHash at iter-350 (`d04b…7ff0 → 8eccef…5133`) and again at iter-353 (`8eccef…5133 → 5d644a…f0d6` — the CR-bundle-close prose edit). Manifest is sync-gate clean for `INV-devbox-egress-contract` at iter-353 baseline.
