# Implementation Plan

## NOW

- [ ] `Run /bmad-testarch-trace (args: "yolo")` fresh-context for Story 2.11 — AC→test coverage gate + traceability matrix. Per § Story Lifecycle Decision Matrix row `in-dev → traced|trace-fixes-pending`. Forecast: **WAIVED** per 22nd cumulative ATDD-skip-trace-WAIVED co-application pairing (chain now 22 stories; grounds (a)+(b)+(c) per iter-256 ATDD-skip signpost): iter-248 stub-docker smoke pattern applies to resolver + compose-config + env-check stderr-grep smokes at impl time (landed at iter-257); ground-(c) doc-only variant for AC 4 concurrency decision; no test runner wired at 1.0 substrate to exercise the AC→test matrix mechanically. Alternative partial-waive is possible (ACs 1-3 smoke-covered in iter-257 dev-story Debug Log, AC 4 doc-only) — let the trace skill decide.

## QUEUE (Story 2.11..2.17 → Epic 2 close-out)

- [ ] _(after Story 2.11 `traced`)_ `Run /bmad-create-story (args: "review")` post-dev SM verification. Story State `traced → sm-verified` OR `sm-fixes-pending`. Two-subagent verification pattern per iter-235 LESSON (AC-satisfaction verifier + substrate-citation drift detector in parallel). Forecast: 1-3 drift findings per iter-250 carry-forward (deferred per SC-17 Epic 2 close-out reconciliation).
- [ ] _(after Story 2.11 `sm-verified`)_ `Run /bmad-code-review (args: "2")` adversarial CR via three-layer Ralph-hosted triage (Blind Hunter `bmad-agent-architect` diff-only + Edge Case Hunter `bmad-tea` diff+project-read + Acceptance Auditor `bmad-agent-dev` diff+spec+INV). Story State `sm-verified → done` OR `fixes-pending`. Chain-reset forecast: Stories 2.11..2.17 begin fresh ZERO-PATCH-CR forecast cycle per iter-253 LESSON (0-1 PATCH + 2-4 second-class DEFERs band-aligned). Story 2.11's novel surface = resolver-function extension + compose parameterisation + env-check orphan warning = moderate; forecast 1 first-class PATCH + 2-3 second-class DEFERs.
- [ ] _(after Story 2.11 `done`)_ Iterate Stories 2.12..2.17 through full lifecycle — 6 remaining substrate stories spanning loopback-bound port publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-24 per iter-219..257 repeated pre-push confirmation — `gh pr view 230` returns `statusCheckRollup: []`).

## BLOCKED

- [ ] iter-257 step-5 push EXPECTED to fail — SSH :22 to github.com still timing out per iter-256 BLOCKED carry-forward (probed at iter-257 orient via `git ls-remote origin HEAD` with 15s timeout → RC=124). 3 unpushed commits locally (iter-256 × 2 + iter-257-dev-story-landing). Commits flush together when upstream recovers per iter-249..256 recovery-banking pattern.
  - Attempted: `git ls-remote origin HEAD` at iter-257 orient — 15s timeout; step-5 `git push` will either succeed if the window has opened OR re-fail with SSH :22 timeout.
  - Error/Issue: SSH :22 egress to github.com `140.82.121.{3,4,5,6}:22` intermittently timed out across iter-219..257; iter-255 saw a brief recovery window; iter-256 + iter-257 re-blocked. HTTPS :443 correlates (iter-256 `gh pr checks 230` also timed out). Suggests upstream network-path issue from iter-env to github.com ASN rather than port-specific egress block. PR #230 has no CI configured so HTTPS timeout is not a CI gate.
  - Next: next iter retries SSH :22 push on orient per the iter-249..255 carry-forward recovery-banking pattern. Commits are safe in local branch — no force-push, no amend, preserved intact. When SSH :22 recovers, `git push` flushes all unpushed commits in a single round-trip with no conflict.

## ATDD Red Phase

_(none — Story 2.11 ATDD-skipped at iter-256 per grounds (c)+(ii)+(iii). Dev-agent impl at iter-257 landed resolver smokes + bash syntax-checks + `pnpm keel-invariants:check` pass as the functional substitute; these are logged in Story 2.11 Dev Agent Record § Debug Log References. Trace gate will WAIVE per 22nd-cumulative ATDD-skip-trace-WAIVED pairing at `/bmad-testarch-trace` next.)_

## DONE (iter-257 Story 2.11 `/bmad-dev-story` — Story State `atdd-scaffolded → in-dev`)

- [x] iter-257: **STORY 2.11 `/bmad-dev-story` LANDING — all 6 Tasks complete; Status `ready-for-dev → review`; sprint-row flipped twice (`ready-for-dev → in-progress` at Step 4; `in-progress → review` at Step 9).** Twenty-seven files touched: (1) `packages/devbox/scripts/lib/main-repo-resolver.sh` — added `resolve_mode_specific_state()` function (62 LOC net-new + header doc-block update); (2) `packages/devbox/docker-compose.yml` — top-level `name:` parameterised to `${KEEL_DEVBOX_COMPOSE_PROJECT:-keel-devbox}` + comment-block updates + TODO marker removed; (3-20) 18 host-side shims wired with resolver + `KEEL_DEVBOX_CONTAINER_NAME_RESOLVED` (8 already-sourcing APPEND, 10 non-sourcing NEW block); (21) `packages/keel-invariants/src/invariants.manifest.ts` — `INV-devbox-mode` registered (manifest count 29 → 30); (22) `docs/invariants/devbox-mode.md` — new authoritative contract doc (contentHash `4ddc4eea3a3f28cde90a1c7944f14d52a18aa1a0a214d9a45050aea1ec313cf2`); (23) `INVARIANTS.md` — `### Devbox mode (Story 2.11)` H3 + anchor bullet; (24) `packages/devbox/README.md` — `## Per-fork vs shared devbox mode (Story 2.11)` H2 with three walkthroughs + both verbatim orphan-warning renderings; (25) `AGENTS.md` — `### Per-fork vs shared devbox mode (Story 2.11)` H3 with agent guardrails; (26) `packages/devbox/.envrc.example` — SC-15 past-tense comment; (27) `_bmad-output/implementation-artifacts/sprint-status.yaml` — row flipped twice + `last_updated` bumps. Story file Change Log v1.2 prepended (~130-line entry) + Dev Agent Record populated (Agent Model Used / Debug Log References / Completion Notes with 4-AC + 17-SC checklist / full File List). **ZERO impl-time PATCHes** — pre-dev SM review at iter-255 (v1.1 CRITICAL + 6 ENHANCEMENTs absorbed) produced sufficiently specified story file that dev-story landed cleanly on first pass. Resolver functional smokes 6/6 green including SC-4 shared+override-IGNORED verification. Bash `-n` syntax-check clean on all 18 shims. `pnpm keel-invariants:check` PASSES (after `pnpm --filter @keel/keel-invariants build` rebuild — first check fired `removed-from-docs-only INV-devbox-mode` drift against stale `dist/check.js`). Live `docker compose config` + two-forks-attach + AC 3 `docker inspect` orphan probe deferred to operator workstation per Story 2.5 iter-186 substrate-smoke posture (subagent env has no `docker` on PATH). Budget consumed iter-257: ~75K tokens (orient ~12K + story file read + 6-task impl ~50K including sync-gate rebuild + 27 file edits + resolver smokes + Change Log v1.2 authoring + RALPH.md signpost + commit ~13K). Well inside ~117K execution budget. **Carry-forward novel LESSONs** to RALPH.md § Lessons: (a) manifest rebuild MUST precede sync-gate verification after adding a new InvariantSchema entry — always run `pnpm --filter @keel/keel-invariants build` between manifest edit and `pnpm keel-invariants:check`; (b) shared-mode operator-override INTENTIONAL ignore (SC-4) is load-bearing — smoke test any future mode-opinionated shim to verify operator environment overrides are correctly discarded.

_(iter-253..256 Story 2.10 closure + Story 2.11 create-story / pre-dev SM / ATDD-skip iters pruned per Guardrail 2 — see Story 2.11 § Change Log v1.0/v1.1/v1.2 + RALPH.md Signposts iter-253..256 for per-gate detail.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **10/17 stories done** (2.1-2.10) + 1/17 in review (2.11) + 6/17 backlog (2.12..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered at iter-248 9/9 stub-docker harness + iter-252 9/9 mock-docker PATH-shim harness + iter-257 6/6 resolver functional smokes.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:119). Host-side shim count: **18** at iter-257 (unchanged — Story 2.11 extends the 18 existing shims with resolver invocation; does NOT add new shims).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **10 done** (2.1-2.10); 1 in-review (2.11); 6 backlog (2.12..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.11 — Per-fork vs shared devbox mode (`KEEL_DEVBOX_SHARED`)
- **Story File:** `_bmad-output/implementation-artifacts/2-11-per-fork-vs-shared-devbox-mode-keel-devbox-shared.md`
- **Story State:** `in-dev` — Story 2.11 `/bmad-dev-story` landed at iter-257; Status `ready-for-dev → review`; sprint-row `ready-for-dev → in-progress → review`. Next iter `/bmad-testarch-trace` transitions `in-dev → traced` (or `trace-fixes-pending`).
- **GitHub Issue:** Story 2.11 likely unwired (`RALPH_ISSUE_NUMBER` unset at iter-257 orient; skipping `Refs #<story-issue>` in commits per § Issue Tracking guard). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out, NOT iter-257).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — last confirmed `{"isDraft":true,"mergeStateStatus":"CLEAN","state":"OPEN","statusCheckRollup":[]}` at iter-254 orient; PR state unchanged across iter-255..257 (no merge events expected pre-Epic-2-close). iter-257 push status: SSH :22 still intermittent (probed at orient with `git ls-remote origin HEAD` 15s timeout → RC=124); `gh pr checks 230` confirms no CI configured (`statusCheckRollup: []`) so HTTPS-timeout is not a CI gate.

## Notes

- **iter-257 Story 2.11 `/bmad-dev-story` LANDING — `in-dev` after single-iter clean-pass landing.** ZERO impl-time PATCHes; pre-dev SM at iter-255 produced sufficiently specified story file (v1.1 CRITICAL + 6 ENHANCEMENTs absorbed) that dev-story landed cleanly on first pass. Pattern matches Story 2.10 iter-248 clean-pass landing (after iter-246 v1.1 PATCH-dense pre-dev SM) — the dense-pre-dev-SM → mechanical-impl → clean-post-dev-SM cascade holds for Story 2.11.
- **iter-257 NOVEL LESSON — manifest rebuild required after adding new InvariantSchema entry.** `pnpm keel-invariants:check` runs against compiled `dist/check.js`; after editing `src/invariants.manifest.ts` MUST invoke `pnpm --filter @keel/keel-invariants build` before sync-gate reflects the change. First check run returned `removed-from-docs-only INV-devbox-mode` drift against stale dist; rebuild → clean (exit 0). Pattern carry-forward for Stories 2.12..2.17 + any future story adding a manifest entry: ALWAYS rebuild before verifying sync-gate.
- **iter-257 NOVEL LESSON — shared-mode operator-override INTENTIONAL ignore (SC-4) is load-bearing.** Tested path: `KEEL_DEVBOX_SHARED=true KEEL_DEVBOX_CONTAINER_NAME=my-fork-devbox` + resolver call → `KEEL_DEVBOX_CONTAINER_NAME_RESOLVED=keel-devbox-shared` (override DISCARDED). Without this enforcement, AC 2's "fork A + fork B attach to SAME container" fails silently when operator A has customised the per-fork container name. Functional smoke verified pre-commit. Carry-forward: any future mode-opinionated shim (Stories 2.12..2.17 that HARDCODE a mode-specific value regardless of operator env) must smoke-test operator-override-IGNORED paths.
- **iter-255 Story 2.11 pre-dev review gate — "substrate-truth corrections" as the dominant review-finding class for Stories N≥11** (carry-forward). Pattern: every enumeration claim (shim list, recompute inventory, anchor format, Docker primitive behaviour) MUST be grep/ls/doc-verified at create-story OR pre-dev-review time. Forecast for Stories 2.12..2.17: 1-3 CRITICAL "inventory correction" findings per pre-dev review; ENHANCEMENT class reduces as writing pattern stabilises.
- **iter-253 Story 2.10 CR re-run closure — SECOND-class operator-edge DEFER pattern** (carry-forward). PATCH iterations close FIRST-class adversarial findings but commonly surface SECOND-class operator-edges in the same surface the prior bug had hidden. Story 2.11's resolver-function + compose parameterisation + env-check orphan warning constitute "multi-site novel runtime probe" surface; expect 2-4 second-class DEFERs at CR re-run (Docker Desktop wake-race, Podman-compat semantics, compound-failure short-circuit doctrine carry-forward forecast classes).
- **iter-252 Story 2.10 PATCH-1 — `set -e`+`if cmd; then` rc-suppression LESSON applied at iter-257.** Task 4 env-check orphan-container probe uses canonical `rc=0; docker inspect … || rc=$?; case "${rc}" in 0) emit warning ;; 1) : ;; *) : ;; esac` pattern. No rc-suppression hazard at landing time.
- **iter-252 GOTCHA — iter-env `/tmp` is `noexec`** (carry-forward, applies to Story 2.13 healthcheck emitter + Story 2.14 legacy-branch cleanup; NOT applicable to Story 2.11 which does not use mock-binary PATH shims).
- **iter-244/246 audit-findings carry-forward** unchanged from iter-247..256: (1) restart.sh transitive-delegate prepend-only (applied at iter-257: restart.sh invokes resolver for fail-fast inspect consistency even though stop.sh + start.sh each re-run it downstream); (2) benchmark.sh OUT-OF-SCOPE clarification (SC-11); (3) Manifest entry count drift (29 → **30** at iter-257 after `INV-devbox-mode` landing); (4) Alpine egress non-applicability doctrine. Manifest count will not move again until next story adds a new invariant.
- **Substrate-citation drift forecast for Stories 2.11..2.17:** Story 2.11 at iter-257 touched `docker-compose.yml:43` (`name:` parameterisation) + comment-block amendments at L43-50 + workspace-mount doc-block L103-107 rewritten past-tense + TODO marker L239 removed. Line-number drift forecast unchanged at iter-257: post-iter-257 HEAD of `docker-compose.yml` has shifted line numbers at the compose `name:` block + workspace-mount comments; story-file § References L408-413 cite original line numbers (pre-edit); drift pre-declared deferred to Story 2.17 SC-17 polish pass. Total Epic 2 close-out reconciliation forecast: ~15-25 cumulative line-number drifts (updated from ~10-20 at iter-256).
