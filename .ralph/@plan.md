# Implementation Plan

## NOW

- [ ] `/bmad-create-story` — create Story 2.10 (prerequisite-check docker-runtime + Claude-auth + gh-auth with pointer errors) per § Story Lifecycle Decision Matrix row `done → _(no story)_` intra-epic auto-advance. Fresh context window. Story 2.9 closed iter-244 CR ZERO-PATCH; Epic 2 auto-advances within-epic (PR #230 stays Draft — Epic 2 still has 7 backlog stories after 2.10). Forecast: draft density similar to Story 2.9 baseline (4 ACs + 4-6 tasks + 15-17 SCs); novel surface is the prerequisite-check primitive (new `pnpm devbox:env:check`-adjacent verb OR extension to existing `env:check.sh`) plus pointer-error messages for 3 distinct failure modes (docker runtime down → exit 8; claude token missing → queue `pnpm claude`; gh token missing → queue `pnpm gh:auth`). Budget forecast: ~70K (orient + skill dispatch + commit).

## QUEUE (Stories 2.10..2.17 → Epic 2 close-out)

- [ ] _(post-Story-2.10-draft)_ Iterate Story 2.10 through full lifecycle: drafted → validated (pre-dev SM review) → atdd-scaffolded (FR14n ATDD-skip forecast under ground-(a) substrate-verification for prereq-check primitive) → in-dev (impl landed) → traced (trace WAIVED forecast — 20th cumulative precedent candidate) → sm-verified (post-dev SM) → done (CR closure).
- [ ] _(post-Story-2.10-done)_ Iterate Stories 2.11..2.17 through full lifecycle — 7 remaining substrate stories spanning shared mode + port-publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-23 per iter-219..244 repeated pre-push confirmation — `gh pr view 230` shows `statusCheckRollup: []`).

## BLOCKED

_(none)_

## ATDD Red Phase

_(none — Story 2.9 closed done at iter-244 with FR14n ATDD-skip applied at iter-236; NINETEENTH cumulative precedent under inherited ground-(c) "external-service-owns-behavior-under-test" variant.)_

## DONE (iter-244 Story 2.9 CR CLOSURE ZERO-PATCH ONE-PASS — Story State `sm-verified → done`; Change Log v1.6; FOURTH cumulative one-pass ZERO-PATCH precedent)

- [x] iter-244: **STORY 2.9 CR CLOSURE ZERO-PATCH ONE-PASS via `/bmad-code-review (args: "2")` fresh-context — Story State `sm-verified → done`; sprint-status `2-9-gh-cli-oauth-via-pnpm-gh-auth: review → done`. FOURTH cumulative one-pass ZERO-PATCH CR closure precedent achieved** (chain: Story 1.13 iter-85 → Story 2.7 iter-226 → Story 2.8 iter-233 → Story 2.9 iter-244). **Three-layer Ralph-hosted parallel adversarial fan-out** against iter-237 impl commit `fdbd5a2` (222-line diff; 4 files). **Blind Hunter** (`bmad-review-adversarial-general` prompt via `general-purpose` subagent, diff-only no project context): 22 raw findings. **Edge Case Hunter** (`bmad-review-edge-case-hunter` prompt via `general-purpose` subagent with repo read access): 15 JSON-format unhandled-path findings. **Acceptance Auditor** (diff + spec file + substrate cross-refs via `general-purpose` subagent): PASS — 0 violations ("All ACs and SCs verified satisfied by the diff"). **Triage: 0 PATCH / 11 DEFER / 19 DISMISS** after dedupe + SC cross-check. **11 DEFER all substrate-wide pre-existing patterns** (AR-60..AR-70) inherited from shell.sh + attach.sh + claude-host.sh carrying to post-Epic-2 `_lib.sh` refactor (SC-14 chain): TOCTOU race (inspect+exec); `|| true` masking docker-inspect non-ENOENT failures; CONTAINER_NAME validation gap; SIGPIPE-forward comment imprecision; github.txt 7-entry enumeration drift without sync-gate extension; OAuth device-code log-leak risk via `tee`; no IFS reset; no `docker info` timeout; ambiguous container name in multi-compose; paused/restarting/removing state misdiagnosis; "mirrors claude-host.sh verbatim" without drift gate. **19 DISMISS** breakdown: on-spec per SCs 5/6/8/10/11/14/15/16 (TTY-gate per SC-6, exit-2 "unused at 1.0" per SC-5, --help consumed by pnpm per SC-16, no-cleanup-trap per SC-10, gh/dev-user contract per Story 2.1+2.5 substrate, -w /workspace substrate pattern, upstream-CLI-owned token lifecycle per SC-15, upstream gh-owns-arg-validation per SC-5); upstream-CLI-owned (concurrent OAuth writes; argv validation); shell/argv correct (empty-string via `:-` fallback, newline/null-byte argv preservation, --scopes quoting); prose-nit (17-verb math phrasing with correct total; re-run overwrite semantic simplification; docker-info `--format` rationale inheritance); self-acknowledged N/A (log `$*` vs `$@`); project-convention (pnpm on PATH); spec-file existence verified (4 ACs + 17 SCs confirmed). **Story file v1.6 Change Log entry prepended** above v1.5 (reverse-chronological per precedent). **`### Review Findings` subsection appended** to Tasks/Subtasks in story file (11 `[x] [Review][Defer]` bullets with source attribution + carry-to pointers). **deferred-work.md append:** new `## Deferred from: code review of story-2.9 (2026-04-23)` section with 11 bullets (AR-60..AR-70) each with _Source:_ / _Location:_ / _Nature:_ / _Defer reason:_ / _Carry-to:_ blocks echoing the Review Findings content but with AR-numbering for cross-story deferred-work indexing continuity (AR-52 Story-2.8 → AR-60 Story-2.9 — gap reserved for iter-238..241 retrofits that did NOT surface deferred work). **Sprint-status.yaml flipped** `2-9-gh-cli-oauth-via-pnpm-gh-auth: review → done`; last_updated comment trail + live field updated to `Story-2-9-done-CR-closure-ZERO-PATCH-one-pass-iter-244`. **RALPH.md § Lessons append (iter-244):** pattern-strength LESSON — dense-pre-dev-SM-PATCH-density → verbatim-spec-template-impl → zero-drift-trace-WAIVED → ZERO-PATCH-post-dev-SM → ONE-PASS-ZERO-PATCH-CR now has 4 successive confirmations (Story 1.13 / 2.7 / 2.8 / 2.9); forecast band for Stories 2.10..2.17 tightens to "if pre-dev SM lands ≥2 PATCHes against spec-template-compliant drafts, one-pass ZERO-PATCH CR closure is the base-rate expectation rather than the exceptional outcome." **Pattern reconfirmation only** — no novel lesson surfaced; full mechanical inheritance from Story 2.8's precedent held through all 7 Story 2.9 lifecycle gates. **Next iter per § Story Lifecycle matrix row `done → _(no story)_` intra-epic auto-advance:** `/bmad-create-story` for Story 2.10 (prerequisite-check docker-runtime + Claude-auth + gh-auth with pointer errors). Epic 2 still has 7 backlog stories after Story 2.10 draft; PR #230 stays Draft until Story 2.17 Epic 2 close-out triggers Draft→Open transition. **No halt** — auto-advance continues per § Cross-epic transition within-epic route (sprint-status has `2-10-...: backlog` + 6 more rows; PR recorded as #230 Draft; no `EPIC_DONE` fire until Epic 2 final story 2.17 closes). Budget iter-244: ~105K (orient + 3-layer parallel-subagent dispatch + triage + deferred-work.md append + v1.6 Change Log prepend + Review Findings subsection + sprint-status flip + IP update + RALPH.md lesson append + commit + push).

_(iter-234..243 Story 2.9 lifecycle gates pruned per Guardrail 2 — see Story 2.9 file § Change Log v1.0/1.1/1.2/1.3/1.4/1.5 for per-gate details; iter-244 supersedes as the closure marker.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **9/17 stories done** (2.1-2.9) + 8/17 backlog (2.10..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live OAuth AC 1-4 smokes operator-workstation-deferred per ground-(c) carry-forward. Post-iter-237 infrastructure retrofits (iter-238..241) landed path-mirroring + ralph-TUI + whitelist + build fixes on same branch; all orthogonal to Story 2.9 AC verification envelope per iter-243 post-dev SM subagent-A verdict + iter-244 CR Auditor verdict.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:119). Host-side shim count: **17 verbs** at Story 2.9 closure (unchanged through CR gate).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **9 done** (2.1-2.9); 8 backlog (2.10..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** _(no story — Story 2.9 done; next iter creates Story 2.10)_
- **Story File:** _(n/a — next iter's `/bmad-create-story` produces `_bmad-output/implementation-artifacts/2-10-prerequisite-check-docker-runtime-claude-auth-gh-auth-with-pointer-errors.md`)_
- **Story State:** `_(no story)_` — next iter action per § Story Lifecycle Decision Matrix row `_(no story) → drafted` is `/bmad-create-story` fresh-context window; skill auto-marks sprint-status `2-10-...: backlog → ready-for-dev` + Epic 2 stays `in-progress`.
- **GitHub Issue:** Story 2.9 unwired at iter-234 (`RALPH_ISSUE_NUMBER` unset; skip `Refs #<story-issue>` in commits). Story 2.10 likely also unwired pending GH Project configuration. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2; `statusCheckRollup: []` at iter-242..244 pre-push confirmation; CLEAN merge-state).

## Notes

- **iter-244 CR closure pattern confirmation — FOURTH cumulative one-pass ZERO-PATCH precedent.** Chain: Story 1.13 iter-85 → Story 2.7 iter-226 → Story 2.8 iter-233 → Story 2.9 iter-244. Causal chain crystallized across 4 successive stories: dense pre-dev SM PATCH density (≥2 per Story 2.9 baseline OR ≥3 per iter-233 LESSON forecast) → verbatim-spec-template impl → zero-drift trace-WAIVED → ZERO-PATCH post-dev SM → ONE-PASS ZERO-PATCH CR. Forecast band tightens for Stories 2.10..2.17: if pre-dev SM lands ≥2 PATCHes against spec-template-compliant drafts, one-pass ZERO-PATCH CR closure becomes base-rate expectation rather than exceptional outcome. Pattern-strength LESSON recorded in RALPH.md § Lessons iter-244 entry; no novel mechanism surfaced (pure reconfirmation).
- **iter-244 CR auditor on-spec verdict is the 4th consecutive clean auditor outcome.** Story 1.13 iter-85 / Story 2.7 iter-226 / Story 2.8 iter-233 / Story 2.9 iter-244 all returned zero Auditor AC violations. The pattern suggests the spec-template-verbatim-impl discipline (Ralph guardrail implicit via `/bmad-dev-story`'s "do NOT modify" posture + Story 2.9 SC-17 explicit "MIRRORS claude-host.sh verbatim") is now structurally enforced — the dev-story skill at iter-230/237/etc. produces artefacts that cannot drift because the spec IS the implementation template. This has downstream implications for the pre-dev SM PATCH density forecast: if spec is template + spec is verbatim-impl, then pre-dev SM quality determines CR outcome entirely.
- **iter-243 carry-forward — post-dev SM substrate-citation drift pattern now has Story 2.9 closure evidence.** The iter-235 LESSON's prospective framing ("drift can be introduced by implementation edits to substrate files between SM gates") validated at iter-243 (13 drifts surfaced + deferred per SC-17 polish convention) + reconfirmed at iter-244 CR (Auditor verified content resolves at all cited locations despite line-number drift). Forecast: Stories 2.10..2.17 will each surface comparable drifts at post-dev SM as iter-238..241 retrofits accumulate more substrate changes on the branch; resolution policy is uniform — defer line-number reconciliation to Story 2.17 Epic 2 close-out per SC-17.
- **iter-244 CR triage yielded 11 deferred items (AR-60..AR-70) all pointing at post-Epic-2 `_lib.sh` refactor.** The shared-helper-extraction amortization-point is materializing — Story 2.9 post-iter-239 retrofit already extracted `lib/main-repo-resolver.sh` + `lib/check-mount-source.sh` organically under runtime-bug pressure (first evidence of the refactor beginning without an explicit "refactor" story). Forecast: Story 2.17 Epic 2 close-out may need to include a `_lib.sh` consolidation task to collapse the duplicated pre-flight boilerplate across all 17 host-side shims before Epic 3 adds further shims on top. Alternatively, a post-Epic-2 dedicated story (candidate Epic-2.x or Epic-13.x scope) can ship the refactor as a standalone unit. Decision TBD at Story 2.17 close-out or Epic 2 retrospective.
- **iter-235 carry-forward — two-subagent verification pattern is the reference implementation for post-dev SM substrate-citation audits.** Source-file claims + planning-artefact claims in parallel via `Explore` subagents returning VERIFIED / DRIFTED / MISSING verdicts. Applied at iter-243 post-dev SM with full effect (14 VERIFIED + 13 DRIFTED surfaced). Story 2.9's FOURTH ZERO-PATCH precedent suggests the pattern is now substrate-stable; Stories 2.10+ SHOULD re-apply verbatim.
- **iter-234 carry-forward — killed-before-commit skill recovery: inspect disk state before re-invoking.** Unchanged from iter-243 carry-forward; remains a valid recovery primitive for fresh-context skill invocations whose primary output is committed artefacts.
- **iter-233 carry-forward — one-pass ZERO-PATCH CR causal chain now has 4 successive confirmations.** Chain crystallized as a reproducible pattern — pre-dev SM PATCH density + verbatim spec-template impl + ATDD-skip + trace-WAIVED + zero-PATCH post-dev SM → reliable one-pass ZERO-PATCH CR closure. Story 2.9 retroactively confirms Story 2.8's iter-233 prospective forecast. Stories 2.10..2.17 forecast assumes continued one-pass closures unless novel-substrate-interaction surfaces surprise drifts.
- **iter-221 carry-forward — pre-dev SM `~5-PATCH` ceiling is independent of draft-SC density.** Story 2.9 landed at low-end 2 PATCHes (iter-235) vs forecast 3-7 band; the narrower novel surface drove the lower count. Pattern types (a/b/c/d from iter-221) still hold: drifting H-levels/line numbers, template-vs-schema fidelity, implicit read-only assumptions, Dev Notes gotchas.
- **TTY-detect at docker-exec shim (iter-214 LESSON) — Story 2.9 CONFIRMED NON-APPLICATION across all 7 lifecycle gates:** SC-6 explicitly pins hardcoded `-it` ("OAuth flow IS the AC"). Same deferral posture as Story 2.7's `docker attach` (SC-8) + Story 2.8 SC-6. Pattern held iter-234..244 uniformly.
- **`<verb>-host.sh` host-side shim naming convention (iter-199 EXTENDED through iter-244):** Story 2.9 `gh-auth-host.sh` ships substrate-quality under `packages/devbox/scripts/`, holding host-side shim count at 17 through CR gate closure. Remaining Epic 2 candidate: Story 2.12 (`ssh-host.sh` for loopback-bound port publication if the story adopts the same pattern). Forecast: Story 2.17 landing → 17+ total shims → post-Epic-2 `_lib.sh` refactor threshold triggered (iter-239 already extracted `lib/main-repo-resolver.sh` + `lib/check-mount-source.sh` shared helpers — first evidence of the refactor beginning organically under runtime-bug pressure; AR-60..AR-70 defer-stack extends the trajectory).
