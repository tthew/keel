# Implementation Plan

## NOW

- [ ] **Run `/bmad-testarch-atdd`** for Story 2.5 per § Story Lifecycle `validated → atdd-scaffolded`. Fresh context; the ATDD skill assesses Story 2.5's 5 ACs + 12 Tasks and produces either red-phase test scaffolds OR a documented skip rationale. **Expected outcome: skip** — Epic-2 precedent at Stories 2.1..2.4 all ATDD-skipped (ACs are compose-shape + kernel-enforcement smokes with no testable unit logic); Story 2.5's ACs (`docker inspect` cap_drop/cap_add/security_opt/tmpfs/volumes + kernel `/tmp` noexec + no-new-privileges) are the same class. If scaffolded, red-phase failures land in IP § ATDD Red Phase and do not block `atdd-scaffolded → in-dev`. ~small.

## QUEUE (Story 2.5 lifecycle → Epic 2 Story 2.6..2.17 → Epic 2 close-out)

- [ ] _(after 2.5 atdd-scaffolded)_ `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-5-container-hardening-non-root-user-capabilities-tmpfs-noexec-named-volume.md")` per § Story Lifecycle `atdd-scaffolded → in-dev`.
- [ ] _(after 2.5 in-dev)_ `/bmad-testarch-trace (args: "yolo")` per § Story Lifecycle `in-dev → traced`.
- [ ] _(after 2.5 traced)_ `/bmad-create-story (args: "review")` post-dev SM per § Story Lifecycle `traced → sm-verified`.
- [ ] _(after 2.5 sm-verified)_ `/bmad-code-review (args: "2")` CR opener per § Story Lifecycle `sm-verified → (fixes-pending | done)`.
- [ ] _(conditional, post-Story-2.5 done)_ `/bmad-create-story` for Story 2.6 (host-side `pnpm devbox:*` CLI surface) per § Story Lifecycle + § Cross-epic within-epic path. 12 backlog stories remaining in Epic 2 (2.6..2.17) after 2.5 done.
- [ ] _(conditional, post-Story-2.17 done)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge. Epic 2 completion.
- [ ] _(conditional, post-Epic-2 merged)_ § Cross-epic transition → Epic 3 Story 3.1 OR halt EPIC_DONE awaiting merge.

## BLOCKED

_(none — Story 2.5 pre-dev SM validated cleanly at iter-185 per § Story Lifecycle `drafted → validated`. Two surgical polish edits applied (SC-13 + Task 11.5 `printf` fix; SC-15 ambient-cap mechanism clarified); no AC / task / SC / scope changes. FR14n Story State `drafted → validated`.)_

## ATDD Red Phase

_(none — Story 2.5 has not yet reached `validated → atdd-scaffolded` state. iter-186 NOW `/bmad-testarch-atdd` will make the per-story ATDD decision; expected-skip per Epic-2 infrastructure-security precedent.)_

## DONE (Story 2.5 lifecycle — keep current story chain, prune after Story 2.6 starts)

- [x] iter-184: **`/bmad-create-story` drafted Story 2.5 — Container hardening (non-root user + capabilities + tmpfs noexec + named volume).** Story file created at `_bmad-output/implementation-artifacts/2-5-container-hardening-non-root-user-capabilities-tmpfs-noexec-named-volume.md` (342 LOC; 19 SC-1..SC-19; 12 Tasks with ~45 subtasks). Key design tension surfaced as SC-4 three-cap reconciliation (cap_drop:ALL strips NET_BIND_SERVICE from bounding set → dnsmasq cannot bind :53 → three-cap list `[NET_ADMIN, NET_RAW, NET_BIND_SERVICE]` pinned with Story 2.3's existing compose-line-93 handoff comment as precedent). sprint-status row `2-5: backlog → ready-for-dev`.
- [x] iter-185: **`/bmad-create-story (args: "review")` pre-dev SM validated Story 2.5 — FR14n `drafted → validated` with two surgical polish edits.** Competition-checklist systematic re-analysis against the 342-LOC story file via fresh-context read of epics.md lines 1100-1334 (Epic 2 + Story 2.5 verbatim), architecture.md lines 60-300 (I5 §Devbox-Reference-Config + cross-cutting concerns), prd.md lines 1060-1095 (NFR1-NFR18), docker-compose.yml (all 110 lines — Story 2.3 line 93-97 compose comment already pre-flagged the NET_BIND_SERVICE handoff, which validates SC-4's interpretive stance as pre-planned not unexpected), and `.envrc.example` (confirmed Story 2.2 `_MB` knob names at lines 29-31 match Story 2.5 AC 3 references). **Findings summary:** ZERO critical gaps; story handles the three known tensions (SC-4 three-cap, SC-8 `/workspace/logs` tmpfs deferral, SC-14 `/run/` empirics) with pinned resolutions + empirical-verification gates. Applied two surgical polish edits: (a) **SC-13 Smoke A + Task 11.5** — `echo "#!/bin/sh\necho hello"` → `printf "#!/bin/sh\necho hello\n"`; POSIX `echo` does not interpret `\n` without `-e`, so original command writes a single-line literal not a valid shebang+body (operator copy-paste bug in smoke recipe). (b) **SC-15 capability-inheritance clarification** — the original "belt-and-braces posture works under either Docker daemon ambient-cap behavior OR explicit exec-time file-cap inheritance" overstates setcap effectiveness under `PR_SET_NO_NEW_PRIVS=1`; capabilities(7) kernel semantics disable the file-cap `F(effective)` bit under NNP, so ambient-cap propagation (Docker ≥19.03 `prctl(PR_CAP_AMBIENT_RAISE, ...)` on PID 1 for each `cap_add`-ed cap under USER non-root) IS the load-bearing mechanism; setcap remains as portability fallback but is a no-op under Story 2.5's NNP posture. Dev-story Task 11.7 empirically verifies via `nft list` + dnsmasq :53 bind smokes. **Skipped (optimization/low-value):** NET_RAW rationale wording (inherited from Story 2.3 compose comment), capsh grep format (util-linux version-dependent; current form good enough), PRD NFR7 text amendment (FR44 AMEND path covered by SC-9/SC-10/SC-11 invariant registration — docs/invariants/devbox-hardening.md supersedes PRD narrative per manifest-authority discipline). **Change Log v0.2 entry appended.** Story file remains 342 LOC functional, +~10 LOC for the SC-15 mechanism clarification; Story Status field stays `ready-for-dev` (file-level Status tracks sprint-status enum, not Ralph's FR14n Story State). **iter-186 NOW = `/bmad-testarch-atdd`** fresh context; expected-skip per Stories 2.1..2.4 infrastructure-security precedent. PR #230 stays Draft.

## Context

- **Phase:** 4-implementation — Epic 2 at 4/17 stories `done` (2.1 + 2.2 + 2.3 + 2.4) in sprint-status enum + 12/17 `backlog` (2.6..2.17) + 1/17 `ready-for-dev` (2.5).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.5 live smokes (Task 11.1–11.8) operator-workstation-deferred per Story 2.4 SC-17 precedent — DinD-B cannot safely exercise full `docker exec` sequences against cap-dropped containers without poisoning the host's docker state. Dev-story SHOULD attempt a dry compose-parse + container-start in DinD-B to confirm happy path; full AC-verification matrix is M4-Pro-native-authoritative.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Story 2.5 is the first Epic-2 story to touch `docker-compose.yml` in a structural way (cap_drop/cap_add/security_opt/tmpfs/volumes) + entrypoint.sh WORKSPACE_OWNER default flip + Dockerfile USER directive + setcap on dnsmasq/nft binaries. Expect rebuild requirement for Dockerfile changes to take effect.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; 2.1 + 2.2 + 2.3 + 2.4 done; 2.5 ready-for-dev; 2.6..2.17 in `backlog`. Epic 2 closes at Story 2.17 (PR #230 transitions Draft→Open at that point).
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2; PR #230 stays Draft until Story 2.17 completion triggers the final-CI-gate + Draft→Open transition).
- **Story:** 2.5 — Container hardening (non-root user + capabilities + tmpfs noexec + named volume).
- **Story File:** `_bmad-output/implementation-artifacts/2-5-container-hardening-non-root-user-capabilities-tmpfs-noexec-named-volume.md`
- **Story State:** `validated` — transitioned at iter-185 via `/bmad-create-story (args: "review")` pre-dev SM approval (2 surgical polish edits; zero AC/task/SC changes). iter-186 `/bmad-testarch-atdd` will transition `validated → atdd-scaffolded` (expected-skip per Epic-2 infrastructure-security precedent).
- **GitHub Issue:** Story 2.5 → #45 (https://github.com/tthew/ralph-bmad/issues/45); Epic 2 → #10 (stays open; Ralph closes epic issues on EPIC_DONE halt only).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **Story 2.5 SC-4 three-cap reconciliation (load-bearing, dev-story carries through).** AC 2 drafted as `cap_add: [NET_ADMIN, NET_RAW]`; under `cap_drop: [ALL]`, dnsmasq cannot bind :53 without NET_BIND_SERVICE in the bounding set. SC-4 pins `[NET_ADMIN, NET_RAW, NET_BIND_SERVICE]` with narrow-cap rationale. Pre-flagged by Story 2.3's existing compose line 93-97 comment ("Story 2.5 handoff decision"), so this is a pre-planned resolution not an unexpected PRD amendment. docs/invariants/devbox-hardening.md (Task 6) supersedes PRD NFR7 narrative per FR44 manifest-authority discipline.
- **Story 2.5 SC-14 `/run/` empirical gate (dev-story resolves).** Task 5.1 smokes whether Docker preserves image-layer `/run/` ownership under USER dev. Task 5.2 conditionally relocates whitelist.sh state files to `/tmp/keel-state/` if the empirical result requires. Branch (i) ~= /run stays as image-layer regular dir (dev-writable after chown); branch (ii) ~= tmpfs auto-mount zeros ownership (relocate). Happy-path likely (i); (ii) expands dev-story by ~1 iteration.
- **Story 2.5 SC-15 capability mechanism (iter-185 clarified).** Under `PR_SET_NO_NEW_PRIVS=1`, file-cap effective bit is kernel-disabled per capabilities(7); load-bearing path is Docker ≥19.03's ambient-cap propagation via `prctl(PR_CAP_AMBIENT_RAISE, ...)` on PID 1 for each cap_add'd cap under USER non-root. `setcap +eip` remains as portability fallback (harmless no-op under NNP). Dev-story Task 11.7 verifies via `nft list table inet keel_egress` + `ss -tlnp | grep :53` smokes — both success = ambient-cap path working.
- **Story 2.5 NFR2 budget impact forecast.** Dockerfile changes (useradd + chown + setcap) add ~100ms to cold-start (image-build cached after first bake); compose stanzas are constant-time kernel ops at container-start. No measurable cold-start or warm-start impact; no NFR2 re-measurement required at story close. M4-Pro-native NFR2 authority remains Story 2.1's iter-130 capture per SC-12.
- **Story 2.5 implementation-surface forecast.** 12 Tasks + ~45 subtasks; expected dev-story single-iteration landing per Story 2.4 iter-174 precedent (~360 LOC). Task 5.2b relocation branch is the only 2-iter expansion path; Tasks 1-4 + 6-12 are surgical. ~100-150 patch LOC baseline (8 files: Dockerfile + docker-compose.yml + entrypoint.sh + new docs/invariants/devbox-hardening.md + INVARIANTS.md + invariants.manifest.ts + packages/devbox/README.md + AGENTS.md).
- **Substrate sanity check (post-iter-185):** iter-185 touches ONLY: story file (two surgical edits + v0.2 Change Log entry), IP, RALPH.md. No Dockerfile / compose / entrypoint / invariants-manifest / INVARIANTS.md edits — those land at iter-187+ dev-story. `INV-devbox-egress-contract` + `INV-devbox-dind-available` contentHashes unchanged; no invariant manifest drift at this iteration.
