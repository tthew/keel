# Implementation Plan

## NOW

- [ ] Re-run `/bmad-code-review (args: "2")` — Story State `fixes-pending → done | fixes-pending` confirmation gate after iter-261 PATCH-1 landing. Three-layer Ralph-hosted adversarial fan-out per § Story Lifecycle Decision Matrix row `fixes-pending → done | fixes-pending`. Expected closure pattern per iter-253 LESSON + iter-260 NOVEL LESSON: ZERO new first-class PATCH + possibly 0-1 new second-class DEFER → `done`. If NEW convergent-across-2-of-3-layers findings surface, re-enter `fixes-pending` with new QUEUE entries (Story 2.10 iter-251 precedent: 5th-cumulative-ZERO-PATCH-CR falsified posture). Patch surface to re-review: resolver one-liner + docstring + AGENTS.md + devbox-mode.md (two sites) + story Change Log v1.5 + manifest contentHash — per iter-261 DONE entry. Sprint-status row UNCHANGED at `review` on PATCH re-run; only confirmation `done` flips sprint.

## QUEUE (Story 2.11 CR closure → Stories 2.12..2.17 → Epic 2 close-out)

- [ ] _(after Story 2.11 `done`)_ Iterate Stories 2.12..2.17 through full lifecycle — 6 remaining substrate stories spanning loopback-bound port publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-24 per iter-219..260 repeated pre-push confirmation — `gh pr view 230` returns `statusCheckRollup: []`; iter-260 + iter-261 pre-flight `gh pr view` hit HTTPS :443 timeout — transient, not a CI gate).

## BLOCKED

_(none — iter-261 landing anticipates SSH :22 / HTTPS :443 fluctuation per iter-249..260 precedent; recovery-banking pattern remains load-bearing across iter-249..261. PR #230 still has no CI configured (statusCheckRollup: []); HTTPS :443 timeout on `gh pr view` at iter-261 orient is diagnostic-only, not a push gate.)_

## ATDD Red Phase

_(none — Story 2.11 ATDD-skipped at iter-256 per grounds (c)+(ii)+(iii); iter-258 trace gate WAIVED; iter-259 post-dev SM ZERO-PATCH + 2 MINOR drift DEFERs; iter-260 CR closure 1-PATCH + 16 DEFER + 44 DISMISS; iter-261 PATCH-1 landed.)_

## DONE (iter-261 Story 2.11 CR PATCH-1 impl — Story State `fixes-pending` → `fixes-pending` QUEUE-emptied; ready for CR re-run)

- [x] iter-261: **STORY 2.11 PATCH-1 LANDING — SC-4 opinionated-shared-mode posture extended to `REPO_NAME`.** Convergent-across-2-of-3-layers finding from iter-260 CR (Winston #5 Blind + Edge E6 Edge on silent-override class) closed with compact single-task-per-iter patch surface. **Code change (one-liner):** `packages/devbox/scripts/lib/main-repo-resolver.sh` shared-branch `REPO_NAME="${KEEL_DEVBOX_REPO_NAME:-$shared_parent_name}"` → `REPO_NAME="$shared_parent_name"` (drop operator override; AC 2 correctness under multi-fork-with-override setup — Fork A `KEEL_DEVBOX_REPO_NAME=fork-a-custom` + Fork B `fork-b-custom` previously produced divergent `/workspace/fork-*/` bind targets with second fork silent `docker exec -w` ENOENT). Inline load-bearing-rationale comment added above the assignment. **Docstring update:** resolver header § Shared mode REPO_NAME entry mirrors existing `KEEL_DEVBOX_CONTAINER_NAME_RESOLVED` IGNORED language structurally — cites AC 2 rationale + iter-261 PATCH-1 + SC-4 extension anchor. **Three-site doc lockstep:** (1) `AGENTS.md § Per-fork vs shared devbox mode (Story 2.11) § Operator override bullet` — extended from single-env-var to both-env-vars (both respected in per-fork; both INTENTIONALLY IGNORED in shared; AC 2 divergent-bind-target rationale pinned); (2) `docs/invariants/devbox-mode.md § Shared mode contract` — new REPO_NAME HARDCODED sibling bullet after existing Container name bullet with identical structural shape; (3) `docs/invariants/devbox-mode.md § Invariant stability` — operator override bullet extended to name both env vars. **Manifest contentHash rebuild:** `docs/invariants/devbox-mode.md` sha256 `4ddc4eea3a3f28cde90a1c7944f14d52a18aa1a0a214d9a45050aea1ec313cf2 → 0647445551c8a5ab0d84cc71dca25f96536f5d19f5d3ccaa1350176689e5728a`; `packages/keel-invariants/src/invariants.manifest.ts:277` updated in lockstep; `pnpm --filter @keel/keel-invariants build` + `pnpm keel-invariants:check` sync-gate verified clean (no drift). **Story file updates:** `2-11-…md` Status HTML comment updated to reflect iter-261 PATCH-1 landing; v1.2 Task 1 bullet extended with `iter-261 PATCH-1 correction` note cross-referring v1.5 entry; v1.5 Change Log entry added with full triage detail; Review Findings `[Review][Patch]` task marked `[x]` with landing annotation. **Bash syntax check:** resolver clean. **Sprint-status:** row UNCHANGED at `review` (per iter-202 precedent — interim states hold row; only CR-closure `review → done` flips sprint). Budget consumed iter-261: ~15-20K tokens (orient ~7K + 2 resolver edits + 1 AGENTS.md edit + 2 devbox-mode.md edits + 2 story-file edits + 1 manifest edit + rebuild + sync-gate check + commit = compact patch-and-exit). Well within ~117K execution budget.

_(iter-253..260 Story 2.10 closure + Story 2.11 create-story / pre-dev SM / ATDD-skip / dev-story / trace / post-dev SM / CR iters pruned per Guardrail 2 — see Story 2.11 § Change Log v1.0/v1.1/v1.2/v1.3/v1.4/v1.5 + RALPH.md Signposts iter-253..261 for per-gate detail.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **10/17 stories done** (2.1-2.10) + 1/17 in review (2.11 at state `fixes-pending` post-PATCH-1 QUEUE-emptied; sprint row unchanged at `review`) + 6/17 backlog (2.12..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered at iter-248 9/9 + iter-252 9/9 + iter-257 6/6 + iter-259 adversarial SM + iter-260 three-layer adversarial CR + iter-261 PATCH-1 one-liner sync-gate clean.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:119). Host-side shim count: **18** at iter-261 (unchanged — PATCH-1 one-liner in resolver + doc lockstep; no shim count delta).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **10 done** (2.1-2.10); 1 in-review at state `fixes-pending` post-PATCH-1 (2.11); 6 backlog (2.12..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.11 — Per-fork vs shared devbox mode (`KEEL_DEVBOX_SHARED`)
- **Story File:** `_bmad-output/implementation-artifacts/2-11-per-fork-vs-shared-devbox-mode-keel-devbox-shared.md`
- **Story State:** `fixes-pending` — Story 2.11 PATCH-1 landed at iter-261 (resolver one-liner + three-site doc lockstep + manifest contentHash rebuild + sync-gate clean). QUEUE emptied. Next iter re-runs `/bmad-code-review (args: "2")` for `fixes-pending → done | fixes-pending` confirmation gate.
- **GitHub Issue:** Story 2.11 likely unwired (`RALPH_ISSUE_NUMBER` unset at iter-261 orient; skipping `Refs #<story-issue>` in commits per § Issue Tracking guard). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out, NOT iter-261).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — last confirmed `{"isDraft":true,"mergeStateStatus":"CLEAN","state":"OPEN","statusCheckRollup":[]}` at iter-259 orient; iter-260 + iter-261 pre-flight `gh pr view 230` hit HTTPS :443 timeout (`dial tcp 140.82.*:443: i/o timeout`). Transient network fluctuation per iter-249..260 pattern; no CI configured so not a push gate.

## Notes

- **iter-261 LESSON — PATCH-surface-well-scoped-compact-iter pattern.** Single-task-per-iter + single-source-of-truth patch discipline enables compact patch-and-exit iters (~15-20K token consumption) when the PATCH surface is well-scoped: one-liner code change + three-site doc lockstep + manifest contentHash rebuild. Contrast iter-257 dev-story landing (~80K tokens — 27 files touched). Forward posture: future CR-PATCH iters with scoped surface should bank the compact-iter pattern; avoid conflating with multi-file impl iters. Promote to RALPH.md § Lessons at commit if pattern recurs at iter-262 CR re-run closure.
- **iter-261 operator-workstation-deferred smoke (shared-mode with KEEL_DEVBOX_REPO_NAME override).** Post-PATCH verification smoke: set `KEEL_DEVBOX_SHARED=true` + `KEEL_DEVBOX_REPO_NAME=fork-custom` in operator `.envrc` → `pnpm devbox:env:check` + `pnpm devbox:start` → assert resolver resolves `REPO_NAME` to parent-dir basename (NOT `fork-custom`) via `docker inspect <container> --format '{{index .Config.Labels "com.docker.compose.project.working_dir"}}'` or equivalent. Functional correctness at iter-261 covered by static bash-syntax check + resolver-source inspection + sync-gate pass; live-Docker smoke deferred to operator workstation per Story 2.11 Task 6 operator-walkthrough context.
- **iter-260 LESSONS carry-forward to iter-261 + beyond:** (1) three-layer adversarial fan-out convergence signal (2-of-3 layer overlap = high-confidence PATCH; PATCH-1 is exemplar); (2) Blind Hunter false-positive class #1 (unset-array-access under `set -u` without REQUIRED_VARS fence verification); (3) iter-259 two-subagent post-dev SM pattern distinct from CR three-layer adversarial (different lens: AC-satisfaction + drift-detection vs adversarial code-hazard + path-gap + spec-fidelity).
- **iter-259 LESSONS carry-forward unchanged at iter-261:** (1) post-dev SM two-subagent pattern; (2) D2 architecture.md:547 7-site cascade pattern — pre-existing deferred; (3) partial-waive trace-gate vocabulary absence; (4) manifest rebuild required after new InvariantSchema entry (NOW: also required after contentHash update — iter-261 re-confirms).
- **iter-258 partial-waive trace-gate LESSON + iter-257 shared-mode operator-override INTENTIONAL ignore (SC-4)** remain relevant at iter-261 — SC-4 posture NOW EXTENDED to REPO_NAME per iter-261 PATCH-1; original SC-4 carry-forward posture converted from "preserved for CONTAINER_NAME only" to "preserved for CONTAINER_NAME + REPO_NAME". Future Stories 2.12..2.17 may surface additional env-var override candidates worth same SC-4 extension treatment.
- **iter-244/246 audit-findings carry-forward** unchanged from iter-247..260: (1) restart.sh transitive-delegate prepend-only; (2) benchmark.sh OUT-OF-SCOPE clarification (SC-11); (3) Manifest entry count **31** post-Story-2.11 (corrected from off-by-one "30" in story file per Amelia F1 / AR-76 defer); (4) Alpine egress non-applicability doctrine. Manifest count will not move until next story adds a new invariant (not iter-261 PATCH-1 which only updates contentHash).
- **Substrate-citation drift cumulative forecast for Stories 2.11..2.17 — unchanged at iter-261:** iter-260 totals (2 MINOR + 5 SC-17 deferrals = 7 items for Story 2.11 alone) hold. iter-261 PATCH-1 impl did NOT introduce new drift (v1.5 Change Log adds content but all citations verified at edit-time: manifest line 277 confirmed; doc sites confirmed; Change Log references v1.2 → v1.5 cross-link verified). Cumulative Epic 2 forecast holds at ~23-34 cumulative line-number drifts + 2-3 substantive citation-claim misplacements. SC-17 scope holds: single reconciliation pass at Story 2.17 landing. Deferred-work.md remains at 562 lines — iter-261 added no new AR entries.
