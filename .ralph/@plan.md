# Implementation Plan

## NOW

- [ ] **CR fix task AI-7 (Low):** Add `## INV-devbox-dind-available` heading in the body of `docs/invariants/devbox-dind.md` so the manifest `anchors: ['INV-devbox-dind-available']` claim is self-verifiable. Regenerate `contentHash` in `packages/keel-invariants/src/invariants.manifest.ts:947` in the same commit (Story 1.9 sync-gate drift-detection per FR43). ~small

## QUEUE (Story 2.1 CR fixes-pending — iter-128 /bmad-code-review verdict; FR14n: one action item per iteration until QUEUE empties → re-run /bmad-code-review)
- [ ] **CR fix task AI-8 (Low):** `packages/devbox/README.md:522` — demote `### Pending first bake` → `####` so it nests correctly under `### Benchmarks`. Keeps benchmark.sh's future `### Run <ts>` appends at consistent H3 level.
- [ ] **CR fix task AI-9 (Low):** `packages/devbox/VERSIONS.md` — add a one-paragraph note documenting upstream cc-devbox apt-extras (`software-properties-common`, `sudo`, `tini`, `procps`, `iproute2`, `dnsutils`, `netcat-openbsd`, `openssh-client`, `vim`, `less`, `locales`) as inherited-by-design, not Story 2.1 scope creep.
- [ ] Re-run `/bmad-code-review (args: "2")` — confirm `fixes-pending → done` (no new findings) after AI-1..AI-9 QUEUE empties. Expected outcome: zero new patch action items; defers already documented in `deferred-work.md § code review of story-2.1 (2026-04-21)`.
- [ ] Implement `packages/keel-invariants/` runtime rule for `INV-devbox-dind-available` — write `packages/keel-invariants/src/check-devbox-dind.ts` asserting (a) `command -v docker && docker info` exit 0, (b) `docker run --rm hello-world` exits 0 (functional criterion per iter-122 doc rewrite), (c) record detected backend (A/B) for downstream consumers to gate on. Add unit test (Epic 13 framework pending; Story 1.9 sync-gate pattern in the meantime), wire into `pnpm keel-invariants:check-all` composite. Spec source: `docs/invariants/devbox-dind.md`. After this lands, bump `contentHash` in the manifest entry accordingly if the doc file changes.
- [ ] Add pre-flight bind-mount probe to `packages/devbox/scripts/benchmark.sh` — after `detect_backend()`, if `BACKEND=B`, run `docker run --rm -v "${workspace}":/_probe alpine:latest true` (or reuse `hello-world` if pulled); on failure emit a structured error ("backend B + workspace not host-shared; see README § Pending first bake; either run on operator workstation with worktree under `/Users/...` or switch to backend A") and exit 2 BEFORE the destructive/up-d phase. Mirrors the iter-122 cold-prune refusal-gate pattern. Spec source: RALPH.md iter-124 signpost.
- [ ] Monitor PR CI — queue fix tasks for any failures
- [ ] Transition PR Draft→Open — final CI gate (after all Epic 2 stories done)

## BLOCKED

_(none — AI-1..AI-9 are all Story 2.1-scope + actionable without human input; defers are documented in `deferred-work.md` as out-of-scope or follow-up.)_

## DONE (Epic 2 Story 2.1)

- [x] iter-134: **CR fix AI-6 (Medium) landed — `fixes-pending` QUEUE drain 6/9.** `packages/devbox/Dockerfile` supabase-cli `else` branch at L154-180 (post-edit; +7 net lines from expanded comment header) swaps silent-skip for fail-closed `exit 1` matching git-delta at L182-201. Error-message phrasing normalised to `"supabase-cli: no matching release asset for arch=$arch"` mirroring git-delta's wording so both fail paths present identical operator-facing failure modes. 10-line rationale-comment header documents AC 2's unconditional enumeration of supabase-cli, the arch coverage analysis (supabase's release matrix covers linux/amd64 + linux/arm64 = Ralph's target archs), the defensive fail-fast posture for exotic host archs (linux/arm/v7, linux/riscv64) where silent-skip would produce a misleading artefact, and the cross-reference to git-delta's pattern. The iter-128 CR finding's `Dockerfile:343-345` line pointer was stale (Dockerfile is 232 lines post-edit; cited text was at L170-172 pre-edit) — pragmatic location was the existing supabase RUN block. **Verification:** (i) `grep -n "no matching release asset" packages/devbox/Dockerfile` returns exactly two hits (L178 supabase + L199 git-delta) each followed by `exit 1;` on the next line — structural parity confirmed; (ii) `grep -c "; skipping" packages/devbox/Dockerfile` returns 0 — no silent-skip remnants; (iii) shell-style line continuations intact. Zero-regression on iter-123 bake envelope: the edit only fires on arch mismatch (not encountered on linux/arm64 or linux/amd64); primary install path at L170-176 untouched. No VERSIONS.md update needed (no version pin change). Review Findings AI-6 `[ ] → [x]` with closing evidence. Story 2.1 v2.2 Change Log row appended (before v2.1 per existing sort-order convention). Story State stays `fixes-pending` (AI-7..AI-9 remain TOP-of-QUEUE). Next NOW: AI-7 (add `## INV-devbox-dind-available` heading in body of `docs/invariants/devbox-dind.md` + regen manifest `contentHash` in same commit per Story 1.9 sync-gate).

- [x] iter-133: **CR fix AI-5 (Medium) landed — `fixes-pending` QUEUE drain 5/9.** `packages/devbox/entrypoint.sh` gains three bundled robustness edits (post-edit 93 lines; +44 / -0 net on iter-99's 49-line narrow surface): (a) the two iter-99 blanket `chown … || true` calls (workspace chown + OAuth-dir loop chown) replaced with a captured-stderr pattern — `if ! chown -- "${WORKSPACE_OWNER}" <target> 2>/tmp/chown.err; then cat /tmp/chown.err >&2 || true; fi; rm -f /tmp/chown.err`. Preserves best-effort posture while making failures diagnosable in `docker compose logs`. (b) regex gate `^[A-Za-z0-9_-]+:[A-Za-z0-9_-]+$` runs against `KEEL_DEVBOX_WORKSPACE_OWNER` before either chown; on mismatch prints two-line diagnostic + exits 2. `chown --` on every call is belt-and-braces (terminates option parsing — defence-in-depth against `--reference=/etc/shadow` class injection). (c) `exec "$@"` replaced with `if [ "$#" -eq 0 ]; then exec sleep infinity; else exec "$@"; fi` so `docker run keel-devbox:local` without a CMD stays alive for operator probing. Every edit wrapped in rationale-comment block per iter-129/130/131/132 pattern. **Verification:** `bash -n` exit 0; Task 7.1 forbidden-pattern grep still exits 1; Task 7.4 executable bit `100755` preserved; regex-gate rejects `--reference=/etc/shadow` + `justname` + `user:group:extra` → exit 2 with diagnostic; regex-gate accepts `root:root`; `timeout 3 bash entrypoint.sh` exits 124 (sleep infinity killed — fallback demonstrated); `bash entrypoint.sh echo forwarded-ok` → exit 0 (non-empty CMD branch intact). Zero-regression on Task 7 bundle + zero-regression on iter-123 bake envelope (edit is container-run-time, not image-build-time). CR finding text simplified to "log on failure, continue" from "known-tolerated ENOPERM / EROFS codes" — GNU coreutils chown does not expose errno in exit status cleanly; the substantive defect was the silent-swallow, not errno miss-classification. Review Findings AI-5 `[ ] → [x]` with closing evidence. Story 2.1 v2.1 Change Log row appended (before v2.0 per existing sort-order convention). Story State stays `fixes-pending` (AI-6..AI-9 still TOP-of-QUEUE). Next NOW: AI-6 (supabase-cli silent-skip → fail-closed `exit 1` at Dockerfile:343-345 matching git-delta pattern at Dockerfile:365).

- [x] iter-132: **CR fix AI-4 (Medium) landed — `fixes-pending` QUEUE drain 4/9.** `packages/devbox/scripts/benchmark.sh` gains four bundled safety/UX edits (post-edit file is 196 lines; +46 / -1 net): (a) SIGINT/SIGTERM trap at L146 (after `compose_version` capture + before the cold branch → covers cold AND warm) tears compose down before `exit 130` so ^C doesn't leave stray containers / networks; rationale-comment block names the iter-124 bind-mount-denial abort as the representative case. (b) `docker info` reachability probe at L82-86 (between `command -v docker` and `detect_backend()`) fails fast with exit 127 + two-line operator hint when the daemon is unreachable — prevents `detect_backend()` silently degrading to fail-safe-B and the caller wasting a cold+warm invocation. (c) `--skip-cold + --allow-broad-prune` combo warning at L66-68 (mutually useless — `--skip-cold` bypasses the destructive pass the second flag authorises); continues warm-only; prior behaviour silently dropped `--allow-broad-prune`. (d) Flag-text branching at L176-180 — `BACKEND=A` emits "host: native (backend=A) — AC 4 authoritative if uname matches M4-Pro baseline; single-run values remain modelled per architecture.md § NFR28b"; `BACKEND=B` preserves existing DinD-indicative text. Correct because iter-131 `detect_backend()` only asserts A when `/.dockerenv` is absent. **Verification:** `bash -n` exit 0; combo-warning snippet emits to stderr; label-branch produces expected strings byte-for-byte; `docker info` probe exits 0 under cc-devbox iteration env (backend B). Zero-regression on iter-123 bake envelope — none of the four edits touch the destructive-prune refusal gate at L118-133 or `detect_backend()` at L88-114. Review Findings AI-4 `[ ] → [x]` with closing evidence. Story 2.1 v2.0 Change Log row appended (before v1.9 per existing sort-order convention). Story State stays `fixes-pending` (AI-5..AI-9 still TOP-of-QUEUE). Next NOW: AI-5 (entrypoint.sh robustness bundle — narrow `chown ... || true`, validate `KEEL_DEVBOX_WORKSPACE_OWNER` shape, empty-CMD fallback).

- [x] iter-131: **CR fix AI-3 (High) landed — `fixes-pending` QUEUE drain 3/9.** `packages/devbox/scripts/benchmark.sh` `detect_backend()` (post-edit L65-88) inverts the `/.dockerenv` fallback: any `/.dockerenv` presence now returns `B` unconditionally (after the case-statement fast-path for Docker Desktop / Moby / LinuxKit). Previously the function returned `B` only when `daemon_name` was non-empty AND distinct from `$(hostname)`; empty `daemon_name` (probe failed) AND `daemon_name == $(hostname)` under `/.dockerenv` BOTH fell through to `echo A` — making destructive-prune misdetection the DANGEROUS default. 10-line rationale-comment block names the ambiguity sources, the § Safety rule "when in doubt, refuse destructive" reference, and the `--allow-broad-prune` escape path for callers who know they are on an isolated nested dockerd. **Verification:** `bash -n` parse exit 0; extracted-function smoke under cc-devbox iteration env returns `B` via the case-statement fast-path (Docker Desktop match — same as pre-edit); simulated ambiguity inputs under `/.dockerenv` now correctly return `B` (was `A` pre-edit). **Doc re-verification (per AI-3 spec):** `docs/invariants/devbox-dind.md § Safety rule` canonical heuristic lists TWO signals that IMPLY B; new code is STRICTER (defaults B when it cannot prove A) which CONFORMS to the contract without requiring a doc edit — the heuristic names sufficient B-signals, not exhaustive ones. No `contentHash` regen needed; no manifest update. Review Findings AI-3 `[ ] → [x]` with closing evidence. Story 2.1 v1.9 Change Log row appended. Story State stays `fixes-pending` (AI-4..AI-9 still TOP-of-QUEUE). Next NOW: AI-4 (benchmark.sh safety + UX bundle — SIGINT/SIGTERM trap + `docker info` reachability probe + mutually-useless flag combo warning + native-A-vs-DinD modelled-baseline flag branching).

- [x] iter-130: **CR fix AI-2 (High) landed — `fixes-pending` QUEUE drain 2/9.** `packages/devbox/docker-compose.yml:23` (pre-edit literal `container_name: keel-devbox`) replaced with `container_name: ${KEEL_DEVBOX_CONTAINER_NAME:-keel-devbox}`, wrapped in an 8-line rationale-comment block naming the backend-B multi-worktree / multi-fork collision hazard, the `.envrc` override path, and the drop-entirely alternative (kept as explicit default for log-greppability). Verified via `docker compose config` — default expansion `container_name: keel-devbox` (preserves iter-124 Task 7.3 envelope) + override expansion `KEEL_DEVBOX_CONTAINER_NAME=keel-devbox-worktree-ralph docker compose config` → `container_name: keel-devbox-worktree-ralph`. Env_file-comment block extended to document the Compose v2.20+ requirement (`path:` / `required:` object-form; `docker/compose#11365` 2024-02) + flat-list `env_file: [../../.envrc]` fallback (loses `required: false` → missing-file parse error between Story 2.1 and Story 2.2). VERSIONS.md gains new `## Host Compose floor` section. The `L702-704` literal in the iter-128 CR finding was a stale line-pointer (compose file is 78 lines); pragmatic docs location = existing env_file comment. Review Findings AI-2 `[ ] → [x]` with closing evidence. Story 2.1 v1.8 Change Log row appended (before v1.2 per existing sort-order convention). Story State stays `fixes-pending` (AI-3..AI-9 still TOP-of-QUEUE). Next NOW: AI-3 (harden `detect_backend()` default-B on ambiguous/empty daemon name — current `echo A` fall-through is the DANGEROUS destructive-prune default).

- [x] iter-129: **CR fix AI-1 (High) landed — `fixes-pending` QUEUE drain 1/9.** `packages/devbox/Dockerfile:102-111` (post-edit lines): the unpinned `npm install -g @anthropic-ai/claude-code \ && claude --version || true` replaced with `npm install -g @anthropic-ai/claude-code@2.1.116 \ && (claude --version || echo "claude version probe failed (install succeeded)" >&2)`. Pin matches iter-123 first-bake capture; Renovate (Story 1.15 npm custom manager) raises bump PRs. Parenthesised `||` scopes the fallback to the probe only so a failed `npm install` surfaces as RUN exit-1 instead of being masked by shell-form `&& … || true` precedence. VERSIONS.md updates: (a) `Baked at image-build` table row `@anthropic-ai/claude-code` `_recorded-at-bake_ → 2.1.116 (iter-128 CR-pinned; iter-123 bake capture)`; (b) new `Bake log` entry for iter-129 documenting the pin-hardening edit (not a bake). Review Findings AI-1 `[ ] → [x]` with closing evidence. Story 2.1 v1.7 Change Log row appended. RALPH.md § Gotchas entry added codifying the `RUN A && B || C` shell-precedence rule for every future Dockerfile story (2.3/2.4/2.5/2.13/6.1). Story State stays `fixes-pending` (AI-2..AI-9 still TOP-of-QUEUE). Next NOW: AI-2 (parameterise / drop `container_name` + Compose v2.20+ note).

- [x] iter-128: **`/bmad-code-review (args: "2")` CR adversarial action-items gate — verdict `fixes-pending`; Story State `sm-verified → fixes-pending`; Status `review → in-progress`.** Twelfth cumulative Epic post-SM CR precedent + first infrastructure-smoke class CR gate. Three-layer Ralph-hosted adversarial fan-out against `git diff main...HEAD` scoped to Story-2.1-owned files (packages/devbox/{Dockerfile, docker-compose.yml, entrypoint.sh, scripts/benchmark.sh, VERSIONS.md, README.md} + docs/invariants/devbox-dind.md + invariants.manifest.ts entry + INVARIANTS.md anchor + architecture.md I5a + AGENTS.md + RALPH.md). **Blind Hunter** (diff-only) 14 findings; **Edge Case Hunter** (diff + project-read) 29 findings; **Acceptance Auditor** (diff + prd.md + epics.md + architecture.md + devbox-dind.md + trace artefacts) 6 findings (all Low; all 4 ACs SATISFIED; scope-creep CLEAN; manifest `contentHash: a9675f7d…b3ad` byte-matches `sha256sum docs/invariants/devbox-dind.md`). Deduplicated + triaged → **9 patch action items AI-1..AI-9** (3 High + 3 Medium + 3 Low) + **12 defer items** (carried to `deferred-work.md § code review of story-2.1 (2026-04-21)`) + **~11 dismissed**. Review Findings subsection appended to story file between Tasks/Subtasks and Dev Notes. Story Status flipped `review → in-progress`; sprint-status.yaml `2-1-…: review → in-progress`; `last_updated: 2026-04-21 Story-2-1-fixes-pending UTC`. Story file v1.6 change-log entry appended before v1.2 per existing sort-order convention. Next NOW: AI-1 (pin @anthropic-ai/claude-code@2.1.116 + drop `|| true`).

- [x] iter-127: **`/bmad-create-story (args: "review")` post-dev SM verification — verdict `sm-verified`; Story State `traced → sm-verified`.** Eleventh cumulative Epic post-dev SM precedent + first infrastructure-smoke class post-dev SM review. AC-by-AC verdict SATISFIED ×4 + operator-owned carve-out ACCEPTED + scope-creep CLEAN + substrate evidence re-verified LIVE byte-identical to iter-126 records. Story file v1.5 change-log entry.

- [x] iter-126: **`/bmad-testarch-trace (args: "yolo")` — WAIVED gate; Story State `in-dev → traced`.** 11th cumulative Epic WAIVED precedent + first infrastructure-smoke class. Four trace artifacts authored under `_bmad-output/test-artifacts/traceability/`: coverage matrix + e2e-trace-summary + gate decision + full trace doc. Substrate evidence re-verified LIVE byte-identical to iter-123 bake.

- [x] iter-125: **`/bmad-dev-story` re-invocation codifies iter-123+iter-124 empirical evidence.** Story file v1.4 change-log; `sprint-status.yaml` 2-1 `in-progress → review`; Story State `in-dev (partial) → in-dev`.

- [x] iter-124: **Exercise the baked image — empirical outcome.** Task 7.3 PASS (compose config exit 0). Tasks 4/5-run BLOCKED on backend-B bind-mount denial; README § Pending first bake updated with iter-123+iter-124 summary + operator-owned carve-out scope extension.

- [x] iter-123: **First successful devbox image bake (safe-subset, backend B).** `keel-devbox:local` — sha256:e7e91f1537f196…, 848 MB, linux/arm64, ~4.5 min. Pinned tool versions captured to `packages/devbox/VERSIONS.md § Bake log`.

- [x] iter-121 + iter-122 (summarised): Docker landed iter-121 via operator install; `INV-devbox-dind-available` codified at iter-121. iter-122 discovered backend B (host socket-passthrough); rewrote invariant doc with § Backend contract + § Safety rule; added `detect_backend()` + backend-B destructive-op refusal gate + `--allow-broad-prune` + `--skip-cold` to `benchmark.sh`.

- [x] iter-99 (summarised; full detail in git log + RALPH.md iter-99 signpost): Story 2.1 source tree authored; sprint-status `ready-for-dev → in-progress`; Change Log v1.3 in story file; baseline commit `5278738`.

## Context

- **Phase:** 4-implementation — Epic 2 open, Story 2.1 at `fixes-pending` at iter-134 (AI-1 + AI-2 + AI-3 + AI-4 + AI-5 + AI-6 landed; AI-7..AI-9 remain). Next lifecycle gate: QUEUE drain of AI-7..AI-9 → re-run `/bmad-code-review` to confirm `fixes-pending → done`.
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available` § Backend contract). Iteration-context bind-mount denial is an operational constraint; operator-workstation resolves via M4-Pro native with worktree under host-shared `/Users/...` OR backend-A isolated DinD.
- **Baked image:** `keel-devbox:local` (first bake iter-123 2026-04-21). Version matrix in `packages/devbox/VERSIONS.md § Bake log`. linux/arm64 — matches M4-Pro operator workstation.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; Story 2.1 is the image + compose + entrypoint foundation.
- **Epic Branch:** `feat/epic-2-packaged-devbox`.
- **Story:** 2.1 — `packages/devbox/` absorb from cc-devbox (image + compose + substrate tooling access).
- **Story File:** `_bmad-output/implementation-artifacts/2-1-packages-devbox-absorb-from-cc-devbox-image-compose-substrate-tooling-access.md`
- **Story State:** fixes-pending — iter-128 `/bmad-code-review (args: "2")` verdict; iter-129 landed AI-1 (Dockerfile pin + `|| true` drop + VERSIONS.md); iter-130 landed AI-2 (`container_name` parameterisation + Compose v2.20+ annotation + VERSIONS.md § Host Compose floor); iter-131 landed AI-3 (`detect_backend()` fail-safe-to-B under `/.dockerenv`); iter-132 landed AI-4 (benchmark.sh SIGINT trap + `docker info` probe + flag-combo warning + native-A-vs-DinD flag-text branching); iter-133 landed AI-5 (entrypoint.sh: narrowed `chown` failure handling + `KEEL_DEVBOX_WORKSPACE_OWNER` regex gate + `chown --` option-parse terminator + empty-CMD `sleep infinity` fallback); iter-134 landed AI-6 (Dockerfile supabase-cli `else` branch fail-closed `exit 1` matching git-delta pattern + expanded rationale-comment header). AI-7..AI-9 patch action items remain TOP-of-QUEUE; Status stays `in-progress` per CR skill step-04 § 6. Advances `fixes-pending → done` when QUEUE empties + CR re-runs with zero new findings.
- **GitHub Issue:** Story 2.1 → #41 (https://github.com/tthew/ralph-bmad/issues/41). Parent Epic 2 → #10. Commit trailer `Refs #41`.
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (no CI runners pre-Epic-13; Draft→Open transition queued at end of Epic 2).

## Notes

- **DinD is fork-time substrate, not transitional.** Every fork's cc-devbox-equivalent environment must provide Docker per `INV-devbox-dind-available`. Forks substituting Podman/rootless Docker remain conformant if daemon reachability + function hold.
- **Backend B is the reference environment at 2026-04-21.** Host socket-passthrough to macOS Docker Desktop. Broad-state-mutation scripts MUST detect the backend + refuse destructive ops by default (`benchmark.sh` is the reference at iter-122). Bind-mount-based compose runtime requires host-shared File Sharing allowlist coverage of the workspace path — operator-owned from iteration context.
- **NFR2 authority unchanged.** AC 4 cold ≤ 300s / warm ≤ 30s remains authoritative on M4-Pro native. Post iter-124, BOTH cold AND warm runtime measurement are operator-owned.
- **Cross-epic auto-advance pattern validated** (iter-96). **Partial-completion pattern validated** (iter-99). **Backend-B safety-gate pattern established** (iter-122). **Review-with-operator-owned-carve-out pattern validated** (iter-125; story flips `review` without force-closing operator-owned `[ ]` tasks because the carve-out is an AC scope clarification, not a task skip). **SM-verified-with-operator-owned-carve-out pattern validated** (iter-127; SM accepts the carve-out as scope-clarification-consistent). **CR-fixes-pending-adversarial-default pattern validated** (iter-128; 9 patch + 12 defer + ~11 dismiss triage from 49 raw findings, adversarial-default adherence per FR14n).
- **Story 2.1 scope bounded by AC1-AC4 literally.** Substrate's pre-existing `packages/devbox/` TS scaffold preserved; Story 2.1 ADDED runtime-infrastructure siblings + substrate-level DinD invariant files.
