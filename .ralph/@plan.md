# Implementation Plan

## NOW

- [ ] **Run `/bmad-create-story` (no args) to pick Story 2.6 from sprint-status backlog** per § Story Lifecycle Decision Matrix row `done → next story in the current epic` + § Cross-epic within-epic route. Story 2.6 title per sprint-status: `2-6-host-side-pnpm-devbox-cli-surface` — scope-owner for the two iter-197 closure DEFERs (AR-11 envvar validation + AR-12 Compose project-name docs polish) plus carry-overs from iter-190 CR opener (AR-7 `/run` relocation SC-14 branch (ii) + AR-9 `/etc/*` writes + AR-10 pre-Story-2.5 volume migration docs). Fresh context window per § BMad Workflows one-skill-per-iteration rule. After the skill completes, Story State = `drafted`; sprint-status `2-6: backlog → ready-for-dev`. Epic 2 stays `in-progress` (12/17 remaining: 2.6..2.17). No halt. ~medium (story-creation cost ~45-55K tokens per Story 2.4 iter-172 + Story 2.5 iter-184 envelope).

## QUEUE (Story 2.6 lifecycle → Epic 2 Story 2.7..2.17 → Epic 2 close-out)

- [ ] _(post-`drafted`)_ `/bmad-create-story (args: "review")` — pre-dev SM validation (drafted → validated) per § Story Lifecycle row `drafted`.
- [ ] _(post-`validated`)_ `/bmad-testarch-atdd` — red-phase scaffolds per § Story Lifecycle row `validated`. Skip-to-`in-dev` allowed only if Story 2.6 has no testable ACs (record rationale in IP).
- [ ] _(post-`atdd-scaffolded`)_ `/bmad-dev-story (args: "{story_file_path}")` — primary implementation per § Story Lifecycle row `atdd-scaffolded`.
- [ ] _(post-`in-dev`)_ `/bmad-testarch-trace (args: "yolo")` — AC→test coverage gate per § Story Lifecycle row `in-dev`.
- [ ] _(post-`traced`)_ `/bmad-create-story (args: "review")` — post-dev SM verification per § Story Lifecycle row `traced`.
- [ ] _(post-`sm-verified`)_ `/bmad-code-review (args: "2")` — CR opener per § Story Lifecycle row `sm-verified`.
- [ ] _(post-CR-drain; QUEUE empty)_ `/bmad-code-review (args: "2")` — CR-closure gate (expected ZERO-PATCH per Epic-2 3-story precedent: 2.3 iter-171, 2.4 iter-183, 2.5 iter-197).
- [ ] _(post-Story-2.6-done; through Story 2.17-done)_ Iterate Story 2.7..2.17 through the full lifecycle — 11 remaining substrate stories spanning operator migration docs + healthcheck + shared-workspace + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (will apply to any push; currently PR has no CI checks configured — "no checks reported" per iter-197 pre-flight).

## BLOCKED

_(none)_

## ATDD Red Phase

_(none — Story 2.5 ATDD-skipped at iter-186 per § Story Lifecycle `validated → atdd-scaffolded` (skip branch). Story 2.6 ATDD posture TBD at iter-200 `validated → atdd-scaffolded` transition; host-side CLI surface MAY warrant bash/Playwright e2e smokes for pnpm subcommand contracts.)_

## DONE (Story 2.5 lifecycle — keep current story chain, prune after Story 2.6 starts)

- [x] iter-197: **CR-closure re-run — `/bmad-code-review (args: "2")` ZERO-PATCH close for Story 2.5 `fixes-pending → sm-verified → done`.** Three-layer Ralph-hosted adversarial fan-out (Blind + Edge + Acceptance) re-executed against same `b2533b0..HEAD` baseline post-drain (9 SC-19 in-scope files, 645 lines, +466/-22). **Triage:** 0 PATCH + 2 DEFER + ~13 DISMISS = ~15 triaged findings from ~20 raw (Blind ~14 + Edge ~6 + Auditor 0 + ~5 de-duplicated). **Acceptance Auditor ZERO-finding audit** — all 5 ACs + 19 SCs satisfied; 6 AI-1..AI-6 opener patches each verified landed without regression. DEFER: AR-11 `KEEL_DEVBOX_TMPFS_{TMP,VARTMP}_MB` envvar validation (Story 2.6 scope per SC-18 Story-2.2-owner); AR-12 `COMPOSE_PROJECT_NAME` / `-p` override docs polish (Story 2.6 operator-migration docs). DISMISS: 5 cosmetic Blind; 3 self-withdrawn-on-re-read; 2 already-AR-10-deferred; 1 out-of-1.0-scope (rootless Podman); 2 documented-mechanism prose-precision. **Sprint-status `2-5-container-hardening-...: review → done`; Status line `fixes-pending → done`.** Epic 2 counter 4/17 → 5/17 `done`. PR #230 stays Draft (Epic 2 closes at Story 2.17). Change Log v1.9 appended via last-row-anchor pattern. Story 2.5 § Review Findings subsection v1.9 appended with full narrative + drain cross-check + DISMISS rationale. deferred-work.md § "code review of story-2-5 closure (2026-04-22)" section added with AR-11 + AR-12. **Quality gates ALL GREEN (5/5):** `pnpm keel-invariants:check-all` (21 manifest entries; contentHashes stable at v1.8 values — no invariant sourcePath edits in closure), `pnpm -w run format:check` (prettier clean), `pnpm --filter @keel/devbox lint` (eslint clean; markdown not linted), `pnpm --filter @keel/devbox typecheck` (`tsc -b --noEmit` clean), `docker compose config --quiet` parse-smoke exit 0. Budget: ~50-55K tokens (orient ~8K + diff prep ~3K + 3-subagent fan-out ~40K + triage/IP/RALPH/commit/push ~5K). Mid-range of iter-190 opener (~80K); closures run lighter than openers against same diff baseline per Story 2.3 iter-171 (~55K) + Story 2.4 iter-183 (~45K) precedent. **CR-closure Epic-2 convergence re-confirmed (3 stories: 2.3 + 2.4 + 2.5) — ZERO-PATCH CR-closure is the Epic-2 substrate-security norm.**
- [x] iter-191..iter-196: CR drain AI-1 → AI-2 → AI-6 → AI-3 → AI-4 → AI-5 one-AI-per-iter per § Story Lifecycle `fixes-pending` row. Change Log v1.3..v1.8. All 6 PATCHes from iter-190 opener drained without scope creep.
- [x] iter-187..iter-190: `/bmad-dev-story` → `/bmad-testarch-trace yolo` (WAIVED) → `/bmad-create-story review` post-dev SM (ZERO-PATCH ZERO-DEFER) → `/bmad-code-review (args: "2")` CR opener (6 PATCH + 4 DEFER + ~17 DISMISS; FIRST Epic-2 architecture-security class CR).

## Context

- **Phase:** 4-implementation — Epic 2 at 5/17 stories `done` (2.1 + 2.2 + 2.3 + 2.4 + 2.5) + 12/17 `backlog` (2.6..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.5 live smokes operator-workstation-deferred per Story 2.4 SC-17 precedent. CR closure at iter-197 ran entirely docs-and-triage surface (no Dockerfile / compose / dnsmasq.conf edits); sync-gate + typecheck + lint + format:check + docker compose parse-smoke are the 5-gate quality-gate chain.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Story 2.5 substrate (USER dev + setcap + cap_drop/security_opt/tmpfs/volumes + named volume) shipped; operator-workstation rebuild materialises it. Libcap2-bin + dnsmasq pre-created log file + Compose project-name all landed at iter-191..iter-196 drain; post-drain HEAD builds cleanly through setcap RUN.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; 5 done (2.1-2.5); 12 backlog (2.6..2.17). Epic 2 closes at Story 2.17 (PR #230 Draft→Open transition at that point).
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** _(next-up)_ Story 2.6 — host-side `pnpm devbox:*` CLI surface. Scope-owner for AR-7 `/run` relocation (SC-14 branch (ii)) + AR-9 `/etc/*` writes + AR-10 pre-Story-2.5 volume migration docs + AR-11 envvar validation + AR-12 Compose project-name docs polish.
- **Story File:** _(not yet created — Story 2.6 file will be created by next iter's `/bmad-create-story` invocation)_
- **Story State:** _(no story)_ — first action on next iter MUST be `/bmad-create-story` per § Story Lifecycle Decision Matrix row `_(no story)_`.
- **GitHub Issue:** Story 2.5 → #45 (closed via `Closes #45` in PR body when Epic 2 merges at Story 2.17); Epic 2 → #10 (Ralph closes on EPIC_DONE halt). Story 2.6 issue TBD on `/bmad-create-story` invocation.
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **Epic-2 CR-closure convergence re-confirmed at iter-197 (3-story precedent):** ZERO-PATCH CR-closure is the Epic-2 substrate-security-story norm. Story 2.3 iter-171 ZERO-PATCH (infrastructure-security class, 9-PATCH opener); Story 2.4 iter-183 ZERO-PATCH (user-facing-CLI class, 5-PATCH opener); Story 2.5 iter-197 ZERO-PATCH (architecture-security class, 6-PATCH opener). The "opener >0 PATCH, drain one-per-iter, closure ZERO" arc is canonical for multi-surface substrate stories; the hypothetical "opener ZERO, skip drain, single-iter sm-verified → done" arc would apply only to spec-pure AC-only stories (no observed Epic-2 example at 1.0). LESSON: architecture-security / infrastructure-security classes empirically produce multi-PATCH openers even with ZERO-PATCH post-dev SM AC verdicts because the SM-verification bar is AC-satisfaction under declared-spec-surface while CR-opener bar is AC-satisfaction under FULL-runtime-integration-chain. Different bars, same landing outcome on closure re-run.
- **Ralph-hosted CR-workflow agent-type (re-confirmed iter-197):** `bmad-review-adversarial-general` + `bmad-review-edge-case-hunter` subagent types NOT exposed in runtime Agent-tool catalog; invocation MUST route through `general-purpose` subagents with role-specific Blind Hunter / Edge Case Hunter / Acceptance Auditor framing. Three parallel subagents end-to-end ~50-55K tokens at closure iter (vs ~80K at opener) due to DISMISS-dominant findings + already-deferred bucket on re-read.
- **CR-closure budget forecast (validated iter-197):** closure re-runs run lighter than openers against same diff baseline per Story 2.3 iter-171 (~55K) + Story 2.4 iter-183 (~45K) + Story 2.5 iter-197 (~50-55K) precedent. Under 117K execution budget with 25K buffer for quality gates + commit + push. No budget concern for future Epic-2 CR closures.
- **Within-epic § Cross-epic transition (validated iter-197 → iter-198):** Story 2.5 `done` + Epic 2 has 12/17 backlog stories (2.6..2.17) remaining → NO halt required. Next iter auto-advances within-epic with `/bmad-create-story` fresh context to pick Story 2.6 from sprint-status. Epic 2 stays `in-progress`. This matches FR14n § Cross-epic within-epic route.
- **Story 2.6 scope preview (iter-198+):** host-side `pnpm devbox:*` CLI — absorbs 4 iter-190 opener DEFERs (AR-7 `/run` relocation, AR-9 `/etc/*` writes, AR-10 pre-volume migration docs) + 2 iter-197 closure DEFERs (AR-11 envvar validation, AR-12 COMPOSE_PROJECT_NAME docs polish) + new primary-contract surface (operator-facing pnpm subcommands for devbox lifecycle). Expected story shape TBD at iter-198 `/bmad-create-story` draft; expected to be user-facing CLI class (parallel to Story 2.4 CLI class; 5-PATCH opener forecast).
- **Sprint-status flip gate (iter-197):** sprint-status.yaml `2-5` row flipped `review → done` atomically in this iter per Story 2.4 iter-183 + Story 2.3 iter-171 precedent. Change Log v1.9 + Review Findings subsection appended as a single commit. No drain-to-closure timing ambiguity — the sprint-status flip is the CR-closure gate output, not a parallel lifecycle marker.
- **Invariant-sourcePath contentHash stability at closure (iter-197):** `INV-devbox-homedev-named-volume` contentHash `f34cb62f…` stable at v1.8 value; no invariant-doc edits in this closure iter; `pnpm keel-invariants:check-all` exit 0 without rebuild-sequence. Builds-system signpost (RALPH.md § Gotchas 2026-04-21) applies only when invariant sourcePath prose changes — closure iter doesn't touch registered sourcePaths.
