# Implementation Plan

## NOW

- [ ] `/bmad-testarch-atdd` — FR14n row 3 `validated → atdd-scaffolded`. Fifth cumulative ATDD-skip application expected (no `vitest.config.*` / `jest.config.*` / `playwright.config.*` at Story 1.11 time; test runner lands Story 1.16 per architecture.md:650-652 + story § Testing Standards). Hybrid ground-(c) rationale re-applicable: (c.ii) Story 1.13 pre-commit schema-validation gate owns AC 1 integration coverage + (c.iii) `/bmad-code-review (args: "2")` adversarial pass is AC 2 grep-based backstop per story § Testing Standards Dev Notes. Expected skip, not scaffold. ~small

## QUEUE (Story 1.11 — design-token source, Direction A baseline with motion + density scales)

- [ ] `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/1-11-design-token-source-direction-a-baseline-with-motion-density-scales.md")` — FR14n row 4 `atdd-scaffolded → in-dev`. ~large budget (Task 1 author `packages/ui/tokens.json` ~86-leaf DTCG population ≈ medium-large + Task 2 schema-validate one-shot ≈ small + Task 3 manifest+INVARIANTS 1-entry register ≈ small + Task 4 quality gates + 2-3 sync-gate smokes + sprint-status bump ≈ medium). If partial completion, re-queue with `Story State = in-dev (partial)` per iter-45 Lesson. Expected dev ordering: Task 1 (authoring) BEFORE Task 2 (validation) BEFORE Task 3 (`sha256sum` compute requires final canonical byte-form — run prettier first per Task 4 ordering lesson) BEFORE Task 4 (smokes require Task 3's manifest+anchor pairing).
- [ ] `/bmad-testarch-trace (args: "yolo")` — FR14n row 5 `in-dev → traced`. Fifth cumulative WAIVED precedent expected (Stories 1.7/1.8/1.9/1.10 + now 1.11 — contract-populator stories at substrate stage inherit the same gate posture; mirror Story 1.10's trace artefact structure per iter-46 structural-inheritance Lesson — ~90% less reasoning authoring cost).
- [ ] `/bmad-create-story (args: "review")` — FR14n row 7 `traced → sm-verified`. Expected: first-round pass if the iter-53 preventative audit held through dev + the iter-54 pre-dev SM review caught the last mechanical misses. Mechanical-fix round-trip budget = 1 iter if any structural gap surfaces (Story 1.10 iter-47..49 precedent).
- [ ] `/bmad-code-review (args: "2")` — FR14n row 9 `sm-verified → done` (or `sm-verified → fixes-pending` if action items surface; budget 1-3 CR rounds per iter-7 contract-only-single-pass hypothesis + iter-52 halving-to-zero-at-round-2 trajectory for contract-populator stories with ≤1 convergent PATCH at round 1).

## BLOCKED

_(none)_

## DONE (Story 1.11 authoring + pre-dev review; Story 1.10 closure at iter-52)

- [x] iter-54: **`/bmad-create-story (args: "review")` pre-dev SM review; FR14n state transition `drafted → validated` per matrix row 2.** Two findings surfaced + fixed in-place (FR14n has no `drafted-fixes-pending` state per iter-53 IP Notes):
  - **SM-1 [MEDIUM] Task 1 color.status line internal inconsistency + mis-citation.** Story line 75 claimed "OKLCH hue targets per ux-spec:497: info ≈ blue (hue 220)..." but the representative value on the same line was `oklch(52% 0.14 230)` — hue 230, not 220. Also the citation "per ux-spec:497" was imprecise: ux-spec:497 carries status semantic labels only (info/success/warning/error/critical names), no numeric hue bindings. Numeric hue values live in `ux-design-directions.html` Direction A block. Fix applied: internal consistency — `220 → 230` to match representative value; citation reframed — named ux-spec:497 as semantic-label source, representative numerics as Story 1.11 engineering choices informed by `ux-design-directions.html` Direction A block, retunable within ±15° under AA-contrast math.
  - **SM-2 [LOW] Task 3 off-by-one line reference.** Story line 119 claimed `INV-tokens-semantic-rationale` at `line 136` of `packages/keel-invariants/src/invariants.manifest.ts`; actual position is `line 137` (subagent-verified by counting `id:` occurrences in the raw array). Fix applied: `136 → 137`. Ratio-of-confusion at dev time small but non-zero — `raw`-array insertion site needs correct landmark.
  - **Verified zero findings** on: (a) Task 1 leaf enumeration mechanically identical to schema `required` trees (~86 leaves cover root `color | type | font | space | radius | motion | density | breakpoint` + nested `color.*` + `color.status.*.{fg|bg}` + `$modes.{light,dark}` exactly — no gaps); (b) epic AC mapping all four story ACs trace to `epics.md:962-980` (story AC 4 is NARROWED for source-output-sync deferral to Story 1.13, explicit in carve-out prose; others EXPANDED with Story-1.11 scope carve-outs or MATCH); (c) all other ux-spec citations (ux-spec:358 tokens.json SOURCE OF TRUTH; ux-spec:492 accent.500 = `oklch(62% 0.16 245)`; ux-spec:528 modular 1.125 ratio; ux-spec:524-525 font stacks; ux-spec:547 space; ux-spec:551 radius; ux-spec:560 breakpoints; ux-spec:613 Direction A table; ux-spec:621 Direction A ratification); (d) all tokens.md rationale citations (:56-61 Surfaces; :65/:67 Text AAA/AA; :102-105 severity aliases; :107-114 state aliases; :116-125 Motion; :127-134 Density); (e) anchor regex pass for `INV-tokens-source` (pattern `/^-\s+\*\*\`(INV-[a-z0-9]+(?:-[a-z0-9]+)+)\`\*\*/gm` at `sync-gate.ts:24`); (f) INVARIANTS.md structure — `### Design-token schema + semantic rationale (Story 1.10)` at `:48`, `## Consumption` at `:53`, story insertion point accurately referenced; (g) manifest pre-count = 12 entries (pre-Story-1.11); post-Story-1.11 projection = 13 entries.
  - Pure prose Edits; zero AC / Task restructure; zero sprint-status mutation (state transition tracked in IP § Context only; `ready-for-dev` stays through `/bmad-dev-story` completion per BMad convention). Change Log v1.1 row appended. PR #226 CI `no checks reported` at iter-54 start; push-safe. Story 1.11 GitHub issue #35 referenced in commit trailer.
- [x] iter-53: **`/bmad-create-story` drafted `_bmad-output/implementation-artifacts/1-11-design-token-source-direction-a-baseline-with-motion-density-scales.md`; FR14n state transition `_(no story)_ → drafted` per matrix row 1.** 4 ACs / 4 Tasks / DTCG JSON ratification / Direction A value precedence rule / leafFontFamily string-vs-array deferral. Preventative three-point audit (stable-IDs / Task-vs-schema leaf diff / sprint-status wording correction). Sprint-status: `1-11-...: backlog → ready-for-dev`; `last_updated: 2026-04-20 Story-1-11-ready-for-dev UTC`. Change Log v1.0 row. Story #35 referenced.

## Context

- **Phase:** 4-implementation
- **Epic:** Epic 1 — Substrate Foundation & Machine-Enforced Invariants (Stories 1.1–1.10 done; 1.11 validated (pre-dev SM review complete) + next-up for ATDD-skip; 1.12–1.16 backlog → 5 more stories × full FR14n lifecycle remaining after 1.11 closes before EPIC_DONE)
- **Epic Branch:** `feat/story-1-8-invariants-manifest-ts-contract-exporter` (continues through Epic 1; PR #226 stays Draft until EPIC_DONE per PROMPT_build.md step 5c)
- **Story:** Story 1.11 — Design-token source, Direction A baseline with motion + density scales
- **Story File:** `_bmad-output/implementation-artifacts/1-11-design-token-source-direction-a-baseline-with-motion-density-scales.md`
- **Story State:** `validated` — FR14n matrix row 2 (`drafted → validated`) landed at iter-54 with two in-place fixes (SM-1 MEDIUM, SM-2 LOW); next iter-55 runs `/bmad-testarch-atdd` for row 3 `validated → atdd-scaffolded`.
- **GitHub Issue:** Story 1.11 tracked at **#35** (OPEN); parent Epic 1 tracked at **#9** (OPEN).
- **PR:** #226 (Draft, MERGEABLE, no-CI-checks-reported — stays Draft through Epic 1; Draft→Open at EPIC_DONE after stories 1.11–1.16 all complete FR14n lifecycle).

## Notes

- **Story 1.11 sizing projection update post-iter-54:** Two-finding pre-dev SM review at iter-54 was applied in-place (no round-trip). Best-case projection from iter-53 (7 iter floor) holds; realistic case narrows to 8–9 iter median: iter-53 drafted → iter-54 validated (applied two fixes in-place) → iter-55 ATDD-skip-scaffolded → iter-56 in-dev (single-pass) → iter-57 traced → iter-58 sm-verified → iter-59 CR-done = **7 iterations** (floor). If dev partial-completes at iter-56 or a post-dev SM finding emerges, add 1-2 iter → **8-9 iterations** (median). Epic 1 close-out timing unchanged at ~45 more iterations to EPIC_DONE halt.
- **Iter-54 Lesson — preventative-audit payoff is partial, not full.** The iter-53 preventative three-point audit (stable-IDs / task-vs-schema / sprint-status wording) caught every schema-enumeration gap + ID-shape issue, but did NOT catch: (a) internal-consistency drift between prose hue targets and representative value numerics on the same line (SM-1), (b) off-by-one line references to an adjacent in-repo file (SM-2). Expand iter-55+ preventative audits for future stories to add a **FOURTH audit**: **internal-consistency diff** — for any story line that cites BOTH a prose descriptor AND a machine-readable value, verify they match numerically. And a **FIFTH audit**: **cross-file line-number check** — for any story line citing `<file>:<line>` or `at line N` for a non-story file, verify the line number is current (files may have shifted since the story was drafted).
- **Carry-forward rules INTO Story 1.11 dev-time + SM/CR layers** (iter-43 + iter-47 + iter-51 + iter-52 + iter-54):
  - **Scoped-build optimisation** (iter-51): pre-pinned in Story 1.11 Task 3.
  - **Two-layer CR convergence doctrine** (iter-36 + iter-52): single-layer Blind-Hunter findings are default DISMISS unless corroborated. Apply aggressively at Story 1.11's CR triage iterations.
  - **Trace artefact structural inheritance** (iter-46): mirror Story 1.10's trace artefact at iter-57.
  - **Hybrid ground-(c) ATDD-skip rationale** (iter-44 + Story 1.10): cite both (c.ii) downstream-story-covers-integration + (c.iii) spec-declared-CR-substitution.
  - **Drift-smoke filter precision** (iter-45): pre-pinned in Task 4.
  - **New-untracked-file revert pattern** (iter-45): for Story 1.11, `packages/ui/tokens.json` is committed before Task 4 drift-smoke runs; `git checkout --` works for drift-revert.
  - **Internal-consistency + cross-file-line-number preventative audits** (iter-54 NEW): add to drafting checklist for Story 1.12 onwards.
- **Next iteration (iter-55) expected outcome:** `/bmad-testarch-atdd` runs in a fresh context window. Fifth cumulative ATDD-skip application expected (no test runner at Story 1.11 time). Skip-rationale file or inline IP note captures: `grounds: c.ii + c.iii; c.ii = Story 1.13 pre-commit schema-validation gate owns AC 1 integration coverage; c.iii = /bmad-code-review Blind/Edge/Acceptance fan-out is AC 2 grep-based backstop per story § Testing Standards`. State transition `validated → atdd-scaffolded`. No new files; no ATDD red-phase scaffolds added to IP § ATDD Red Phase (empty). Commit pure-IP + state transition; push after CI-clear check; exit.
- **Issue Tracking:** Story 1.11 tracked at GitHub issue **#35** (OPEN at iter-54 time). `Refs #35` in iter-54 commit trailer. `Closes #35` deferred to PR body when PR #226 transitions Draft→Open at EPIC_DONE halt. Parent Epic 1 issue **#9** — `Refs #9` optional; never `Closes` it (ralph.py transitions epic issues to Done on EPIC_DONE halt).
