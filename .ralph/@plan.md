# Implementation Plan

## NOW

- [ ] **Run `/bmad-create-story (args: "review")`** — post-dev SM requirements-satisfaction verification per § Story Lifecycle row `traced → sm-verified`. Same skill that ran pre-dev at iter-199 (`drafted → validated`); state governs intent. Forecast: 0-2 unmet-AC findings. Story 2.6 has 9 ACs all LANDED at iter-201 with STRONG substrate evidence + iteration-env-safe smokes for AC-1/2/3/4/7/8/9 (bash -n × 13; pnpm wiring count 13; env-check 3-branch exit 3/0/2; dispatcher usage; compose parse-smoke). On `sm-verified` → `/bmad-code-review (args: "2")`. On `sm-fixes-pending` → top-QUEUE fix task per matrix row. Fresh context; fresh iteration. ~small.

## QUEUE (Story 2.6 lifecycle → Epic 2 Story 2.7..2.17 → Epic 2 close-out)

- [ ] _(post-`sm-verified`)_ `/bmad-code-review (args: "2")` — CR opener per § Story Lifecycle row `sm-verified`. Forecast: 4-6 PATCH opener (user-facing-CLI class; matches Story 2.4 5-PATCH envelope; Story 2.6 broader surface but simpler per-script).
- [ ] _(post-CR-drain; QUEUE empty)_ `/bmad-code-review (args: "2")` — CR-closure gate (expected ZERO-PATCH per Epic-2 3-story precedent: 2.3 iter-171, 2.4 iter-183, 2.5 iter-197).
- [ ] _(post-Story-2.6-done; through Story 2.17-done)_ Iterate Story 2.7..2.17 through the full lifecycle — 11 remaining substrate stories spanning ralph-auto-start + claude OAuth + gh OAuth + prereq-check + shared mode + port-publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-22 per iter-197..iter-202 pre-flight — "no checks reported on the 'feat/epic-2-packaged-devbox' branch").

## BLOCKED

_(none)_

## ATDD Red Phase

_(none — Story 2.6 ATDD-skipped at iter-200 per § Story Lifecycle `validated → atdd-scaffolded` (skip branch). 16th-cumulative FR14n ATDD-skip precedent. Grounds (c)+(ii)+(iii) recorded in iter-200 IP + RALPH.md signpost. No red-phase failures to drain — Task 8 iteration-env-safe smokes + Task 9 operator-workstation recipes are shipped as implementation per Story 2.4 iter-174 + Story 2.5 iter-187 precedent. iter-202 trace gate WAIVED confirms the ATDD-skip-trace-WAIVED pairing — SEVENTEENTH cumulative pairing across Epic 1 + 2.)_

## DONE (Story 2.6 lifecycle — keep current story chain, prune after Story 2.7 starts)

- [x] iter-202: **`/bmad-testarch-trace (args: "yolo")` — Story 2.6 WAIVED.** 16th-cumulative FR14n trace-WAIVED precedent (Stories 1.7 iter-4 + 1.8 iter-3 + 1.9 iter-3 + 1.10 iter-46 + 1.11 iter-57 + 1.12 iter-64 + 1.13 iter-71 + 1.14 iter-78 + 1.15 iter-85 + 1.16 iter-92 → 2.1 iter-126 → 2.2 iter-149 → 2.3 iter-159 → 2.4 iter-175 → 2.5 iter-188 → 2.6 iter-202). 2nd user-facing-CLI class trace-WAIVED after Story 2.4. 9 P2 ACs × 13 host-side scripts; coverage NONE across all 9 (0 tests at substrate stage); gate WAIVED per hybrid ground-(a)-(b)-(c)-(d) variant-(ii)+(iii) rationale. **Iteration-env evidence STRONGER than Story 2.5** — 3 end-to-end env-check branches (no-.envrc exit 3; seeded-.envrc exit 0; bad-tmpfs-ints exit 2) + `bash -n` × 13 syntax smoke + `pnpm run` wiring-count smoke + `clean.sh` dispatcher smoke + `docker compose config --quiet` parse-smoke all PASSED. Per-AC lifecycle live smokes (AC 2-7 build/start/status/shell/logs/stop/restart/clean end-to-end) deferred to M4-Pro operator workstation per SC-13 + Story 2.4 SC-17 + Story 2.5 SC-13 backend-B live-smoke precedent. 4 trace artefacts landed under `_bmad-output/test-artifacts/traceability/`: `2-6-coverage-matrix.json` (45 KB; 9-AC matrix + gap analysis + recommendations), `2-6-e2e-trace-summary.json` (4 KB; schema-v1 machine-readable summary), `2-6-gate-decision.json` (9 KB; slim gate signal with full WAIVED rationale), `2-6-host-side-pnpm-devbox-cli-surface.md` (47 KB; Phase-1 matrix + Phase-2 inventory + Phase-3 gaps + Phase-4 heuristics + Phase-5 recommendations + Phase-6 rationale + gate decision). Story State `in-dev → traced` (IP lifecycle). Sprint-status unchanged (stays `review` — BMad-skill-internal state; IP lifecycle tracks gate chain separately). 17th cumulative ATDD-skip-trace-WAIVED pairing overall.
- [x] iter-201: **`/bmad-dev-story` — Story 2.6 host-side `pnpm devbox:*` CLI surface LANDED.** 13-script + 13-pnpm-entry + 4-docs-surface single-iter landing matching Story 2.4 iter-174 + Story 2.5 iter-187 user-facing-CLI-class precedent. **Scripts:** `build.sh`, `rebuild.sh`, `start.sh`, `stop.sh`, `restart.sh`, `clean.sh`, `shell.sh`, `attach.sh`, `status.sh`, `logs.sh`, `monitor-host.sh`, `whitelist-host.sh`, `env-check.sh` (each 0755; `set -euo pipefail`; self-rooted paths; banner + SC-5 exit-code table). **Uniform exit codes:** `0/2/3/4/5-7/8/9/10/11/124` per SC-5. **`start.sh` healthcheck poll:** `docker inspect` every 2s up to `KEEL_DEVBOX_START_HEALTHCHECK_TIMEOUT_S:-120`s; accepts `healthy|running|starting(<30s grace)`; rejects `unhealthy|exited|dead|removing|paused` immediately. **`clean.sh` three-tier:** default scoped `down --rmi local` (safe under either backend); `--with-volumes` prompts `[y/N]` + under backend B additionally requires `--force-backend-b`; `--allow-broad-prune` RESERVED no-op. Backend-B detection mirrors `benchmark.sh § detect_backend`. **`env-check.sh` (AC 8 + AR-11):** 15 required vars (active `.envrc.example` knobs); 3 positive-int-shape-validated knobs (`TMPFS_{TMP,VARTMP,LOGS}_MB`). Names-only stderr for missing vars. `start.sh` calls `env-check.sh` as pre-flight unless `KEEL_DEVBOX_START_SKIP_ENV_CHECK=true`. **Monitor semantic pin (SC-15 AI-2 carry-forward):** `pnpm devbox:monitor` = FR1a JSONL DNS-event tail per PRD `:494` + architecture `:1003`; NOT `docker stats`. **Scope-owner for DEFERs:** AR-10 operator-migration docs LANDED (SC-21, new README H3), AR-11 envvar shape validation LANDED (SC-14, unconditional), AR-12 COMPOSE_PROJECT_NAME docs polish LANDED (SC-22, unconditional). AR-7 `/run` relocation + AR-9 `/etc/*` chown NOT triggered — deferred pending operator-workstation smoke outcome per SC-19 + SC-20 conditional gate. **Sync-gate 2× fired (SC-17):** `INV-prek-prepare-lifecycle` contentHash `87f37b45…` → `5960e7c4…` (package.json +13 devbox:* entries); `INV-devbox-homedev-named-volume` contentHash `f34cb62f…` → `5e868749…` (AR-12 qualifier in hardening doc). Both manifest-update + `dist/check.js` rebuild + `keel-invariants:check-all` exit 0. **Docs landing:** packages/devbox/README.md = new H3 § Operator migration + AR-12 qualifier at § Verification + new H2 § Host-side CLI (Story 2.6); AGENTS.md = new H3 § Host-side CLI (Story 2.6) under § Devbox iteration environment. **Quality gates:** format:check ✓, lint ✓ (16), typecheck ✓ (16), keel-invariants:check-all ✓, `docker compose config --quiet` ✓. **Iteration-env-safe smokes:** bash -n × 13 OK; pnpm run count = 13; env-check no-.envrc → exit 3; seeded-.envrc → exit 0 + "15 of 15 required vars present"; bad-tmpfs-ints → exit 2 + 2 shape violations named; clean `--unknown` → exit 2 + usage; clean `--help` → exit 0 + usage. Sprint-status `ready-for-dev → review`. Story State `atdd-scaffolded → in-dev`.
- [x] iter-200: **`/bmad-testarch-atdd` → ATDD-skip (FR14n Story State `validated → atdd-scaffolded`).** 16th-cumulative FR14n ATDD-skip precedent. **Grounds (c)+(ii)+(iii):** CLI-smoke-class ACs + no live test runner + ATDD would duplicate Task 8 + Task 9 recipes shipped as implementation.
- [x] iter-199: **Ran `/bmad-create-story (args: "review")` — pre-dev SM validation of Story 2.6.** 8 findings applied as v0.2 Change Log entry (AI-1..AI-8): AI-1 CRITICAL SC-2 monitor-naming self-contradiction resolved; AI-2 CRITICAL PRD `:494` vs epics AC 7 semantic drift reconciled; AI-3 ENHANCEMENT Task 10.6 split; AI-4..AI-8 OPTIMIZATION. Story State `drafted → validated`.

## Context

- **Phase:** 4-implementation — Epic 2 at 5/17 stories `done` (2.1 + 2.2 + 2.3 + 2.4 + 2.5) + 12/17 remaining; Story 2.6 traced (review) + 11 backlog (2.7..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Story 2.6 live lifecycle smokes operator-workstation-deferred per Story 2.4 SC-17 + Story 2.5 iter-187 precedent. 5-gate quality chain for docs-surface iters: sync-gate + typecheck + lint + format:check + docker compose parse-smoke.
- **Baked image:** `keel-devbox:local` (iter-123 first bake; 848 MB, linux/arm64). Story 2.5 substrate live. Story 2.6 is a host-side CLI layer — no Dockerfile changes at iter-201/iter-202 (SC-25 scope-boundary forbids; AR-9 escalation path conditional + not triggered).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; 5 done (2.1-2.5); Story 2.6 traced (review); 11 backlog (2.7..2.17). Epic 2 closes at Story 2.17 (PR #230 Draft→Open transition at that point).
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.6 — host-side `pnpm devbox:*` CLI surface. Scope-owner for AR-7 `/run` relocation (SC-19, conditional — NOT triggered) + AR-9 `/etc/*` chown (SC-20, conditional — NOT triggered) + AR-10 pre-Story-2.5 volume migration docs (SC-21, LANDED iter-201) + AR-11 envvar shape validation (SC-14, LANDED iter-201) + AR-12 COMPOSE_PROJECT_NAME docs polish (SC-22, LANDED iter-201).
- **Story File:** `_bmad-output/implementation-artifacts/2-6-host-side-pnpm-devbox-cli-surface.md`
- **Story State:** `traced` — transitioned at iter-202 via `/bmad-testarch-trace (args: "yolo")` WAIVED gate. Story file Status = `review` (BMad-skill-internal); IP lifecycle Story State tracks the coverage/requirements/quality gate chain separately. iter-203 runs `/bmad-create-story (args: "review")` to transition `traced → sm-verified` (post-dev SM requirements-satisfaction verification).
- **GitHub Issue:** Story 2.6 TBD (will be surfaced by ralph.py at next iter if the GH project is configured). Epic 2 → #10 (Ralph closes on EPIC_DONE halt).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2).

## Notes

- **Epic-2 story-class prediction validated (iter-198 + iter-199 + iter-201 + iter-202):** Story 2.6 is user-facing-CLI class. Trace outcome WAIVED confirms iter-201 forecast. CR-opener forecast unchanged from iter-198: 4-6 PATCH. Post-dev SM review forecast: 0-2 unmet-AC findings (substrate evidence is STRONG; iteration-env-safe smokes exercise 7 of 9 ACs end-to-end — AC-5 shell + AC-6 attach are the only ACs without iteration-env-safe runtime evidence; per Story 2.4 + 2.5 post-dev SM precedent the finding count is usually 0-1).
- **Single-iter landing precedent — user-facing-CLI class (iter-201 formalises):** Story 2.4 iter-174 (4-verb whitelist CLI, 1 script) + Story 2.5 iter-187 (hardening substrate, 2 files) + Story 2.6 iter-201 (13-verb CLI + 4 docs surfaces, 19 files) are all single-iter landings. Larger Story-2.6 surface did NOT require multi-iter splitting because the per-script complexity stayed low (most scripts are <50 LOC wrappers). Carry-forward budget benchmark: ~95-105K per single-iter landing of this class.
- **Cross-source semantic-drift audit rule (iter-199 LESSON, iter-201 VALIDATED, iter-202 TRACE-RECORDED):** SC-15 pinned PRD `:494` as authoritative for `pnpm devbox:monitor` semantic. iter-201 `monitor-host.sh` implementation shipped as JSONL tail (not `docker stats`) per the pin; iter-202 trace AC-7 substrate evidence records the SC-15 monitor-semantic pin verbatim with PRD `:494` + architecture `:1003` citations as the authoritative source reconciling the epics AC 7 "cpu/memory/network" historical drift. Carry-forward rule for Stories 2.7 + 2.8 + 2.9 + 2.12 + 2.15 remains live.
- **`<verb>-host.sh` host-side shim naming convention (iter-199 LESSON, iter-201 EXTENDED, iter-202 TRACED):** Story 2.6 ships `monitor-host.sh` + `whitelist-host.sh` as the canonical host-side shim naming pattern. Stories 2.7 (Ralph TUI wrapper), 2.8 (claude wrapper?), 2.9 (gh wrapper?), 2.12 (ssh wrapper?) candidates for the same pattern when in-container primitives exist.
- **Sync-gate 2× refresh iter-201 LESSON (iter-202 inherits):** when a story edits BOTH a non-invariant file AND an invariant-sourcePath file, two hashes drift. The `pnpm keel-invariants:check` drift report enumerates all; update each hash in the manifest; single `pnpm --filter @keel/keel-invariants build`; `keel-invariants:check-all` exit 0. Carry-forward: don't assume one-hash-per-iter — scan the full drift report.
- **`bash -n` as iteration-env-safe syntax smoke (iter-201 formalises; iter-202 retains):** every script added by a Story 2.6-class CLI-family story can be smoked via `for f in <scripts>; do bash -n "$f"; done` to catch syntax errors before push. Zero side effects; works under any backend. Add this to the iteration-env-safe smokes list in every future CLI-family story's ATDD-skip grounds.
- **Story 2.6 invariant-sourcePath stability after sync-gate (iter-201 → iter-202 STABLE):** `INV-devbox-homedev-named-volume` at `5e868749…` (v1.9 Story 2.6 value). `INV-prek-prepare-lifecycle` at `5960e7c4…` (Story 2.6 value). Both values carry into Stories 2.7+ unless those stories edit the same sourcePaths (package.json edits in Stories 2.7-2.13 are likely; hardening doc edits less likely unless AR-7/AR-9 fire).
- **Iteration-env-safe smoke delta vs Story 2.5 (iter-202 LESSON):** Story 2.6 has SIGNIFICANTLY stronger iteration-env runtime evidence than Story 2.5 for user-facing-CLI class stories — 3 end-to-end env-check branches (exit 3/0/2) + bash -n × 13 + pnpm run wiring count + dispatcher usage smokes all exercise real runtime behaviour. Story 2.5 was DinD-B-blocked from ANY runtime assertion for cap-dropped-container probes; Story 2.6's host-side-bash nature dodges that constraint entirely. Carry-forward rule: for future user-facing-CLI class stories (Stories 2.7, 2.8, 2.9, 2.10, 2.11, 2.12), exercise every branch that can execute without docker lifecycle mutation AND record each branch in the trace substrate_evidence list — this produces a tighter CR opener (fewer Blind Hunter "what does this actually do?" questions).
- **Within-epic § Cross-epic transition (iter-202 trace → iter-203 SM review):** Story 2.6 `traced` + Epic 2 has 12/17 backlog stories (2.6 at `traced` review, 2.7..2.17 remain backlog) → NO halt required. Next iter runs `/bmad-create-story (args: "review")` per lifecycle matrix row `traced → sm-verified`. Epic 2 stays `in-progress`.
