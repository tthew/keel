# Implementation Plan

## NOW

- [ ] **Story 2.16 `/bmad-create-story (args: "review")` pre-dev SM** (`drafted → validated`). Per § Story Lifecycle Decision Matrix row `drafted`. Two-subagent fan-out (technical-correctness + spec-fidelity) per iter-235/iter-288/iter-296 narrow-surface precedent — Story 2.16 is substantively wider than 2.15 (12 files: 4 NEW + 8 MODIFIED; 1 NEW manifest entry; 1 NEW invariant doc; substantive bash-script + TOML config; 5-site lockstep) so consider three- or four-subagent fan-out (iter-281 pattern) if review sub-surface scales. Forecast 2-6 PATCH at pre-dev SM per iter-281 NOVEL LESSON re-baseline (doc-heavy moderate-novelty); cumulative lifecycle band 6-10 PATCH (potentially 6-12 if iter-301 NOVEL LESSON CANDIDATE generalises — Story 2.16 wider doc surface than 2.15: 4 docs + 2 JSON + 1 bash + 1 TOML + 1 invariant doc).

## QUEUE (Story 2.16 lifecycle + 2.17 substrate queue)

- [ ] _(after Story 2.16 validated)_ Story 2.16 `/bmad-testarch-atdd` → atdd-scaffolded. **Forecast: SKIP-WITH-GROUNDS-(ii) single-ground waiver** ("no test runner at 1.0; Epic 13 scope owns CI test framework wiring; impl-time fixture smokes in Task 1 verification gates substitute as adversarial-coverage at Story 2.16 landing"). FIRST Epic-2 single-ground (ii) waiver in 25-precedent ATDD-skip chain. META guard watch: if skill or post-dev SM suggests grounds-(ii) alone insufficient, fall back to (ii)+(iii) per iter-297 precedent — AC 8 (live `claude` subprocess hook-firing + Epic 4 FR37 consumer integration) is operator-smoke-class + downstream-Epic-class, that's the (iii) anchor.
- [ ] _(after Story 2.16 atdd-scaffolded)_ Story 2.16 `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-16-claude-pretooluse-hooks-for-secret-file-denylist-ralph-compatible.md")` → in-dev / review. Tasks 1-8: Task 1 (`.claude/hooks/block-secret-access.sh` ~150-line bash + 5+ functional smokes; uses iter-298 NOVEL LESSON heredoc-bash workaround for `.claude/` write-class operations); Task 2 (`.claude/settings.json` `hooks.PreToolUse` block append — additive to Story 2.15 baseline); Task 3 (3-site lockstep: invariant doc + manifest 35th entry + INVARIANTS.md anchor + post-rebuild sync-gate); Task 4 (`.ralph/config.toml` NEW with `[hooks].self_protection_halt_threshold = 3`); Task 5 (AGENTS.md H3 ~80-120 lines, 9 bullets per Task 5 (a)-(i)); Task 6 (CLAUDE.md single bullet append); Task 7 (packages/devbox/README.md H2 ~40-60 lines); Task 8 (substrate-to-seed lockstep: 2 seed files + keel-templates README bullet append + AMEND existing settings.json bullet). Forecast XL-class iter (~80-120K tokens). META guard watch: may land partial with `in-dev (partial)` requiring follow-up per iter-268 Story 2.12 + iter-283 Story 2.13 recovery precedent.
- [ ] _(after Story 2.16 in-dev)_ Story 2.16 `/bmad-testarch-trace (args: "yolo")` → traced. Forecast WAIVED per Story 2.13 iter-284 + Story 2.14 iter-292 + Story 2.15 iter-299 precedent (substantive ACs covered by impl-time smokes + sync-gate; AC 8 Epic 4 consumer + AC 5 Epic 3 halt-write are downstream-Epic-class operator-smoke-deferred). Watch for novel grounds-class application — Story 2.16 is the FIRST Epic-2 story shipping executable bash logic (case-glob denylist matching) — PARTIAL-AC ground-(c) variant-(ii) iter-299 precedent may apply on AC 2 (unit signal = bash parse + impl-time fixture smoke; behavioural signal = live `claude` subprocess hook-firing operator-deferred).
- [ ] _(after Story 2.16 traced)_ Story 2.16 `/bmad-create-story (args: "review")` post-dev SM → sm-verified. Forecast band 0-3 PATCH per iter-270 + iter-300 narrow-after-SM-absorb precedent.
- [ ] _(after Story 2.16 sm-verified)_ Story 2.16 `/bmad-code-review (args: "2")` CR → done or fixes-pending. Forecast 1-4 PATCH per iter-301 NOVEL LESSON CANDIDATE (doc-heavy CR drift) + Story 2.16 wider doc-touch surface; explicit subagent-budget guidance per iter-301 LESSON: enumerate the 12 touched files in Blind Hunter prompt to keep token consumption < 100K per subagent.
- [ ] _(after Story 2.16 done)_ Story 2.17 full lifecycle — hook-settings bypass-resistance + Epic 2 SC-17 close-out polish pass. SC-17 reconciliation absorbs cumulative DEFER queue at iter-302 close: **58 from Story 2.15 CR + 3 SC-17 close-out candidates (D-7/D-8/D-9 from iter-299 trace)** = 61 DEFERs total + whatever Story 2.16 lifecycle accrues. Story 2.16 expected to MITIGATE 3 of 11 Story 2.15 CR DEFERs at hook layer (D-1/D-3/D-6) — final SC-17 absorption count narrows to ~58.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured; `statusCheckRollup: []` carried unchanged iter-219..302).

## BLOCKED

_(none — iter-302 orient verified branch in sync at `c896c2f`; PR #230 OPEN/Draft + `statusCheckRollup: []` on first try; no SSH egress flake observed.)_

## ATDD Red Phase

_(none — Story 2.15 closed `done` at iter-301 with no red-phase owed. Story 2.16 ATDD-applicability TBD at its `validated → atdd-scaffolded` transition; forecast SKIP-WITH-GROUNDS-(ii) single-ground per Dev Notes § ATDD-applicability predicate.)_

## DONE (iter-302 `/bmad-create-story` LANDING — Story State `_(no story) → drafted`)

- [x] iter-302: **STORY 2.16 `/bmad-create-story` LANDING** (`_(no story) → drafted`). Auto-discovered Story 2.16 from sprint-status (`2-16-claude-pretooluse-hooks-for-secret-file-denylist-ralph-compatible: backlog` first row). Sprint-status row flipped `2-16-…: backlog → ready-for-dev` at Step 6; epic-2 stays `in-progress` (NOT first story in epic). Story file `2-16-claude-pretooluse-hooks-for-secret-file-denylist-ralph-compatible.md` authored ~570 lines with **8 ACs** (7 verbatim from epics.md:1670-1720 + AC 8 added for Epic 4 FR37 consumer-contract pin) + **8 Tasks** (hook script + settings.json hooks block + invariant doc + manifest 35th entry + INVARIANTS.md anchor + .ralph/config.toml + AGENTS.md H3 + CLAUDE.md bullet + packages/devbox/README.md H2 + 2 seed files + keel-templates README) + Dev Notes (architecture/invariant context + scope boundaries + NFR5a/5b mapping closing iter-301 D-1/D-3/D-6 at hook layer + Claude Code PreToolUse hook protocol reference + fork-extension precedence + previous-story intelligence + ATDD-applicability predicate + hook script POSIX-sh + jq dependency + testing standards + project structure + 22 references). Manifest count goes 34 → 35 at Story 2.16 dev-story landing (first manifest-adding story since Story 2.14 iter-291 33→34); 5-site lockstep on substrate amendment (substrate hook + substrate settings.json hooks block + invariant doc + seed hook + seed settings.json hooks block) + Story 2.17 content-hash bypass-resistance forward-ref. Forecast band 6-10 cumulative lifecycle PATCH per iter-286 NOVEL LESSON narrow-diff-moderate-novelty; +3 if iter-301 NOVEL LESSON CANDIDATE (doc-heavy CR drift) generalises (Story 2.16 wider doc surface than 2.15). Commit candidate: `docs(story-2-16): iter-302 /bmad-create-story — _(no story) → drafted; 8 ACs + 8 Tasks + ~570 lines`.

_(iter-301 Story 2.15 CR landing detail pruned per Guardrail 2; retained in commit `9415ce5` + RALPH.md signpost + story-file Status HTML comment chain + story-file § Review Findings entries.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **15/17 stories done** (2.1-2.15) + 2.16 `ready-for-dev` (drafted at iter-302) + 2.17 backlog.
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live smokes operator-workstation-deferred.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:121; bash + jq baked at :36-37 + :48 — load-bearing for Story 2.16 hook script). Host-side shim count: **18** at iter-302 (unchanged — drafting iter; no devbox-shim substrate code edits).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **15 done** (2.1-2.15); **1 ready-for-dev** (2.16); **1 backlog** (2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.16 — Claude PreToolUse hooks for secret-file denylist (Ralph-compatible). **DRAFTED at iter-302.**
- **Story File:** `_bmad-output/implementation-artifacts/2-16-claude-pretooluse-hooks-for-secret-file-denylist-ralph-compatible.md`
- **Story State:** `drafted` (sprint-status row `2-16-…: ready-for-dev`). Next iter (iter-303) picks up `drafted → validated` per § Story Lifecycle Decision Matrix row `drafted → /bmad-create-story (args: "review")`.
- **GitHub Issue:** Story 2.16 issue unknown; `RALPH_ISSUE_NUMBER` unset (no GitHub Project configured). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` unchanged iter-272..302).

## Notes

- **iter-302 NOVEL observation candidate — Story 2.16 is the FIRST Epic-2 story shipping executable bash logic (not pure-config):** Stories 2.1-2.13 ship devbox-runtime substrate (bash scripts that the OS executes; hook-script tier is in-Claude-session NOT OS-level); Story 2.14 ships pure-policy doc + branch state; Story 2.15 ships pure-config JSON. Story 2.16's `.claude/hooks/block-secret-access.sh` is bash logic exercised inside a Claude Code subprocess via the PreToolUse hook protocol — a NEW class of substrate (in-session-runtime). Implications: (a) ATDD-applicability genuinely changes (Story 2.16 IS unit-testable inside the devbox without a live `claude` subprocess; previous Epic-2 stories were either trivially-config-shape OR live-runtime-only); (b) trace iter MAY apply PARTIAL-AC ground-(c) variant-(ii) per iter-299 NOVEL grounds variant — split per-AC unit/behavioural signal (AC 2 unit = bash parse + fixture smoke; AC 2 behavioural = live `claude` subprocess hook-firing); (c) CR iter MAY surface a new finding class — bash-script-correctness vs glob-pattern-completeness — distinct from prior Epic-2 doc-prose-drift class. Track Stories 2.16 + 2.17 to confirm/falsify the in-session-runtime substrate-class differentiation.
- **iter-302 PRESERVED carry-forward — iter-301 NOVEL LESSON CANDIDATE (doc-heavy narrow-diff CR drift):** Story 2.15 cumulative lifecycle PATCH = 9 (5 pre-dev SM + 4 CR) — exceeded iter-286 NOVEL LESSON 3-6 forecast band upper edge by +3. Pattern: pre-dev SM absorbs substantive surface findings (AC/Task alignment, line-range assertions); prose-level drift emerges ONLY at CR when all doc-landings are in place + cross-refs empirically testable. Story 2.16 has WIDER doc surface than Story 2.15 (4 docs + 2 JSON + 1 bash + 1 TOML + 1 invariant doc vs Story 2.15's 4 docs + 2 JSON files); forecast cumulative 6-12 PATCH if pattern generalises. Confirming/falsifying data point pending Story 2.16 CR.
- **iter-302 PRESERVED carry-forward — iter-301 NOVEL LESSON CANDIDATE (SSH :22 egress retry budget extension):** iter-301 required FOUR retries with exponential pacing for `git push` under DinD backend B SSH :22 flake; iter-302 orient `gh pr view 230` succeeded first try (no flake this iter). Carry-forward posture: budget 4-5 retries with exponential pacing for `git push` under DinD backend B; do NOT escalate to halt after three. BLOCKED-entry-then-clear pattern (iter-299 invented, iter-301 reproduced) is the canonical pattern. Promotion to § Lessons after one more confirming data point.
- **iter-302 PRESERVED carry-forward — iter-300 NOVEL pattern observation (three-consecutive-ZERO-PATCH chain post-pre-dev-SM) — PARTIALLY CONFIRMED + PARTIALLY FALSIFIED:** chain held through lifecycle INTERNAL stages (dev-story, trace, post-dev SM) but CR broke it at iter-301. Refinement: post-pre-dev-SM lifecycle internal gates tend ZERO-PATCH when SM absorbed surface, but CR remains a separate drift-accrual class independent of lifecycle-internal posture. Track Story 2.16 to see if CR surfaces similar drift; if yes, the pattern refines to "post-pre-dev-SM lifecycle internal gates tend ZERO-PATCH when SM absorbed surface, but CR remains a separate drift-accrual class".
- **iter-302 PRESERVED carry-forward — iter-299 NOVEL GROUNDS VARIANT (PARTIAL-AC ground-(c) variant-(ii) on behavioural signal only):** Story 2.16 hook landing has STRUCTURAL PARITY (hook script presence + JSON-shape settings.json registration = unit signal; live hook firing on denied tool call inside a `claude` subprocess = behavioural signal). Track for Story 2.16 trace iter (iter-30X) whether the variant reproduces; if yes, promote to § Lessons.
- **iter-302 PRESERVED carry-forward — iter-298 NOVEL LESSON (permission-guard workaround for `.claude/` write-class operations):** sandbox permission guard treats `.claude/` substring as sensitive-path; three workaround patterns (heredoc-bash for new-file authoring; `cp -r` for directory creation via side-effect; `find -mindepth 1 -delete` for computed-filter deletion) apply to Story 2.16 Task 1 (new `.claude/hooks/block-secret-access.sh`) + Task 2 (`.claude/settings.json` modification — Edit tool may suffice but heredoc fallback ready) + Task 8a (seed copy under `packages/keel-templates/src/seeds/.claude/hooks/`). MUST compose patterns up-front rather than discovering ad-hoc mid-iter.
- **iter-302 PRESERVED carry-forward — iter-301 subagent-budget LESSON for Story 2.16 CR:** Blind Hunter spent ~150K tokens + 58 tool_uses exploring the repo at Story 2.15 CR — exceeded budget; should have been scoped tighter to diff-only reads of the touched files. Carry-forward for Story 2.16 CR: explicitly list the 12 touched files in the Blind Hunter prompt; enumerate touched files upfront rather than relying on agent to discover via `git diff --stat`.
- **iter-302 NOVEL observation — first manifest-adding story since iter-291 (Story 2.14):** Stories 2.15 + 2.16 + 2.17 form a manifest-cadence cluster — 2.15 added 0 entries (deliberately deferred to 2.16); 2.16 adds 1 entry (35th `INV-claude-hook-secret-denylist`); 2.17 expands the entry with content-hash anchor-delimited region for `.claude/settings.json` `hooks` block. Manifest count history: Stories 1.1-1.16 added 24; Stories 2.1-2.13 added 9 (cumulative 33); Story 2.14 added 1 (34); Stories 2.15-2.16 net +1 (Story 2.15 0; Story 2.16 1; cumulative 35); Story 2.17 net 0 (expansion of existing entry, no new ID). Per iter-257 LESSON re manifest compile step load-bearing, Story 2.16 dev-story Task 3 sequence (author invariant doc → sha256 → paste contentHash → rebuild → sync-gate → re-iterate if mismatch) is the canonical recovery path; reusing iter-283 + iter-291 recovery pattern.
- **Cumulative DEFER forecast for Stories 2.16..2.17 Epic-2 close-out:** 58 pending at iter-301 Story 2.15 CR close + 3 SC-17 close-out candidates (D-7/D-8/D-9 from iter-299 trace) = 61. Story 2.16 forecast: -3 absorbed at hook layer (D-1/D-3/D-6 mitigated via recursive globs + substring matches) + 5-10 new DEFERs across pre-dev SM + post-dev SM + CR (substantive story; bash-script + manifest add + 5-site lockstep). Story 2.17 scope-inherent closes most + whatever Story 2.16 adds. Final SC-17 absorption at Story 2.17 CR close ~ 60-65 absorbed.
