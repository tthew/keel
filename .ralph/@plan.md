# Implementation Plan

## NOW

- [ ] `/bmad-create-story` — Story 2.9 draft (`2-9-gh-cli-oauth-via-pnpm-gh-auth`) per § Cross-epic within-epic route (Story 2.8 `done` → QUEUE empty → sprint-status has 9 backlog rows 2.9..2.17 → auto-advance, no halt). Skill auto-picks next `backlog` story from sprint-status, writes draft, flips `backlog → ready-for-dev`. Forecast: 17-SC density target (match Story 2.8 v1.0 baseline + Story 2.7 post-PATCH precedent); ground-(c) "external-service-owns-behavior-under-test" variant inherits verbatim with GitHub CLI + `github.com` OAuth endpoint substitution. Iter-229 mechanical-inheritance pattern holds.

## QUEUE (Story 2.9 lifecycle → Stories 2.10..2.17 → Epic 2 close-out)

- [ ] _(post-Story-2.9-drafted → done; through Story 2.17-done)_ Iterate Stories 2.9..2.17 through full lifecycle — 9 remaining substrate stories spanning gh OAuth + prereq-check + shared mode + port-publication + healthcheck + legacy branch + settings + hooks + bypass-resistance. Per § Cross-epic within-epic route, auto-advance continues without halt as long as sprint-status has backlog rows.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; next iter on re-entry auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-22 per iter-219..233 repeated pre-push confirmation — `gh pr view 230` shows `statusCheckRollup: []`).

## BLOCKED

_(none)_

## ATDD Red Phase

_(none — Story 2.8 closed at iter-233 with zero ATDD scaffolds carried forward. Story 2.9 ATDD will be skip-forecast again per FR14n row 3 NINETEENTH cumulative precedent with inherited ground-(c) "external-service-owns-behavior-under-test" variant.)_

## DONE (iter-233 Story 2.8 CR CLOSURE ZERO-PATCH ONE-PASS — `sm-verified → done`; sprint-status `review → done`; v1.6)

- [x] iter-233: **STORY 2.8 CR CLOSURE ZERO-PATCH ONE-PASS — Story State `sm-verified → done`; sprint-status `review → done` (CR gate DOES flip sprint-status; contrast SM + trace gates which do not).** Skill `/bmad-code-review (args: "2")` invoked fresh-context via Ralph build-mode per § Story Lifecycle matrix row `sm-verified → done | fixes-pending`. **Three-layer adversarial triage:** Blind Hunter (diff-only, 10 findings), Edge Case Hunter (diff + read access, 12 findings), Acceptance Auditor (diff + spec + context, 1 finding). **Triage outcome:** 0 decision-needed, **0 patch**, 9 defer, 13 dismiss. **THIRD cumulative one-pass ZERO-PATCH CR precedent** (chain: Story 1.13 iter-85 first; Story 2.7 iter-226 second; Story 2.8 iter-233 third). **Nine DEFER items → deferred-work.md AR-51..AR-59:** AR-51 non-TTY vs hardcoded `-it` (SC-6 spec-authorised interactive-by-contract); AR-52 TOCTOU inspect→exec (narrow race, `*` exit code bubbles); AR-53 daemon-dies-between races collapse to exit-9 (narrow mid-flight race); AR-54 non-running container states collapse to one exit-9 message (UX refinement); AR-55 nonexistent vs stopped indistinguishable (same UX family as AR-54); AR-56 UID 1000 ≠ dev → token at wrong HOME (Story 2.5 image-contract substrate); AR-57 `/home/dev/.claude/` non-writable silently fails (Dockerfile:251-252 + SC-13 substrate); AR-58 concurrent `pnpm claude` races OAuth (first-run only, not corrupting); AR-59 `pnpm claude --help` consumed by pnpm (SC-16 + Story 2.6 AR-18 inheritance). **Thirteen DISMISS items:** `CONTAINER_NAME` metachar validation (docker CLI is canonical validator; all args quoted); banner's unused exit-2 vs AGENTS.md schema (different completeness levels, not contradictory); `log()` to stderr for informational (sibling-script convention); hardcoded `-w /workspace` (substrate invariant); remote `DOCKER_HOST` (operator-controlled); stale claude binary (compose lifecycle); `docker info --format` vs plain `docker info` sibling inconsistency (SC-1 spec-authorised tighter variant); SIGINT propagation (correct via docker-exec); File List 54→58 line-count drift (documentary-only pre-draft estimate; auditor rated not load-bearing); etc. **Auditor verdict:** fully compliant with 5/5 ACs + 17/17 SCs + § Shim structure template verbatim + File List scope boundary + substrate contracts preserved (Stories 2.1-2.7 unchanged). **Spec-vs-impl drift: ZERO** (re-confirms iter-232 post-dev SM ZERO-PATCH verdict). Story file § Review Findings appended; § Change Log v1.6 appended; Status `review → done`. Sprint-status `2-8-claude-code-oauth-via-pnpm-claude: review → done`; `last_updated: Story-2-8-done-CR-closure-ZERO-PATCH-one-pass-iter-233`. **Budget consumed iter-233:** ~70K (3 parallel subagents each ~20-25K). **Next iter:** `/bmad-create-story` fresh-context per § Cross-epic within-epic route (Epic 2 has 9 backlog rows 2.9..2.17; no halt, continue iteration).

- [x] iter-232: **STORY 2.8 POST-DEV SM VERIFIED — Story State `traced → sm-verified`; sprint-status UNCHANGED (SM review gate does NOT flip sprint-status per iter-156 + iter-221 + iter-225 precedent).** Skill `/bmad-create-story (args: "review")` invoked fresh-context. **Per-AC verdict — ZERO PATCHes surfaced:** AC 1-5 SATISFIED via substrate + ground-(c) external-service carve-out. **Spec-vs-impl drift: ZERO** — `claude-host.sh:1-58` matches § Shim structure template verbatim; all 17 SCs honoured. **SECOND cumulative post-dev SM ZERO-PATCH precedent** (Story 2.7 iter-225 first). Story file § Change Log v1.5 appended. Budget ~25K. Next iter: `/bmad-code-review (args: "2")` — SECOND one-pass ZERO-PATCH CR forecast (candidate).

- [x] iter-231: **STORY 2.8 TRACE GATE WAIVED — Story State `in-dev → traced`; sprint-status UNCHANGED (trace gate records in Change Log only per iter-202 + iter-224 precedent).** Skill `/bmad-testarch-trace (args: "yolo")` invoked fresh-context. Four artefacts produced; EIGHTEENTH cumulative trace-WAIVED precedent + NINETEENTH ATDD-skip-trace-WAIVED pairing. NEW ground-(c) variant `external-service-owns-behavior-under-test` for AC 1-5. Densest per-script smoke coverage in Epic 2 (7 smokes over 1 script = 7.0/script). Budget ~45K. Story file § Change Log v1.4 appended.

## Context

- **Phase:** 4-implementation — Epic 2 at **8/17 stories done** (2.1-2.8) + 9/17 backlog (2.9..2.17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live OAuth AC 1-5 smokes operator-workstation-deferred per ground-(c) carry-forward.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:119). Story 2.5 substrate live. Host-side shim count: **16 verbs** (13 Story 2.6 + 2 Story 2.7 + 1 Story 2.8 `claude`).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **8 done** (2.1-2.8); 9 backlog (2.9..2.17). Epic 2 closes at Story 2.17 (PR #230 Draft→Open transition at that point).
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** _(no story — Story 2.8 closed iter-233; Story 2.9 queued for next iter's `/bmad-create-story`)_.
- **Story File:** n/a (next iter creates `2-9-gh-cli-oauth-via-pnpm-gh-auth.md`).
- **Story State:** _(no story)_ — next iter action per § Story Lifecycle Decision Matrix is `/bmad-create-story` in fresh context window; § Cross-epic within-epic route auto-advances without halt (9 backlog rows remain in sprint-status).
- **GitHub Issue:** Story 2.9 unwired at iter-233 (projections: `RALPH_ISSUE_NUMBER` will remain unset through Epic 2 remaining stories per iter-229..232 env inspection pattern; skip `Refs #<story-issue>` in commits per § Issue Tracking block). Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (stays Draft across Epic 2; `statusCheckRollup: []` at iter-233 confirmation).

## Notes

- **iter-233 new lesson — "one-pass ZERO-PATCH CR compounds with dense pre-dev SM + zero-drift impl + zero-PATCH post-dev SM" (THIRD cumulative precedent):** Story 1.13 iter-85 first (4-PATCH pre-dev SM + zero-drift impl + ZERO-PATCH post-dev + ZERO-PATCH CR); Story 2.7 iter-226 second (5-PATCH + zero-drift + ZERO + ZERO); Story 2.8 iter-233 third (4-PATCH + zero-drift + ZERO + ZERO). **Forecast pattern crystallizes:** pre-dev SM PATCH density ≥ 3 AND verbatim spec-template impl AND ATDD-skip-trace-WAIVED under ground-(c) → reliable one-pass ZERO-PATCH CR closure. Below 3 PATCHes pre-dev SM, expect ≥1 PATCH at post-dev SM or CR (drift-leak). Story 2.9 candidate for FOURTH one-pass ZERO-PATCH precedent (inherit verbatim; gh CLI + `github.com` ground-(c) substitution).
- **iter-233 new lesson — "deferred-work.md AR-51..AR-59 cluster adds 9 entries, bringing substrate-polish backlog to 50+ items":** The `_lib.sh` refactor target has accreted substantial evidence (AR-48 daemon-hang timeout + AR-49 symlink `readlink -f` + AR-52 TOCTOU retry + AR-53 daemon-error disambiguation + AR-54 per-state guidance + AR-55 nonexistent-vs-stopped disambiguation). Post-Epic-2 `_lib.sh` lift is the right amortization point; do NOT extract mid-Epic-2 (SC-14 inheritance chain Story 2.6 AR-19 → 2.7 SC-14 → 2.8 SC-14 continues).
- **iter-232 post-dev SM ZERO-PATCH SECOND precedent + iter-231 trace-WAIVED EIGHTEENTH cumulative precedent + iter-230 zero-drift implementation validates iter-228's 4-PATCH SM density** — carry-forward unchanged for Story 2.9 (gh OAuth via GitHub CLI) verbatim template inheritance with ground-(c) variant swap.
- **TTY-detect at docker-exec shim (iter-214 LESSON) — Story 2.8 CONFIRMED NON-APPLICATION at CR gate:** Edge Case Hunter (iter-233) rated non-TTY invocation HIGH, but classified DEFER because SC-6 explicitly pins hardcoded `-it` ("OAuth flow IS the AC"). Same deferral posture as Story 2.7's `docker attach` (SC-8). Pattern holds for Story 2.9 gh OAuth (interactive-by-contract).
- **`<verb>-host.sh` host-side shim naming convention (iter-199 EXTENDED, iter-219 3-PASS-CR-VERIFIED, iter-223 LANDING, iter-227 EXTENSION, iter-228 VALIDATED, iter-229 ATDD-SKIPPED, iter-230 LANDED, iter-231 TRACE-WAIVED, iter-232 SM-VERIFIED, iter-233 CR-CLOSED):** Story 2.8 `claude-host.sh` ships substrate-quality under `packages/devbox/scripts/`, 16 host-side shims total. Stories 2.9/2.12 remain candidates for the same pattern. Forecast: Story 2.17 landing → 17+ total shims → post-Epic-2 `_lib.sh` refactor threshold triggered.
