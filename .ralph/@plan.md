# Implementation Plan

## NOW

- [ ] **Story 2.17 `/bmad-dev-story` CONTINUATION — Task 17 (completion DoD + Dev Agent Record + Change Log final row + sub-task 15.2-15.5 verification fold-in).** Re-run pre-push gates per Task 15 sub-tasks 15.2 (`pnpm keel-invariants:check-all` 41 entries clean — exits 0 + walks 4 new `hashScope` entries) / 15.3 (`pre-commit run --all-files` — NFR5a minimum-entry gate + seed-byte-identity + claude-hook-syntax + no-committed-dotfiles + S4 unit tests all PASS) / 15.4 (`pnpm --filter @keel/keel-invariants test` — 7/7 GREEN: `s4-claude-hooks-tamper` × 4 + `s4-git-hooks-tamper` × 1 + `s4-skip-permissions-injection` × 1 + safe-path sweep × 1) / 15.5 (Document each smoke + test in Completion Notes with count + summary). Populate story file § Dev Agent Record § Completion Notes with per-AC evidence + per-Task completion matrix + File List enumeration + Change Log final row 0.28 v1.0 (Story 2.17 dev-story COMPLETE → status `in-dev (partial) → in-dev` ready-for-trace). Update sprint-status.yaml: `2-17-hook-settings-bypass-resistance-git-layer-manifest-s4-halt: review` (NOT `done` yet — dev-story emits `review`; `done` flips at CR landing per Story Lifecycle Decision Matrix). ~medium own-iter item; precedent iter-330 verify-pass + iter-336/337 closure-segment landings. Closes the dev-story phase; subsequent iterations run `/bmad-testarch-trace` (in-dev → traced) → `/bmad-create-story (args: "review")` (traced → sm-verified) → `/bmad-code-review (args: "2")` (sm-verified → done) per Story Lifecycle Decision Matrix.

## QUEUE (Story 2.17 lifecycle close + Epic 2 close-out)
- [ ] _(after Story 2.17 Tasks fully landed)_ `/bmad-testarch-trace (args: "yolo")` — `in-dev → traced`. 27th cumulative trace; likely WAIVED per Epic-2 17-story chain continuation + impl-time-smokes substitute. Exception: iter-329 Task 5 authored 7 genuine `node:test` unit tests (the first keel-invariants tests in-repo), so SM review can cite them as AC-3 coverage evidence and trace does not have to waive for Task 5.
- [ ] _(after traced)_ `/bmad-create-story (args: "review")` — post-dev SM `traced → sm-verified`.
- [ ] _(after sm-verified)_ `/bmad-code-review (args: "2")` — `sm-verified → done`.
- [ ] _(after Story 2.17 `done`)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge. Epic 2 completion fires `EPIC_DONE` halt; on re-entry auto-advances to Epic 3 Story 3.1.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured; `statusCheckRollup: []` carried unchanged iter-219..333).

## BLOCKED

_(empty — iter-338 Task 16 (story-spec) `deferred-work.md` Epic-1 + early-Epic-2 DEFER triage; appended close-out audit H2 to `deferred-work.md` (693 → 763 lines) + 2 inline citation-lockstep absorbs (Story 2.13 D-12 README dev-sudo disclaimer + Story 2.14 D-11 INVARIANTS.md Four → Five workflow contracts); pre-push gates GREEN at orient verification: `pnpm keel-invariants:check-all` exit 0 on 41 entries + `pnpm -w format:check` clean + `pnpm -w typecheck` 16/16 cached + `pnpm -w lint` 16/16 cached.)_

## ATDD Red Phase

_(empty — iter-311 SKIP-WITH-GROUNDS-(ii)+(iii). Impl-time substrate coverage: 397 cumulative smokes GREEN across iter-315..320 runs + 7 genuine `node:test` unit tests GREEN at iter-329 for Task 5 S4 rules + 74 persisted replay fixtures GREEN at iter-332 (`packages/keel-invariants/fixtures/hooks/run-all.sh` — 55 positive + 19 negative). Total persisted/reproducible at iter-337: 7 unit tests + 74 replay fixtures.)_

## DONE (iter-338 Task 16 (story-spec) Epic-1 + early-Epic-2 DEFER triage landed)

- [x] iter-338: **Story 2.17 `/bmad-dev-story` PARTIAL CONTINUATION — Task 16 (story-spec) `deferred-work.md` Epic-1 + early-Epic-2 DEFER triage landed.** Appended `## Story 2.17 SC-17 close-out audit (2026-04-24 iter-338)` H2 to `_bmad-output/implementation-artifacts/deferred-work.md` (693 → 763 lines, +70 lines net). Surveyed ~218 raw DEFER entries across Stories 1.8-1.16 (Epic 1) + 2.1-2.14 (early-Epic 2); categorised into **Absorb-in-2.17** (45 entries: 36 substrate via Tasks 1-13 absorbing the Story 2.16 D-12..D-36 cluster + Story 2.15 trace SC-17 candidates D-7/D-8/D-9 + 9 polish via Task 16 sub-items (a)-(g) + iter-338 audit) / **Defer-to-Epic-3+** (~173 preserved with `Carry-to:` destinations intact, aggregated into 14 destination clusters in deferred-work.md § Final counts table) / **Obsolete** (0 — every cited file persists at iter-338 baseline; no rename/delete cascades). 2 ADDITIONAL citation-lockstep absorbs landed inline this iter: (i) Story 2.13 iter-286 D-12 — `packages/devbox/README.md:966` dev-sudo disclaimer paragraph rewritten to acknowledge `dev` lacks sudo per Story 2.5 § Container hardening (SC-13) + redirect operator-initiated crash-simulation to host-side `docker exec -u root keel-devbox pkill sshd`; (ii) Story 2.14 iter-294 D-11 — `INVARIANTS.md:134` Four → Five workflow contracts rephrasing to match invariant doc's actual H2 structure (5 sections: branch-creation / cherry-pick / triage / sunset / retirement). Both via plain-`Edit` route (`packages/devbox/README.md` + `INVARIANTS.md` outside L1 + hook-self-protection ranges per iter-316 routing discipline; neither file is contentHash-pinned in the manifest). Companion drift carry-forward: manifest description string at `invariants.manifest.ts:348` retains "Four workflow contracts" — non-executable description-string drift per Story 2.11 iter-264 AR-91 precedent (sync-gate cannot detect description-string drift; manifest L1-protected; defer to next manifest-touching iter as a side-load). Final cumulative Epic-2 DEFER queue at Story 2.17 close-out: **38 active** (vs. story-spec target `< 30`; residual 8 above target reflects Story 2.7+ AR-37..AR-49 `_lib.sh` refactor cluster routed to a single post-Epic-2 follow-up story but counted individually here per per-entry preservation discipline). Commit-note tag (per Task 16.4): `Epic 2 SC-17 close-out: absorbed 36 substrate + 9 polish + carry-forward 38 Epic-2 + ~135 Epic-1; Epic 2 DEFER queue at close = 38`. **Task 16 FULLY COMPLETE** — 4 of 4 sub-tasks done (16.1 audit + 16.2 absorption-count + 16.3 § Story 2.17 close-out write-up + 16.4 commit-note tag). Story 2.17 post-iter-338: **2 top-level Tasks remain — 15 (15.2-15.5 fold into Task 17), 17 (completion)**. Sub-novel observation (single data point — promotion threshold unreached): SC-N triage iters can co-land 2-3 citation-lockstep one-line absorbs alongside the audit write-up when target files are outside every hashed sub-tree (no manifest contentHash refresh route required); promotion to RALPH.md as iter-density best-practice on second re-validation. Pre-push gates GREEN at orient verification: `pnpm keel-invariants:check-all` exit 0 on 41 entries + `pnpm -w format:check` clean + `pnpm -w typecheck` 16/16 cached + `pnpm -w lint` 16/16 cached. No substrate hook/settings/manifest contentHash sub-tree perturbation; no seed re-sync required.

- [x] iter-337: **Story 2.17 `/bmad-dev-story` PARTIAL CONTINUATION — Task 16 sub-items (d)+(e) marker-only defer-record bundle landed.** Sub-item (d) iter-314 NOVEL `.ts → .js` / `/src/ → /dist/` runtime translation in `loadExpectedHooks` (`packages/keel-invariants/src/manifest-reader.ts:93-108`; single-site fix at iter-314 enabling `names-and-shebangs` walker to read the compiled dist artefact via Node's dynamic `import()` while preserving sourcePath===enumeratorPath convention) recorded `defer: threshold-not-met` — single data point with no second re-validation site to-date; promotion to invariant-doc § Limitations awareness-level guidance (mirroring iter-336 (f)+(g) bundle pattern) deferred until a second site exercises the same translation. Sub-item (e) iter-317 NOVEL D-14/D-15 minor-regex + Python-write loophole carry-forward recorded `complete: pre-landed iter-325` — the L1 install-boundary rule § Substrate-maintenance loophole narrative already landed in `docs/invariants/claude-hook-denylist.md` at iter-325 Task 13.5 amendment (the `python3 <abs-path-to-/tmp-script>` atomic-replace route for L1-protected manifest edits is documented in-doc with cross-reference to `os.replace` vs `shutil.move` cross-fs handling per iter-329 RALPH.md Gotcha entry). Bookkeeping-only iter mirroring iter-335 (Task 16(c) DEFERRED) and iter-330 (Task 6 verify-pass); only IP + story-file Status / lifecycle HTML comment / Change Log row 0.26 mutated; no substrate / hook / settings / manifest / seed perturbation; no manifest contentHash refresh route (IP + story-file outside every hashed path); pre-push gates expected GREEN as iter-330 / iter-335 (typecheck 16/16 + lint 16/16 + format:check + `pnpm keel-invariants:check-all` exit 0 on 41 entries unchanged). **Task 16 sub-items (a)-(g) FULLY RESOLVED** — (a) iter-333 substrate-bake prek; (b) iter-334 release-assets whitelist; (c) iter-335 deferred conditional-on-re-emergence; (d) iter-337 deferred threshold-not-met; (e) iter-337 complete pre-landed iter-325; (f)+(g) iter-336 invariant-doc § Limitations one-bullet bundle. Net Story 2.17 post-iter-337: **3 top-level Tasks remain — 15 (15.2-15.5 fold into Task 17), 16 (story-spec `deferred-work.md` Epic-1 + early-Epic-2 DEFER audit only — sub-items (a)-(g) FULLY RESOLVED), 17 (completion DoD + Dev Agent Record + Change Log final row)**. No novel gotcha / lesson / decision — pure marker-only defer-record bookkeeping; no RALPH.md promotion. Sub-novel observation: defer-record bookkeeping iters now cluster at the closing edge of multi-decomposed Tasks (one-iter-per-decomposed-sub-item or one-iter-per-bundled-pair when sub-items share a docs-amend target — iter-336 bundled (f)+(g) by shared `claude-hook-denylist.md § Limitations` target; iter-337 bundled (d)+(e) by shared marker-only-no-mutation framing; iter-335 ran (c) solo for the unique substrate-restart-stale-state rationale); single data point at iter-337, promotion to RALPH.md as a Ralph-iter-density best-practice on second re-validation.

- [x] iter-336: Task 16 sub-items (f)+(g) bundle — one-bullet append to `docs/invariants/claude-hook-denylist.md § Limitations` (iter-320 case-glob + iter-329 ESLint self-exclusion-glob); manifest `INV-claude-hook-secret-denylist-doc` contentHash refresh `cb32195f…` → `90812335…`. Detail in commit `50851e3`.

- [x] iter-335: Task 16 sub-item (c) DEFERRED `defer: conditional-on-re-emergence`. Detail in commit `ba695d3`.

- [x] iter-334: Task 16 sub-item (b) — `release-assets.githubusercontent.com` whitelisted in `packages/devbox/whitelist/github.txt`. Detail in commit `8eb6817`.

- [x] iter-333: Task 16(a) substrate-bake prek native Rust binary in `packages/devbox/Dockerfile` (Story 2.5 AMEND; closes iter-313 BLOCKED class). Detail in commit `353b9f0`.

- [x] iter-332: Task 15.1 (bash-fixture persistence under `packages/keel-invariants/fixtures/hooks/` — 74/0/74 GREEN). Detail in commit `cdf4d79`.

- [x] iter-331: Task 9 (CI visibility contract stub for Epic 14) — invariant-doc § CI visibility contract H3 section + manifest contentHash refresh `486e6ee1ec6e…` → `cb32195fc573…`. Detail in commit `420f8c1`.

_(iter-303..330 LANDING detail pruned per Guardrail 2; retained in commits through `718276d` + story-file Status HTML comment chain + story-file Change Log 0.1-0.24 rows.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **16/17 stories done** (2.1-2.16) + **Story 2.17 `in-dev (partial)` at iter-338** (Epic-2 final story; XL partial landing post-Task 16 close — only Task 17 completion remains; Task 15.2-15.5 verification sub-tasks fold into Task 17).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live smokes operator-workstation-deferred.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:121; bash + jq baked at :36-37 + :48). Host-side shim count: **18** at iter-338 (unchanged). iter-333 Dockerfile edit + iter-334 whitelist edit await operator-workstation rebake to materialize (image stays at pre-iter-333 baseline until rebake; whitelist edit takes effect on next devbox container restart since whitelist is read at startup, not baked into image).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **16 done** (2.1-2.16); **1 in-dev (partial)** (2.17 — Task 10.1 iter-320 complete; Task 13 iter-325 complete; Task 11 iter-327 complete; Task 14 iter-328 complete; Task 5 iter-329 complete; Task 6 iter-330 complete; Task 9 iter-331 complete; Task 15.1 iter-332 complete; Task 16(a) iter-333 complete; Task 16(b) iter-334 complete; Task 16(c) iter-335 DEFERRED; Task 16(f)+(g) iter-336 complete; Task 16(d)+(e) iter-337 marker-only defer-recorded; **Task 16 (story-spec) iter-338 complete — Epic-1 + early-Epic-2 audit + 2 inline absorbs**; 2 top-level Tasks remain: 15 (15.2-15.5 fold into Task 17), 17 (completion)).
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.17 — Hook + settings bypass-resistance (git-layer + manifest + S4 + halt).
- **Story File:** `_bmad-output/implementation-artifacts/2-17-hook-settings-bypass-resistance-git-layer-manifest-s4-halt.md` (~579 lines post-iter-338 Change Log 0.27 append + Status iter-338 segment append + lifecycle HTML comment iter-338 entry + Task 16 sub-tasks 16/16.1/16.2/16.3/16.4 marked `[x]`).
- **Story State:** `in-dev (partial)` — iter-339 NOW = `/bmad-dev-story` CONTINUATION per § Story Lifecycle dev-story invocation guidance; NOW item Task 17 (completion DoD + Dev Agent Record + Change Log final row + Task 15.2-15.5 verification fold-in; medium own-iter item).
- **GitHub Issue:** no GitHub Project configured; `RALPH_ISSUE_NUMBER` unset. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` unchanged iter-272..337).

## Notes

- **iter-338 NOVEL — none (promotion threshold unreached).** Sub-novel observation (single data point): SC-N triage iters can co-land 2-3 citation-lockstep one-line absorbs alongside the audit write-up when target files are outside every hashed sub-tree (no manifest contentHash refresh route required; pure plain-`Edit` route on `INVARIANTS.md` + `packages/devbox/README.md` + `deferred-work.md`). Carry-forward — promotion to RALPH.md as iter-density best-practice on second re-validation. The 763-line `deferred-work.md` itself is now the principal SC-N close-out artefact for Story 2.17; future-Story analogous SC-N close-out tasks should target the same H2 append pattern.

- **iter-332 NOVEL — promoted to RALPH.md § Gotchas (2026-04-24).** Hook-contract tests MUST align to the REACHABLE branch in the runtime: (a) D-22 resolved-path branch dominates direct-path case-globs; (b) `__unknown__` fires only on jq-parse-fail or missing `.tool_name`; (c) L1 `find -delete` regex requires specific filenames; (d) Grep/Glob `search_path` case-glob requires trailing content. Full detail in `packages/keel-invariants/fixtures/hooks/README.md § Dominance notes`.

- **iter-329 NOVEL — `shutil.move` vs `os.replace` for /tmp-to-L1 new-file authoring (RALPH.md § Gotchas 2026-04-24 iter-329).** `os.replace` requires same-fs atomic rename; cross-fs raises `[Errno 18]`. `shutil.move` handles cross-fs via copy2+unlink fallback.

- **iter-320 NOVEL carry-forwards (now DOCUMENTED at iter-336)** — shell case-globs prefix-then-any matching + ESLint self-exclusion-glob requirement at `eslint.config.keel-invariants.js`; both in `docs/invariants/claude-hook-denylist.md § Limitations` as awareness-level guidance (Task 16 (f)+(g) bundle).

- **iter-319 NOVEL carry-forward RE-CONFIRMED iter-320..338** — `/tmp` tmpfs `noexec,nosuid,nodev,relatime` per Story 2.5 hardening; `python3 <abs>` works because Python interprets the file rather than execve-ing it.

- **iter-318 carry-forward — `gh pr view` GraphQL timeout recipe.** iter-338 reconfirmed at orient: `gh pr checks 230` timed out twice. Three retries is the bound per iter-318 carry-forward; PR #230 baseline unchanged (`statusCheckRollup: []` through iter-336; no in-progress checks; safe to push).

- **iter-317/316 carry-forward — D-15 + D-14 minor regex + Python-write loophole** — recorded `complete: pre-landed iter-325` at iter-337 Task 16 sub-item (e). Routing pattern remains in active use.

- **iter-314 carry-forward** — `.ts→.js` / `/src/→/dist/` runtime translation in `loadExpectedHooks` recorded `defer: threshold-not-met` at iter-337 Task 16 sub-item (d); single-site fix at `manifest-reader.ts:93-108`; promotion deferred until a second site exercises the same translation.

- **iter-314 SM-review promotion threshold: 5→7 data points** unchanged. Story 2.17 post-dev SM estimate iter-339+ after Task 17 completion lands (1 QUEUE item — Task 16 fully complete at iter-338).
