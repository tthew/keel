# Implementation Plan

## NOW

- [ ] **Story 2.17 `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-17-hook-settings-bypass-resistance-git-layer-manifest-s4-halt.md")`** (`atdd-scaffolded → in-dev`). Per § Story Lifecycle Decision Matrix row `atdd-scaffolded`. XL-class (~80-120K tokens) forecast — 17 Tasks + schema extension + 4 new manifest entries (35→39) + 3 S4 rules + ≥25 impl-time smokes + 2 seed syncs + ~15 doc updates + 36-DEFER primary absorption + 15-25 SC-17 polish absorption. MAY land partial (`in-dev (partial)`) requiring Task-split across 2 iters per iter-268/iter-283 recovery precedent — flag pre-emptively if budget pressure emerges at Task 10 (D-12..D-36 hook rule-expansion). Task ordering discipline (iter-305 NOVEL LESSON): apply D-groups in small increments (D-12/D-13/D-35 anchoring first; D-14/D-15/D-16 mutation-path coverage second; etc.) with `bash -n` + `dash -n` after each group; recover via Monitor escape-hatch if hook self-immolation bricks tool surfaces. Permission-guard workaround for `.claude/` write-class (iter-298 LESSON): compose heredoc-bash + `cp -r` + `find -mindepth 1 -delete` patterns up-front.

## QUEUE (Story 2.17 lifecycle + Epic 2 close-out)
- [ ] _(after in-dev / review)_ `/bmad-testarch-trace (args: "yolo")` — `in-dev → traced`. 27th cumulative trace; likely WAIVED per Epic-2 16-story chain continuation + impl-time-smokes substitute; MAY apply PARTIAL-AC ground-(c) variant-(ii) on AC 3/AC 4/AC 8 behavioural signals per iter-299 Story 2.15 NOVEL grounds variant.
- [ ] _(after traced)_ `/bmad-create-story (args: "review")` — post-dev SM `traced → sm-verified`. Forecast per Story 2.16 iter-307 ZERO-PATCH baseline; refinement of "narrow-after-SM-absorb" 5-data-point → 6-data-point STABLE LESSON promotion at iter-313+ post-dev SM gate.
- [ ] _(after sm-verified)_ `/bmad-code-review (args: "2")` — `sm-verified → done`. Forecast 2-6 PATCH absorbed inline (Story 2.16 CR hit 2; Story 2.15 hit 4; Story 2.17's wider surface suggests upper band). Sub-tree-hash design + schema extension + 3-layer defense = larger adversarial surface than prior Stories' single-layer deltas. Expect 25-40 new DEFERs if Epic-2 SC-17 close-out polish pass is exhaustive; target absorbed-in-2.17 count: 15-25 of those; remainder carry-forward to Epic 3+ substrate code.
- [ ] _(after Story 2.17 `done`)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured; `statusCheckRollup: []` carried unchanged iter-219..311).

## BLOCKED

_(none — iter-311 ATDD-skip landed cleanly at Step 1 § 2 Prerequisites HALT; no framework-prereq pathology; no `gh pr view` retries required this iter.)_

## ATDD Red Phase

_(empty — iter-311 SKIP-WITH-GROUNDS-(ii)+(iii) means no red-phase scaffolds authored. Task 15.1's ≥25 impl-time fixture smokes + Task 1.4/3.4/5.5/7.5/11/12.3 subtask-level smokes + unit tests stand in as substrate-coverage substitute per Story 2.16 iter-306 NOVEL LESSON.)_

## DONE (iter-311 `/bmad-testarch-atdd` SKIP-WITH-GROUNDS-(ii)+(iii) — Story State `validated → atdd-scaffolded`)

- [x] iter-311: **STORY 2.17 `/bmad-testarch-atdd` SKIP-WITH-GROUNDS-(ii)+(iii)** (`validated → atdd-scaffolded`). **27th cumulative ATDD-skip precedent** (10 Epic-1 + 17 Epic-2; Story 2.17 is Epic-2 final). Skill HALTed cleanly at Step 1 § 2 Prerequisites — no test framework configured: `ls playwright.config.* cypress.config.* vitest.config.* jest.config.* pyproject.toml pom.xml build.gradle go.mod Cargo.toml Gemfile` all empty; `find . -maxdepth 3 -name "*_test.go" -o -name "conftest.py" -o -name ".rspec"` empty; `packages/keel-invariants/package.json` has NO `scripts.test` key + no vitest/jest/mocha/@playwright/test devDependencies (Story 2.17 Tasks 1.4/3.4/5.5/11.1/11.2/12.3 mention unit tests but wiring is Epic 13 scope). **Grounds (ii)+(iii) hybrid applied:** (ii) Epic 13 horizon — CI test framework wiring is Epic 13 PRD scope (~10 epics downstream from Epic 2); (iii) multi-downstream-epic-deferred ACs — AC 3 S4 scanner binary → Epic 4 Story 4.x (story file § Scope boundaries #12); AC 4 halt-write → Epic 3 Story 3.7 (§ Scope boundaries #11); AC 8 CI visibility dashboard → Epic 14 (§ Scope boundaries #14); AC 6 live L1 in-session verification → operator-workstation-deferred; AC 5 fork-operator AMEND PR flow → operator-interactive. **Fallback from variant-(ii) solo to (ii)+(iii) hybrid per iter-304 NOVEL LESSON rule:** Story 2.17 lacks an explicit § ATDD-applicability predicate section (grep empty) naming Epic 13 scope verbatim — the iter-304 LESSON pinned "broader-horizon variant-(ii) applications require an AC-level-pinned forecast clause in the story file's § ATDD-applicability predicate naming the downstream epic + its scope; without this pin, the grounds-(ii) application degrades toward bare ground-(b) insufficiency." Story 2.17's § Scope boundaries #11-15 supply the (iii) anchor directly (Epic 3/4/14 + operator-interactive deferrals) but DO NOT name Epic 13 → (ii)-solo bar unmet → (ii)+(iii) hybrid is the correct grounds choice, aligning with Story 2.14 iter-289 + Story 2.15 iter-297 (ii)+(iii) precedent. **Impl-time fixture smokes substrate-coverage substitute** (Story 2.16 iter-306 NOVEL LESSON pattern reconfirmed): Task 15.1 plans ≥25 bash-fixture hook smokes + Task 1.4 8 tmpdir sync-gate fixtures + Task 3.4 prek-hook-manifest fixture + Task 5.5 3 positive + 2 negative S4 rule fixtures + Task 7.5 5 L1-install-boundary smokes + Task 11.1-11.3 seed-byte-identity + NFR5a-minimum fixtures + Task 12.3 claude-hook-syntax fixture = ≥44 cumulative impl-time static probes planned. No red-phase scaffolds authored (consistent with 26 prior ATDD-skip precedents). **Sprint-status UNCHANGED at `2-17-…: ready-for-dev`** (atdd-scaffolded is lifecycle-internal per iter-282/iter-289/iter-297/iter-304 precedent; sprint-status.yaml flip to `review` happens at dev-story landing iter-312+). Story-file Status HTML comment (line 5) appended with `atdd-scaffolded (iter-311 SKIP-WITH-GROUNDS-(ii)+(iii); 27th cumulative ATDD-skip; 17th Epic-2)` lifecycle annotation; no Change Log row (ATDD-skip is substantive-less lifecycle transition per iter-282/iter-289 convention); no separate `atdd-checklist-{story_key}.md` artefact generated (skill HALT before Step 7 save — matches 26 prior precedents). Commit candidate: `docs(story-2-17): iter-311 /bmad-testarch-atdd SKIP-WITH-GROUNDS-(ii)+(iii) — validated → atdd-scaffolded; 27th cumulative ATDD-skip; impl-time-smokes substrate-coverage substitute`.

_(iter-303 / iter-304 / iter-305 / iter-306 / iter-307 / iter-308 / iter-309 / iter-310 LANDING detail pruned per Guardrail 2; retained in commits `441d710` / `7c89ff1` / `128e1b7` / `efde582` / `dfe14fd` / `1fa09ff` / `04cca7e` / `3d3105f` + story-file Status HTML comment chain + story-file Change Log 0.1-0.2 rows.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **16/17 stories done** (2.1-2.16) + **Story 2.17 `atdd-scaffolded` at iter-311** (Epic-2 final story; SKIP-WITH-GROUNDS ATDD).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live smokes operator-workstation-deferred.
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:121; bash + jq baked at :36-37 + :48). Host-side shim count: **18** at iter-311 (unchanged — SKIP-WITH-GROUNDS ATDD iter; no substrate code edits this iter).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **16 done** (2.1-2.16); **1 atdd-scaffolded** (2.17). Epic 2 closes at Story 2.17 done.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.17 — Hook + settings bypass-resistance (git-layer + manifest + S4 + halt).
- **Story File:** `_bmad-output/implementation-artifacts/2-17-hook-settings-bypass-resistance-git-layer-manifest-s4-halt.md` (479 lines post-iter-311 lifecycle HTML-comment annotation).
- **Story State:** `atdd-scaffolded` — iter-312 NOW = `/bmad-dev-story (args: "_bmad-output/implementation-artifacts/2-17-hook-settings-bypass-resistance-git-layer-manifest-s4-halt.md")` (atdd-scaffolded → in-dev) per § Story Lifecycle row `atdd-scaffolded`.
- **GitHub Issue:** no GitHub Project configured; `RALPH_ISSUE_NUMBER` unset. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` unchanged iter-272..311).

## Notes

- **iter-311 NOVEL observation (carry-forward reinforcement, not a new lesson) — Story 2.17 (ii)+(iii) fallback validates iter-304 LESSON rule mechanics.** Story 2.16 iter-304 pioneered variant-(ii)-solo broader-horizon waiver with an explicit § ATDD-applicability predicate naming Epic 13 (story file line 430 verbatim). Story 2.17 iter-311 tests the rule by LACKING that predicate — grep for "ATDD-applicability" in the story file returns empty. Per iter-304 rule this correctly forces fallback to (ii)+(iii) hybrid, aligning Story 2.17 with the broader 26-chain precedent rather than the single Story 2.16 outlier. **No new lesson; rule mechanics confirmed functional.** Future-Ralph carry-forward: if a story wants variant-(ii) solo, it MUST include a § ATDD-applicability predicate in Dev Notes naming the downstream epic scope; without the pin, (ii)+(iii) hybrid is the default.
- **iter-311 NOVEL DATA POINT — 27th cumulative ATDD-skip chain sustained; 17th Epic-2; Epic-2 exit at 17/17 projected.** The 26-chain + Story 2.17 makes Epic 2 a **17/17 ATDD-skip epic**. Pattern stabilises: Epic 2 is substrate-as-infrastructure (devbox / hooks / settings / manifest) — NOT feature-as-behaviour. CI test framework (Epic 13) is the first epic where red-phase ATDD genuinely applies. Track Epic 3 (Ralph loop) + Epic 4 (S3/S4 scanners) + Epic 13 (CI harness) for ATDD-skip rate shift. Epic 3 MAY continue skip chain (Ralph runtime substrate) or MAY trigger first ATDD-scaffold iter (if Story 3.x owns user-story behavioural ACs). Promotion threshold for "Epic 2 17/17 ATDD-skip epic" STABLE LESSON: Epic 2 close-out at Story 2.17 CR land + retrospective.
- **iter-310 PRESERVED carry-forward — narrow-after-SM-absorb 5→6-data-point STABLE LESSON promotion eligibility:** iter-310 SM absorbed 4 PATCH (at the upper bound of iter-309's 3-5 forecast); this is the 6th data point in the chain but does NOT meet the "<4" threshold for narrow-after-SM-absorb STABLE LESSON. Promotion now deferred to 7-data-point threshold at Story 2.17 post-dev SM (iter-313+).
- **iter-310 PRESERVED carry-forward — HIGH-priority PATCH class: filter-semantics + ID-rename contradictions** (4 PATCH detail pinned in iter-310 DONE entry; commit `3d3105f`). Detection patterns generalisable for future stories with invariants-manifest extensions + ID-rename ops.
- **iter-309 PRESERVED carry-forward — comprehensive-research-subagent for XL drafting iter promotion threshold:** Story 2.17 dev-story at iter-312+ will confirm the pattern under XL implementation scope (not just drafting). Promotion eligible at iter-314+ after dev-story lands.
- **iter-309 PRESERVED carry-forward — `hashScope` schema extension architectural decision:** three `kind` variants (`jq-subtree` / `anchor-range` / `names-and-shebangs`) cover three distinct fork-extension semantics. Promotion from story Dev Notes to `docs/invariants/fork.md` § Content-hash scoping at dev-story iter-312 close.
- **iter-309 PRESERVED carry-forward — iter-301 NOVEL LESSON CANDIDATE (SSH :22 egress retry budget extension):** iter-302..310 first-try successes + 1 retry at iter-308 + iter-311 `gh pr view` NOT attempted this iter (ATDD-skip iter; no PR checks). 7-iter baseline window with 6 first-try + 1 one-retry. **PROMOTION to § Lessons STABLE eligible at next iter where `gh pr view` is exercised.** Carry-forward posture: budget 4-5 retries with exponential pacing for `gh pr view` / `git push` under DinD backend B; do NOT escalate to halt after three.
- **iter-310 PRESERVED carry-forward — iter-305 NOVEL LESSON (hook self-immolation + pre-install `bash -n` discipline):** Story 2.17 Task 12 codifies this as a pre-commit invariant gate (`check-claude-hook-syntax.ts`) — transforming the iter-305 LESSON from Ralph-private journal entry to machine-enforced substrate contract. If Task 12 lands cleanly at Story 2.17 dev-story (iter-312), the lesson graduates from RALPH.md (private) to `docs/invariants/claude-hook-denylist.md § Pre-install discipline` (substrate) — maximally strong promotion class.
- **iter-310 PRESERVED carry-forward — iter-301 NOVEL LESSON CANDIDATE (doc-heavy narrow-diff CR drift) fate pending Story 2.17 CR:** Story 2.16 CR PATCH = 2 (undercut hypothesis). Story 2.17 CR will be the 6-data-point threshold for "narrow-after-SM-absorb ZERO-PATCH + CR separate drift class" STABLE LESSON promotion. **iter-310 SM absorbed 4 PATCH (upper-bound of 3-5 forecast) — CR forecast: 2-6 PATCH unchanged.**
- **Cumulative DEFER forecast for Story 2.17 Epic-2 close-out:** 83 cumulative Epic-2 DEFERs at iter-308 close (55 carry-forward + 25 new + 3 SC-17 candidates). Primary-scope absorption at Story 2.17 dev-story: 36 (25 hook-layer + 8 permissions-layer + 3 trace). Secondary SC-17 polish absorption at CR or dev-story Task 16: 15-25. Expected carry-forward to Epic 3+ at Epic-2 close: **<30 DEFERs** (all Epic-1 doc-drift + early-Epic-2 devbox-substrate-code-changes out of Story 2.17 touch-set).
