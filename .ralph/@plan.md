# Implementation Plan

## NOW

- [ ] Run `/bmad-create-story (args: "review")` — Story 2.13 pre-dev SM validation per § Story Lifecycle row `drafted → validated`. Story file at `_bmad-output/implementation-artifacts/2-13-healthcheck-on-dnsmasq-sshd-replaces-upstream-s-broken-curl-3000-healthcheck.md` (landed iter-280 at Status `ready-for-dev`). Four parallel Sonnet subagents (citation audit + AC+Task implementability + cross-story consistency + security+runtime hazards) per iter-266 pattern; Ralph-synthesis triage. Forecast **1-3 PATCH** at pre-dev SM band (moderate novelty — compose healthcheck + POSIX-sh probe + env-var branching; no new entrypoint code path or capability interaction per iter-271 NOVEL LESSON novel-runtime-behaviour-surface class; smaller than Story 2.12's 8-PATCH outlier; larger than Story 2.11's 5-PATCH band). Forecast **1-3 DEFER** (probe-tool portability + Dockerfile HEALTHCHECK scope carve-out clarity + Story 2.12 D-1 absorption elegance). Next iter-282 = `/bmad-testarch-atdd` forecast SKIP-WITH-GROUNDS-(c)+(ii)+(iii) per iter-267 22nd-cumulative precedent (ACs 1, 2, 3, 5 bash-functional-testable via `docker compose config` shape + file-content grep; AC 4 exercises live mid-run service kill requiring Docker container runtime — operator-workstation-deferred).

## QUEUE (Story 2.13 lifecycle + 2.14..2.17 substrate queue)

- [ ] _(iter-282, post-pre-dev-SM)_ `/bmad-testarch-atdd` per matrix row `validated → atdd-scaffolded`. Forecast: SKIP-WITH-GROUNDS-(c)+(ii)+(iii) per Story 2.12 iter-267 22nd-cumulative precedent (mixed-class ACs; no test runner wired at substrate stage; adversarial-coverage substitutes via post-dev SM + CR gates).
- [ ] _(after atdd-scaffolded)_ `/bmad-dev-story (args: "{story_file_path}")` per matrix row `atdd-scaffolded → in-dev`. Scope: 5 Tasks — compose healthcheck block + probe-tooling verification + `INV-devbox-healthcheck` manifest entry + invariant doc + README § Healthcheck + AGENTS.md § Healthcheck + optional Task 5 Dockerfile sshd.log pre-create (absorbs Story 2.12 iter-279 D-1 DEFER). Forecast ~30-50K (smaller than Story 2.12's ~70-100K; fewer files touched; no new apt package; no new resolver function).
- [ ] _(after in-dev)_ `/bmad-testarch-trace (args: "yolo")` per matrix row `in-dev → traced` (forecast WAIVED per Story 2.12 iter-269 pattern — ACs 1, 2, 3, 5 substrate-covered by static smokes + sync-gate; AC 4 operator-workstation-deferred).
- [ ] _(after traced)_ `/bmad-create-story (args: "review")` post-dev SM per matrix row `traced → sm-verified`. Two-subagent pattern (iter-235 LESSON). Forecast 0-2 PATCH per iter-270 NOVEL LESSON drift-band re-baseline (pre-dev SM absorbs CRITICAL; post-dev narrower).
- [ ] _(after sm-verified)_ `/bmad-code-review (args: "2")` per matrix row `sm-verified → done | fixes-pending` — three-layer Ralph-hosted adversarial fan-out (iter-271 pattern + iter-277 NOVEL LESSON #2 META guard carry-forward). Forecast 1-3 first-class PATCH (moderate-novelty band per iter-264 LESSON; narrower than Story 2.12 novel-runtime-behaviour outlier).
- [ ] _(after Story 2.13 done)_ Story 2.14 legacy branch retention policy — full lifecycle.
- [ ] _(after Story 2.14 done)_ Story 2.15 committed Claude settings.json deny/allow — full lifecycle.
- [ ] _(after Story 2.15 done)_ Story 2.16 Claude PreToolUse hooks for secret-file denylist — full lifecycle.
- [ ] _(after Story 2.16 done)_ Story 2.17 hook-settings bypass-resistance + Epic 2 SC-17 close-out polish pass. SC-17 reconciliation absorbs cumulative **18 Story 2.12 DEFERs carried to Story 2.17** (11 iter-271 + 3 remaining iter-277 post-iter-278-and-iter-280-absorptions + 4 iter-279) if Story 2.13 Task 5 lands D-1 absorption; PLUS cumulative Epic 2 Story 2.13..2.16 DEFERs accrued during those stories.
- [ ] _(post-Story-2.17-done; Epic 2 final story)_ Transition PR #230 Draft→Open — final CI gate; monitor; merge per § Story Lifecycle + § PR Transition block. Epic 2 completion fires `EPIC_DONE` halt on next iter if PR pending merge; on re-entry after merge auto-advances to Epic 3 Story 3.1 per § Cross-epic transition.
- [ ] _(after every push)_ Monitor PR CI — queue fix tasks for any failures (PR #230 has no CI checks configured at 2026-04-24 per iter-219..280 confirmation; `statusCheckRollup: []` continues; iter-280 orient `gh pr view 230` GREEN first try — SSH :22 recovery asymmetry carry-forward from iter-263 LESSON quiescent this iter).

## BLOCKED

_(none — Story 2.12 DONE at iter-279 CR closure re-run #2 ZERO-PATCH; Story 2.13 drafted at iter-280; branch in sync with origin at `ce3ffb4` pre-commit.)_

## ATDD Red Phase

_(none — iter-267 ATDD-skip-with-grounds-(c)+(ii)+(iii) per FR14n; 22nd-cumulative ATDD-skip precedent for Story 2.12; new ATDD decision for Story 2.13 forecast at iter-282; no red-phase tests currently owed.)_

## DONE (iter-280 Story 2.13 DRAFTED — `/bmad-create-story` landing; `_(no story) → drafted`; sprint-status row `backlog → ready-for-dev`)

- [x] iter-280: **STORY 2.13 DRAFTED — `/bmad-create-story` landing.** FR14n Story State transition `_(no story) → drafted`. Sprint-status row `2-13-…: backlog → ready-for-dev` at `_bmad-output/implementation-artifacts/sprint-status.yaml:125`; `last_updated` line appended `2026-04-24 Story-2-13-ready-for-dev-iter-280 UTC`. Epic-2 row UNCHANGED at `in-progress` (workflow Step 1 "first story in epic" check does not fire — Story 2.13 is 13/17; epic already `in-progress` since Story 2.1 iter-128).

  - **Story file:** `_bmad-output/implementation-artifacts/2-13-healthcheck-on-dnsmasq-sshd-replaces-upstream-s-broken-curl-3000-healthcheck.md` — ~275 lines; Status: `ready-for-dev`; 5 ACs (1 negative-assertion + 2 positive + 1 mid-run + 1 timing-documented); 5 Tasks (compose healthcheck block + probe-tooling verification + invariant doc + README/AGENTS doc + optional D-1 absorption); Dev Notes (13 entries covering no-new-envrc / probe-domain-stability / log-volume / exec-context / POSIX-sh-semantics / negative-curl-3000 assertion / SSH-branch semantics / detection-latency / D-1-absorption-rationale / start.sh-status.sh-UNCHANGED / forecast-band / previous-story-intelligence / ATDD-decision-forecast); Project Structure Notes (files to create / edit / NOT edit + manifest entry count 32 → 33); References (19 source citations with `file:line` anchors).

  - **Exhaustive artifact analysis executed (workflow Step 2-4):** epics.md Epic 2 full block + Story 2.13 AC block (`:1574-1600`) + § NFRs anchor line (`:1161`); architecture.md S5 egress mechanism (`:278-281`) + Epic 2 context scan (no explicit healthcheck section — carry-forward advisory); PRD forward references (FR1 + FR1a + NFR6); Story 2.12 full carry-forward analysis (iter-279 CR closure + 4 DEFERs D-1..D-4 + `INV-devbox-ssh` invariant doc pattern + `resolve_ssh_state()` resolver function + `KEEL_DEVBOX_SSH_RESOLVED` compose-env propagation PATCH-2 pattern + compose-args helper PATCH-5 pattern); `packages/devbox/docker-compose.yml` full file review (healthcheck TODO at `:263`; Story-roadmap comment block at `:1-28`; base-compose `environment:` propagation at `:136-149`); `packages/devbox/Dockerfile` apt layer (`:43-92` — `dnsutils` at `:61` + `netcat-openbsd` at `:64`; both baked) + USER dev at `:347` + dnsmasq.log pre-create pattern at `:317` (Task 5 absorption template); `packages/devbox/entrypoint.sh` Story 2.12 sshd block (`:120-249` — healthcheck runs external to entrypoint via docker healthcheck primitive; no entrypoint edit needed); `packages/devbox/scripts/start.sh` (`:92-120` — already consumes `State.Health.Status` with fallback; Story 2.13 UNBLOCKS healthy/unhealthy branch without code change); `packages/devbox/scripts/status.sh` (`:54-58` — already reports healthcheck with `(no healthcheck configured)` fallback; Story 2.13 UNBLOCKS meaningful-value branch); `packages/devbox/whitelist/github.txt` probe-domain stability (`api.github.com` is load-bearing for every fork per Story 2.9 substrate); `INVARIANTS.md` § Devbox SSH (Story 2.12) + preceding/succeeding H3 structure (insertion point for new § Devbox healthcheck H3 between `:124` and `:126`).

  - **Previous-story intelligence (Story 2.12 carry-forward):** iter-271 NOVEL LESSON (static smokes cannot catch dynamic-runtime defects) → Story 2.13 healthcheck CMD executes live when Docker runs the probe; impl-time smokes possible under `bash -n` + `docker compose config` YAML shape + sha256 + sync-gate; live probe semantics operator-workstation-deferred. iter-278 LESSON (DEFER absorption discipline) → Task 5 absorbs Story 2.12 iter-279 D-1 (sshd.log first-boot pre-create). iter-273 LESSON (compose env-propagation chokepoints) → healthcheck sources `KEEL_DEVBOX_SSH` from canonical `KEEL_DEVBOX_SSH_RESOLVED` stream (same PATCH-2 posture); no case-variant drift. iter-276 LESSON (hazard-class discrimination under word-splitting) → healthcheck CMD uses POSIX-sh not bash; YAML folded-scalar `>-` normalisation avoids multi-line-arg word-splitting. iter-266 NOVEL LESSON (container-loopback ≠ host-loopback under Docker userland-proxy) → healthcheck runs INSIDE the container probing `127.0.0.1:53` (dnsmasq) + `127.0.0.1:2222` (sshd); container's own loopback is the CORRECT surface for in-container service liveness; semantic distinct from Story 2.12 AC 4's external-refusal posture.

  - **Forecast band (iter-264 LESSON + iter-271 NOVEL LESSON application):** Story 2.13 moderate-novelty surface — compose healthcheck CMD lifecycle × POSIX-sh × env-var branching × invariant authoring. SMALLER than Story 2.12's novel-runtime-behaviour outlier (no new daemon, no new capability interaction, no new entrypoint code path); LARGER than Story 2.7/2.8/2.9 one-verb shim class. Pre-dev SM forecast 1-3 PATCH (density-amortisation heuristic governs narrow-lens; substantive-runtime class unlikely absent novel daemon); post-dev SM forecast 0-2 PATCH per iter-270 drift-band re-baseline; CR forecast 1-3 first-class PATCH. Cumulative Story 2.13 PATCH forecast: 2-8 spread across gates — within all established bands. **Closure re-runs:** budget 1 closure re-run at worst (iter-264 LESSON; narrower than Story 2.12's 2 closure re-runs required).

  - **Downstream drift forecast (Stories 2.13..2.17 cumulative):** Story 2.13 introduces 1 new invariant (`INV-devbox-healthcheck`) + 1 new H3 in INVARIANTS.md + 1 new H2 in packages/devbox/README.md + 1 new H3 in AGENTS.md + manifest entry count 32 → 33. Stories 2.14..2.17 each forecast ~1-3 new invariants; Epic 2 final manifest count forecast 36-40. SC-17 single close-out reconciliation at Story 2.17 landing. Cumulative substrate-citation drift forecast for Stories 2.13..2.17: ~5-12 substantive + ~30-50 mechanical (narrower than original forecast — revised downward after iter-280 Story 2.13 drafted with fewer cross-invariant dependencies than Story 2.12's 4-Task delivery).

  - **PR:** #230 **Draft** — `statusCheckRollup: []` carries unchanged across iter-272..280; iter-280 orient `gh pr view 230` GREEN first try — SSH :22 recovery asymmetry carry-forward from iter-263 LESSON quiescent this iter (no HTTPS :443 flake surfaced). Branch in sync with origin at `ce3ffb4` pre-commit; about to push commit with Story 2.13 drafting artifacts (story file + sprint-status row + IP + RALPH.md entry).

  - **Budget consumed:** ~45K tokens (orient ~8K + parallel artifact research via 4 parallel reads ~10K + Story 2.12 Tasks pattern analysis ~5K + story file authoring ~18K + sprint-status edit + IP + RALPH.md + commit-prep ~4K stacking). Well within ~117K execution budget; exit cleanly per Guardrail 12.

- [x] iter-279: **STORY 2.12 DONE — `/bmad-code-review (args: "2")` closure re-run #2 ZERO-PATCH.** Three-layer Ralph-hosted adversarial fan-out converged ZERO-PATCH; Story State `fixes-pending → done`; sprint-status row `review → done`; cumulative Story 2.12 metrics FROZEN at 6 PATCHes + 2 closure re-runs + 22 cumulative DEFERs queued for Story 2.17 SC-17 reconciliation. See commit `ce3ffb4`.

- [x] iter-278: **STORY 2.12 PATCH-6 LANDED** — entrypoint.sh rc-capture subshell rewrite; 2 iter-277 DEFERs absorbed. See commit `947cbce`.

- [x] iter-272..277: Story 2.12 PATCH-1..5 LANDED + iter-277 CR closure re-run #1. Commits `30b7d8d`/`1df64ab`/`0d83fae`/`820591a`/`43b4e4b`/`3eadcff`.

- [x] iter-271: `/bmad-code-review (args: "2")` — `sm-verified → fixes-pending`; 5 PATCH + 11 DEFER + ~10 DISMISS. See commit `a777224`.

_(iter-253..270 Story 2.10/2.11/2.12 closure + Story 2.12 drafting/pre-dev-SM/ATDD/dev-story/trace-gate/post-dev-SM iters pruned per Guardrail 2.)_

## Context

- **Phase:** 4-implementation — Epic 2 at **12/17 stories done** (2.1-2.12) + 5/17 in-flight (2.13 drafted at iter-280; 2.14..2.17 backlog). Epic 2 current active story = 2.13 (healthcheck on dnsmasq + sshd).
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available`). Live fresh-fork first-run smokes operator-workstation-deferred; substrate smokes covered through iter-280 (`pnpm keel-invariants:check` GREEN at `946f1ac1…907029` carried from iter-276; no contentHash edit this iter).
- **Baked image:** `keel-devbox:local` (iter-123 bake; 848 MB, linux/arm64; `@anthropic-ai/claude-code@2.1.116` at Dockerfile:120). Host-side shim count: **18** at iter-280 (unchanged — no shim edits this iter; story-file-authoring-only).
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; **12 done** (2.1-2.12); **1 drafted** (2.13 at iter-280); **4 backlog** (2.14..2.17). Epic 2 closes at Story 2.17.
- **Epic Branch:** `feat/epic-2-packaged-devbox` (stays Draft across full Epic 2).
- **Story:** Story 2.13 — Healthcheck on dnsmasq + sshd (replaces upstream's broken `curl :3000` healthcheck).
- **Story File:** `_bmad-output/implementation-artifacts/2-13-healthcheck-on-dnsmasq-sshd-replaces-upstream-s-broken-curl-3000-healthcheck.md`
- **Story State:** `drafted` (iter-280 `/bmad-create-story` landed; next gate `/bmad-create-story (args: "review")` at iter-281 for `drafted → validated`).
- **GitHub Issue:** Story 2.13 issue unknown; `RALPH_ISSUE_NUMBER` unset at iter-280 orient. Epic 2 → #10 (Ralph closes on EPIC_DONE halt at Story 2.17 close-out).
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 — stays Draft across full Epic 2. No CI configured (`statusCheckRollup: []` carried unchanged iter-272..280; iter-280 orient GREEN first try).

## Notes

- **iter-280 meta-observation (drafting iter is routine):** Story 2.13 `/bmad-create-story` landing fits the 23rd+ cumulative drafting precedent (Stories 1.2..2.12 each followed the same pattern). No novel-lesson-producing iter; no META guard triggers. Signpost entry in RALPH.md is terse (one-line + iter-number marker).
- **iter-280 meta-observation (DEFER absorption queued proactively in draft):** Story 2.13 Task 5 proactively absorbs Story 2.12 iter-279 D-1 (sshd.log first-boot pre-create) per iter-278 LESSON. Task 5 is explicitly OPTIONAL (dev agent may defer to Story 2.17 if commit-atomicity concerns outweigh); D-2/D-3/D-4 remain at Story 2.17 scope. Pattern carry-forward: when drafting a story whose scope TOUCHES a region with queued DEFERs from a prior story, proactively queue absorption Tasks in the draft rather than waiting for CR re-touch discovery. Generalises iter-278 LESSON from "PATCH absorbs DEFERs when re-touching" to "story draft absorbs DEFERs when scope naturally covers the region".
- **iter-279 meta-observation carry-forward (ZERO-PATCH CR closure as-expected):** iter-280 is not a CR iter; observation quiescent. Pattern remains codified for Story 2.13 CR at iter-285-ish.
- **iter-279 meta-observation carry-forward (Edge Case Hunter self-downgrade discipline):** iter-280 is not a CR iter; observation quiescent.
- **iter-278 meta-observation carry-forward (DEFER absorption discipline):** iter-280 applied the rule proactively at drafting time (see above).
- **iter-277 NOVEL LESSON #1 carry-forward (bash subshell `||`-LHS errexit suppression is recursive):** stable at iter-280. Story 2.13 healthcheck CMD uses POSIX-sh `&&`/`||` chain under `/bin/sh` (dash on Ubuntu 24.04; NOT bash); `||`-LHS suppression mechanism does NOT apply to dash's errexit semantics. But the LESSON remains load-bearing for Story 2.13 dev-story if impl-time smokes use bash-wrapped probe testing.
- **iter-277 NOVEL LESSON #2 carry-forward (META empirical-test override):** guard did NOT fire at iter-280 (not a CR iter). Remains codified.
- **iter-276 NOVEL LESSON carry-forward (hazard-class discrimination under word-splitting):** stable at iter-280. Story 2.13 healthcheck CMD uses YAML folded-scalar `>-` to avoid multi-line word-splitting; dig `+short +time=3 +tries=1` args are space-separated in the YAML literal (no embedded whitespace hazard).
- **iter-276 observation carry-forward (contentHash edit cost model):** iter-280 did NOT touch any contentHash-tracked source (no code edits this iter — story-file-authoring-only). `pnpm keel-invariants:check` GREEN carried from iter-276 at `946f1ac1…907029`.
- **iter-273 NOVEL LESSON carry-forward (compose env-propagation sites are normalisation chokepoints):** stable at iter-280. Story 2.13 healthcheck reuses Story 2.12 PATCH-2 `KEEL_DEVBOX_SSH_RESOLVED` canonical stream — no new compose env propagation; no new chokepoint introduced.
- **iter-272 LESSON carry-forward (orient-time PATCH order decision framework):** not applicable at iter-280 (no PATCHes to order).
- **iter-271 NOVEL LESSON carry-forward (forecast band breach via novel-runtime-behaviour surface):** Story 2.13 forecast MODERATE novelty (compose healthcheck + POSIX-sh probe; no new daemon / capability / entrypoint path); pre-budget 1-3 first-class PATCH at pre-dev SM (narrower than Story 2.12's 8-PATCH outlier).
- **iter-271 LESSON carry-forward (three-subagent pattern):** applicable at iter-281 pre-dev SM (four parallel Sonnet subagents per iter-266 pattern) + iter-285 CR (three-layer adversarial fan-out per iter-271/277 pattern). Not exercised at iter-280 (drafting iter).
- **iter-270 NOVEL LESSON carry-forward (drift-band re-baseline):** applicable at iter-284 post-dev SM (bimodal drift: substantive vs mechanical). Not exercised at iter-280.
- **iter-264 LESSON carry-forward:** CR re-run DEFER-vs-PATCH severity-class-first; Story 2.13 forecast closure budget = 1 closure re-run at worst (narrower than Story 2.12's 2).
- **iter-263 LESSON carry-forward:** SSH :22 / HTTPS :443 asymmetric recovery; iter-280 orient GREEN first try — no recovery pattern needed this iter.
- **iter-257 LESSON reaffirmed (no contentHash edit at iter-280):** no sync-gate recompute; `pnpm keel-invariants:check` GREEN carried from iter-276 unchanged. Will fire at Story 2.13 dev-story when `INV-devbox-healthcheck` manifest entry + `docs/invariants/devbox-healthcheck.md` author both land (manifest count 32 → 33 at dev-story landing).
- **iter-244/246 audit-findings carry-forward:** restart.sh transitive-delegate; benchmark.sh OUT-OF-SCOPE (SC-11) threading compose-override; Manifest entry count **32** at iter-280 (unchanged; forecast 33 at Story 2.13 dev-story landing).
- **Substrate-citation drift cumulative forecast for Stories 2.13..2.17 REVISED:** ~5-12 cumulative substantive + ~30-50 cumulative mechanical drifts (revised downward after iter-280 Story 2.13 drafted with narrower cross-invariant dependency surface than Story 2.12's 4-Task delivery). SC-17 single reconciliation at Story 2.17 landing. Cumulative DEFER tally at iter-280 Story 2.13 drafting = 22 (Story 2.12 final) + 0 (Story 2.13 pre-dev-SM not yet run) = 22. Task 5 optional absorption of D-1 would bring total to 21 if exercised.
