# Implementation Plan

## NOW

- [ ] Land CR re-run **AR-4 (Low)** — Story 2.1 file Task 3 sub-bullet wording accuracy per iter-138 Review Findings: update the Task 3 sub-bullet currently describing `chown -R dev:dev /workspace` to match iter-133 AI-5's narrowed implementation (single-level `chown -- "$WORKSPACE_OWNER" /workspace` — recursive chown on a Linux-host bind-mount clobbers host-side ownership; already in prior CR defer list as "chown Linux host-ownership clobber on bind-mount"). Preferred fix: remove the `-R` flag and add a cross-reference to `_bmad-output/implementation-artifacts/deferred-work.md § code review of story-2.1 (2026-04-21)` "chown Linux host-ownership clobber on bind-mount" defer entry — single-file edit in the story artifact itself. **Verification target:** post-edit grep for `chown -R` against the workspace-chown Task 3 sub-bullet returns zero matches; cross-reference to deferred-work.md resolves. After close, tick `AR-4` Review Findings `[ ] → [x]` with closing evidence; Story 2.1 Change Log v2.10 entry (before v2.9 per descending-v2.x convention); Story State stays `fixes-pending` (AR-5 still TOP-of-QUEUE). ~small

## QUEUE (Story 2.1 `fixes-pending` — CR-re-run AR-5 + follow-ups)
- [ ] **AR-5 (Low)** — `packages/devbox/scripts/benchmark.sh` `--help`/`-h` flag handling: add `--help|-h) print_usage; exit 0 ;;` case arm to the flag parser (or inline Usage block from script header). Consistent with AI-4 UX-bundle posture. Verification: `bash packages/devbox/scripts/benchmark.sh --help` exits 0 and prints Usage; `-h` equivalent; zero-regression on existing flag-parsing smokes.
- [ ] Re-run `/bmad-code-review (args: "2")` — third CR gate for Story 2.1. Confirm `fixes-pending → done` (expected outcome: zero new patch action items after AR-1..AR-5 drain; any new findings triage per FR14n adversarial default). If still finds new patch items, another CR-re-run cycle is acceptable (Story 1.9 multi-cycle precedent at iter-7 → iter-14 → iter-17).
- [ ] Implement `packages/keel-invariants/` runtime rule for `INV-devbox-dind-available` — write `packages/keel-invariants/src/check-devbox-dind.ts` asserting (a) `command -v docker && docker info` exit 0, (b) `docker run --rm hello-world` exits 0 (functional criterion per iter-122 doc rewrite), (c) record detected backend (A/B) for downstream consumers to gate on. Add unit test (Epic 13 framework pending; Story 1.9 sync-gate pattern in the meantime), wire into `pnpm keel-invariants:check-all` composite. Spec source: `docs/invariants/devbox-dind.md`. After this lands, bump `contentHash` in the manifest entry accordingly if the doc file changes.
- [ ] Add pre-flight bind-mount probe to `packages/devbox/scripts/benchmark.sh` — after `detect_backend()`, if `BACKEND=B`, run `docker run --rm -v "${workspace}":/_probe alpine:latest true` (or reuse `hello-world` if pulled); on failure emit a structured error ("backend B + workspace not host-shared; see README § Pending first bake; either run on operator workstation with worktree under `/Users/...` or switch to backend A") and exit 2 BEFORE the destructive/up-d phase. Mirrors the iter-122 cold-prune refusal-gate pattern. Spec source: RALPH.md iter-124 signpost.
- [ ] Monitor PR CI — queue fix tasks for any failures
- [ ] Transition PR Draft→Open — final CI gate (after all Epic 2 stories done)

## BLOCKED

_(none — AR-1..AR-5 are all Story 2.1-scope + actionable without human input; defers are routed to `deferred-work.md` as scope-appropriate to downstream stories.)_

## DONE (Epic 2 Story 2.1)

- [x] iter-141: **CR-re-run fix AR-3 (Low) landed — `fixes-pending` QUEUE drains 3/5 (AR-1..AR-3 of AR-1..AR-5).** `packages/devbox/VERSIONS.md` doc-drift bundle: two row updates in the `## Baked at image-build (Dockerfile)` version-matrix table. (a) Line 28 `Supabase CLI` Notes column changed from `Arch-aware fallback; skipped if asset missing for host.` to `Fail-closed if asset missing for host arch (matches git-delta pattern per iter-134 AI-6).` — the pre-edit text directly contradicted iter-134 AI-6's Dockerfile fix (`packages/devbox/Dockerfile:178-179` emits `supabase-cli: no matching release asset for arch=$arch` to stderr then `exit 1;`); the new wording cross-references the git-delta row's established fail-closed phrasing at L29. (b) Line 26 `uv (Astral)` Pin column changed from `_recorded-at-bake_` to `` `0.11.7` (iter-123 bake capture) ``, following the iter-129 AI-1 claude-code row precedent (`2.1.116 (iter-128 CR-pinned; iter-123 bake capture)`) — backtick-quoted literal + parenthesised capture-provenance. `0.11.7` was already recorded in the lower `## Bake log` § iter-123 capture table (L138) but hadn't propagated to the top-of-file version-matrix; AR-3 (b) closes that gap. **Verification (landed):** (i) `grep -n 'Fail-closed if asset missing' packages/devbox/VERSIONS.md` returns exactly two hits (supabase L28 + git-delta L29) — spec target met; (ii) `grep -c '_recorded-at-bake_' packages/devbox/VERSIONS.md` returns 0 (pre-edit was 1); (iii) `grep -c '0.11.7' packages/devbox/VERSIONS.md` returns 2 (L26 + L138) — spec target `≥ 1` met; (iv) zero-regression on iter-123 bake envelope (VERSIONS.md is documentation / Renovate tracking only, not a build surface). Zero-regression on iter-134 AI-6 closure posture — AR-3 (a) brings VERSIONS.md into lock-step with landed Dockerfile behaviour. Zero-regression on iter-129 AI-1 precedent format — AR-3 (b) uses the identical backtick-literal + parenthesised-provenance shape. Review Findings AR-3 `[ ] → [x]` with closing evidence; Change Log v2.9 prepended before v2.8 per descending-v2.x convention. Story State stays `fixes-pending` (AR-4..AR-5 remain TOP-of-QUEUE; AR-4 is next-NOW — story Task 3 `chown -R` wording fix). Twenty-fifth cumulative Epic post-SM CR / CR-re-run-fix iteration across Stories 1.7..1.16 + Story 2.1. **Pattern lesson:** a single-file two-edit closure (Supabase row + uv row both in the same version-matrix table, both doc-drift from earlier AI-* fixes) landing in one iteration is strictly cheaper than two sequential doc-drift iterations — rationale compresses into one Change Log entry and reviewers see AR-3 as a single natural-surface delta. One-task-per-iteration preserved because the "task" is the AR-3 bundle, not individual row edits.

- [x] iter-140: **CR-re-run fix AR-2 (Medium) landed — `fixes-pending` QUEUE drains 2/5 (AR-1..AR-2 of AR-1..AR-5).** `packages/devbox/entrypoint.sh` gains two bundled hardening edits at the AI-5 surface; post-edit file is 118 lines (+25 net from two rationale-comment blocks). (a) `chown_err="$(mktemp -t chown.XXXXXX)"` captured at L68 replaces AI-5's hard-coded `/tmp/chown.err`; both chown sites (L81 workspace + L95 named-volume loop) reference `${chown_err}` via `2>"${chown_err}"` / `cat "${chown_err}" >&2` / `rm -f "${chown_err}"`. mktemp yields a process-unique path so Story 2.11 shared-mode + back-to-back `docker compose run --rm` cannot clobber each other's stderr captures or each other's cleanup rm. (b) Regex anchor widens from `^[A-Za-z0-9_-]+:[A-Za-z0-9_-]+$` to `^[A-Za-z_][A-Za-z0-9_.+-]*:[A-Za-z_][A-Za-z0-9_.+-]*$`; error-message hint widens in lock-step. First-char `[A-Za-z_]` still rejects leading digits + leading `-` (POSIX username shape + AI-5's option-parse-injection defence); tail `[A-Za-z0-9_.+-]*` adds `.` and `+` for enterprise AD-synced names (`first.last:eng`, `dev+ops:team`). `chown --` belt-and-braces preserved at both sites. Both edits wrapped in AI-5-pattern rationale-comment blocks (11 + 12 lines). **Verification:** (i) `bash -n entrypoint.sh` exit 0; (ii) Task 7.1 forbidden-pattern grep exit 1 (new mktemp/regex/comment text doesn't match any forbidden pattern); (iii) Task 7.4 `git ls-files -s` → `100755` preserved; (iv) concurrent-run smoke — stand-in script ran 50 parallel A/B pairs of the mktemp + chown-stderr-capture pattern with chown forced to fail via non-existent target paths; `collisions=0` across all 50 iterations (no duplicate `$chown_err` paths between A and B); follow-up single-pair Alpha/Beta run confirmed each captured stderr references its OWN label (content isolation PASS). Stand-in needed because `entrypoint.sh` itself tries to chown `/workspace` and mkdir `/home/dev/...` as root under `set -e`; pattern-extraction smoke isolates the AR-2 (a) mechanics — operator-workstation runs inherit transparently; (v) regex accept/reject smoke ran against the new regex under bash `[[ =~ ]]`: accepts `first.last:eng`, `dev+ops:team`, `root:root`, `user_1:group_2` (4/4 PASS); rejects `--reference=/etc/shadow`, `1user:group`, `-hyphen:group`, `user::group` (4/4 PASS); adversarial extras all PASS (empty reject, `:group` reject, `user:` reject, `a:b` accept). Zero-regression on iter-123 bake envelope (entrypoint.sh consumed at container-run time, not image-build time). Zero-regression on iter-133 AI-5 closure posture — AR-2 extends AI-5's belt-and-braces surface rather than replacing it (`chown --` terminator still in place; capture-and-re-emit diagnostic pattern still in place; regex-validation-before-chown still in place; empty-CMD fallback at L114-118 still in place). Review Findings AR-2 `[ ] → [x]` with closing evidence; Change Log v2.8 prepended before v2.7 per descending-v2.x convention. Story State stays `fixes-pending` (AR-3..AR-5 remain TOP-of-QUEUE; AR-3 is the next-NOW — VERSIONS.md doc-drift bundle). Twenty-fourth cumulative Epic post-SM CR / CR-re-run-fix iteration across Stories 1.7..1.16 + Story 2.1. **Pattern lesson (see RALPH.md iter-140 signpost):** when a CR re-run cluster points at the same hardening surface (mktemp race + character-class under-cut both live inside AI-5's entrypoint.sh commit region), bundling them in a single-script edit is strictly better than two sequential iterations — the mutual context compresses into one rationale-comment block per sub-edit and future editors see AR-2 as a single natural-surface delta. One-task-per-iteration stays intact because the "task" is the bundle, not the individual line edits.

- [x] iter-139: **CR-re-run fix AR-1 (Medium) landed — `fixes-pending` QUEUE drains 1/5.** `packages/devbox/scripts/benchmark.sh` trap-completeness bundle: EXIT trap `cleanup_on_exit` centralises `docker compose down`; per-signal traps split POSIX exit codes (INT=130, TERM=143, HUP=129, QUIT=131); ERR path now covered via EXIT. Stand-in smoke validated six exit paths with cleanup firing exactly once each. Zero-regression on detect_backend + destructive-prune refusal gate + iter-123 bake envelope. Change Log v2.7; Story State stays `fixes-pending` (AR-2..AR-5 queued). (Full detail in git log + story file v2.7 entry + prior IP version.)

- [x] iter-138: **`/bmad-code-review (args: "2")` RE-RUN — verdict stays `fixes-pending`; Story State `fixes-pending → fixes-pending` (regen); second CR gate for Story 2.1 post-AI-1..AI-9 drain.** Twenty-second cumulative Epic post-SM CR / CR-re-run iteration across Stories 1.7..1.16 + Story 2.1. Three-layer Ralph-hosted adversarial fan-out against `git diff main...HEAD` (2,504 lines / 20 files) using parallel subagents (each no-conversation-context): **Blind Hunter** (diff-only adversarial prose) → 16 findings; **Edge Case Hunter** (diff + project read, path-tracer JSON) → 21 findings; **Acceptance Auditor** (diff + spec + context docs {prd.md + epics.md + architecture.md + devbox-dind.md + trace artefacts + deferred-work.md}) → 2 substantive findings + 5 non-findings (AC verdicts: AC 1 SATISFIED + AC 2 SATISFIED + AC 3 PARTIAL under WAIVED operator-owned carve-out + AC 4 PARTIAL under WAIVED operator-owned carve-out; scope-creep CLEAN; defer-list discipline CLEAN — none of the 12 iter-128 defers re-raised; all 9 AI-*s verified closed byte-identical to their Change Log closure-evidence — AI-1 `@2.1.116` pin at Dockerfile L104-117, AI-2 `container_name: ${KEEL_DEVBOX_CONTAINER_NAME:-keel-devbox}` + Compose v2.20+ annotation at L38, AI-3 `detect_backend()` fail-safe-B at benchmark.sh L113-115, AI-4 trap+probe+warning+flag-text at L72-92,146,182-186, AI-5 chown capture-stderr+regex+empty-CMD at entrypoint.sh L45-66,95-99, AI-6 supabase-cli `exit 1;` at Dockerfile L183-186, AI-7 heading + contentHash `8e382a5396…f723` at devbox-dind.md L8 + manifest L253, AI-8 `#### Pending first bake` at README L117, AI-9 `## Upstream-inherited apt extras` at VERSIONS.md with 11 packages enumerated). Deduplicated — 6 merges across Blind ↔ Edge (tini-not-wired, /tmp/chown.err race, detect_backend case variants, playwright@latest, WORKSPACE_OWNER regex, mid-prune-trap) → **33 unique findings**. Triaged per FR14n adversarial default: **9 patch findings bundled into 5 action items AR-1..AR-5** (AR-1 benchmark.sh trap-completeness = F1+F27+F28 Medium; AR-2 entrypoint.sh concurrency+charset = F5+F13 Medium; AR-3 VERSIONS.md doc-drift = F8+F32 Low; AR-4 story Task 3 wording = F33 Low; AR-5 benchmark.sh --help flag = F30 Low); **18 defer items** carried to `deferred-work.md § Deferred from: code review RE-RUN of story-2.1 (2026-04-21)` with named carry-to targets (Epic 4 Story 4.10 OWASP ASVS for F17+F18+F19 supply-chain checksum pinning + F20+F21 GitHub API rate-limits; Story 2.2 `.envrc.example` + `pnpm devbox:up` wrapper for F9+F23+F24+F29 ergonomics; Story 2.5 hardening for F25+F26 tmpfs+named-volume contracts; Story 2.6 `pnpm devbox:rebuild` auto-regen for F7 playwright@latest; Story 2.11 shared-mode for F6 enterprise-case variants; Story 1.10+ schema hardening for F4 description-sync; Epic 13 CI bench-timing harness for F2 NFR2 granularity + F12 warm-run contamination; Story 11.x image-size optimization for F10 dead apt deps); **6 dismissed** as noise / intentional / covered-by-AI-9-text (F3 tini explicitly Story 2.5 scope per AI-9 VERSIONS.md; F11 ENV PATH dead-micro-polish; F14 grep-comment-rewording speculative; F15 restart 'no' YAML 1.1 covered by AI-2 Compose v2.20+ floor; F16 trap-before-prune safe concurrency; F31 README symlink/ro unlikely). **Artifacts authored/updated:** story file `### Review Findings` gains new `#### iter-138 RE-RUN` subsection (AR-1..AR-5 patch list + 18 defer list + 6 dismiss list + ~2k-word context paragraph); Change Log v2.6 entry (ordered before v2.5 per descending-v2.x convention); `deferred-work.md § Deferred from: code review RE-RUN of story-2.1 (2026-04-21)` new section with 18 defer entries (each with Source / Location / Nature / Defer reason / Carry-to); `sprint-status.yaml` `last_updated: 2026-04-21 Story-2-1-fixes-pending-rerun UTC` (stays `in-progress`). **Sprint-status sync:** `2-1: in-progress` unchanged per CR skill step-04 § 6 (action items left for the operator → stays `in-progress` until AR-1..AR-5 drain + re-run confirms `done`). **Infrastructure-smoke class precedent extension:** first Epic 2 story requiring a SECOND CR-re-run cycle (iter-128 → iter-138), consistent with Story 1.9's iter-7 → iter-14 → iter-17 multi-cycle pattern — validates that FR14n adversarial default can yield multi-round CR cycles without degenerating into churn (bundling 9 patch findings into 5 action items preserves one-task-per-iteration while producing disciplined coverage). Next iteration NOW: AR-1 (`packages/devbox/scripts/benchmark.sh` trap-completeness bundle — split INT/TERM exit codes + HUP/QUIT coverage + EXIT trap for ERR path). QUEUE: AR-2 → AR-3 → AR-4 → AR-5 → third CR re-run → then the two Story-2.1-follow-up items (INV-devbox-dind-available runtime rule + bind-mount pre-flight probe) that remain scope-appropriate for Story 2.1 completion.

- [x] iter-137: **CR fix AI-9 (Low) landed — `fixes-pending` QUEUE drain 9/9 (final AI-* item).** `packages/devbox/VERSIONS.md` (post-edit) gains `## Upstream-inherited apt extras (cc-devbox provenance)` section enumerating 11 packages by role. All 9 AI-1..AI-9 landed across iter-129..iter-137; iter-138 re-runs CR to confirm `fixes-pending → done`. (Full detail in git log + story file v2.5 entry + prior IP version.)

- [x] iter-129..iter-136 (summarised; full detail in git log + story file Change Log v1.7..v2.4 + prior IP versions): AI-1 Dockerfile @2.1.116 pin + `||` scoping (iter-129); AI-2 container_name parameterisation + Compose v2.20+ annotation (iter-130); AI-3 detect_backend fail-safe-B under `/.dockerenv` (iter-131); AI-4 benchmark.sh bundle — SIGINT/SIGTERM trap + docker info probe + flag-combo warning + native-A/DinD flag-text (iter-132); AI-5 entrypoint.sh bundle — chown capture-stderr + WORKSPACE_OWNER regex + `chown --` option-parse terminator + empty-CMD sleep-infinity fallback (iter-133); AI-6 supabase-cli fail-closed on arch mismatch matching git-delta pattern (iter-134); AI-7 `## INV-devbox-dind-available` doc heading + manifest contentHash regen `a9675f7d…b3ad → 8e382a5396…f723` (iter-135); AI-8 README `### Pending first bake → ####` heading demotion (iter-136).

- [x] iter-128: **`/bmad-code-review (args: "2")` CR adversarial action-items gate — verdict `fixes-pending`; 9 patch (AI-1..AI-9) + 12 defer + ~11 dismiss from 49 raw findings.** First CR gate for Story 2.1.

- [x] iter-121 + iter-122 (summarised): Docker landed iter-121 via operator install; `INV-devbox-dind-available` codified. iter-122 discovered backend B (host socket-passthrough); rewrote invariant doc with § Backend contract + § Safety rule; added `detect_backend()` + backend-B destructive-op refusal gate + `--allow-broad-prune` + `--skip-cold` to `benchmark.sh`.

- [x] iter-123: First successful devbox image bake (safe-subset, backend B). `keel-devbox:local` — sha256:e7e91f1537f196…, 848 MB, linux/arm64, ~4.5 min. Pinned tool versions captured to `packages/devbox/VERSIONS.md § Bake log`.

- [x] iter-124: Exercise the baked image — empirical outcome. Task 7.3 PASS (compose config exit 0). Tasks 4/5-run BLOCKED on backend-B bind-mount denial; README § Pending first bake updated with iter-123+iter-124 summary + operator-owned carve-out scope extension.

- [x] iter-125: **`/bmad-dev-story` re-invocation codifies iter-123+iter-124 empirical evidence.** Story file v1.4 change-log; `sprint-status.yaml` 2-1 `in-progress → review`; Story State `in-dev (partial) → in-dev`.

- [x] iter-126: **`/bmad-testarch-trace (args: "yolo")` — WAIVED gate; Story State `in-dev → traced`.** 11th cumulative Epic WAIVED precedent + first infrastructure-smoke class.

- [x] iter-127: **`/bmad-create-story (args: "review")` post-dev SM verification — verdict `sm-verified`; Story State `traced → sm-verified`.**

- [x] iter-99 (summarised; full detail in git log + RALPH.md iter-99 signpost): Story 2.1 source tree authored; sprint-status `ready-for-dev → in-progress`; Change Log v1.3 in story file; baseline commit `5278738`.

## Context

- **Phase:** 4-implementation — Epic 2 open, Story 2.1 at `fixes-pending` at iter-141 (AR-1..AR-3 landed; AR-4..AR-5 queued). Next lifecycle gate: drain AR-4..AR-5 → re-run `/bmad-code-review (args: "2")` for third CR gate to confirm `fixes-pending → done`.
- **Runtime:** cc-devbox iteration env with Docker via host socket-passthrough (backend B per `INV-devbox-dind-available` § Backend contract). Iteration-context bind-mount denial is an operational constraint; operator-workstation resolves via M4-Pro native with worktree under host-shared `/Users/...` OR backend-A isolated DinD.
- **Baked image:** `keel-devbox:local` (first bake iter-123 2026-04-21). Version matrix in `packages/devbox/VERSIONS.md § Bake log`. linux/arm64 — matches M4-Pro operator workstation.
- **Epic:** Epic 2 — Sandboxed Execution Environment (devbox). 17 stories total; Story 2.1 is the image + compose + entrypoint foundation.
- **Epic Branch:** `feat/epic-2-packaged-devbox`.
- **Story:** 2.1 — `packages/devbox/` absorb from cc-devbox (image + compose + substrate tooling access).
- **Story File:** `_bmad-output/implementation-artifacts/2-1-packages-devbox-absorb-from-cc-devbox-image-compose-substrate-tooling-access.md`
- **Story State:** fixes-pending — iter-141 landed AR-3 (VERSIONS.md doc-drift bundle: Supabase CLI row fail-closed wording synchronised with iter-134 AI-6 Dockerfile fix; uv `_recorded-at-bake_` → backtick-literal `` `0.11.7` (iter-123 bake capture) `` matching iter-129 AI-1 claude-code row precedent). Remaining: AR-4 story Task 3 `chown -R` wording (F33) + AR-5 benchmark --help (F30). Status stays `in-progress` per CR skill step-04 § 6. Advances `fixes-pending → done` after AR-4..AR-5 drain + next re-run returns zero new patch items.
- **GitHub Issue:** Story 2.1 → #41 (https://github.com/tthew/ralph-bmad/issues/41). Parent Epic 2 → #10. Commit trailer `Refs #41`.
- **PR:** #230 **Draft** — https://github.com/tthew/ralph-bmad/pull/230 (no CI runners pre-Epic-13; Draft→Open transition queued at end of Epic 2).

## Notes

- **DinD is fork-time substrate, not transitional.** Every fork's cc-devbox-equivalent environment must provide Docker per `INV-devbox-dind-available`. Forks substituting Podman/rootless Docker remain conformant if daemon reachability + function hold.
- **Backend B is the reference environment at 2026-04-21.** Host socket-passthrough to macOS Docker Desktop. Broad-state-mutation scripts MUST detect the backend + refuse destructive ops by default (`benchmark.sh` is the reference at iter-122). Bind-mount-based compose runtime requires host-shared File Sharing allowlist coverage of the workspace path — operator-owned from iteration context.
- **NFR2 authority unchanged.** AC 4 cold ≤ 300s / warm ≤ 30s remains authoritative on M4-Pro native. Post iter-124, BOTH cold AND warm runtime measurement are operator-owned.
- **Cross-epic auto-advance pattern validated** (iter-96). **Partial-completion pattern validated** (iter-99). **Backend-B safety-gate pattern established** (iter-122). **Review-with-operator-owned-carve-out pattern validated** (iter-125). **SM-verified-with-operator-owned-carve-out pattern validated** (iter-127). **CR-fixes-pending-adversarial-default pattern validated** (iter-128; 9 patch + 12 defer + ~11 dismiss from 49 raw findings). **CR-re-run-adversarial-default + action-item-bundling pattern validated** (iter-138; 9 patch bundled into 5 action items + 18 defer + 6 dismiss from 33 deduped findings; second CR gate produces disciplined fewer action items than first CR gate via bundling natural surfaces without violating one-task-per-iteration). **Doc-drift two-row bundle closure pattern validated** (iter-141; AR-3 Supabase row + uv row both in VERSIONS.md `## Baked at image-build` table — single-file two-edit closure cheaper than two sequential doc-drift iterations; bundling cross-row sibling drift into one AR-* action item compresses rationale into one Change Log entry).
- **Story 2.1 scope bounded by AC1-AC4 literally.** Substrate's pre-existing `packages/devbox/` TS scaffold preserved; Story 2.1 ADDED runtime-infrastructure siblings + substrate-level DinD invariant files.
