# @keel/create-keel-app

Bootstrap CLI (`npx create-keel-app`) for new Keel projects.

Empty shell in Story 1.1 — populated in Epic 15a. Intentionally excluded from `tsconfig.base.json` `paths` (CLI bootstrap, never imported as a library).
