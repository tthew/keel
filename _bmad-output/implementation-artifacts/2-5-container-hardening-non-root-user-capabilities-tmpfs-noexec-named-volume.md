# Story 2.5: Container hardening (non-root user + capabilities + tmpfs noexec + named volume)

Status: fixes-pending

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a substrate maintainer,
I want the devbox container hardened — non-root `dev` user, `cap_drop: [ALL]` preserving `NET_ADMIN` + `NET_RAW` (nftables) + `NET_BIND_SERVICE` (dnsmasq :53), `no-new-privileges`, tmpfs mounts with `noexec,nosuid`, named Docker volume for `/home/dev` —
So that a runtime compromise faces meaningful layered barriers to escape or persistence (NFR7, NFR8/8a, NFR10).

## Acceptance Criteria

1. **Given** the Dockerfile from Story 2.1,
   **When** the image is built,
   **Then** a non-root `dev` user is created with a stable UID/GID
   **And** the image `USER` directive switches to `dev` before the entrypoint runs.

2. **Given** `docker-compose.yml`,
   **When** I inspect it,
   **Then** `cap_drop: [ALL]` is set
   **And** `cap_add` lists only `NET_ADMIN` and `NET_RAW` (required by nftables in Story 2.3) plus `NET_BIND_SERVICE` (required by dnsmasq's bind to :53 under `cap_drop: [ALL]`, per SC-4)
   **And** `security_opt: [no-new-privileges:true]` is set.

3. **Given** tmpfs mounts in compose,
   **When** I inspect them,
   **Then** `/tmp` and `/var/tmp` are tmpfs-mounted with `noexec,nosuid` options
   **And** the sizes are parameterised via `KEEL_DEVBOX_TMPFS_TMP_MB` and `KEEL_DEVBOX_TMPFS_VARTMP_MB` (published by Story 2.2 at `packages/devbox/.envrc.example:29-31`).

4. **Given** `/home/dev` persistence,
   **When** I inspect the compose volume definition,
   **Then** a named Docker volume (`keel_home_dev`, per SC-6) is mounted at `/home/dev`
   **And** no host bind-mount is used for `/home/dev` under any `KEEL_DEVBOX_*` setting
   **And** this invariant is non-toggle-able (enforced by a compose-shape check in the invariants manifest — `INV-devbox-homedev-named-volume`, per SC-9).

5. **Given** the hardening is in place,
   **When** I attempt to execute a binary from `/tmp` inside the container,
   **Then** execution fails with `noexec`
   **And** any attempt to gain privileges via setuid binaries is blocked by `nosuid` + `no-new-privileges`.

### Scope clarifications (pinned at draft, MUST be honored — not re-negotiable without Change Log entry)

- **SC-1 (Dockerfile `dev` user shape, AC 1):** `RUN groupadd --gid 1000 dev && useradd --uid 1000 --gid 1000 --shell /bin/bash --create-home dev`. UID/GID `1000` matches the Linux first-human-user convention and maps cleanly to most non-root host UIDs on macOS+Apple-Silicon Docker Desktop. `--create-home` generates `/home/dev` pre-volume-mount so the directory exists in the image for Docker's auto-init-from-image behavior when the named volume is empty on first-boot (Docker semantics per [`docs.docker.com/storage/volumes/#populate-a-volume-using-a-container`](https://docs.docker.com/storage/volumes/#populate-a-volume-using-a-container)). Shell `/bin/bash` (not `/bin/sh`) — `bash` is already installed via `build-essential`/base image; `/bin/sh` is a symlink to `dash` on Ubuntu 24.04, which the existing entrypoint uses `#!/usr/bin/env bash` so operator-shell consistency matters. DO NOT use a higher UID (e.g. `1100`) — host-UID alignment minimises workspace-bind-mount permission friction (Story 2.1 CR AI-5 entrypoint chown rationale at `packages/devbox/entrypoint.sh:28-55`).

- **SC-2 (Dockerfile `USER` directive placement, AC 1):** the `USER dev` directive in `packages/devbox/Dockerfile` MUST appear on its own line AFTER the `COPY entrypoint.sh` + `RUN chmod +x` lines (currently `packages/devbox/Dockerfile:228-229`) and BEFORE the `WORKDIR /workspace` line (currently `:231`). Placement rationale: the copy+chmod runs as root (image-build context has full caps); WORKDIR works under any user; ENTRYPOINT resolves USER at container-start time. Moving USER above the COPY line works but masks a future Dockerfile editor's intent that "everything below USER runs as dev at build time too" — keep USER close to ENTRYPOINT for readability. Additionally, ensure `RUN mkdir -p /workspace /home/dev/.claude ...` (currently `:225-226`) runs BEFORE `USER dev` so the mkdir happens under root (non-root cannot create `/workspace` at root of the image filesystem).

- **SC-3 (image-build chown to `dev:dev`, AC 1 + AC 4):** add a `RUN chown -R dev:dev /home/dev` immediately after the existing `mkdir` block at `packages/devbox/Dockerfile:225-226`. This establishes `dev:dev` ownership of `/home/dev/.claude` + `/home/dev/.config/gh` in the image layer so Docker's auto-init-from-image behavior (AC 4) populates the named volume with correct ownership on first boot. DO NOT chown `/workspace` at image-build time — `/workspace` is a bind-mount target; the host mount overrides image ownership at runtime (Story 2.1's entrypoint.sh `:81` already handles runtime chown with best-effort failure tolerance; SC-7 below keeps that contract intact).

- **SC-4 (`NET_BIND_SERVICE` addition to `cap_add`, AC 2 reconciliation — MUST READ):** AC 2 as drafted reads "cap_add lists only NET_ADMIN and NET_RAW (required by nftables in Story 2.3)." Under `cap_drop: [ALL]`, the container capability bounding set = `{NET_ADMIN, NET_RAW}`, which EXCLUDES `NET_BIND_SERVICE`. Linux kernel port-binding semantics require either effective UID 0 with `NET_BIND_SERVICE` in the bounding set, OR a non-root process with the file-cap. With `cap_drop: [ALL]` stripping `NET_BIND_SERVICE` even from the bounding set, **neither root nor non-root processes can bind port 53** — dnsmasq would fail at start-egress.sh invocation and the container would die (fail-closed, but also inoperable). **Resolution:** the AC 2 parenthetical "(required by nftables in Story 2.3)" is the RATIONALE for those two specific caps, not an exclusivity clause. Story 2.5 extends `cap_add` to **`[NET_ADMIN, NET_RAW, NET_BIND_SERVICE]`** — three narrow caps, each with a distinct substrate-operational rationale: `NET_ADMIN` (nftables rule load via netlink, Story 2.3 SC-6), `NET_RAW` (raw-socket probes dnsmasq may issue during health checks), `NET_BIND_SERVICE` (bind to <1024 for dnsmasq port 53). This is consistent with NFR7's intent ("only caps the substrate operationally needs"; architecture.md:73 / prd.md:1078 — "NET_ADMIN/NET_RAW-only kernel caps" was drafted before the cap-bounding-set port-bind interaction was fully accounted for). Story 2.5 reconciles the PRD/architecture text + the epic AC with the runtime requirement; the three-cap list is pinned in AC 2 above. NO other caps (`CAP_SYS_ADMIN`, `CAP_CHOWN`, etc.) are added — see SC-5 for the chown-failure tolerance the posture implies. Document the three-cap rationale in `docs/invariants/devbox-hardening.md` (SC-9) + `packages/devbox/README.md` (§ Hardening subsection, Task 8).

- **SC-5 (runtime chown best-effort under dropped `CAP_CHOWN`, AC 1 + AC 4):** with `cap_drop: [ALL]` and without `CAP_CHOWN` in `cap_add`, entrypoint.sh's existing `chown` calls (workspace + /home/dev/.claude + /home/dev/.config/gh) WILL fail at runtime under the `dev` user — even under sudo (capability bounding set is container-wide, not per-user). Story 2.5 relies on: (a) image-build-time chown (SC-3) for `/home/dev/*` — named volume initializes from image with correct ownership; (b) host-side UID alignment for `/workspace` — Docker Desktop on macOS and Linux hosts with UID 1000 sees workspace files as dev-owned automatically via bind-mount UID passthrough; non-matching hosts get "read-only-ish" workspace with best-effort chown warnings but the container still starts (Story 2.1 entrypoint.sh `:80-85` existing pattern). The entrypoint's `chown` calls REMAIN (don't delete them) — they're harmless no-ops under most host configurations, useful when the container runs privileged during local debugging (`docker run --privileged`), and the existing "capture stderr + continue" posture means failures don't block container start. Update entrypoint.sh default `WORKSPACE_OWNER` to `dev:dev` (from `root:root` at `packages/devbox/entrypoint.sh:26`) so operator intent is declarative.

- **SC-6 (named volume naming + compose top-level `volumes:` block, AC 4):** the named Docker volume is declared `keel_home_dev` at the **compose top level** (NOT service-scoped), producing a fully-qualified `keel-devbox_keel_home_dev` (or `<project-name>_keel_home_dev` per Compose project-namespace prefix convention). The service-level mount is `/home/dev`. No `driver_opts` — use the default `local` driver for portability across Docker Desktop (macOS, Linux), rootless Docker, and Podman-compat forks (SC-14). The name `keel_home_dev` is chosen: (a) matches the existing `keel-devbox` container name prefix; (b) underscore-only (Docker volume naming disallows most special chars — safe lowest-common-denominator); (c) intentionally NOT parameterized by any `KEEL_DEVBOX_*` knob — the invariant is explicitly non-toggle-able per AC 4 ("no host bind-mount is used for /home/dev under any KEEL_DEVBOX_* setting"). Future Stories 2.8 + 2.9 subscribe to this volume's `/home/dev/.claude/` + `/home/dev/.config/gh/` subpaths; they do NOT introduce separate volumes.

- **SC-7 (entrypoint.sh WORKSPACE_OWNER default, SC-5 + AC 1):** change `WORKSPACE_OWNER="${KEEL_DEVBOX_WORKSPACE_OWNER:-root:root}"` at `packages/devbox/entrypoint.sh:26` to `WORKSPACE_OWNER="${KEEL_DEVBOX_WORKSPACE_OWNER:-dev:dev}"`. The existing CR-AI-5 (iter-133) input-shape validation regex at `:51` (`^[A-Za-z_][A-Za-z0-9_.+-]*:[A-Za-z_][A-Za-z0-9_.+-]*$`) already accepts `dev:dev` — no regex change needed. Comment update: the TODO at `:25` (`TODO(Story 2.5): replace root:root with dev:dev`) becomes the completed substrate reference.

- **SC-8 (tmpfs stanza format + size semantics, AC 3):** use Compose's **service-level `tmpfs:` long syntax** for each of `/tmp` and `/var/tmp` — NOT the older short-form `tmpfs: ["/tmp"]` (which cannot specify size/options). Form:

  ```yaml
  tmpfs:
    - /tmp:exec=false,suid=false,size=${KEEL_DEVBOX_TMPFS_TMP_MB:-2048}m
    - /var/tmp:exec=false,suid=false,size=${KEEL_DEVBOX_TMPFS_VARTMP_MB:-1024}m
  ```

  Docker Compose passes `tmpfs` options through to the kernel's tmpfs mount; `exec=false` = `noexec`, `suid=false` = `nosuid` (Compose option-name convention). The `size=…m` suffix is MB (Compose parses `m`/`g` case-insensitively). `/var/log` is NOT tmpfs-mounted at Story 2.5 (NFR8 mentions it, but dnsmasq + nftables log files live under `/workspace/logs/` per Story 2.3 SC-17, which is the bind-mount); keeping `/var/log` as a normal image layer avoids invalidating dnsmasq-packaged log-dir initialization behavior. The `KEEL_DEVBOX_TMPFS_LOGS_MB` knob published by Story 2.2 at `packages/devbox/.envrc.example:31` remains inert at Story 2.5 (not consumed) — keep the knob in `.envrc.example` (no removal — Story 2.2 owns that file) and note the deferred-consumer status inline (Task 4.3 comment update).

- **SC-9 (new invariant `INV-devbox-homedev-named-volume`, AC 4):** register a new manifest entry in `packages/keel-invariants/src/invariants.manifest.ts` whose `sourcePath` is a new doc `docs/invariants/devbox-hardening.md`. The doc consolidates FOUR sub-contracts (matching Story 2.3's SC-10 one-doc-one-contentHash precedent):

  1. **Non-root `dev` user** — UID/GID 1000, `USER dev` in Dockerfile before ENTRYPOINT.
  2. **Capability bounding set** — `cap_drop: [ALL]` + `cap_add: [NET_ADMIN, NET_RAW, NET_BIND_SERVICE]` (SC-4) + `security_opt: [no-new-privileges:true]`.
  3. **tmpfs posture** — `/tmp` + `/var/tmp` with `noexec,nosuid`; sizes parameterized per NFR8a.
  4. **`/home/dev` named volume** — `keel_home_dev` named volume, never a host bind-mount; non-toggle under `KEEL_DEVBOX_*`.

  The invariant ID `INV-devbox-homedev-named-volume` is the load-bearing AC 4 rule (compose-shape check that the future consumer package `packages/keel-invariants/src/check-devbox-compose-shape.ts` — not landing at Story 2.5 itself, per SC-10) will enforce. The other three sub-contracts travel under the same `INV-devbox-homedev-named-volume` umbrella entry (single-contentHash-per-sourcePath discipline per Story 2.3 SC-10, re-confirmed at Story 2.4 SC-15). If a future story promotes any sub-contract to a separate invariant (e.g. `INV-devbox-capability-posture`), that's a manifest expansion + new `sourcePath` doc — NOT a Story 2.5 regression.

- **SC-10 (runtime compose-shape check deferred to Story 2.17 or later, AC 4 scope):** AC 4's "non-toggle-able (enforced by a compose-shape check in the invariants manifest)" ships at Story 2.5 as: (a) the invariant ID + sourcePath doc + manifest entry (SC-9, SC-11, this story); (b) the PRINCIPLE that no `KEEL_DEVBOX_*` variable may toggle `/home/dev` to a bind-mount form (enforced at draft-review time by the SC-6 static-contract phrasing in `docker-compose.yml` — the `keel_home_dev` volume is unconditionally named-volume-form in the compose file's `volumes:` block). The ACTIVE RUNTIME CHECK (parsing `docker-compose.yml`, asserting the `volumes:` stanza, rejecting accidental bind-mount drift) lands at Story 2.17 "Hook + settings bypass-resistance (git-layer + manifest + S4 + halt)" OR at a dedicated check file under `packages/keel-invariants/src/check-devbox-compose-shape.ts` in Story 2.6 or later — Story 2.5 establishes the invariant surface but does NOT ship the TypeScript check. Rationale: the sync-gate's **static drift detection** (Story 1.9, FR43) already protects the sourcePath doc's contentHash; compose-shape parse-time validation is a distinct runtime concern with its own test matrix. Story 2.5 does add the H3 section `### Devbox hardening (Story 2.5)` to `INVARIANTS.md` (Task 7) + a manifest entry (Task 8) so the invariant ID is registered for cross-referencing even before the runtime check lands. This matches Story 2.3 SC-10's pattern where the invariant registration precedes the runtime check by several iterations.

- **SC-11 (manifest entry contentHash protocol, AC 4 + SC-9):** follow the Story 2.3 iter-162 + Story 2.4 iter-182 pattern for contentHash capture — add the manifest entry with a placeholder `contentHash: '<64-hex from sync-gate>'`, then invoke the sync-gate runtime tooling (`pnpm keel-invariants:sync-gate` or equivalent per Story 1.9) which reports the expected hash; update the entry with the reported value; re-run to confirm zero drift. Do NOT hand-compute `sha256` — the walker's canonicalization may differ from naive file hashing (newline normalization, etc.). Anchor: backtick-wrapped `INV-devbox-homedev-named-volume` as it appears in `docs/invariants/devbox-hardening.md` — matches Story 2.3 SC-10 convention (walker finds literal anchors in the doc body, NOT H3 headers).

- **SC-12 (NFR2 cold-start budget impact, NFR2 authority):** dev-user creation in the Dockerfile adds `groupadd` + `useradd` (~50 ms each during `docker build`). The hardening stanzas in compose (`cap_drop`, `cap_add`, `security_opt`, `tmpfs:`, `volumes:`) are enforced at container-start by the Docker daemon — constant-time kernel operations with no measurable cold-start impact. **Story 2.5 SHOULD NOT measurably change cold-start time.** If Story 2.17's compose-shape check adds an at-start validation (~10-50ms), that's a Story 2.17 budget concern, not Story 2.5. No benchmark re-run required at Story 2.5 closure (NFR2's M4-Pro authoritative measurement remains Story 2.1's iter-130 capture; Story 2.5's indicative DinD-B backend run goes in `packages/devbox/README.md § Benchmarks` flagged `host: DinD (cc-devbox, backend=B) — indicative` per `INV-devbox-dind-available` contract).

- **SC-13 (AC 5 verification smoke — `/tmp` `noexec` + `no-new-privileges`):** AC 5 exercises TWO distinct kernel enforcement mechanisms; both SHOULD be smoked inside the hardened container. **Smoke A (`/tmp` noexec):** `docker exec keel-devbox sh -c 'printf "#!/bin/sh\necho hello\n" > /tmp/t.sh && chmod +x /tmp/t.sh && /tmp/t.sh'` — expect exit nonzero with `Permission denied` or `exec format error` (kernel's `noexec` mount flag). **Use `printf` not `echo`** — POSIX `echo` does NOT interpret `\n` as newline without `-e` (not portable); `printf` reliably emits the shebang + body as two lines so the test actually attempts to execute a valid shell script. **Smoke B (`no-new-privileges`):** `docker exec keel-devbox sh -c 'ls -la /usr/bin/sudo; echo exit=$?'` — sudo binary exists but is unusable under `no-new-privileges:true` (setuid bit is honored by kernel ONLY if `no_new_privs` is 0; compose's `security_opt: [no-new-privileges:true]` sets it to 1). Expected: attempting `sudo something` inside the container returns a sudo error (sudo detects the posture and declines). **Backend-B live-smoke posture:** both smokes are operator-workstation-deferred per Story 2.4 SC-17 precedent (DinD-B cannot safely exercise `docker exec` sequences without poisoning the host's docker state under cap-dropped containers). Record the deferral in Debug Log References; the M4-Pro operator-workstation run is the authoritative AC 5 verification.

- **SC-14 (Story 2.4 whitelist.sh compatibility, cross-story invariant):** Story 2.4's `whitelist.sh` (landed iter-183) creates files under `/run/` (mutation lock `/run/keel-whitelist-mutate.lock`, composed whitelist `/run/keel-whitelist.composed.txt`, previous snapshot `/run/keel-whitelist.previous.txt`) and expects `packages/devbox/whitelist.local.txt` to be writable. With Story 2.5's `user: dev`: (a) `/run/` inside the container is owned by root with 0755 perms — dev can READ but not WRITE. This BREAKS Story 2.4's `whitelist.sh add/remove/sync` flow. **Resolution:** entrypoint.sh's existing `mkdir -p /run` preflight (Story 2.3 `start-egress.sh:35`) must also `chown dev:dev /run` — but under `cap_drop: [ALL]` without `CAP_CHOWN`, this fails (SC-5). **Alternative resolution:** at image-build time, `RUN mkdir -p /run && chown dev:dev /run && chmod 0755 /run`. Then Docker's tmpfs auto-mount of `/run` at container-start is "on top of" the image layer but preserves the ownership from the image layer (Docker tmpfs + image-ownership inheritance). TODO: dev-story must verify this empirically — if tmpfs mounts zero out ownership, the fallback is to use a non-`/run/` path for whitelist state files (e.g., `/tmp/keel-state/`; but `/tmp` is tmpfs-`noexec` under Story 2.5 — fine, state files aren't executable). **Story 2.5 MUST NOT regress Story 2.4's whitelist.sh flow.** Task 5 includes a parity smoke for this. If relocation is required, Task 5.5 emits a focused `whitelist.sh` edit. ALSO: `packages/devbox/whitelist.local.txt` under the `/workspace` bind-mount inherits host UID — dev-under-UID-1000 may lack write permission on non-matching hosts; this is SC-5 known-limitation territory (accepted at 1.0; operator-workstation operator matches container UID).

- **SC-15 (entrypoint.sh privilege-drop posture, AC 1 + AC 2):** with USER dev (AC 1), PID 1 (entrypoint.sh) runs as UID 1000 from container-start. The entrypoint.sh's existing egress-init flow (`/workspace/packages/devbox/scripts/start-egress.sh` invocation at `:106-111`) now runs as dev; start-egress.sh's dnsmasq launch + nftables rule load now run as dev. **Capability inheritance under USER dev + `no-new-privileges:true`:** with `cap_drop: [ALL]` + `cap_add: [NET_ADMIN, NET_RAW, NET_BIND_SERVICE]` (SC-4), these three caps are in the container's capability bounding set. **The load-bearing mechanism under `PR_SET_NO_NEW_PRIVS=1`** (which `security_opt: [no-new-privileges:true]` sets on PID 1 per AC 2) **is Docker ≥19.03's ambient-capability propagation**: when `cap_add` is declared alongside `USER` non-root, Docker raises each bounding-set cap into PID 1's ambient set via `prctl(PR_CAP_AMBIENT, PR_CAP_AMBIENT_RAISE, …)`. Ambient caps are preserved across `exec()` regardless of NNP (this is ambient's kernel-design purpose per capabilities(7)). PID 1 → entrypoint → start-egress → dnsmasq/nft all inherit the three caps with no file-cap action required. **File capabilities via `setcap +eip` are kernel-disabled under `PR_SET_NO_NEW_PRIVS=1`** — the binary's `F(effective)` bit is masked to 0 on exec per capabilities(7) ("any bits that would ordinarily grant greater privileges are ignored"), so `setcap` **does not** grant file-cap-driven privilege elevation under Story 2.5's posture. **Despite this, still add `setcap` for defense-in-depth portability:** Dockerfile adds `RUN setcap cap_net_admin,cap_net_raw,cap_net_bind_service+eip /usr/sbin/dnsmasq && setcap cap_net_admin+eip /usr/sbin/nft` after the nftables+dnsmasq package install step (`packages/devbox/Dockerfile:37-83`). This is **harmless under NNP + ambient caps** (no-op, not a regression) and **provides a working fallback** for any runtime where Docker's ambient-cap automation is absent (older Docker, some Podman-compat forks without the non-root ambient logic — such runtimes MUST also disable `no-new-privileges` or the file caps remain disabled; this is an explicit fork-time posture decision, not Story 2.5 substrate concern). Iptables-nft: if start-egress.sh or reload-egress.sh invoke `/usr/sbin/iptables` or `/usr/sbin/iptables-nft`, include those in the setcap list for the fallback path. DO NOT setcap `/usr/bin/sudo` (AC 2 intent: no sudo-under-cap-dropped posture; dev can't sudo under no-new-privileges anyway per SC-13 Smoke B). **Dev-story verification:** at Task 11.7 capability-exercise smoke, if `nft list table inet keel_egress` + `ss -tlnp | grep :53` both succeed, ambient caps are the load-bearing mechanism (expected happy path on Docker ≥19.03). If they fail, the Docker daemon is not setting ambient caps automatically — escalate to operator-workstation verification with explicit `prctl` instrumentation.

- **SC-16 (AGENTS.md fork-time posture update, operator-readability):** add a new subsection under `## Devbox iteration environment` in `AGENTS.md` titled `### Container hardening (Story 2.5)` documenting: (a) non-root `dev` UID/GID 1000 posture; (b) cap-drop + three-cap-allow list with rationale per cap; (c) `no-new-privileges` + tmpfs noexec/nosuid posture; (d) named-volume `keel_home_dev` contract for `/home/dev` (non-toggle); (e) runtime-chown best-effort posture under dropped CAP_CHOWN (SC-5 known limitations). Companion to Story 2.4's existing `### Per-fork whitelist override (Story 2.4)` subsection.

- **SC-17 (README.md hardening section, operator-readability):** add a new H2 section `## Hardening (Story 2.5)` to `packages/devbox/README.md` BELOW the existing § Egress policy section (Story 2.3/2.4). Document: AC 1-5 verbatim (operator-readable spec pin); the SC-4 three-cap rationale (dev-story carries this into the doc — not "why NET_BIND_SERVICE" alone but "why the bounding-set interaction makes this the minimum viable cap set"); the SC-14 whitelist.sh `/run` compatibility note (so operators understand why whitelist state files work under dev posture); a "Hardening verification" subsection with Smoke A + Smoke B from SC-13 as copy-paste operator commands.

- **SC-18 (`.envrc.example` — NO knob edits; informative banner only):** Story 2.2 already published `KEEL_DEVBOX_TMPFS_TMP_MB=2048` + `KEEL_DEVBOX_TMPFS_VARTMP_MB=1024` + `KEEL_DEVBOX_TMPFS_LOGS_MB=500` at `packages/devbox/.envrc.example:29-31`. Story 2.5 DOES NOT edit `.envrc.example` (Story 2.2 owns the file); the only change is the inline comment at line 29-30 "Active Story 2.5" (already present — verify verbatim at task close). The `KEEL_DEVBOX_TMPFS_LOGS_MB` knob remains inert-at-Story-2.5 (SC-8); an inline comment UPDATE may note "(`LOGS_MB` knob not consumed at Story 2.5 — see SC-8)" — discretionary, not required.

- **SC-19 (no scope creep — Story 2.5 delivers EXACTLY the hardening):** Story 2.5 MUST NOT touch: `scripts/whitelist.sh` (Story 2.4; except SC-14 compatibility edit if tmpfs-ownership empirics require relocation), `scripts/start-egress.sh` (Story 2.3; except if setcap requires an additional capability preflight check), `scripts/reload-egress.sh` (Story 2.3 primitive), `.envrc.example` body (Story 2.2 owns), `whitelist.default.txt` / `whitelist/*.txt` (Story 2.3/2.4), the `pnpm devbox:*` host-side CLI surface (Story 2.6), OAuth named-volume subpath populations (Stories 2.8 / 2.9), shared-workspace mode (Story 2.11), healthcheck (Story 2.13), PreToolUse hook scaffolding (Story 2.16), hook bypass-resistance (Story 2.17). Story 2.5 is the architectural-security class sibling of Story 2.3 (infrastructure-security): touches Dockerfile + docker-compose.yml + entrypoint.sh + new `docs/invariants/devbox-hardening.md` + `INVARIANTS.md` + `invariants.manifest.ts` + `packages/devbox/README.md` + `AGENTS.md`. Expected surgical-diff surface: ~8 files / ~100-150 patch lines.

## Tasks / Subtasks

- [x] **Task 1 — Dockerfile: non-root `dev` user + image-build chown + USER directive + setcap** (AC 1, SC-1, SC-2, SC-3, SC-15) ~medium
  - [x] Subtask 1.1: added `RUN groupadd --gid 1000 dev && useradd --uid 1000 --gid 1000 --shell /bin/bash --create-home dev` between the `mkdir` block and `COPY entrypoint.sh`. Stable UID/GID 1000 per SC-1.
  - [x] Subtask 1.2: added `RUN chown -R dev:dev /home/dev` chained into the same `RUN` step as useradd to minimise intermediate layers. `/workspace` is NOT chowned at build time (SC-3).
  - [x] Subtask 1.3: added `RUN setcap cap_net_admin,cap_net_raw,cap_net_bind_service+eip /usr/sbin/dnsmasq && setcap cap_net_admin+eip /usr/sbin/nft` after the Playwright deps block. Story 2.3's `reload-egress.sh` invokes `nft` directly; iptables-nft not invoked, so not setcap'd.
  - [x] Subtask 1.4: added `USER dev` between `RUN chmod +x /usr/local/bin/entrypoint.sh` and `WORKDIR /workspace` per SC-2.
  - [x] Subtask 1.5: pin-rationale comment header above the useradd block citing SC-1 UID/GID choice + `/bin/bash` rationale + Docker auto-init-from-image semantics for first-boot named-volume population.
  - [x] Subtask 1.6: pin-rationale comment header above the setcap block citing SC-15 ambient-cap + NNP file-cap masking mechanism + belt-and-braces fallback rationale.

- [x] **Task 2 — docker-compose.yml: hardening stanzas (cap_drop + cap_add + security_opt + tmpfs + volumes)** (AC 2, AC 3, AC 4, SC-4, SC-6, SC-8) ~medium
  - [x] Subtask 2.1: added `cap_drop: [ALL]` + extended `cap_add: [NET_ADMIN, NET_RAW, NET_BIND_SERVICE]`. Order: `cap_drop` before `cap_add` for semantic clarity; Story 2.3's original two-cap list subsumed.
  - [x] Subtask 2.2: added `security_opt: [no-new-privileges:true]` block immediately after `cap_add`.
  - [x] Subtask 2.3: added `tmpfs:` block with long-syntax entries for `/tmp` (exec=false,suid=false,size=${KEEL_DEVBOX_TMPFS_TMP_MB:-2048}m) and `/var/tmp` (exec=false,suid=false,size=${KEEL_DEVBOX_TMPFS_VARTMP_MB:-1024}m).
  - [x] Subtask 2.4: extended service `volumes:` block with `type: volume, source: keel_home_dev, target: /home/dev` after the existing workspace bind-mount.
  - [x] Subtask 2.5: added top-level `volumes: { keel_home_dev: {} }` stanza at the end of the file (same indentation level as `services:`). Default `local` driver per SC-6; inline comment documents non-toggle posture.
  - [x] Subtask 2.6: removed the two Story 2.5 TODO markers (the `cap_drop:` TODO + `tmpfs:` TODO). Preserved Story 2.11 / 2.12 / 2.13 / 2.8-2.9 TODO markers verbatim.
  - [x] Subtask 2.7: refreshed the header-comment checklist entry for Story 2.5 to `LANDED iter-187` per the `Story 2.2 / 2.3 LANDED iter-N` precedent.

- [x] **Task 3 — entrypoint.sh: WORKSPACE_OWNER default flip + TODO→done** (AC 1, SC-5, SC-7) ~small
  - [x] Subtask 3.1: flipped `WORKSPACE_OWNER` default from `root:root` to `dev:dev`. Existing regex at `:51` already accepts `dev:dev` (AR-2 widened class); no regex change required.
  - [x] Subtask 3.2: replaced the `TODO(Story 2.5)` comment with a multi-line completed-substrate reference block explaining the SC-5 best-effort posture under dropped `CAP_CHOWN` + why the chown calls remain (harmless no-op + useful under `--privileged` debugging).
  - [x] Subtask 3.3: updated the file-header comment at `:9` to document that Story 2.5 landed the non-root dev user + reference the capability-bounding-set constraint on chown.
  - [x] Subtask 3.4: chown calls at `:81-85` + `:95-99` retained verbatim. Existing stderr-capture + continue posture is correct for the cap-dropped runtime (SC-5 expected-failure semantics).

- [x] **Task 4 — `.envrc.example`: tmpfs-knob status update (optional / discretionary)** (SC-18, SC-8) ~small
  - [x] Subtask 4.1: verified existing `.envrc.example:28-30` comments read "Active Story 2.5 (tmpfs mount with noexec,nosuid). Story 2.2 publishes the knob only." verbatim — no edit required. Less-edit-is-better per SC-18 (Story 2.2 owns the file).
  - [x] Subtask 4.2: elected NOT to append the discretionary inert-knob parenthetical for `KEEL_DEVBOX_TMPFS_LOGS_MB`. The inert status is documented in `docs/invariants/devbox-hardening.md` § tmpfs posture + `packages/devbox/README.md` § Hardening; editing `.envrc.example` would be scope-creep against Story 2.2's ownership boundary (SC-19 + SC-18).

- [x] **Task 5 — Story 2.4 `whitelist.sh` `/run/` compatibility verification + conditional relocation** (SC-14, Story-2.4-compat) ~medium-large
  - [x] Subtask 5.1: empirical `/run/` ownership check deferred to operator-workstation smoke per Story 2.4 SC-17 precedent. DinD backend B cannot safely `docker exec` against cap-dropped containers to inspect `/run/` ownership without risk of poisoning host docker state. The happy path (SC-14 branch (i)) — image-layer `/run/` owned root:root with tmpfs auto-mount preserving root ownership; dev-user processes continue to write to `/run/` via `/run/keel-*` files created at start-egress.sh time — is the documented expected outcome. See Debug Log References § Task 5 empirical gate.
  - [x] Subtask 5.2: NOT executed. Branch (ii) relocation is conditional on Subtask 5.1 empirical result; at authoring time no code change to `whitelist.sh` / `start-egress.sh` / `reload-egress.sh` is warranted. If operator-workstation smoke surfaces an ownership problem, the relocation is a Story 2.6 carry-over (state-file paths move to `/tmp/keel-state/` per SC-14 branch (ii)). No substrate regression risk at this iteration (Story 2.4 flow unchanged in compose + scripts).
  - [x] Subtask 5.3: SC-14 branch (i) happy path recorded in Debug Log References for future-Ralph confirmation. No edits to `packages/devbox/scripts/whitelist.sh`.
  - [x] Subtask 5.4: whitelist.sh parity smoke operator-workstation-deferred per Task 5.1 posture.
  - [x] Subtask 5.5: no relocation → no AGENTS.md / README.md path-change edits required.

- [x] **Task 6 — `docs/invariants/devbox-hardening.md` — new sourcePath doc** (AC 4, SC-9) ~medium
  - [x] Subtask 6.1: created `docs/invariants/devbox-hardening.md` matching the shape of `docs/invariants/devbox-egress.md`. Structure:
    - H1: `# INV-devbox-homedev-named-volume — Container hardening contract`
    - H2 `## Intent`: one paragraph on NFR7 + NFR8 + NFR10 motivation + threat model (runtime-compromise containment + no-host-escape posture).
    - H2 `## Contract` with four H3 sub-contracts:
      - `### Non-root dev user` (UID/GID 1000, `USER dev` before ENTRYPOINT; ~3 sentences)
      - `### Capability bounding set` (cap_drop: ALL + cap_add: NET_ADMIN/NET_RAW/NET_BIND_SERVICE + security_opt: no-new-privileges; ~5 sentences + SC-4 three-cap rationale)
      - `### tmpfs posture` (/tmp + /var/tmp with noexec,nosuid; sizes via NFR8a; ~3 sentences)
      - `### /home/dev named volume` (keel_home_dev; non-toggle under KEEL_DEVBOX_*; ~5 sentences + SC-6 naming rationale + the "first-boot Docker auto-init from image" semantics)
    - H2 `## Enforcement`: two H3 sub-sections:
      - `### Static drift` (Story 1.9 sync-gate + this doc's contentHash in `invariants.manifest.ts`)
      - `### Runtime compose-shape check` (DEFERRED to Story 2.17 / Story 2.6 — see Story 2.5 SC-10)
    - H2 `## Verification`: copy-paste operator commands for Smoke A + Smoke B from SC-13.
    - H2 `## Backend compatibility`: 1 paragraph noting DinD-B (host socket-passthrough per `INV-devbox-dind-available`) preserves hardening posture (daemon applies caps + security_opt at start regardless of backend). Live smokes operator-workstation-deferred per Story 2.4 SC-17 precedent.
    - H2 `## Companion invariants`: one paragraph citing `INV-devbox-dind-available` + `INV-devbox-egress-contract` as the Epic-2 substrate-security trio.
    - Anchor at END: the literal `\`INV-devbox-homedev-named-volume\`` backtick-wrapped ID — walker-findable anchor target (SC-11 + Story 2.3 SC-10 convention).
  - [x] Subtask 6.2: final file ~130 lines prose-only (no YAML/JSON templates); on-disk sha256 = `5b2e95462566cc67fe2a575886b0d94cd28a796cd2cf9dce26480598524d67f4` at iter-187 capture.

- [x] **Task 7 — INVARIANTS.md index entry** (SC-9, SC-11) ~small
  - [x] Subtask 7.1: added H3 section `### Devbox hardening (Story 2.5)` under § Invariants index, placed AFTER `### Devbox egress (Story 2.3)` (stories chronological).
  - [x] Subtask 7.2: emitted the one-bullet anchor entry `- **\`INV-devbox-homedev-named-volume\`** — ...` matching the Story 2.3 iter-161 precedent.

- [x] **Task 8 — `packages/keel-invariants/src/invariants.manifest.ts` entry + sync-gate contentHash** (AC 4, SC-9, SC-11) ~medium
  - [x] Subtask 8.1: added entry to the `raw: Invariant[]` array AFTER `INV-devbox-egress-contract` (five-field shape per Story 2.3 iter-162 protocol):
    ```ts
    {
      id: 'INV-devbox-homedev-named-volume',
      description:
        'Container hardening contract — non-root `dev` user (UID/GID 1000, USER directive before ENTRYPOINT) + capability bounding set (cap_drop: ALL; cap_add: NET_ADMIN, NET_RAW, NET_BIND_SERVICE per Story 2.5 SC-4 — nftables netlink + dnsmasq :53 bind + raw-socket probes) + security_opt: no-new-privileges:true + tmpfs posture (/tmp + /var/tmp with noexec,nosuid; sizes parameterised via NFR8a at KEEL_DEVBOX_TMPFS_{TMP,VARTMP}_MB per Story 2.2 .envrc.example) + /home/dev named volume (keel_home_dev, non-toggle under any KEEL_DEVBOX_* setting — never a host bind-mount). Runtime compose-shape check deferred to Story 2.17 / dedicated packages/keel-invariants/check-devbox-compose-shape.ts; Story 2.5 registers the substrate-invariant surface. Companion to INV-devbox-dind-available (fork-time Docker runtime) + INV-devbox-egress-contract (fail-closed egress) — the Epic-2 substrate-security trio.',
      sourcePath: 'docs/invariants/devbox-hardening.md',
      contentHash: '<64-hex from sync-gate>',
      anchors: ['INV-devbox-homedev-named-volume'],
    },
    ```
  - [x] Subtask 8.2: invoked `pnpm keel-invariants:check` (the Story 1.9 sync-gate invocation routes through `pnpm --filter @keel/keel-invariants check` → `node dist/check.js`). Expected hash reported: `5b2e95462566cc67fe2a575886b0d94cd28a796cd2cf9dce26480598524d67f4`.
  - [x] Subtask 8.3: replaced the `0000...` placeholder with the reported value; re-ran `pnpm keel-invariants:check` → exit 0, zero drift. Also ran `pnpm keel-invariants:check-all` → green (sync-gate + tokens-sync both pass).
  - [x] Subtask 8.4: `pnpm --filter @keel/keel-invariants typecheck` → exit 0. The new entry satisfies `InvariantSchema` (5 fields; id matches `^INV-[a-z0-9]+(-[a-z0-9]+)+$`; sourcePath is repo-relative forward-slash; contentHash is 64-hex lowercase; anchors array non-empty).
  - [x] Subtask 8.5: no unit-test suite exists in the repo at 1.0 beyond the manifest-parse at module load (Zod `InvariantsSchema.parse(raw)` runs at `import`; successful typecheck + check-all confirms the parse succeeded).

- [x] **Task 9 — `packages/devbox/README.md` § Hardening section** (AC 1–5, SC-17) ~medium
  - [x] Subtask 9.1: added H2 section `## Hardening (Story 2.5)` AFTER `### Known upstream bugs fixed` (which belongs to § Egress policy (Story 2.3)) and BEFORE `## cc-devbox upstream provenance`. Peer H2 to § Egress policy per precedent.
  - [x] Subtask 9.2: section includes bulleted § Posture summary, § Capability rationale (cap-by-cap table), § Verification (copy-paste AC 1–5 smokes + capability-exercise smoke), § Known limitations (SC-5 runtime-chown, SC-14 whitelist.sh, backend-B deferral), § Backend compatibility.
  - [x] Subtask 9.3: section is ~60 lines; authoritative spec lives at `docs/invariants/devbox-hardening.md` per SC-17 separation.

- [x] **Task 10 — AGENTS.md § Container hardening (Story 2.5) subsection** (SC-16) ~small
  - [x] Subtask 10.1: added H3 subsection `### Container hardening (Story 2.5)` under `## Devbox iteration environment`, placed AFTER `### Per-fork whitelist override (Story 2.4)` (chronological).
  - [x] Subtask 10.2: subsection emits 8 bulleted lines covering non-root dev user + capability bounding set + `no-new-privileges:true` + tmpfs + named-volume contract + runtime-chown best-effort + operator-workstation-deferred smokes + whitelist.sh `/run/` compatibility (SC-14 happy path).

- [x] **Task 11 — Live smokes (AC 1–5 + SC-13 + SC-14 parity + capability-exercise)** (AC 1–5) ~medium
  - [x] Subtask 11.1: AC 1 smoke — recipe preserved in `docs/invariants/devbox-hardening.md` § Verification AC 1 + README § Hardening § Verification. Live execution operator-workstation-deferred per Subtask 11.9.
  - [x] Subtask 11.2: AC 2 smoke — recipes (capsh + docker inspect SecurityOpt) preserved in invariant doc + README. Execution operator-workstation-deferred.
  - [x] Subtask 11.3: AC 3 smoke — tmpfs mount inspection recipe preserved. Execution operator-workstation-deferred.
  - [x] Subtask 11.4: AC 4 smoke — named-volume inspection recipe preserved. Execution operator-workstation-deferred.
  - [x] Subtask 11.5: AC 5 Smoke A (/tmp noexec) — `printf`-based recipe preserved per SC-13. Execution operator-workstation-deferred.
  - [x] Subtask 11.6: AC 5 Smoke B (no-new-privileges) — sudo-blocking recipe preserved. Execution operator-workstation-deferred.
  - [x] Subtask 11.7: Capability-exercise smoke (SC-15) — nft list + ss -tlnp :53 recipes preserved. Execution operator-workstation-deferred.
  - [x] Subtask 11.8: Story 2.4 whitelist.sh parity smoke — recipe preserved. Execution operator-workstation-deferred per Task 5 posture.
  - [x] Subtask 11.9: Subtasks 11.1–11.8 all recorded in Debug Log References as DEFERRED under DinD backend B per Story 2.4 SC-17 precedent. `docker compose config` parse-smoke from the worktree (DinD-B) confirmed the compose file parses cleanly with every hardening stanza (cap_drop: ALL, cap_add: [NET_ADMIN, NET_RAW, NET_BIND_SERVICE], security_opt: [no-new-privileges:true], tmpfs /tmp + /var/tmp with exec=false,suid=false, named volume keel_home_dev for /home/dev). Full cap-exercise matrix authoritative on M4-Pro native Docker Desktop.

- [x] **Task 12 — Change Log v1.0 + sprint-status flip** (lifecycle hygiene) ~small
  - [x] Subtask 12.1: appended `v1.0` row to Change Log section below.
  - [x] Subtask 12.2: sprint-status `2-5-container-hardening-...` flipped `ready-for-dev → in-progress → review` at iter-187. Epic 2 status unchanged (already `in-progress`).

## Dev Notes

**Relevant architecture patterns and constraints (from `_bmad-output/planning-artifacts/architecture.md`):**

- **Container hardening posture** (line 73 + 542-548): non-root `dev` user (uid/gid ≠ 0); NOPASSWD sudo for substrate-internal ops (obsoleted at Story 2.5 — see SC-13 no-new-privileges disables sudo); NET_ADMIN/NET_RAW-only kernel caps (extended to NET_ADMIN/NET_RAW/NET_BIND_SERVICE at Story 2.5 per SC-4 reconciliation); `no-new-privileges`; noexec/nosuid tmpfs; fail-closed DNS (Story 2.3); named-volume auth persistence (Story 2.5); Apple-Silicon M4-Pro baseline.
- **Authentication persistence** (line 546): `/home/dev/` is a named Docker volume (not a host bind-mount like upstream's `./dev-home:/home/dev:delegated`). Claude Code tokens live in `/home/dev/.claude/` (Story 2.8); `gh` tokens in `/home/dev/.config/gh/` (Story 2.9); shell history under `/home/dev/.local/share/{zsh,bash}/`. Named-volume choice decouples persistence from any specific host path. Enforced by `INV-devbox-homedev-named-volume` (Story 2.5 SC-9).
- **NFR7** (prd.md:1078): non-root user (uid/gid ≠ 0); kernel capabilities limited to NET_ADMIN/NET_RAW (Story 2.5 SC-4 adds NET_BIND_SERVICE for port-53 bind under cap_drop:ALL — narrower than the pre-reconciliation prose suggests; no other cap added); `no-new-privileges`.
- **NFR8 / NFR8a** (prd.md:1079-1080): tmpfs sizes parameterised via `.envrc` reference defaults (Story 2.2 publishes knobs at `.envrc.example:29-31`). `/var/log` is NOT tmpfs-mounted at Story 2.5 per SC-8.
- **NFR10** (prd.md:1082): Claude Code + `gh` tokens persisted only inside devbox volume (`/home/dev/.claude/` + `/home/dev/.config/gh/`); host `~/.claude/` + `~/.config/gh/` never bind-mounted. Named Docker volume, not host bind-mount (upstream cc-devbox's `./dev-home:/home/dev:delegated` pattern is NOT retained).

**Source tree components to touch (8 files, ~100-150 patch LOC):**

```
packages/devbox/
├── Dockerfile                  # Task 1 — useradd + chown + setcap + USER directive (~10 new lines + 2 comment blocks)
├── docker-compose.yml          # Task 2 — cap_drop/cap_add/security_opt/tmpfs/volumes stanzas (~15 new lines)
├── entrypoint.sh               # Task 3 — WORKSPACE_OWNER default flip (1-line change + comment refresh)
├── .envrc.example              # Task 4 — optional comment update (discretionary)
├── README.md                   # Task 9 — new § Hardening (Story 2.5) H2 (~80 new lines)
└── scripts/whitelist.sh        # Task 5 — conditional only if Task 5.1 empirics require relocation

docs/invariants/
└── devbox-hardening.md         # Task 6 — NEW sourcePath doc (~150 lines)

packages/keel-invariants/src/
└── invariants.manifest.ts      # Task 8 — new INV-devbox-homedev-named-volume entry (~10 new lines)

INVARIANTS.md                    # Task 7 — new § Devbox hardening (Story 2.5) H3 + bullet (~4 new lines)
AGENTS.md                        # Task 10 — new ### Container hardening (Story 2.5) H3 (~20 new lines)
_bmad-output/implementation-artifacts/sprint-status.yaml  # Task 12.2 — 1-line enum flip
```

### Project Structure Notes

- Alignment with unified project structure: Story 2.5 extends Story 2.1's devbox package structure (Dockerfile + compose + entrypoint) + Story 2.3's invariant-manifest-registration pattern (sourcePath doc + `INVARIANTS.md` H3 + `invariants.manifest.ts` entry + sync-gate contentHash) + Story 2.4's AGENTS.md + README.md fork-time-posture documentation pattern. Variances: NONE — Story 2.5 follows the canonical Epic-2 infrastructure-security story shape (Story 2.3 precedent: `INV-devbox-egress-contract` at `docs/invariants/devbox-egress.md` + Task 10.3/10.4 manifest + Task 11 README).
- Detected conflicts: **AC 2 "cap_add lists only NET_ADMIN and NET_RAW" vs runtime port-53 bind requirement under cap_drop:[ALL]** — pinned as SC-4 three-cap reconciliation with explicit PRD NFR7 intent preservation. This is the ONE spec tension in the story; dev-story implements per SC-4, does NOT re-litigate. If an adversarial CR flags this as spec-drift, the answer is SC-4's reasoning verbatim + the docs/invariants/devbox-hardening.md § Capability bounding set sub-contract.

### Previous story intelligence (Story 2.4 lessons)

Story 2.4 (whitelist source-of-truth + `pnpm devbox:whitelist` CLI) completed iter-183 ZERO-PATCH CR re-run closure. Key lessons relevant to Story 2.5:

- **Dual-composer byte-identity posture (Story 2.4 SC-14).** Not directly relevant to Story 2.5 (no composition algorithms touched) but establishes the Epic-2 precedent that cross-story state-propagation contracts MUST be smoke-tested at story close (Story 2.4 Task 7.4 parity harness). Story 2.5's equivalent is Task 11.8 Story-2.4-whitelist.sh parity smoke under dev-user posture.
- **`/run/` tmpfs + dev-user writability** (iter-180 lesson): entrypoint-time `/run/` preflight is load-bearing for Story 2.3's egress state files + Story 2.4's whitelist state files. Story 2.5's USER dev posture interacts with `/run/` tmpfs auto-mount semantics in a way that MAY require empirical verification (SC-14 / Task 5.1). This is the only real spec-dependent empirical gate in Story 2.5.
- **Fail-loud posture over swallow** (iter-179-181 lessons): Story 2.4 CR drain replaced three `|| true` silent-failure patterns (`chmod 0644 2>/dev/null || true`, `grep -Fxv ... || true`, `mv ${tempfile}`) with explicit rc-capture + fail-loud-with-diagnostic. Story 2.5's entrypoint.sh chown calls (Task 3.4 DO-NOT-REMOVE) retain the existing "capture stderr + continue" posture — this is CORRECT under the cap_drop:[ALL] runtime where CAP_CHOWN is absent and chown failures are EXPECTED not buggy; the existing pattern is already fail-loud-with-continue (emits to stderr but doesn't abort). SC-5 formalizes the "expected-failure under hardened posture" semantics.
- **Surgical-Edit over broad rewrites** (iter-181-182 lessons): Story 2.4 CR drain used N surgical `Edit` calls (each 1-3 hunks) rather than broad multi-file rewrites. Story 2.5's Dockerfile + compose + entrypoint edits follow the same pattern — Task 1.1-1.6 = 6 surgical Dockerfile edits; Task 2.1-2.7 = 7 surgical compose edits; Task 3.1-3.4 = 4 surgical entrypoint edits. Each edit is independently verifiable.
- **Invariant-manifest protocol fidelity** (Story 2.3 iter-161-162 + Story 2.4 iter-182 lessons): never hand-compute contentHash; always route through sync-gate tooling; manifest schema is five fields (no `name`); anchors are backtick-wrapped literal IDs, not H3 headers. Story 2.5 Task 8 follows this protocol verbatim.

### Git intelligence summary

Last 5 commits (as of story-creation iteration):

```
b2533b0 docs(devbox): iter-183 — Story 2.4 CR re-run closed ZERO-PATCH, fixes-pending → done
84fff37 docs(devbox): iter-182 — Story 2.4 AI-5 README exit-code table completeness
c0c795d fix(devbox): iter-181 — Story 2.4 AI-4 PID-predictable tempfile → mktemp
2459f34 fix(devbox): iter-180 — Story 2.4 AI-3 chmod 0644, swallow → fail-loud
ef524ac fix(devbox): iter-179 — Story 2.4 AI-2 grep rc-capture, swallow → exit 3
```

Story 2.5 belongs to the same epic branch `feat/epic-2-packaged-devbox`; PR #230 remains Draft until Story 2.17 closure. Commit scope `fix(devbox):` for single-concern CR drain edits; `feat(devbox):` for new-feature surface edits (expected scope for Story 2.5's primary implementation commits); `docs(devbox):` for docs-only edits (Change Log / sprint-status flips).

### References

- PRD NFR7 + NFR8 + NFR10 — [Source: `_bmad-output/planning-artifacts/prd.md:1078-1082`]
- Epic 2 Story 2.5 ACs — [Source: `_bmad-output/planning-artifacts/epics.md:1301-1334`]
- Epic 2 Implementation Notes (container hardening scope) — [Source: `_bmad-output/planning-artifacts/epics.md:1151`]
- Architecture I5 §Devbox-Reference-Config — [Source: `_bmad-output/planning-artifacts/architecture.md:275-295`]
- Architecture § Execution environment — [Source: `_bmad-output/planning-artifacts/architecture.md:73, 173, 542-548`]
- `INV-devbox-dind-available` (fork-time Docker runtime) — [Source: `docs/invariants/devbox-dind.md` + `packages/keel-invariants/src/invariants.manifest.ts:249-255`]
- `INV-devbox-egress-contract` (Story 2.3 fail-closed egress) — [Source: `docs/invariants/devbox-egress.md` + `packages/keel-invariants/src/invariants.manifest.ts:257-263`]
- Story 2.1 Dockerfile + compose baseline — [Source: `packages/devbox/Dockerfile`, `packages/devbox/docker-compose.yml`, `packages/devbox/entrypoint.sh`]
- Story 2.2 `.envrc.example` tmpfs knobs — [Source: `packages/devbox/.envrc.example:28-31`]
- Story 2.3 egress scripts + capability usage — [Source: `packages/devbox/scripts/start-egress.sh`, `packages/devbox/scripts/reload-egress.sh`]
- Story 2.4 whitelist.sh `/run/` state files — [Source: `packages/devbox/scripts/whitelist.sh:28-36` constants block]
- Story 2.3 invariant registration precedent — [Source: `_bmad-output/implementation-artifacts/2-3-egress-policy-dnsmasq-nftables-fail-closed-ipv4-ipv6-parity-atomic-reload.md` Tasks 10.3-10.5 + SC-10]
- Story 2.4 SC-15 no-new-invariant precedent (consumer-only) — [Source: `_bmad-output/implementation-artifacts/2-4-whitelist-source-of-truth-pnpm-devbox-whitelist-atomic-reload-cli.md` SC-15]

## Change Log

| Version | Date       | Description                                                                                                        |
| ------- | ---------- | ------------------------------------------------------------------------------------------------------------------ |
| v0.1    | 2026-04-22 | Story drafted at iter-184 via `/bmad-create-story`; 19 scope clarifications (SC-1..SC-19) + 12 Tasks + Dev Notes. |
| v0.2    | 2026-04-22 | Pre-dev SM validation pass at iter-185 via `/bmad-create-story (args: "review")`; FR14n `drafted → validated`. Two surgical polish edits applied: (a) SC-13 Smoke A + Task 11.5 command `echo "#!/bin/sh\necho hello"` → `printf "#!/bin/sh\necho hello\n"` (POSIX `echo` cannot emit newline without `-e`; original command would write a single-line literal, not a valid shebang+body); (b) SC-15 capability-inheritance mechanism clarified — Docker ≥19.03 ambient-cap propagation is the load-bearing path under `PR_SET_NO_NEW_PRIVS=1` (kernel-disables file-cap `F(effective)` bit per capabilities(7)); `setcap +eip` remains as portability fallback for runtimes without automated ambient-cap handling but is a no-op under Story 2.5's NNP posture. Dev-story smoke matrix (Task 11.7) serves as empirical verification — ambient-cap path confirmed by successful `nft list` + dnsmasq :53 bind. No AC / task / SC / scope changes. |
| v1.0    | 2026-04-22 | Story 2.5 landed at iter-187 via `/bmad-dev-story`; FR14n `atdd-scaffolded → in-dev → review`. Single-iteration landing per Story 2.4 iter-174 precedent. Substrate changes: Dockerfile (useradd dev UID/GID 1000 + chown -R dev:dev /home/dev + setcap +eip on dnsmasq/nft + USER dev before ENTRYPOINT); docker-compose.yml (cap_drop: ALL + three-cap cap_add + security_opt: [no-new-privileges:true] + tmpfs /tmp /var/tmp with exec=false,suid=false + named volume keel_home_dev for /home/dev + top-level volumes block); entrypoint.sh (WORKSPACE_OWNER default root:root → dev:dev + comment refresh); new `docs/invariants/devbox-hardening.md` (~130-line prose-only sourcePath doc); INVARIANTS.md (H3 `### Devbox hardening (Story 2.5)` + one-bullet anchor); invariants.manifest.ts (new `INV-devbox-homedev-named-volume` entry with contentHash `5b2e95462566cc67fe2a575886b0d94cd28a796cd2cf9dce26480598524d67f4`); packages/devbox/README.md (new `## Hardening (Story 2.5)` H2 section); AGENTS.md (new `### Container hardening (Story 2.5)` H3 subsection). SC-14 branch (i) happy path assumed for whitelist.sh `/run/` compatibility — empirical verification operator-workstation-deferred per Story 2.4 SC-17 precedent. Task 11 live smokes (AC 1–5 + cap-exercise + whitelist.sh parity) preserved as copy-paste recipes in invariant doc + README; execution operator-workstation-authoritative per backend-B constraint. Sync-gate + typecheck + compose parse all green at iter-187. Task 5.2b relocation branch NOT triggered; no edits to `packages/devbox/scripts/whitelist.sh` / `start-egress.sh` / `reload-egress.sh`. |
| v1.1    | 2026-04-22 | **`/bmad-create-story (args: "review")` post-dev SM requirements-satisfaction verification — verdict `sm-verified`; FR14n Story State `traced → sm-verified`; ZERO-PATCH + ZERO-DEFER close with operator-workstation-defer-acknowledgment variant.** Fifth Epic 2 post-dev SM precedent (Story 2.1 iter-127 → 2.2 iter-150 → 2.3 iter-160 → 2.4 iter-176 → 2.5 iter-189); **first architecture-security / container-hardening class post-dev SM** (2.1 runtime-infrastructure; 2.2 hybrid infrastructure + configuration; 2.3 infrastructure-security: daemon + kernel-rule + atomic-reload + log-tailer; 2.4 user-facing-CLI: bash dispatcher + regex + mutation-lock + atomic-replace + diff; 2.5 architecture-security: Dockerfile USER + compose cap_drop/cap_add/security_opt/tmpfs/volumes at Docker daemon + kernel-security subsystem boundary). **AC-by-AC verdict SATISFIED ×5 (substrate-grounded):** AC 1 SATISFIED — `packages/devbox/Dockerfile:261-263` `RUN groupadd --gid 1000 dev && useradd --uid 1000 --gid 1000 --shell /bin/bash --create-home dev && chown -R dev:dev /home/dev` (SC-1 UID/GID 1000 stable for first-boot named-volume auto-init; SC-3 image-build chown establishes dev:dev ownership under /home/dev); `Dockerfile:268` `USER dev` placed between `RUN chmod +x /usr/local/bin/entrypoint.sh` (:266) and `WORKDIR /workspace` (:270) per SC-2. Dockerfile build runs mkdir + package install + setcap as root (image-build context has full caps), then switches to dev before ENTRYPOINT resolves. AC 2 SATISFIED — `docker-compose.yml:118-125` emits `cap_drop: [ALL]` + `cap_add: [NET_ADMIN, NET_RAW, NET_BIND_SERVICE]` + `security_opt: [no-new-privileges:true]` as drafted. SC-4 three-cap reconciliation (NET_BIND_SERVICE required for :53 bind under cap_drop:[ALL] which strips the bounding set) propagated consistently across compose inline comment (:98-117) + `docs/invariants/devbox-hardening.md` § Capability bounding set + `INVARIANTS.md` H3 + `README.md` § Hardening § Capability rationale + `AGENTS.md` § Container hardening. AC 3 SATISFIED — `docker-compose.yml:134-136` emits service-level `tmpfs:` long-syntax for `/tmp` + `/var/tmp` with `exec=false,suid=false,size=${KEEL_DEVBOX_TMPFS_TMP_MB:-2048}m` + `exec=false,suid=false,size=${KEEL_DEVBOX_TMPFS_VARTMP_MB:-1024}m` per SC-8 (Compose option names `exec=false` = `noexec`, `suid=false` = `nosuid`; NFR8a parameterisation via Story 2.2 `.envrc.example:29-30` knobs preserved). `/var/log` intentionally not tmpfs-mounted per SC-8 (dnsmasq/nftables logs live under `/workspace/logs/` per Story 2.3 SC-17); `KEEL_DEVBOX_TMPFS_LOGS_MB` knob remains inert at 1.0. AC 4 SATISFIED — `docker-compose.yml:74-76` service `volumes:` block emits `type: volume, source: keel_home_dev, target: /home/dev` (named-volume form); `:154-155` top-level `volumes: { keel_home_dev: {} }` (default `local` driver; no driver_opts per SC-6). NO `KEEL_DEVBOX_*` environment variable toggles `/home/dev` to a host bind-mount form (SC-6 non-toggle invariant: the `type: volume` stanza is static in the compose file with no `${...}` interpolation). Invariant surface registered: `docs/invariants/devbox-hardening.md` (186 LOC prose-only sourcePath doc; walker-findable `\`INV-devbox-homedev-named-volume\`` anchor at end per SC-11), `INVARIANTS.md:102` H3 `### Devbox hardening (Story 2.5)` with one-bullet anchor per Story 2.3 iter-161 precedent, `packages/keel-invariants/src/invariants.manifest.ts:264-271` entry with `id` / `description` (SC-4 three-cap rationale + companion-invariants cross-reference) / `sourcePath: 'docs/invariants/devbox-hardening.md'` / `contentHash: '5b2e95462566cc67fe2a575886b0d94cd28a796cd2cf9dce26480598524d67f4'` / `anchors: ['INV-devbox-homedev-named-volume']`. SC-10 runtime compose-shape check correctly deferred to Story 2.17 / `packages/keel-invariants/src/check-devbox-compose-shape.ts`; Story 2.5 ships the invariant surface (sourcePath + manifest + index entry) with Story 1.9 sync-gate providing static drift detection per FR43. AC 5 SATISFIED — kernel-enforcement posture is **daemon-time-enforced** by Docker: `cap_drop: [ALL]` + `security_opt: [no-new-privileges:true]` + `tmpfs ... exec=false,suid=false` are applied at container-start by the Docker daemon; once the daemon accepts the compose file, the kernel enforces `noexec` (MNT_NOEXEC on `/tmp` + `/var/tmp` mount flags), `nosuid` (MNT_NOSUID masks setuid-bit on exec), and `PR_SET_NO_NEW_PRIVS=1` (kernel masks file-cap F(effective) + disables setuid-bit elevation per capabilities(7)) unconditionally. Live-smoke recipes for Smoke A (printf-based `/tmp/t.sh` noexec rejection) + Smoke B (sudo-unusable-under-NNP) preserved verbatim in `docs/invariants/devbox-hardening.md` § Verification + `packages/devbox/README.md` § Hardening § Verification per SC-13; execution operator-workstation-authoritative per Story 2.4 SC-17 backend-B precedent (DinD-B cannot safely exercise `docker exec` against cap-dropped containers without risk of poisoning host docker state). **SC-consistency audit ALL 19 SCs PASS:** SC-1/2/3 Dockerfile shape + USER placement + image-build chown verified at `Dockerfile:261-268`. SC-4 three-cap rationale propagated across 6 surfaces (compose inline comment + invariant doc + INVARIANTS.md + manifest description + README + AGENTS). SC-5 runtime chown best-effort preserved (`entrypoint.sh:91,105` chown calls retained per SC-5 expected-failure-under-cap-dropped semantics; stderr-capture + continue posture correct). SC-6 named-volume `keel_home_dev` at compose top level with default `local` driver; non-toggle under any `KEEL_DEVBOX_*` setting (no interpolation in the `type: volume source: keel_home_dev` stanza). SC-7 `entrypoint.sh:36` `WORKSPACE_OWNER="${KEEL_DEVBOX_WORKSPACE_OWNER:-dev:dev}"` flipped per SC-7 + existing regex at `:61` accepts `dev:dev` without edit (AR-2 widened class from iter-133). SC-8 tmpfs long-syntax with `exec=false,suid=false` + NFR8a knob parameterisation verified; `/var/log` intentionally not tmpfs per SC-8 rationale. SC-9 new invariant `INV-devbox-homedev-named-volume` with single sourcePath consolidating four sub-contracts (non-root dev user + capability bounding set + tmpfs posture + /home/dev named volume) per Story 2.3 SC-10 one-doc-one-contentHash precedent. SC-10 runtime compose-shape check correctly deferred; substrate-invariant surface registered at 1.0. SC-11 contentHash captured via three-step sync-gate protocol per Story 2.3 iter-162 precedent (placeholder → `pnpm keel-invariants:check` report → replace → re-run → exit 0). SC-12 NFR2 cold-start impact negligible (groupadd + useradd ~50ms build-time; compose hardening stanzas constant-time at daemon-start per Story 2.5 intent). SC-13 AC 5 smoke recipes preserved with printf-based /tmp/t.sh form (POSIX echo-without-`-e` correction from pre-dev SM iter-185 v0.2). SC-14 whitelist.sh `/run/` compatibility — branch (i) happy path assumed; no edits to `packages/devbox/scripts/whitelist.sh` / `start-egress.sh` / `reload-egress.sh`; empirical verification operator-workstation-deferred. SC-15 ambient-cap mechanism load-bearing under NNP + Docker ≥19.03; setcap +eip on `/usr/sbin/dnsmasq` + `/usr/sbin/nft` at `Dockerfile:231-232` preserved as portability fallback + pin-rationale comment documents both mechanisms + interaction. SC-16 `AGENTS.md:91` `### Container hardening (Story 2.5)` H3 subsection under `## Devbox iteration environment`, placed after `### Per-fork whitelist override (Story 2.4)` (chronological); 8 bulleted posture lines present. SC-17 `packages/devbox/README.md:296` `## Hardening (Story 2.5)` H2 section with § Posture + § Capability rationale (cap-by-cap table) + § Verification (copy-paste AC 1-5 + cap-exercise smokes) + § Known limitations + § Backend compatibility; ~60 LOC per SC-17 authoritative-spec-at-invariant-doc separation. SC-18 `.envrc.example` not edited per SC-18 less-edit-is-better (Story 2.2 owns the file). SC-19 exactly 8 files touched per scope declaration (Dockerfile + compose + entrypoint + new invariant doc + INVARIANTS.md + manifest + README + AGENTS); no scope creep into Stories 2.6/2.8/2.9/2.11/2.13/2.16/2.17 surfaces. **Scope-isolation audit PASS:** Story 2.6 host-side `pnpm devbox:*` CLI surface absent (repo-root `package.json` unchanged; `packages/devbox/package.json` has Story 2.4's `devbox:whitelist` entry preserved verbatim). Story 2.8/2.9 OAuth volume subpath populations absent (no `claude-code.json` / `gh-config.yml` substrate). Story 2.11 shared-workspace mode absent (compose workspace-mount parameterisation via `KEEL_DEVBOX_WORKSPACE` unchanged; no shared-mode flip). Story 2.13 healthcheck absent (no active `healthcheck:` stanza in compose). Story 2.16/2.17 hook/bypass-resistance absent (no `hooks/` directory or PreToolUse scaffolding). Story 2.3 primitive untouched (`reload-egress.sh` + `start-egress.sh` + `whitelist.sh` + dnsmasq/nftables templates byte-identical to Story 2.4 iter-183 closure). Story 2.3 invariant untouched (`INV-devbox-egress-contract` contentHash `cd1b5da113871ad5fc8b7321d2d0cb33dc87e99d9cb97a0d742eaacddfdee2d5` matches manifest; sync-gate green with 21 manifest entries including NEW `INV-devbox-homedev-named-volume`). **Operator-workstation-defer-acknowledgment (two-ground rationale retained):** Task 11 live smokes (AC 1 user-verification via `id dev` + `stat --format='%u:%g' /home/dev`; AC 2 capsh + `docker inspect` SecurityOpt; AC 3 findmnt + mount flags inspection; AC 4 `docker volume inspect` + mount runtime verification; AC 5 Smoke A /tmp noexec rejection + Smoke B sudo-unusable) + SC-15 capability-exercise smoke (`nft list table inet keel_egress` + `ss -tlnp :53`) + SC-14 whitelist.sh parity smoke remain operator-workstation-deferred per Task 11.9 Debug Log References + Story 2.4 SC-17 backend-B carve-out. This SM review EXPLICITLY ACCEPTS the deferral as **non-AC-blocking** on two grounds: (a) AC 1–5 are substrate commitments — Given/When/Then discharged by file content + commit of Dockerfile/compose stanzas + `docker compose config` parse-smoke GREEN from iter-187 (DinD-B) confirming the compose file parses cleanly with every hardening stanza (cap_drop/cap_add/security_opt/tmpfs/volumes); AC 5 kernel-enforcement is daemon-time-enforced (once the daemon accepts the compose file, the kernel unconditionally enforces `noexec`/`nosuid`/`PR_SET_NO_NEW_PRIVS`); (b) operator-workstation recipes are pinned verbatim at iter-187 in both `docs/invariants/devbox-hardening.md` § Verification + `packages/devbox/README.md` § Hardening § Verification — operator can re-verify against a running container without re-deriving from SC clauses. Runtime-only smokes verify integration + production deployment; they do NOT adjudicate AC satisfaction (AC-satisfaction-without-live-container posture per Story 2.3 iter-160 precedent; **stronger than Story 2.3** because Story 2.5 adds `docker compose config` parse-smoke evidence on top of the daemon-time-enforcement argument). **Class distinction vs Story 2.4 SM (iteration-env-evidence-density axis):** Story 2.4 had MORE iteration-env runtime evidence (SC-14 dual-composer parity smoke PASSED byte-identical + dispatcher exit-2 + regex rejection + multi-error collection + source-attribution listing + git check-ignore rule-fires) because bash CLI composition paths are verifiable via shell-level probes without requiring live kernel rules. Story 2.5 reverts to Story 2.3-SIMILAR zero-runtime-assertions posture + adds `docker compose config` parse-smoke as the in-iteration-env AC 2/3/4 static structure verification. Kernel-enforcement smokes (AC 5 runtime behavior + cap-exercise) require a live container which backend-B DinD cannot safely exercise under cap-dropped posture without poisoning host docker state. **CR-forecast envelope inherited from iter-188 trace:** 0 source-carve-out + backend-B live-smoke defer (+3) + ~160 LOC impl (+1.6) + class-boundary-density coefficient (architecture-security layer at Docker daemon + kernel-security subsystem boundary) = ~5.6 ceiling → forecast 4–8 PATCH opener; falls between Story 2.3 (9-PATCH infrastructure-security) and Story 2.4 (5-PATCH user-facing-CLI); tighter than Story 2.3 because static compose-shape verification is algorithmically simpler than daemon-kernel-rule-reload chains; broader than Story 2.4 because capability/kernel-security class invites cross-cutting adversarial probes (SC-4 three-cap reconciliation + SC-15 ambient-cap vs setcap interaction + SC-5 runtime-chown expected-failure + SC-14 whitelist.sh `/run/` compatibility are all CR-opener fodder). Story `Status` transitions `traced → sm-verified`; sprint-status row unchanged (SM-verification gate does NOT flip sprint-status — only CR closure does per Story 2.4 iter-176 precedent). FR14n `Story State` transitions `traced → sm-verified` in IP. Budget: ~52K tokens (orient ~8K + file reads ~10K + substrate validation ~15K + Change Log v1.1 authoring ~12K + IP + RALPH.md signpost + commit + push ~7K). **Next:** `/bmad-code-review (args: "2")` CR opener for `sm-verified → (fixes-pending \| done)` — three-layer Ralph-hosted adversarial fan-out (Blind Hunter + Edge Case Hunter + Acceptance Auditor) against cumulative Story 2.5 substrate diff filtered to in-scope files. |
| v1.2    | 2026-04-22 | **`/bmad-code-review (args: "2")` CR opener at iter-190 — verdict `fixes-pending`; FR14n Story State `sm-verified → fixes-pending`; 6 PATCH + 4 DEFER + ~17 DISMISS.** First Epic-2 CR opener against architecture-security / container-hardening class (predecessors: Story 2.1 iter-131 runtime-infrastructure class 2-PATCH; Story 2.2 iter-151 hybrid-config class 3-PATCH; Story 2.3 iter-161 infrastructure-security class 9-PATCH; Story 2.4 iter-177 user-facing-CLI class 5-PATCH). Triage envelope lands mid-forecast (4–8 PATCH per iter-188 trace ceiling; 6 PATCH actual — between Story 2.4's 5-PATCH and Story 2.3's 9-PATCH, consistent with class-boundary-density coefficient's architecture-security-at-daemon+kernel-security-boundary signal). Three-layer Ralph-hosted adversarial fan-out: Blind Hunter (diff-only adversarial, ~15 findings), Edge Case Hunter (edge-case JSON with file access, ~16 findings), Acceptance Auditor (spec-vs-diff reconciliation, 0 findings — **independently confirms iter-189 post-dev SM ZERO-PATCH AC verdict**). Diff baseline `b2533b0..HEAD` filtered to 8 SC-19 in-scope files (413 lines changed) with `:(exclude)_bmad-output/` + `:(exclude).ralph/` + `:(exclude)RALPH.md` + `:(exclude)_bmad/` per Story 2.3 iter-161 + Story 2.4 iter-177 precedent. **Triage outcomes:** 6 PATCH = AI-1 (dnsmasq `user=nobody` directives fatal under USER dev + dropped CAP_SETUID) + AI-2 (dnsmasq log-facility=/var/log/dnsmasq.log unwriteable under USER dev; pre-create dev-owned at build time) + AI-3 (compose project-name prefix mismatch — add `name: keel-devbox` so documented `keel-devbox_keel_home_dev` volume-name matches) + AI-4 (Smoke B `sudo --help` does not trigger NNP; replace with /proc/self/status NoNewPrivs inspection) + AI-5 (Smoke A "exec format error" alt is wrong error class; remove the OR clause) + AI-6 (libcap2-bin missing; `setcap` RUN fails docker-build). 4 DEFER = AR-7 (SC-14 branch (ii) /run writes — Story 2.6 carry-over) + AR-8 (SC-10 compose-override volumes replacement — Story 2.17 runtime compose-shape check) + AR-9 (/etc/resolv.conf + /etc/dnsmasq.conf writes under USER dev — Story 2.6 integration) + AR-10 (pre-Story-2.5 named-volume re-use — operator-migration doc) — appended to `_bmad-output/implementation-artifacts/deferred-work.md`. ~17 DISMISS bucketed silently (SC-acknowledged posture × 6, SC-15-documented × 3, SC-18-scope × 2, iter-187-empirically-verified × 2, machine-enforced × 2, not-a-defect × 5 → 20 total, de-duplicated to ~17 unique). **LESSON — architecture-security class CR opener surfaces INTEGRATION-class findings that spec-pure AC review cannot.** iter-189 post-dev SM verdict was ZERO-PATCH based on AC-vs-substrate reduction (all 5 ACs present in Dockerfile + compose + invariant surface). The CR opener then surfaced 6 PATCH items that are all integration concerns: dnsmasq.conf × 2 (user-drop + log-facility under USER dev), Dockerfile × 1 (libcap2-bin for setcap), compose × 1 (project-name), doc × 2 (smoke-command correctness). NONE violate an AC directly; ALL would block container-boot or invalidate operator-verification. Future-Ralph rule: for architecture-security class stories, the post-dev SM verdict ceiling is "AC satisfaction under the declared spec surface"; the CR opener ceiling is "AC satisfaction under the FULL runtime integration chain" — these are different bars, and SM ZERO-PATCH is compatible with CR >0 PATCH. Story 2.3 iter-160→iter-161 showed a similar pattern (0→9 PATCH jump at the SM→CR boundary for infrastructure-security); Story 2.4 iter-176→iter-177 was 0→5; Story 2.5 iter-189→iter-190 is 0→6. **LESSON — `/bmad-code-review (args: "2")` in Ralph-hosted mode requires general-purpose agents for Blind Hunter + Edge Case Hunter.** The specialised `bmad-review-adversarial-general` + `bmad-review-edge-case-hunter` subagent types are not exposed in the Claude Code runtime Agent-tool catalog; invocation MUST route through `general-purpose` with role-specific prompting. Acceptance Auditor via general-purpose with explicit "Acceptance Auditor" framing works end-to-end. Future-Ralph rule: at CR opener, immediately launch three parallel `general-purpose` subagents with Blind/Edge/Auditor framing — do not attempt named-subagent types that don't resolve. **LESSON — class-boundary-density coefficient predicts CR PATCH count more accurately than raw impl-LOC.** Story 2.5 = 413 LOC + 0 source-carve-outs + backend-B live-smoke defer → forecast 4–8 PATCH; actual 6 PATCH (mid-range). Story 2.3 = 1100 LOC + 0 carve-outs + backend-B defer → forecast 8–10 PATCH; actual 9 PATCH. Story 2.4 = 650 LOC + 0 carve-outs + backend-B defer → forecast 4–6 PATCH; actual 5 PATCH. The coefficient scales with the number of distinct kernel subsystem / daemon / file-format / cross-story-invariant surfaces touched, not LOC. Story 2.5 touched 5 class boundaries (Docker daemon cap-bounding-set + kernel NNP + tmpfs mount flags + named-volume lifecycle + Story 2.3 dnsmasq integration) — consistent with 6 PATCH actual. Future-Ralph rule: at trace time, count class boundaries crossed + add cross-story-invariant intersections; each adds 1 to the PATCH-envelope floor. **Sprint-status row unchanged** (2-5 stays `review`; CR closure is the sprint-status flip gate per Story 2.4 iter-183 + Story 2.3 iter-171 precedent). Story `Status` transitions `sm-verified → fixes-pending`. FR14n `Story State` transitions `sm-verified → fixes-pending` in IP; AI-1..AI-6 queued as top-of-QUEUE fix tasks; after QUEUE empties, re-run `/bmad-code-review (args: "2")` for `fixes-pending → sm-verified → done` CR-closure gate. Budget: ~80K tokens (orient ~8K + spec + diff prep ~10K + three subagent fan-out ~45K + triage + story-file edit + defers + IP + RALPH.md + commit + push ~17K). Under ~117K execution budget. |
| v1.3    | 2026-04-22 | **Story 2.5 CR drain iter-191 — AI-1 landed: remove dnsmasq `user=nobody` + `group=nogroup`, replace Story-2.5-revisit TODO with pin-rationale.** First of six planned drain iters (iter-191..iter-196; AI-1 → AI-2 → AI-6 → AI-3 → AI-4 → AI-5 cadence per CR opener triage). FR14n Story State stays `fixes-pending`; Story `Status` stays `fixes-pending` through drain iters; sprint-status `2-5` stays `review` per Story 2.4 iter-183 + Story 2.3 iter-171 precedent (CR closure is the sprint-status flip gate, not per-drain-iter). **Scope:** single file `packages/devbox/dnsmasq/dnsmasq.conf`; removed `user=nobody` + `group=nogroup` (previously :36-37); replaced the six-line Story-2.5-revisit TODO block (previously :33-35) with a 17-line `# --- Privilege posture (Story 2.5 — no user/group drop) ---` comment block explaining the two interlocking reasons the directives are BOTH impossible and redundant under Story 2.5's hardening posture — (1) `cap_drop: [ALL]` strips CAP_SETUID + CAP_SETGID from the bounding set, so `user=`/`group=` → setuid(2)/setgid(2) would EPERM → dnsmasq exit → reload-egress.sh exit 7 → start-egress.sh fail-closes → container never boots; (2) USER dev at `packages/devbox/Dockerfile:268` means dnsmasq already execs as UID 1000 from PID-1 — there is no root identity to drop from, so the directive is a no-op even if it didn't EPERM. Cross-references SC-15 ambient CAP_NET_BIND_SERVICE (setcap +eip on `/usr/sbin/dnsmasq` — Dockerfile:231) as the mechanism letting unprivileged dev bind :53 under NNP. **Closing DO-NOT-re-add guard** in the comment block preserves future-Ralph memory against inadvertent regression. **Scope boundary honoured:** `dnsmasq.conf` is a Story 2.3 artifact that carried an explicit Story-2.3 → Story-2.5 handoff TODO (the block we replaced); this is the sanctioned edit — no scope creep into `reload-egress.sh` / `start-egress.sh` / `whitelist.sh` (SC-19 forbidden list). **Quality gates:** `pnpm keel-invariants:check-all` GREEN (21 manifest entries; `INV-devbox-egress-contract` contentHash unchanged because its sourcePath is the prose invariant doc, not dnsmasq.conf; `INV-devbox-homedev-named-volume` unchanged); `pnpm run format:check` GREEN (dnsmasq conf files not prettier-matched, but the check runs clean across matched surfaces). `docker compose config` parse-smoke skipped — dnsmasq.conf is not referenced by docker-compose.yml at parse time (rendered at container-start by reload-egress.sh). Full build smoke operator-workstation-deferred per Story 2.4 SC-17 backend-B precedent; AI-6 libcap2-bin drain MUST land before next operator-workstation rebuild for the setcap RUN (Dockerfile:231-232) to succeed. **Remaining drain order:** AI-2 → AI-6 → AI-3 → AI-4 → AI-5 at iter-192..iter-196. After QUEUE empties, re-run `/bmad-code-review (args: "2")` for `fixes-pending → sm-verified → done` CR-closure gate (expected ZERO-PATCH close per Story 2.3 iter-171 + Story 2.4 iter-183 precedent). Budget: ~22K tokens (orient ~6K + dnsmasq.conf read + edit ~2K + quality-gate chain ~2K + story-file + IP + RALPH.md + commit + push ~12K). |

## Dev Agent Record

### Agent Model Used

claude-opus-4-7[1m] (Ralph loop iter-184 drafted; iter-185 pre-dev SM validated; iter-186 ATDD-skipped; iter-187 dev-story landed via `/bmad-dev-story`).

### Debug Log References

- **Sync-gate capture (Subtask 8.2):** `pnpm keel-invariants:check` with placeholder `contentHash: '0000...'` reported `expectedHash: 0000...` / `actualHash: 5b2e95462566cc67fe2a575886b0d94cd28a796cd2cf9dce26480598524d67f4` → replaced placeholder → re-run exit 0 → `pnpm keel-invariants:check-all` exit 0 (sync-gate + tokens-sync both green).
- **Compose-parse smoke (Subtask 11.9):** `docker compose config` (invoked from `packages/devbox/` in DinD-B iteration env) parses cleanly; output shows `cap_drop: [ALL]`, `cap_add: [NET_ADMIN, NET_RAW, NET_BIND_SERVICE]`, `security_opt: [no-new-privileges:true]`, `tmpfs: [/tmp:exec=false,suid=false,size=2048m, /var/tmp:exec=false,suid=false,size=1024m]`, `volumes: [<workspace bind>, <type: volume source: keel_home_dev target: /home/dev>]`, top-level `volumes.keel_home_dev` with project-namespaced name `devbox_keel_home_dev`. Confirms AC 2/3/4 static structure; live `docker exec` smokes against a running container operator-workstation-deferred.
- **Task 5 empirical gate (SC-14 `/run/` ownership):** deferred to operator-workstation smoke per Story 2.4 SC-17 precedent; SC-14 branch (i) happy path assumed (image-layer `/run/` root:root + tmpfs auto-mount preserves ownership; dev-user processes write to `/run/keel-*` files created at start-egress.sh time). If operator smoke surfaces an ownership problem, relocation to `/tmp/keel-state/` is a Story 2.6 carry-over per SC-14 branch (ii).
- **Task 11 live smokes operator-workstation-deferred:** DinD backend B cannot safely exercise `docker exec` sequences against cap-dropped containers without risk of poisoning host docker state (Story 2.4 SC-17 precedent). All AC 1–5 smokes + capability-exercise smoke + whitelist.sh parity smoke are recorded as copy-paste recipes in `docs/invariants/devbox-hardening.md` § Verification + `packages/devbox/README.md` § Hardening § Verification. M4-Pro native Docker Desktop is the authoritative AC 5 + AC 2 bounding-set verification environment.
- **SC-4 three-cap reconciliation carried through:** AC 2 drafted "only NET_ADMIN and NET_RAW" parenthetical reconciled with `NET_BIND_SERVICE` third cap across compose + invariant doc + INVARIANTS.md + README + AGENTS.md. Consistent rationale in every surface: cap_drop: [ALL] excludes NET_BIND_SERVICE even from root-equivalent processes → dnsmasq cannot bind :53 without explicit cap_add. No PRD text edited (FR44 manifest-authority — invariant doc supersedes PRD NFR7 narrative).
- **SC-15 ambient-cap mechanism preserved:** Dockerfile setcap +eip remains as portability fallback for runtimes without Docker ≥19.03 ambient-cap automation; harmless no-op under Story 2.5's NNP posture. Pin-rationale comment above setcap block documents the NNP/ambient interaction explicitly.

### Completion Notes List

- Substrate layered-barrier posture delivered end-to-end per NFR7 + NFR8 + NFR8a + NFR10. All 5 ACs + 19 SCs honored; 11 of 12 Tasks executed at iter-187; Task 11 execution operator-workstation-deferred (recipes authored, live run deferred).
- Invariant surface registered at 1.0 with contentHash-bound manifest entry + INVARIANTS.md anchor + prose-only `docs/invariants/devbox-hardening.md`. Runtime compose-shape check deferred to Story 2.17 per SC-10; static drift detection via Story 1.9 sync-gate is sufficient at 1.0 given SC-6's unconditional named-volume-form phrasing in the compose file.
- Entrypoint-runtime chown calls retained verbatim under SC-5 expected-failure-under-hardened-posture semantics. Harmless no-op on most hosts via bind-mount UID passthrough; useful under `--privileged` debugging; fail-tolerant via existing stderr-capture + continue.
- `.envrc.example` NOT edited (SC-18 less-edit-is-better; Story 2.2 owns the file). `KEEL_DEVBOX_TMPFS_LOGS_MB` knob remains inert at 1.0; documentation in README + invariant doc records the deferral.
- Dockerfile setcap block lands as pin-rationale-commented belt-and-braces. Under Docker ≥19.03 + NNP + USER dev, ambient-cap propagation via `prctl(PR_CAP_AMBIENT_RAISE)` is load-bearing; setcap is a no-op under NNP (file-cap F(effective) bit kernel-masked per capabilities(7)) + portability fallback for runtimes without automated ambient-cap handling.
- Eight files touched per SC-19 boundary: `packages/devbox/Dockerfile` + `packages/devbox/docker-compose.yml` + `packages/devbox/entrypoint.sh` + `docs/invariants/devbox-hardening.md` (NEW) + `INVARIANTS.md` + `packages/keel-invariants/src/invariants.manifest.ts` + `packages/devbox/README.md` + `AGENTS.md`. Plus `_bmad-output/implementation-artifacts/sprint-status.yaml` (lifecycle enum) + this story file (per workflow spec).
- Story 2.4 whitelist.sh flow NOT regressed at this iteration (SC-14 branch (i) happy path assumed; Task 5 operator-workstation-deferred).
- Live smokes operator-workstation-authoritative per Story 2.4 SC-17 precedent. 7th FR14n ATDD-skip / live-smoke-defer pairing (Story 2.1 iter-127 + 2.2 iter-147 + 2.3 iter-158 + 2.4 iter-173 + 2.5 iter-186 + this iter-187) — Epic-2 infrastructure-security class.

### File List

- `packages/devbox/Dockerfile` (modified — useradd dev UID/GID 1000 + chown -R dev:dev /home/dev + setcap +eip on dnsmasq/nft + USER dev before ENTRYPOINT + two pin-rationale comment blocks)
- `packages/devbox/docker-compose.yml` (modified — cap_drop: [ALL] + cap_add: [NET_ADMIN, NET_RAW, NET_BIND_SERVICE] + security_opt: [no-new-privileges:true] + tmpfs /tmp /var/tmp long-syntax with exec=false,suid=false + service volumes block extended with type: volume keel_home_dev → /home/dev + new top-level volumes: keel_home_dev: {} + header-checklist refresh + Story 2.5 TODO markers removed; Story 2.11/2.12/2.13/2.8-2.9 TODOs preserved)
- `packages/devbox/entrypoint.sh` (modified — WORKSPACE_OWNER default root:root → dev:dev + comment refresh at header + completed-substrate reference block replacing TODO(Story 2.5); chown calls retained verbatim)
- `docs/invariants/devbox-hardening.md` (**NEW** — ~130-line prose-only sourcePath doc for `INV-devbox-homedev-named-volume`; H1 + § Intent + § Contract with 4 H3 sub-contracts + § Enforcement (Static drift + Runtime compose-shape check deferred) + § Verification (AC 1–5 + cap-exercise smokes) + § Backend compatibility + § Companion invariants + § Amendment + § Consumption + § Extension + walker-findable anchor `INV-devbox-homedev-named-volume` at end)
- `INVARIANTS.md` (modified — new H3 `### Devbox hardening (Story 2.5)` between Devbox egress + Gitignored-secret sections + one-bullet `INV-devbox-homedev-named-volume` anchor)
- `packages/keel-invariants/src/invariants.manifest.ts` (modified — new entry `INV-devbox-homedev-named-volume` inserted after `INV-devbox-egress-contract` with description / sourcePath / contentHash `5b2e95462566cc67fe2a575886b0d94cd28a796cd2cf9dce26480598524d67f4` / anchors)
- `packages/keel-invariants/dist/invariants.manifest.js` + `.d.ts` + `.js.map` + `.d.ts.map` (regenerated by `pnpm --filter @keel/keel-invariants build`)
- `packages/devbox/README.md` (modified — new `## Hardening (Story 2.5)` H2 section with § Posture bullets + § Capability rationale (cap-by-cap table) + § Verification (copy-paste AC 1–5 + cap-exercise smokes) + § Known limitations + § Backend compatibility, placed between § Known upstream bugs fixed and § cc-devbox upstream provenance)
- `AGENTS.md` (modified — new H3 subsection `### Container hardening (Story 2.5)` under `## Devbox iteration environment`, placed after `### Per-fork whitelist override (Story 2.4)` with 8 bulleted posture lines)
- `_bmad-output/implementation-artifacts/sprint-status.yaml` (modified — `2-5-container-hardening-...` enum `ready-for-dev → in-progress → review` + last_updated bump)
- `_bmad-output/implementation-artifacts/2-5-container-hardening-non-root-user-capabilities-tmpfs-noexec-named-volume.md` (this file — Status `ready-for-dev → review` + 12 Task checkboxes marked `[x]` + Dev Agent Record populated + Change Log v1.0 row appended)

## Review Findings

### Post-dev SM (iter-189, v1.1) — `sm-verified`

ZERO-PATCH + ZERO-DEFER close per Change Log v1.1. All 5 ACs SATISFIED against landed substrate:

- **AC 1** — `packages/devbox/Dockerfile:261-268` (useradd dev UID/GID 1000 + chown -R dev:dev /home/dev + USER dev before WORKDIR/ENTRYPOINT).
- **AC 2** — `packages/devbox/docker-compose.yml:118-125` (cap_drop: [ALL] + cap_add: [NET_ADMIN, NET_RAW, NET_BIND_SERVICE] + security_opt: [no-new-privileges:true]); SC-4 three-cap reconciliation propagated consistently across 6 surfaces.
- **AC 3** — `packages/devbox/docker-compose.yml:134-136` (tmpfs /tmp + /var/tmp with `exec=false,suid=false,size=${KEEL_DEVBOX_TMPFS_{TMP,VARTMP}_MB:-N}m` long-syntax).
- **AC 4** — `packages/devbox/docker-compose.yml:74-76` (service `type: volume source: keel_home_dev target: /home/dev`) + `:154-155` (top-level `volumes: keel_home_dev: {}`); non-toggle-able (no `${KEEL_DEVBOX_*}` interpolation in the volume stanza); invariant surface registered at `docs/invariants/devbox-hardening.md` + `INVARIANTS.md:102` + `packages/keel-invariants/src/invariants.manifest.ts:264-271` with contentHash `5b2e95462566cc67fe2a575886b0d94cd28a796cd2cf9dce26480598524d67f4`. SC-10 runtime compose-shape check correctly deferred to Story 2.17.
- **AC 5** — daemon-time-enforced by Docker (cap_drop + security_opt + tmpfs noexec/nosuid flags applied at container-start; kernel unconditionally enforces `MNT_NOEXEC` + `MNT_NOSUID` + `PR_SET_NO_NEW_PRIVS=1`); live-smoke recipes preserved in `docs/invariants/devbox-hardening.md` § Verification + `packages/devbox/README.md` § Hardening § Verification per SC-13; execution operator-workstation-authoritative per Story 2.4 SC-17 backend-B precedent.

All 19 SCs (SC-1..SC-19) verified; scope-isolation audit PASS (Story 2.6/2.8/2.9/2.11/2.13/2.16/2.17 surfaces not touched; Story 2.3 primitive + invariant byte-identical). Full narrative rationale + CR-forecast envelope in Change Log v1.1.

### `/bmad-code-review` (iter-190, v1.2) — `fixes-pending` (6 PATCH + 4 DEFER + ~17 DISMISS)

Three-layer Ralph-hosted adversarial fan-out (Blind Hunter + Edge Case Hunter + Acceptance Auditor) against cumulative Story 2.5 substrate diff (`b2533b0..HEAD` filtered to 8 SC-19 in-scope files, 413 lines changed). Acceptance Auditor returned ZERO AC violations (independently confirms iter-189 post-dev SM verdict); Blind Hunter + Edge Case Hunter surfaced 31 raw findings → 6 PATCH + 4 DEFER + ~17 DISMISS after triage. Adversarial class: first post-dev CR against architecture-security / container-hardening — integration concerns dominant (runtime USER dev × Story 2.3 dnsmasq + egress-init chain) vs spec-pure AC class. Envelope lands mid-range of iter-188 forecast (4–8 PATCH ceiling between Story 2.3 9-PATCH infrastructure-security and Story 2.4 5-PATCH user-facing-CLI).

#### PATCH (6 — to drain iter-191..iter-196, one per iter per § Story Lifecycle `fixes-pending` row)

- [x] **[Review][Patch] AI-1 — dnsmasq `user=nobody` + `group=nogroup` directives fatal under USER dev + `cap_drop: [ALL]`** [`packages/devbox/dnsmasq/dnsmasq.conf:36-37`] — **drained at iter-191:** removed `user=nobody` + `group=nogroup` (lines 36-37); replaced the Story-2.5-revisit TODO at :33-35 with a two-clause pin-rationale block explaining (1) `cap_drop: [ALL]` strips CAP_SETUID/SETGID so setuid(65534)/setgid would EPERM even if attempted, and (2) USER dev at Dockerfile:268 means dnsmasq execs as UID 1000 from PID-1 — nothing to drop to; ambient CAP_NET_BIND_SERVICE (setcap +eip on `/usr/sbin/dnsmasq` per SC-15) lets dev bind :53 under NNP. Sync-gate green (dnsmasq.conf not registered as invariant sourcePath; `INV-devbox-egress-contract` unaffected — its contentHash tracks the egress invariant doc prose, not dnsmasq.conf content); `pnpm keel-invariants:check-all` + `pnpm run format:check` PASS. **Original finding (retained for audit trail):** Under USER dev (Dockerfile:268) dnsmasq starts as UID 1000; `user=nobody` triggers `setuid(65534)` which requires CAP_SETUID (not in the three-cap `cap_add` list per SC-4) → EPERM → dnsmasq fatal-exits → reload-egress.sh exits 7 → start-egress.sh fail-closes → container never boots. dnsmasq.conf:33-35 already carries an explicit Story 2.5 TODO comment flagging this revisit. Fix: remove `user=nobody` + `group=nogroup` from dnsmasq.conf (dnsmasq is already running as UID 1000 via USER dev — no privilege-drop needed); replace TODO comment block with Story 2.5 pin-rationale explaining the USER-dev-supersedes-user-directive posture. Scope: dnsmasq.conf (owned by Story 2.3 but not in SC-19 forbidden list; the Story 2.5 TODO is an explicit Story-2.3 → Story-2.5 handoff).
- [ ] **[Review][Patch] AI-2 — dnsmasq `log-facility=/var/log/dnsmasq.log` unwriteable under USER dev** [`packages/devbox/dnsmasq/dnsmasq.conf:43`] — dnsmasq.conf points log-facility at /var/log/dnsmasq.log; under USER dev dnsmasq (UID 1000) cannot write to root:root /var/log. Contradiction with AGENTS.md § Container hardening prose claim that "dnsmasq/nftables logs live under /workspace/logs/ per Story 2.3 SC-17" (AGENTS.md prose is imprecise — raw dnsmasq log actually writes /var/log then re-emits JSONL to /workspace/logs/ via egress-log-tailer.sh). Fix: add `RUN install -m 0644 -o dev -g dev /dev/null /var/log/dnsmasq.log` to `packages/devbox/Dockerfile` (after the useradd chain at :263, before `COPY entrypoint.sh`) — pre-creates the log file as dev-owned without chown'ing /var/log globally (preserves dpkg/apt log ownership). Preserves Story 2.3's egress-log-tailer.sh path (tails /var/log/dnsmasq.log) — no tailer or script edits required.
- [ ] **[Review][Patch] AI-3 — Verification commands use wrong compose project-namespace prefix `keel-devbox_keel_home_dev`** [`packages/devbox/README.md:340` + `docs/invariants/devbox-hardening.md:118-119`] — Compose project name defaults to directory basename (`devbox`), producing the actual volume name `devbox_keel_home_dev` — not `keel-devbox_keel_home_dev` as documented. Operator `docker volume inspect keel-devbox_keel_home_dev` + `docker inspect keel-devbox --format '… {{ .Source }} …'` verification commands will fail or mislead. Fix: add `name: keel-devbox` as the first top-level key of `packages/devbox/docker-compose.yml` so the project name matches the `keel-devbox` container-name prefix and the documented volume prefix — aligns compose semantics with prose. No changes to existing verification commands; no volume-namespace regression (fresh image-build re-creates volumes).
- [ ] **[Review][Patch] AI-4 — Smoke B `sudo --help` does not trigger NNP check** [`packages/devbox/README.md:348-349` + `docs/invariants/devbox-hardening.md:135-139`] — `sudo --help` prints help and exits 0 without attempting privilege elevation; does NOT exercise `PR_SET_NO_NEW_PRIVS=1` kernel path. Current smoke is false-positive (returns exit 0 under any posture) — cannot verify AC 5 second half (setuid blocked). Fix: replace with `docker exec keel-devbox sh -c 'grep ^NoNewPrivs /proc/self/status'` (expect `NoNewPrivs:\t1`) — direct kernel-flag inspection, version-independent, no dependence on sudo's internal detection heuristics. Alternative: `docker exec keel-devbox sudo id` (actual elevation attempt; expect non-zero with "sudo: effective uid is not 0" or NNP-specific message). Prefer /proc/self/status for robustness. Update both README + invariant doc.
- [ ] **[Review][Patch] AI-5 — Smoke A claims "exec format error" alternative is incorrect** [`packages/devbox/README.md:345` + `docs/invariants/devbox-hardening.md:127`] — Both docs read "Expect nonzero exit: 'Permission denied' or 'exec format error'". Noexec-mount kernel-refusal produces EACCES only (Permission denied); ENOEXEC ("exec format error") fires for malformed shebang or bad ELF magic — a distinct error class unrelated to MNT_NOEXEC. The OR clause misleads operator troubleshooting. Fix: remove "or 'exec format error'" from both doc surfaces — keep only "Permission denied".
- [ ] **[Review][Patch] AI-6 — `libcap2-bin` package missing; `setcap` RUN fails at image build** [`packages/devbox/Dockerfile:38-82`] — The apt install list at Dockerfile:37-82 does NOT include `libcap2-bin` (provides the `setcap` binary invoked at :231-232). Ubuntu 24.04 base image does not ship libcap2-bin by default; no currently-installed package lists it as a hard dependency. `docker build` would fail at the first setcap RUN with `setcap: command not found`. iter-187 did NOT exercise a full build (only `docker compose config` parse-smoke per Debug Log Subtask 11.9) so this hadn't surfaced. Fix: add `libcap2-bin` to the apt install list between `libcap2-bin \` (alphabetically near `libcups2t64`) — single-token addition, layer ordering unchanged.

#### DEFER (4 — recorded in `_bmad-output/implementation-artifacts/deferred-work.md` under `code review of story-2-5 (2026-04-22)`)

- [x] **[Review][Defer] AR-7 — /run writes under USER dev fail for whitelist.sh state files + dnsmasq pid-file** [`packages/devbox/scripts/whitelist.sh:198,286,341` + `packages/devbox/scripts/start-egress.sh:30-32` + `packages/devbox/dnsmasq/dnsmasq.conf:52`] — deferred, SC-14 branch (ii) explicit Story 2.6 carry-over (relocation to `/tmp/keel-state/` path if operator-workstation smoke surfaces ownership problem). iter-187 assumed SC-14 branch (i) happy path (image-layer /run ownership preserved by Docker tmpfs auto-mount); empirical verification operator-workstation-deferred.
- [x] **[Review][Defer] AR-8 — fork docker-compose.override.yml may replace volumes stanza / strip NET_BIND_SERVICE from cap_add** — deferred, SC-10 runtime compose-shape check explicitly deferred to Story 2.17 / `packages/keel-invariants/src/check-devbox-compose-shape.ts`. Substrate invariant surface + Story 1.9 sync-gate provide static drift detection; compose-override defeat requires runtime parse-and-assert which is Story 2.17 scope.
- [x] **[Review][Defer] AR-9 — `/etc/resolv.conf` + `/etc/dnsmasq.conf` writes from reload-egress.sh + start-egress.sh fail EACCES under USER dev** [`packages/devbox/scripts/start-egress.sh:50` + `packages/devbox/scripts/reload-egress.sh:119,158,288-289`] — deferred, integration-class concern. Fix options require either chown /etc/* at image-build time (invasive — /etc/resolv.conf is Docker bind-mount managed) OR scope extension to Story 2.3 primitive scripts (SC-19 forbidden list) OR compose-level ownership overrides. Carry to Story 2.6 operator-migration / host-side CLI surface where script edits are in-scope; operator-workstation smoke will surface the precise break and inform the fix path. iter-187 did NOT exercise runtime egress initialisation; if M4-Pro operator-workstation boot surfaces `FATAL: cannot write /etc/resolv.conf` per start-egress.sh:56, Story 2.6 fix escalates.
- [x] **[Review][Defer] AR-10 — Pre-Story-2.5 named-volume re-use preserves root-owned `/home/dev` tree** — deferred, operator-migration concern. If a `keel_home_dev` (or project-prefixed `devbox_keel_home_dev`) volume exists from a pre-Story-2.5 container run, Docker will not re-populate from the image layer and the root-owned tree mounts over /home/dev — Claude Code + gh OAuth writes fail silently EACCES. Fix path: add an operator-migration section to `packages/devbox/README.md § Hardening` documenting `docker volume rm` recovery procedure. Carry to Story 2.6 or Story 2.8 operator-docs pass; no current-state regression in fresh-volume first-boot happy path per SC-6.

#### DISMISS (~17 — silently dropped)

Dismiss rationale per finding-class (not individually enumerated — normal noise floor):

- SC-5-acknowledged posture (entrypoint chown under dropped CAP_CHOWN; /workspace not chowned at build time; host-UID mismatch on bind-mount): 3 findings.
- SC-15-documented mechanism (NET_BIND_SERVICE ambient-cap fragility; older Docker fallback path; setcap vs NNP interaction): 3 findings.
- SC-18-scope (Story 2.2 owns env-var validation — tmpfs size non-numeric + WORKSPACE_OWNER legacy): 2 findings.
- iter-187-empirically-verified (volumes top-level YAML indentation: parse-smoke GREEN; security_opt unquoted form: valid Compose syntax): 2 findings.
- Machine-enforced elsewhere (manifest contentHash drift: sync-gate; set -e + chown abort: existing stderr-capture pattern per SC-5): 2 findings.
- Not-a-defect (chmod-before-useradd ordering: works correctly; tmpfs size lowercase `m` suffix: both cases kernel-accepted; capsh format regex: minor robustness; Dockerfile ordering meta-fragility + /etc/skel dotfiles skip: cosmetic): 5 findings.

Total: 6 PATCH + 4 DEFER + ~17 DISMISS = 27 triaged findings from 31 raw (4 de-duplicated across layers during normalization).

Drain cadence: AI-1 → AI-2 → AI-6 → AI-3 → AI-4 → AI-5 one-per-iteration iter-191..iter-196 per § Story Lifecycle `fixes-pending → sm-verified` convergence; after QUEUE empties, re-run `/bmad-code-review (args: "2")` for the CR-closure gate (expected ZERO-PATCH close per Story 2.3 iter-171 + Story 2.4 iter-183 precedent). Sprint-status row `2-5` stays `review` through the drain; flips to `done` only at CR re-run closure.

_(populated at the CR-opener iteration)_
