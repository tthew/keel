# Story 1.10: Design-token schema + semantic-rationale contract

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a UX designer or front-end developer,
I want `packages/ui/tokens.schema.json` defining the DTCG-compatible token-contract shape AND `docs/invariants/tokens.md` documenting every semantic token's meaning with a stable `TOKEN-<slug>` ID,
So that downstream consumers (Story 1.11 source population, Story 1.12 emitter pipeline, Epic 7 catalog, Epic 3 TUI) reference tokens against a shared validated contract — no invisible semantic drift between runtimes (W1 party-mode amendment; UX-DR4).

## Acceptance Criteria

1. **Given** the substrate,
   **When** I inspect `packages/ui/tokens.schema.json`,
   **Then** the JSON Schema defines the shape of the token source (semantic names, scales, primitive references)
   **And** validates DTCG-format-compatible structure.

   **Story 1.10 scope carve-out:** the schema validates **structure** only — it asserts that every token object carries a `$type` ∈ `{color, dimension, duration, number, fontFamily, fontWeight, cubicBezier}`, a `$value` (string literal or `{reference}` alias), and that the top-level object exposes the named semantic groups below (§ Dev Notes → Semantic token inventory). Population of concrete token values is Story 1.11's scope; schema does not constrain numeric ranges, contrast ratios, or value quality (those are Story 1.13's pre-commit gates).

2. **Given** `docs/invariants/tokens.md`,
   **When** I read it,
   **Then** every semantic token (`surface.raised`, `status.success`, `text.primary`, `motion.scale.*`, `density.scale.*`, etc.) has a prose rationale line
   **And** each rationale carries a stable `TOKEN-<slug>` ID (e.g. `TOKEN-surface-raised`, `TOKEN-status-success`, `TOKEN-motion-snap`).

   **Story 1.10 scope carve-out:** `motion.scale.*` / `density.scale.*` in the epic's AC 2 shorthand refer jointly to the global dial (`motion.scale` / `density.scale` — leaf, `$type: number`, reduced-motion respects `= 0`) AND the named tiers that are the dial's siblings (`motion.{instant|snap|swift|smooth|drift}` / `density.{compact|default|comfortable}` — leaves, `$type: duration | number`). Stable IDs therefore resolve as: `TOKEN-motion-scale` (dial) + `TOKEN-motion-{instant|snap|swift|smooth|drift}` (tiers); `TOKEN-density-scale` (dial) + `TOKEN-density-{compact|default|comfortable}` (tiers). Three-segment IDs like `TOKEN-motion-scale-snap` are NOT used — the dial is the scaling multiplier (a leaf, not a group node), the tiers are discrete duration/density leaves, and DTCG does not permit a group node to carry `$type`. The flat two-segment form also matches architecture.md:693 `<category>.<semantic-name>.<modifier?>` ID pattern (category = `motion | density`; semantic-name = `scale | instant | snap | ...`; modifier unused).

3. **Given** schema + rationale,
   **When** Epic 7's catalog or Epic 3's TUI references a semantic token,
   **Then** the reference can be cross-linked back to `tokens.md`'s rationale via the stable `TOKEN-<slug>` ID
   **And** `tokens.md` is the source-of-truth rationale (Sally's "catalog header references rationale" requirement met — see UX spec § Architecture of the Design System).

4. **Given** the schema is the contract,
   **When** the manifest (Story 1.8) tracks it,
   **Then** an entry `INV-tokens-schema-contract` registers `packages/ui/tokens.schema.json` (schema contract), plus a companion entry `INV-tokens-semantic-rationale` registers `docs/invariants/tokens.md` (rationale contract)
   **And** drift between either file and its manifest `contentHash` is caught by Story 1.9's `pnpm keel-invariants:check`.

   **Story 1.10 scope carve-out:** Story 1.8's `Invariant` shape carries one `sourcePath` per entry (`packages/keel-invariants/src/invariants.manifest.ts:6-11`). Per-file registration is the established pattern (see Story 1.8's 10 existing entries + Story 1.9's `INV-ralph-halt-path-resolution` close-out); the two tokens-contract files get two sibling IDs. The epic's single-entry phrasing at `epics.md:951` (`entry INV-tokens-schema-contract registers tokens.schema.json + tokens.md`) is collapsed here to match the existing manifest shape without changing the `Invariant` schema. If a future story wants multi-file bundles, `InvariantSchema` extension lands there.

## Tasks / Subtasks

- [x] **Task 1: Author `packages/ui/tokens.schema.json`** (AC: 1)
  - [x] Create `packages/ui/tokens.schema.json` at repo-relative path (not under `src/` — it's a data contract consumed at lint / build time, not TS-compiled; parallels `packages/keel-invariants/src/schemas/*.schema.json` location pattern from architecture.md:920-927, adjusted for the `ui` package's data layout per ux-design-specification.md:358).
  - [x] Use JSON Schema draft-2020-12 (`"$schema": "https://json-schema.org/draft/2020-12/schema"`) — matches the `InvariantSchema` Zod-generated JSON Schema convention implicit in `packages/keel-invariants/src/schemas/` architecture handoff.
  - [x] Define DTCG-compatible token-node shape. Every leaf token MUST have:
    - `$type`: enum (`"color" | "dimension" | "duration" | "number" | "fontFamily" | "fontWeight" | "cubicBezier"`) — DTCG type classifier.
    - `$value`: string — either a literal (e.g. `"oklch(62% 0.16 245)"`, `"16px"`, `"150ms"`) OR a `{path.to.token}` alias. Regex: `/^(\{[a-z][a-z0-9]*(\.[a-z0-9][a-z0-9-]*)+\}|[^{}]+)$/`.
    - `$description` (optional): string — human-readable prose. OPTIONAL at schema level; AC 2 pins the rationale prose in `tokens.md`, not in the schema.
  - [x] Define top-level semantic groups as required `object` properties of the root schema. Minimum set (extracted from ux-design-specification.md § Visual Design Foundation):
    - `color.neutral.{50..950}` (primitive ramp)
    - `color.accent.{400|500|600}` (accent ramp; 400 for focus ring, 500 default, 600 hover)
    - `color.surface.{default|raised|inset|overlay}` (semantic surface)
    - `color.text.{primary|secondary|muted|inverse|accent}` (semantic text)
    - `color.border.{default|muted|accent}` (semantic border)
    - `color.status.{info|success|warning|error|critical}.{fg|bg}` (semantic status — fg/bg variants per architecture.md:693 modifier pattern)
    - `color.severity.{low|medium|high|critical}` (alias of status — see tokens.md rationale block)
    - `color.state.{pending|in-progress|blocked|done}` (Ralph kanban vocabulary; shared TUI + web)
    - `type.{xs|sm|base|lg|xl|2xl|3xl|4xl}` (modular type scale, 1.125 ratio — ux-design-specification.md:530)
    - `font.{sans|mono}` (font stack)
    - `space.{0..24}` (4px-base Tailwind stops — ux-design-specification.md:547)
    - `radius.{none|sm|md|lg|full}` (radius scale — ux-design-specification.md:551)
    - `motion.scale` (global dial, `$type: number`) + `motion.{instant|snap|swift|smooth|drift}` (named tiers — epics.md:973, stored as `$type: duration` leaves aliasable by `motion.scale`)
    - `density.scale` (global dial, `$type: number`) + `density.{compact|default|comfortable}` (named tiers — epics.md:974)
    - `breakpoint.{sm|md|lg|xl|2xl}` (viewport breakpoints — ux-design-specification.md:560)
  - [x] DTCG `modes` support: root schema accepts an optional `$modes` object keyed by mode name (`light | dark | high-contrast`). Mode overlays have the same shape as the base but may override any subset of leaf tokens. Story 1.10 declares the two required modes (`light`, `dark`) per ux-design-specification.md:484; Story 1.11 populates the values.
  - [x] Duplicate-path rejection: schema uses `additionalProperties: false` at every level so misspelled keys fail validation (AC 1 + UX-DR4 intent: "Malformed structure, missing `$type`, unresolved `{reference}`, or duplicate paths fail" — ux-design-specification.md:375).
  - [x] Unresolved-`{reference}` check: the literal/alias regex ensures `{...}` aliases are syntactically well-formed; full transitive-alias resolution is runtime (Story 1.12 emitter) not schema. Schema catches shape, not semantics.

- [x] **Task 2: Author `docs/invariants/tokens.md`** (AC: 2, 3)
  - [x] Create `docs/invariants/tokens.md` parallel to the established `docs/invariants/knowledge-files.md` + `docs/invariants/ralph-execute.md` pattern (same folder; same audience — normative spec for humans + AI agents).
  - [x] Header structure:
    - Title + one-paragraph purpose statement (why a separate rationale doc: Sally's "catalog header references rationale" requirement — prevents invisible semantic drift when Epic 7 catalog / Epic 3 TUI references tokens by name).
    - Promotion-rule pointer (same 4-row table used in `INVARIANTS.md` + `AGENTS.md` + `RALPH.md`; reference not duplicate).
    - Stable-ID convention: `TOKEN-<slug>` where `slug` is the token path lowercased with `.` → `-` (e.g. `surface.raised` → `TOKEN-surface-raised`; `motion.scale.snap` → `TOKEN-motion-scale-snap`; `status.success.fg` → `TOKEN-status-success-fg`).
    - Cross-runtime reminder: semantic tokens are shared between the Tailwind preset (`packages/ui/tailwind.preset.ts`, Story 1.12) + Textual TUI theme (`packages/devbox/tui/theme.py`, Story 1.12); rationale lines therefore speak to both runtimes (architecture.md:90 — cross-runtime semantic tokens).
  - [x] Rationale sections (one `###` per semantic group; one bullet per semantic token). Required groups + anchor bullet form:
    - `### Surfaces` — `TOKEN-surface-default`, `TOKEN-surface-raised`, `TOKEN-surface-inset`, `TOKEN-surface-overlay`.
    - `### Text` — `TOKEN-text-primary`, `TOKEN-text-secondary`, `TOKEN-text-muted`, `TOKEN-text-inverse`, `TOKEN-text-accent`.
    - `### Borders` — `TOKEN-border-default`, `TOKEN-border-muted`, `TOKEN-border-accent`.
    - `### Accent` — `TOKEN-accent-default`, `TOKEN-accent-fg`, `TOKEN-accent-focus`.
    - `### Status` — `TOKEN-status-info`, `TOKEN-status-success`, `TOKEN-status-warning`, `TOKEN-status-error`, `TOKEN-status-critical` (with `-fg` / `-bg` modifier variants enumerated explicitly).
    - `### Severity (alias of status)` — `TOKEN-severity-low|medium|high|critical` (rationale notes each aliases the matching `TOKEN-status-*`).
    - `### State (kanban vocabulary)` — `TOKEN-state-pending|in-progress|blocked|done` (rationale cites Ralph TUI origin + web-side parity).
    - `### Motion` — `TOKEN-motion-scale` (global dial; 0 = reduced-motion) + `TOKEN-motion-instant|snap|swift|smooth|drift`.
    - `### Density` — `TOKEN-density-scale` (global dial) + `TOKEN-density-compact|default|comfortable`.
    - `### Type scale` — `TOKEN-type-xs|sm|base|lg|xl|2xl|3xl|4xl`.
    - `### Spacing / radius / breakpoints` — one consolidated section is acceptable (primitive scales; rationale is the scale ratio + base unit, not per-stop); stable IDs `TOKEN-space-{N}`, `TOKEN-radius-{name}`, `TOKEN-breakpoint-{name}`.
  - [x] Each rationale line follows a consistent template: `- **\`TOKEN-<slug>\`** — *<semantic purpose>*. <one-sentence why this token exists / when to use it>. Value: <source citation — "see ux-design-specification.md § …" OR literal note that value lands in Story 1.11>.` Terse; one line per token. Rationale is *semantic* (why the slot exists), not *syntactic* (the schema covers shape).
  - [x] Closing section — § Consumption (same pattern as `INVARIANTS.md:52-54`): who reads this file + how Stories 1.11, 1.12, 1.13, and Epic 7 cross-reference it. Keep under 10 lines.
  - [x] Closing section — § Extension (FR44 pattern): forks override via `apps/web/tokens.fork.json` (DTCG merge; ux-design-specification.md:387). Keep under 5 lines.

- [x] **Task 3: Register both files in the invariants manifest + `INVARIANTS.md`** (AC: 4)
  - [x] Compute `sha256sum` of the two authored files post-Task 1 + Task 2:
    - `sha256sum packages/ui/tokens.schema.json`
    - `sha256sum docs/invariants/tokens.md`
  - [x] Append two entries to `packages/keel-invariants/src/invariants.manifest.ts` → `raw` array (after the last entry `INV-ralph-halt-path-resolution`; maintain trailing comma + array closure):
    ```typescript
    {
      id: 'INV-tokens-schema-contract',
      description:
        'Design-token JSON Schema contract — DTCG-compatible; defines shape of every semantic + primitive token group (color / type / space / radius / motion / density / breakpoint) plus optional $modes overlays. Validated at pre-commit (Story 1.13); consumed by the Story 1.11 source + Story 1.12 emitter.',
      sourcePath: 'packages/ui/tokens.schema.json',
      contentHash: '<sha256 from sha256sum invocation above>',
      anchors: ['INV-tokens-schema-contract'],
    },
    {
      id: 'INV-tokens-semantic-rationale',
      description:
        'Semantic-rationale doc pairing every TOKEN-<slug> with a prose line explaining why the slot exists + how cross-runtime consumers reference it. Companion to INV-tokens-schema-contract; Sally "catalog header references rationale" requirement.',
      sourcePath: 'docs/invariants/tokens.md',
      contentHash: '<sha256 from sha256sum invocation above>',
      anchors: ['INV-tokens-semantic-rationale'],
    },
    ```
  - [x] Append two sibling anchors to `INVARIANTS.md` under a new `### Design-token schema + semantic rationale (Story 1.10)` section (insert between the existing `### Ralph loop contracts` section at `INVARIANTS.md:48` and the § Consumption section). Anchor format MUST match the existing bullet shape (`- **\`INV-tokens-schema-contract\`** — <description>. Source: \`packages/ui/tokens.schema.json\`.`) — Story 1.9's anchor regex binds column 0 `-\s+\*\*\`INV-[a-z0-9]+(?:-[a-z0-9]+)+\`\*\*` per `packages/keel-invariants/src/sync-gate.ts:24`, so the two new bullets must be column-0 non-indented.
  - [x] **Forbidden**: adding anchors inside triple-backtick code fences (Story 1.9 CR defer iter-14 flagged this as a latent footgun — `deferred-work.md:38`). The rationale doc for the anchor format should use inline backticks (e.g. `` `INV-tokens-schema-contract` `` in prose), not fenced block examples with bare `INV-*` bullets.
  - [x] Verify: run `pnpm -w typecheck` — Zod `InvariantsSchema.parse(raw)` at import time validates both new entries (id regex, sha256-hex-length regex, anchors non-empty, sourcePath traversal refine, ID-uniqueness superRefine, shared-sourcePath cross-hash consistency superRefine — per `packages/keel-invariants/src/invariants.manifest.ts:1-47`). Any malformed field fails the typecheck loudly at first import.

- [x] **Task 4: Quality gates + sync-gate smoke + sprint-status bump** (no AC — substrate verification)
  - [x] `pnpm install` at repo root (no new deps expected — schema is static JSON, doc is markdown; confirm lockfile unchanged with `git diff --stat pnpm-lock.yaml`).
  - [x] `pnpm -w typecheck` — green (existing passes + the Task 3 import-time Zod validation covers the two new manifest entries).
  - [x] `pnpm -w lint` — green (no new TS source; ESLint walks JSON only if JSON plugin is wired — it isn't at Story 1.10 time; lint scope unchanged).
  - [x] `pnpm -w build` — green (no new TS → no dist change in `@keel/ui`; `@keel/keel-invariants` dist reflects the 2 new manifest entries).
  - [x] `pnpm format:check` — green (run `pnpm exec prettier --write packages/ui/tokens.schema.json docs/invariants/tokens.md packages/keel-invariants/src/invariants.manifest.ts INVARIANTS.md` first to normalise).
  - [x] `pnpm exec commitlint --from origin/main --to HEAD --verbose` — green on any commits made during this story.
  - [x] `pnpm exec prek run --all-files` — 3 hooks Pass (typecheck / lint / format-check).
  - [x] **Sync-gate smoke (clean path, AC 4 end-to-end):** `pnpm keel-invariants:check` → exit 0 on the post-Task-3 state. Zero stderr output. Confirms both new manifest entries pair cleanly with their `INVARIANTS.md` anchors AND the recomputed `sha256sum` of the two files matches the manifest `contentHash` fields.
  - [x] **Sync-gate smoke (drift path, AC 4 end-to-end):** append a single trailing newline to `packages/ui/tokens.schema.json`; rerun `pnpm keel-invariants:check` → exit 1 + structured JSON drift report on stderr naming `INV-tokens-schema-contract` as `content-hash-mismatch`; revert with `git checkout -- packages/ui/tokens.schema.json`; post-revert rerun → exit 0 + byte-identical restore (`git diff --stat packages/ui/tokens.schema.json` empty). Capture evidence in the Dev Agent Record § Completion Notes. Mirrors Story 1.9 Task 5's drift-smoke convention at `1-9-...md:95`.
  - [x] **Sync-gate smoke (anchor drift, AC 4 second branch):** delete the `INV-tokens-schema-contract` anchor line from `INVARIANTS.md`; rerun `pnpm keel-invariants:check` → exit 1 + `added-to-source-only` Drift entry naming `INV-tokens-schema-contract` + `sourcePath packages/ui/tokens.schema.json`; revert with `git checkout -- INVARIANTS.md`; post-revert rerun → exit 0. Confirms the sync-gate's anchor-walker branch realises the second half of drift detection (Story 1.9 AC 2 / AC 5 symmetric coverage). Capture evidence in § Completion Notes.
  - [x] Bump `_bmad-output/implementation-artifacts/sprint-status.yaml`: `1-10-design-token-schema-semantic-rationale-contract: ready-for-dev → done` (direct jump at `/bmad-dev-story` completion; skipped the `in-progress → review` BMad intermediate per Story 1.7 / 1.8 precedent — Ralph's FR14n lifecycle owns the trace / SM-review / CR gates that follow this dev-completion commit). `last_updated: 2026-04-20 Story-1-10-done UTC` (or current date). Preserve all existing comments + structure + STATUS DEFINITIONS block. The `backlog → ready-for-dev` hop already landed at `/bmad-create-story` time (iter-42); this Task covers only the `ready-for-dev → done` side.
  - [x] Flip this story file's Status to `done` at `/bmad-dev-story` completion (per Story 1.8 precedent — BMad `Status: done` is the terminal state in the story file; Ralph's FR14n `atdd-scaffolded → in-dev → traced → sm-verified → done` intermediates run in parallel via IP § Context state tracking, not story-file Status). All Task subtasks `[x]`. `## Dev Agent Record` populated with File List + Completion Notes.

## Dev Notes

- **Relevant architecture patterns:**
  - **Three-layer invariant pattern applied to design tokens** (architecture.md:85). This story ships the *source-layer* (schema) + *agent-readable-layer* (rationale doc); Story 1.12 ships the *runtime-layer* (generated `tokens.css` + `tailwind.preset.ts` + `theme.py`); Story 1.13 ships the machine-enforced gates (schema validation + contrast check + source-output sync).
  - **Cross-runtime semantic tokens** (architecture.md:90). Token names must work in both the Tailwind class vocabulary (`text-status-success`, `bg-surface-raised`) and the Textual style vocabulary (`$status-success`, `$surface-raised`). The rationale doc is the shared contract — UX designers + front-end devs + Ralph read the same file.
  - **Design-token ID pattern** (architecture.md:693). Format: `<category>.<semantic-name>.<modifier?>`. Examples: `color.status.success`, `color.status.success.bg`, `space.density.compact`, `motion.duration.fast`. Schema + rationale doc both follow this pattern; primitives (`color.neutral.500`, `type.base`) and semantics (`color.surface.raised`, `status.success.fg`) coexist in the same tree.
  - **W1 party-mode amendment** (epics.md:653-661). Design tokens are substrate, not Epic 7 scope — Ralph's TUI (`theme.py`) + Epic 5 generator consume them early; waiting for Epic 7 creates retcon debt. The *schema contract* (this story) and the *source population* (Story 1.11) and the *emitter pipeline* (Story 1.12) all land in Epic 1 so the three-layer invariants pattern has token-layer teeth from 1.0.

- **Source-tree components to touch:**
  - `packages/ui/tokens.schema.json` — NEW. JSON Schema contract. No `dist/` counterpart — this is a data file, not TS.
  - `docs/invariants/tokens.md` — NEW. Rationale doc. Joins the existing `docs/invariants/{knowledge-files.md, ralph-execute.md}` family.
  - `packages/keel-invariants/src/invariants.manifest.ts` — APPEND 2 entries. `raw` array grows from 10 → 12 invariants.
  - `INVARIANTS.md` — APPEND 1 new `###` section with 2 anchor bullets.
  - `_bmad-output/implementation-artifacts/sprint-status.yaml` — state bump (backlog → review via in-progress).

- **Semantic token inventory** (authoritative source: ux-design-specification.md § Visual Design Foundation, lines 480-604). Dev MUST enumerate every token in both the schema AND the rationale doc. Tokens explicitly named in AC 2 as canonical examples:
  - `surface.raised`, `status.success`, `text.primary` (three AC-named exemplars — MUST appear in `tokens.md`).
  - `motion.scale.*` — at least five tiers per epics.md:973: `instant | snap | swift | smooth | drift`.
  - `density.scale.*` — at least three tiers per epics.md:974: `compact | default | comfortable`.
  - Full inventory is enumerated by Task 1 subtasks (see § Tasks → Task 1 bullet list for the top-level semantic groups). Primitive scales (`neutral.50..950`, `type.xs..4xl`, `space.0..24`) are registered as contract shape but carry terser group-level rationale; semantic tokens (`surface.*`, `text.*`, `status.*`, `severity.*`, `state.*`) each get a dedicated bullet line with stable ID.

- **DTCG format refresher** (ux-design-specification.md:350-376):
  - Each leaf: `{ "$type": "...", "$value": "..." }` OR `{ "$type": "...", "$value": "{path.to.other.token}" }` for aliases.
  - Groups nest naturally: `"color": { "status": { "success": { "fg": { "$type": "color", "$value": "..." } } } }`.
  - `$type` enum used here: `color | dimension | duration | number | fontFamily | fontWeight | cubicBezier`. Full DTCG spec also supports `strokeStyle | border | transition | shadow | gradient | typography` — not needed at Story 1.10.
  - Modes: top-level `"$modes": { "light": {...overlay}, "dark": {...overlay} }` per ux-design-specification.md:484.

- **Testing standards summary** (architecture.md:650-652 + Story 1.8 / 1.9 precedent):
  - **No test runner at Story 1.10 time** (lands Story 1.16). Verification is substrate-layer: `pnpm -w {typecheck,lint,build,format:check}` + `pnpm exec prek run --all-files` + `pnpm keel-invariants:check` (Story 1.9 runtime tool).
  - **Sync-gate smoke** (AC 4) is the end-to-end evidence for this story — analogous to Story 1.9 Task 5 smoke patterns at `1-9-...md:94-97`. Two smoke branches required: `content-hash-mismatch` (modify schema file → exit 1 → revert) + `added-to-source-only` (delete anchor → exit 1 → revert). Byte-identical round-trip is the pass criterion.
  - **Deferred unit + integration tests**: once Story 1.16 lands the test runner, backfill per-AC unit tests can verify schema-structural validation (AC 1 schema rejects malformed inputs), rationale-doc completeness (AC 2 every token has a rationale ID — grep-based assertion), cross-link integrity (AC 3 every schema token ID ≡ a `TOKEN-<slug>` in the doc). None of these block Story 1.10's `review → done` transition.

### Project Structure Notes

- **Alignment with unified project structure.** `packages/ui/tokens.schema.json` follows architecture.md:358 exactly (`packages/ui/tokens.schema.json ← JSON Schema for validation`). `docs/invariants/tokens.md` matches the existing `docs/invariants/*.md` family (architecture.md:858 — `tokens.md ← Design-token manifest explainer`).
- **Variance from architecture handoff**: architecture.md:915 and :1216 name the token source as `packages/keel-invariants/src/design-tokens.ts` (typed variant); the UX spec and epics.md:963 carry the **DTCG JSON variant** as the preferred source with the typed variant listed as a deferred decision. Story 1.10 ships the schema for the DTCG variant per the UX spec's primary recommendation (ux-design-specification.md:358 + :373). The typed-variant path is not closed off — Story 1.11 ratifies the source-file choice (schema validates either representation since DTCG is portable). No blocking variance.
- **No conflict** with Story 1.8 manifest shape — per § AC 4 scope carve-out, the two-files-two-entries convention preserves the single-`sourcePath` `Invariant` contract pinned at `packages/keel-invariants/src/invariants.manifest.ts:6-11`. No manifest schema extension required.
- **Ralph L1/L2 layering** (architecture.md:786-788 + `.ralph-safe-set.yaml` — not yet landed; Story 3.24 scope). `packages/ui/tokens.schema.json` + `docs/invariants/tokens.md` are L3 lint-guarded at Story 1.10 time (standard substrate files; no Ralph self-modification risk). When Story 3.24's safe-set manifest lands, these files can be registered as L3 if desired.

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story-1.10-Design-token-schema--semantic-rationale-contract] — lines 926-952 — primary ACs + W1 amendment context.
- [Source: _bmad-output/planning-artifacts/epics.md#Epic-1-Substrate-Foundation] — lines 635-666 — W1/W2 amendment rationale; token-contract positioning as substrate.
- [Source: _bmad-output/planning-artifacts/architecture.md#Design-token-ID-Pattern] — lines 691-695 — `<category>.<semantic-name>.<modifier?>` ID format; cross-runtime stable-ID contract.
- [Source: _bmad-output/planning-artifacts/architecture.md#Three-layer-invariant-pattern] — lines 85-90 — cross-runtime semantic tokens invariant.
- [Source: _bmad-output/planning-artifacts/ux-design-specification.md#Architecture-of-the-Design-System] — lines 340-398 — DTCG source-of-truth decision; emitter pipeline diagram; customization + extension strategy.
- [Source: _bmad-output/planning-artifacts/ux-design-specification.md#Visual-Design-Foundation] — lines 480-604 — complete semantic token inventory (color / typography / spacing / radius / density / motion / breakpoints).
- [Source: _bmad-output/implementation-artifacts/1-8-invariants-manifest-ts-contract-exporter.md] — `Invariant` interface + `InvariantSchema` precedent; 10-entry canonical list.
- [Source: _bmad-output/implementation-artifacts/1-9-invariant-sync-gate-runtime-tooling-reader-walker-drift-detector.md] — sync-gate runtime tool + smoke-test pattern; `pnpm keel-invariants:check` entry point.
- [Source: packages/keel-invariants/src/invariants.manifest.ts] — `raw` array append site; Zod schema (regex + refine + superRefine layers).
- [Source: packages/keel-invariants/src/sync-gate.ts:24] — `ANCHOR_REGEX` column-0 bullet form — required anchor shape in `INVARIANTS.md`.
- [Source: INVARIANTS.md] — section-heading convention (`### <topic> (Story N.M)`); anchor bullet shape; § Consumption + § Extension patterns.
- [Source: docs/invariants/knowledge-files.md + docs/invariants/ralph-execute.md] — sibling normative-spec format for `docs/invariants/*.md` files; purpose statement + stable-ID convention + cross-reference footer.
- [Source: _bmad-output/implementation-artifacts/deferred-work.md] — lines 36-40 — Story 1.9 carry-forward: anchor-regex column-0 binding + code-fence footgun (avoid both in the new `INVARIANTS.md` section).

## Dev Agent Record

### Agent Model Used

claude-opus-4-7[1m] (Ralph iter-45; `/bmad-dev-story` workflow).

### Debug Log References

- **Story-spec internal inconsistency resolved (Accent group):** Task 1 schema enumerates `color.accent.{400|500|600}` (primitive ramp); Task 2 rationale bullet lists `TOKEN-accent-default|fg|focus` (semantic triad). Resolution: schema extended to include both (`color.accent.{400,500,600,default,fg,focus}` — six `leafColor` leaves under the flat `accent` group); rationale doc carries six bullets (three primitive + three semantic) under `### Accent`. This preserves AC 3 cross-link integrity — every schema token ID resolves to a rationale bullet. Tailwind + Textual consumers will reach for the semantic form (`TOKEN-accent-default`) first; the ramp is for bespoke needs.
- **Neutral rationale:** Task 2 bullet list omits a Neutral section; schema has `color.neutral.{50..950}`. Resolution: added `### Neutral (primitive ramp)` section with group-level rationale (mirrors the consolidated `### Spacing / radius / breakpoints` pattern explicitly permitted by Task 2); enumerates all eleven stable IDs (`TOKEN-neutral-50..950`) for AC 3 cross-link parity.
- **Drift-smoke #1 (`content-hash-mismatch`):** appended a single trailing newline to `packages/ui/tokens.schema.json` (new untracked file — `git checkout --` unavailable); `pnpm keel-invariants:check` → exit 1 with structured JSON drift naming `INV-tokens-schema-contract` as `content-hash-mismatch` (expected `abb5bc4c...e07ac`, actual `85a4eee6...1744c`). Reverted via `pnpm exec prettier --write`; post-revert sha256 matches manifest byte-for-byte; rerun → exit 0.
- **Drift-smoke #2 (`added-to-source-only`):** deleted both Story-1.10 anchor bullets from `INVARIANTS.md` via `node -e` filter (the filter was over-broad — it matched `INV-tokens-schema-contract` in the Companion reference on the semantic-rationale bullet, removing both bullets). `pnpm keel-invariants:check` → exit 1 with `added-to-source-only` drift for both `INV-tokens-schema-contract` and `INV-tokens-semantic-rationale`. Still valid sync-gate coverage of the anchor-walker branch. Reverted from `/tmp/INVARIANTS.md.bak`; `diff -q` byte-identical; rerun → exit 0.

### Completion Notes List

- **AC 1 satisfied.** `packages/ui/tokens.schema.json` is a JSON Schema draft-2020-12 document with `additionalProperties: false` at every level, `$type` constrained via `const` at each `leaf<Type>` def, `$value` pattern-validated as literal-or-`{alias}`, required semantic groups declared at the root. DTCG-format-compatible; covers `color` (neutral ramp + accent primitive/semantic + surface + text + border + status fg/bg + severity + state) · `type` · `font` · `space` · `radius` · `motion` (scale dial + 5 duration tiers) · `density` (scale dial + 3 number tiers) · `breakpoint`, plus optional `$modes` overlay keyed by `light|dark|high-contrast` with sparse overlay defs per group. Population of concrete values deferred to Story 1.11.
- **AC 2 satisfied.** `docs/invariants/tokens.md` carries one bullet per semantic token with stable `TOKEN-<slug>` IDs. All three AC-named exemplars (`surface.raised` → `TOKEN-surface-raised`; `status.success` → `TOKEN-status-success-fg`/`-bg`; `text.primary` → `TOKEN-text-primary`) present. Motion tier IDs resolve per scope carve-out (`TOKEN-motion-scale` + `TOKEN-motion-{instant|snap|swift|smooth|drift}` — two-segment, matching architecture.md:693); density likewise (`TOKEN-density-scale` + `TOKEN-density-{compact|default|comfortable}`).
- **AC 3 satisfied.** Every semantic token path in `packages/ui/tokens.schema.json` maps to a `TOKEN-<slug>` bullet in `docs/invariants/tokens.md` (and vice-versa). Epic 7 catalog / Epic 3 TUI will resolve `TOKEN-surface-raised` → the rationale line directly.
- **AC 4 satisfied.** Two sibling manifest entries registered at `packages/keel-invariants/src/invariants.manifest.ts:121..137`: `INV-tokens-schema-contract` (→ `packages/ui/tokens.schema.json`, sha256 `abb5bc4c...e07ac`) + `INV-tokens-semantic-rationale` (→ `docs/invariants/tokens.md`, sha256 `2d8d0e3f...25c0`). Two sibling anchors appended to `INVARIANTS.md` under new `### Design-token schema + semantic rationale (Story 1.10)` section (column-0 `- **\`INV-*\`**` form per Story 1.9's anchor-regex binding at `packages/keel-invariants/src/sync-gate.ts:24`; no fenced-code-block examples). Both drift-smoke branches (`content-hash-mismatch` on schema file; `added-to-source-only` on anchor deletion) produce exit 1 + structured JSON drift report with byte-identical revert.
- **Quality gates green.** `pnpm -w typecheck` (16 tasks) ✓ — covers import-time Zod `InvariantsSchema.parse(raw)` validation of the two new manifest entries (id regex + sha256 regex + sourcePath refine + ID-uniqueness + shared-sourcePath hash-consistency superRefines). `pnpm -w lint` (16 tasks) ✓. `pnpm -w build` (16 tasks) ✓. `pnpm format:check` ✓. `pnpm exec prek run --all-files` (3 hooks: typecheck / lint / format-check) ✓. `pnpm keel-invariants:check` clean-path ✓ (exit 0).
- **No test runner run.** Per story § Testing Standards, no vitest/jest/playwright runner is configured at Story 1.10 time (lands Story 1.16). Verification is substrate-layer: the three sync-gate smoke branches + `pnpm -w {typecheck,lint,build,format:check}` + `pnpm exec prek run --all-files` constitute AC coverage end-to-end at the shell-invocation level.
- **Manifest count:** `raw` array grows from 10 → 12 `Invariant` entries.

### File List

- **NEW** `packages/ui/tokens.schema.json` — JSON Schema draft-2020-12 DTCG-compatible token-source contract.
- **NEW** `docs/invariants/tokens.md` — semantic-rationale doc pairing every `TOKEN-<slug>` with a prose line.
- **MODIFIED** `packages/keel-invariants/src/invariants.manifest.ts` — appended two entries (`INV-tokens-schema-contract`, `INV-tokens-semantic-rationale`) to the `raw` array; no schema changes.
- **MODIFIED** `INVARIANTS.md` — appended new `### Design-token schema + semantic rationale (Story 1.10)` section with two column-0 anchor bullets.
- **MODIFIED** `_bmad-output/implementation-artifacts/sprint-status.yaml` — `1-10-design-token-schema-semantic-rationale-contract: ready-for-dev → done`; `last_updated: Story-1-10-done`.
- **MODIFIED** `_bmad-output/implementation-artifacts/1-10-design-token-schema-semantic-rationale-contract.md` — Tasks/Subtasks ticked; Dev Agent Record populated (Debug Log + Completion Notes + File List); Change Log v1.2 row added; Status `ready-for-dev → done`.

## Review Findings

### Post-dev SM requirements-satisfaction review (Ralph iter-47 — `traced → sm-fixes-pending`)

Scope: verify that the iter-45 implementation artefacts satisfy each AC as written. AC 1 / AC 2 / AC 4 verified green (see § Change Log v1.3 row for per-AC attestation). One MEDIUM unmet-AC finding surfaced — Story State transitions `traced → sm-fixes-pending` per FR14n matrix row 7.

- [x] **SM-1 [MEDIUM]** — AC 3 cross-link parity gap: **`### Font` section missing from `docs/invariants/tokens.md`**. The schema at `packages/ui/tokens.schema.json:249-257` declares `fontGroup` as a required root group (`font` listed at line 8 and referenced at line 12) with two required leaves — `font.sans` and `font.mono` (both `$ref: #/$defs/leafFontFamily`, `$type: fontFamily`). The rationale doc `docs/invariants/tokens.md` has no `### Font` section, no `TOKEN-font-sans` bullet, no `TOKEN-font-mono` bullet — the rationale index jumps from `### Type scale` (lines 136-147) directly to `### Spacing / radius / breakpoints` (lines 149-155) and never covers the font group. Consequence: AC 3's assertion ("**Then** the reference can be cross-linked back to `tokens.md`'s rationale via the stable `TOKEN-<slug>` ID") fails for the two font leaves — Epic 7's catalog or Epic 3's TUI cannot resolve `TOKEN-font-sans` / `TOKEN-font-mono` to a rationale line. Fix: add `### Font` section between `### Type scale` and `### Spacing / radius / breakpoints` (fonts are primitive-like, mirroring the Neutral group-level bullet pattern explicitly permitted by Task 2 for primitive scales — see Dev Notes line 153) with one consolidated bullet (`- **\`TOKEN-font-{name}\`** — *typographic font stack roles (sans / mono)*. Cross-runtime: the Tailwind preset exposes \`font-sans\` / \`font-mono\` utility classes; the Textual TUI uses \`$font-sans\` / \`$font-mono\` style vars. Individual slugs: \`TOKEN-font-sans\`, \`TOKEN-font-mono\`. Value: see ux-design-specification.md § Visual Design Foundation; Story 1.11 populates.`); recompute `sha256sum docs/invariants/tokens.md`; update `INV-tokens-semantic-rationale.contentHash` at `packages/keel-invariants/src/invariants.manifest.ts:141`; re-run `pnpm keel-invariants:check` clean-path → exit 0. Root cause: Task 2's subtask enumeration listed 11 required `###` sections (Surfaces / Text / Borders / Accent / Status / Severity / State / Motion / Density / Type scale / Spacing-radius-breakpoints) but omitted Font. Dev-agent's iter-45 drift-audit caught the structurally-parallel **Neutral** omission (same gap class — schema-required primitive group Task 2 missed) and added `### Neutral (primitive ramp)` inline per iter-45 Debug Log, but did not continue the audit and so missed Font — a mirror-gap in the same class. Pre-dev SM review (iter-43) should have caught both Task-2-vs-schema gaps (Neutral + Font) in one sweep; iter-43 did not perform a Task-2-enumeration-vs-schema-group-enumeration cross-check (it scoped its drift-audit to AC-2 stable-ID alignment + Task 4 sprint-status transition wording, per iter-43 Change Log v1.1 row).

### Code review (Ralph iter-50 — 2026-04-20; `sm-verified → fixes-pending`)

Scope: `/bmad-code-review (args: "2")` adversarial three-layer fan-out against Story 1.10's diff (`535bee0..HEAD`, 1919 insertions / 19 deletions across 12 files; normative surface = `packages/ui/tokens.schema.json` (+547), `docs/invariants/tokens.md` (+173), `packages/keel-invariants/src/invariants.manifest.ts` (+16), `INVARIANTS.md` (+5); non-normative surface = story file, sprint-status.yaml, iter-46 traceability artefacts, `.ralph/@plan.md`, `RALPH.md`). **Blind Hunter: NEW_FINDINGS** (23 raw — 6 HIGH / 6 MEDIUM / 11 LOW). **Edge Case Hunter: NEW_FINDINGS** (23 raw — MEDIUM/LOW schema-portability + mode-enforcement + slug-convention + DTCG-metadata classes). **Acceptance Auditor: NEW_FINDINGS** (1 LOW — sprint-status.yaml `last_updated` data-field drift).

**Triage outcome: 1 PATCH + 15 DEFER + 30 DISMISS.** PATCH rationale: convergent Blind+Edge finding against an explicit spec Task-1 prescription (`modesOverlay` missing `required: ["light", "dark"]` enforcement). DEFER rationale: AC 1 scope carve-out pins this story to **schema structure only** — value quality, DTCG array-value shapes, `$extensions`/`$deprecated` metadata, WCAG contrast, regex-DRY refactor, alias-vs-literal semantic intent for severity aliases are all Stories 1.11–1.13's scope. DISMISS rationale per iter-36 convergence doctrine: single-layer adversarial overreach, bookkeeping editorial noise on gate-waived trace artefacts, or directly contradicted by spec / anchor-regex binding precedent.

**PATCH**

- [x] [Review][Patch] **CR-1 [MEDIUM]** — `modesOverlay.required` missing enforcement of `["light", "dark"]`. Evidence: `packages/ui/tokens.schema.json:326-334` declares three optional mode slots (`light`, `dark`, `high-contrast`) with `additionalProperties: false` but no `required` array, so an empty `$modes: {}` or a `$modes` with only `high-contrast` passes validation. Spec Task 1 subtask line 66 explicitly states "Story 1.10 declares the two required modes (`light`, `dark`) per ux-design-specification.md:484; Story 1.11 populates the values." Convergent: Blind Hunter MEDIUM (`$modes overlay allows arbitrary/unpopulated modes; Task 1 says declares the two required modes (light, dark). The schema as landed declares zero required modes.`) + Edge Case Hunter MEDIUM (`$modes overlay required empty; light and dark not enforced`). **Landed iter-51:** added `"required": ["light", "dark"]` inside `modesOverlay` def after `"additionalProperties": false` (`packages/ui/tokens.schema.json:328`); recomputed `sha256sum packages/ui/tokens.schema.json` `abb5bc4c...e07ac → 15380636645d6eb1610845c7f295629b1f3a0ba3fec9f6c04262ece581b055e6`; updated `INV-tokens-schema-contract.contentHash` at `packages/keel-invariants/src/invariants.manifest.ts:133`; re-ran `pnpm -w typecheck` ✓ + `pnpm --filter @keel/keel-invariants build` + `pnpm keel-invariants:check` clean-path ✓ exit 0. iter-52 CR re-run #2 confirms zero findings; PATCH discharged.

**DEFER** (scope carve-out to Story 1.11/1.13; editorial follow-up; or gate-waived trace artefacts)

- [x] [Review][Defer] (BH + EH) Leaf `$value` regex right-alternation `[^{}]+` permits arbitrary non-brace literals — schema does not constrain value quality. AC 1 carve-out line 20: `schema does not constrain numeric ranges, contrast ratios, or value quality (those are Story 1.13's pre-commit gates)`. Deferred as-designed.
- [x] [Review][Defer] (EH) `leafFontFamily.$value` string-only vs DTCG-canonical array-of-strings; `leafCubicBezier.$value` string-only vs DTCG-canonical 4-number array; `leafNumber.$value` string-only vs JSON number; `leafFontWeight.$value` string-only vs 100..1000 integer. (4 findings bundled.) Rationale: AC 1 carve-out — structural only; Story 1.11 populator may land a `oneOf` extension per leaf without breaking the current contract. Deferred.
- [x] [Review][Defer] (EH) Schema forbids DTCG `$extensions` / `$deprecated` leaf metadata. Rationale: Story 1.10 scope is the minimum-viable DTCG subset; FR44 fork-extension path is composition via `apps/web/tokens.fork.json` (DTCG deep-merge), not per-leaf metadata. Deferred to Story 1.16 fork-extension concern.
- [x] [Review][Defer] (EH) Regex pattern `^(\{...\}|[^{}]+)$` duplicated 7× across leaf `$defs`. Rationale: refactor-to-`$ref` is maintenance hygiene, not a correctness gap; co-located leaf defs at lines 21-111 mitigate drift risk. Deferred to Story 1.11 populator when per-`$type` pattern refinement may land.
- [x] [Review][Defer] (BH) `leafCubicBezier` / `leafFontWeight` defined in `$defs` but never referenced by required root groups. Rationale: forward-compat shapes for Story 1.11 / 1.12 motion easing + typography weights. Harmless bloat.
- [x] [Review][Defer] (BH) `colorSeverity` leaves are `leafColor` literals while rationale prose says they alias to matching `color.status.*.fg`. Rationale: schema cannot enforce alias-vs-literal semantic intent at the structural layer — Story 1.13's source-output-sync + WCAG gate catches semantic divergence. Deferred.
- [x] [Review][Defer] (BH) `colorStatus.critical` overlaps with `colorSeverity.critical` in slug space (`TOKEN-status-critical-fg/-bg` vs `TOKEN-severity-critical`); severity aliases status-critical-fg per prose. Rationale: alias-chain validation is Story 1.13 semantic layer, not Story 1.10 structural layer. Deferred.
- [x] [Review][Defer] (BH + EH) Group-level placeholder bullets (`TOKEN-space-{N}`, `TOKEN-radius-{name}`, `TOKEN-breakpoint-{name}`, `TOKEN-font-{name}`, `TOKEN-neutral-{N}`) in `docs/invariants/tokens.md`. Rationale: Task 2 line 88 explicitly permits consolidated group-level bullets for primitive scales; SM-1 iter-48 fix deliberately mirrored this permitted pattern. Deferred as-designed.
- [x] [Review][Defer] (BH + EH) Slug convention drops `color.` top-level prefix without explicit rule statement at `docs/invariants/tokens.md:27`; hyphen-bearing paths (`in-progress`) reverse-parse non-uniquely. Rationale: convention is forward-generator-correct (schema path → slug is deterministic); reverse-parse is not a sync-gate concern. Doc-editorial follow-up for Story 1.11 when the populator adds concrete values. Deferred.
- [x] [Review][Defer] (BH + EH) Manifest description enumeration at `packages/keel-invariants/src/invariants.manifest.ts:128-135` omits `font` from the 7-group list (`color / type / space / radius / motion / density / breakpoint`); schema `required` array correctly lists 8 groups including `font`. Spec Task 3 line 102 prescribes this exact enumeration verbatim; manifest faithfully reproduces spec text. Rationale: spec-internal drift — dev-story cannot resolve without reopening the spec narrative. Doc-editorial follow-up (story-spec amendment). Deferred.
- [x] [Review][Defer] (EH) `sparseRoot` allows empty `{}` mode overlay. Rationale: sparse-by-design (zero-override is degenerate but not invalid). Deferred.
- [x] [Review][Defer] (EH) `$modes` schema hardcodes three mode names (`light | dark | high-contrast`); no `patternProperties` escape-hatch for fork-extension custom modes. Rationale: FR44 pattern is the fork-extension route (via `apps/web/tokens.fork.json` DTCG deep-merge), not schema-level pattern escape. Deferred.
- [x] [Review][Defer] (BH + EH) Traceability artefacts (`_bmad-output/test-artifacts/traceability/1-10-*.json` + `1-10-*.md`) carry placeholder `generated_at` timestamps, vacuous P0 100% coverage math, heuristic `*_status: "present"` vs trace.md "Not applicable" mismatches, and `/tmp/INVARIANTS.md.bak` ephemeral-path citations. Rationale: iter-46 gate WAIVED (fourth cumulative — 1.7/1.8/1.9/1.10 — no test runner pre-Story-1.16); artefacts are narrative scaffolds, not auto-generated fixtures. Deferred to Story 1.16 when test-runner landing re-runs trace. (Multiple findings bundled.)
- [x] [Review][Defer] (BH) Change Log row ordering `1.0 / 1.5 / 1.4 / 1.3 / 1.2 / 1.1` in story file. Rationale: recent-at-top pattern with v1.0 anchor-row at start is the Ralph-established convention (mirrors Story 1.9's same sort). Not drift. Deferred (no fix intended).

**DISMISS** (single-layer adversarial overreach, bookkeeping editorial noise, or dispatched by spec/precedent — 30 raw items; representative dispositions only — full raw findings archived in commit transcript per iter-41 triage doctrine's bias toward exiting)

- [x] [Review][Dismiss] (BH) Slug-collision claim (`TOKEN-accent-default` vs `TOKEN-text-accent`). Not actually the same slug — BH's own hypothetical slugifier would produce distinct IDs; convention is forward-generator-correct.
- [x] [Review][Dismiss] (BH) Three-segment-ID assertion mis-describes segment semantics. `docs/invariants/tokens.md:27` correctly distinguishes `status.success.fg` (modifier) from `neutral.500` (primitive-stop index) — two-segment vs three-segment applies to the semantic `<category>.<semantic-name>.<modifier?>` path, not to the underlying path arity. Convention matches architecture.md:693 verbatim.
- [x] [Review][Dismiss] (EH) Sprint-status.yaml `last_updated` data-field drift. Addressed THIS iteration via CR workflow step-6 status resync (`Story-1-10-ready-for-dev → Story-1-10-in-progress`; header comment synced; `1-10: done → in-progress`). Not a separate QUEUE fix task. (See § Change Log v1.6.)
- [x] [Review][Dismiss] (EH + BH) Story file `Status: done` preemptive. Addressed THIS iteration — story Status flipped `done → in-progress` via CR workflow step-6 per FR14n matrix row 9 transition to `fixes-pending`. Not a separate fix task.
- [x] [Review][Dismiss] (BH) Manifest `anchors: ['INV-tokens-schema-contract']` field redundant when anchor ID equals entry `id`. ANCHOR_REGEX at `packages/keel-invariants/src/sync-gate.ts:24` binds on column-0 bullet shape; redundancy is intentional forward-compat for future multi-anchor entries. Not drift.
- [x] [Review][Dismiss] (BH) `INVARIANTS.md` bullet trailing `Source: \`…\`.` format. The ANCHOR_REGEX is column-0 opening binding only; trailing `Source:` line is narrative convention (matches Story 1.8's 10 entries), not regex-bound. Not drift.
- [x] [Review][Dismiss] (BH) `$value` regex permits `{color.0state}` / `{a.0--0-}` malformed alias paths via the subsequent-segment rule `[a-z0-9][a-z0-9-]*`. Not convergent (single-layer); DTCG path segments in actual use (`color.neutral.500`, `space.24`, `breakpoint.2xl`, `state.in-progress`) all parse correctly under the rule; speculative pathologies are theoretical.
- [x] [Review][Dismiss] (BH) Alias regex first-segment `[a-z][a-z0-9]*` stricter than subsequent segments. Asymmetry is by design — DTCG top-level token groups are alphabetic identifiers (no digit-first, no hyphen); subsequent segments accommodate `in-progress` / `2xl` / per-stop numeric keys. Not drift.
- [x] [Review][Dismiss] (EH) `$value` regex rejects empty string silently via `+` quantifier. Rejection IS the intended guard (one-or-more chars); `+` quantifier correctly disallows empty. Doc note optional, not a defect.
- [x] [Review][Dismiss] (BH) Cross-runtime prose in `docs/invariants/tokens.md` lines 29-37 references the cross-runtime reminder without Sally/ux-spec proof trace. Narrative scope-framing paragraph; cross-reference to ux-design-specification.md is inline (§ Architecture of the Design System at line 6). Not drift.
- [x] [Review][Dismiss] (BH) Coverage-matrix artefact vocabulary (`runner-hosted` / `backstop` / level_notes shape). Pre-existing iter-46 trace artefacts — gate WAIVED four cumulative stories (1.7/1.8/1.9/1.10); Story 1.9's iter-36 SIBLING-VOCABULARY-CLUSTER doctrine explicitly classifies these as broader-corpus prose-vocabulary hits that do not gate PATCH promotion at structural-only contract stories.
- [x] [Review][Dismiss] (EH) DTCG canonical-shape portability class ({leafFontFamily, leafCubicBezier, leafNumber, leafFontWeight}.$value array/number alternation). AC 1 carve-out scope — Story 1.11 population is the correct layer. (5 findings dismissed on scope carve-out; not a new class of Story-1.10 defect.)
- [x] [Review][Dismiss] (BH) Rationale doc `docs/invariants/tokens.md:5` "Machine-enforced in: ... → `INV-tokens-semantic-rationale` (content-hash drift, Story 1.8 + 1.9)" — Edge Case Hunter read this as "Story 1.8 owns the entry". Narrative correctly refers to the **drift-enforcement infrastructure** lineage (manifest landed Story 1.8; sync-gate landed Story 1.9; this entry is registered by Story 1.10). Not drift — adversarial mis-read.
- [x] [Review][Dismiss] (BH) `### Font` bullet in `docs/invariants/tokens.md:153` uses group-level `TOKEN-font-{name}` shape rather than per-slug column-0 bullets. SM-1 iter-48 fix explicitly adopted the permitted Neutral group-level bullet pattern (Task 2 Dev Notes line 153). Re-opening regresses SM's iter-49 closure; BH is single-layer and directly contradicts established spec prescription. Dismissed.
- [x] [Review][Dismiss] (BH) sprint-status.yaml comment-header (`# last_updated:`) vs data-field (`last_updated:`) mismatch. Addressed this iteration via CR workflow step-6 resync. Not a separate fix.
- [x] [Review][Dismiss] (BH) Schema `description` refers to "the named semantic groups" generically (`packages/ui/tokens.schema.json:5`) — Edge Case Hunter mis-read it as a 7-group enumeration. The description does NOT enumerate group names; enumeration is only in the `required` array (line 8, 8 groups, including `font`). No internal inconsistency. Dismissed.
- [x] [Review][Dismiss] (BH + EH) Remainder of raw findings (~12 items) — editorial / speculative / single-layer stylistic-overreach class (per Story 1.9 iter-41 residue-family catalogue class 5). Dismissed en bloc per iter-36 two-layer-convergence doctrine.

Status: story-file Status `done → in-progress`; sprint-status.yaml `1-10: done → in-progress` + `last_updated: Story-1-10-ready-for-dev → Story-1-10-in-progress UTC` (CR workflow step-6 resync addresses Auditor's sole finding + the preemptive `done` state). Story State: `sm-verified → fixes-pending` per FR14n matrix row 9. Next iteration picks up Fix CR-1 (top of QUEUE); when QUEUE empties, re-run `/bmad-code-review (args: "2")` for `fixes-pending → done` per FR14n matrix row 10. **Contract-only-single-pass-CR hypothesis (iter-7 Decisions) partially falsified at the CR layer** — Story 1.10 surfaces 1 convergent Blind+Edge PATCH finding vs. Story 1.8's zero-round clean CR; fix is ~small (single-line schema edit + single hash bump), mirroring the iter-47..49 SM closure pattern (small mechanical fix, no reasoning redo).

### Code review RE-RUN #2 (Ralph iter-52 — 2026-04-20; `fixes-pending → done`)

Scope: `/bmad-code-review (args: "2")` re-run against the same Story 1.10 diff (`535bee0..HEAD`, now including iter-51's CR-1 fix — +3 lines across `packages/ui/tokens.schema.json:328` + `packages/keel-invariants/src/invariants.manifest.ts:133` hash char-swap). Three-layer parallel fan-out. **Blind Hunter: NEW_FINDINGS** (8 raw — 5 MEDIUM + 3 LOW; all single-layer without Edge or Auditor convergence). **Edge Case Hunter: PASS** (no edge cases — walked every branching path in the iter-51 delta; modesOverlay `required: ["light", "dark"]` guard now closed; hash-resync correctness verified; no unhandled branches introduced). **Acceptance Auditor: PASS** (14 positive checks across AC 1 / AC 2 / AC 3 / AC 4 — CR-1 PATCH cleanly discharged; schema structure + per-`$type` leaf defs + modesOverlay overlay + rationale-doc stable-ID cross-links + manifest entries + INVARIANTS.md anchors + sync-gate smoke branches all satisfied).

**Triage outcome: 0 PATCH + 3 DEFER + 5 DISMISS.** All findings single-layer Blind-Hunter-only per iter-36 two-layer-convergence doctrine. DEFER class: pre-existing AC-1 scope carve-outs (structural-only) + spec-editorial drift already surfaced as iter-50 defers (no new QUEUE fix tasks needed). DISMISS class: forward-compat schema $defs reframed as "dead"; spec section-enumeration vs publishable-slug prose nuance; frozen trace-artefact byte-count / line-pointer drift (iter-30 paper-trail rule); spec-intentional mode asymmetry.

**DEFER** (pre-existing AC-1 scope + spec-editorial follow-up; carried to Stories 1.11–1.13 or doc-polish passes)

- [x] [Review][Defer] (BH) **Manifest `description` enumeration at `packages/keel-invariants/src/invariants.manifest.ts:128`** omits `font` from the 7-group list (`color / type / space / radius / motion / density / breakpoint`); schema root `required` correctly lists 8 including `font`. Same finding as iter-50 DEFER class — reconfirmed by iter-52 Blind Hunter independently. Spec Task 3 prescribes this exact prose; dev-story cannot resolve without spec re-author. Doc-editorial follow-up (story-spec amendment).
- [x] [Review][Defer] (BH) **`leafFontFamily.$value` schema type `string` vs rationale doc prose "arrays of family names"** at `packages/ui/tokens.schema.json:249-257` vs `docs/invariants/tokens.md § Font`. AC 1 scope carve-out pins this story to structural-only; DTCG canonical fontFamily array-value shape is Story 1.11 populator scope (when concrete values land, `oneOf: [string, array<string>]` can extend the leaf). Same reasoning-class as iter-50's deferred leafCubicBezier/leafNumber/leafFontWeight portability set.
- [x] [Review][Defer] (BH) **Story-file stable-ID convention example line 191** (`motion.scale.snap` → `TOKEN-motion-scale-snap` three-segment form) inconsistent with AC 2 scope carve-out at line 27 (`Three-segment IDs like TOKEN-motion-scale-snap are NOT used`). Editorial drift in the story spec's Dev Notes example; iter-43 pre-dev review fixed the inline example but left the Dev Notes slug-convention illustration using the pre-fix three-segment form as an explanatory foil. Story-file editorial polish; carry to Story 1.11 pre-dev SM if the same illustration is copy-inherited into 1.11's drafting.

**DISMISS** (single-layer adversarial overreach per iter-36 two-layer-convergence doctrine — 5 items)

- [x] [Review][Dismiss] (BH) `leafFontWeight` + `leafCubicBezier` `$defs` flagged as "dead" (never `$ref`d). Forward-compat shapes for Story 1.11 source-population (motion easing + typography weights); not dead schema — Story 1.10's structural contract intentionally enumerates all DTCG `$type` values so 1.11 populators can land concrete tokens without schema surgery. Not a Story 1.10 defect.
- [x] [Review][Dismiss] (BH) Story-file Task 2 enumerates Status primary slugs (`TOKEN-status-info`, `TOKEN-status-success`, …) that rationale-doc prose at `docs/invariants/tokens.md` marks as "reserved — downstream consumers always address `-fg`/`-bg`". The Task-2 prescription is *section-ID enumeration* (sections under `### Status` in the rationale doc), not a publishable-slug inventory. The rationale-doc's "reserved" note is a consumer-usage guide at the prose layer. Adversarial mis-read of two distinct semantic roles for the same prefix.
- [x] [Review][Dismiss] (BH) Trace artefact byte-count claim `19866 bytes` at `_bmad-output/test-artifacts/traceability/1-10-coverage-matrix.json:471` + `.../1-10-design-token-schema-semantic-rationale-contract.md:715` predates iter-51 schema edit (now ~30 bytes larger post-CR-1-fix). Frozen iter-46 trace artefacts preserved for audit trail per iter-30 paper-trail rule (gate WAIVED fourth cumulative); editing post-fact corrupts the audit. Not drift.
- [x] [Review][Dismiss] (BH) Trace report line pointer `invariants.manifest.ts:137` cites position drift of ±2 lines vs actual `INV-tokens-semantic-rationale.id` field line. Same frozen-trace-artefact class as the byte-count drift; iter-30 paper-trail rule dismisses post-fact pointer corrections in gate-WAIVED trace artefacts.
- [x] [Review][Dismiss] (BH) `modesOverlay.properties.high-contrast` declared optional while `light` + `dark` required — asymmetric treatment. Per spec Task 1 line 66 explicit "Story 1.10 declares the two required modes (`light`, `dark`)" — the asymmetry is the exact prescribed contract shape. `high-contrast` is a future-declared optional overlay (Story 1.16 WCAG testing hook per FR-contrast). Not a defect.

**Closure signal:** Story State `fixes-pending → done` per FR14n matrix row 10 single-transition edge (zero PATCH promotion from iter-52 CR). Story file Status `in-progress → done`; sprint-status.yaml `1-10: in-progress → done` + `last_updated: Story-1-10-in-progress → Story-1-10-done`. **Halving-to-zero CR trajectory confirmed for Story 1.10**: iter-50 (1 PATCH + 15 DEFER + 30 DISMISS) → iter-52 re-run (0 PATCH + 3 DEFER + 5 DISMISS) — contract-only single-pass CR hypothesis (iter-7 Decisions) partially vindicated for Story 1.10's CR layer despite iter-50's one-PATCH partial falsification. No source-code changes at iter-52 (CR-re-verification-only iteration); no quality gates re-run (iter-51 already landed clean-path exit 0 + typecheck ✓ + scoped build ✓ after the fix). Story 1.10 closure at iter-52 after 11-iteration FR14n lifecycle (iter-42..52; ~1.5× Story 1.8's 7-iter closure — consistent with a contract-only story that surfaced one convergent CR PATCH).

## Change Log

| Date       | Version | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Author |
| ---------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 2026-04-20 | 1.0     | Initial story authoring (Ralph, iter-42; `_(no story)_ → drafted` per FR14n matrix row 1). `/bmad-create-story` drafted the file with 4 ACs, 4 Tasks (schema author / rationale-doc author / manifest+INVARIANTS register / quality gates+sync-gate smokes+sprint-status bump), Dev Notes, Project Structure Notes (alignment + DTCG-vs-typed-source variance documented), 13 References. AC 4 scope carve-out collapses epic's single-`INV-tokens-schema-contract` entry into two sibling entries (`INV-tokens-schema-contract` + `INV-tokens-semantic-rationale`) to preserve Story 1.8's single-`sourcePath`-per-`Invariant` shape. Sprint-status `1-10: backlog → ready-for-dev`.                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Ralph  |
| 2026-04-20 | 1.7     | Code review RE-RUN #2 (Ralph, iter-52; FR14n matrix row 10 → `fixes-pending → done`). Ran `/bmad-code-review (args: "2")` re-run against the iter-51-post-Fix-CR-1 diff (`535bee0..HEAD`; +3 lines — schema `modesOverlay.required` + manifest hash swap — vs iter-50's 1919-line pre-fix diff). Three-layer parallel fan-out: Blind Hunter surfaced 8 single-layer findings (5 MEDIUM + 3 LOW), Edge Case Hunter PASS (no unhandled edges introduced by the fix), Acceptance Auditor PASS with 14 positive checks across all 4 ACs. **Triage: 0 PATCH + 3 DEFER + 5 DISMISS.** All BH findings failed the iter-36 two-layer-convergence gate (no Edge or Auditor corroboration). DEFER class: 3 items reconfirm iter-50 DEFER entries (manifest description `font` omission, fontFamily string-vs-array portability, story-file Dev-Notes stable-ID illustration editorial drift) — captured in `_bmad-output/implementation-artifacts/deferred-work.md § Deferred from: code review RE-RUN of story-1.10 (2026-04-20)`. DISMISS class: forward-compat `$def` schemas reframed as dead (not-a-defect); section-ID-enumeration vs publishable-slug role conflation; frozen trace-artefact byte-count + line-pointer drift (iter-30 paper-trail rule); spec-intentional `high-contrast` mode optionality. CR-1 PATCH checkbox flipped `[ ] → [x]` in § Review Findings iter-50 block. § Review Findings iter-52 subsection appended. Story State `fixes-pending → done`. Story file Status `in-progress → done`; sprint-status.yaml `1-10: in-progress → done` + `last_updated: Story-1-10-in-progress → Story-1-10-done` (both header-comment + data-field). Zero source-code changes at iter-52; zero quality gates re-run (iter-51 landed clean-path exit 0 + typecheck ✓ after the fix). **Contract-only-single-pass-CR hypothesis update:** Story 1.10 closed CR layer in 3 rounds (iter-50 review → iter-51 fix → iter-52 re-run clean) — one convergent PATCH + halving-to-zero trajectory, matching the SM-layer 3-iter shape (iter-47..49). Total Story 1.10 FR14n lifecycle: 11 iterations (iter-42..52), ~1.5× Story 1.8's 7-iter closure. **Next story:** Story 1.11 via `/bmad-create-story` per FR14n matrix row 1 (`_(no story)_ → drafted`). Epic 1 remains in-progress (6 stories 1.11–1.16 remain before EPIC_DONE halt + PR #226 Draft→Open transition). Tracked at GitHub issue #34 — `Refs #34` in commit trailer; `Closes #34` in PR body deferred until PR transitions to Open at EPIC_DONE halt. | Ralph  |
| 2026-04-20 | 1.6     | Adversarial code review (Ralph, iter-50; FR14n matrix row 9 → `sm-verified → fixes-pending`). Ran `/bmad-code-review (args: "2")` — three-layer parallel fan-out against Story 1.10 diff `535bee0..HEAD` (1919 insertions / 19 deletions / 12 files; normative = schema + rationale + manifest + INVARIANTS; non-normative = story / sprint-status / iter-46 traceability / Ralph bookkeeping). Raw findings: Blind Hunter 23 (6 HIGH / 6 MEDIUM / 11 LOW) + Edge Case Hunter 23 (MEDIUM/LOW schema-portability + mode-enforcement + slug + DTCG-metadata classes) + Acceptance Auditor 1 (LOW sprint-status drift). **Triage: 1 PATCH + 15 DEFER + 30 DISMISS.** PATCH = **CR-1 [MEDIUM]** `modesOverlay` missing `required: ["light", "dark"]` (convergent Blind + Edge; spec Task 1 line 66 explicit "Story 1.10 declares the two required modes `light`, `dark`"); fix queued as top of QUEUE for iter-51 (single-line schema edit + manifest hash bump + clean-path sync-gate re-run). DEFER class: AC 1 scope carve-out (structural-only; value quality / DTCG array-value shapes / $extensions / WCAG / regex-DRY / severity-alias-vs-literal all Story 1.11–1.13 scope) + iter-46 trace artefacts (gate WAIVED) + doc-editorial follow-up items. DISMISS class: single-layer adversarial overreach, anchor-regex-mis-read, slug-collision claim that doesn't collide, Story-1.8-provenance mis-read of tokens.md line 5, `Status: done` preemptive (addressed this iter by step-6 resync), sprint-status drift (addressed this iter). **CR workflow step-6 status resync applied this iteration:** story-file `Status: done → in-progress`; sprint-status.yaml `1-10: done → in-progress` + `last_updated: Story-1-10-ready-for-dev → Story-1-10-in-progress UTC` (both header-comment + data-field) — discharges Auditor's sole finding without a separate fix task. No source-code changes at iter-50 (CR-verification-only iteration); no quality gates re-run. Story State `sm-verified → fixes-pending`. Next iteration (iter-51) applies Fix CR-1; iter-52 re-runs `/bmad-code-review (args: "2")` for `fixes-pending → done` per FR14n matrix row 10. **Contract-only-single-pass-CR hypothesis (iter-7 Decisions):** partially falsified — Story 1.10 surfaces 1 convergent PATCH finding vs. Story 1.8's zero; but closure trajectory mirrors the iter-47..49 SM pattern (one small mechanical fix, no reasoning redo), so total CR round count projected at 2 (iter-50 review + iter-51 fix + iter-52 re-run = 3 iterations total, parallel to SM's 3-iter closure). Tracked at GitHub issue #34 — `Refs #34` in commit trailer. | Ralph  |
| 2026-04-20 | 1.5     | Post-dev SM re-verify (Ralph, iter-49; FR14n matrix row 8 → `sm-fixes-pending → sm-verified`). Re-ran `/bmad-create-story (args: "review")` to confirm the iter-48 SM-1 fix discharges the finding. **Per-AC re-attestation:** **AC 1** — no change since iter-47 (schema at `packages/ui/tokens.schema.json` untouched; sha256 `abb5bc4c...e07ac` unchanged at manifest line 133). **AC 2** — no change since iter-47 (semantic-token rationale bullets intact). **AC 3 now fully satisfied** — `### Font` section present at `docs/invariants/tokens.md:149` between `### Type scale` (line 136) and `### Spacing / radius / breakpoints` (line 155); two-sentence role-intro paragraph at line 151 explains `sans` / `mono` typographic role slots with DTCG `fontFamily` stacks + cross-runtime semantic addressing; one group-level bullet `TOKEN-font-{name}` at line 153 enumerates individual slugs `TOKEN-font-sans` + `TOKEN-font-mono` (mirrors the Neutral primitive-ramp bullet pattern permitted by Task 2 Dev Notes line 153). Schema-required `fontGroup` (lines 249-257; `sans` + `mono` required leaves via `$ref: #/$defs/leafFontFamily`) now has full cross-link parity back to `tokens.md` rationale — Epic 7's catalog or Epic 3's TUI can resolve `TOKEN-font-sans` / `TOKEN-font-mono` to this rationale line. **AC 4** — manifest `INV-tokens-semantic-rationale.contentHash` at `packages/keel-invariants/src/invariants.manifest.ts:141` = `efd5fa0d84d3478cd4af530f3cc57c734f9b4e23415d0c7085fb8e6296d1a82c`; on-disk `sha256sum docs/invariants/tokens.md` = `efd5fa0d84d3478cd4af530f3cc57c734f9b4e23415d0c7085fb8e6296d1a82c` — byte-exact match; `pnpm keel-invariants:check` clean-path exit 0 confirms sync-gate detects no drift. § Review Findings: **all checkboxes `[x]`** — SM-1 (sole finding from iter-47) discharged at iter-48; no new findings surfaced at iter-49. No source-code changes at iter-49 (verification-only pass); no quality gates re-run beyond the clean-path sync-gate confirmation. Story State `sm-fixes-pending → sm-verified`. Next iteration runs `/bmad-code-review (args: "2")` for `sm-verified → done` per FR14n matrix row 9. **Contract-only single-pass SM hypothesis final status:** Story 1.10 closed SM layer in two rounds (iter-47 review + iter-48 fix + iter-49 re-verify — mechanical fix, no reasoning redo) versus Story 1.8's single-round zero-finding close. Pattern for Story 1.11+ contract-authoring stories: a Task-enumeration-vs-schema-group-enumeration pre-dev diff (carry-forward rule documented in RALPH.md iter-47 Signpost) should restore single-round closure. | Ralph  |
| 2026-04-20 | 1.4     | Fix SM-1 (Ralph, iter-48; FR14n matrix row 8 — top QUEUE fix task while `sm-fixes-pending`). Added `### Font` section to `docs/invariants/tokens.md` between `### Type scale` and `### Spacing / radius / breakpoints` — one group-level bullet `TOKEN-font-{name}` listing `TOKEN-font-sans` + `TOKEN-font-mono` (mirrors Neutral primitive-ramp pattern permitted by Task 2); plus a two-sentence role-intro paragraph introducing the group. Recomputed `sha256sum docs/invariants/tokens.md`: `2d8d0e3f...25c0 → efd5fa0d84d3478cd4af530f3cc57c734f9b4e23415d0c7085fb8e6296d1a82c`; updated `INV-tokens-semantic-rationale.contentHash` at `packages/keel-invariants/src/invariants.manifest.ts:141` to match. Quality gates re-run clean: `pnpm -w typecheck` ✓ (Zod import-time validation), `pnpm -w build` ✓, `pnpm keel-invariants:check` clean-path ✓ exit 0. § Review Findings SM-1 checkbox flipped `[ ] → [x]`. No drift-smokes re-run (drift branches already proven comprehensively at iter-45; this iteration only reverifies clean-path). Story State stays `sm-fixes-pending` per FR14n matrix row 8 — QUEUE now empty of fix tasks; iter-49 re-runs `/bmad-create-story (args: "review")` for `sm-fixes-pending → sm-verified`. Tracked at GitHub issue #34 — `Refs #34` in commit trailer. | Ralph  |
| 2026-04-20 | 1.3     | Post-dev SM requirements-satisfaction review (Ralph, iter-47; `traced → sm-fixes-pending` per FR14n matrix row 7). **1 MEDIUM unmet-AC finding surfaced (SM-1).** Per-AC attestation: **AC 1 satisfied** — `packages/ui/tokens.schema.json` is a JSON Schema draft-2020-12 document with `additionalProperties: false` at every level, seven per-`$type` `leaf<Color\|Dimension\|Duration\|Number\|FontFamily\|FontWeight\|CubicBezier>` defs (lines 21-111) constraining `$type` via `const` + `$value` via literal-or-`{alias}` regex, eight required root groups (`color type font space radius motion density breakpoint` at line 8), optional `$modes` overlay keyed by `light\|dark\|high-contrast` with sparse overlay defs (lines 326-348). **AC 2 satisfied** — every semantic token (outside the Font gap) has a `TOKEN-<slug>`-tagged rationale bullet; three AC-named exemplars present (`TOKEN-surface-raised` at line 59, `TOKEN-status-success-fg`/`-bg` at line 93, `TOKEN-text-primary` at line 65); motion/density two-segment IDs per iter-43 scope carve-out (`TOKEN-motion-scale` + 5 tiers at lines 120-125; `TOKEN-density-scale` + 3 tiers at lines 131-134); Neutral + Accent groups included with group-level + enumerated bullets per iter-45 Debug Log resolutions. **AC 3 NOT fully satisfied — SM-1 finding** — `### Font` section missing from `docs/invariants/tokens.md`; see § Review Findings for fix-plan. All other schema groups cross-link cleanly: Neutral (line 54) / Surfaces (lines 58-61) / Text (lines 65-69) / Borders (lines 73-75) / Accent (lines 81-86) / Status (lines 92-96) / Severity (lines 102-105) / State (lines 111-114) / Motion (lines 120-125) / Density (lines 131-134) / Type scale (lines 140-147) / Spacing / radius / breakpoints (lines 153-155). **AC 4 satisfied** — two manifest entries at `packages/keel-invariants/src/invariants.manifest.ts:128-143` (`INV-tokens-schema-contract` sha256 `abb5bc4c...e07ac` + `INV-tokens-semantic-rationale` sha256 `2d8d0e3f...25c0`); two column-0 anchor bullets at `INVARIANTS.md:50-51`; three sync-gate smoke branches proven per iter-45 Debug Log (clean-path exit 0 + content-hash-mismatch exit 1 + added-to-source-only exit 1 + byte-identical reverts). Pure prose — no source code changes in iter-47. Quality gates not re-run (no TS/JS touched). Fix SM-1 queued for iter-48; re-run `/bmad-create-story (args: "review")` for `sm-fixes-pending → sm-verified` at iter-49 (post-Fix-SM-1). Story State stays `sm-fixes-pending` until QUEUE empties + SM re-verify confirms all findings discharged. Contract-only-single-pass-CR hypothesis (iter-7 Decisions) is now **partially falsified** at the SM-review layer — Story 1.10 needed 1 post-dev SM fix round (zero Story 1.8 needed); CR round count remains to-be-determined. | Ralph  |
| 2026-04-20 | 1.2     | `/bmad-dev-story` implementation (Ralph, iter-45; `atdd-scaffolded → in-dev` per FR14n matrix row 4). Four tasks executed: (Task 1) authored `packages/ui/tokens.schema.json` — JSON Schema draft-2020-12 DTCG-compatible; `additionalProperties: false` everywhere; per-`$type` leaf defs (`leafColor|Dimension|Duration|Number|FontFamily|FontWeight|CubicBezier`); eight required root groups (`color type font space radius motion density breakpoint`); optional `$modes` overlay keyed by `light|dark|high-contrast` with sparse-group defs. Accent group extended beyond Task 1's ramp-only bullet to include semantic triad (`default fg focus`) per Task 2 rationale list — documented in Dev Agent Record as deliberate story-spec-consistency resolution. (Task 2) authored `docs/invariants/tokens.md` — purpose + promotion-rules + stable-ID convention + cross-runtime reminder + motion/density scope carve-out + rationale index covering Neutral / Surfaces / Text / Borders / Accent / Status / Severity / State / Motion / Density / Type scale / Spacing-radius-breakpoints. (Task 3) registered two sibling manifest entries (`INV-tokens-schema-contract` sha256 `abb5bc4c...e07ac` + `INV-tokens-semantic-rationale` sha256 `2d8d0e3f...25c0`) at `packages/keel-invariants/src/invariants.manifest.ts:121..137`; appended `### Design-token schema + semantic rationale (Story 1.10)` section with two column-0 anchor bullets to `INVARIANTS.md`. (Task 4) quality gates green — `pnpm -w typecheck` ✓ (Zod import-time validation of new entries passes); `pnpm -w lint` ✓; `pnpm -w build` ✓; `pnpm format:check` ✓; `pnpm exec prek run --all-files` 3/3 hooks Pass; sync-gate clean path ✓ exit 0; drift-smoke #1 (`content-hash-mismatch`) ✓ exit 1 + byte-identical revert via prettier; drift-smoke #2 (`added-to-source-only`) ✓ exit 1 + byte-identical revert. Sprint-status `ready-for-dev → done`; story-file Status `ready-for-dev → done`. Tracked at GitHub issue #34. | Ralph  |
| 2026-04-20 | 1.1     | Pre-dev SM review (Ralph, iter-43; `drafted → validated` per FR14n matrix row 2). Applied two critical fixes: (a) **AC 2 stable-ID alignment** — example ID corrected `TOKEN-motion-scale-snap` → `TOKEN-motion-snap` + added scope carve-out clarifying the epic's `motion.scale.*` / `density.scale.*` shorthand resolves to the dial (`TOKEN-motion-scale` / `TOKEN-density-scale`, leaves with `$type: number`) and tier siblings (`TOKEN-motion-{instant|snap|swift|smooth|drift}` / `TOKEN-density-{compact|default|comfortable}`, not nested three-segment IDs), matching architecture.md:693's two-segment `<category>.<semantic-name>` pattern and DTCG's group-node restriction. (b) **Task 4 sprint-status transition** — rewrote the `backlog → in-progress → review` phrasing to the canonical Ralph direct-jump pattern `ready-for-dev → done` at `/bmad-dev-story` completion (per Story 1.7 / 1.8 precedent — Ralph's FR14n owns the trace/SM-review/CR intermediates; BMad `in-progress → review` stage is skipped). Story-file Status terminal state corrected `review → done` in the same subtask. ACs 1, 3, 4 well-formed; scope carve-outs (AC 1 schema-vs-value separation + AC 4 two-entry manifest carve-out) intact; 13 References spot-validated. | Ralph  |
