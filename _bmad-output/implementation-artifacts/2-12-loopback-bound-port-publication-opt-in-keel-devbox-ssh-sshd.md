# Story 2.12: Loopback-bound port publication + opt-in `KEEL_DEVBOX_SSH` sshd

Status: review <!-- Ralph-internal `Story State` = `fixes-pending` after iter-278 PATCH-6 LANDING — rc-capture rewrite of `packages/devbox/entrypoint.sh:141-211` (comment block L141-170 rewritten to describe actual bash(1) `||`-LHS recursive-suppression mechanism + empirical reproduction reference + PATCH-3 historical cascade note; subshell body re-indented +2 inside `(` absorbing iter-277 cosmetic DEFER; PATCH-4 comment rewritten to reference "immediately above" absorbing iter-277 line-pin DEFER). Empirical verification: `bash -c 'set +e; (set -e; false; echo X); rc=$?; set -e; [[ $rc -ne 0 ]] && echo "DIAG rc=$rc"; echo OK'` outputs `DIAG rc=1` then `OK` (failure path correct); happy-path variant outputs `X` then `OK rc=0`. `bash -n` syntax-clean; entrypoint.sh NOT contentHash-tracked so no sync-gate recompute needed; `pnpm keel-invariants:check` carried GREEN from iter-276. Cumulative Story 2.12 PATCH count = 5 (iter-271) + 1 (iter-277/278) = 6 — within iter-271 LESSON 4-7 band. Sprint-status row UNCHANGED at `review`. Next iter (iter-279): `/bmad-code-review (args: "2")` closure re-run #2 (`fixes-pending → done | fixes-pending`) — forecast ZERO-PATCH per iter-264 LESSON multi-pass CR cycle (single-region rewrite convergence). Prior state: `fixes-pending` after iter-277 `/bmad-code-review (args: "2")` confirmation re-run #1 (`fixes-pending → fixes-pending`) — 1 NEW PATCH (PATCH-6: bash `||`-LHS errexit suppression empirically disconfirms PATCH-3's stated mechanism; pre-spawn failures cascade silently and PATCH-4's destructive `rm -rf host_keys` runs unconditionally) + 9 DEFER + 3 DISMISS. Three-layer fan-out reproduced iter-271 pattern; Winston ZERO-PATCH verdict EMPIRICALLY DISCONFIRMED by Ralph in-iter `bash 5.2.21` reproduction (`set -euo pipefail; ( false; echo X ) || echo Y` outputs `X`, NOT `Y`). NEW LESSON: bash `||`-LHS subshell errexit suppression is recursive; ONLY `bash -c '...' || diag` subprocess and `set +e; (set -e; ...); rc=$?; set -e` rc-capture patterns escape — promoted to RALPH.md § Lessons + § Signposts at iter-277. Cumulative Story 2.12 PATCH count = 5 (iter-271) + 1 (iter-277) = 6 — within iter-271 LESSON 4-7 band for novel-runtime-behaviour stories. Per iter-264 LESSON Story 2.6/2.11 multi-pass CR cycle, closure re-run #2 (iter-279) forecast ZERO-PATCH. Sprint-status row UNCHANGED at `review` per iter-202 + iter-260 + iter-271 precedent. Next iter (iter-278): PATCH-6 sole NOW task per Guardrail 5; iter-279 re-runs CR for closure re-run #2. Prior state: `fixes-pending` after iter-271 CR landing (`sm-verified → fixes-pending`) — 5 PATCH queued (4 substantive HIGH/MEDIUM-HIGH + 1 MEDIUM scope refactor), 11 DEFER, ~10 DISMISS. Forecast band breach: 5 PATCH lands ABOVE iter-264 LESSON 0-3 first-class PATCH band — driven by iter-266 NOVEL LESSON's "novel-runtime-behaviour" surface (sshd-host-key bootstrap + cap_drop posture interaction + entrypoint set -e discipline). iter-270 SM ZERO-PATCH was structurally unable to catch PATCH-1 through PATCH-4 because all 4 only manifest at first live container exec under cap_drop:[ALL] which iter-270 had NO EXECUTION PATH to (live smokes operator-workstation-deferred). NEW LESSON promoted to RALPH.md § Lessons + § Signposts at iter-271. Sprint-row stays `review` across CR-fix-loop window per iter-202 + iter-260 precedent (flips to `done` only at CR closure with all PATCHes applied). Three-layer fan-out: Blind Hunter (`general-purpose` diff-only — 24 raw 3H/9M/12L); Edge Case Hunter (`general-purpose` diff+project-read — 27 raw 1H/10M/16L); Acceptance Auditor (`bmad-agent-architect` Winston diff+spec+context — 12 raw 2H/2M/8L). All 5 PATCHes ≥2-layer convergent (iter-260 LESSON convergence-signal-high-confidence-triage applies). PATCH list at QUEUE TOP: P-1 (HIGH; entrypoint.sh:146+:162 chmod via gosu under cap_drop) → P-2 (HIGH; docker-compose.yml:145 KEEL_DEVBOX_SSH_RESOLVED propagation) → P-3 (HIGH; entrypoint.sh:138-176 subshell-wrap honouring SC-10) → P-4 (MEDIUM-HIGH; entrypoint.sh:157 rm-then-mv recovery) → P-5 (MEDIUM; 15-site refactor to compose-args.sh helper). 11 DEFERs logged in `### Review Findings` + `_bmad-output/implementation-artifacts/deferred-work.md § Deferred from: code review of story-2.12 (2026-04-24)`. Next iter (iter-272): PATCH-1 sole NOW task per Guardrail 5; QUEUE drain through PATCH-5 → re-run `/bmad-code-review (args: "2")` for `fixes-pending → done|fixes-pending` confirmation gate per § Story Lifecycle. Per iter-264 LESSON Story 2.6/2.11 multi-pass cycle, budget TWO closure re-runs at worst case. Prior state: `sm-verified` after iter-270 post-dev SM landing — ZERO-PATCH outcome per SEVENTH cumulative ZERO-PATCH advance precedent (extends iter-264 ZERO-PATCH CR class with first-of-kind ZERO-PATCH post-dev SM precedent). Two-subagent pattern per iter-235 LESSON (third cumulative application after iter-235 + iter-259): AC-verifier `bmad-agent-dev` (Amelia) + drift-detector `bmad-agent-architect` (Winston) converged on clean advance. Amelia: all 5 ACs PASS, NONE unmet, F3 (liveness non-fatal) + F4 (ListenAddress unset) CRITICAL pre-dev SM PATCHes from iter-266 both confirmed landed verbatim, sync-gate GREEN (sha256 e1d693cb…2a3c2 matches manifest contentHash; entry count 32), 15-site shim tally exact, SC-17 sibling-insertion discipline honoured across INVARIANTS.md / README.md / AGENTS.md, `.envrc.example:40` past-tense per SC-15. Winston: ZERO CRITICAL drift, 8 MINOR drifts all MECHANICAL line-shift artifacts of dev-pass's own substrate edits (Tasks 1/2/3/4 touched cited files) → DEFER to Story 2.17 close-out per SC-17 carry-forward; cross-invariant coherence INTACT across `INV-devbox-homedev-named-volume` + `INV-devbox-mode` + `INV-devbox-prereq-check` + fork/AMEND posture; PATCH-1/PATCH-2/PATCH-7 from iter-266 ALL HELD. Trace artefacts at `_bmad-output/test-artifacts/traceability/2-12-*` (iter-269 WAIVED). Sprint-row stays `review` across SM-gate window; flips to `done` at CR closure per Story 2.11 iter-258 precedent. Next iter (iter-271): `/bmad-code-review (args: "2")` per § Story Lifecycle row `sm-verified → done|fixes-pending` — three-layer adversarial fan-out; ZERO-PATCH CR precedent candidate would create first cumulative double-ZERO-PATCH story. Prior state: `traced` after iter-269 `/bmad-testarch-trace (args: "yolo")` landing — WAIVED per TWENTY-SECOND cumulative trace-WAIVED precedent + TWENTY-THIRD ATDD-skip-trace-WAIVED pairing (ground-(a)+(b)+(c) hybrid conjunction: substrate-verification covers ACs 1-3 via iter-268 impl-time smokes; ACs 4-5 operator-workstation-deferred by AC contract; no test runner wired at Story 2.12 substrate stage per Epic 13 scope). Prior state: `in-dev` after iter-268 `/bmad-dev-story` landing (`atdd-scaffolded → in-dev`). All 6 Tasks + 5 ACs landed in one-pass (no partial split). Task 1 Dockerfile openssh-server apt package + baked sshd_config template + pre-created /home/dev/.ssh/host_keys. Task 2 packages/devbox/docker-compose.ssh.yml compose override + resolve_ssh_state() function appended to main-repo-resolver.sh + 15 compose-invoking call sites threaded across 8 shims (build/rebuild/start/stop/status/logs/clean/benchmark) via the ${KEEL_DEVBOX_COMPOSE_FILE_SSH:+-f "${KEEL_DEVBOX_COMPOSE_FILE_SSH}"} conditional-expansion idiom. Task 3 entrypoint.sh conditional sshd-launch block with first-boot atomic ed25519 + rsa 4096 host-key generation + liveness-check post-spawn. Task 4 docker-compose.yml environment block KEEL_DEVBOX_SSH propagation + TODO removal + Story 2.12 LANDED roadmap line. Task 5 INV-devbox-ssh manifest entry (contentHash e1d693cb0ffa0c7c8d6966b8ce311c2e1daf13d83155d8a0b88028d96112a3c2) + docs/invariants/devbox-ssh.md + INVARIANTS.md H3 + anchor bullet; manifest entry count 31→32; `pnpm keel-invariants:check-all` GREEN. Task 6 .envrc.example:40 past-tense update + packages/devbox/README.md § Opt-in SSH (Story 2.12) H2 sibling-append + AGENTS.md § Opt-in SSH (Story 2.12) H3 sibling-append. Impl-time smokes: resolver normalisation verified under strict-true-only posture (true/TrUe → true; false/yes/empty/unset → false); `pnpm keel-invariants:check-all` confirms manifest + anchor regex compliance; bash -n syntax-clean across all 10 modified shell scripts; js-yaml parse-clean on both compose files. Live-container smokes (docker compose config port-form assertions + pgrep sshd + docker port output + operator pubkey flow) operator-workstation-deferred per Story 2.5 iter-186 posture (DinD backend B cannot exercise AC 4 non-loopback LAN peer; Docker not available in iteration env). Sprint-status flipped `ready-for-dev → in-progress` at iter-268 start, → `review` at Step 9 close-out. Prior transitions preserved: `drafted` iter-265; `validated` iter-266 (8 PATCH + 8 DEFER + 3 DISMISS); `atdd-scaffolded` iter-267 (SKIP-WITH-GROUNDS-(c)+(ii)+(iii); 22nd-cumulative skip precedent). Next gate: `/bmad-testarch-trace (args: "yolo")` at iter-269 (`in-dev → traced` or `trace-fixes-pending`) — forecast WAIVE-partial per iter-256 Story 2.11 pattern (ACs 1-3 covered by impl-time smokes + manifest sync-gate; ACs 4-5 operator-workstation-deferred). -->

<!-- Note: Validation is optional. Run `/bmad-create-story (args: "review")` for pre-dev quality check before `/bmad-testarch-atdd` / `/bmad-dev-story`. -->

## Story

As a substrate maintainer,
I want every published devbox port bound to `127.0.0.1` (no `0.0.0.0`) and an opt-in sshd (enabled via `KEEL_DEVBOX_SSH=true`) that is pubkey-only and loopback-bound at `127.0.0.1:2222`,
So that the devbox's attack surface is limited to the host loopback, never a LAN or internet-reachable interface.

## Acceptance Criteria

1. **Loopback-bound port publication — every `ports:` mapping uses `127.0.0.1:<host>:<container>` form.** Given `packages/devbox/docker-compose.yml`, when I inspect the `ports:` block, then every port mapping uses the explicit `127.0.0.1:<host>:<container>` form (no `0.0.0.0:...` form, no bare `<host>:<container>` form which Docker silently binds to `0.0.0.0`). Story 2.2 (landed iter-148) already established this posture for the four publish ports `KEEL_DEVBOX_PORT_WEB` / `..._API` / `..._STORYBOOK` / `..._VITE_HMR` at `docker-compose.yml:153-157`; Story 2.12 PINS the posture as an invariant so future story edits cannot regress it. The new sshd port mapping (when added by AC 3) MUST also use the loopback-bound form.

2. **Default — `KEEL_DEVBOX_SSH=false` — sshd does NOT run AND port 2222 is NOT published.** Given `KEEL_DEVBOX_SSH=false` (the `.envrc.example:40` default; iterating per Story 2.2 SC the unset case also defaults to `false`), when the container starts (`pnpm devbox:start`), then no `sshd` process is running inside the container (`pgrep sshd` inside the container returns no PIDs / exit 1) AND `docker port keel-devbox` lists no `2222/tcp` mapping AND `docker compose -f packages/devbox/docker-compose.yml config` emits NO `127.0.0.1:2222:2222` line (compose stack matches the no-SSH posture at the YAML layer, not just the runtime layer). The four Story 2.2 publish ports remain published unaffected.

3. **Opt-in — `KEEL_DEVBOX_SSH=true` — sshd runs, pubkey-only, port 2222 loopback-bound via published-port confinement, host keys persisted in named volume.** Given `KEEL_DEVBOX_SSH=true` in `.envrc`, when the container starts, then sshd runs inside the container listening on port 2222 (container-side `ListenAddress` intentionally unset — inbound packets arrive on container eth0, not container-loopback, under both `userland-proxy=true` and `userland-proxy=false` Docker modes; loopback confinement is enforced by the `127.0.0.1:2222:2222` publish in `docker-compose.ssh.yml`, NOT by sshd config) AND password auth is disabled (`PasswordAuthentication no`, `ChallengeResponseAuthentication no`, `KbdInteractiveAuthentication no`) AND only pubkey auth is allowed (`PubkeyAuthentication yes`) AND root login is disabled (`PermitRootLogin no`) AND host keys auto-generate on first boot via explicit `ssh-keygen -t ed25519 -f /home/dev/.ssh/host_keys/ssh_host_ed25519_key -N ""` + `ssh-keygen -t rsa -b 4096 -f /home/dev/.ssh/host_keys/ssh_host_rsa_key -N ""` (entrypoint logic — the keys persist in the `keel_home_dev` named volume per Story 2.5 so subsequent restarts re-use the same keys; first-boot generation is a one-time event gated on key absence). `docker port keel-devbox` lists `2222/tcp -> 127.0.0.1:2222` AND `docker compose … config` emits the loopback-bound `127.0.0.1:${KEEL_DEVBOX_SSH_PORT:-2222}:2222` line.

4. **External (non-loopback) connection refusal.** Given sshd is enabled (`KEEL_DEVBOX_SSH=true`), when an external (non-loopback) client attempts to connect (e.g., `ssh -p 2222 dev@<host-LAN-IP>` from a peer machine on the host's LAN), then the connection is refused at the Docker published-port layer (the `127.0.0.1:2222:2222` mapping binds the host socket exclusively to the `127.0.0.1` host-loopback interface — under `userland-proxy=true` mode, `docker-proxy` only listens on `127.0.0.1:2222`; under `userland-proxy=false` mode, Docker's iptables `DOCKER` chain DNAT rules are scoped to the loopback destination. Both modes refuse LAN-sourced traffic) AND only `ssh -p 2222 dev@127.0.0.1` from the host (the Docker-daemon host) succeeds with a registered pubkey. AC verification is operator-workstation only — DinD backend B (the iteration env) cannot exercise non-loopback LAN clients meaningfully; substrate verification per Story 2.5 iter-186 posture (config-shape correctness via `docker compose config` + invariant doc claim). NOTE: Defence-in-depth via container-side sshd `ListenAddress 127.0.0.1` is intentionally NOT used — see AC 3 + sshd_config header comment — because container-loopback is disjoint from host-loopback and would silently reject all inbound traffic under either Docker userland-proxy mode. Single-layer host-side publish confinement is correct and sufficient.

5. **Operator authorized-keys flow — pubkey written to `/home/dev/.ssh/authorized_keys` inside named volume; persists across restarts.** Given a fork operator wants to add their pubkey, when they follow the documented flow in `packages/devbox/README.md § Opt-in SSH (Story 2.12)`, then the pubkey is appended to `/home/dev/.ssh/authorized_keys` inside the `keel_home_dev` named volume (Story 2.5 substrate; persists across container restarts + image rebuilds; tokens never bind-mounted to host per NFR10) AND a subsequent `ssh -p 2222 dev@127.0.0.1 -i <matching-private-key>` succeeds AND the pubkey survives `pnpm devbox:stop && pnpm devbox:start` AND survives `pnpm devbox:rebuild` (image rebuild) because the named volume is preserved across both operations (Story 2.6 AC 4 + Story 2.11 AC 1 contracts).

## Tasks / Subtasks

- [x] **Task 1: Add `openssh-server` to the Dockerfile apt layer + bake `sshd_config` template** (AC 3, AC 4)
  - [x] Edit `packages/devbox/Dockerfile:43-91` (apt-get layer): add `openssh-server` package alongside the existing `openssh-client` (line 65). Renovate `apt` manager (Story 1.15) tracks the package version.
  - [x] Bake a hardened `sshd_config` template at image-build time. Source: `packages/devbox/sshd/sshd_config` (NEW file under `packages/devbox/sshd/` directory, mirroring the Story 2.3 `packages/devbox/dnsmasq/` + `packages/devbox/nftables/` template-directory convention). Copy via Dockerfile `COPY packages/devbox/sshd/sshd_config /etc/ssh/sshd_config` (path relative to compose build context — `packages/devbox/Dockerfile`'s context is `packages/devbox/` per `docker-compose.yml:69-71` `build.context: .`). Set permissions: `chmod 0644 /etc/ssh/sshd_config && chown root:root /etc/ssh/sshd_config`.
  - [x] `sshd_config` content (verbatim — copy into `packages/devbox/sshd/sshd_config`; NOT contentHash-tracked since not a `sourcePath` of any `INV-*` entry):
    ```
    # packages/devbox/sshd/sshd_config — Story 2.12
    # Hardened sshd config; pubkey-only, root-disabled.
    #
    # Loopback binding at container-side is NOT specified here — the
    # container's network namespace receives incoming published-port
    # traffic on its eth0 interface (via Docker's iptables-NAT rules
    # under default `userland-proxy=false` mode, or via docker-proxy's
    # re-originated connection under `userland-proxy=true`). In BOTH
    # modes, packets arrive on eth0, NOT on the container's 127.0.0.1.
    # Binding sshd to `ListenAddress 127.0.0.1` would silently drop
    # all inbound connections. Leave `ListenAddress` at sshd's default
    # (any address in container netns); host-side loopback confinement
    # is enforced by the `127.0.0.1:2222:2222` publish in
    # docker-compose.ssh.yml (AC 4). The container netns is already an
    # isolated attack surface — external peers cannot reach eth0
    # without traversing the published-port gate on the host.
    Port 2222
    HostKey /home/dev/.ssh/host_keys/ssh_host_ed25519_key
    HostKey /home/dev/.ssh/host_keys/ssh_host_rsa_key
    PermitRootLogin no
    PasswordAuthentication no
    ChallengeResponseAuthentication no
    KbdInteractiveAuthentication no
    PubkeyAuthentication yes
    AuthorizedKeysFile /home/dev/.ssh/authorized_keys
    AllowUsers dev
    UsePAM no
    PrintMotd no
    AcceptEnv LANG LC_*
    Subsystem sftp /usr/lib/openssh/sftp-server
    LogLevel INFO
    SyslogFacility AUTH
    ```
  - [x] Pre-create `/home/dev/.ssh/` + `/home/dev/.ssh/host_keys/` directories in the Dockerfile near line 252 (where `/home/dev/.claude` + `/home/dev/.config/gh` are pre-created). Mode 0700; owner `dev:dev` (matches Story 2.5 non-root-user posture). Do NOT pre-generate the host keys at image-build time — first-boot generation in entrypoint.sh ensures the keys live in the named-volume payload (per AC 3 persistence requirement).
  - [x] **No `setcap` invocation.** Port 2222 > 1024; `NET_BIND_SERVICE` is NOT required for the bind. Sshd runs as `dev` (UID 1000) with no extra capability needed. Verify at impl time: `ss -tlnp` (or `ss -tlpn` if `-p` requires root) inside the container confirms `0.0.0.0:2222` is bound by `sshd` running as `dev`. Do NOT add a `setcap +ep /usr/sbin/sshd` line to the Dockerfile — it would violate Story 2.5's `no-new-privileges=1` posture expectations and is unnecessary.

- [x] **Task 2: Conditionally publish port 2222 via a compose override file `docker-compose.ssh.yml` + resolver wiring** (AC 2, AC 3)
  - [x] Create `packages/devbox/docker-compose.ssh.yml` — Compose override that ADDS the port 2222 mapping when included. Shape:
    ```yaml
    # packages/devbox/docker-compose.ssh.yml — Story 2.12
    # Compose override: adds loopback-bound port 2222 publication for opt-in sshd.
    # Included by host-side shims when KEEL_DEVBOX_SSH=true. NEVER edit the
    # base docker-compose.yml ports block to add 2222 — this file is the
    # only site that publishes 2222.
    services:
      devbox:
        ports:
          - '127.0.0.1:${KEEL_DEVBOX_SSH_PORT:-2222}:2222'
    ```
  - [x] **Compose-CLI inclusion path.** When `KEEL_DEVBOX_SSH=true`, host-side shims that invoke `docker compose -f "${COMPOSE_FILE}"` MUST extend the invocation with a second `-f "${COMPOSE_FILE_SSH}"` after the base file. Compose v2 multi-file merge spec: `services.<svc>.ports` is a sequence-typed key that CONCATENATES across `-f` files (ref: compose-spec.io § Merge/override — `ports`, `expose`, `external_links`, `dns`, `dns_search`, `tmpfs`, `volumes` are additive sequences). Base file's 4 Story-2.2 ports + override's 1 SSH port = 5 total; no duplicate-2222 risk because the base file does NOT publish 2222.
  - [x] **Resolver extension.** Add a NEW function `resolve_ssh_state()` to `packages/devbox/scripts/lib/main-repo-resolver.sh` (sibling to `resolve_main_repo_and_workdir` + `resolve_mode_specific_state`). Function reads `KEEL_DEVBOX_SSH` from the process env, normalises (lowercase via `tr '[:upper:]' '[:lower:]'` + accept-`true`-only — fail-closed to no-SSH for any other value including `yes`/`1`/`on`), mirroring the Story 2.11 `KEEL_DEVBOX_SHARED` normalisation idiom at `lib/main-repo-resolver.sh:152` (the `tr` line; the `resolve_mode_specific_state()` function opener is at `:146`). Exports:
    - `KEEL_DEVBOX_SSH_RESOLVED=true|false` (canonical normalised value).
    - `KEEL_DEVBOX_COMPOSE_FILE_SSH` — EITHER the empty string (no-SSH) OR the absolute path `"${DEVBOX_DIR}/docker-compose.ssh.yml"` (opt-in SSH). The shim pattern then becomes `docker compose -f "${COMPOSE_FILE}" ${KEEL_DEVBOX_COMPOSE_FILE_SSH:+-f "${KEEL_DEVBOX_COMPOSE_FILE_SSH}"}` — the `${VAR:+-f "${VAR}"}` parameter-expansion form expands to `-f <path>` when non-empty and to the empty string when empty, avoiding the word-splitting hazard of passing a joined `-f <a> -f <b>` string through unquoted expansion (RALPH.md 2026-04-21 AR-9 bash-iterable-word-splitting LESSON). Shared mode (Story 2.11) does NOT change this — the override composes regardless of mode (sshd is per-container, not per-mode).
  - [x] Caller invocation order: `resolve_main_repo_and_workdir` → `resolve_mode_specific_state` → `resolve_ssh_state` → exports → downstream `docker compose` invocations.
  - [x] **Update the 8 actual compose-invoking shims** to thread `${KEEL_DEVBOX_COMPOSE_FILE_SSH:+-f "${KEEL_DEVBOX_COMPOSE_FILE_SSH}"}` through every `docker compose -f "${COMPOSE_FILE}"` call site. Verified inventory (via `grep -c '^[^#]*docker compose -f' packages/devbox/scripts/*.sh` at iter-266): `build.sh` (1 site), `rebuild.sh` (1), `start.sh` (1), `stop.sh` (1), `status.sh` (1), `clean.sh` (2 sites — preserve + destructive paths), `logs.sh` (2 sites — follow-mode + passthrough-mode), `benchmark.sh` (6 sites — NFR2 cold/warm instrumentation, all paths MUST pick up the override so sshd-enabled benchmarks measure the enabled posture). Scripts WITHOUT compose invocations and therefore NOT in scope for substitution: `restart.sh` (delegates to stop.sh + start.sh), `env-check.sh` (parses `.envrc` only; no compose call), `monitor-host.sh` (`docker exec` shim, not compose), `attach.sh` / `shell.sh` / `claude-host.sh` / `gh-auth-host.sh` / `ralph-build-host.sh` / `ralph-plan-host.sh` / `whitelist-host.sh` (docker-exec shims). Story 2.7's 2 ralph wrappers + Story 2.8/2.9 auth shims + Story 2.6 shell/attach do NOT compose-invoke and therefore inherit sshd state via the already-running container's environment (set at `docker compose up` time via Task 4 env propagation).
  - [x] Header comment block update: the existing `lib/main-repo-resolver.sh:1-28` doc-block covers only the original three WORKTREE_ROOT / REPO_NAME / CONTAINER_WORKDIR resolver outputs; Story 2.11 extended the file via a SECOND doc paragraph at `:35-77` describing `resolve_mode_specific_state()`. Task 2 adds a THIRD doc paragraph AFTER the Story 2.11 paragraph (i.e., inserted at approximately `:78` — adjust line for current file) describing `resolve_ssh_state()`: the two outputs (`KEEL_DEVBOX_SSH_RESOLVED`, `KEEL_DEVBOX_COMPOSE_FILE_SSH`), the `true`-only normalisation, and the caller invocation order contract. Do NOT edit the original `:1-28` or the Story 2.11 `:35-77` paragraphs.

- [x] **Task 3: First-boot host-key generation + conditional sshd start in `entrypoint.sh`** (AC 3, AC 5)
  - [x] **Insertion point.** Edit `packages/devbox/entrypoint.sh` — add a NEW block AFTER the existing Story 2.3 egress-init block (`entrypoint.sh:121-130` — the `start-egress.sh` dispatch wrapped in an `if [[ ... ]]` conditional) and BEFORE the CMD-empty fallback + `exec gosu dev "$@"` handoff at `entrypoint.sh:155-159` (`:154` is the `if [ "$#" -eq 0 ]; then` opener; `:155` and `:157` are the two exec handoffs). The new block MUST land BEFORE the if/else block, not inside it.
  - [x] **Entrypoint block contract** (copy-paste-ready; NO placeholders):
    ```bash
    # Story 2.12: opt-in sshd. KEEL_DEVBOX_SSH is normalised on the host by
    # resolve_ssh_state() before container start; here we honour the
    # container-side env var (propagated via docker-compose.yml § environment
    # by Task 4). Case-sensitive "true" match mirrors Story 2.11 shared-mode
    # entrypoint posture — normalisation already done host-side.
    if [[ "${KEEL_DEVBOX_SSH:-false}" == "true" ]]; then
      # Pre-creation runs as root (pre-gosu). Use gosu to create the tree
      # with dev:dev ownership from t=0 so the subsequent keygen + sshd both
      # run as dev without relying on CAP_CHOWN (Story 2.5 hardening).
      gosu dev mkdir -p /home/dev/.ssh/host_keys
      # Idempotent perm enforcement EVERY boot (not gated on existence).
      # The named volume may have been inspected from host OR pre-populated
      # with stray perms on upgrade; sshd's StrictModes (default yes) rejects
      # any parent dir more permissive than 0700, silently. Enforce.
      chmod 0700 /home/dev/.ssh /home/dev/.ssh/host_keys
      # Atomic host-key generation: a mid-keygen container kill leaves a
      # partial keypair which sshd refuses. Generate BOTH keys into a
      # scratch dir then atomically mv into place. Guard on BOTH algorithms'
      # final filenames — if either is missing, regenerate both.
      if [[ ! -f /home/dev/.ssh/host_keys/ssh_host_ed25519_key ]] \
         || [[ ! -f /home/dev/.ssh/host_keys/ssh_host_rsa_key ]]; then
        gosu dev rm -rf /home/dev/.ssh/host_keys.tmp
        gosu dev mkdir -p /home/dev/.ssh/host_keys.tmp
        gosu dev ssh-keygen -q -t ed25519 -f /home/dev/.ssh/host_keys.tmp/ssh_host_ed25519_key -N "" < /dev/null
        gosu dev ssh-keygen -q -t rsa -b 4096 -f /home/dev/.ssh/host_keys.tmp/ssh_host_rsa_key -N "" < /dev/null
        gosu dev mv -T /home/dev/.ssh/host_keys.tmp /home/dev/.ssh/host_keys
      fi
      # Touch authorized_keys with 0600 every boot (idempotent). Empty file
      # = no inbound auth; operator appends pubkeys via Task 6 flow (AC 5).
      [[ -f /home/dev/.ssh/authorized_keys ]] || gosu dev touch /home/dev/.ssh/authorized_keys
      chmod 0600 /home/dev/.ssh/authorized_keys
      # Launch sshd as dev (port 2222 > 1024; NET_BIND_SERVICE not needed).
      # -D = foreground; backgrounded with `&` because PID 1 is the `exec
      # gosu dev "$@"` handoff below. Liveness verified post-spawn so a
      # silent sshd startup failure does NOT leave the entrypoint proceeding
      # with an un-started sshd + operator seeing port 2222 published +
      # connections refusing (§ Dev Notes § Liveness verification contract).
      gosu dev /usr/sbin/sshd -D -e 2>>/var/log/sshd.log &
      SSHD_PID="$!"
      sleep 0.5
      if ! kill -0 "${SSHD_PID}" 2>/dev/null; then
        echo "entrypoint: sshd failed to start; tail /var/log/sshd.log:" >&2
        tail -n 20 /var/log/sshd.log >&2 || true
        echo "entrypoint: sshd startup failure is NON-FATAL under Story 2.12 SC-10 (background process; PID 1 remains the exec handoff). Operator: pnpm devbox:logs keel-devbox + investigate." >&2
      fi
    fi
    ```
  - [x] **`set -e` + background-spawn interaction.** The `gosu dev /usr/sbin/sshd -D … &` returns exit 0 from the FORK regardless of whether sshd successfully bound the port (bash's `&` operator itself never fails). Under `set -e` this means a crashing sshd would NOT trip the entrypoint — the container would proceed cleanly with port 2222 published (by Docker) but no listener inside. The `kill -0 ${SSHD_PID}` check 0.5s later catches the common failure modes (config syntax error, host-key unreadable, port already in use by stray process); non-fatal reporting preserves SC-10 (background process lifecycle = operator's responsibility) while surfacing the failure in container logs. Story 2.13 healthcheck is the authoritative long-term signal.
  - [x] **Ownership under `cap_drop: [ALL]`.** Running `mkdir -p` + key generation as `gosu dev` ensures the directory tree + key files are created with `dev:dev` ownership from t=0. The entrypoint's `chmod` calls do NOT need CAP_CHOWN (chmod only needs process ownership, which dev has). The separate `chown` calls from the pre-Story-2.12 named-volume-dir block (`entrypoint.sh:111-117`) retain their existing `2>/dev/null || true` best-effort posture — Story 2.12 adds no new root-`chown` calls.
  - [x] **Forbidden runtime install.** `sshd_config` is BAKED at image-build time (Task 1); entrypoint MUST NOT modify it at runtime. `ssh-keygen` is a binary from `openssh-server` — no apt invocation, no curl-pipe-sh, conforms to entrypoint.sh's forbidden-runtime-install posture (Story 2.1 AC 2; entrypoint header comment block lines 17-19).

- [x] **Task 4: Propagate `KEEL_DEVBOX_SSH` into the container via compose `environment:` block** (AC 3 prerequisite)
  - [x] Edit `packages/devbox/docker-compose.yml` — extend the existing `environment:` block at line 135-136 (currently propagates only `KEEL_DEVBOX_REPO_NAME`) to add `KEEL_DEVBOX_SSH`:
    ```yaml
    environment:
      KEEL_DEVBOX_REPO_NAME: ${KEEL_DEVBOX_REPO_NAME:-ralph-bmad}
      KEEL_DEVBOX_SSH: ${KEEL_DEVBOX_SSH:-false}
    ```
  - [x] Without this propagation, entrypoint.sh's `${KEEL_DEVBOX_SSH:-false}` reads from the container's env which is empty for non-explicitly-propagated vars (compose's `${VAR}` interpolation runs on the HOST at parse time; the container only sees vars in `environment:` or `env_file:`).
  - [x] Remove the existing TODO marker at `docker-compose.yml:250` (`# TODO(Story 2.12): opt-in sshd + port 2222 publication via KEEL_DEVBOX_SSH=true.`) since Story 2.12 lands the full opt-in path.
  - [x] Amend the Story-roadmap comment block at `docker-compose.yml:1-27` to add a Story 2.12 LANDED line: `#   - Story 2.12 : opt-in sshd via KEEL_DEVBOX_SSH=true (port 2222, pubkey-only, host keys persisted in keel_home_dev). LANDED iter-<this>.` Insertion point: between the existing Story 2.11 line (`docker-compose.yml:22`) and the existing Story 2.13 line (`docker-compose.yml:23`). Match the past-tense LANDED-iter-NNN pattern of Stories 2.2 / 2.3 / 2.5 / 2.11.

- [x] **Task 5: Register `INV-devbox-ssh` + author `docs/invariants/devbox-ssh.md`** (AC 1–5 machine-enforced contract)
  - [x] Add new entry to `packages/keel-invariants/src/invariants.manifest.ts`:
    - `id: 'INV-devbox-ssh'`
    - `description: 'Opt-in sshd via KEEL_DEVBOX_SSH=true — pubkey-only, root-disabled, loopback-bound 127.0.0.1:2222, host keys + authorized_keys persisted in keel_home_dev named volume; loopback-bound port publication invariant for ALL ports (no 0.0.0.0 / no bare-port bindings) (Story 2.12).'`
    - `sourcePath: 'docs/invariants/devbox-ssh.md'`
    - `contentHash: '<sha256 of the authored md file content>'`
    - `anchors: ['INV-devbox-ssh']`
  - [x] **InvariantSchema five-field compliance** (Story 2.3 iter-156 LESSON; verified at `packages/keel-invariants/src/invariants.manifest.ts:3-15`): `InvariantSchema` = `{id, description, sourcePath, contentHash, anchors}` — no `name` field; `anchors` entries are bare ID strings; `contentHash` is bare 64-char lowercase hex. Cross-check existing Story 2.11 `INV-devbox-mode` entry for canonical shape.
  - [x] Author `docs/invariants/devbox-ssh.md` with the following H2 sections (Story 2.3 iter-156 LESSON: multi-faceted contracts embed sub-schemas verbatim in the `sourcePath` doc):
    - `## Loopback-bound port publication contract` — every `ports:` mapping MUST use `127.0.0.1:<host>:<container>` form; bare `<host>:<container>` and explicit `0.0.0.0:...` are forbidden. Applies to base compose + every override (sibling `docker-compose.ssh.yml`, future per-fork add-ons).
    - `## SSH signal` — `KEEL_DEVBOX_SSH=true|false|unset`; normalisation rules (lowercase compare; non-`true` defaults to `false`; fail-closed to no-SSH on any unrecognised value).
    - `## Opt-in sshd contract` — sshd runs as UID 1000 (`dev`); `Port 2222`; `PermitRootLogin no`; `PasswordAuthentication no`; `ChallengeResponseAuthentication no`; `KbdInteractiveAuthentication no`; `PubkeyAuthentication yes`; `AllowUsers dev`; `UsePAM no`; host keys at `/home/dev/.ssh/host_keys/ssh_host_{ed25519,rsa}_key`; `authorized_keys` at `/home/dev/.ssh/authorized_keys`; first-boot atomic key generation via explicit `-t ed25519` + `-t rsa -b 4096`; persistence via `keel_home_dev` named volume. Container-side `ListenAddress` INTENTIONALLY unset (see § External connection refusal for rationale).
    - `## No-SSH default contract` — `KEEL_DEVBOX_SSH=false` (or unset): sshd is NOT started AND port 2222 is NOT published AND `docker compose config` emits no 2222 mapping. Compose override file `packages/devbox/docker-compose.ssh.yml` is the SINGLE site that publishes 2222; the base `docker-compose.yml` MUST NOT publish 2222.
    - `## Resolver contract` — `packages/devbox/scripts/lib/main-repo-resolver.sh § resolve_ssh_state()` is the single resolution site; every host-side compose-invoking shim invokes it after `resolve_mode_specific_state`; exports `KEEL_DEVBOX_SSH_RESOLVED` + `KEEL_DEVBOX_COMPOSE_FILES_ARGS`.
    - `## External (non-loopback) connection refusal` — Docker's `127.0.0.1:2222:2222` mapping is the SINGLE loopback-confinement layer. Under `userland-proxy=true` (default), `docker-proxy` binds host-side `127.0.0.1:2222` only; under `userland-proxy=false`, iptables DNAT targets are scoped to the host's loopback destination. In BOTH modes, LAN-sourced traffic is refused. Container-side sshd `ListenAddress` is NOT used — container-loopback is disjoint from host-loopback and would silently drop traffic that arrives on container eth0. Operator-workstation verification only; substrate verifies via `docker compose config` shape + AC 4 invariant doc claim.
    - `## Operator authorized-keys flow` — pubkey appended to `/home/dev/.ssh/authorized_keys` from inside the container (`pnpm devbox:shell` then `echo 'ssh-ed25519 AAAA…' >> ~/.ssh/authorized_keys`); persistence guaranteed by the named volume across restarts + image rebuilds; agents MUST NOT bind-mount or copy `authorized_keys` outside the named volume (NFR10 — same posture as Story 2.8/2.9 OAuth tokens).
    - `## Named volume relationship to INV-devbox-homedev-named-volume` — Story 2.5's `INV-devbox-homedev-named-volume` pins the unqualified name `keel_home_dev`; Story 2.12's host keys + authorized_keys live INSIDE that volume; Story 2.12 does NOT touch the volume's name or substrate posture.
    - `## Invariant stability` — loopback-bound port publication is a SUBSTRATE INVARIANT — forks MAY NOT publish to `0.0.0.0` or use bare-port form. Opt-in sshd is OPERATOR-DEFAULT-OFF — forks MAY NOT change the default to `true`. Fork-level growth-tier `INVARIANTS.fork.md` rules MAY add fork-specific sshd_config overlays (e.g., MFA via `AuthenticationMethods publickey,keyboard-interactive`) but MAY NOT weaken substrate defaults (no PasswordAuthentication; no PermitRootLogin).
  - [x] Compute `contentHash`: `sha256sum docs/invariants/devbox-ssh.md | awk '{print $1}'`. Paste 64-char lowercase hex into the manifest entry. `pnpm keel-invariants:check` MUST pass after manifest + doc both land (Story 1.9 sync-gate verification).
  - [x] Append entry to `INVARIANTS.md` under the devbox section after `INV-devbox-mode` (Story 2.11 anchor) as: ``- **`INV-devbox-ssh`** — Opt-in sshd + loopback-bound port publication contract (`KEEL_DEVBOX_SSH=true` opens 127.0.0.1:2222 pubkey-only; ALL ports must use 127.0.0.1:<host>:<container> form). Source: `docs/invariants/devbox-ssh.md`.`` Index-only, no body (FR42).
  - [x] Anchor bullet MUST match the verbatim regex `/^-\s+\*\*\`(INV-[a-z0-9]+(?:-[a-z0-9]+)+)\`\*\*/gm` per `packages/keel-invariants/src/sync-gate.ts:24` (Story 1.9 sync-gate). Lowercase-after-`INV-` prefix MANDATORY (Story 1.9 iter-7 LESSON).
  - [x] Add new `### Devbox SSH (Story 2.12)` H3 in `INVARIANTS.md` AFTER the `### Devbox mode (Story 2.11)` H3 body + its `INV-devbox-mode` anchor bullet, and BEFORE the `### Gitignored-secret commit-deny (Story 2.2)` H3 (verified at `INVARIANTS.md:120` at iter-266 — the devbox H3 sequence is NOT strictly numerical; Story 2.2's gitignored-secret H3 lives AFTER all Story 2.1/2.3/2.5/2.10/2.11 devbox H3s). One-line H3 body (mirroring Story 2.11's one-line body), then the anchor bullet underneath.

- [x] **Task 6: Operator + agent documentation** (AC 1–5 comprehension)
  - [x] **`packages/devbox/README.md`** — append new H2 `## Opt-in SSH (Story 2.12)` AFTER the existing `## Per-fork vs shared devbox mode (Story 2.11)` H2 and BEFORE `## cc-devbox upstream provenance` (SC-17 sibling-append; do NOT edit prior story sections). Content:
    - (a) Loopback-bound port publication invariant statement (cite `INV-devbox-ssh` for machine-enforcement); apply to ALL future port additions.
    - (b) `.envrc` snippet showing `KEEL_DEVBOX_SSH=false` (default) and `KEEL_DEVBOX_SSH=true` (opt-in).
    - (c) Three operator walkthroughs:
      - **Default no-SSH walkthrough:** fresh fork, `.envrc` default, `pnpm devbox:start` → no port 2222 mapping (`docker port keel-devbox` confirms).
      - **Opt-in sshd walkthrough:** operator sets `KEEL_DEVBOX_SSH=true` in `.envrc`, runs `pnpm devbox:start` (host-side resolver appends `-f docker-compose.ssh.yml` to the compose CLI), entrypoint generates host keys on first boot + starts sshd, operator confirms via `docker port keel-devbox` (lists `2222/tcp -> 127.0.0.1:2222`) + `pgrep sshd` inside `pnpm devbox:shell`.
      - **Authorized-keys flow walkthrough:** operator runs `pnpm devbox:shell` → `cat ~/.ssh/authorized_keys` (empty initially) → `echo 'ssh-ed25519 AAAAxxx user@host' >> ~/.ssh/authorized_keys` (append from inside the container; pubkey persists in named volume) → exit shell → from host, `ssh -p 2222 -i ~/.ssh/id_ed25519 dev@127.0.0.1` succeeds.
    - (d) Mode-flip note: switching between `KEEL_DEVBOX_SSH=false` and `=true` requires container teardown (`pnpm devbox:stop && pnpm devbox:start`) — entrypoint reads the env var ONCE at container start. Mid-session env-var flips have no effect until restart.
    - (e) `INV-devbox-ssh` citation for the machine-enforced contract.
  - [x] **DO NOT modify existing `## Host-side CLI (Story 2.6)`, `## Ralph loop (Story 2.7)`, `## Claude Code authentication (Story 2.8)`, `## gh CLI authentication (Story 2.9)`, `## Prerequisite check (Story 2.10)`, or `## Per-fork vs shared devbox mode (Story 2.11)` sections** — append a NEW sibling H2 only (SC-17).
  - [x] **`AGENTS.md`** — append new H3 `### Opt-in SSH (Story 2.12)` AFTER the existing `### Per-fork vs shared devbox mode (Story 2.11)` H3 under § Devbox iteration environment. Content:
    - (a) Loopback-bound port publication invariant one-line statement: `Every published devbox port MUST use 127.0.0.1:<host>:<container> form. New port additions in any compose override file must follow this contract.`
    - (b) `KEEL_DEVBOX_SSH` opt-in one-liner + resolver site citation: `packages/devbox/scripts/lib/main-repo-resolver.sh § resolve_ssh_state()`.
    - (c) Compose override file pattern: `packages/devbox/docker-compose.ssh.yml` is the SINGLE site that publishes port 2222; included by the resolver into the compose CLI when `KEEL_DEVBOX_SSH=true`.
    - (d) Token persistence: host keys + `authorized_keys` live INSIDE the `keel_home_dev` named volume (Story 2.5 substrate; mirrors Story 2.8/2.9 OAuth-token-persistence posture). Agents MUST NOT bind-mount, copy, surface, or inspect these files outside the named volume.
    - (e) Re-auth pointer: if a Ralph subagent reports an SSH connection failure to `127.0.0.1:2222`, queue `pnpm devbox:shell` + manual `~/.ssh/authorized_keys` append as a fix task (operator-interactive). Agents SHOULD NOT auto-modify `authorized_keys` from outside the container.
    - (f) `INV-devbox-ssh` citation for the machine-enforced contract.
    - (g) Cross-references: § Container hardening (Story 2.5) for the named volume substrate; § Claude Code authentication (Story 2.8) + § gh CLI authentication (Story 2.9) for the token-persistence pattern parallel.
  - [x] **DO NOT modify existing `### Host-side CLI (Story 2.6)`, `### Ralph loop (Story 2.7)`, `### Claude Code authentication (Story 2.8)`, `### gh CLI authentication (Story 2.9)`, `### Prerequisite check (Story 2.10)`, or `### Per-fork vs shared devbox mode (Story 2.11)` sections** — append a NEW sibling H3 only (SC-17).
  - [x] **`packages/devbox/.envrc.example:40` comment update (SC-15):** current comment reads `# Opt-in sshd + port 2222 publication. Active Story 2.12; Story 2.2 publishes the knob only.` Update to past tense: `# false = no sshd (default); true = opt-in sshd + port 2222 publication. Active at Story 2.12 (landed iter-<this>). Resolver: packages/devbox/scripts/lib/main-repo-resolver.sh § resolve_ssh_state(). Compose override: packages/devbox/docker-compose.ssh.yml.`
  - [x] **Change Log v1.0 entry** — record 5 ACs + 6 tasks + ~14 SCs (see § Success Criteria); initial draft; dev-ready; ATDD forecast: NON-SKIP for ACs 1, 2, 3 (port-form assertions via `docker compose config` grep + entrypoint behaviour smokes); operator-workstation-deferred for ACs 4, 5 (live SSH connect requires real Docker + SSH client); trace forecast NON-WAIVED; CR PATCH forecast 0–3 opener (moderate novel surface — entrypoint extension + compose override + resolver function are structurally mirrored on Stories 2.3/2.5/2.11 precedents but the sshd-host-key-bootstrap path is genuinely novel; iter-253 LESSON forecast-carry: ~1/1000/year operator-edge SECOND-class DEFERs likely).
  - [x] **Sprint-status housekeeping:** `/bmad-create-story` workflow's step 6 flips `2-12-…: backlog → ready-for-dev` at THIS iteration's landing. `/bmad-dev-story` Step 4 flips `ready-for-dev → in-progress`; Step 9 flips `in-progress → review`. `/bmad-code-review (args: "2")` closure flips `review → done`.

### Review Findings

Iter-271 `/bmad-code-review (args: "2")` three-layer Ralph-hosted adversarial fan-out: Blind Hunter (`general-purpose`, diff-only — 24 raw, 3 HIGH/9 MEDIUM/12 LOW) + Edge Case Hunter (`general-purpose`, diff+project-read — 27 raw, 1 HIGH/10 MEDIUM/16 LOW) + Acceptance Auditor (`bmad-agent-architect` Winston, diff+spec+context — 12 raw, 2 HIGH/2 MEDIUM/8 LOW). Diff range `77f1748..HEAD -- ':!.ralph/' ':!RALPH.md'` (Story 2.12 substrate scope) = 2161 lines / 26 files / 1788 insertions. **Triage: 5 PATCH + 11 DEFER + ~10 DISMISS** (post-dedup/convergence). Three-layer convergence on substantive substrate-runtime defects unverified-by-construction at iter-270 SM (live container exec under cap_drop:[ALL] + KEEL_DEVBOX_SSH=true is operator-workstation-deferred per AGENTS.md § Container hardening live-smokes-deferred posture). Forecast band (iter-270 SM ZERO-PATCH carry-forward + iter-264 LESSON severity-class-first rule) anticipated 0-3 first-class PATCH; **5 PATCH lands ABOVE the band — driven by the iter-266 NOVEL LESSON's "novel-runtime-behaviour" surface (sshd-host-key bootstrap + cap_drop posture interaction + entrypoint set -e discipline) which static smokes (sha256 / bash -n / sync-gate / 15-site tally) cannot exercise.**

- [x] [Review][Patch] **`chmod` under `cap_drop: [ALL]` requires `gosu dev` prefix — root EPERMs on dev-owned files** [`packages/devbox/entrypoint.sh:146`, `:162`] — _LANDED iter-272 (PATCH-1, commit `30b7d8d`); confirmed iter-277 CR re-run._ — _Source: Acceptance Auditor #1+#3 (Winston HIGH-class)._ Lines 146 (`chmod 0700 /home/dev/.ssh /home/dev/.ssh/host_keys`) and 162 (`chmod 0600 /home/dev/.ssh/authorized_keys`) run as ROOT (`user: '0:0'` per `docker-compose.yml:216`). Targets are dev-owned (created by `gosu dev mkdir -p` line 141 + `gosu dev touch` line 161; named-volume bring-up under image-layer-owned `chown -R dev:dev /home/dev` from `Dockerfile:291`). Per `capabilities(7) § CAP_FOWNER`: root WITHOUT CAP_FOWNER cannot chmod files it does not own. Story 2.5 hardening pins `cap_drop: [ALL]` + `cap_add: [NET_ADMIN, NET_RAW, NET_BIND_SERVICE, SETUID, SETGID]` — CAP_FOWNER intentionally NOT in the bounding set. Result: chmod EPERMs → `set -euo pipefail` aborts entrypoint BEFORE `exec gosu dev "$@"` handoff → container fails to start with `KEEL_DEVBOX_SSH=true`. Existing entrypoint chown calls (`:101-104`, `:115-117`) wrap stderr-capture + continue precisely because of this; new chmod block does NOT follow the pattern. Iter-270 SM verified all 5 ACs PASS via static smokes only (YAML-parse + bash -n + sync-gate + sha256) — no live container exec. Dockerfile L261-262 explicitly notes "chown under cap_drop: [ALL] would fail anyway" — same logic applies to chmod. **Fix:** prefix both chmod calls with `gosu dev` (mirrors mkdir/keygen/mv pattern in same block; dev owns the files → no CAP_FOWNER needed). Two-line change.

- [x] [Review][Patch] **`KEEL_DEVBOX_SSH` host/container normalisation drift — propagate `_RESOLVED` not raw value** [`packages/devbox/docker-compose.yml:145`] — _LANDED iter-273 (PATCH-2, commit `1df64ab`); confirmed iter-277 CR re-run (resolver exports `KEEL_DEVBOX_SSH_RESOLVED` on both branches at `lib/main-repo-resolver.sh:244 + :259`)._ — _Source: Blind Hunter HIGH + Edge Case Hunter MEDIUM + Acceptance Auditor convergent on case-sensitivity split-brain._ Host-side `resolve_ssh_state()` (`lib/main-repo-resolver.sh:236`) case-folds `KEEL_DEVBOX_SSH` via `tr '[:upper:]' '[:lower:]'` and accepts `True`/`TRUE`/`true` → exports `KEEL_DEVBOX_COMPOSE_FILE_SSH` (port 2222 published via override). But `docker-compose.yml:145` propagates the RAW value `KEEL_DEVBOX_SSH: ${KEEL_DEVBOX_SSH:-false}` (verbatim, NOT the resolver's exported `KEEL_DEVBOX_SSH_RESOLVED`). Container-side `entrypoint.sh:137` compares case-sensitively `[[ "${KEEL_DEVBOX_SSH:-false}" == "true" ]]`. Operator with `KEEL_DEVBOX_SSH=True` in `.envrc`: port 2222 published, no sshd starts inside container → `Connection refused` with no log signal (no spawn occurred → liveness check never runs → no diagnostic at all). **Fix:** `docker-compose.yml:145` becomes `KEEL_DEVBOX_SSH: ${KEEL_DEVBOX_SSH_RESOLVED:-false}` — propagates the canonical normalised value the resolver exports unconditionally. The resolver runs in the parent shell of every host wrapper before `docker compose` invocation (per resolver header L96-99), so `KEEL_DEVBOX_SSH_RESOLVED` is always set in the env compose interpolates. Update entrypoint comment block at `:132-136` to point at `KEEL_DEVBOX_SSH_RESOLVED` propagation instead of "honour the container-side env var" framing. One-line code change + comment update.

- [x] [Review][Patch] **`set -e` halts entrypoint on opt-in SSH init failures — contradicts SC-10 non-fatal posture** [`packages/devbox/entrypoint.sh:138-176`] — _LANDED iter-274 (PATCH-3, commit `0d83fae`); SC-10 entrypoint-non-halt posture confirmed BUT iter-277 CR surfaced PATCH-6: the `( ... ) || diag` form's `set -e` propagation claim is empirically broken — see iter-277 § Review Findings sub-section below._ — _Source: Blind Hunter HIGH + Edge Case Hunter MEDIUM + Acceptance Auditor MEDIUM convergent._ SC-10 + entrypoint comment lines 165-168 + 175 frame the SSH init as "non-fatal — sshd startup failure does NOT block container start". But ONLY the post-spawn liveness check (lines 172-176) is wrapped non-fatal. Pre-spawn failures — `gosu dev mkdir` (`:141`), `chmod 0700` (`:146`; see PATCH-1), `ssh-keygen` (`:155-156`), `mv -T` (`:157`; see PATCH-4) — all halt under `set -euo pipefail` BEFORE the `exec gosu dev "$@"` handoff at `:202-204`. Container fails to start at all (not just "sshd failed to start"); operator sees `docker logs` = "container exited 1" with no SC-10 diagnostic, no remediation pointer, and no fall-through to the Story 2.3 egress init or the `sleep infinity` keepalive. **Fix:** wrap the entire opt-in block (lines 138-177) in a subshell with `|| true` + diagnostic emit on failure, OR `set +e` ... `set -e` bracketing with explicit error capture. Pattern (option A — preferred for minimal diff):
```
if [[ "${KEEL_DEVBOX_SSH_RESOLVED:-false}" == "true" ]]; then
  if ! (
    gosu dev mkdir -p /home/dev/.ssh/host_keys
    gosu dev chmod 0700 /home/dev/.ssh /home/dev/.ssh/host_keys
    if [[ ! -f /home/dev/.ssh/host_keys/ssh_host_ed25519_key ]] \
       || [[ ! -f /home/dev/.ssh/host_keys/ssh_host_rsa_key ]]; then
      gosu dev rm -rf /home/dev/.ssh/host_keys.tmp /home/dev/.ssh/host_keys
      gosu dev mkdir -p /home/dev/.ssh/host_keys.tmp
      gosu dev ssh-keygen -q -t ed25519 -f /home/dev/.ssh/host_keys.tmp/ssh_host_ed25519_key -N "" < /dev/null
      gosu dev ssh-keygen -q -t rsa -b 4096 -f /home/dev/.ssh/host_keys.tmp/ssh_host_rsa_key -N "" < /dev/null
      gosu dev mv -T /home/dev/.ssh/host_keys.tmp /home/dev/.ssh/host_keys
    fi
    [[ -f /home/dev/.ssh/authorized_keys ]] || gosu dev touch /home/dev/.ssh/authorized_keys
    gosu dev chmod 0600 /home/dev/.ssh/authorized_keys
    gosu dev /usr/sbin/sshd -D -e 2>>/var/log/sshd.log &
    SSHD_PID="$!"
    sleep 0.5
    kill -0 "${SSHD_PID}" 2>/dev/null
  ); then
    echo "entrypoint: SSH init failed (NON-FATAL per SC-10); container proceeds without sshd. Operator: pnpm devbox:logs keel-devbox + investigate." >&2
    tail -n 20 /var/log/sshd.log >&2 2>/dev/null || true
  fi
fi
```
This composes cleanly with PATCH-1 (gosu chmod), PATCH-2 (`KEEL_DEVBOX_SSH_RESOLVED` switch — included above), and PATCH-4 (rm-then-mv replacement — included above). Remove the inline `if ! kill -0 ...` block at `:172-176` — the subshell's exit status now captures the same signal.

- [x] [Review][Patch] **`mv -T host_keys.tmp host_keys` fails when destination dir is non-empty (partial-keypair recovery scenario)** [`packages/devbox/entrypoint.sh:141`, `:157`] — _LANDED iter-275 (PATCH-4, commit `820591a`); confirmed iter-277 CR re-run BUT iter-277 surfaced PATCH-6 cascade — under broken-`set -e` PATCH-3 the `rm -rf host_keys` becomes destructively unconditional._ — _Source: Blind Hunter HIGH + Edge Case Hunter MEDIUM + Acceptance Auditor MEDIUM convergent._ Line 141 always pre-creates `host_keys/` (empty on first boot). GNU coreutils `mv -T <srcdir> <dstdir>` with the `--no-target-directory` semantic SUCCEEDS when destination is non-existent or empty (atomic via `rename(2)`), but FAILS with "Directory not empty" when destination is non-empty. Recovery scenario: prior boot wrote ed25519 directly into final `host_keys/` (legacy or hand-recovery state) → existence guard at lines 151-152 fires `if either is missing` → keygen into `host_keys.tmp/` → `mv -T host_keys.tmp host_keys` fails because `host_keys/` contains the stale ed25519 → set -e abort. The "atomic recovery" the comment lines 147-150 promises is silently broken in exactly the partial-corruption mode it claims to handle. **Fix:** replace `:157` with `gosu dev rm -rf /home/dev/.ssh/host_keys && gosu dev mv -T /home/dev/.ssh/host_keys.tmp /home/dev/.ssh/host_keys` — atomically destroy the stale dir before atomic mv. Or fold into PATCH-3's subshell rewrite (`gosu dev rm -rf /home/dev/.ssh/host_keys.tmp /home/dev/.ssh/host_keys` upfront). Two-line change in isolation.

- [x] [Review][Patch] **`${VAR:+-f "${VAR}"}` paths-with-spaces hazard — refactor 15 sites to bash array idiom** [`packages/devbox/scripts/{build.sh, rebuild.sh, start.sh, stop.sh, status.sh, clean.sh, logs.sh, benchmark.sh}` 15 call sites] — _LANDED iter-276 (PATCH-5, commit `43b4e4b`); helper `lib/compose-args.sh` + 15-site refactor confirmed iter-277 CR re-run (zero inline `${KEEL_DEVBOX_COMPOSE_FILE_SSH:+-f` remain in scripts/*.sh; all 8 callers source helper after `resolve_ssh_state`; manifest contentHash sync-gate GREEN at `946f1ac1…907029`)._ — _Source: Blind Hunter MEDIUM + Edge Case Hunter HIGH convergent on the explicit DEFER-6 mis-mitigation._ The wired idiom `${KEEL_DEVBOX_COMPOSE_FILE_SSH:+-f "${KEEL_DEVBOX_COMPOSE_FILE_SSH}"}` sits unquoted at the outer level (intentional — empty-string elision). Bash's `${VAR:+...}` alternate-value expansion produces the substitution result as a STRING which is then re-tokenised by word-splitting BEFORE the inner double-quote characters are re-parsed (which become literal `"` characters post-expansion). Verified semantics: `foo="hello world"; printf '%s\n' ${VAR:+-f "${foo}"}` → `-f`, `hello`, `world` (three separate tokens). Forks cloned under paths-with-whitespace (`~/My Code/...`, `/Users/Some User/projects/ralph-bmad`) get `docker compose -f` `"/Users/Some` `User/.../docker-compose.ssh.yml"` as separate argv → "no such file or directory" failure. Story 2.12 Dev Notes citation of RALPH.md 2026-04-21 AR-9 is misapplied — that lesson was about `for x in $(...)` list-iterables word-splitting on IFS, NOT about embedded-quote-under-unquoted-`${VAR:+...}`-expansion. The cited mitigation does NOT cover this hazard. **Fix:** refactor 15 sites to bash array pattern. Canonical helper extraction (preferred — avoids 15-site repetition):
```
# packages/devbox/scripts/lib/compose-args.sh (new)
compose_args_with_ssh() {
  local -n out_arr="$1"
  local base_file="$2"
  out_arr=(-f "$base_file")
  [[ -n "${KEEL_DEVBOX_COMPOSE_FILE_SSH:-}" ]] && out_arr+=(-f "${KEEL_DEVBOX_COMPOSE_FILE_SSH}")
}
```
Caller idiom (replaces `docker compose -f "${COMPOSE_FILE}" ${KEEL_DEVBOX_COMPOSE_FILE_SSH:+-f "${KEEL_DEVBOX_COMPOSE_FILE_SSH}"} <verb>`):
```
local compose_args
compose_args_with_ssh compose_args "${COMPOSE_FILE}"
docker compose "${compose_args[@]}" <verb>
```
Refactor scope: 8 files, 15 sites, +1 new lib file. Story 2.6 AR-19 + Story 2.7 SC-14 deferred a `_lib.sh` extraction until shim count crosses substrate-wide refactor threshold — this PATCH crosses that threshold for the compose-args concern specifically. Update Story 2.12 Dev Notes DEFER-6 to remove the misapplied AR-9 citation.

- [x] [Review][Defer] **PrivSep chroot may fail under `cap_drop: [ALL]` — depends on Ubuntu openssh-server build sandbox vs chroot mode** [`packages/devbox/sshd/sshd_config (full file)` + `packages/devbox/entrypoint.sh:169`] — deferred, empirical-verification-required class (Blind Hunter MEDIUM solo). OpenSSH 7.5+ removed the `UsePrivilegeSeparation` directive and made sandbox-mode the default (seccomp_filter, NOT chroot). Modern Ubuntu builds (24.04 LTS shipped at iter-123 image bake) should use sandbox mode → no CAP_SYS_CHROOT needed. If empirical first-live-operator-workstation test surfaces "fatal: chroot: Operation not permitted", remediate by adding `UsePrivilegeSeparation no` to sshd_config OR add CAP_SYS_CHROOT to compose `cap_add` (weakens Story 2.5 hardening posture; prefer the former). Operator-workstation-deferred per AGENTS.md § Container hardening live-smokes-deferred posture.

- [x] [Review][Defer] **SFTP subsystem enabled — sshd_config:30 widens "pubkey-only debug shell" surface to file-transfer** [`packages/devbox/sshd/sshd_config:30`] — deferred, design-intent-clarification class (Blind Hunter MEDIUM + Edge Case Hunter MEDIUM convergent). AC 3 spec text says "pubkey-only ssh into dev shell" — does not explicitly cover SFTP. Authenticated pubkey-holder can `sftp dev@127.0.0.1:2222` and bulk-transfer `/home/dev/.claude/.credentials.json` (Story 2.8 OAuth tokens) or `/workspace/.envrc.local` (secrets) without producing shell session logs. Operator-trust boundary: only operators authorised the pubkey into `/home/dev/.ssh/authorized_keys`, so impact is bounded by operator's own pubkey hygiene. Defer rationale: removing SFTP is a one-line config edit but changes the operator UX (no `scp <host> dev@...:/workspace/file` workflow); intentional-vs-accidental distinction belongs in Story 2.17 close-out polish OR a future Story 2.18+ hardening pass with explicit AC. SC-17 carry-forward.

- [x] [Review][Defer] **`KEEL_DEVBOX_SSH_PORT` undocumented + unvalidated** [`packages/devbox/docker-compose.ssh.yml:19` + `packages/devbox/.envrc.example`] — deferred, operator-edge knob (Blind Hunter LOW + Edge Case Hunter MEDIUM + Acceptance Auditor LOW convergent). Override interpolates `${KEEL_DEVBOX_SSH_PORT:-2222}` but the var is absent from `.envrc.example` (line 40 documents `KEEL_DEVBOX_SSH=false` only), env-check.sh REQUIRED_VARS, and operator README walkthroughs. Embedded colons or non-integer values mangle the publish-port spec (compose may silently rebind to `0.0.0.0` or fail loudly at `up` time). Privileged-port escape (`KEEL_DEVBOX_SSH_PORT=22`) requires Docker daemon root which it has → silently shadows host sshd. SC-17 close-out doc-precision class. Operators discover the knob by reading the override file. Add `.envrc.example` line + env-check.sh REQUIRED_VARS entry + validation in resolver at Story 2.17.

- [x] [Review][Defer] **Machine-enforced loopback-bound port check NOT wired — SC-1 declares "machine-enforced" but only contentHash binds the prose claim** [`packages/keel-invariants/src/check-*.ts` (no entry exists)] — deferred per Story 2.12 § Scope carve-out (Blind Hunter MEDIUM + Acceptance Auditor MEDIUM convergent). Story 2.17 / `packages/keel-invariants/src/check-devbox-compose-shape.ts` explicitly owns this delivery per Story 2.5 § Container hardening AGENTS.md note. Iter-270 SM verified contentHash protects `docs/invariants/devbox-ssh.md` prose only; YAML compliance is honour-system at 1.0. No new defer surface — Story 2.17 carry-forward already pinned at iter-263 deferred-work.md.

- [x] [Review][Defer] **AC4 / invariant doc claim about `userland-proxy=false` overstates IPv6 confinement** [`docs/invariants/devbox-ssh.md § External connection refusal` + `AGENTS.md § Container-side ListenAddress`] — deferred, doc-precision class (Edge Case Hunter MEDIUM solo). Docker installs IPv4 DNAT scoped to `127.0.0.1` under `userland-proxy=false`, but IPv6 paths governed by separate `ip6tables` daemon flag (default disabled until ~Docker 26.x). Operator with LAN-IPv6 connectivity and Docker daemon `ip6tables=false` may reach `[::]:2222` on the host's IPv6 stack. Invariant doc claim "BOTH modes refuse LAN-sourced traffic" is conditional on daemon-config combinations not enumerated. Doc precision; defer to Story 2.17 invariant-doc polish pass — extend § External connection refusal to enumerate the `(userland-proxy, ip6tables)` matrix and the operator-config gates each refusal posture depends on. Behavioural defence-in-depth (per-fork operator's loopback-only Docker daemon config is OS-default on Docker Desktop) bounds real-world impact.

- [x] [Review][Defer] **`pnpm devbox:clean --with-volumes` regenerates host keys → operator's `~/.ssh/known_hosts` mismatch — operator-lockout/revoke flow** [`packages/devbox/entrypoint.sh:151` + `packages/devbox/scripts/clean.sh`] — deferred, explicitly Story 2.17 scope per Story 2.12 § Scope carve-out (Blind Hunter MEDIUM + Edge Case Hunter MEDIUM + Acceptance Auditor LOW convergent). Operator runs clean --with-volumes (any reason: disk reclamation, troubleshooting, mode flip) → host keys regenerate on next opt-in → `ssh -p 2222 dev@127.0.0.1` errors with "REMOTE HOST IDENTIFICATION HAS CHANGED" → manual `ssh-keygen -R '[127.0.0.1]:2222'` recovery. Story 2.17 owns operator-lockout/revoke flow + recovery doc. No PATCH at Story 2.12.

- [x] [Review][Defer] **`StrictModes` parent-dir `/home/dev` perms not chmod-enforced — silent auth refuse on backup-restored named volume** [`packages/devbox/entrypoint.sh:146`] — deferred, operator-edge class (Edge Case Hunter MEDIUM solo). Idempotent `chmod 0700` covers `.ssh` and `.ssh/host_keys` but not `/home/dev` itself. If named volume restored from rsync backup with `/home/dev` mode 0755 (or stricter on group/other write), sshd's StrictModes silently rejects auth with cryptic `/var/log/sshd.log` entry. Operator-edge: requires operator to restore named volume from non-Docker-init source. Dockerfile `chmod 0755 /home/dev /home/dev/.claude /home/dev/.config /home/dev/.config/gh` (line 254) sets initial state cleanly — only operator-restore paths get the surprise. Defer to Story 2.17; consider adding `/home/dev` to chmod block OR document operator-restore prerequisite.

- [x] [Review][Defer] **`/etc/ssh/ssh_host_*_key` baked-but-unused by `openssh-server` postinst — fingerprinting surface if config drifts** [`packages/devbox/Dockerfile:66`] — deferred, image-tidy class (Blind Hunter LOW + Edge Case Hunter LOW convergent). Ubuntu's openssh-server postinst generates `/etc/ssh/ssh_host_{rsa,ecdsa,ed25519}_key` at apt install time → baked into image layer, identical across every fork's build. sshd_config doesn't reference these paths (HostKey points at `/home/dev/.ssh/host_keys/...`), so they're unused bytes. If sshd_config ever reverts/amends to default paths (regression or mistaken edit), every fork's sshd would present the same fingerprint → MITM-vector / cross-fork key-reuse. No Dockerfile cleanup. Defer to Story 2.17 image-tidy pass: add `RUN rm -f /etc/ssh/ssh_host_*_key` after openssh-server install.

- [x] [Review][Defer] **No `MaxAuthTries` / `LoginGraceTime` / `MaxStartups` overrides + `AllowUsers dev` lacks source-IP** [`packages/devbox/sshd/sshd_config (full file)`] — deferred, hardening-future class (Blind Hunter LOW + Edge Case Hunter LOW convergent). sshd defaults: 6 / 120s / 10:30:100; `AllowUsers dev` permits any source IP that traverses publish gate. Pubkey-only + loopback-bound mitigates brute-force impact; defence-in-depth would specify `AllowUsers dev@127.0.0.1` to refuse if Docker's host-side confinement lapses (AC4 ip6tables edge from prior DEFER). Design choice was single-layer host-side confinement per sshd_config L13-16 comment; defer to future hardening story (post-Epic 2; or Story 2.18+ if added).

- [x] [Review][Defer] **Mode-flip orphan warning missing — no parallel to Story 2.11 `KEEL_DEVBOX_SHARED` env-check warning** [`packages/devbox/scripts/env-check.sh + start.sh`] — deferred, UX-parity class (Edge Case Hunter MEDIUM solo). Operator started container with `KEEL_DEVBOX_SSH=true`, then flips `.envrc` to false without restart, then runs `pnpm devbox:status` / `stop` / `clean` — env-check has no analog warning to Story 2.11's mid-flip orphan-container probe. Story 2.12 § Mode-flip discipline pins "container teardown required" but no machine-enforced reminder. Defer to Story 2.17 close-out: add SSH-mode-flip probe + warning-only stderr emit (mirrors Story 2.11 pattern; orthogonal to SHARED-mode probe).

- [x] [Review][Defer] **`docker exec` shims (shell, attach, claude-host, gh-auth-host, ralph-build/plan-host, whitelist-host, monitor-host) bypass SSH override entirely — latent regression-trap** [`packages/devbox/scripts/{shell,attach,claude-host,gh-auth-host,ralph-build-host,ralph-plan-host,whitelist-host,monitor-host}.sh`] — deferred, latent-regression class (Edge Case Hunter LOW solo). These shims `docker exec` against a presumed-running container; current behaviour is loud-fail when container missing. They do NOT call `resolve_ssh_state` and do NOT thread the SSH compose override. If a future story adds "auto-start if missing" to any of these shims, the absent SSH override would silently start a no-SSH container despite `KEEL_DEVBOX_SSH=true`. No correctness regression at 1.0; defer to Story 2.17 close-out audit OR Epic 3+ Ralph TUI auto-start path.

- [x] [Review][Defer] **Various LOW operator-edges — `UsePAM no` skips per-user limits; `chmod` follows symlinks; `KEEL_DEVBOX_COMPOSE_FILE_SSH` empty-export ambiguity; tail-on-missing-file silent; container-side `Port 2222` hardcoded; `AcceptEnv LANG LC_*`; log rotation; ssh-keygen `-q` no-stderr-capture; resolver `pwd -P` worktree-symlink edge; `KEEL_DEVBOX_REPO_ROOT` not honoured by resolver** [substrate-wide LOW findings] — deferred en-masse, SC-17 close-out polish class (Edge Case Hunter LOW + Blind Hunter LOW). Cumulative ~10 narrow operator-edge findings; none triggers in default operator config; each fix is 1-3 lines but their dispatch density is higher than benefit at this story scope. Story 2.17 close-out reconciliation pass sweeps cumulative DEFER queue across Epic 2; Story 2.18+ may absorb hardening-class items. iter-264 LESSON severity-class-first rule applies: LOW second-class operator-edges DEFER by default unless drain-introduced regression OR load-bearing safety defect.

**Dismissed (~10):** AGENTS.md H3 nesting before `## Ralph loop` H2 [MECH-DRIFT-ALREADY-TRIAGED iter-270 SM precedent]; manifest description ~1KB-vs-siblings-one-liner [non-load-bearing metadata; no functional impact]; Dockerfile `chmod 0644` + `chown root:root` after COPY are no-ops [single RUN layer; harmless]; `LogLevel INFO` + `SyslogFacility AUTH` dead-code under `-e` flag [stylistic, not bug]; `mv -T` GNU-extension portability concern [Debian/Ubuntu base; non-issue]; AGENTS.md citation drift on `INV-devbox-ssh` anchor [precedent-consistent with Story 2.11; MECH-DRIFT-ALREADY-TRIAGED]; iter-270 SM-already-triaged 8 mechanical line-shift drifts [pre-triaged, not new]; trace artifacts "ACs 1-3 substrate-verified but smokes not committed" [process-class, by design WAIVED at iter-269]; `pnpm devbox:rebuild` regenerates keys claim [contradicted by named-volume persistence — false alarm]; sshd_config `Subsystem sftp` discoverability concern [duplicate of D2 SFTP defer].

**Convergence signal:** all 5 PATCHes are HIGH/MEDIUM convergent across ≥2 reviewer layers (chmod CAP_FOWNER: Auditor solo HIGH but explicitly verified against Dockerfile L261-262 author note + capabilities(7) spec; KEEL_DEVBOX_SSH propagation: Blind+Edge+Auditor 3-of-3 convergent; set -e halt: Blind+Edge+Auditor 3-of-3 convergent; mv -T recovery: Blind+Edge+Auditor 3-of-3 convergent; ${VAR:+...} paths-with-spaces: Blind+Edge 2-of-3 convergent on idiom mis-mitigation). Iter-260 LESSON convergence-signal-high-confidence-triage applies. Forecast band breach (5 PATCH vs 0-3 anticipated) is REAL substrate gap signal, not over-fitting — these are dynamic-runtime defects iter-270 SM static smokes had no path to catch.

**Story State transition:** `sm-verified → fixes-pending` (5 PATCH queued at TOP of QUEUE). Sprint-status row UNCHANGED at `review` (per iter-202 + iter-260 precedent: only CR closure `review → done` flips sprint rows; interim CR states keep `review`). Next iter executes PATCH-1 (chmod under cap_drop) as its sole NOW task per Guardrail 5 + § Story Lifecycle Decision Matrix row `fixes-pending → fixes-pending` until QUEUE empties, then re-run `/bmad-code-review (args: "2")` for `fixes-pending → done | fixes-pending` confirmation gate.

#### iter-277 confirmation-gate re-run (`fixes-pending → fixes-pending`)

Three-layer Ralph-hosted adversarial fan-out: Blind Hunter (`general-purpose`, diff-only — 12 raw, 3H/3M/6L) + Edge Case Hunter (`general-purpose`, diff+project-read — 7 raw, 2H/1M/4L) + Acceptance Auditor (`bmad-agent-architect` Winston, diff+spec+context — 2 LOW, ZERO-PATCH verdict). Diff scope: PATCH-only commits `30b7d8d~1..HEAD` (15 files, 476 lines — narrowed to substrate + invariant doc + manifest, excluded process artifacts). **Triage: 1 PATCH (PATCH-6) + 9 DEFER + 3 DISMISS.**

**Critical convergence**: Blind Hunter HIGH + Edge Case Hunter HIGH (both layers) reproduced empirically by Ralph in-iteration with `bash 5.2.21`: the `( ... ) || diagnostic` form **DISABLES `set -e` inside the subshell** per the bash(1) `||`-LHS recursive rule (`set -euo pipefail; ( false; echo X ) || echo Y` outputs `X`, NOT `Y`). PATCH-3's stated mechanism — "subshell scopes `set -e` so a non-zero inner exit is swallowed by `|| <diagnostic>`" (entrypoint.sh:147-150) — is materially false. Even explicit `set -e` inside the subshell, `set -e` inside `if ! ( ... )`, and `shopt -s inherit_errexit` ALL reproduce the same suppression in this bash version. Only `bash -c '...' || diag` (separate subprocess) and `set +e; (set -e; ...); rc=$?; set -e; [[ $rc -ne 0 ]] && diag` patterns actually propagate errexit. Winston's ZERO-PATCH verdict was based on the (incorrect) assumption that `set -e` propagates inside the subshell — empirical disconfirmation overrides the verdict.

- [x] [Review][Patch] **PATCH-6 (HIGH; PATCH-3 mechanism + PATCH-4 cascade): `( ... ) || diag` does NOT scope `set -e` inside subshell — pre-spawn failures cascade silently and PATCH-4's destructive `rm -rf host_keys` runs even when keygen fails** [`packages/devbox/entrypoint.sh:151-210`] — _LANDED iter-278 (rc-capture rewrite `set +e; (set -e; ...); ssh_init_status=$?; set -e; [[ $ssh_init_status -ne 0 ]] && diag` per Fix option A; comment block at L141-170 rewritten to describe actual bash(1) `||`-LHS recursive-suppression mechanism + empirical reproduction reference + PATCH-3 historical note; subshell body absorbed the iter-277 DEFER cosmetic indent drift — body re-indented +2 inside `(`; in-iter happy-path + failure-path bash simulation both confirm correct semantics; bash -n syntax GREEN; entrypoint.sh NOT contentHash-tracked so no sync-gate recompute needed)._ — _Source: Blind Hunter HIGH x2 + Edge Case Hunter HIGH x2 convergent._ Bash 5.2 (project version) `set -euo pipefail; ( false; echo "after" ) || echo "diag"` outputs `after`, NOT `diag` — the bash(1) `||`-LHS rule disables errexit inside the subshell. Comment block at L141-150 LIES: pre-spawn failures (gosu dev mkdir EPERM, gosu dev chmod EPERM under unhealthy substrate, gosu dev ssh-keygen failure under entropy starvation) do NOT abort the subshell. Subsequent commands (including PATCH-4's `gosu dev rm -rf /home/dev/.ssh/host_keys` at L186) run unconditionally. Cascade: keygen-into-tmp fails silently → tmp/ empty or partial → `rm -rf host_keys` destroys the existing host-keys directory → `mv -T host_keys.tmp host_keys` may move empty/partial dir → sshd starts, dies (no host keys), inner `if ! kill -0` liveness check emits POST-spawn diagnostic → subshell exits 0 (the `if/then` returns 0 with no else) → outer `||` PRE-spawn diagnostic NEVER fires. Operator sees only the misleading post-spawn diagnostic; on retry, host keys are permanently destroyed; container stuck in no-host-keys state until `pnpm devbox:clean --with-volumes` (which also wipes Claude/gh tokens). Real-world impact: HAPPY-path (healthy substrate) works correctly — bug is silent; DEGENERATE-path (entropy starvation, named-volume corruption, gosu binary missing) cascades destructively. Story 2.13 healthcheck (next story) would surface sshd-not-listening but not the destructive-rm root cause.

  **Empirical reproduction (run in iter-277 by Ralph):**
  ```
  $ bash -c 'set -euo pipefail; ( false; echo "AFTER-FALSE" ) || echo "DIAG"; echo "OK"'
  AFTER-FALSE
  OK                                  # ← DIAG never fires; AFTER-FALSE prints

  $ bash -c 'set -euo pipefail; ( set -e; false; echo "X" ) || echo "DIAG"'
  X                                   # ← explicit `set -e` inside ALSO suppressed

  $ bash -c 'set -euo pipefail; bash -c "set -euo pipefail; false; echo X" || echo "DIAG"'
  DIAG                                # ← bash -c subprocess WORKS
  ```

  **Fix (option A — preferred; minimal diff):** wrap the subshell body explicitly:
  ```
  set +e
  (
    set -e
    gosu dev mkdir -p /home/dev/.ssh/host_keys
    gosu dev chmod 0700 /home/dev/.ssh /home/dev/.ssh/host_keys
    if [[ ! -f /home/dev/.ssh/host_keys/ssh_host_ed25519_key ]] \
       || [[ ! -f /home/dev/.ssh/host_keys/ssh_host_rsa_key ]]; then
      gosu dev rm -rf /home/dev/.ssh/host_keys.tmp
      gosu dev mkdir -p /home/dev/.ssh/host_keys.tmp
      gosu dev ssh-keygen -q -t ed25519 -f /home/dev/.ssh/host_keys.tmp/ssh_host_ed25519_key -N "" < /dev/null
      gosu dev ssh-keygen -q -t rsa -b 4096 -f /home/dev/.ssh/host_keys.tmp/ssh_host_rsa_key -N "" < /dev/null
      gosu dev rm -rf /home/dev/.ssh/host_keys
      gosu dev mv -T /home/dev/.ssh/host_keys.tmp /home/dev/.ssh/host_keys
    fi
    [[ -f /home/dev/.ssh/authorized_keys ]] || gosu dev touch /home/dev/.ssh/authorized_keys
    gosu dev chmod 0600 /home/dev/.ssh/authorized_keys
    gosu dev /usr/sbin/sshd -D -e 2>>/var/log/sshd.log &
    SSHD_PID="$!"
    sleep 0.5
    if ! kill -0 "${SSHD_PID}" 2>/dev/null; then
      echo "entrypoint: sshd failed to start; tail /var/log/sshd.log:" >&2
      tail -n 20 /var/log/sshd.log >&2 || true
      echo "entrypoint: sshd startup failure is NON-FATAL under Story 2.12 SC-10 (background process; PID 1 remains the exec handoff). Operator: pnpm devbox:logs keel-devbox + investigate." >&2
    fi
  )
  ssh_init_status=$?
  set -e
  if [[ ${ssh_init_status} -ne 0 ]]; then
    echo "entrypoint: sshd opt-in PRE-spawn setup failed (rc=${ssh_init_status}); inspect stderr above; proceeding without sshd. Operator: pnpm devbox:logs keel-devbox + investigate." >&2
  fi
  ```

  Key changes vs current iter-276 form: (a) `set +e` BEFORE the subshell (cancels parent errexit so the subshell exit non-zero doesn't abort entrypoint); (b) `set -e` as the FIRST command inside the subshell (re-enables errexit; works because the subshell is no longer in `||`-LHS context — there's a `;` separator to `ssh_init_status=$?` instead); (c) explicit `rc=$?` capture (no `||` token); (d) `set -e` restored in parent before downstream commands; (e) `if [[ ${ssh_init_status} -ne 0 ]]` then-emit diagnostic. Same SC-10 non-fatal posture but errexit ACTUALLY scoped this time. Update comment block at L141-150 to reflect the actual mechanism + reference the iter-277 PATCH-6 empirical reproduction.

  **Fix (option B — alternative): `bash -c '...'` subprocess.** Cleaner conceptually but requires careful single-quoting + env propagation (KEEL_DEVBOX_SSH already in env via PATCH-2 mapping). Preferred for green-field; option A preferred for minimal diff against the landed PATCH-3 shape.

  **Severity rationale:** HIGH because the comment block lies + because the cascade with PATCH-4's destructive rm is recoverable only via `pnpm devbox:clean --with-volumes` which wipes Claude/gh tokens; HAPPY-path masks the bug indefinitely. Real-world trigger probability LOW (requires substrate health degradation), but the cost when triggered is operator-visible and trust-eroding. Per iter-264 LESSON severity-class-first rule, HIGH carries unconditional PATCH; defer-eligibility does NOT apply.

- [x] [Review][Defer] **Cosmetic — entrypoint.sh subshell body not re-indented; visual scope drift on read** [`packages/devbox/entrypoint.sh:151-210`] — Blind Hunter LOW. _ABSORBED iter-278 under PATCH-6: the rc-capture rewrite re-touched the whole region and re-indented the subshell body +2 inside `(`. No longer needs Story 2.17 polish; class-closed at Story 2.12 landing._ Subshell body left at prior 2-space indent rather than indented +2 inside the `(`. Cosmetic only; defer to Story 2.17 close-out polish (PATCH-6 will re-touch this region anyway, may absorb).

- [x] [Review][Defer] **PATCH-4 comment line ref drift — claims `mkdir at L155` but L155 is `host_keys.tmp` mkdir, not `host_keys`** [`packages/devbox/entrypoint.sh:184`] — Blind Hunter LOW. _ABSORBED iter-278 under PATCH-6: the PATCH-4 comment block was rewritten as part of the rc-capture re-indent to point at "immediately above" instead of a brittle L155 line reference. Line-pin drift class pre-emptively addressed; class-closed at Story 2.12 landing._ Comment in PATCH-4 cites L155 as the host_keys mkdir; actually L155 is the host_keys.tmp mkdir (the host_keys mkdir is at L155 of the original block but post-PATCH-3 subshell the line numbers shift). Cosmetic; defer to Story 2.17 SC-17 line-pin polish pass (will re-touch under PATCH-6).

- [x] [Review][Defer] **`COMPOSE_FILE` not validated under `set -u` in helper** [`packages/devbox/scripts/lib/compose-args.sh:50`] — Blind Hunter MEDIUM + Edge Case Hunter LOW. Helper assumes caller set `COMPOSE_FILE`; verified all 8 current callers do, so future-guard only. Optional explicit guard `: "${COMPOSE_FILE:?resolve_compose_args: COMPOSE_FILE must be set by caller}"` would surface contract violations cleanly. Defer to Story 2.17.

- [x] [Review][Defer] **`COMPOSE_ARGS` is unscoped global — caller-collision future-guard** [`packages/devbox/scripts/lib/compose-args.sh:50-53`] — Blind Hunter MEDIUM. No current collision; namespace via `KEEL_DEVBOX_COMPOSE_ARGS` or `local -n` would prevent future caller-with-own-`COMPOSE_ARGS` clobber. Defer to Story 2.17.

- [x] [Review][Defer] **Helper does NOT validate `KEEL_DEVBOX_COMPOSE_FILE_SSH` non-empty value points at existing file** [`packages/devbox/scripts/lib/compose-args.sh:51-53`] — Edge Case Hunter LOW. Stripped-fork edge; `docker compose -f` errors loudly with "open: no such file or directory". Defer to Story 2.17 if defensive; resolver-side `[[ -f ... ]]` check could move ASSERT into `resolve_ssh_state` directly.

- [x] [Review][Defer] **PATCH-2 raw-compose flips behaviour for non-shim invocations — fail-safe but undocumented** [`packages/devbox/docker-compose.yml:149`] — Blind Hunter MEDIUM + Edge Case Hunter LOW. Operators bypassing the shim (raw `docker compose up`) get `KEEL_DEVBOX_SSH=false` always (since `KEEL_DEVBOX_SSH_RESOLVED` only gets set by `resolve_ssh_state()`). This is ACTUALLY fail-safe (no SSH unless shim path), but creates a documentation gap: `KEEL_DEVBOX_SSH=true docker compose up` won't enable SSH. Defer to Story 2.17 README docs polish — add operator-facing one-liner explaining the shim-only opt-in path.

- [x] [Review][Defer] **`docs/invariants/devbox-ssh.md` Compose-CLI example omits `COMPOSE_FILE=…` setup line** [`docs/invariants/devbox-ssh.md:113-119`] — Blind Hunter LOW. Example shows source + resolve calls but never sets `COMPOSE_FILE`. New readers copy-pasting will hit the unset-var failure. Defer to Story 2.17 doc polish (one-line addition).

- [x] [Review][Defer] **`devbox-hardening.md` cap_add doc drift — claims 3 caps, docker-compose.yml declares 5** [`docs/invariants/devbox-hardening.md:28, :77`] — Acceptance Auditor (Winston) LOW. Pre-existing drift; NOT introduced by Story 2.12 PATCHes. Doc says `cap_add: [NET_ADMIN, NET_RAW, NET_BIND_SERVICE]` but compose declares the 3 plus `SETUID, SETGID` (Story 2.5 enables gosu user-switching under no-new-privileges). Sync-gate doesn't catch because the doc's own contentHash matches itself. Defer to Story 2.17 invariant-doc reconciliation pass.

**Dismissed (3):**
1. **PATCH-3 backgrounded sshd dies when subshell exits** (Blind HIGH) — incorrect mental model. Backgrounded process via `&` becomes child of subshell, gets reparented to entrypoint PID 1 when subshell exits — survives. `SSHD_PID` only used inside subshell for kill -0 check, not parent-scope. Already invalid concern.
2. **PATCH-1 chmod target may not exist on first boot** (Blind MEDIUM — `chmod 0700 host_keys` before mkdir) — Blind Hunter misread the diff. Verified: `gosu dev mkdir -p /home/dev/.ssh/host_keys` runs at L155 BEFORE the `gosu dev chmod 0700 /home/dev/.ssh /home/dev/.ssh/host_keys` at L165. Order is correct.
3. **Manifest contentHash unverifiable** (Blind LOW) — Edge Case Hunter ran `sha256sum docs/invariants/devbox-ssh.md` and verified hash matches `946f1ac1b04bdde424df7ef46b1450b72481396cdeb8ab4038d85ae694907029` exactly. `pnpm keel-invariants:check` GREEN at iter-276. No drift.

**Convergence signal:** PATCH-6 is 4-of-4 convergent across the four findings (Blind #1+#2 Subshell + PATCH-4 race; Edge Hunter Findings #1+#2 set -e suppressed + destructive rm cascade) PLUS Ralph in-iter empirical reproduction with bash 5.2.21. Highest-confidence-PATCH-class. Iter-260 LESSON convergence-signal-high-confidence-triage applies even more strongly when convergence is supplemented by empirical disconfirmation of an Acceptance Auditor positive control.

**Forecast band reconciliation:** iter-271 forecasted "0-2 PATCH (CR-fix-introduced regression class)" at the re-run; PATCH-6 lands at the upper bound (1 PATCH). Per iter-264 LESSON Story 2.6/2.11 multi-pass cycle, budget TWO closure re-runs at worst case → iter-277 is closure re-run #1; PATCH-6 lands → iter-278 will be PATCH-6 sole NOW task; iter-279 closure re-run #2. Cumulative PATCH count for Story 2.12 = 5 (iter-271) + 1 (iter-277) = 6 — within the iter-271 LESSON 4-7 PATCH band for novel-runtime-behaviour stories.

**NEW LESSON (iter-277 — bash subshell `||`-LHS errexit suppression):** The bash(1) `||`-LHS rule that disables `set -e` for commands "part of any command executed in a `&&` or `||` list except the command following the final `&&` or `||`" applies RECURSIVELY into subshells. Empirically reproduced: `set -euo pipefail; ( false; echo X ) || echo Y` outputs `X`. Even explicit `set -e` inside the subshell does NOT escape the suppression. Even `if ! ( ... )` form has the SAME bug (the subshell is in the test of an `if !`, which is a `!` list — subject to the same suppression rule). Even `shopt -s inherit_errexit` does NOT help — that flag is for command substitution `$(...)`, not for `(...)` subshells. **The ONLY working forms are: (1) `bash -c '...' || diag` separate subprocess; (2) `set +e; (set -e; ...); rc=$?; set -e; [[ $rc -ne 0 ]] && diag` explicit rc-capture pattern.** Generalises: ANY future "wrap risky block in `(...) || diag` for non-fatal posture" pattern in any Story 2.13..2.17 substrate work needs the rc-capture pattern OR `bash -c` subprocess; the naive `(...) || diag` is broken. Promoted to RALPH.md § Lessons + § Signposts at iter-277. Carry-forward applies to: Story 2.13 (healthcheck non-fatal failure paths), Story 2.17 (bypass-resistance scripts), Epic 13 (CI harness smokes), Epic 6 (RLS debugger lifecycle scripts). The CR re-run discipline value: a multi-pass CR cycle with empirical-disconfirmation discipline can catch errors in PATCHes that all three reviewer layers AT-FIRST-INSPECTION accept (Winston ZERO-PATCH verdict was structurally unable to catch this without empirical bash-test execution — his analysis assumed the documented mechanism worked).

**Story State transition:** `fixes-pending → fixes-pending` (PATCH-6 queued at TOP of QUEUE; closure re-run #1 outcome was 1-PATCH, not 0-PATCH; one more closure re-run pending). Sprint-status row UNCHANGED at `review` (per iter-202 + iter-260 + iter-271 precedent). Next iter (iter-278) executes PATCH-6 (rewrite entrypoint subshell to set +e → set -e → rc-capture pattern OR bash -c subprocess) as sole NOW task per Guardrail 5; iter-279 re-runs `/bmad-code-review (args: "2")` for closure re-run #2 (`fixes-pending → done | fixes-pending`). Forecast iter-279: ZERO-PATCH likely (PATCH-6 is a single-region rewrite; CR-fix-introduced regression class typically converges at re-run #2 per iter-264 LESSON Story 2.6/2.11 precedent).

## Dev Notes

### Relevant architecture patterns and constraints

- **Substrate authority: `packages/devbox/`.** All Story 2.12 changes scoped to `packages/devbox/Dockerfile`, `packages/devbox/docker-compose.yml`, the new `packages/devbox/docker-compose.ssh.yml`, the new `packages/devbox/sshd/sshd_config`, `packages/devbox/entrypoint.sh`, `packages/devbox/scripts/lib/main-repo-resolver.sh`, and the 9 compose-invoking shims. Plus the new `docs/invariants/devbox-ssh.md` invariant doc + `INVARIANTS.md` anchor + manifest entry + operator docs at `packages/devbox/README.md` + `AGENTS.md` + `.envrc.example` comment update.
- **Compose-override pattern.** Conditional port publication uses Docker Compose v2's `-f` file-merge semantics (last-file-wins for scalars; APPEND for list-typed keys like `services.<svc>.ports`). Pinned in Story 2.6 architecture (the `${KEEL_DEVBOX_COMPOSE_FILES_ARGS}` env-var carrying the compose `-f` arg list). Docker Compose v2.20+ floor pinned at `packages/devbox/docker-compose.yml:78` (Story 2.1 substrate). The override file's `services.devbox.ports` array adds the port 2222 entry to the base file's existing ports array (which has 4 entries from Story 2.2).
- **`set -euo pipefail` discipline + resolver function shape.** Mirror Story 2.11 `resolve_mode_specific_state()` pattern: pure string manipulation (no command rc-dependence), default-substitution `${KEEL_DEVBOX_SSH:-false}` to survive `set -u`. Case-fold normalisation via `tr '[:upper:]' '[:lower:]'` (mirrors `lib/main-repo-resolver.sh:152` — the actual `tr` line inside `resolve_mode_specific_state()`; the function opener is at `:146` and the `if [[ "$shared" != "true" ]]` gate is at `:154`).
- **Entrypoint forbidden-runtime-install posture.** Story 2.1 AC 2 + entrypoint header lines 17-19 forbid `apt-get` / `npm install` / `pip install` / `curl…|sh` at runtime. Task 3's entrypoint extension only invokes `ssh-keygen` (binary baked at image-build time via `openssh-server` apt package) and `gosu dev /usr/sbin/sshd -D`; neither violates the posture.
- **`INV-devbox-homedev-named-volume` interaction.** Host keys + authorized_keys live INSIDE `keel_home_dev` (Story 2.5 substrate). Story 2.12 does NOT change the named-volume contract; it only adds new file-tree paths under `/home/dev/.ssh/`. Persistence semantics inherited unchanged.
- **`INV-devbox-mode` interaction (Story 2.11).** SSH state is per-CONTAINER, not per-MODE. Both per-fork mode (`keel-devbox` container) and shared mode (`keel-devbox-shared` container) honour `KEEL_DEVBOX_SSH` independently. The compose-files-args resolver output is the same in both modes; the per-mode container-name resolution composes orthogonally.
- **Defence-in-depth posture for AC 4.** Two layers refuse non-loopback connections: (1) Docker's iptables-NAT layer (`127.0.0.1:2222:2222` mapping); (2) sshd's own `ListenAddress 127.0.0.1` directive. If the operator's host firewall fails / Docker daemon misconfigures, sshd still refuses non-loopback inbound. If sshd_config is corrupted, Docker still refuses. Both layers must misbehave for an external IP to reach the dev shell.
- **No PRD amendment required.** PRD `:544` (Attach UX) + `:550` (Healthcheck) + `:551` (Ports) and architecture.md `:74` (Executive summary) + `:285-292` (.envrc block) + `:291` (KEEL_DEVBOX_SSH=false default) are the verbatim sources for the AC 1-5 contract. All sources are unchanged; Story 2.12 lands the substrate without amending planning artefacts.
- **Story 2.13 forward-compat.** Story 2.12's sshd-running-as-background-process leaves PID 1 free for the existing `sleep infinity` (or Epic 3's Ralph TUI). Story 2.13 will add a healthcheck that probes sshd liveness when `KEEL_DEVBOX_SSH=true`; Story 2.12 makes no healthcheck claim.

### Source tree components to touch

- `packages/devbox/Dockerfile` — apt-layer addition (`openssh-server`); `COPY packages/devbox/sshd/sshd_config`; pre-create `/home/dev/.ssh/host_keys/`.
- `packages/devbox/sshd/sshd_config` — NEW file (verbatim contents in Task 1).
- `packages/devbox/docker-compose.yml` — `environment:` block addition for `KEEL_DEVBOX_SSH`; remove TODO marker; Story 2.12 LANDED roadmap line.
- `packages/devbox/docker-compose.ssh.yml` — NEW file (conditional port-2222 override).
- `packages/devbox/entrypoint.sh` — first-boot host-key-gen + conditional sshd-launch block.
- `packages/devbox/scripts/lib/main-repo-resolver.sh` — new `resolve_ssh_state()` function + header doc-block extension.
- `packages/devbox/scripts/{start,stop,restart,build,rebuild,clean,logs,status,env-check}.sh` — 9 compose-invoking shims; substitute hardcoded `-f docker-compose.yml` with `${KEEL_DEVBOX_COMPOSE_FILES_ARGS}`.
- `packages/keel-invariants/src/invariants.manifest.ts` — new `INV-devbox-ssh` entry.
- `docs/invariants/devbox-ssh.md` — NEW authoritative contract doc.
- `INVARIANTS.md` — new `### Devbox SSH (Story 2.12)` H3 + one-line anchor bullet.
- `packages/devbox/README.md` — new `## Opt-in SSH (Story 2.12)` H2.
- `AGENTS.md` — new `### Opt-in SSH (Story 2.12)` H3.
- `packages/devbox/.envrc.example` — line 40 comment past-tense update.

### Testing standards summary

- **ATDD forecast: NON-SKIP for ACs 1, 2, 3.** Red-phase scaffolds candidate:
  - AC 1: smoke — `docker compose -f packages/devbox/docker-compose.yml config | grep -E '"published":\s*"127\.0\.0\.1:'` returns 4 lines (Story 2.2 ports); NO line missing the `127.0.0.1:` prefix; NO `0.0.0.0:` prefix.
  - AC 2: smoke — `KEEL_DEVBOX_SSH=false docker compose ${KEEL_DEVBOX_COMPOSE_FILES_ARGS} config | grep '2222'` returns NO matches.
  - AC 3: smoke — `KEEL_DEVBOX_SSH=true docker compose ${KEEL_DEVBOX_COMPOSE_FILES_ARGS} config | grep -E '127\.0\.0\.1:2222:2222'` returns ≥1 match.
  - AC 4 + AC 5: red-phase scaffold INFEASIBLE (live SSH connect requires real Docker daemon + SSH client + non-loopback peer for AC 4). Operator-workstation deferral (Story 2.5 iter-186 substrate-smoke posture). Trace-gate per-AC `defer:` candidate.
- **Trace-gate forecast: NON-WAIVED for ACs 1-3; partial-defer for ACs 4, 5.** Story 2.7 / 2.11 precedent for operator-workstation-only ACs.
- **CR forecast: 0-3 PATCH opener.** Novel surface (compose-override-file resolution, sshd entrypoint integration, host-key-bootstrap) is moderate. Precedents: Stories 2.1 (compose) + 2.5 (Dockerfile/entrypoint hardening) + 2.11 (resolver-function pattern) cover all implementation sites. Per iter-251/253 LESSON, adversarial CR catches what narrow-AC verification cannot; forecast 0-1 first-class PATCH + 2-4 second-class operator-edge DEFERs is band-aligned.
- **No harness infrastructure deferred to this story.** Resolver function + compose-config grep both testable via bash functional tests; entrypoint behaviour testable via `docker compose run --rm` smoke + `docker exec` probes (operator workstation).

### Success Criteria (SCs)

1. **Loopback-bound port publication is invariant.** Every `ports:` mapping (base compose + override + every future override) MUST use `127.0.0.1:<host>:<container>` form. Pinned in `INV-devbox-ssh § Loopback-bound port publication contract`. Future story edits adding ports MUST honour this — fork-level extension permitted, weakening forbidden.
2. **Opt-in sshd is fail-closed.** `KEEL_DEVBOX_SSH=true` is the opt-in signal; case-fold normalisation accepts `true`, `True`, `TRUE`, `TrUe` (i.e., any-case spelling of the literal `true`). EVERY other value — `false`, `yes`, `on`, `1`, empty, unset, `garbage` — defaults to NO sshd. Strict-true-only posture mirrors Story 2.11 `KEEL_DEVBOX_SHARED` normalisation idiom at `lib/main-repo-resolver.sh:152`; forks MAY NOT extend the accepted-signal set.
3. **Compose-override single-source.** `packages/devbox/docker-compose.ssh.yml` is the ONLY site that publishes port 2222. The base `packages/devbox/docker-compose.yml` MUST NOT have a 2222 entry in `ports:`. Lockstep enforced by code review + `INV-devbox-ssh § No-SSH default contract`.
4. **Resolver single-site discipline.** `resolve_ssh_state` is the ONLY site that decides SSH mode + composes the `-f` arg list. No shim re-computes mode independently; no inline `if [[ $KEEL_DEVBOX_SSH == true ]]` blocks outside the resolver and entrypoint.sh.
5. **Sshd runs as `dev` UID 1000.** No CAP_NET_BIND_SERVICE required for port 2222 (>1024). Sshd process visible in `ps -eo pid,user,cmd | grep sshd` as `dev sshd: /usr/sbin/sshd -D`. Honours Story 2.5 non-root posture.
6. **Pubkey-only authentication.** `PasswordAuthentication no` + `ChallengeResponseAuthentication no` + `KbdInteractiveAuthentication no` + `PubkeyAuthentication yes` in baked sshd_config. `PermitRootLogin no` + `AllowUsers dev` further constrain auth surface.
7. **Host keys persist via named volume.** Host keys at `/home/dev/.ssh/host_keys/ssh_host_ed25519_key` + `ssh_host_rsa_key` survive `pnpm devbox:stop && pnpm devbox:start` AND `pnpm devbox:rebuild` (image rebuild) because they live inside `keel_home_dev` named volume (Story 2.5 substrate).
8. **Authorized_keys flow operator-controlled.** Empty `authorized_keys` at first boot — no inbound SSH possible. Operator appends pubkeys from inside `pnpm devbox:shell`; pubkeys persist in named volume; survive restart + rebuild.
9. **No environment leakage.** `KEEL_DEVBOX_SSH` propagated via compose `environment:` (NOT `env_file:`-only). Without explicit `environment:` propagation, container sees empty env-var even though host's `.envrc` set it (compose's `${VAR}` interpolation runs on host at parse time).
10. **Entrypoint sshd is background.** `gosu dev /usr/sbin/sshd -D &` — backgrounded so PID 1 remains the `exec gosu dev "$@"` keepalive. PID 1 death tears down the container; sshd process death does NOT (Story 2.13 healthcheck adds detection).
11. **No new exit codes.** Story 2.6's uniform schema (0/2/3/8/9/10/11/12) + Story 2.10's additions + Story 2.11's no-additions apply unchanged. SSH start failure is logged to `/var/log/sshd.log` inside the container, not surfaced as a host-side exit code.
12. **Single-layer published-port confinement.** Loopback confinement is enforced by Docker's `127.0.0.1:2222:2222` publish in `docker-compose.ssh.yml` (host-side). Container-side sshd `ListenAddress 127.0.0.1` is INTENTIONALLY NOT set — container-loopback is disjoint from host-loopback, and binding there would silently drop all inbound traffic under both Docker `userland-proxy` modes. The container network namespace is an isolated attack surface; external peers cannot reach container eth0 without traversing the host-side `127.0.0.1:2222` publish. If the operator circumvents the publish (runtime `docker run --network host`; socat wrap; operator-edited compose), the attack surface widens to the operator's trust posture — this is an operator escape hatch, not a Story-2.12 regression.
13. **No PRD amendment.** PRD `:74` + `:551` + architecture `:550` + `:551` are unchanged. Story 2.12 is substrate-only.
14. **SC-17 close-out scope carry-forward.** Inherit from Stories 2.6/2.8/2.9/2.10/2.11: DO NOT modify prior stories' docs sections in README.md / AGENTS.md; append NEW sibling sections only. Rewriting prior sections is scope-creep for Epic 2 close-out polish (Story 2.17).

### Project Structure Notes

- **Alignment with unified project structure.** Story 2.12 lives entirely under `packages/devbox/` (Dockerfile + compose + sshd/ template + entrypoint + scripts/lib/) + `docs/invariants/devbox-ssh.md` + `INVARIANTS.md` + `packages/keel-invariants/src/invariants.manifest.ts` + `packages/devbox/README.md` + `AGENTS.md` + `packages/devbox/.envrc.example`. No cross-package leakage. Source tree follows architecture.md:975-999 (`packages/devbox/scripts` layout, `packages/keel-invariants/src/invariants.manifest.ts`).
- **No detected conflicts or variances.** The change set composes cleanly on Story 2.1 (compose / entrypoint surface) + Story 2.2 (KEEL_DEVBOX_SSH knob publication + four loopback-bound ports already established) + Story 2.3 (entrypoint egress-init pattern) + Story 2.5 (Dockerfile non-root + cap_drop + named volume) + Story 2.11 (resolver-function pattern + 18-shim wiring contract). The new `packages/devbox/sshd/` template directory mirrors the existing `packages/devbox/dnsmasq/` + `packages/devbox/nftables/` template-directory convention from Story 2.3.
- **`packages/devbox/sshd/` directory provenance.** NEW directory (Story 2.12 first occupant). Mirrors Story 2.3 pattern (`packages/devbox/dnsmasq/`, `packages/devbox/nftables/`) — config templates baked into the image at build time live under their own subdirectory. No README needed at 1.0; the file count is 1 (`sshd_config`) and the contract is documented in `docs/invariants/devbox-ssh.md`.

### References

- [Source: _bmad-output/planning-artifacts/architecture.md#74] Executive summary: "Host surface = `pnpm <subcommand>` only. Users never type `docker` / `docker-compose` / `ssh` directly. sshd is opt-in via `KEEL_DEVBOX_SSH=true` (pubkey-only, loopback-bound `127.0.0.1:2222`)."
- [Source: _bmad-output/planning-artifacts/architecture.md#291] `.envrc.example` snippet inside architecture.md § Devbox Implementation Contract: `KEEL_DEVBOX_SSH=false` default.
- [Source: _bmad-output/planning-artifacts/architecture.md#285-292] `.envrc` parameterisation block enumerating `KEEL_DEVBOX_SSH` + `KEEL_DEVBOX_SHARED` toggles verbatim.
- [Source: _bmad-output/planning-artifacts/prd.md#104] PRD Executive Summary — "users never invoke Docker, docker-compose, or SSH directly (sshd is an opt-in escape hatch via `KEEL_DEVBOX_SSH=true`, off by default)."
- [Source: _bmad-output/planning-artifacts/prd.md#544] PRD § Devbox Implementation Contract — Attach UX: "Opt-in sshd is enabled via `KEEL_DEVBOX_SSH=true` in `.envrc`; when enabled, sshd is pubkey-only (no password auth), host keys auto-generate on first boot, and the port is bound to loopback (`127.0.0.1:2222`)."
- [Source: _bmad-output/planning-artifacts/prd.md#550] Healthcheck contract: "Based on dnsmasq process liveness (and sshd liveness when `KEEL_DEVBOX_SSH=true`)" — Story 2.13 consumer; Story 2.12 prerequisite (sshd existence).
- [Source: _bmad-output/planning-artifacts/prd.md#551] Ports: "3000 (dev FE), 3001 (dev BE), 6006 (Storybook), 24679 (Vite HMR), all bound to `127.0.0.1` (not `0.0.0.0`) for host-only exposure. SSH port 2222 only exposed when `KEEL_DEVBOX_SSH=true`. Port list parameterisable via `.envrc`."
- [Source: _bmad-output/planning-artifacts/epics.md#1540-1572] Story 2.12 epic block: 5 acceptance criteria verbatim (loopback-bound ports + no-SSH default + opt-in sshd + external-refusal + authorized-keys flow).
- [Source: packages/devbox/.envrc.example#40] `KEEL_DEVBOX_SSH=false` default — Story 2.2 published the knob; Story 2.12 activates.
- [Source: packages/devbox/docker-compose.yml#22-24] Story-roadmap comment block — Task 4 inserts Story 2.12 LANDED line.
- [Source: packages/devbox/docker-compose.yml#135-136] `environment:` block — Task 4 extends with `KEEL_DEVBOX_SSH` propagation.
- [Source: packages/devbox/docker-compose.yml#150-157] Existing loopback-bound ports block (Story 2.2; AC 1 already satisfied).
- [Source: packages/devbox/docker-compose.yml#250] TODO marker for Story 2.12 — Task 4 removes.
- [Source: packages/devbox/Dockerfile#43-91] apt-get layer — Task 1 extends with `openssh-server`.
- [Source: packages/devbox/Dockerfile#65] Existing `openssh-client` package — Task 1 adds sibling `openssh-server`.
- [Source: packages/devbox/Dockerfile#252-253] `mkdir -p /workspace /home/dev/.claude /home/dev/.config/gh` — Task 1 extends with `/home/dev/.ssh/host_keys`.
- [Source: packages/devbox/entrypoint.sh#107-119] Named-volume directory bring-up loop — Task 3 inserts sshd-launch block AFTER this loop and AFTER the Story 2.3 egress-init block.
- [Source: packages/devbox/entrypoint.sh#121-130] Story 2.3 egress-init block (comment at :121; conditional dispatch at :125-130) — Task 3 inserts sshd block AFTER this block so egress policy is active during host-key-gen (any reverse-DNS or remote lookup sshd-keygen performs resolves through dnsmasq's whitelist).
- [Source: packages/devbox/entrypoint.sh#154-159] CMD-empty fallback + exec handoff (`:154` = `if [ "$#" -eq 0 ]; then`; `:155` + `:157` = the two `exec gosu dev ...` statements) — Task 3 block inserted BEFORE this if/else, not inside it.
- [Source: packages/devbox/scripts/lib/main-repo-resolver.sh#1-28] Original header doc-block (WORKTREE_ROOT / REPO_NAME / CONTAINER_WORKDIR) — DO NOT edit; Story 2.11 already extended via a SECOND doc paragraph.
- [Source: packages/devbox/scripts/lib/main-repo-resolver.sh#35-77] Story 2.11 Mode-specific-state doc paragraph — DO NOT edit; Task 2 appends a THIRD paragraph describing `resolve_ssh_state()` AFTER this one.
- [Source: packages/devbox/scripts/lib/main-repo-resolver.sh#146-203] `resolve_mode_specific_state()` function (opener at `:146`; `tr` case-fold at `:152`; gate at `:154`) — Task 2 adds sibling `resolve_ssh_state()` AFTER this function.
- [Source: packages/keel-invariants/src/invariants.manifest.ts#3-15] InvariantSchema five-field shape — Task 5 compliance.
- [Source: packages/keel-invariants/src/sync-gate.ts#24] Anchor regex `/^-\s+\*\*\`(INV-[a-z0-9]+(?:-[a-z0-9]+)+)\`\*\*/gm` — Task 5 INVARIANTS.md bullet compliance.
- [Source: docs/invariants/devbox-hardening.md (Story 2.5)] `INV-devbox-homedev-named-volume` authoritative doc; Story 2.12 doc cross-references for named-volume relationship (host keys + authorized_keys live INSIDE the volume).
- [Source: docs/invariants/devbox-mode.md (Story 2.11)] `INV-devbox-mode`; Story 2.12 SSH state is per-CONTAINER, not per-MODE; orthogonal composition documented in Story 2.12 invariant doc.
- [Source: docs/invariants/devbox-prereq-check.md (Story 2.10)] `INV-devbox-prereq-check`; Story 2.12 does NOT extend prereq-check — sshd is opt-in, never required.
- [Source: docs/invariants/fork.md § Amendment-vs-fork decision tree (Story 1.16)] Substrate default preservation; fork-level `INVARIANTS.fork.md` additive rules; Task 5 § Invariant stability.
- [Source: _bmad-output/implementation-artifacts/2-11-…md] Story 2.11 resolver-function pattern + 18-shim-wiring + 3-site-doc-lockstep — Story 2.12 reuses the resolver pattern (`resolve_ssh_state`) and the SC-17 sibling-append docs convention.
- [Source: _bmad-output/implementation-artifacts/2-5-…md] Story 2.5 Dockerfile + entrypoint hardening pattern — Story 2.12 entrypoint extension follows the chown-best-effort + cap_drop-aware posture.
- [Source: _bmad-output/implementation-artifacts/2-3-…md] Story 2.3 in-container template directory pattern (`packages/devbox/dnsmasq/`, `packages/devbox/nftables/`) — Story 2.12 mirrors with new `packages/devbox/sshd/`.

## Dev Agent Record

### Agent Model Used

claude-opus-4-7[1m] (Claude Opus 4.7, 1M-context)

### Debug Log References

- Resolver smoke (impl-time): `bash -c 'source packages/devbox/scripts/lib/main-repo-resolver.sh && KEEL_DEVBOX_SSH=true resolve_ssh_state && echo "$KEEL_DEVBOX_SSH_RESOLVED $KEEL_DEVBOX_COMPOSE_FILE_SSH"'` → `true /workspace/ralph-bmad/packages/devbox/docker-compose.ssh.yml`. With `KEEL_DEVBOX_SSH=false`/unset → `false` + empty string. Case-fold: `KEEL_DEVBOX_SSH=TrUe` → `true`. Fail-closed: `KEEL_DEVBOX_SSH=yes` → `false`. Strict-true-only normalisation verified (SC-2).
- Bash syntax check (`bash -n`) clean across 10 modified scripts: `entrypoint.sh`, `main-repo-resolver.sh`, `build.sh`, `rebuild.sh`, `start.sh`, `stop.sh`, `status.sh`, `logs.sh`, `clean.sh`, `benchmark.sh`.
- YAML parse clean (`js-yaml`) on both compose files: `docker-compose.yml`, `docker-compose.ssh.yml`.
- Shim wiring tally (`grep -c 'KEEL_DEVBOX_COMPOSE_FILE_SSH' packages/devbox/scripts/*.sh`): `build.sh:1 / rebuild.sh:1 / start.sh:1 / stop.sh:1 / status.sh:1 / logs.sh:2 / clean.sh:2 / benchmark.sh:6` → 15 call sites across 8 files, matching Task 2's verified inventory.
- Manifest sync-gate (`pnpm keel-invariants:check-all`) EXIT=0 — `INV-devbox-ssh` entry registered with contentHash `e1d693cb0ffa0c7c8d6966b8ce311c2e1daf13d83155d8a0b88028d96112a3c2` against `docs/invariants/devbox-ssh.md`; INVARIANTS.md anchor bullet regex match confirmed; tokens-sync also green. Manifest entry count 31 → 32.
- Live-container smokes (docker compose config port-form + pgrep sshd + docker port output + operator pubkey flow) operator-workstation-deferred: Docker not available in DinD iteration env (same posture as Story 2.5 iter-186; Story 2.11 iter-257). ACs 4-5 always operator-workstation-deferred by contract.

### Completion Notes List

- All 6 Tasks landed one-pass (no `in-dev (partial)` split). Dockerfile apt-layer adds `openssh-server`; baked `sshd_config` template at `packages/devbox/sshd/sshd_config`; `/home/dev/.ssh/host_keys/` pre-created mode 0700 (chown to dev:dev via the existing `chown -R dev:dev /home/dev` in the useradd block). `docker-compose.ssh.yml` compose override publishes `127.0.0.1:${KEEL_DEVBOX_SSH_PORT:-2222}:2222` (SINGLE site publishing port 2222 per SC-3). `resolve_ssh_state()` appended to `main-repo-resolver.sh` with strict-true-only normalisation (SC-2); computes override absolute path from the lib's own `BASH_SOURCE[0]` location (self-contained; no caller-set-DEVBOX_DIR dependency). Entrypoint's conditional block: gosu-dev mkdir + idempotent `chmod 0700` on `.ssh` + atomic keygen into scratch dir then `mv -T` into place (guards mid-keygen container kill) + touch-if-absent `authorized_keys` + backgrounded `gosu dev /usr/sbin/sshd -D -e 2>>/var/log/sshd.log &` + post-spawn `sleep 0.5 && kill -0 ${SSHD_PID}` liveness check with non-fatal stderr report on failure (preserves SC-10 background-process lifecycle). `KEEL_DEVBOX_SSH` propagated via compose `environment:` block (without this, entrypoint's `${KEEL_DEVBOX_SSH:-false}` reads the unset container env → no-SSH regardless of host `.envrc`).
- `INV-devbox-ssh` + `docs/invariants/devbox-ssh.md` cover the full contract across 8 H2 sections (Loopback-bound port publication / SSH signal / Opt-in sshd / No-SSH default / Resolver / External connection refusal / Operator authorized-keys flow / Named-volume-relationship + Mode-orthogonality + Invariant-stability). `INVARIANTS.md § Devbox SSH (Story 2.12)` H3 sibling-inserted between Story 2.11's `### Devbox mode` and Story 2.2's `### Gitignored-secret commit-deny` (devbox H3 sequence is non-numerical per iter-266 verification). Manifest entry count rises 31 → 32.
- Operator docs: `packages/devbox/README.md § Opt-in SSH (Story 2.12)` H2 sibling-appended AFTER `## Per-fork vs shared devbox mode (Story 2.11)` and BEFORE `## cc-devbox upstream provenance` (SC-17 sibling-append discipline). `AGENTS.md § Opt-in SSH (Story 2.12)` H3 sibling-appended AFTER `### Per-fork vs shared devbox mode (Story 2.11)` and BEFORE `## Ralph loop`. `.envrc.example:40` comment updated past-tense with LANDED iter-268 + resolver + override-file citations (SC-15).
- Intentional scope carve-outs preserved: no sshd healthcheck (Story 2.13 consumer); no operator lockout/revoke flow (deferred to Epic 2 close-out docs polish per Story 2.17); no `setcap +ep /usr/sbin/sshd` (port 2222 > 1024, no CAP_NET_BIND_SERVICE needed, Story 2.5 `no-new-privileges=1` posture preserved); container-side sshd `ListenAddress` INTENTIONALLY unset (container-loopback ≠ host-loopback under Docker userland-proxy — binding would silently drop AC 3 inbound; documented in sshd_config header + `INV-devbox-ssh § External connection refusal`).
- Strict-true-only normalisation (SC-2) chosen to mirror Story 2.11 `KEEL_DEVBOX_SHARED` idiom rather than extending to `yes`/`1`/`on`. Rationale: forks inheriting the substrate contract must not drift on accepted-signal semantics; substrate-wins-precedence makes drift a FR44 AMEND concern, not a FORK customisation.
- Substrate smokes only: live-container smokes (AC 4 non-loopback LAN peer, AC 5 full pubkey flow) operator-workstation-deferred per Story 2.5 iter-186 posture. DinD backend B cannot exercise either meaningfully; substrate verifies via `docker compose config` port-form assertion (ACs 1-3 at operator workstation) + manifest sync-gate + invariant-doc claim.

### File List

- `packages/devbox/Dockerfile` — MOD: add `openssh-server` to apt layer (line 65 sibling to `openssh-client`); pre-create `/home/dev/.ssh/` + `/home/dev/.ssh/host_keys/` in the existing mkdir block with mode 0700; `COPY sshd/sshd_config /etc/ssh/sshd_config` + chmod 0644 + chown root:root.
- `packages/devbox/sshd/sshd_config` — NEW: hardened sshd config (Port 2222, PermitRootLogin no, PasswordAuthentication no, ChallengeResponseAuthentication no, KbdInteractiveAuthentication no, PubkeyAuthentication yes, AllowUsers dev, HostKey paths in named volume, no ListenAddress).
- `packages/devbox/docker-compose.yml` — MOD: add `KEEL_DEVBOX_SSH: ${KEEL_DEVBOX_SSH:-false}` to `environment:` block; remove Story 2.12 TODO marker; add Story 2.12 LANDED line to roadmap comment block.
- `packages/devbox/docker-compose.ssh.yml` — NEW: compose override publishing `127.0.0.1:${KEEL_DEVBOX_SSH_PORT:-2222}:2222` for opt-in SSH.
- `packages/devbox/entrypoint.sh` — MOD: conditional block (gated on `${KEEL_DEVBOX_SSH:-false} == "true"`) inserted AFTER Story 2.3 egress-init block and BEFORE the CMD-empty fallback. First-boot atomic ed25519 + rsa 4096 host-key generation via scratch-dir + mv -T; idempotent `chmod 0700` on .ssh/host_keys; `authorized_keys` touch + chmod 0600; backgrounded `gosu dev /usr/sbin/sshd -D -e` with post-spawn liveness check + non-fatal stderr report.
- `packages/devbox/scripts/lib/main-repo-resolver.sh` — MOD: third doc paragraph describing `resolve_ssh_state()`; new `resolve_ssh_state()` function sibling to `resolve_mode_specific_state()`. Strict-true-only normalisation; exports `KEEL_DEVBOX_SSH_RESOLVED` + `KEEL_DEVBOX_COMPOSE_FILE_SSH`.
- `packages/devbox/scripts/build.sh` — MOD: add `resolve_ssh_state` call; thread `${KEEL_DEVBOX_COMPOSE_FILE_SSH:+-f "${KEEL_DEVBOX_COMPOSE_FILE_SSH}"}` into the 1 compose site.
- `packages/devbox/scripts/rebuild.sh` — MOD: same pattern as build.sh (1 site).
- `packages/devbox/scripts/start.sh` — MOD: same pattern (1 site).
- `packages/devbox/scripts/stop.sh` — MOD: same pattern (1 site).
- `packages/devbox/scripts/status.sh` — MOD: same pattern (1 site).
- `packages/devbox/scripts/logs.sh` — MOD: same pattern (2 sites — follow + passthrough).
- `packages/devbox/scripts/clean.sh` — MOD: same pattern (2 sites — preserve + destructive paths).
- `packages/devbox/scripts/benchmark.sh` — MOD: source `main-repo-resolver.sh` + call `resolve_ssh_state` (resolve_main_repo_and_workdir + resolve_mode_specific_state intentionally not called — benchmark doesn't invoke compose against a named project); thread override through 6 compose invocations (2 cold + 3 warm + 1 initial down).
- `packages/keel-invariants/src/invariants.manifest.ts` — MOD: add `INV-devbox-ssh` entry (contentHash `e1d693cb0ffa0c7c8d6966b8ce311c2e1daf13d83155d8a0b88028d96112a3c2`). Count 31 → 32.
- `docs/invariants/devbox-ssh.md` — NEW: authoritative contract doc, 8 H2 sections (Loopback-bound port publication / SSH signal / Opt-in sshd / No-SSH default / Resolver / External connection refusal / Operator authorized-keys / Named-volume-relationship + Mode-orthogonality + Invariant-stability).
- `INVARIANTS.md` — MOD: new `### Devbox SSH (Story 2.12)` H3 sibling-inserted between `### Devbox mode (Story 2.11)` and `### Gitignored-secret commit-deny (Story 2.2)`; anchor bullet matches Story 1.9 sync-gate regex.
- `packages/devbox/README.md` — MOD: new `## Opt-in SSH (Story 2.12)` H2 sibling-appended AFTER `## Per-fork vs shared devbox mode (Story 2.11)` and BEFORE `## cc-devbox upstream provenance`. Four subsections (.envrc snippet / Default no-SSH walkthrough / Opt-in sshd walkthrough / Authorized-keys flow walkthrough / Mode-flip note / External connection refusal / Cross-references).
- `AGENTS.md` — MOD: new `### Opt-in SSH (Story 2.12)` H3 sibling-appended AFTER `### Per-fork vs shared devbox mode (Story 2.11)` and BEFORE `## Ralph loop`. Agent-facing guardrails covering loopback-bound invariant, resolver idiom, compose-override single-site, token persistence, re-auth pointer, ListenAddress rationale, mode-flip discipline, no-CAP_NET_BIND_SERVICE note, scope carve-out.
- `packages/devbox/.envrc.example` — MOD: line 40 comment updated past-tense (LANDED iter-268 + resolver + override-file citations).
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — MOD: Story 2.12 row `ready-for-dev → in-progress → review` across iter-268; last_updated field updated.
- `_bmad-output/implementation-artifacts/2-12-loopback-bound-port-publication-opt-in-keel-devbox-ssh-sshd.md` — MOD: Status updated `ready-for-dev → review`; all Task checkboxes marked `[x]`; Dev Agent Record populated; File List authored; Change Log v1.2 entry added.

## Change Log

### v1.7 — iter-278 PATCH-6 LANDED (`fixes-pending` — closure re-run #2 pending at iter-279)

Single NOW task per Guardrail 5: rewrote `packages/devbox/entrypoint.sh:141-211` from the PATCH-3 `( ... ) || diagnostic` shape to the canonical rc-capture pattern per Fix option A in § Review Findings § iter-277 § PATCH-6. Changes:

1. **Mechanism** — `set +e` before the subshell, `set -e` as the first command inside, explicit `ssh_init_status=$?` capture (no `||` token), `set -e` restore in parent, `if [[ ${ssh_init_status} -ne 0 ]]` then-emit diagnostic. This removes the bash(1) `||`-LHS recursive rule that suppressed errexit inside the PATCH-3 subshell shape. Empirically verified in-iter: `bash -c 'set +e; (set -e; false; echo X); rc=$?; set -e; [[ $rc -ne 0 ]] && echo "DIAG rc=$rc"; echo OK'` outputs `DIAG rc=1` then `OK` (failure path) and happy path `true; echo X` outputs `X` then `OK rc=0` (happy path). Both paths correct.
2. **Comment block rewrite (L141-170)** — replaced the PATCH-3 comment block that claimed "the subshell scopes `set -e` so a non-zero inner exit is swallowed by `|| <diagnostic>` at close" (materially false per iter-277 empirical reproduction) with a new block that: (a) describes the actual rc-capture mechanism; (b) explains WHY `( ) || diag` doesn't work (bash(1) `||`-LHS recursive rule); (c) references the iter-277 spec § Review Findings § PATCH-6 empirical reproduction; (d) documents the PATCH-3 historical cascade (destructive rm -rf host_keys under keygen-failure).
3. **Subshell body absorbed iter-277 DEFERs:** (a) re-indented subshell body +2 inside `(` — closes "cosmetic visual scope drift" iter-277 DEFER (Blind Hunter LOW); (b) rewrote PATCH-4 comment to reference "immediately above" instead of brittle L155 line-number — closes "PATCH-4 comment line ref drift" iter-277 DEFER (Blind Hunter LOW). Both class-closed at Story 2.12 landing rather than Story 2.17 SC-17 polish.
4. **Static smokes GREEN.** `bash -n packages/devbox/entrypoint.sh` syntax-clean. `entrypoint.sh` is NOT contentHash-tracked in `invariants.manifest.ts` (only `docs/invariants/devbox-ssh.md` is, contentHash `946f1ac1…907029` unchanged — no edit); `pnpm keel-invariants:check` carried GREEN from iter-276. No sync-gate recompute needed. Live container exec verification operator-workstation-deferred per Story 2.5 iter-186 posture (DinD backend B cannot safely exercise the rewritten cap_drop:[ALL] path).
5. **Story State transition:** `fixes-pending → fixes-pending` (PATCH-6 LANDED; closure re-run #2 pending at iter-279). Sprint-status row UNCHANGED at `review`. Cumulative Story 2.12 PATCH count = 5 (iter-271) + 1 (iter-277) = 6 — within iter-271 LESSON 4-7 PATCH band for novel-runtime-behaviour stories.
6. **Budget consumed:** ~40K tokens (orient ~8K + spec read ~4K + entrypoint read ~3K + Edit x4 + empirical bash tests ~2K + IP + commit ~15K stacking). Well within ~117K execution budget; exit cleanly per Guardrail 12.

Next iter (iter-279): `/bmad-code-review (args: "2")` closure re-run #2 (`fixes-pending → done | fixes-pending`). Forecast ZERO-PATCH per iter-264 LESSON Story 2.6/2.11 multi-pass CR cycle (single-region rewrite typically converges at re-run #2). If iter-279 lands ZERO-PATCH, Story State → `done`, sprint-status row flips `review → done`, then QUEUE advances to Story 2.13.

### v1.6 — iter-277 `/bmad-code-review (args: "2")` confirmation re-run (`fixes-pending → fixes-pending`)

Three-layer Ralph-hosted adversarial fan-out (Blind Hunter `general-purpose` diff-only, 12 raw 3H/3M/6L; Edge Case Hunter `general-purpose` diff+project-read, 7 raw 2H/1M/4L; Acceptance Auditor `bmad-agent-architect` Winston diff+spec+context, 2 LOW + ZERO-PATCH verdict). Diff scope: PATCH-only commits `30b7d8d~1..HEAD` (15 files / 476 lines / 227 insertions / 97 deletions — narrowed to substrate code + invariant doc + manifest). **Triage: 1 PATCH (PATCH-6) + 9 DEFER + 3 DISMISS.**

**PATCH-6 (HIGH; entrypoint.sh:151-210)** — Bash 5.2 (project version) `( ... ) || diagnostic` form does NOT scope `set -e` inside the subshell per the bash(1) `||`-LHS recursive rule. Empirically reproduced in iter-277: `set -euo pipefail; ( false; echo X ) || echo Y` outputs `X`, NOT `Y`. PATCH-3's stated mechanism (entrypoint.sh:147-150 comment block) lies. Pre-spawn failures cascade silently; PATCH-4's `gosu dev rm -rf /home/dev/.ssh/host_keys` runs unconditionally, destroying host keys when keygen fails. Even explicit `set -e` inside subshell, `if ! ( ... )` form, and `shopt -s inherit_errexit` ALL reproduce the same suppression. ONLY `bash -c '...' || diag` subprocess and `set +e; (set -e; ...); rc=$?; set -e; [[ $rc -ne 0 ]] && diag` patterns actually propagate errexit. **Fix:** rewrite entrypoint.sh:151-210 with the explicit rc-capture pattern (canonical form in iter-277 Review Findings § PATCH-6) — preserves SC-10 non-fatal posture WHILE actually scoping errexit. Update L141-150 comment block to reflect the actual mechanism + empirical reproduction.

**Convergence:** Blind #1+#2 (subshell sshd survival concerns + diag swallow) + Edge Case Hunter Findings #1+#2 (set -e suppressed + destructive rm cascade) — 4-of-4 convergent across the two layers; supplemented by Ralph in-iter empirical disconfirmation of Winston's ZERO-PATCH positive control. Highest-confidence-PATCH class. Iter-260 LESSON convergence-signal-high-confidence-triage applies maximally when convergence is supplemented by empirical disconfirmation of a positive-control verdict.

**9 DEFER** logged in story `### Review Findings` § iter-277 + `_bmad-output/implementation-artifacts/deferred-work.md § Deferred from: code review of story-2.12 iter-277 confirmation gate (2026-04-24)`. Class breakdown: 8 → Story 2.17 close-out (cosmetic indent + line-pin polish + helper future-guards + helper file-existence check + raw-compose docs gap + devbox-ssh.md example COMPOSE_FILE setup + devbox-hardening.md cap_add doc-drift); 1 → Story 2.17 invariant-doc reconciliation pass.

**3 DISMISS:** PATCH-3 backgrounded sshd dies when subshell exits (incorrect mental model; sshd reparents to entrypoint PID 1); PATCH-1 chmod target may not exist on first boot (Blind Hunter misread; verified mkdir at L155 runs before chmod at L165); manifest contentHash unverifiable (Edge Case Hunter verified hash matches via sha256sum — no drift).

**NEW LESSON (iter-277 — bash subshell `||`-LHS errexit suppression is recursive into subshells AND into explicit `set -e` re-enable AND into `if !` form):** the bash(1) `||`-LHS rule that disables errexit "for any command executed in a `&&` or `||` list except the command following the final `&&` or `||`" applies RECURSIVELY into subshells. Empirically reproduced. ONLY `bash -c '...' || diag` (separate subprocess) and `set +e; (set -e; ...); rc=$?; set -e` rc-capture patterns escape the suppression. Generalises to ALL "wrap risky block in `(...) || diag` for non-fatal posture" patterns in any future Story 2.13..2.17 / Epic 6 / Epic 13 substrate work. Promoted to RALPH.md § Lessons + § Signposts at iter-277.

**Forecast band reconciliation:** iter-271 forecasted "0-2 PATCH (CR-fix-introduced regression class)" at the re-run; PATCH-6 lands at the upper bound (1 PATCH). Per iter-264 LESSON Story 2.6/2.11 multi-pass cycle, budget TWO closure re-runs at worst case → iter-277 is closure re-run #1; iter-279 closure re-run #2 forecast ZERO-PATCH (PATCH-6 is a single-region rewrite per iter-264 precedent). Cumulative Story 2.12 PATCH count = 5 (iter-271) + 1 (iter-277) = 6 — within iter-271 LESSON 4-7 PATCH band for novel-runtime-behaviour stories.

**Story State transition:** `fixes-pending → fixes-pending` (PATCH-6 queued at TOP of QUEUE; closure re-run #1 outcome was 1-PATCH not 0-PATCH; one more closure re-run pending). Sprint-status row UNCHANGED at `review` per iter-202 + iter-260 + iter-271 precedent. Next iter (iter-278): PATCH-6 sole NOW task per Guardrail 5. Iter-279: re-run `/bmad-code-review (args: "2")` for closure re-run #2.

### v1.5 — iter-271 `/bmad-code-review (args: "2")` (`sm-verified → fixes-pending`)

Three-layer Ralph-hosted adversarial fan-out (Blind Hunter `general-purpose` diff-only, 24 raw findings 3H/9M/12L; Edge Case Hunter `general-purpose` diff+project-read, 27 raw 1H/10M/16L; Acceptance Auditor `bmad-agent-architect` Winston diff+spec+context, 12 raw 2H/2M/8L). Diff range `77f1748..HEAD -- ':!.ralph/' ':!RALPH.md'` (Story 2.12 substrate scope) = 2161 lines / 26 files / 1788 insertions. Triage: **5 PATCH + 11 DEFER + ~10 DISMISS** (post-dedup/convergence). Forecast band breach (5 PATCH vs 0-3 anticipated per iter-264 LESSON + iter-270 SM ZERO-PATCH carry-forward) traces to iter-266 NOVEL LESSON's "novel-runtime-behaviour" surface — sshd-host-key bootstrap + cap_drop posture interaction + entrypoint set -e discipline — which static smokes (sha256 / bash -n / sync-gate / 15-site tally) cannot exercise. All 5 PATCHes are HIGH/MEDIUM convergent across ≥2 reviewer layers; iter-260 LESSON convergence-signal-high-confidence-triage applies.

- **PATCH-1 (HIGH; entrypoint.sh:146 + :162)** — `chmod` runs as ROOT on dev-owned files; under `cap_drop: [ALL]` (no CAP_FOWNER per Story 2.5 hardening posture), root EPERMs → `set -euo pipefail` aborts entrypoint BEFORE `exec gosu dev "$@"` handoff → container fails to start with `KEEL_DEVBOX_SSH=true`. Existing entrypoint chown calls wrap stderr-capture+continue precisely because of this; new chmod block does NOT follow the pattern. Iter-270 SM verified all 5 ACs PASS via static smokes only — no live container exec under cap_drop posture. Dockerfile L261-262 explicitly notes "chown under cap_drop: [ALL] would fail anyway" — same logic applies to chmod. **Fix:** prefix both chmod calls with `gosu dev` (mirrors mkdir/keygen/mv pattern in same block).
- **PATCH-2 (HIGH; docker-compose.yml:145)** — `KEEL_DEVBOX_SSH` host/container normalisation drift: host-side `resolve_ssh_state()` case-folds (accepts `True`/`TRUE`/`true` → publishes port 2222) but compose propagates RAW value, and entrypoint compares case-sensitively. Operator with `KEEL_DEVBOX_SSH=True` in `.envrc`: port 2222 published, no sshd starts inside → connection refused with no log signal. **Fix:** `docker-compose.yml:145` becomes `KEEL_DEVBOX_SSH: ${KEEL_DEVBOX_SSH_RESOLVED:-false}`.
- **PATCH-3 (HIGH; entrypoint.sh:138-176)** — `set -e` halts entrypoint on opt-in SSH init pre-spawn failures; SC-10 non-fatal posture only covers post-spawn liveness check. Pre-spawn failures (`gosu dev mkdir`, `chmod 0700`, `ssh-keygen`, `mv -T`) abort entrypoint completely. **Fix:** wrap entire opt-in block in subshell with `|| true` + diagnostic emit; folds in PATCH-1 + PATCH-2 + PATCH-4 cleanly.
- **PATCH-4 (MEDIUM-HIGH; entrypoint.sh:141 + :157)** — `mv -T host_keys.tmp host_keys` fails when destination dir is non-empty (partial-keypair recovery scenario). The "atomic recovery" the comment promises is silently broken in exactly the partial-corruption mode it claims to handle. **Fix:** `gosu dev rm -rf /home/dev/.ssh/host_keys && gosu dev mv -T /home/dev/.ssh/host_keys.tmp /home/dev/.ssh/host_keys` (or fold into PATCH-3's subshell rewrite).
- **PATCH-5 (MEDIUM; 15 sites across 8 scripts)** — `${VAR:+-f "${VAR}"}` paths-with-spaces hazard: forks cloned under paths-with-whitespace get word-split argv. Story 2.12 Dev Notes citation of RALPH.md 2026-04-21 AR-9 is misapplied (different word-splitting hazard class). **Fix:** refactor 15 sites to bash array idiom via new `lib/compose-args.sh` helper (extracts the conditional-arg-build into a single function, mirrors Story 2.6 AR-19 / Story 2.7 SC-14 deferred `_lib.sh` extraction pattern — Story 2.12 crosses the threshold for the compose-args concern specifically).

**11 DEFER** logged in story `### Review Findings` + `_bmad-output/implementation-artifacts/deferred-work.md § Deferred from: code review of story-2.12 (2026-04-24)`. Spec-silent or future-story-scope; none exploitable in default operator config (loopback-bound + cap_drop:[ALL] + named-volume persistence). DEFER class breakdown: 5 → Story 2.17 close-out; 1 → first live operator-workstation smoke verification (PrivSep chroot); 1 → Story 2.13 healthcheck would surface loudly; 1 → post-Epic 2 hardening; 1 → Story 2.18+ if added; 2 → cumulative LOW operator-edges sweep at SC-17.

**~10 DISMISS:** AGENTS.md H3 nesting (MECH-DRIFT-ALREADY-TRIAGED iter-270 SM); manifest description size vs siblings (non-load-bearing metadata); Dockerfile chmod-after-COPY no-ops (single RUN layer harmless); LogLevel/SyslogFacility dead-code under -e flag (stylistic); mv -T GNU-extension portability (Debian/Ubuntu base); AGENTS.md cross-ref anchor drift (precedent-consistent); 8 mechanical line-shift drifts (pre-triaged at iter-270); trace artifacts process-class concerns (WAIVED at iter-269); pnpm devbox:rebuild regenerates-keys claim (contradicted by named-volume persistence); SFTP discoverability concern (duplicate of D2 SFTP defer).

**NEW LESSON (iter-271):** Forecast band breach via novel-runtime-behaviour surface — when a story introduces multi-layer dynamic-runtime interactions (sshd lifecycle × cap_drop posture × set -e discipline × resolver-to-entrypoint env-var propagation), iter-264 LESSON severity-class-first rule's 0-3 first-class PATCH band breaks because static smokes (YAML-parse, bash -n, sync-gate, sha256, shim-tally) have NO EXECUTION PATH to dynamic-runtime defects. iter-270 SM ZERO-PATCH verdict was structurally unable to catch PATCH-1 through PATCH-4 because all 4 only manifest at first live container exec under cap_drop:[ALL]. Forecast adjustment: stories with novel-runtime-behaviour surface (sshd, healthcheck, dynamic resolver-to-runtime propagation) should pre-budget 4-7 first-class PATCH at CR even with clean SM gates. Promoted to RALPH.md § Lessons + § Signposts at iter-271. Carry-forward applies to Stories 2.13 (healthcheck — runtime liveness signalling), 6.x RLS debugger (multi-process orchestration), Epic 13 CI harness smokes (live execution against substrate).

**Story State transition:** `sm-verified → fixes-pending`. Sprint-status row UNCHANGED at `review` (per iter-202 + iter-260 precedent: only CR closure `review → done` flips sprint rows). Next iter (iter-272) executes PATCH-1 (chmod under cap_drop) as sole NOW task per Guardrail 5; QUEUE order PATCH-1 → PATCH-2 → PATCH-3 (folds in 1+2+4) → PATCH-4 (subsumed by PATCH-3's subshell rewrite — likely closes as `[x] PATCH-4 covered by PATCH-3`) → PATCH-5 (compose-args helper extraction; 15-site refactor; consider folding into PATCH-2 since both touch resolver/compose surface). Re-run `/bmad-code-review (args: "2")` after QUEUE empties for `fixes-pending → done | fixes-pending` confirmation gate. Per iter-264 LESSON Story 2.6 / 2.11 multi-pass CR cycle (Story ≥10-scripts-class), budget TWO closure re-runs at worst case.

### v1.4 — iter-270 post-dev `/bmad-create-story (args: "review")` SM gate (`traced → sm-verified`)

Two-subagent parallel fan-out per iter-235 LESSON (now THIRD cumulative application after iter-235 + iter-259 + iter-270): AC-verifier `bmad-agent-dev` (Amelia, ~123K subagent tokens) + drift-detector `bmad-agent-architect` (Winston, ~93K subagent tokens) → both converged on **clean advance — ZERO-PATCH SM verdict**. All 5 ACs PASS (none unmet, zero fix-tasks queued). F3 + F4 CRITICAL pre-dev SM PATCHes from iter-266 both confirmed landed verbatim. Substrate smokes: `pnpm keel-invariants:check` GREEN (sha256 of `docs/invariants/devbox-ssh.md` matches manifest contentHash exactly; manifest entry count 32); bash -n PASS on 11 modified shells; 15-site shim tally exact (build=1 / rebuild=1 / start=1 / stop=1 / status=1 / logs=2 / clean=2 / benchmark=6); INVARIANTS.md H3 + README.md H2 + AGENTS.md H3 sibling-insertions all SC-17-correct; .envrc.example:40 past-tense per SC-15 verified.

Winston substrate-citation drift verdict: ZERO CRITICAL drift; 8 MINOR drifts all classified as MECHANICAL line-shift artifacts of dev-pass's own substrate edits (D-1 entrypoint.sh:154-159 → :201-205; D-2 main-repo-resolver.sh :146/:152/:154 → :171/:177/:179 + in-code self-ref at :232 stale; D-3 Dockerfile apt :43-91 → :43-92; D-4 Dockerfile mkdir :252-253 → :253-255; D-6 compose env :135-136 → :136-145; D-7 compose ports :150-157 → :162-166; D-8 compose TODO :250 removed; D-9 manifest narrative parenthetical placement). Cross-invariant coherence INTACT across `INV-devbox-homedev-named-volume` + `INV-devbox-mode` + `INV-devbox-prereq-check` + fork/AMEND posture.

Outcome class: **SEVENTH cumulative ZERO-PATCH advance precedent — extends iter-264 ZERO-PATCH CR class (six precedents at that gate) with first-of-kind ZERO-PATCH SM precedent.** Pattern signal: when pre-dev SM absorbs CRITICAL substantive findings (iter-266: 2 of 8 = F3 + F4), post-dev SM has no novel surface left → clean advance becomes the dominant outcome.

Disposition: ALL 8 MINOR drifts → DEFER to Story 2.17 close-out per SC-17 carry-forward (story line-pin polish; not substantive to AC contract or runtime behaviour). NO QUEUE fix-tasks added per § Story Lifecycle Decision Matrix (no unmet-AC findings; substrate-citation drifts at MINOR severity defer-eligible).

**NEW LESSON (iter-270 drift-band re-baseline):** Substrate-citation drift class is BIMODAL — substantive band 0-2 per story for post-dev SM (narrow because pre-dev SM should have caught most) + mechanical band 8-12 for any story whose dev-pass touches ≥4 cited substrate files. Apply to forecast for Stories 2.13..2.17 SM gates. SC-17 scope holds: single reconciliation pass at Story 2.17 landing condenses both bands.

**Story State transition:** `traced → sm-verified` (Ralph internal FR14n state). Sprint-status row UNCHANGED at `review` across SM-gate window per Story 2.11 iter-258 precedent (flips to `done` only at CR closure). HTML annotation block updated `traced → sm-verified` with iter-270 audit summary; Status field unchanged. Next gate: `/bmad-code-review (args: "2")` per § Story Lifecycle Decision Matrix row `sm-verified → done | fixes-pending`.

### v1.3 — iter-269 `/bmad-testarch-trace (args: "yolo")` (`in-dev → traced`)

Trace-gate WAIVED per ground-(a)+(b)+(c) hybrid conjunction — 22nd-cumulative trace-WAIVED + 23rd-cumulative ATDD-skip-trace-WAIVED pairing (Story 2.5 iter-186 canonical pattern continued). `coverage_basis=acceptance_criteria` against zero committed test files maps all 5 ACs to `NONE` under strict trace vocabulary; partial-waive vocabulary not available at 1.0 (Story 2.11 iter-258 NOVEL LESSON). Substrate smokes from iter-268 dev-story landing cover ACs 1-3 per Story 2.11 iter-257 dev-agent pattern; ACs 4-5 always operator-workstation-deferred. **Story State transition:** `in-dev → traced` (Ralph internal FR14n state). Next gate: `/bmad-create-story (args: "review")` post-dev SM per § Story Lifecycle Decision Matrix row `traced → sm-verified | sm-fixes-pending`.

### v1.2 — iter-268 `/bmad-dev-story` (`atdd-scaffolded → in-dev → review`)

All 6 Tasks + 5 ACs landed in one `/bmad-dev-story` pass. Story file Status flipped `ready-for-dev → review`; sprint-status row flipped `ready-for-dev → in-progress → review` across the iteration (step 4 + step 9 per dev-story workflow). 18 source files touched (4 new — `sshd_config`, `docker-compose.ssh.yml`, `docs/invariants/devbox-ssh.md`, and this Change Log entry; 14 modified); Manifest entry count 31 → 32 with `INV-devbox-ssh` at contentHash `e1d693cb0ffa0c7c8d6966b8ce311c2e1daf13d83155d8a0b88028d96112a3c2`.

- **Task 1 (Dockerfile + sshd_config template).** Apt-layer extended with `openssh-server` (sibling to existing `openssh-client` per Task 1 subtask a). New `packages/devbox/sshd/` template directory mirrors Story 2.3 `packages/devbox/dnsmasq/` + `packages/devbox/nftables/` convention; hardened `sshd_config` baked at image-build time via `COPY sshd/sshd_config /etc/ssh/sshd_config` + chmod 0644 + chown root:root. Pre-created `/home/dev/.ssh/` + `/home/dev/.ssh/host_keys/` mode 0700 in the existing Dockerfile mkdir block at line 252; dev:dev ownership guaranteed by the existing `chown -R dev:dev /home/dev` in the useradd block. No `setcap` added — port 2222 > 1024, no CAP_NET_BIND_SERVICE required.
- **Task 2 (compose override + resolver + 15-site shim wiring).** New `packages/devbox/docker-compose.ssh.yml` publishes `127.0.0.1:${KEEL_DEVBOX_SSH_PORT:-2222}:2222` (SINGLE site publishing port 2222 per SC-3; base `docker-compose.yml` unchanged for port block). New `resolve_ssh_state()` function appended to `main-repo-resolver.sh` — strict-true-only normalisation (case-folded via `tr '[:upper:]' '[:lower:]'`; any non-`true` fail-closes to no-SSH per SC-2); exports `KEEL_DEVBOX_SSH_RESOLVED` + `KEEL_DEVBOX_COMPOSE_FILE_SSH` (absolute path or empty string); computes override absolute path from the lib's own `BASH_SOURCE[0]` location (self-contained). Third doc paragraph inserted AFTER the Story 2.11 paragraph describing the new function's contract. 15 compose-invoking call sites across 8 shims threaded through the `${KEEL_DEVBOX_COMPOSE_FILE_SSH:+-f "${KEEL_DEVBOX_COMPOSE_FILE_SSH}"}` conditional-expansion idiom (RALPH.md 2026-04-21 AR-9 iterable-word-splitting-hazard avoidance): build.sh (1) / rebuild.sh (1) / start.sh (1) / stop.sh (1) / status.sh (1) / logs.sh (2) / clean.sh (2) / benchmark.sh (6 — 2 cold + 3 warm + 1 initial down). `benchmark.sh` sources `main-repo-resolver.sh` specifically for `resolve_ssh_state` (skips the two mode resolvers which it doesn't need).
- **Task 3 (entrypoint.sh first-boot keygen + conditional sshd start).** Conditional block (gated on `${KEEL_DEVBOX_SSH:-false} == "true"`) inserted AFTER Story 2.3 egress-init block (line 121-130) and BEFORE the CMD-empty fallback at line 154 (NOT inside the if/else). Sequence: `gosu dev mkdir -p host_keys → chmod 0700 .ssh host_keys (idempotent every boot per SC-7 StrictModes defence) → existence guard on BOTH final filenames → atomic scratch-dir-then-mv-T keygen (prevents mid-kill partial keypair) → ssh-keygen -q -t ed25519 -N "" + ssh-keygen -q -t rsa -b 4096 -N "" (explicit algorithms; iter-266 PATCH-3 verified the prior `-A` idiom was literally broken since `-A` ignores `-f`) → touch authorized_keys if absent + chmod 0600 every boot → gosu dev /usr/sbin/sshd -D -e 2>>/var/log/sshd.log & → capture SSHD_PID=$! → sleep 0.5 → kill -0 "${SSHD_PID}" liveness check → non-fatal stderr report on failure (tail last 20 /var/log/sshd.log lines + sc-10-lifecycle operator pointer)`. Background `&` returns exit 0 from the fork regardless of sshd startup; liveness check catches sshd-config-syntax errors, unreadable host-keys, and stray-process port conflicts while preserving SC-10 (sshd lifecycle is operator's responsibility post-spawn; Story 2.13 healthcheck is the authoritative long-term signal).
- **Task 4 (compose environment propagation + TODO removal + roadmap line).** `environment:` block extended with `KEEL_DEVBOX_SSH: ${KEEL_DEVBOX_SSH:-false}` per SC-9 (compose's `${VAR}` interpolation runs on the HOST at parse time; the container only sees vars in `environment:` or `env_file:`); without this, entrypoint's gate reads unset → no-SSH regardless of `.envrc`. TODO marker at `docker-compose.yml:250` removed. Story 2.12 LANDED line added to roadmap comment block between the existing Story 2.11 and Story 2.13 lines.
- **Task 5 (invariant registration + 8-section contract doc + anchor bullet).** `INV-devbox-ssh` entry added to `packages/keel-invariants/src/invariants.manifest.ts` after Story 2.11's `INV-devbox-mode` entry and BEFORE `INV-gitignored-secret-commit-deny` (Story 2.2); five-field InvariantSchema-compliant (id / description / sourcePath / contentHash / anchors with ['INV-devbox-ssh']). contentHash `e1d693cb0ffa0c7c8d6966b8ce311c2e1daf13d83155d8a0b88028d96112a3c2` (sha256sum on `docs/invariants/devbox-ssh.md`). `docs/invariants/devbox-ssh.md` authored with 8 H2 sections (Loopback-bound port publication / SSH signal / Opt-in sshd / No-SSH default / Resolver / External connection refusal / Operator authorized-keys flow / Named-volume relationship + Mode orthogonality + Invariant stability) — mirrors Story 2.3 multi-faceted-contract posture from iter-156 LESSON. `INVARIANTS.md § Devbox SSH (Story 2.12)` H3 sibling-inserted between `### Devbox mode (Story 2.11)` and `### Gitignored-secret commit-deny (Story 2.2)`; anchor bullet ``- **`INV-devbox-ssh`** — …`` matches Story 1.9 sync-gate regex `/^-\s+\*\*\`(INV-[a-z0-9]+(?:-[a-z0-9]+)+)\`\*\*/gm` verbatim. `pnpm keel-invariants:check-all` EXIT=0 (manifest-sync + tokens-sync both green post rebuild).
- **Task 6 (operator + agent docs + .envrc.example + Change Log).** `packages/devbox/README.md § Opt-in SSH (Story 2.12)` H2 sibling-appended between `## Per-fork vs shared devbox mode (Story 2.11)` and `## cc-devbox upstream provenance` (SC-17 discipline — no edits to prior story sections). Content: AC 1 invariant statement + `.envrc` snippet + three walkthroughs (default no-SSH / opt-in sshd / authorized-keys flow) + mode-flip note + external refusal explanation + cross-references. `AGENTS.md § Opt-in SSH (Story 2.12)` H3 sibling-appended between `### Per-fork vs shared devbox mode (Story 2.11)` and `## Ralph loop` — agent-facing guardrails (loopback invariant, resolver idiom + compose-override single-site, token persistence + re-auth pointer, ListenAddress rationale, mode-flip discipline, scope carve-outs). `.envrc.example:40` comment updated past-tense (SC-15) with LANDED iter-268 + resolver + override-file citations.
- **Smokes (operator-workstation-deferred for AC 4-5; substrate smokes for ACs 1-3).** Substrate smokes: resolver normalisation verified (true/TrUe → true; false/yes/empty/unset → false); manifest sync-gate green; 15 call-site tally confirmed; bash -n syntax-clean across 10 modified scripts; js-yaml parse-clean on both compose files. Docker not available in DinD iteration env (expected; iter-267 ATDD-skip grounds (c)+(ii) flagged this posture). Live-container smokes (`docker compose config` port-form grep + `pgrep sshd` + `docker port keel-devbox` + operator pubkey flow) operator-workstation-deferred per Story 2.5 iter-186 + Story 2.11 iter-257 precedent. AC 4 external LAN peer and AC 5 full pubkey-auth flow always operator-workstation-deferred by AC-contract.
- **Carry-forward observations.** No new lessons; the dev-story pass aligns with iter-251/253/257 substrate smoke-only posture. PATCH-count forecast at post-dev SM (iter-269 `/bmad-testarch-trace` + iter-270 `/bmad-create-story (args: "review")`): iter-266 NOVEL LESSON forecast band (6-10 at pre-dev; 0-3 at CR post-PATCH) narrows to ~0-4 at post-dev SM given the resolver + compose-override + entrypoint pattern all mirror Story 2.3/2.5/2.11 precedents. The novel substrate surfaces (sshd-host-key-bootstrap + liveness-check non-fatal) may surface second-class DEFERs aligning with the iter-253 LESSON 1/1000/year operator-edge band.

### v1.1 — iter-266 pre-dev SM gate (`drafted → validated`)

Four parallel Sonnet subagents re-analysed story against sources. Ralph triage produced 8 PATCH + 8 DEFER + 3 DISMISS across critical + second-class findings.

**PATCH-1 (§ References).** Swapped `prd.md ↔ architecture.md` file-paths for 6 citations. Verified at iter-266: `architecture.md:74` holds "Host surface…sshd opt-in"; `architecture.md:291` holds `KEEL_DEVBOX_SSH=false` snippet; `prd.md:550` holds Healthcheck contract; `prd.md:551` holds Ports contract. Replaced architecture.md:1153 / :1160 citations (stale lines pointing at app-tree content) with architecture.md:285-292 (.envrc parameterisation block) + prd.md:104 (Executive Summary) + prd.md:544 (Attach UX). Removed the redundant stale "PRD line 551 … architecture.md :551 verbatim matches" Dev Note — replaced with a clean enumeration of the 6 authoritative source sites.

**PATCH-2 (Task 2 shim inventory).** Corrected 3-way wrong claim: original spec said "9 shims" hardcoded-path-based via a grep that returns 0 hits (scripts use `"${COMPOSE_FILE}"` var pattern, not a literal). Verified at iter-266 via `grep -c '^[^#]*docker compose -f' packages/devbox/scripts/*.sh`: actual 8 compose-invoking scripts are `build.sh, rebuild.sh, start.sh, stop.sh, status.sh, clean.sh (2 sites), logs.sh (2 sites), benchmark.sh (6 sites)`. Non-invoking scripts explicitly named OUT-OF-SCOPE: `restart.sh` (delegates to stop+start), `env-check.sh` (parses .envrc only), plus the docker-exec shims (`monitor-host/attach/shell/claude-host/gh-auth-host/ralph-*-host/whitelist-host`). Also switched the resolver's export shape from a word-split-fragile string (`KEEL_DEVBOX_COMPOSE_FILES_ARGS`) to a conditional-expansion single-path (`KEEL_DEVBOX_COMPOSE_FILE_SSH`), using the `${VAR:+-f "${VAR}"}` idiom that avoids RALPH.md 2026-04-21 AR-9 iterable-word-splitting hazards.

**PATCH-3 (Task 3 entrypoint block).** Critical — original code block had `gosu dev ssh-keygen -A -f /home/dev/.ssh/` which is LITERALLY BROKEN (`-A` ignores `-f`, writes to `/etc/ssh/` which is read-only-after-image-build). Three-way convergent from Subagents 2/3/4. Replaced with explicit `-t ed25519 -f <path> -N ""` + `-t rsa -b 4096 -f <path> -N ""` invocations, wrapped in an atomic `mkdir tmpdir → generate → mv -T` pattern that survives mid-keygen container kills. Added idempotent perm enforcement every boot (chmod outside existence guards, per Subagent 4 F2 — StrictModes silent-reject defence). Changed root-`mkdir` to `gosu dev mkdir` so keygen doesn't hit "permission denied" against a root-owned directory (Subagent 3 finding). Added `< /dev/null` to every ssh-keygen invocation (prevents interactive prompt under `set -e`). Corrected entrypoint insertion-point claim: exec handoff is at `:155`/`:157`, NOT `:154` (line 154 is the CMD-empty `if` opener).

**PATCH-4 (Task 3 liveness verification).** Added post-spawn `SSHD_PID="$!"; sleep 0.5; kill -0 "${SSHD_PID}"` check with non-fatal stderr reporting (Subagent 4 F3 CRITICAL). Background `&` returns exit 0 from the fork regardless of sshd's actual startup success; without the liveness check, a crashing sshd leaves operator seeing `docker port` reporting 2222 published + connections refusing, with no signal until operator tries to ssh. Non-fatal posture preserves SC-10 (background process lifecycle is operator's responsibility post-spawn); fatal halt would regress the no-SSH-default-should-just-work contract on sshd-config-corruption edge cases. Story 2.13 healthcheck remains the authoritative long-term signal.

**PATCH-5 (sshd_config + AC 3 + AC 4 + SC-12 + invariant doc).** Removed `ListenAddress 127.0.0.1` from sshd_config (Subagent 4 F4 CRITICAL). Rationale: container's network namespace has its own loopback; inbound traffic from Docker's published port arrives on container eth0 (via iptables DNAT under `userland-proxy=false`, or via docker-proxy re-origination under `userland-proxy=true` default), NOT on container-loopback. Binding sshd to `127.0.0.1` inside the container would SILENTLY reject all inbound traffic under both modes — AC 3 would be broken. Loopback confinement is correctly + solely enforced by Docker's `127.0.0.1:2222:2222` publish in `docker-compose.ssh.yml` (host-side). Rewrote AC 3 + AC 4 + SC 12 + invariant doc § Opt-in sshd contract + § External connection refusal to reflect single-layer host-side-publish posture. Added sshd_config header comment explaining the intentional absence of `ListenAddress`.

**PATCH-6 (SC-2 normalisation).** Removed "`yes`, `1`, etc. all also accepted" misstatement; pinned strict `true`-only posture mirroring Story 2.11 `resolve_mode_specific_state()` idiom at `lib/main-repo-resolver.sh:152`. Forks MAY NOT extend the accepted-signal set. Aligns SC-2 with AC 3 + Task 2 resolver contract.

**PATCH-7 (Dev Notes + References line pins).** Corrected `lib/main-repo-resolver.sh:146` line-pin (the function opener; normalisation at `:152`; gate at `:154`). Corrected the "already extended by Story 2.11" claim about `:9-28` — Story 2.11's extension is at `:35-77`, original doc-block at `:1-28` is untouched. Clarified Task 2 inserts a THIRD doc paragraph AFTER the Story 2.11 paragraph. Added source-citations for entrypoint.sh `:121-130` (egress-init block is actually :121-130, not :125-130) and `:154-159` (CMD-empty fallback + exec handoff).

**PATCH-8 (Task 1 setcap).** Removed self-cancelling subtask ("Add `setcap +ep` if needed…NET_BIND_SERVICE is NOT required") — replaced with an affirmative "No `setcap` invocation" instruction that closes the read-body-cancels-title confusion. Verification hook preserved.

**DEFER items (8):**
1. sshd_config `StrictModes no` opt-out (kept StrictModes default; Task 3 idempotent perm enforcement sufficient).
2. docker-exec shims `-e KEEL_DEVBOX_SSH=...` propagation safeguard (operator-edge; once-at-start contract already clear in Task 6(d) mode-flip note).
3. Task 5 invariant-doc H2 section overlap (8→6 consolidation); polish-only, defer to CR cycle or Story 2.17 close-out.
4. Override file `name:` subtask hardening (Compose v2 implicitly picks up from base file; correct-by-default).
5. SC-13 ("No PRD amendment") — narrative SC without Task-enforcement step; keep for traceability.
6. `resolve_ssh_state()` paths-with-spaces edge (current repo `/workspace/ralph-bmad` is safe; fork under `/Users/<space>/…` would hit embedded-quote-under-unquoted-alt-expansion word-splitting). ~~mitigated by PATCH-2's `${VAR:+-f "${VAR}"}` single-path idiom~~ — superseded at iter-271 CR PATCH-5 + iter-276 landing: the inline `${VAR:+-f "${VAR}"}` form is NOT a mitigation for the paths-with-spaces hazard; bash's alt-value expansion re-tokenises by word-splitting BEFORE embedded double-quote chars are re-parsed (they become literal post-expansion). AR-9 citation removed — AR-9 is about `for x in $(...)` iterable word-splitting on IFS, a distinct hazard class that the single-path idiom does mitigate but which is not this one. Actual fix: bash array idiom via `packages/devbox/scripts/lib/compose-args.sh` helper (iter-276 PATCH-5).
7. Operator lockout/revoke flow documentation (Task 6 gap; queue for CR cycle).
8. Task 6 authorized-keys echo `>>` command-injection surface (operator-trusted content; defer to Story 2.13 or Epic 2 close-out docs polish).

**DISMISS items (3):**
1. InvariantSchema description-field compliance (already compliant; Story 2.3 iter-156 LESSON applies cleanly).
2. SC-1/SC-3 overlap with AC 1/AC 2 (standard pattern across Epic 2 stories; not a defect).
3. Compose v2 `ports:` merge REPLACE-vs-CONCATENATE claim (per compose-spec.io, `ports` IS in the additive-sequence key list alongside `expose`, `external_links`, `dns`, `dns_search`, `tmpfs`, `volumes` — the original spec claim was correct).

**Outcome.** Story State `drafted → validated`. 8 PATCH applied in-place; 8 DEFER + 3 DISMISS logged. Next gate: `/bmad-testarch-atdd` for `validated → atdd-scaffolded` (forecast: NON-SKIP for ACs 1-3 via `docker compose config` + entrypoint behaviour smokes; ACs 4-5 operator-workstation-deferred per iter-256/258 precedent). Pre-dev SM review PATCH-count (8) lands slightly above iter-221 LESSON's ~5-ceiling — driven by the two CRITICAL substantive findings (F3 liveness + F4 userland-proxy) that are NOT density-amortisable; the three additional non-blocker PATCHes (7, 8, and half of 2) are narrow pins.
