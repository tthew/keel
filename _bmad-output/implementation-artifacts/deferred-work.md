# Deferred work (code-review carry-forward)

Items surfaced by `/bmad-code-review` adversarial triage that were classified `defer` rather than `patch`. Each entry cites the originating review and the reason for deferral so future stories can absorb the work with full context.

## Deferred from: code review of story-1.8 (2026-04-20)

_Story 1.8 (`invariants.manifest.ts` contract + exporter) — iter-7 `/bmad-code-review (args: "2")`. Three-layer adversarial review (Blind Hunter + Edge Case Hunter + Acceptance Auditor). Auditor returned zero AC violations; the six defers below all originate from Blind Hunter + Edge Case Hunter defensive-hardening findings that are either explicitly Story 1.9's enforcement-side scope (per Story 1.8's § Scope Carve-Out + FR43) or spec-silent tightening candidates._

- **contentHash drift validation at manifest-use time.** _Source:_ Blind Hunter. _Location:_ `packages/keel-invariants/src/invariants.manifest.ts` (all 9 entries). _Nature:_ the manifest stores a pre-computed `contentHash` but never re-reads the `sourcePath` file to verify the hash is still current. _Carry-to:_ Story 1.9 sync-gate — FR43 enforcement side explicitly owns the anchor-walker + file-read + sha256 re-computation + drift-detection flow. Absorbed naturally; no action needed ahead of Story 1.9.
- **sourcePath path-traversal / format guard in Zod schema.** _Source:_ Blind Hunter + Edge Case Hunter. _Location:_ `packages/keel-invariants/src/invariants.manifest.ts:6` (`sourcePath: z.string().min(1)`). _Nature:_ schema accepts absolute paths, `..` traversal, Windows backslashes, leading slashes. Current 9 entries are all repo-relative forward-slash, so no live exposure. _Carry-to:_ Story 1.9 walker does file-resolution and will naturally reject non-resolvable paths; if tightening the schema itself is wanted earlier, a one-line `.regex(/^[^/\\][^\\]*$/).refine(p => !p.includes('..'))` does it — but that's scope creep vs Story 1.8's contract-only remit.
- **`id` uniqueness across array not enforced by schema.** _Source:_ Blind Hunter + Edge Case Hunter. _Location:_ `packages/keel-invariants/src/invariants.manifest.ts:11` (`InvariantsSchema = z.array(InvariantSchema)`). _Nature:_ Zod's `z.array` permits duplicate elements; two entries with the same `id` would silently validate. Acceptance Auditor confirmed current 9 IDs are unique. _Carry-to:_ Story 1.9's drift-detection gate treats unregistered / duplicate-registered rules as drift per AC 3 enforcement side — duplicates surface there. Alternatively, a `.superRefine(arr => Set uniqueness check)` on `InvariantsSchema` in Story 1.9's refactor pass.
- **Shared-sourcePath cross-entry `contentHash` consistency refine.** _Source:_ Edge Case Hunter. _Location:_ `packages/keel-invariants/src/invariants.manifest.ts:11`. _Nature:_ four current entries pair up on two shared source files (`eslint.config.keel-invariants.js` hosts `INV-eslint-shared` + `INV-eslint-import-boundary`; `.pre-commit-config.yaml` hosts `INV-prek-pre-commit-config` + `INV-prek-commit-msg-config`). Whole-file carve-out means these pairs MUST share identical `contentHash` values (they do — auditor verified). If a future authoring error broke that invariant (same `sourcePath`, different `contentHash`), the schema wouldn't catch it. _Carry-to:_ Story 1.9 walker re-computes `sha256` per distinct `sourcePath` and surfaces cross-entry mismatch immediately.
- **`invariants` array immutability — `readonly` / `Object.freeze`.** _Source:_ Blind Hunter + Edge Case Hunter. _Location:_ `packages/keel-invariants/src/invariants.manifest.ts:89` (`export const invariants: Invariant[]`). _Nature:_ consumer of `@keel/keel-invariants` receives a live reference to a mutable array; a misbehaving consumer (`invariants.push(...)` / `invariants[0].id = '...'`) would corrupt the module-level singleton for every other importer. No current consumer is known to mutate it. _Carry-to:_ Story 1.9 refactor — two-line change (`readonly Invariant[]` type + `Object.freeze(InvariantsSchema.parse(raw))`). Spec-silent hardening; low-cost but out-of-scope for contract-only 1.8.
- **Schema-evolution metadata on `Invariant` entries (`schemaVersion` / `deprecated` / `since`).** _Source:_ Blind Hunter. _Location:_ `packages/keel-invariants/src/invariants.manifest.ts:3-9` (`InvariantSchema` object shape). _Nature:_ no first-class way to mark an invariant as deprecated-but-not-yet-drifted, no schema version field on the root manifest, no `since` / introduced-in-story field per entry. _Carry-to:_ Story 1.9's enforcement side will need "deprecated ≠ drift" semantics at minimum (otherwise sunsetting a rule and removing its registration both look like drift). Natural home for this expansion. If earlier would be valuable, a later substrate-hardening story (1.16 scope or adjacent) can add it.

## Deferred from: code review of story-1.9 (2026-04-20)

_Story 1.9 (Invariant sync-gate runtime tooling — reader/walker/drift-detector) — iter-7 `/bmad-code-review (args: "2")`. Three-layer Ralph-hosted adversarial fan-out (Blind Hunter as `bmad-agent-architect` / Edge Case Hunter as `bmad-tea` / Acceptance Auditor as `bmad-agent-dev`). Convergent CRITICAL (regex-class bug on `ANCHOR_REGEX` — Edge + Auditor both reproduced live) + 3 HIGH content-accuracy drifts + 2 MEDIUM framing/citation drifts → 6 patches queued. The 10 defers below are all defensive-hardening candidates natural-scope for Story 1.10+ substrate refactors OR the Story 1.16 test-runner landing; none are exploitable in the current single-consumer, linux-only, text-sources-only baseline._

- **Shallow `Object.freeze` vs "readonly + Object.freeze" spec claim.** _Source:_ Edge Case Hunter. _Location:_ `packages/keel-invariants/src/invariants.manifest.ts:~130`. _Nature:_ `Object.freeze(invariants)` locks the top-level array but entries and their `anchors` sub-arrays remain mutable at runtime (`invariants[0].id = 'HACKED'` succeeds; `invariants[0].anchors.push('X')` succeeds; `invariants.push(fake)` throws because only the outer array is frozen). `readonly Invariant[]` is a compile-time TS signal with zero runtime effect. Task 3 (Story 1.9 CR defer #5 from Story 1.8) advertised "readonly + Object.freeze" as a runtime guarantee; the landed implementation is the shallow-only variant. Not exploitable in-process today (no consumer mutates entries), but the spec over-advertises the guarantee. _Carry-to:_ either Story 1.10+ deep-freeze refactor (`entry + entry.anchors` Object.freeze) OR amend spec text to "shallow freeze (array-reassignment guard only)". Two-line code change either way.
- **`check.ts` lacks try/catch around `runSyncGate` — raw ENOENT stacktrace on missing INVARIANTS.md.** _Source:_ Edge Case Hunter. _Location:_ `packages/keel-invariants/src/check.ts` (top-level await). _Nature:_ if `INVARIANTS.md` is absent (cwd mismatch, dist-layout drift, moved-file) or `repoRoot` resolution misfires, Node emits `UnhandledPromiseRejection` + raw ENOENT stacktrace. AC 6's intent is "non-zero reliably fails the workflow AND the drift report renders in CI logs" — raw stacktrace exits non-zero but violates the "renders a report" half. _Carry-to:_ wrap the top-level await in try/catch, emit `{status: 'error', message: e.message}` and exit 1. Belongs with Story 1.16 CI-wiring story where the error-surface is tested end-to-end in GitHub Actions logs.
- **Traversal refine gaps in `sourcePath` Zod validation.** _Source:_ Edge Case Hunter. _Location:_ `packages/keel-invariants/src/invariants.manifest.ts:~10` (`refine(p => !p.startsWith('/') && !p.startsWith('\\\\') && !p.includes('..') && !p.includes('\\\\'))`). _Nature:_ (a) `foo/bar/%2e%2e/etc/passwd` passes (percent-encoded `..` — benign today since `readFile` doesn't URL-decode, but the refine message implies broader guard); (b) `D:/Windows` passes (Windows drive-absolute — low-risk on Linux CI, non-zero on Windows contributors); (c) null-byte / newline / CR characters not rejected; (d) symlinks defeat the refine at the FS layer regardless (`readFile` follows symlinks, so a committed `src/foo` → `/etc/passwd` symlink hashes the foreign content). _Carry-to:_ tighten refine (`!/\0|\n|\r/.test(p)`, `!/^[a-zA-Z]:/.test(p)`) + use `fs.lstat` + reject `S_IFLNK`. Natural Story 1.10+ schema-hardening pass.
- **UTF-8 binary-file mangling in `manifest-reader.ts`.** _Source:_ Edge Case Hunter. _Location:_ `packages/keel-invariants/src/manifest-reader.ts:8` (`readFile(absPath, 'utf-8')`). _Nature:_ reading bytes as utf-8 string and hashing the decoded string diverges from byte-level sha256 on any non-UTF-8 content (binary files, files with invalid sequences). All current invariants source text files; vacuous today. But a future invariant targeting a binary file (lockfile digest, wasm module) would hash inconsistently with `sha256sum` CLI output. _Carry-to:_ swap to `readFile(absPath)` → Buffer, then `createHash('sha256').update(buffer).digest('hex')` — aligns byte-for-byte with `sha256sum`. One-line change.
- **`Promise.all` error-swallow at `sync-gate.ts:47-57`.** _Source:_ Edge Case Hunter. _Location:_ `packages/keel-invariants/src/sync-gate.ts:47-57` (catch-all returning `null` inside `Promise.all`). _Nature:_ all non-ENOENT failures (permission denied, I/O error, path-is-a-directory) collapse to `removed-from-source-only` drift. Misleading root-cause analysis in CI. _Carry-to:_ narrow catch to `err.code === 'ENOENT'`; re-throw others OR emit `error`-kind drift. Natural hardening alongside the check.ts try/catch defer above.
- **Anchor-parser O(n·m) complexity.** _Source:_ Edge Case Hunter. _Location:_ `packages/keel-invariants/src/sync-gate.ts` (single-pass regex over doc per entry). _Nature:_ linear in entries × doc lines; at current scale (10 entries × ~60 doc lines) sub-millisecond; at 500 entries × 500 lines still sub-second. Not a live risk; flagged for principled scale-awareness. _Carry-to:_ no action needed for 1.x; if manifest grows past ~500 entries or doc past ~5000 lines, revisit with a single-pass map-building approach.
- **Duplicate-id refine message shape.** _Source:_ Edge Case Hunter. _Location:_ `packages/keel-invariants/src/invariants.manifest.ts:~22-28` (id-uniqueness `.superRefine`). _Nature:_ triplicate IDs emit 2 separate issue messages (one per extra occurrence), not one-per-pair. Cosmetic Zod UX only; does not affect correctness (duplicates are still caught). _Carry-to:_ tighten refine to emit a single grouped issue per duplicate-ID cluster. Trivial fix, low priority.
- **Whitespace-in-anchor backtick normalisation.** _Source:_ Edge Case Hunter. _Location:_ `packages/keel-invariants/src/sync-gate.ts` (anchor regex). _Nature:_ a hypothetical anchor line with trailing whitespace inside the backticks (`` `INV-foo-bar ` `` vs `` `INV-foo-bar` ``) would be treated as distinct IDs. Regex doesn't allow spaces today (character class excludes space), so not exploitable. Principled hardening for future-syntax-drift tolerance. _Carry-to:_ `.trim()` the capture group before `anchors.add`. One-line change.
- **`<500ms internal target` prose vs 0.77s measured.** _Source:_ Blind Hunter. _Location:_ `_bmad-output/implementation-artifacts/1-9-…md:128` § Performance posture. _Nature:_ spec declares an internal target of `<500ms` + `<100ms cold` + `<20ms warm` as prose guidance. Task 5 measured 0.77s wall-clock (770ms) — the AC threshold `<2s` is met (AC 7 PASS), but the prose internal target was exceeded by ~50%. The prose is informational (not an AC), so not a fix-task candidate; flagged to either amend the prose down to realistic levels (`<1s warm, ~800ms cold`) OR re-benchmark with the node-module cache warm. _Carry-to:_ cleanup iteration alongside a trace/perf-regression follow-up. Low impact.
- **Provisional-ID bullet leftover "(Actually — …)" scratch-commentary.** _Source:_ Blind Hunter. _Location:_ `_bmad-output/implementation-artifacts/1-9-…md:130` (§ Provisional-ID header discharge bullet). _Nature:_ story file retains parenthetical scratch commentary `"(Actually — Task 2 edits invariants.manifest.ts, not INVARIANTS.md; INVARIANTS.md stays as-is since the 10th anchor is already there since commit 5cfa055. The provisional header removal is a cosmetic docs edit; wire it into Task 5 alongside sprint-status bump.)"` — reads as hand-edit scratch-pad leakage, not polished prose. Cosmetic only. _Carry-to:_ docs-cleanup iteration; replace the parenthetical with a concise "Provisional-header removed alongside Task 5 sprint-status bump." sentence.

## Deferred from: code review RE-RUN of story-1.9 (2026-04-20)

_Story 1.9 (Invariant sync-gate runtime tooling) — iter-14 `/bmad-code-review (args: "2")` CR RE-RUN after iter-8..iter-13 fix pass. Three-layer Ralph-hosted adversarial fan-out. **Acceptance Auditor verdict PASS** (all 7 AC validated end-to-end with LIVE reproductions including byte-identical revert-restore for AC 2 manifest-side missing-anchor + AC 5 docs-side orphan-anchor smokes). **Edge Case Hunter verdict MINOR_ONLY** (4 LOW new-edge-case findings — 2 new + 2 reconfirmations of iter-7 defers). **Blind Hunter verdict NEW_FINDINGS** (3 HIGH trace-bundle residue + 1 MEDIUM spec self-contradiction — all absorbed into iter-15..iter-17 fix queue as Fix A/B/C; NOT deferred)._

_The 4 defers below are all Edge Case Hunter LOWs — defensive-hardening candidates natural-scope for Story 1.10+ schema-hardening OR the Story 1.16 test-runner landing; none exploitable in current single-consumer linux-only text-sources-only baseline with column-0 bullet convention and code-fence-free INVARIANTS.md. No patches required for these defers; adversarial-triage-default holds (per `PROMPT_build.md § Story Lifecycle`)._

- **Anchor-regex `^-\s+` column-0 binding forbids indented bullets.** _Source:_ Edge Case Hunter (iter-14 re-run). _Location:_ `packages/keel-invariants/src/sync-gate.ts:24` (`ANCHOR_REGEX = /^-\s+\*\*\`(INV-[a-z0-9]+(?:-[a-z0-9]+)+)\`\*\*/gm`). _Nature:_ the leading `^-\s+` binds column 0 with multiline-mode `^`. Indented bullets (`  - **\`INV-foo\`**`) do NOT match. Reproducible: `node -e "console.log([...'  - **\`INV-foo-bar\`**'.matchAll(/^-\s+\*\*\`(INV-[a-z0-9]+(?:-[a-z0-9]+)+)\`\*\*/gm)])"` → `[]`. All current INVARIANTS.md entries are column-0 bullets (verified — zero indented matches), so zero active drift. If a future section groups invariants as sub-bullets under a parent header (a natural Markdown editing pattern — e.g., "### Core invariants → - **\`INV-*\`**"), those anchors will be silently invisible to the gate → treated as "manifest-only" → trip false `added-to-source-only` drift on otherwise-valid manifest entries. No spec in the story forbids nested bullets in INVARIANTS.md. _Carry-to:_ Story 1.10+ schema-hardening — relax leading anchor to `^\s*-\s+` to accept optional indent; OR add an explicit spec clause forbidding indented anchor bullets in INVARIANTS.md and a lint-rule gate (latter is zero-cost guard-rail discipline).
- **Anchor-regex matches anchors inside code fences.** _Source:_ Edge Case Hunter (iter-14 re-run). _Location:_ `packages/keel-invariants/src/sync-gate.ts:24` (`ANCHOR_REGEX` + `readAnchors` loop at `:26-34`). _Nature:_ the regex is applied to the raw markdown file content without code-fence stripping. An anchor-like bullet inside a triple-backtick fenced block (e.g., documenting the anchor ID format by example: `` ```\n- **\`INV-example-id\`**\n``` ``) will be matched. With no corresponding manifest entry for `INV-example-id`, the gate emits `removed-from-docs-only` and exits 1 on an otherwise-valid CI run. Current INVARIANTS.md has no code fences containing INV-prefixed content (verified — zero matches in fenced blocks), so zero active exposure. However, the spec is silent on forbidding code-fence examples with INV-prefixed IDs, making this an easy footgun for future doc editors who want to document the anchor format. _Carry-to:_ Story 1.10+ schema-hardening — strip code fences before regex application (`const stripped = docText.replace(/^```[\s\S]*?^```/gm, '')`) OR augment the spec with an explicit "INVARIANTS.md SHALL NOT contain INV-prefixed anchor bullets inside code fences" clause + a lint gate.
- **Reconfirmation: traversal refine `!p.includes('..')` over-broad rejection.** _Source:_ Edge Case Hunter (iter-14 re-run — complements iter-7 defer "Traversal refine gaps in sourcePath Zod validation"). _Location:_ `packages/keel-invariants/src/invariants.manifest.ts:~10` (`.refine(p => … && !p.includes('..') …)`). _Nature:_ new angle on iter-7's traversal-refine defer. iter-7 flagged false-negatives (`foo/%2e%2e/etc/passwd` passes); iter-14 flags the inverse false-positive: any innocuous filename containing the literal two-character sequence `..` (e.g., `packages/foo/bar..config.js`, `modules/lib..test.js`) is rejected by the refine with a misleading "traversal" message. No current sourcePath contains `..` — zero active impact — but an over-broad guard that forbids legal filenames. Correct guard: split on `/` and reject segments equal to `..`, not any substring occurrence. _Carry-to:_ subsumed by iter-7's Story 1.10+ schema-hardening traversal-refine pass; refactor to path-segment-aware guard (e.g., `!p.split('/').includes('..')`) alongside the null-byte / CR / Windows-drive tightening already flagged.
- **Reconfirmation: UTF-8 byte-lossy hashing in `manifest-reader.ts`.** _Source:_ Edge Case Hunter (iter-14 re-run — duplicate of iter-7 defer "UTF-8 binary-file mangling in `manifest-reader.ts`"). _Location:_ `packages/keel-invariants/src/manifest-reader.ts:~7-9` (`readFile(absPath, 'utf-8')` then sha256 the decoded string). _Nature:_ same defect as iter-7 defer; re-surfaced by iter-14 Edge Case Hunter's independent branch-walking. Hashing decoded UTF-8 strings instead of raw bytes means a sourcePath containing invalid UTF-8 sequences (binary file accidentally listed, config with stray 0xFF, lockfile) yields U+FFFD replacement characters and hashes lossily. Two files differing only in non-UTF-8 bytes would hash identically. All current sourcePaths are well-formed UTF-8 text (.json / .js / .ts / .md / .yaml) → zero active exposure. Contract silently coerces rather than failing loud, contradicting the story's "fail loudly on drift" framing at line 11. _Carry-to:_ subsumed by iter-7's one-line fix (`readFile(absPath)` → Buffer → `createHash('sha256').update(buffer).digest('hex')`). Independent second-source confirmation strengthens the defer's signal but adds no new carry-to.

## Deferred from: code review RE-RUN #6 of story-1.9 (2026-04-20)

_Story 1.9 iter-36 `/bmad-code-review (args: "2")` CR RE-RUN #6 against post-Fix-M/N/O/P/Q diff (`bcd18e2..HEAD`, 121 lines, 4 files, 2 Story-1.9 substantive — trace.md + coverage-matrix.json). Three-layer Ralph-hosted fan-out. **Acceptance Auditor: PASS** (zero AC violations; 9 checks verify byte-identical smoke-evidence match + AC-2 scope carve-out preserved + Fix Q vocabulary swap surgical + coverage-matrix level_notes faithful reduction + Fix O schema-refine clause Task-3 consistent + line-citation audit clean + sibling-vocabulary inflation check clean + AC-scope boundary clean + JSON validity). **Blind Hunter: NEW_FINDINGS** (15 raw — 2 HIGH + 6 MEDIUM + 5 LOW + 2 borderline). **Edge Case Hunter: NEW_FINDINGS** (15 items — heavy SIBLING-FIELD-PARALLEL + SYMMETRIC-AC-PARALLEL on AC-3 Coverage header/HP-Gaps item + in-trace.md § Gaps / § Recommendation paragraphs for AC-2 + AC-5 that Fix M/N/O/P did not reach + AC-1/AC-3/AC-5/AC-6 level_notes shape asymmetries + 3 unbolded runner-hosted AC-7 cluster sites)._

_Triage: **4 PATCH** (Fix R: coverage-matrix.json:39 substrate_verification backstop→complement; Fix S: trace.md :175/:236/:484 runner-hosted→test-runner-hosted sweep; Fix T: trace.md:112 + :205 AC-3 Coverage + HP-Gaps template-prefix symmetry; Fix U: trace.md:105 + :145 AC-2 + AC-5 backstop-vocabulary reframe), **6 DEFER** (below), **13 DISMISS** (editorial / bookkeeping / spec-accurate per evidence class). Halving hypothesis DECISIVELY falsified AGAIN: iter-14 4 → iter-18 2 → iter-21 2 → iter-24 5 → iter-30 5 → iter-36 30 raw (4 PATCH post-triage)._

- **Coverage-matrix.json AC-2 `scope_notes` DEFERRED posture vs `level_notes` `end-to-end evidence` angle.** _Source:_ Blind Hunter (BH2, rated HIGH). _Location:_ `_bmad-output/test-artifacts/traceability/1-9-coverage-matrix.json:40-41`. _Nature:_ scope_notes says "source-tree auto-discovery of unregistered rules is DEFERRED — anchor-side realisation only at 1.9"; level_notes now says "smoke (shell) — end-to-end evidence". Blind Hunter reads this as a posture mix — "end-to-end evidence + DEFERRED" on the same JSON object. _Defer reason:_ end-to-end and deferral describe orthogonal evidence paths (anchor-side branch vs source-tree auto-discovery); substrate_verification body at :39 explicitly qualifies the end-to-end claim to the anchor-side branch ("AC-2 anchor-side branch verified by ephemeral INV-commitlint-shared anchor deletion"); the one-line level_notes classifier legitimately reduces to the in-scope realised branch. Not a hard contradiction. _Carry-to:_ Story 1.16 when source-tree auto-discovery lands and can be classified alongside anchor-side in level_notes.
- **Coverage-matrix.json AC-2 :41 vs AC-5 :71 level_notes shape asymmetry (backstop clause on AC-2, none on AC-5).** _Source:_ Blind Hunter (BH3). _Location:_ `_bmad-output/test-artifacts/traceability/1-9-coverage-matrix.json:41` + `:71`. _Nature:_ Fix O extended AC-2 level_notes with `"; schema-level uniqueness refine is the load-bearing complement"` backstop clause; Fix P left AC-5 level_notes bare. Blind Hunter reads this as broken symmetry. _Defer reason:_ AC-2 legitimately has schema-level uniqueness refine (Story 1.9 Task 3 InvariantsSchema.superRefine — a genuine load-bearing complement); AC-5 does not (the anchor-walker has no schema-refine equivalent for docs-orphan-anchor detection — it's walker-only). Adding a null/empty backstop clause to AC-5 would manufacture false parallel. Per-AC evidence class legitimately differs. _Carry-to:_ Story 1.16 when test runner replaces the level_notes+substrate_verification dual evidence system with actual test-runner-hosted coverage.
- **Coverage-matrix.json:51 AC-3 level_notes `"structural"` bare leaf.** _Source:_ Edge Case Hunter (EH2). _Location:_ `_bmad-output/test-artifacts/traceability/1-9-coverage-matrix.json:51`. _Nature:_ AC-3 level_notes stays bare `"structural"` after Fix P purged the same wording from AC-5 :71, making AC-3 the newly-sole bare leaf. _Defer reason:_ AC-3 IS spec-accurately structural-only per spec:152 (fs.readFile rejection → removed-from-source-only Drift branch); bare "structural" classifier is semantically precise. Expanding it to include CR-adversarial-backstop clause would be editorial. _Carry-to:_ If Fix T (trace.md:112 AC-3 Coverage header) lands the template-prefix expansion, consider co-landing an AC-3 level_notes expansion in the same commit as a complementary sibling edit — otherwise hold for Story 1.16.
- **Coverage-matrix.json:31 AC-1 level_notes bare `runner-hosted` + lacks `end-to-end evidence` suffix.** _Source:_ Edge Case Hunter (EH3). _Location:_ `_bmad-output/test-artifacts/traceability/1-9-coverage-matrix.json:31`. _Nature:_ AC-1 level_notes uses bare `runner-hosted` vocabulary + lacks the `"— end-to-end evidence"` suffix that Fix O/P introduced at :41 + :71. Vocabulary + shape drift. _Defer reason:_ Fix S closes the trace.md side of the runner-hosted cluster (3 sites: :175, :236, :484); coverage-matrix.json-side is a different file with different review cadence. Opening the coverage-matrix sweep (AC-1/AC-3/AC-6/AC-7 level_notes) expands Fix S scope beyond its iter-35-pre-admitted boundary and may re-enter surface-inflation. _Carry-to:_ Revisit if CR re-run #8 surfaces as a convergent (two-layer) finding, otherwise hold for Story 1.16 test-runner landing where level_notes becomes vestigial.
- **Coverage-matrix.json:81 AC-6 level_notes lacks `smoke (shell)` prefix.** _Source:_ Edge Case Hunter (EH4). _Location:_ `_bmad-output/test-artifacts/traceability/1-9-coverage-matrix.json:81`. _Nature:_ AC-6 level_notes reads `"CLI contract (smoke-verified via AC 4 path); workflow-wiring deferred"` — lacks the `"smoke (shell) — end-to-end evidence"` template prefix that AC-1/AC-2/AC-4/AC-5/AC-7 carry post-Fix-O/P. _Defer reason:_ AC-6 is CLI-contract-only per spec:41 carve-out (workflow-wiring deferred to Epic 13); level_notes correctly reflects this distinct evidence class. Prefixing with "smoke (shell) — end-to-end evidence" would obscure the AC-6-distinct-semantics (AC-6 is not smoke-evidence in the AC-1/AC-2/AC-4/AC-5 sense — it's CLI-contract satisfaction via transitive coverage through the AC-4 drift path). _Carry-to:_ Epic 13 when workflow-wiring lands and AC-6 graduates from CLI-contract-only to end-to-end-CI-exercised.
- **trace.md:106 AC-2 Recommendation retains `WAIVED` posture + scope carve-out language.** _Source:_ Edge Case Hunter (EH10). _Location:_ `_bmad-output/test-artifacts/traceability/1-9-invariant-sync-gate-runtime-tooling-reader-walker-drift-detector.md:106`. _Nature:_ § AC-2 Recommendation bullet reads `Defer source-tree auto-discovery... Defer unit coverage to Story 1.16. WAIVED.` Edge Case Hunter flags this as stale-vocabulary-post-Fix-O. _Defer reason:_ AC-2 Recommendation encodes the spec-accurate scope carve-out (source-tree auto-discovery DEFERRED per AC-2 spec line 23) + WAIVED trace-gate outcome (consistent with Story 1.7/1.8 precedent + zero-coverage substrate-stage pattern). The WAIVED/DEFERRED vocabulary belongs here (it describes the TRACE-GATE outcome); retagging as `load-bearing complement` would conflate the scope-carve-out signal with the schema-refine evidence classifier. Fix U (narrower) reframes only the backstop vocabulary at :105 + :145; :106 Recommendation stays intact. _Carry-to:_ No carry; this is spec-accurate description of the scope posture.

## Deferred from: code review RE-RUN of story-1.10 (2026-04-20)

_Story 1.10 iter-52 `/bmad-code-review (args: "2")` CR RE-RUN #2 against post-Fix-CR-1 diff (`535bee0..HEAD`, now including iter-51's 3-line delta: `packages/ui/tokens.schema.json:328` `required: ["light", "dark"]` + `packages/keel-invariants/src/invariants.manifest.ts:133` hash char-swap `abb5bc4c...e07ac → 15380636...b055e6`). Three-layer Ralph-hosted parallel fan-out. **Acceptance Auditor: PASS** (14 positive checks; CR-1 PATCH cleanly discharged; all 4 ACs satisfied). **Edge Case Hunter: PASS** (no unhandled edges — modesOverlay required guard closes the iter-50 convergent gap; hash-resync byte-integrity verified). **Blind Hunter: NEW_FINDINGS** (8 raw — 5 MEDIUM + 3 LOW; all single-layer without Edge or Auditor convergence per iter-36 doctrine)._

_Triage: **0 PATCH** (halving-to-zero trajectory achieved at round 2 — matches iter-36 residue-family catalogue's prediction that post-convergent-fix CR rounds collapse when only single-layer residue remains), **3 DEFER** (below — 2 reconfirmations of iter-50 DEFER class + 1 new story-spec editorial item), **5 DISMISS** (forward-compat `$def`s not dead; section-ID-enumeration vs publishable-slug prose conflation; frozen trace-artefact byte-count + line-pointer drift per iter-30 paper-trail rule; spec-intentional mode asymmetry). Closure: Story State `fixes-pending → done` per FR14n matrix row 10 single-transition edge._

- **Manifest `description` enumeration at `packages/keel-invariants/src/invariants.manifest.ts:128` omits `font` from the 7-group list.** _Source:_ Blind Hunter (iter-52 re-run; reconfirms iter-50 DEFER class). _Location:_ `packages/keel-invariants/src/invariants.manifest.ts:128` (`description: 'Design-token JSON Schema contract — DTCG-compatible; defines shape of every semantic + primitive token group (color / type / space / radius / motion / density / breakpoint) plus optional $modes overlays.'`) vs schema root `"required": ["color", "type", "font", "space", "radius", "motion", "density", "breakpoint"]`. _Nature:_ manifest description documents 7 semantic groups but the schema contract declares 8 (including `font`); readers consulting `INV-tokens-schema-contract.description` get a misleading inventory. _Defer reason:_ Spec Task 3 line 102 prescribes this exact 7-group description verbatim; dev-story cannot resolve without spec re-author. Doc-editorial follow-up (story-spec amendment). _Carry-to:_ Story 1.11 pre-dev SM review can land a one-line description extension (`…space / radius / motion / density / breakpoint / font …`) alongside the populator's source-population edits.
- **`leafFontFamily.$value` schema type `string` vs rationale doc prose "DTCG fontFamily stacks (arrays of family names with ordered fallbacks)".** _Source:_ Blind Hunter (iter-52 re-run). _Location:_ `packages/ui/tokens.schema.json:249-257` (`"$value": { "type": "string", "pattern": "^(\\{...\\}|[^{}]+)$" }`) vs `docs/invariants/tokens.md § Font`. _Nature:_ rationale prose documents the DTCG-canonical fontFamily value as an array of family-name strings with ordered fallbacks; schema declares value `type: string`. An array-shaped source value would fail schema validation. _Defer reason:_ AC 1 scope carve-out pins Story 1.10 to **structural only** — value-quality contract (including DTCG array-value shapes) is Story 1.11's populator scope. Same reasoning-class as iter-50 DEFER for leafCubicBezier / leafNumber / leafFontWeight portability. _Carry-to:_ Story 1.11 populator may extend leaves to `oneOf: [{type: string, pattern: …}, {type: array, items: {type: string}}]` when concrete fontFamily stacks land; OR narrow the rationale prose to single-string semantics if the fork-extension path prefers homogeneous string-only values.
- **Story-file stable-ID convention example at Dev Notes line 191** (`motion.scale.snap` → `TOKEN-motion-scale-snap` three-segment form) contradicts AC 2 scope carve-out at line 27 (`Three-segment IDs like TOKEN-motion-scale-snap are NOT used`). _Source:_ Blind Hunter (iter-52 re-run). _Location:_ `_bmad-output/implementation-artifacts/1-10-design-token-schema-semantic-rationale-contract.md` Dev Notes § Stable-ID convention illustration. _Nature:_ iter-43's pre-dev SM fix inline-corrected the AC 2 stable-ID example (`TOKEN-motion-scale-snap → TOKEN-motion-snap`) but left the Dev Notes slug-convention illustration using the pre-fix three-segment form as an explanatory foil for "what the epic's shorthand does NOT mean". _Defer reason:_ Dev-Notes illustrative foil vs shipped AC 2 scope carve-out normative text — the foil may be intentional pedagogy (showing the pre-fix form to clarify why it's rejected). Story-file editorial polish; not implementation-affecting. _Carry-to:_ Story 1.11 pre-dev SM review — if the illustration is copy-inherited into 1.11's drafting, convert it to explicit "DOES/DOES-NOT" table form (e.g., `motion.scale.snap → TOKEN-motion-snap (DOES)` vs `motion.scale.snap → TOKEN-motion-scale-snap (DOES NOT)`) for unambiguous pedagogy; or drop the illustration if redundant with the AC 2 carve-out narrative.

## Deferred from: code review of story-1.11 (2026-04-20)

_Story 1.11 (design-token source — Direction A baseline with motion + density scales) — iter-59 `/bmad-code-review (args: "2")`. Three-layer adversarial review; Acceptance Auditor returned **0 spec deviations** (4/4 AC MET, 4/4 Task MET). After iter-36 + iter-52 two-layer convergence-doctrine triage of 18 Blind Hunter + 16 Edge Case Hunter findings: **0 PATCH, 0 DECISION-NEEDED, 5 DEFER (below), 17 DISMISS**. All 5 defers are WCAG-AA-contrast or sRGB-gamut concerns that Story 1.12's emitter pipeline and Story 1.13's contrast gate naturally absorb. Closure: Story State `sm-verified → done` per FR14n matrix row 9 ZERO-PATCH edge. 7-iteration lifecycle held exactly (iter-53 drafted → iter-54 validated → iter-55 atdd-scaffolded → iter-56 in-dev → iter-57 traced → iter-58 sm-verified → iter-59 done)._

- **Dark-mode `status.<X>.fg` contrast failures against overridden `status.<X>.bg`.** _Source:_ Blind Hunter + Edge Case Hunter (convergent). _Location:_ `packages/ui/tokens.json:58-78` (light `status.*.fg`) + `:187-202` (dark `$modes.dark.status.*.bg` overrides). _Nature:_ story Task 1 explicitly mandates "keep `fg` values equal across modes", so the dark `$modes.dark` overlay remaps only `status.<X>.bg` — leaving light-tuned fg (e.g. `status.critical.fg = oklch(45% 0.20 15)`) paired against dark bg (`oklch(20% 0.08 15)`) at ECH-computed ratios info **3.35:1**, success **3.03:1**, warning **3.79:1**, error **2.78:1**, critical **2.37:1** (all below AA 4.5 threshold). Severity / state aliases inherit the same gap: `severity.low` 4.07, `severity.high` 3.44, `severity.critical` 2.67, `state.done` 3.69, `state.in-progress` 4.29. _Defer reason:_ source-layer authoring decision deferring contrast-retune to Story 1.13's WCAG AA gate + Story 1.12's emitter rendering. Story AC 1 only requires schema validation; contrast math is explicitly a Story 1.13 gate. _Carry-to:_ **Story 1.13** — add dark-mode `status.<X>.fg` remaps (e.g. `oklch(75% 0.14 <hue>)`) OR accept status fg inheritance with AA-failure signalling via the gate. **Story 1.12** emitter pipeline may also synthesize dark-mode fg via contrast-targeting algorithm.
- **Light-mode `status.info.fg` (4.45:1) + `status.warning.fg` (3.70:1) under AA 4.5 threshold.** _Source:_ Edge Case Hunter. _Location:_ `packages/ui/tokens.json:59-62` (info pair), `:67-70` (warning pair). _Nature:_ ECH computed `oklch(52% 0.14 230)` fg on `oklch(95% 0.03 230)` bg = 4.45:1 (info) and `oklch(58% 0.14 75)` fg on `oklch(95% 0.06 75)` bg = 3.70:1 (warning). Story AC 1 scope carve-out states "AA-contrast math as the gate; status/severity hues retuned within ±15°"; iter-56 authoring shipped values without live AA verification. _Defer reason:_ single-layer ECH finding with concrete math; contrast-retune is explicitly carve-out language for "status/severity hues retuned within ±15°" — Story 1.13 WCAG gate owns the retune. _Carry-to:_ **Story 1.13** contrast gate — retune info (230) and warning (75) fg within ±15° hue or adjust L/C downward for 4.5:1 compliance. One-shot fix candidate: drop L to ~48% for info.fg and ~52% for warning.fg (hold hue + chroma).
- **`text.accent` + `border.accent` fail AA on several surface pairings.** _Source:_ Blind Hunter + Edge Case Hunter (convergent). _Location:_ `packages/ui/tokens.json:51` (light `text.accent` = `{color.accent.default}`), `:56` (light `border.accent` = `{color.accent.focus}`), `:169-186` + `:524-536` (dark mode overlays without `accent` under `text` or `border`). _Nature:_ ECH computed `text.accent` (resolves to `accent.500 = oklch(54% 0.18 245)`) on dark `surface.default` **4.29:1** (fail), dark `surface.raised` **4.15:1** (fail), light `surface.raised` **4.31:1** (fail); only light `surface.default` passes at 4.71. `$modes.dark.color.text` + `.border` overlays omit `accent` remaps — only `accent.fg` is remapped. _Defer reason:_ same class as dark-mode status.fg defer above — substrate deliberately ships `accent.500` constant across modes; dark-mode text/border.accent contrast is Story 1.13 gate scope. _Carry-to:_ **Story 1.13** — three options: (a) add dark-mode `$modes.dark.color.text.accent` + `.border.accent` remaps pointing to a lighter accent variant (e.g. `{color.accent.400}`); (b) retune `accent.500` L upward; (c) emit `text.accent` as raised-surface-only constraint in Story 1.12 emitter with fallback to `text.primary` on default surfaces.
- **Surface slot collapse: `raised` / `inset` (light) + `raised` / `inset` / `overlay` (dark) alias to identical neutral values.** _Source:_ Blind Hunter + Edge Case Hunter (convergent). _Location:_ `packages/ui/tokens.json:42-44` (light `surface.{raised,inset,overlay}`) + `:168-172` (dark). _Nature:_ light `surface.raised` and `surface.inset` both alias `{color.neutral.100}`; dark `surface.raised` + `surface.inset` + `surface.overlay` all alias `{color.neutral.900}`. ECH computed dark `surface.inset` vs `surface.default` separation at 1.03:1 (near-invisible 5pp L delta). Debug Log + iter-58 pre-staged defense (b) document: visual separation comes from shadow + border machinery emitted by Story 1.12 pipeline. _Defer reason:_ source-layer collapse is intentional — neutral-ramp alias design + shadow-machinery differentiation. _Carry-to:_ **Story 1.12** emitter pipeline — render shadow tokens (e.g. inner-shadow for inset, drop-shadow for raised, elevation-shadow for overlay) so the three surface slots render visually distinct at the consumer layer. Alternative: add a shadow token group to tokens.json in a follow-up story and expand dark overlay to differentiate the three slots numerically.
- **OKLCH out-of-sRGB-gamut leaves: `status.warning.fg` (75°), `status.error.fg` (25°), `status.critical.fg` (15°) yield slightly-negative linear RGB components.** _Source:_ Edge Case Hunter. _Location:_ `packages/ui/tokens.json:68` (warning.fg), `:72` (error.fg), `:76` (critical.fg). _Nature:_ ECH computed `oklch(58% 0.14 75)` → linear RGB `[0.40, 0.15, -0.019]` (blue channel negative); `oklch(45% 0.20 15)` → `[0.39, -0.012, 0.028]` (green channel negative); `oklch(52% 0.18 25)` → `[0.49, 0.024, 0.027]` (tight but in-gamut). Browsers clip on render, altering perceived hue/chroma and shifting actual contrast vs computed. _Defer reason:_ gamut-mapping is rendering-layer concern, not source-layer; Direction A value-fidelity is preserved at source. _Carry-to:_ **Story 1.12** emitter — optional gamut-check + fallback-to-in-gamut-neighbour pass on emit (map OKLCH to nearest sRGB boundary preserving hue). **Story 1.13** contrast gate — gamut-map before AA math so the gate operates on rendered pairings rather than abstract OKLCH literals.

---

### Classification methodology (`/bmad-code-review (args: "2")` — "Create action items")

Three parallel adversarial layers per `.claude/skills/bmad-code-review/steps/step-02-review.md`: Blind Hunter (`bmad-review-adversarial-general`; diff-only, no project context), Edge Case Hunter (`bmad-review-edge-case-hunter`; diff + repo read access; path-enumeration method), Acceptance Auditor (diff + spec file + context docs; AC-violation-only).

Triage bucketing per `step-03-triage.md`:

- `patch` — fix unambiguous, no human input needed.
- `defer` — real issue, not actionable now (e.g. scope carve-out, spec-silent hardening).
- `dismiss` — false positive, by-design, or handled elsewhere.
- `decision_needed` — ambiguous choice (not applicable when auditor found zero AC violations).

Under adversarial-triage-is-default (per `.ralph/PROMPT_build.md § Story Lifecycle: "EVERY CR action item becomes a QUEUE fix task unless IP records defer: <reason> per item. Adversarial triage is the default."`): every non-dismissed finding must either get a `patch` fix task queued or an explicit `defer: <reason>` recorded. Story 1.8's 6 defers above are all either Story 1.9-enforcement-side scope or spec-silent hardening — no patches needed for the `sm-verified → done` transition.

## Deferred from: code review of 1-12-token-emitter-pipeline-web-css-tailwind-preset-tui-theme (2026-04-21)

_Story 1.12 (token emitter pipeline → web CSS + Tailwind preset + TUI theme) — iter-66 `/bmad-code-review (args: "2")`. Three-layer adversarial review on commit `c14b6df` code surface (Ralph-bookkeeping files excluded). Acceptance Auditor returned **3 spec deviations**. After iter-36+iter-52+iter-59 two-layer convergence-doctrine triage + iter-58+iter-65 ZERO-PATCH doctrine applied to AA1: **0 PATCH, 0 DECISION-NEEDED, 10 DEFER (below), 26 DISMISS**. All 10 defers route to Story 1.13 (sync-gate / schema-validation / reliability / observability absorption), Story 1.13+ spec amendments (AA1/AA2/AA3), reliability follow-up (REPO_ROOT), or Epic 7/3 consumer stories (fontSize line-height). Closure: Story State `sm-verified → done` per FR14n matrix row 9 ZERO-PATCH + 10 DEFER ceiling (matches iter-59 Story 1.11 precedent within "5-10 DEFER" target). 7-iteration lifecycle held exactly (iter-60 drafted → iter-61 validated → iter-62 atdd-scaffolded → iter-63 in-dev → iter-64 traced → iter-65 sm-verified → iter-66 done). **Story 1.12 THIRD cumulative ZERO-PATCH CR precedent** (matches Story 1.10 iter-52 RE-RUN + Story 1.11 iter-59 first-round)._

- **Source-SHA resolver command literal deviation from spec § AC 1 carve-out.** _Source:_ Acceptance Auditor (pre-staged defense at iter-64 IP + iter-65 SM ZERO-PATCH precedent). _Location:_ `packages/ui/scripts/generate-tokens.ts:309` uses `git log -1 --format=%h --abbrev=12 -- packages/ui/tokens.json`; spec § AC 1 carve-out prescribes `git rev-parse --short=12 HEAD -- packages/ui/tokens.json`. _Nature:_ structurally-impossible-literal — `rev-parse --short=12 HEAD -- <path>` returns repo HEAD regardless of path (the `--` is a rev/path separator in `rev-parse` syntax, not a log-filter arg), violating the same carve-out paragraph's "NOT `HEAD` of the repo" constraint. Intent-matching `git log` returns the file's latest commit SHA (`6b1790a02adb`) — satisfies AC 1 prose literally. Deviation documented at 3 sites (emitter source-file header + Debug Log References row + Change Log v1.2 row). _Defer reason:_ ZERO-PATCH doctrine (iter-58+iter-65) — literal is self-contradictory, impl is correct per AC prose. Spec text should be harmonised at next opportunity. _Carry-to:_ **Story 1.13** pre-dev drafting — amend Story 1.12 § AC 1 carve-out literal command to the intent-matching `git log` form so future CR scans see matching spec + impl; or a dedicated spec-editorial story.
- **`.prettierignore` entry bypasses Task 6 prettier-normalization of two emitted outputs.** _Source:_ Acceptance Auditor + Blind Hunter (AA2 + BH#12 convergent). _Location:_ `.prettierignore:13-16` adds `packages/ui/src/tokens.css` + `packages/ui/tailwind.preset.ts`; spec Task 6 prescribes `pnpm exec prettier --write packages/ui/scripts/generate-tokens.ts packages/ui/src/tokens.css packages/ui/tailwind.preset.ts packages/devbox/tui/theme.py ...` before format:check. _Nature:_ prettier normalises OKLCH decimals (`0.20 → 0.2`), converts attribute-selector quote style (`"dark" → 'dark'`), and converts JSON-stringify double-quotes to single-quotes — none of which the emitter reproduces natively; running prettier on emitter outputs would break AC 2 byte-identical round-trip guarantee. Rationale documented in Debug Log ("emitter owns canonical byte-form of generated files; prettier owns the emitter source"). Task 6 format:check is green because outputs are ignored, not because they match prettier — AC 2 byte-identical holds because determinism is owned by emitter alone. _Defer reason:_ multi-layer-corroborated authoring-choice finding per iter-59 doctrine; fix is a spec-text amendment, not a code change. _Carry-to:_ **Story 1.13** (pre-merge source-output sync gate) spec drafting — amend Story 1.12 Task 6 prose to exclude emitted outputs from the prettier-normalization list + document the `.prettierignore` entry as part of the "emitter owns canonical byte-form" contract clause.
- **Manifest entry `INV-tokens-emitter` description paraphrases spec Task 5 trailing parenthetical.** _Source:_ Acceptance Auditor only (AA3). _Location:_ `packages/keel-invariants/src/invariants.manifest.ts:155` uses `... (emitter re-run + diff).`; spec Task 5 line 163 prescribes `... (emitter --check mode).`. _Nature:_ cosmetic description-text deviation — description field has no functional impact on `INV-tokens-emitter` contract (contract is `contentHash` pin + `sourcePath` + `id`). iter-63 dev described what the sync gate is expected to actually do (invoke emitter, diff outputs vs tracked state), not what its CLI verb is. _Defer reason:_ Story 1.13's sync-gate invocation nomenclature is TBD; reconciling now risks thrashing if Story 1.13 lands `re-run + diff` verb. _Carry-to:_ **Story 1.13** drafting — formalise sync-gate invocation nomenclature (`--check` vs `re-run + diff`) + align `invariants.manifest.ts:155` description text at that time.
- **`resolveSourceSha` invoked 4× per emitter run — TOCTOU window + perf.** _Source:_ Blind Hunter + Edge Case Hunter (BH#2 + EH convergent). _Location:_ `packages/ui/scripts/generate-tokens.ts:309` called from `emitCss:409`, `emitTailwind:439`, `emitPython:582`, `main:624`. _Nature:_ each of the three emit stages plus `main` independently spawns `execFileSync('git', ...)` — 4 subprocess spawns per run. Theoretical TOCTOU: concurrent `tokens.json` rewrite mid-emission could stamp three outputs with two different SHAs. Not a correctness defect under normal invocation (Story 1.12 determinism smokes run without concurrent edits; all three outputs carry matching SHA `6b1790a02adb`). _Defer reason:_ authoring factoring decision per iter-59 doctrine; no AC violation. _Carry-to:_ **Story 1.13** (pre-merge sync gate) — hoist SHA resolution to `main` once and pass through as a param; benefit amplified when sync-gate invokes emitter in `--check` mode (single subprocess vs four).
- **`resolveSourceSha` silent fallback to `uncommitted-<content-hash>` swallows multiple failure modes.** _Source:_ Blind Hunter + Edge Case Hunter (BH#1 + EH{git-missing, maxBuffer, empty-stdout, whitespace-stdout, non-hex-stdout} — 5-vector convergent). _Location:_ `packages/ui/scripts/generate-tokens.ts:307-321`. _Nature:_ fallback activates on any `execFileSync` non-zero exit OR empty stdout, treating "git not on PATH / stdlib maxBuffer exceeded / git output malformed / file untracked" as a single branch with no observability. Under normal conditions (git present + file tracked), fallback is never reached. _Defer reason:_ multi-layer-corroborated reliability gap per iter-59 doctrine; not triggered under Story 1.12 AC-validation scope. _Carry-to:_ **Story 1.13** pre-commit gate observability — distinguish `uncommitted` vs `git-missing` vs `stderr-error` in the provenance header (e.g. `source-sha: git-unavailable-<hash>`) so CI pipelines can fail-fast on environment misconfiguration instead of silently switching to content-hash provenance.
- **Alias-cycle detector may false-positive on diamond alias DAGs.** _Source:_ Blind Hunter + Edge Case Hunter (BH#4 + EH{diamond-false-positive} convergent). _Location:_ `packages/ui/scripts/generate-tokens.ts:360-384` (`resolveValue` `visited` Set handling). _Nature:_ `visited` accumulates alias paths via `new Set([...visited, seg])` but the set tracks segments by path-string, not by canonical leaf identity — a diamond graph (A→B, A→C, B→D, C→D resolved within a single leaf's chain) could trigger false-positive "cycle" error. Not triggered by current `tokens.json` (no diamond aliases present; smoke 2's 2-hop chains exercised successfully). _Defer reason:_ multi-layer-corroborated path gap per iter-59 doctrine; not triggered. _Carry-to:_ **Story 1.13** test harness — add synthetic diamond-DAG fixture to exercise alias-cycle detector; key `visited` on resolved-leaf identity rather than alias-path strings.
- **`walkLeaves` silently drops non-leaf, non-object values.** _Source:_ Blind Hunter + Edge Case Hunter (BH#5 + EH{array-as-node, non-object-primitive} convergent). _Location:_ `packages/ui/scripts/generate-tokens.ts:342-354`. _Nature:_ if `tokens.json` contains an array, a raw string, or a stray number at a non-leaf path, `walkLeaves` skips it without warning; regressions from typos-in-source are invisible. Trusted DTCG input today (all sources Story 1.11-frozen and schema-validated); not triggered. _Defer reason:_ multi-layer-corroborated schema-validation gap per iter-59 doctrine. _Carry-to:_ **Story 1.13** schema-validation gate — `tokens.json` must schema-validate before the emitter runs so malformed sources fail fast; emitter can then trust its input invariants.
- **`REPO_ROOT` resolver brittle — 3 hardcoded `..` hops + no `realpath` guard.** _Source:_ Blind Hunter + Edge Case Hunter (BH#7 + EH{fileURLToPath-no-realpath, hardcoded-depth} convergent). _Location:_ `packages/ui/scripts/generate-tokens.ts:278-281`. _Nature:_ `REPO_ROOT = path.resolve(dirname(fileURLToPath(import.meta.url)), '..', '..', '..')` assumes the emitter lives exactly at `packages/ui/scripts/`. If moved, symlinked, or invoked via pnpm-store path, `REPO_ROOT` resolves silently wrong. Not triggered in Story 1.12 scope (emitter ran successfully 5× across determinism smokes). _Defer reason:_ multi-layer-corroborated portability concern per iter-59 doctrine. _Carry-to:_ reliability follow-up story OR **Epic 3** (packages/ralph install-boundary scope) — discover repo root via `git rev-parse --show-toplevel` OR upward `package.json` walk with `name === 'ralph-bmad'` sentinel; fall back to hardcoded depth.
- **`parseInt` on breakpoint values silently accepts non-`px` suffix + NaN leak to Python literal.** _Source:_ Blind Hunter + Edge Case Hunter (BH#9 + EH{rem-em-suffix, NaN-on-garbage} convergent). _Location:_ `packages/ui/scripts/generate-tokens.ts:571` (`collectPyGroups` breakpoint branch). _Nature:_ `parseInt(resolved.replace(/px$/, ''), 10)` accepts `640rem` as `640` (silently drops `rem`), accepts `abc` as `NaN` (which stringifies as bare `NaN` in the Python output — not a valid literal without `float('nan')` wrap; import-time `SyntaxError` in `theme.py`). Current `tokens.json` breakpoints are all `px`-suffixed (enforced by iter-56 source authoring); not triggered. _Defer reason:_ multi-layer-corroborated schema-validation gap per iter-59 doctrine. _Carry-to:_ **Story 1.13** schema-validation — regex-pin breakpoint `$value` to `/^\d+px$/` or equivalent; emitter-side defense: throw if resolved doesn't match `/^\d+px$/` instead of silent NaN.
- **`emitFontSize` hardcodes `lineHeight: "1.5"` for every scale step.** _Source:_ Blind Hunter + Edge Case Hunter (BH#19 + EH{hardcoded-line-height} convergent). _Location:_ `packages/ui/scripts/generate-tokens.ts:504`. _Nature:_ every size in the Tailwind preset receives the same 1.5 line-height regardless of source; ux-design-specification.md Direction A does not specify per-scale line-heights (tight headlines / generous body / compact captions). Authoring choice at iter-63 dev — Tailwind v4 `fontSize` tuple form supports `['size', 'lineHeight']` but iter-56 source does not populate per-scale line-heights. Not an AC violation — AC 4 does not mandate per-scale line-heights. _Defer reason:_ multi-layer-corroborated authoring-choice finding per iter-59 doctrine (DEFER not PATCH). _Carry-to:_ **Epic 7 Story 7-1** (`packages/ui` scaffolding + token consumption) — reveal whether consumer components need ramp-specific line-heights; if yes, extend `tokens.json` `type` group with per-scale `lineHeight` leaves in a follow-up source-layer story and remap emitter to pull per-scale values. OR **Epic 3 Story 3-X** (TUI theme consumption) — Textual line-height is row-based; may not exercise this gap at all.

## Deferred from: code review of 1-13-token-quality-gates-schema-validation-wcag-aa-contrast-source-output-sync (2026-04-21)

_Story 1.13 (token quality gates — schema validation + WCAG AA contrast + source-output sync) — iter-73 `/bmad-code-review (args: "2")`. Three-layer adversarial review on commit `8f5d6f4` code surface (`.ralph/tmp/story-1-13-cr-diff.patch`; Ralph-bookkeeping files excluded). Blind Hunter ~30 findings diff-only; Edge Case Hunter ~80 findings diff + project read (path-enumeration method); Acceptance Auditor 14 spec deviations (5/6 ACs MET, 6/7 Tasks MET). After iter-36+iter-52+iter-59+iter-65+iter-66 two-layer convergence-doctrine triage + iter-58+iter-65+iter-72 ZERO-PATCH doctrine applied to authoring-choice findings + iter-71 architectural-defer precedent: **0 PATCH, 0 DECISION-NEEDED, 13 DEFER (below), ~112 DISMISS**. DEFER count within iter-66 prior forecast (10-15 + iter-71 post-commit-SHA-drift +1 = 11-16 ceiling at gate-authoring scale). Closure: Story State `sm-verified → done` per FR14n matrix row 9 ZERO-PATCH + 13 DEFER. 7-iteration lifecycle held exactly (iter-67 drafted → iter-68 validated → iter-69 atdd-scaffolded → iter-70 in-dev → iter-71 traced → iter-72 sm-verified → iter-73 done). **Story 1.13 FOURTH cumulative ZERO-PATCH CR precedent** (Story 1.10 iter-52 RE-RUN + 1.11 iter-59 + 1.12 iter-66 + 1.13 iter-73). **Compound-ZERO-PATCH satisfied — all 5 conditions (a)-(e) held through full 7-layer lifecycle.**_

- **Post-commit SHA drift on emitter outputs.** _Source:_ Blind Hunter + Edge Case Hunter (convergent; pre-flagged at iter-71 trace). _Location:_ `packages/ui/scripts/generate-tokens.ts` `resolveSourceSha` + `makeProvenanceHeader`. _Nature:_ emitter SHA-resolver uses `git log -1 --format=%h --abbrev=12 -- packages/ui/tokens.json` to stamp outputs. At dev-time (pre-commit), the resolver returns the PREVIOUS commit SHA because the new commit hasn't landed. After the commit lands (post-commit), re-running `--check` computes the NEW SHA → provenance header byte-differs → sync-gate reports drift on unchanged source. Inline iter-71 fix: re-emit + commit-again. Architectural bug, not a Story 1.13 regression — the Story 1.12 SHA-resolver contract carries the defect. _Defer reason:_ HIGH-severity but architectural; fix requires either (a) switch provenance identifier to content-hash-of-source (deterministic + uncommitted-safe — PREFERRED); (b) document dual-commit pattern + add post-commit CI hook; (c) accept + document as known limitation. Fix design warrants its own story. _Carry-to:_ **Epic 3** reliability (or new Story 1.17-TBD) — ship option (a) with a content-hash provenance header that eliminates the dual-commit workflow entirely.
- **Pre-commit `files:` regex triggers only on `packages/ui/tokens.json`; schema/contrast gates don't fire on schema-only or source-file-only edits.** _Source:_ Blind Hunter + Edge Case Hunter (convergent). _Location:_ `.pre-commit-config.yaml:84,91` — `files: ^packages/ui/tokens\.json$` on both `tokens-schema` and `tokens-contrast` hooks. _Nature:_ editing `tokens.schema.json` (Story 1.10-frozen schema) OR `color-math.ts` / `check-tokens-contrast.ts` (the gate's own source) does NOT re-trigger the gates at pre-commit. Pre-merge `keel-invariants:check-all` does catch drift via manifest hash + sync-gate, but the pre-commit coverage hole exists. _Defer reason:_ scope carve-out per Story 1.13 § AC 5 pre-commit tier budget (≤10s; running the full gate on every schema/src edit is out of budget); deliberate authoring choice. _Carry-to:_ future hardening story (pre-commit `files:` regex union for schema + gate sources with caching) or CI-side enforcement (pre-merge slow `keel-invariants:check-all` covers the gap today).
- **`parseOklch` accepts unitless L without `%`; L>1 produces silent garbage luminance.** _Source:_ Blind Hunter + Edge Case Hunter (convergent). _Location:_ `packages/keel-invariants/src/color-math.ts` `parseOklch` regex + L-extraction. _Nature:_ regex `^oklch\(\s*([\d.]+)(%?)\s+...` makes `%` optional. `oklch(50 0.18 245)` (typo dropping `%`) parses to L=50 (not 0.5), producing enormous LMS values that hard-clamp to [0,1] — downstream contrast math runs on nonsense luminance with no warning. Schema gate would catch this via `leafColor` OKLCH pattern (requires `%`) so trigger is unreachable in practice, but defence-in-depth is absent. _Defer reason:_ input-validation robustness gap; schema gate already enforces the `%` at the source layer. _Carry-to:_ Story 1.17-TBD color-math hardening — tighten `parseOklch` regex to reject unitless L + surface an `oklch-parse-error` finding kind distinct from ratio failures.
- **`gamutMap` 3-iteration cap + silent hard-clamp may skew luminance on severely out-of-gamut OKLCH leaves.** _Source:_ Blind Hunter + Edge Case Hunter (convergent; carry-forward from iter-59 Story 1.11 defer #5 contrast-side and iter-71 post-staged). _Location:_ `packages/keel-invariants/src/color-math.ts` `gamutMap` (3-iteration chroma-reduction loop then hard-clamp). _Nature:_ `oklch(78% 0.20 15)` (dark-mode `status.critical.fg`) at high L + high C in red direction may need >3 iterations to reach gamut. After iter-3, hard-clamp applies per-channel — changing luminance non-uniformly. The gate's ratio then diverges from what a CSS-Color-4-compliant browser would render (browsers use binary-search gamut mapping). No `isInGamut` post-clamp check; no `gamut-mapped-to-clamp` finding kind. _Defer reason:_ multi-layer-corroborated authoring-choice per iter-59 doctrine — algorithm is deterministic + fixes Story 1.11 CR defer #5 (OKLCH out-of-gamut leaves); advanced convergence is Epic 7 consumer concern. _Carry-to:_ **Epic 7** advanced color pipeline OR Story 1.17-TBD — replace `C*=0.9` schedule with CSS Color 4 binary-search gamut mapping; add `mapped-to-clamp` kind in finding output.
- **`parseOklch` alpha channel silently dropped; compositional contrast not measured.** _Source:_ Edge Case Hunter (single-layer; upgraded by convergence with Blind Hunter `parseOklch` pair). _Location:_ `packages/keel-invariants/src/color-math.ts:23` alpha group `(?:\s*\/\s*[\d.]+%?)?` is non-capturing. _Nature:_ `oklch(50% 0.18 245 / 0.5)` parses to L=50%, C=0.18, H=245; alpha discarded. Semi-transparent foreground over a known bg would show a compositional colour with lower effective contrast; the gate measures the un-composited fg. Current tokens.json has no alpha-bearing leaves (source schema disallows via leafColor pattern) — gap unreachable today but robustness concern for future overlay modes. _Defer reason:_ authoring-style input-validation concern; not triggered by Story 1.11-frozen source. _Carry-to:_ **Epic 7** advanced color pipeline — add alpha composition + warn on alpha-bearing leaves in the gate when the source schema is relaxed to allow rgba-style colors in overlays.
- **SHA resolver conflates `exit-nonzero empty-stderr` with `exit-0 empty-stdout` — both map to `uncommitted-<hash>`.** _Source:_ Blind Hunter + Edge Case Hunter (convergent). _Location:_ `packages/ui/scripts/generate-tokens.ts` `resolveSourceSha` catch-block. _Nature:_ spec AA1 design distinguished 4 branches (`<12-hex-sha>` / `git-unavailable-` / `stderr-error-` / `uncommitted-`); impl's catch handles `ENOENT` → `git-unavailable-`, `stderr.trim().length > 0` → `stderr-error-`, else → `uncommitted-`. A broken git that exits non-zero with empty stderr (unusual but possible — signal termination, git hard-killed) falls to the `uncommitted-` branch, losing the AA1 distinction. _Defer reason:_ rare failure mode, pragmatic tradeoff; architectural fix needs design around `execFileSync` error shape. _Carry-to:_ emitter reliability follow-up (Epic 3 or Story 1.17-TBD) — enumerate `err.code` / `err.signal` / `err.status` branches explicitly; add `ci-misconfig-<hash>` tag for the empty-stderr non-zero-exit case.
- **Pair-enumeration incomplete — missing `accent.fg × accent.*`, `accent.focus × surface.*`, `severity × surface.{raised,inset,overlay}`, `state × non-default`, `text.accent × surface.{inset,overlay}`, `status.*.fg × surface.*` cross-surface pairs.** _Source:_ Edge Case Hunter × 5 findings + Acceptance Auditor (convergent). _Location:_ `packages/keel-invariants/src/check-tokens-contrast.ts` `PAIRS` table (51 entries). _Nature:_ `accent.fg` (near-white `oklch(99% 0 0)`) is used as foreground on accent buttons; no pair enumerates `accent.fg × accent.default` or `accent.fg × accent.600`. `accent.focus` is a WCAG 1.4.11 UI-component indicator not tested against surfaces. `severity.*` and `state.*` only tested against `surface.default` (tooltip/menu-on-raised scenarios uncovered). `text.accent` only paired with `surface.default`/`surface.raised`, not `inset`/`overlay`. `status.*.fg` only paired with its own `status.*.bg`, not free-surface rendering. _Defer reason:_ authoring-choice scope (spec pair-enumeration table was intentionally finite at drafting time; full cartesian is pre-commit budget-expensive); contrast-gate completeness follow-up. _Carry-to:_ **Story 1.17-TBD** contrast-gate completeness — expand PAIRS to include accent.fg × accent.* / accent.focus × surface.* / severity × raised/inset/overlay / state × raised / text.accent × inset/overlay / status.*.fg × surface.* cross-surface; budget audit vs pre-commit ≤500ms target.
- **`border.default × surface.*` pairs dropped from gate by dev reclassification ("decorative separator not WCAG 1.4.11 UI component"); not reflected in spec text.** _Source:_ Acceptance Auditor (single-layer spec deviation) + Edge Case Hunter (implicit). _Location:_ `packages/keel-invariants/src/check-tokens-contrast.ts:228-259` (impl omits) vs spec line 127 (enumeration table mandates 4 `border.*` pairs at threshold 3.0). _Nature:_ spec mandated border.default × surface.default/raised at threshold 3.0 (per WCAG 1.4.11 non-text UI component). Dev iter-70 dropped both `border.default` pairs citing "decorative separator between same-family surfaces is not a WCAG-1.4.11 UI-component"; kept only `border.accent × surface.*` at threshold 3.0 (focus indicator). Ratio computation with current tokens showed `border.default` pairs failing at 1.11-1.31 vs threshold 3.0 — dev reclass avoided red-on-landing at the cost of spec deviation. Recorded in dev IP + Debug Log inline comment, not in spec text. _Defer reason:_ authoring-choice finding per iter-59 doctrine; dev rationale is defensible but should be captured as a spec amendment. _Carry-to:_ spec editorial amendment (Story 1.13 v1.7 Change Log row OR new spec-amendments story) — harmonise spec pair-table line 127 with the "decorative-separator-not-UI-component" distinction + cite WCAG 1.4.11 scope.
- **`color.accent.500` (54→50%) + `color.accent.600` (46→42%) retunes applied beyond Task 2 retune table.** _Source:_ Acceptance Auditor (single-layer spec deviation). _Location:_ `packages/ui/tokens.json:22-31` (retuned leaves with `$description` citing Story 1.13 retune). _Nature:_ Task 2 retune table listed only `color.status.info.fg` (52→42%) + `color.status.warning.fg` (58→44%) for light-mode retunes. Dev iter-70 added accent.500 + accent.600 L-ramp retunes citing "text-on-raised-surface headroom" (Debug Log). Retunes land in source alongside `$description` provenance + are documented in manifest INV-tokens-source hash + emitter re-run. Scope decision is sound (resolve text.accent × surface.raised 4.47 light failure) but not spec-authorised. _Defer reason:_ over-scope retune documented in-place with rationale; spec v1.7 amendment should reflect. _Carry-to:_ spec editorial amendment — add light-mode accent.500 + accent.600 entries to Task 2 retune table (or new § AC 2 carve-out subsection "accent-ramp headroom retune") with cited reasoning + pre/post ratios.
- **`INV-tokens-contrast-check` description paraphrases spec (pair-ordering + added `border.accent × surface` + "mode-aware walker" wording).** _Source:_ Acceptance Auditor (single-layer) + Blind Hunter implicit (manifest-paraphrase is iter-66 AA3 pattern). _Location:_ `packages/keel-invariants/src/invariants.manifest.ts:170-171`. _Nature:_ spec Task 5 line 199 mandated category order `text × surface / severity × surface / state × surface / status.fg × status.bg / accent × surface`; impl reordered to `text × surface / status.fg × status.bg / severity × surface / state × surface / accent × surface / border.accent × surface pair`. Added `border.accent × surface` addendum + replaced `alias-flatten walker as the emitter` with `mode-aware walker (dark overlay wins when present, else base; aliases then flatten against base)`. Functional contract held by `contentHash` pin + `sourcePath` + `id`; description field is cosmetic. _Defer reason:_ iter-66 AA3 precedent — manifest description paraphrase under ZERO-PATCH doctrine; harmonise at editorial pass. _Carry-to:_ spec editorial or dedicated manifest-description tightening — align `invariants.manifest.ts:170-171` text to spec Task 5 line 199 OR amend spec to match impl's more-accurate wording.
- **`leafBreakpoint` regex has redundant nested `^...$` inside outer `^...$` alternation.** _Source:_ Blind Hunter + Edge Case Hunter + Acceptance Auditor (3-layer convergent; functionally harmless). _Location:_ `packages/ui/tokens.schema.json:55` — `"pattern": "^(\\{[a-z][a-z0-9]*(\\.[a-z0-9][a-z0-9-]*)+\\}|^\\d+px$)$"`. _Nature:_ inner `^\d+px$` within outer `^...$` alternation works correctly (ECMA regex accepts redundant anchors) but is authoring imprecision; `^(\\{...\\}|\\d+px)$` is the clean form. Also permits `0px`, `00000px`, etc. (no leading-zero forbid), and does not permit `1.5px`, `16rem`, `4em` (deliberate narrowing). _Defer reason:_ cosmetic; schema contract functionally correct. _Carry-to:_ schema hygiene pass (Story 1.17-TBD or editorial amendment) — collapse to `^(\\{...\\}|\\d+px)$`; optionally forbid leading zeros via `[1-9]\d*|0`.
- **Dark-mode `severity.*` + `state.*` overlays authored as explicit OKLCH literals instead of alias propagation through `status.*.fg`/`accent.default`.** _Source:_ Edge Case Hunter + Acceptance Auditor (convergent). _Location:_ `packages/ui/tokens.json:211-222` — `$modes.dark.color.severity.{low,medium,high,critical}` + `$modes.dark.color.state.{pending,in-progress,blocked,done}` as literal OKLCH rather than `{color.status.*.fg}` / `{color.accent.default}` aliases. _Nature:_ spec § AC 2 carve-out line 148 expected severity + state to ALIAS into `color.status.*.fg` / `color.accent.default` — "dark-mode retunes propagate automatically through the alias graph to severity + state pairs". Impl converted to explicit literals because dark-overlay `severity.low` aliasing to `{color.status.info.fg}` would pick up LIGHT-mode `status.info.fg` (since `resolveAgainstBase` doesn't consult the overlay for alias targets) — literal overlay was the pragmatic fix. Future retunes of `status.*.fg` will NOT propagate to dark `severity.*` silently. No invariant enforces the "dark severity tracks status" relationship. _Defer reason:_ authoring-style deviation with implementation-rational cause; fix is either (a) mode-aware alias resolution in walker (scope-creep) or (b) document the break-the-cascade pattern in spec. _Carry-to:_ Story 1.17-TBD authoring-style alignment — either extend alias resolver to prefer overlay when resolving alias targets, or pin spec language that dark-overlay severity/state must be authored as literals (not aliases).
- **Ajv `strict: true` mode could fail-closed on `tokens.schema.json` edits introducing unknown keywords.** _Source:_ Blind Hunter + Edge Case Hunter (convergent). _Location:_ `packages/keel-invariants/src/check-tokens-schema.ts:33` — `new Ajv2020({ allErrors: true, strict: true })`. _Nature:_ strict-mode Ajv throws at `ajv.compile(schema)` on unknown keywords (custom DTCG vocabulary, stray `$comment` at unusual levels, unrecognised format). The gate's try/catch swallows this to `{ status: 'error', message }` + exit 1, indistinguishable from "token drift" at the CI layer. A harmless schema edit could hard-fail the gate. _Defer reason:_ robustness concern; fixed under current Story 1.10 schema (Ajv recognises all current keywords). _Carry-to:_ schema-gate robustness follow-up — distinguish `status: 'schema-error'` from `status: 'error'` (Ajv compile failure vs thrown exception); surface Ajv's `addKeyword` / `addVocabulary` hook if DTCG vocabulary grows.


## Deferred from: code review of 1-14-release-please-monorepo-config-single-bundled-mode (2026-04-21)

_Story 1.14 (`release-please monorepo config — single-bundled mode`) — iter-80 `/bmad-code-review (args: "2")`. Three-layer adversarial review (Blind Hunter 20 findings diff-only + Edge Case Hunter ~27 findings diff + repo-read + Acceptance Auditor 6/6 ACs MET / SCOPE-CARVED). FOURTH cumulative ZERO-PATCH CR precedent (Stories 1.11 iter-59 + 1.12 iter-66 + 1.13 iter-73 + 1.14 iter-80). 10 DEFER + ~13 DISMISS after convergence-doctrine triage. All 10 items route to Story 13.5 (6), spec amendments (3), or Epic 3 reliability (1)._

- **Root `.` has no `version` field in `package.json`; `release-type: node` expects `package.json.version`.** _Source:_ Edge Case Hunter + Blind Hunter (convergent). _Location:_ `package.json` (root — verified absent: `private: true`, no `version` field) vs `.github/release-please-config.json:20` (pins `. : { component: keel, package-name: keel }`) + `.github/.release-please-manifest.json:2` (`.`: `0.0.0`). _Nature:_ release-please's `node` release-type reads/writes `package.json.version`; the root has no such field. On first-run release-please may throw, synthesize a `version` key (mutating root shape), or fall back to `release-as`-style behavior (undocumented). _Defer reason:_ runtime-only failure surfaces at Story 13.5 first-run; substrate-level fix options are architectural (add root `"version": "0.0.0"`, OR remove `.` entry from config+manifest, OR switch root to `release-type: manifest` while keeping workspaces as `node`). _Carry-to:_ Story 13.5 workflow delivery — resolve root-path handling in config OR root package.json shape before first Release PR.
- **`linked-versions.components: ["keel"]` plugin shape — 17 packages all carry `component: "keel"` but `linked-versions.components` lists only one component name.** _Source:_ Blind Hunter + Edge Case Hunter (convergent). _Location:_ `.github/release-please-config.json:8` (`plugins: [{ type: linked-versions, groupName: keel, components: [keel] }]`) vs `:21-37` (17 packages × `component: keel`). _Nature:_ release-please's `linked-versions` plugin uses `components` as the list of MEMBER components to link. Listing one component "keel" when 17 packages share `component: "keel"` may deduplicate to "group of one member," leaving 16 packages un-linked and producing 16 separate Release PRs despite `separate-pull-requests: false`. Alternatively, release-please may interpret this correctly as "one group spanning all 17 packages bearing component: keel." Empirical validation needed. _Defer reason:_ plugin semantics not verifiable without live release-please invocation; Story 13.5 workflow first-run is the natural test bed. _Carry-to:_ Story 13.5 workflow delivery — empirically validate single-bundled PR shape on first `push` to `main`; adjust plugin config if 16 separate PRs surface.
- **`docs/invariants/release.md` § Commit-type → semver mapping table has three semantic errors.** _Source:_ Blind Hunter + Edge Case Hunter (convergent). _Location:_ `docs/invariants/release.md:34-46`. _Nature:_ (a) `bump-patch-for-minor-pre-major` flag description at line 14 + 43 misstates the flag's effect — per release-please docs the flag downgrades pre-1.0 BREAKING-change minor → patch, not `feat:` → patch; (b) table row `perf:`, `refactor:`, `docs:` → patch contradicts release-please default (only `feat:` emits minor, `fix:` emits patch, BREAKING emits major; `perf:`/`refactor:`/`docs:` are non-bumping and appear only in changelog); (c) hidden-types row `chore:`, `test:`, `build:`, `ci:` claims "no user-visible bump" which conflates `changelog-sections` visibility with bump arithmetic — release-please's bump classifier is independent of section visibility. _Defer reason:_ spec-authoring error documented in release.md; no impl-side fix needed (config flags are correct); spec amendment needed. _Carry-to:_ release.md spec amendment — rewrite § Commit-type → semver mapping to correctly describe what each flag governs + split non-bumping types into their own row.
- **Missing `bootstrap-sha` / `initial-version` config pins — first-run release-please scans full git history.** _Source:_ Blind Hunter + Edge Case Hunter (convergent). _Location:_ `.github/release-please-config.json` (top-level; no `bootstrap-sha` or `initial-version` field present). _Nature:_ release-please on first run against a manifest-based single-bundled config with every entry at `0.0.0` and no existing tags will scan the full git history from the first commit to find conventional-commit bumps. The first Release PR may be enormous / slow / cross-cutting pre-invariant-discipline commits. Also: `0.0.0` starting state interacts idiosyncratically with `bump-minor-pre-major` (whether `feat:` emits `0.1.0` or `0.0.1` at the bootstrap boundary). _Defer reason:_ runtime-only concern; Story 13.5 workflow owns the pin. _Carry-to:_ Story 13.5 workflow delivery — pin `bootstrap-sha: <merge-commit-of-Story-1.14>` + optionally `initial-version: "0.1.0"` to sidestep the `0.0.0` bootstrap boundary.
- **Missing release-PR ergonomic + safety config — no `pull-request-title-pattern`, header, labels, `draft`, or `prerelease`.** _Source:_ Blind Hunter. _Location:_ `.github/release-please-config.json` (top-level). _Nature:_ the config omits `pull-request-title-pattern` (defaults may collide with repo's `chore/*` / `feat/*` PR-title conventions), `pull-request-header`, release-PR labels (important for triage automation), and `draft: true` / `prerelease: true` safety flags. The moment Story 13.5 merges the first Release PR, release-please auto-publishes a real GitHub Release at whatever semver bumps have accumulated — unreviewed. _Defer reason:_ safety + ergonomics not load-bearing at substrate-authoring time; Story 13.5 workflow owns the first-run safety gate. _Carry-to:_ Story 13.5 workflow delivery — before merging the first Release PR set `draft: true` or `prerelease: true`, add release-PR labels (`release`, `release-please`), pin a title pattern, and author a header.
- **Single-bundled hash-cascade — release-please rewrites `.github/.release-please-manifest.json` atomically on Release-PR merge, invalidating `INV-release-please-manifest` pinned `contentHash` (`4df2aacf…`).** _Source:_ Edge Case Hunter. _Location:_ `packages/keel-invariants/src/invariants.manifest.ts:197` (INV-release-please-manifest). _Nature:_ linked-versions single-bundled merge atomically updates every manifest key; contentHash drift at the Story 1.9 sync-gate will trigger on the next merge unless something regenerates the pin. No automation wires this. Similar cascade exists for `INV-release-please-config` when a fork edits the config per `release.md § Fork extension`. _Defer reason:_ cascade behaviour surfaces first at Story 13.5; one fix-class (post-merge hook to regenerate hash + amend invariants.manifest.ts) is Story 13.5 scope; another (exclude manifest file from content-hash pinning and pin shape via schema check) is a substrate architectural choice. _Carry-to:_ Story 13.5 workflow delivery — add post-Release-PR-merge hash-regen hook that updates `invariants.manifest.ts` for `INV-release-please-manifest` in the Release PR commit itself (atomic) OR switch `INV-release-please-manifest` from contentHash-pin to schema-pin.
- **Release-please-action default `config-file` + `manifest-file` paths differ from `.github/` placement.** _Source:_ Edge Case Hunter. _Location:_ `docs/invariants/release.md:52-54` (§ Consumption). _Nature:_ `googleapis/release-please-action`'s default `config-file` + `manifest-file` inputs resolve to repo-root `release-please-config.json` + `.release-please-manifest.json` (NOT `.github/`-prefixed). Story 1.14 pins `.github/`-prefixed paths per AC 1 (Epic-1 preference over architecture.md:807 root placement). release.md § Consumption asserts Story 13.5 will override via explicit `config-file:` + `manifest-file:` inputs — this claim is correct but the release.md text doesn't assert the default-vs-override distinction prominently. A Story 13.5 workflow author who misses the override will see a runtime "config file not found" CI failure. _Defer reason:_ scope-carved to Story 13.5 workflow authoring; release.md can be tightened. _Carry-to:_ Story 13.5 workflow delivery — explicit `config-file: .github/release-please-config.json` + `manifest-file: .github/.release-please-manifest.json` action inputs; OR release.md amendment hardening § Consumption "default search is repo-root; `.github/` requires explicit overrides" language.
- **`architecture.md:597` citation stale — cites "package-extraction posture" but `:597` is `null vs undefined` content.** _Source:_ Edge Case Hunter + Acceptance Auditor (convergent). _Location:_ `_bmad-output/planning-artifacts/architecture.md:597` (confirmed live content: "Null vs undefined: `undefined` for absent…"); cited from `docs/invariants/release.md:26` + Story 1.14 spec line 148. _Nature:_ line-number citation drifted from source; the spec inherited the stale pointer at drafting time, dev-story faithfully propagated it. Pre-existing spec error, not a dev-story deviation. _Defer reason:_ editorial amendment across 2 sites (spec line 148 + release.md:26) — actual "package-extraction posture" content needs locating in architecture.md (likely moved or re-anchored). _Carry-to:_ release.md + Story 1.14 spec citation amendment — either remove `:597` qualifier (point at section, not line) OR locate the actual package-extraction paragraph and re-cite.
- **`architecture.md:807` source-tree drift — shows `release-please-config.json` at repo root (not `.github/`).** _Source:_ Edge Case Hunter. _Location:_ `_bmad-output/planning-artifacts/architecture.md:807` (source-tree section shows `release-please-config.json` at root and `.github/release-please-manifest.json` under `.github/` — inconsistent placement). _Nature:_ architecture.md § Source-tree drifted from Story 1.14 AC 1 `.github/`-prefixed path decision. Per RALPH.md 2026-04-19 Epic-1-wins-over-architecture precedent, Epic-1 AC is authoritative; architecture.md amendment is the route. _Defer reason:_ editorial amendment on architecture.md; follow-up substrate-story scope. _Carry-to:_ architecture.md source-tree amendment — align to Story 1.14 AC 1 (`.github/release-please-config.json` + `.github/.release-please-manifest.json`) + consistent `.github/` placement for both files.
- **contentHash line-ending / Prettier normalization drift (class-of-issue shared with all 19 prior substrate hash-pins).** _Source:_ Blind Hunter + Edge Case Hunter (convergent; multi-layer). _Location:_ `packages/keel-invariants/src/invariants.manifest.ts:189,197,205` (INV-release-please-config + INV-release-please-manifest + INV-release-please-rationale). _Nature:_ hashes computed over file bytes including trailing newline. Any editor / Prettier / EditorConfig that normalizes line endings (CRLF → LF, LF → CRLF), trailing-newline presence, BOM, etc. invalidates the pinned sha256 at the Story 1.9 sync-gate. No `.gitattributes` entries pin these 3 files to `text eol=lf`. This is the same architectural class as Story 1.13 iter-71 post-commit SHA-drift finding (iter-73 CR absorbed that as a DEFER to content-hash provenance work). _Defer reason:_ architectural class — candidate resolution is content-hash-of-normalised-source (LF-canonical) OR switch to a provenance identifier that survives format-only edits (e.g., canonical-JSON hash, AST hash). Same Epic 3 reliability scope as Story 1.13 iter-71 finding. _Carry-to:_ Epic 3 reliability — content-hash provenance hardening story (unified fix covering all 20 invariant entries; optionally add `.gitattributes` `text eol=lf` + BOM stripping as a bridge tactic).
- **`docs/invariants/release.md` § Fork extension incomplete — regen-script path under-documented; tag-separator migration implicit.** _Source:_ Edge Case Hunter. _Location:_ `docs/invariants/release.md:48-50`. _Nature:_ (a) § Fork extension says "letting the Story 1.9 sync-gate re-hash the file at pre-merge" but the sync-gate is a VERIFIER (reports drift), not a re-hasher — no regen script exists. A fork that edits the config must manually regenerate `invariants.manifest.ts:INV-release-please-config.contentHash`; the section doesn't document that. (b) Switching to per-package mode introduces tag-separator concerns (unique per-component tags like `@keel/audit-v0.1.0`) that the fork-extension section doesn't cover; the claim "manifest format is unchanged across both modes" is true for the manifest file shape itself but incomplete in the broader migration picture. _Defer reason:_ documentation gap; substrate behaviour correct; spec expansion. _Carry-to:_ release.md § Fork extension amendment — add regen command (e.g., `sha256sum .github/release-please-config.json | awk '{print $1}'` + update `invariants.manifest.ts:INV-release-please-config.contentHash`) + note tag-separator implications when switching to per-package mode.

## Deferred from: code review of 1-15-renovate-config-with-version-pinning-rules-i7 (2026-04-21)

_Story 1.15 (`.github/renovate.json` I7 version-pinning config + `INV-deps-version-pinning` + `INV-renovate-rationale`) — iter-87 `/bmad-code-review (args: "2")`. Three-layer Ralph-hosted adversarial fan-out in parallel fresh-context Sonnet subagents: Blind Hunter (diff-only) 6 findings, Edge Case Hunter (diff + repo-read) 10 findings, Acceptance Auditor (AC-verification) all 4 ACs MET / SCOPE-CARVED + 2 pre-existing spec Dev Notes citation drifts. FIFTH cumulative ZERO-PATCH CR precedent (Stories 1.11/1.12/1.13/1.14 → 1.15). 7-iteration `drafted → done` lifecycle held exactly (iter-81 → iter-87). 10 DEFERs — all defensive-hardening / spec-amendment / downstream-consumer-validation candidates natural-scope for Story 2.1 devbox landing OR Epic 3 reliability OR follow-up renovate.md + spec amendments; none are shipping-blockers for the substrate-only `.github/renovate.json` + 2 invariants landing._

- **Redundant `matchPackageNames: ["vitest"]` on Vitest packageRule; fork-maintainer footgun.** _Source:_ Blind Hunter + Edge Case Hunter (convergent). _Location:_ `.github/renovate.json:13-14` (Vitest rule). _Nature:_ `matchPackageNames: ["vitest"]` is fully redundant with the `^vitest($|/)` pattern which also matches the bare `vitest` package (alternation: `vitest` followed by end-of-string OR `/`). The additive-OR semantics of `matchPackageNames` + `matchPackagePatterns` mean both are live but only the patterns load-bear for scoped `@vitest/*` coverage. Zero functional defect today. Risk: a fork maintainer reads the rule, concludes `matchPackageNames` is the primary hook, removes the pattern, and inadvertently unguards `@vitest/*` ecosystem + any future `vitest/` subpath package. _Defer reason:_ cosmetic; no runtime impact; downstream fork-hardening. _Carry-to:_ packageRule cleanup pass (drop redundant `matchPackageNames: ["vitest"]`) OR renovate.md § Per-package pinning rules amendment clarifying the redundancy.

- **`vitest-environment-*` hyphen-separator coverage gap.** _Source:_ Edge Case Hunter. _Location:_ `.github/renovate.json:13` (Vitest `matchPackagePatterns`). _Nature:_ `^vitest($|/)` matches bare `vitest` + `vitest/subpath`, and `^@vitest/` catches scoped `@vitest/ui`, `@vitest/browser`, etc. Neither pattern covers hyphen-separated community packages like `vitest-environment-nuxt`, `vitest-environment-miniflare`, `vitest-pool-workers` — they match `^vitest` but not `^vitest($|/)` (the alternation requires `/` or end-of-string after `vitest`). If a downstream story installs one, it falls through to top-level `rangeStrategy: replace` rather than the `vitest` pin group. _Defer reason:_ spec enumerates only Vitest core + `@vitest/*`; community packages fall outside I7 substrate scope until a consumer story installs them. _Carry-to:_ Epic 13 test-runner landing OR Story 1.16 Vitest install — add a fifth `matchPackagePatterns` entry like `^vitest-` to the Vitest rule when the first `vitest-environment-*` package actually lands.

- **pg_uuidv7 Docker rule `matchPackageNames` vs `matchDepNames` registry-prefix behaviour.** _Source:_ Edge Case Hunter. _Location:_ `.github/renovate.json:95-96` (pg_uuidv7 rule). _Nature:_ Renovate's docker datasource may normalise the depName by stripping the registry prefix (`ghcr.io/`) — if so, the rule's `matchPackageNames: ["ghcr.io/fboulnois/pg_uuidv7"]` silently never matches because Renovate stores the depName as `fboulnois/pg_uuidv7`. The canonical docker-datasource filter in v37+ is `matchDepNames`. Zero live exposure today (devbox compose is Story 2.1 landing; Renovate App not yet installed against the repo). _Defer reason:_ empirically verifiable only at Story 2.1 + Renovate App install landing. _Carry-to:_ Story 2.1 devbox docker-compose first-run empirical validation — on first Renovate-proposed image bump, observe whether the pg-uuidv7 group materializes; if not, switch to `matchDepNames` or strip the `ghcr.io/` prefix.

- **No exhaustiveness guard against future I7 ecosystem additions silently defaulting to `replace`.** _Source:_ Edge Case Hunter. _Location:_ `.github/renovate.json:9` (top-level `rangeStrategy: "replace"`) + all 4 packageRules. _Nature:_ top-level `replace` is intended catch-all for non-I7 packages per spec scope carve-out, but provides no guard against a future I7 ecosystem being missed. If Epic 5/13/14 introduces a fifth pinned-deps decision and a dev-story omits adding a new packageRule, the pinning gets silently skipped. Substrate cannot detect "I7 decision lacks matching packageRule" at pre-commit. _Defer reason:_ principled hardening beyond substrate scope. _Carry-to:_ Epic 3 reliability — declarative-policy-coverage check (e.g. keel-invariants extension that cross-references architecture.md § I7 decisions against `.github/renovate.json` packageRule groupName coverage) OR per-I7-decision lint at substrate-level.

- **`lockFileMaintenance` cannot retroactively pin a previously-range-specified package.** _Source:_ Edge Case Hunter. _Location:_ `.github/renovate.json:42-46` (lockFileMaintenance block). _Nature:_ `lockFileMaintenance` re-resolves the lockfile within existing version-range constraints — it does NOT rewrite declared ranges to pins. A package previously specified as `^1.0.0` that acquires an I7 pin rule after-the-fact will not be collapsed to an exact version by lockFileMaintenance alone; only a regular Renovate dependency PR (triggered by the matching packageRule) will do that. `rangeStrategy` at top-level does not propagate into `lockFileMaintenance` semantics. _Defer reason:_ documentation clarity; substrate behaviour correct. _Carry-to:_ `docs/invariants/renovate.md` § Grouping rationale OR § I7 posture amendment — note that pin enforcement requires a regular Renovate dependency PR cycle, not lockFileMaintenance.

- **`config:recommended` → `config:best-practices` preset-merge automerge interaction (load-bearing assumption).** _Source:_ Edge Case Hunter. _Location:_ `.github/renovate.json:3` (`extends`). _Nature:_ `config:recommended` transitively includes `config:best-practices` which may append an `automerge: true` packageRule for devDependencies (Renovate v37+). The per-rule `automerge: false` on the 4 I7 rules wins ONLY because Renovate's packageRule merge order places repo-authored rules AFTER preset rules (last-match-wins). This ordering is currently safe but is a load-bearing assumption about Renovate's internal preset-merge semantics — any future Renovate change to preset-merge ordering could invert the precedence and silently flip automerge on for I7 devDeps. _Defer reason:_ empirically verifiable only once App is installed + first preset-merge-order regression test runs against a Vitest/OTEL bump. _Carry-to:_ Story 2.1+ empirical validation once App is installed; OR Epic 3 reliability hardening (preset-merge-order regression test covering I7 devDep + top-level `automerge: false` + `config:best-practices` interaction).

- **Fork-extension doc claim misleading — `sync-gate re-pins` vs `sync-gate blocks`.** _Source:_ Edge Case Hunter. _Location:_ `docs/invariants/renovate.md:48`. _Nature:_ the § Fork extension section reads "the Story 1.9 sync-gate then re-pins the new file-byte state on the fork's landing", but the sync-gate is a VERIFIER (reports drift + exits non-zero at pre-merge), NOT a re-hasher. A fork that edits `.github/renovate.json` without also updating `INV-deps-version-pinning.contentHash` in `invariants.manifest.ts` will FAIL the sync-gate, not pass it. The required manual action (recompute sha256 + update manifest contentHash) is un-documented. Class-of-issue identical to Story 1.14 iter-80 defer #10 (`release.md` § Fork extension incomplete on regen-script gap) + Story 1.9 iter-7 defer "no regen script exists". _Defer reason:_ documentation gap; substrate behaviour correct; spec expansion. _Carry-to:_ renovate.md § Fork extension amendment — add regen command (e.g., `sha256sum .github/renovate.json | awk '{print $1}'` → update `invariants.manifest.ts:INV-deps-version-pinning.contentHash`) + unify the gap fix across release.md + tokens.md + renovate.md § Fork extension sections as a class-of-issue correction.

- **No `ignorePaths` / `enabledManagers` — Renovate scans all paths once App is installed.** _Source:_ Edge Case Hunter. _Location:_ `.github/renovate.json` (config root; missing fields). _Nature:_ once the Renovate GitHub App installs against the repo, it scans all paths for every enabled manager (`config:recommended` enables `dockerfile`, `docker-compose`, `github-actions`, `npm`, `pnpm` — effectively all). Future devbox compose files under `packages/devbox/` + `.claude/` tooling + any Dockerfile in non-product paths will trigger unsolicited image-update PRs. Mitigated by `automerge: false` (manual close required), but creates dashboard noise. _Defer reason:_ empirically observable only once App is installed + Story 2.1 devbox compose lands; mitigation is a config amendment, not a substrate defect. _Carry-to:_ Story 2.1 devbox landing — add `ignorePaths: [".claude/**", "**/test/**", …]` OR scope `enabledManagers` to managers in active substrate use.

- **contentHash prettier-formatter version-drift sensitivity (class-of-issue — shared with all 21 prior substrate hash-pins).** _Source:_ Edge Case Hunter. _Location:_ `packages/keel-invariants/src/invariants.manifest.ts:213,221` (`INV-deps-version-pinning` + `INV-renovate-rationale` contentHash fields). _Nature:_ hashes computed over prettier-formatted file bytes including trailing newline + indent width. A future prettier version that changes JSON formatting rules (indent, trailing comma, key ordering) invalidates the pinned sha256 at the Story 1.9 sync-gate. Architectural class identical to Story 1.13 iter-71 + Story 1.14 iter-80 defer #9 content-hash provenance finding. No `.gitattributes` entry pins these 2 files to `text eol=lf`. _Defer reason:_ architectural class — unified fix spans all 22 invariant entries. _Carry-to:_ Epic 3 reliability — content-hash provenance hardening story (unified fix covering all 22 invariant entries; candidate resolutions: pin content-hash of normalised-source LF-canonical, switch to AST / canonical-JSON hash that survives format-only edits, or add `.gitattributes` `text eol=lf` + BOM stripping as a bridge tactic).

- **Spec Dev Notes citation drift — architecture.md:305 + architecture.md:649 stale.** _Source:_ Acceptance Auditor. _Location:_ `_bmad-output/implementation-artifacts/1-15-renovate-config-with-version-pinning-rules-i7.md:198-199` (Dev Notes § Story 1.15 architecture + PRD references). _Nature:_ line 198 cites "Architecture line 305 (§ Party-Mode-driven Implementation Invariants)" but live `architecture.md:305` content is `Client-runtime env vars (front-end build):` (VITE env-vars section); line 199 cites "Architecture line 649 (§ Repository layout / Dev-time tooling)" but live `architecture.md:649` content is `Boundaries: one React error boundary per top-level route…` (Process Patterns § Error handling). The verbatim I7 text actually lives at `architecture.md:342` (rationale doc correctly cites :342 at line 149); the Dev-time tooling enumeration of `.github/renovate.json` appears elsewhere near `architecture.md:810` (§ Source-tree). Pre-existing spec error inherited by dev-story; NOT a dev-story deviation. Class-of-issue identical to Story 1.14 iter-80 defer #6 (architecture.md:597 stale citation pattern). _Defer reason:_ editorial amendment across 2 sites in Story 1.15 spec; implementation-correct. _Carry-to:_ Story 1.15 spec Dev Notes amendment — either (a) relocate the Implementation Invariants index citation to architecture.md:342 + the Dev-time tooling enumeration to the correct § Source-tree line near :810, or (b) remove the exact line-number qualifier and point at section headers only (more resilient to architecture.md line-number drift).

## Deferred from: code review of story 1-16 (2026-04-21)

_(iter-94 `/bmad-code-review (args: "2")` — 14 DEFER findings across 3 adversarial review layers on Story 1.16 iter-91 dev-story commit b6e3fc9. SIXTH cumulative Epic-1 ZERO-PATCH CR precedent. Acceptance Auditor: all 4 ACs SATISFIED. 2 additional Blind Hunter findings dismissed in-review as resolved by Edge Case Hunter verification.)_

- **Substrate-wins framing-depth tradeoff across 5 surfaces.** _Source:_ Blind Hunter (merged #1 + #7). _Location:_ `packages/keel-invariants/templates/INVARIANTS.fork.md § Precedence` + `CLAUDE.md § Knowledge-file contract` precedence paragraph + `AGENTS.md § Fork extension (FR44)` precedence rule vs. `docs/invariants/fork.md § Precedence`. _Nature:_ template says "attempts to override fail at lint/gate time"; CLAUDE.md says "cannot override it"; AGENTS.md says "substrate rules take precedence … via ESLint flat-config last-write-wins semantics + the spread-at-end convention"; `fork.md` explicitly admits substrate-wins is an OPERATIONAL CONVENTION, not a semantic gate. Surface-depth tradeoff (operator-facing summaries lean absolute; rationale doc carries nuance). _Defer reason:_ by-design information-architecture tradeoff; forcing uniform depth across all 5 surfaces inflates operator-facing brevity. _Carry-to:_ Epic 3 reliability — substrate-wins enforceability hardening story (if ever promoted from convention to machine-checked gate, e.g. via an ESLint plugin that errors on spread-at-start in forks; not currently in roadmap).
- **`packages/keel-invariants/package.json:14` line-pinned across 4 surfaces.** _Source:_ Blind Hunter #2 + Edge Case Hunter #5 (merged). _Location:_ `AGENTS.md:58` + `docs/invariants/fork.md:17/:18` + `packages/keel-invariants/src/invariants.manifest.ts:230` (INV-fork-extension-rationale description). _Nature:_ the 4 references hard-code line 14 of `package.json`; `package.json` is not a manifest sourcePath, so reorder of the `"exports"` block silently breaks the 4 references without sync-gate detection. Not broken today (Edge Case verified line 14 is currently `"./eslint": "./eslint.config.keel-invariants.js"`). _Defer reason:_ mechanical fix (drop `:14` and cite by subpath key `"exports['./eslint']"`) is doc hardening, not a correctness gap. _Carry-to:_ Epic 3 reliability — doc-reference-stability pass across all `:<line>` pointers in substrate docs (candidate fix: substitute line-pinned references with anchor/key-pinned references that survive file reformatting).
- **Manifest description "companion doc / drift-detected at doc layer" framing.** _Source:_ Blind Hunter #3. _Location:_ `packages/keel-invariants/src/invariants.manifest.ts` INV-fork-extension-rationale description. _Nature:_ `docs/invariants/fork.md` IS the primary source for the `INV-fork-extension-rationale` hash-pin; "companion doc" framing may mislead a reader into looking for a separate machine-enforced peer. Matches Stories 1.10/1.14/1.15 rationale-doc-pattern language verbatim. _Defer reason:_ cross-story consistency — unilateral change on Story 1.16 creates inconsistency across 4 INV-*-rationale entries. _Carry-to:_ Epic 3 reliability — rationale-doc description pattern retro (unified harmonization across INV-tokens-semantic-rationale + INV-release-please-rationale + INV-renovate-rationale + INV-fork-extension-rationale).
- **Fork-rename edge case (a fork that aliases or vendors `@keel/keel-invariants`).** _Source:_ Blind Hunter #5. _Location:_ `docs/invariants/fork.md § ESLint-extend pattern` + `AGENTS.md § Fork extension (FR44)` code block. _Nature:_ every documentation example hardcodes `@keel/keel-invariants/eslint`; a fork that vendors or renames the package (common in private-registry setups) has no explicit guidance — operator would need to infer that only the subpath identity `/eslint` is stable under rename. _Defer reason:_ speculative edge case out of 1.0 scope; subpath-stable convention is implicit in the code block. _Carry-to:_ docs-hardening candidate — add a one-line note to `docs/invariants/fork.md § ESLint-extend pattern` noting "if your fork renames or vendors the package, update the import specifier — the subpath (`/eslint`) must remain."
- **Story reference cluster non-monotonic ordering.** _Source:_ Blind Hunter #6. _Location:_ `docs/invariants/fork.md:212` ("Stories 1.14 + 1.15 + 1.10 + 1.6 + 1.9"). _Nature:_ non-monotonic enumeration reflects author's mental chain-of-thought; reader cannot infer which story is canonically authoritative. _Defer reason:_ pedagogical ordering (recent companion-doc precedents first, oldest source-level fork path last); minor prose nit. _Carry-to:_ future prose-polish pass to reorder chronologically or pick single authoritative reference.
- **Template "no underscores" naming rule unjustified in scaffold.** _Source:_ Blind Hunter #8. _Location:_ `packages/keel-invariants/templates/INVARIANTS.fork.md § Extension` + `docs/invariants/fork.md § INVARIANTS.fork.md scaffold`. _Nature:_ absolute "no underscores" rule in template/docs has no machine enforcement for FORK-* IDs (sync-gate ANCHOR_REGEX matches only INV- prefix per Edge Case #3). _Defer reason:_ upstream-naming-convention alignment — matches INV-* regex `^INV-[a-z0-9]+(-[a-z0-9]+)+$` at `invariants.manifest.ts:4`; forks that use underscores cannot pass upstream regex when promoted via AMEND path either. Intentional consistency. _Carry-to:_ if Epic 15a extends sync-gate regex to validate FORK-* anchors, harmonize rule.
- **Story 1.8 + 1.9 citation overclaims template provenance.** _Source:_ Blind Hunter #10. _Location:_ `docs/invariants/fork.md:138` (Machine-enforced-in header). _Nature:_ template is brand new in Story 1.16; attributing "machine-enforced in … (Story 1.8 + 1.9)" is shorthand for "drift-detected by the framework authored in Stories 1.8 + 1.9". Matches Stories 1.10/1.14/1.15 Machine-enforced-in header phrasing. _Defer reason:_ cross-story consistency — same harmonization target as the companion-doc framing entry above. _Carry-to:_ Epic 3 rationale-doc harmonization retro.
- **Template placeholder `_(add fork-specific invariants here)_` survives past fork-operator first edit.** _Source:_ Blind Hunter #11. _Location:_ `packages/keel-invariants/templates/INVARIANTS.fork.md § Fork invariants index` line 278. _Nature:_ when fork operator `cp`'s template and adds first FORK-* entry, the italic placeholder is easy to leave behind, producing a permanently-empty "add here" note in committed fork file. No lint enforces removal. _Defer reason:_ standard scaffold-template convention. _Carry-to:_ template-polish candidate — add inline HTML comment `<!-- delete this line once you add entries -->` alongside placeholder.
- **`FORK-<fork-slug>-<category>-<slug>` category/slug parsing ambiguity.** _Source:_ Blind Hunter #12 + Edge Case Hunter #6 (merged). _Location:_ `docs/invariants/fork.md § INVARIANTS.fork.md scaffold` + `packages/keel-invariants/templates/INVARIANTS.fork.md § Extension`. _Nature:_ pattern `<fork-slug>-<category>-<slug>` implies 3 components, but example `FORK-acme-tenancy-region-lock` has 4 hyphen-separated segments after the FORK prefix, and the "≥ 2 hyphen-separated segments after `FORK-<slug>-`" rule allows multi-hyphen slugs — creating ambiguity between `category=tenancy, slug=region-lock` vs. `category=tenancy-region, slug=lock`. Future fork-specific tooling that parses category vs. slug has no machine-readable separator. Inherits upstream `INV-<category>-<slug>` pattern's identical ambiguity at `invariants.manifest.ts:4`. _Defer reason:_ substrate-wide convention — applies to both INV-* and FORK-* IDs. _Carry-to:_ Epic 3 reliability — INV-* / FORK-* naming convention tightening story (candidate: enforce exactly-2-hyphen pattern OR state "category is single-segment; remaining segments are slug").
- **Files section mixes hash-pinned + unpinned entries under one heading.** _Source:_ Blind Hunter #13. _Location:_ `docs/invariants/fork.md § Files`. _Nature:_ two bullets end "Knowledge-file pointer, not a hash-pinned invariant source" alongside two bullets linking to `INV-*` IDs. Naive reader may assume drift-detection covers AGENTS.md + CLAUDE.md too. _Defer reason:_ deliberate one-stop reference list with explicit per-bullet "hash-pinned" vs. "knowledge-file pointer" tags disambiguating status. _Carry-to:_ future prose-organization refactor — split into sub-lists "Hash-pinned substrate surfaces" vs. "Unpinned knowledge-file pointers".
- **`packages/create-keel-app/` shell exists today as Story 1.1 output; doc claims "future" / "when that package lands".** _Source:_ Edge Case Hunter #1. _Location:_ `docs/invariants/fork.md:6/:43/:85`. _Nature:_ the `@keel/create-keel-app` package shell exists since Story 1.1 monorepo scaffold (`packages/create-keel-app/src/index.ts = export {};` + declared pnpm workspace with `@keel/keel-invariants` dep); doc language treats it as nonexistent. **INHERITED-SPEC-DRAFTING-DRIFT** class per Story 1.14 iter-80 defer #6 + Story 1.15 iter-87 defer (architecture.md:305 + :649 stale citations) + Story 1.16 iter-93 SM surface (inherited drafting-drift class carry-forward into iter-94 CR DEFER). AC 4 is satisfied semantically (CLI BEHAVIOUR non-auto-create: no `--include-fork-invariants` flag in the shell), not package-directory-existence. _Defer reason:_ inherited drafting-drift + Epic 15a scope — correction lands naturally when Epic 15a authors the CLI + flag + regression tests; rewording `fork.md` unilaterally now de-syncs the doc from the eventual Epic 15a PR. _Carry-to:_ Epic 15a landing — update `docs/invariants/fork.md:6/:43/:85` phrasing from "future package" / "when that package lands" / "the CLI itself is Epic 15a scope" to "the `--include-fork-invariants` CLI flag in the existing `@keel/create-keel-app` shell (runtime landing in Epic 15a)" as part of Epic 15a's doc updates.
- **Template § Extension instruction "under the appropriate H3 section above" with no H3 sections present in scaffold.** _Source:_ Edge Case Hunter #2. _Location:_ `packages/keel-invariants/templates/INVARIANTS.fork.md § Extension` line 34 vs. § Fork invariants index (zero H3s at line 14 — only HTML comment + `_(add fork-specific invariants here)_` placeholder). _Nature:_ a fork operator following § Extension literally has no "H3 section above" to add under. First-pass consumption friction. _Defer reason:_ template-UX polish, not a correctness gap — competent operators parse intent from HTML comment. _Carry-to:_ future template-polish candidate — ship a commented-out `### Example category` H3 stub inside `## Fork invariants index`.
- **Sync-gate ANCHOR_REGEX matches only `INV-` prefix; `FORK-` anchors silently skipped if landed in upstream INVARIANTS.md.** _Source:_ Edge Case Hunter #3. _Location:_ `packages/keel-invariants/src/sync-gate.ts:24` (`const ANCHOR_REGEX = /^-\s+\*\*\`(INV-[a-z0-9]+(?:-[a-z0-9]+)+)\`\*\*/gm;`). _Nature:_ regex hard-codes INV- prefix. Fork operators who paste FORK-* entries into upstream INVARIANTS.md (instead of their own `INVARIANTS.fork.md`) see anchors silently skipped rather than producing a useful error. Story 1.16 documentation does not mention this asymmetry. _Defer reason:_ by-design at 1.0 — substrate sync-gate scoped to substrate IDs; FORK-* drift-detection is the fork's responsibility. _Carry-to:_ docs-hardening candidate — add one-liner to `docs/invariants/fork.md § Precedence` noting "the substrate sync-gate ANCHOR_REGEX matches only `INV-` anchors; FORK- anchors are the fork's responsibility to drift-detect via its own tooling." If ever extended, widen regex to warn on FORK- IDs that landed in upstream file (Epic 15a scope or Epic 3 reliability pass).
- **"Fork extension" heading vocabulary overload across 3 files.** _Source:_ Edge Case Hunter #4. _Location:_ `AGENTS.md § Fork extension (FR44)` (operational guide) + `docs/invariants/fork.md § Fork extension` (meta-recursive section at line 77) + `packages/keel-invariants/templates/INVARIANTS.fork.md § Extension` (template instructions). _Nature:_ 3 fork-extension-ish headings across 3 files; each scope-appropriate, but close proximity in agent context window could confuse cross-reference. _Defer reason:_ prose vocabulary nit; each heading fits its file's scope. _Carry-to:_ future prose-polish candidate — rename rationale doc's `## Fork extension` (L77) → `## Meta-recursive fork extension` for clarity, OR template's `## Extension` → `## Adding entries`.

## Deferred from: code review of story-2.1 (2026-04-21)

_Story 2.1 (`packages/devbox/` absorb from cc-devbox — image + compose + substrate tooling access) — iter-128 `/bmad-code-review (args: "2")`. Three-layer Ralph-hosted adversarial fan-out (Blind Hunter + Edge Case Hunter + Acceptance Auditor) against `git diff main...HEAD` scoped to Story-2.1-owned files (packages/devbox/{Dockerfile, docker-compose.yml, entrypoint.sh, scripts/benchmark.sh, VERSIONS.md, README.md} + docs/invariants/devbox-dind.md + packages/keel-invariants/src/invariants.manifest.ts entry + INVARIANTS.md anchor + architecture.md I5a cross-ref + AGENTS.md + RALPH.md). Nine `patch` action items queued as TOP-of-QUEUE fix tasks (AI-1..AI-9; 3 High + 3 Medium + 3 Low); ten defers below carry-forward to follow-up stories / epics. First infrastructure-smoke class Epic post-SM CR gate; twelfth cumulative post-SM CR precedent._

- **Bind-mount pre-flight probe in `benchmark.sh` before destructive prune.** _Source:_ Edge Case Hunter #27 + iter-124 RALPH.md signpost. _Location:_ `packages/devbox/scripts/benchmark.sh` pre-`docker compose up --build -d`. _Nature:_ under backend B from iteration context, `benchmark.sh --allow-broad-prune` can succeed on the destructive prune, then fail mid-compose-up on bind-mount denial — maximum-damage path (host state destroyed, then mount fails, no benchmark recorded). Mirrors iter-122's destructive-op refusal-gate pattern at a different phase of the script. _Defer reason:_ **already queued as Story 2.1 follow-up IP Task 2** per iter-124 RALPH.md signpost; becomes the second TOP-of-QUEUE item after AI-9 lands. Not a new defer — the CR gate revalidated it.
- **`INV-devbox-dind-available` runtime rule implementation in `packages/keel-invariants/`.** _Source:_ Ralph IP QUEUE (pre-CR). _Location:_ `packages/keel-invariants/src/check-devbox-dind.ts` (new). _Nature:_ the invariant is specced in `docs/invariants/devbox-dind.md`, registered in `invariants.manifest.ts:944-948`, and anchored in `INVARIANTS.md` — but has no runtime `check()` exporter feeding the `pnpm keel-invariants:check-all` composite. `git diff main...HEAD` reveals the gap; Story 1.9 sync-gate pattern supplies the implementation shape. _Defer reason:_ **already queued as Story 2.1 follow-up IP Task 1** (pre-CR). After AI-1..AI-9 land + bind-mount pre-flight probe lands, this becomes the next Story-2.1-scope substrate completion task.
- **`curl | sh` checksum-pinning for NodeSource / Astral / AWS / GitHub releases in Dockerfile.** _Source:_ Blind Hunter #10 + Edge Case Hunter #20. _Location:_ `packages/devbox/Dockerfile:270 (NodeSource), :291 (gh apt repo), :306 (uv Astral installer), :321 (AWS CLI), :333 (supabase-cli releases API), :353 (git-delta releases API)`. _Nature:_ no supply-chain hardening; MITM / compromised CDN / typo'd URL would execute arbitrary code during image bake. Industry-wide acknowledged pattern; spec-silent. _Defer reason:_ cross-substrate supply-chain concern belongs to **Epic 4 (security-evidence contract)** not Story 2.1 scope. Epic 4 Stories 4.4 (semgrep SAST) + 4.10 (OWASP ASVS L1 baseline at `docs/invariants/security.md`) are the natural absorb point for cross-Dockerfile-installer sha256 pinning. _Carry-to:_ Epic 4 Story 4.10 OWASP ASVS baseline landing.
- **GitHub API rate-limit handling for `api.github.com/repos/{supabase/cli,dandavison/delta}/releases/latest`.** _Source:_ Edge Case Hunter #21. _Location:_ `packages/devbox/Dockerfile:333, :353`. _Nature:_ unauthenticated anon rate limit is 60/hr; back-to-back bakes or CI-scale bakes can exceed, producing an `{"message":"API rate limit exceeded..."}` JSON that `jq` interprets differently per installer stage (supabase = "skip", delta = `exit 1`). Inconsistent policy. _Defer reason:_ belongs to **Epic 13 CI harness reliability** — `GITHUB_TOKEN` build-arg threading + error-message-vs-asset-array discrimination is a CI-scale robustness concern, not Story 2.1 operator-bake scope. _Carry-to:_ Epic 13 Story 13.1 (`pre-merge-fast.yml`) or 13.2 (`pre-merge-slow.yml`) image-bake step if adopted.
- **`postgresql-client` explicit version pin.** _Source:_ Edge Case Hunter #23. _Location:_ `packages/devbox/Dockerfile:240`. _Nature:_ Ubuntu 24.04 default ships psql 16.x today; a future Ubuntu point release could bump to 17.x. VERSIONS.md § Bake log iter-123 records `psql 16.13`. _Defer reason:_ intentional Ubuntu default per AC 3 scope-clarification Dev Note ("Epic 6 forward-compat via baked `psql` without explicit version lock so Ubuntu-managed upgrades track"). **Not a defect** — by-design minor-version drift matching Ubuntu apt-provided psql. If tightening becomes desirable, future Epic 6 Story 6.5 (`pnpm rls:explain` landing) can pin explicitly. _Carry-to:_ Epic 6 Story 6.5 if psql-specific behaviour regression observed.
- **Atomic README benchmark-log append.** _Source:_ Edge Case Hunter #6. _Location:_ `packages/devbox/scripts/benchmark.sh:129-140`. _Nature:_ current `{ echo ...; echo ...; } >> README` pattern writes multiple lines non-atomically; interrupt between lines produces malformed markdown. _Defer reason:_ low-probability fault (append is operator-triggered, not automated); acceptable Low severity. Fix is one-line (build entry in variable, single `printf` append). _Carry-to:_ next `benchmark.sh` iteration after AI-4 safety bundle lands.
- **`/workspace` read-only mount handling in `entrypoint.sh`.** _Source:_ Edge Case Hunter #16. _Location:_ `packages/devbox/entrypoint.sh` workspace chown block. _Nature:_ future Story 2.11 shared-workspace-mode mounts `:ro` for non-owner forks; chown fails with EROFS and current blanket `|| true` swallows silently. Future `dev:dev` user (Story 2.5) then can't write to `/workspace`. _Defer reason:_ **Story 2.11 (per-fork vs shared-devbox mode `KEEL_DEVBOX_SHARED`) scope** — the `:ro` mount branch only exists after Story 2.11 lands. Touching `entrypoint.sh` now would add dead code. _Carry-to:_ Story 2.11 entrypoint.sh modification pass.
- **`chown` Linux host-ownership clobber on bind-mount.** _Source:_ Edge Case Hunter #13. _Location:_ `packages/devbox/entrypoint.sh` workspace chown block. _Nature:_ on macOS + Docker Desktop, bind-mount ownership is virtualised (chown no-ops host); on Linux, bind-mount ownership IS the host's — `chown root:root /workspace` rewrites the operator's worktree to UID 0, requiring `sudo chown -R` recovery. _Defer reason:_ iteration reference env is macOS backend-B at 2026-04-21 (no clobber); Linux-operator edge case surfaces after Story 2.5 non-root-user landing changes the chown target. Documented divergence. _Carry-to:_ Story 2.5 `USER dev` landing — reassess chown logic with the non-root-user pattern.
- **`architecture.md` hardcoded line-number references in `benchmark.sh` + `README.md`.** _Source:_ Edge Case Hunter #29. _Location:_ `packages/devbox/scripts/benchmark.sh:7`, `packages/devbox/README.md:513`. _Nature:_ `architecture.md:264-270` line refs are fragile; any upstream edit shifts the NFR28b section. _Defer reason:_ minor doc-staleness risk; not adversarial Story 2.1 scope. Longer-term fix is anchor-based refs (§ NFR28b) + a sync-gate check. _Carry-to:_ future doc-polish pass OR Story 1.9 sync-gate extension (anchor-ref validation).
- **`uv` installer path assumption (`mv /root/.local/bin/uv`).** _Source:_ Edge Case Hunter #19. _Location:_ `packages/devbox/Dockerfile:306-309`. _Nature:_ Astral installer is not contract-stable on install-dir; if Astral changes to `~/.cargo/bin` or `/usr/local/bin` directly, the `mv` fails. _Defer reason:_ upstream Astral installer contract — no clean fix without Astral API guarantee or `find /root -name uv -executable` fallback. Not exploitable today (iter-123 bake landed uv 0.11.7 cleanly). _Carry-to:_ next Dockerfile iteration after AI-1..AI-9 land, OR Renovate-triggered uv upgrade if Astral changes install-dir.
- **`playwright@latest` vs explicit apt-list drift.** _Source:_ Edge Case Hunter #22. _Location:_ `packages/devbox/Dockerfile:375`. _Nature:_ `playwright @latest install-deps` can pull new apt packages silently; the explicit apt list in the earlier RUN layer doesn't reflect the new deps. Renovate doesn't track. _Defer reason:_ Renovate-tracked (Story 1.15) via VERSIONS.md; future Playwright major-version upgrade will surface the delta naturally. _Carry-to:_ next Renovate-triggered Playwright upgrade.
- **VERSIONS.md node minor-version drift across bakes.** _Source:_ Edge Case Hunter #25. _Location:_ `packages/devbox/VERSIONS.md § Bake log`. _Nature:_ iter-123 records Node 20.20.2; next bake will pull whatever NodeSource `setup_20.x` ships. VERSIONS.md stays at 20.20.2 unless manually refreshed. _Defer reason:_ known bake-log snapshot semantics — each iter-NN entry is provenance-dated to that bake. Auto-regeneration is Story 2.6's `pnpm devbox:*` lifecycle CLI scope, not Story 2.1. _Carry-to:_ Story 2.6 `pnpm devbox:rebuild` → VERSIONS.md bake-log-append side-effect.

## Deferred from: code review RE-RUN of story-2.1 (2026-04-21)

_Story 2.1 — iter-138 `/bmad-code-review (args: "2")` RE-RUN after iter-129..iter-137 drained AI-1..AI-9. Three-layer Ralph-hosted adversarial fan-out against `git diff main...HEAD` (2,504 lines / 20 files) post-fix. **Acceptance Auditor verdict**: 4 ACs re-verified SATISFIED ×2 + PARTIAL ×2 (AC 3 + AC 4 under pre-triaged WAIVED operator-owned carve-out from iter-126 — NOT new defects); scope-creep CLEAN; defer-list discipline CLEAN; all 9 AI-*s closure-evidence byte-matches the diff. **Blind Hunter** 16 findings; **Edge Case Hunter** 21 findings; deduplicated to 33 unique findings → 9 patch (bundled into 5 action items AR-1..AR-5 queued as TOP-of-QUEUE) + 18 defers below + 6 dismissed. Second CR gate for Story 2.1 (iter-128 → iter-138); infrastructure-smoke class precedent extension — first Epic 2 story requiring a second CR-re-run cycle, consistent with Story 1.9's iter-7 → iter-14 → iter-17 multi-cycle pattern._

- **NFR2 wall-clock sub-second measurement precision.** _Source:_ Blind Hunter (F2). _Location:_ `packages/devbox/scripts/benchmark.sh:~155-175` cold/warm `date +%s` subtraction. _Nature:_ `cold_seconds="$(( cold_end - cold_start ))"` uses integer epoch-second granularity; 1-second rounding ±error on every recorded NFR2 value. Header docstring advertises `time docker compose up --build -d` method; actual implementation rounds to seconds. _Defer reason:_ acceptable vs 30s warm / 300s cold budgets (rounding << budget). First-run variance floor is already ±20% M-series noise per arch.md NFR28b; sub-second precision is below the noise floor. _Carry-to:_ Epic 13 CI bench-timing harness where `process_time()`-style sub-millisecond measurement is the natural shape.

- **Invariant `description:` field duplicates ~900 words of `devbox-dind.md` — drift risk outside sync-gate.** _Source:_ Blind Hunter (F4). _Location:_ `packages/keel-invariants/src/invariants.manifest.ts:253` `INV-devbox-dind-available` entry. _Nature:_ the description string restates § Backend contract, § Safety rule, § NFR2 authority verbatim from the companion doc; FR43 sync-gate hashes `sourcePath` content only, not the description field; substantive edits to the doc that update `contentHash` will leave the manifest description silently stale. _Defer reason:_ by-design self-contained description for consumers that don't follow the `sourcePath` cross-reference; no active defect — description is accurate today. _Carry-to:_ Story 1.10+ invariant-schema hardening pass where `descriptionMaxLength` or `descriptionSyncedWith: sourcePath` schema rules could be added to the `InvariantSchema` Zod refine.

- **`detect_backend()` fast-path case-variant + containerd/podman fragility.** _Source:_ Blind Hunter (F6) + Edge Case Hunter #15 (F6 merged). _Location:_ `packages/devbox/scripts/benchmark.sh:~95-115` detect_backend case statement. _Nature:_ (a) `docker-desktop|*-docker-desktop|moby|linuxkit-*` case pattern is case-sensitive; enterprise Windows Docker Desktop installs occasionally report `Docker-Desktop` casing that falls through; Colima/Rancher wrappers use different prefixes; (b) non-Docker container runtimes (containerd/podman) without `/.dockerenv` file return A, misdetected as native host; would enable destructive prune against the wrong daemon. _Defer reason:_ fail-safe path already handles — `/.dockerenv` fallback catches true-container cases; case variants only affect the fast-path ergonomics (not safety). _Carry-to:_ Story 2.11 shared-mode (first-class multi-backend support) OR Epic 15a Growth-tier fork work (Windows/Colima variants need first-class support).

- **`npx --yes playwright@latest install-deps` unpinned — defeats Renovate pinning mechanism.** _Source:_ Blind Hunter (F7) + Edge Case Hunter #6 (merged). _Location:_ `packages/devbox/Dockerfile:~1742` playwright RUN layer. _Nature:_ `playwright@latest` resolves at build-time with no lockfile; `install-deps` silently `apt-get install`s libraries absent from the explicit apt list and from VERSIONS.md bake log; two bakes on different days yield different Playwright OS-dep snapshots. _Defer reason:_ already-listed in prior CR defer (iter-128 "playwright@latest apt-list drift"); Renovate tracks via VERSIONS.md iter-NN bake snapshots. _Carry-to:_ Story 2.6 `pnpm devbox:rebuild` auto-regen pattern that updates VERSIONS.md on every bake, making the snapshot drift observable in git diff.

- **`container_name` default collision when fork lacks `.envrc` override.** _Source:_ Blind Hunter (F9). _Location:_ `packages/devbox/docker-compose.yml:~38` (post-AI-2 `container_name: ${KEEL_DEVBOX_CONTAINER_NAME:-keel-devbox}`). _Nature:_ AI-2 provided the override mechanism but the default value is still the literal `keel-devbox`; a second worktree or a second fork on the same host that hasn't hand-written `KEEL_DEVBOX_CONTAINER_NAME=<unique>` in their `.envrc` still collides. Alternative defaults (e.g. `$(basename $PWD)` or `${COMPOSE_PROJECT_NAME}-devbox`) would prevent the collision without operator action. _Defer reason:_ AI-2 rationale-comment explicitly named log-greppability as the reason for keeping the literal default; collision in practice requires two worktrees on the same host, which is rare at M0.5 (single operator workstation). _Carry-to:_ Story 2.2 `.envrc.example` lands with per-worktree `KEEL_DEVBOX_CONTAINER_NAME` scaffolding that all worktrees inherit by default.

- **Dockerfile `wget` + `gnupg` + `lsb-release` installed without obvious consumer.** _Source:_ Blind Hunter (F10). _Location:_ `packages/devbox/Dockerfile:~62-72` apt install block. _Nature:_ the gh installation uses `curl | dd` (not gpg/apt-key); NodeSource `setup_20.x` ships its own keyring handling; no RUN invocation exercises `wget`, `gnupg`, or `lsb-release` visibly. Under the AI-9 "every upstream apt extra must justify itself" standard, these are undocumented carry-through. _Defer reason:_ removal requires transitive-dep analysis (NodeSource's `setup_20.x` might internally consume `gnupg` for apt-key fallback; `lsb-release` might be consumed by some Playwright dep); not a 1-line safe edit. _Carry-to:_ Story 11.x image-size optimization pass with empirical transitive-dep analysis (build image without each, observe what breaks).

- **Warm-run measurement contaminated by trailing cold `down` when both passes run.** _Source:_ Blind Hunter (F12). _Location:_ `packages/devbox/scripts/benchmark.sh:~175-195`. _Nature:_ cold branch ends with `docker compose down`; warm branch opens with another `docker compose down ... || true`; the two are not serialised against daemon convergence, so the recorded warm-start wall-clock may include residual teardown rather than the "down && up -d against pre-built image" defined in the header. _Defer reason:_ documented method; operator-owned authoritative timing on M4-Pro per AC 4. Pre-Epic-13, the ±20% first-run variance already swamps this contamination. _Carry-to:_ Epic 13 CI bench-timing harness where a clean sync-gate between cold and warm passes is natural.

- **NodeSource setup script piped to bash without checksum verification.** _Source:_ Edge Case Hunter #1 (E1). _Location:_ `packages/devbox/Dockerfile:~89-91` NodeSource `curl ... | bash -`. _Nature:_ compromised upstream or MITM at build-time executes arbitrary code in the image layer; no sha256 pinning. _Defer reason:_ upstream-canonical pattern (NodeSource itself documents this installation mode on [deb.nodesource.com](https://deb.nodesource.com)); supply-chain hardening is a cross-substrate concern. _Carry-to:_ Epic 4 Story 4.10 OWASP ASVS L1 baseline — already listed in prior CR defer as "`curl | sh` checksum-pinning for NodeSource / Astral / AWS / GitHub releases".

- **uv install script piped to sh without checksum verification.** _Source:_ Edge Case Hunter #2 (E2). _Location:_ `packages/devbox/Dockerfile:~131-134` uv Astral installer. _Nature:_ same supply-chain shape as NodeSource — `curl -LsSf https://astral.sh/uv/install.sh | sh`. No sha256. _Defer reason:_ upstream-canonical Astral installer pattern; shares defer carry-target with NodeSource. _Carry-to:_ Epic 4 Story 4.10.

- **GPG keyring empty via `curl | dd` pattern (no keyring integrity check).** _Source:_ Edge Case Hunter #3 (E3). _Location:_ `packages/devbox/Dockerfile:~117-124` gh keyring download. _Nature:_ `curl ... | dd of=...` pipeline lacks `set -o pipefail`; a curl failure producing empty body writes an empty keyring; subsequent apt repo fetches fail at repo-signature-verification time with an unclear error chain. _Defer reason:_ `curl -fsSL` handles HTTP errors with exit 22 (RUN exits non-zero); empty-body case is edge-case. Upstream gh install doc uses this pattern verbatim. _Carry-to:_ Epic 4 Story 4.10 OWASP ASVS L1 — concurrent absorb point for supply-chain hardening.

- **GitHub API anon rate-limit on Supabase release lookup.** _Source:_ Edge Case Hunter #4 (E4). _Location:_ `packages/devbox/Dockerfile:~164-181` supabase-cli release query. _Nature:_ same shape as the iter-128 CR defer for the same surface — anonymous 60/hr limit; back-to-back bakes or Epic 13 CI-parallel bakes can exceed. _Defer reason:_ already in prior CR defer list (iter-128). Not a new finding; confirmed by the re-run as still-deferred. _Carry-to:_ Epic 13 CI harness (`GITHUB_TOKEN` threading via `--build-arg`).

- **GitHub API anon rate-limit on git-delta release lookup.** _Source:_ Edge Case Hunter #5 (E5). _Location:_ `packages/devbox/Dockerfile:~183-201` git-delta release query. _Nature:_ same as supabase — anon rate limit. _Defer reason:_ already in prior CR defer list. _Carry-to:_ Epic 13 CI harness.

- **apt packages installed without version pins.** _Source:_ Edge Case Hunter #7 (E7). _Location:_ `packages/devbox/Dockerfile:~58-92` apt install block. _Nature:_ `apt-get install -y --no-install-recommends tini iproute2 dnsutils ...` with no `=VERSION` clause; Ubuntu pocket updates can change package versions between bakes. _Defer reason:_ intentional for Ubuntu security-update tracking; Renovate `apt` custom manager tracks via VERSIONS.md snapshots. Strict pinning is a larger supply-chain conversation. _Carry-to:_ Epic 11.x supply-chain hardening if strict pinning becomes required (tradeoff: pin-stability vs Ubuntu security-patch velocity).

- **`KEEL_DEVBOX_CONTAINER_NAME` override invalid-chars pre-flight validation.** _Source:_ Edge Case Hunter #9 (E9). _Location:_ `packages/devbox/docker-compose.yml:~27` (post-AI-2 parameterised container_name). _Nature:_ Docker's container-name restriction is `[a-zA-Z0-9_.-]`; an operator setting `KEEL_DEVBOX_CONTAINER_NAME="my devbox"` (space) or unicode would fail docker create with an opaque error. _Defer reason:_ Docker surfaces a clear error at create-time; pre-flight validation in start-script is Story 2.2 `pnpm devbox:up` wrapper concern. _Carry-to:_ Story 2.2 `scripts/devbox-start.sh` regex gate `[[ "$KEEL_DEVBOX_CONTAINER_NAME" =~ ^[A-Za-z0-9_.-]+$ ]] || exit 2`.

- **`KEEL_DEVBOX_WORKSPACE` path existence + host-shared-mount pre-flight.** _Source:_ Edge Case Hunter #10 (E10). _Location:_ `packages/devbox/docker-compose.yml:~63-66` bind-mount source path. _Nature:_ operator passing an invalid `KEEL_DEVBOX_WORKSPACE` to `docker compose up` produces "mounts denied" at container-create after the build cost has been paid. _Defer reason:_ partially-mitigated by iter-124 backend-B bind-mount-denial carve-out; pre-flight probe is queued as Story 2.1 follow-up IP Task 2 (already in prior CR defer as "Bind-mount pre-flight probe in `benchmark.sh`"). The docker-compose.yml surface itself cannot probe; responsibility belongs to start-script. _Carry-to:_ Story 2.2 `pnpm devbox:up` wrapper.

- **`/tmp` read-only under future hardening — `2>/tmp/chown.err` redirect fails.** _Source:_ Edge Case Hunter #12 (E12). _Location:_ `packages/devbox/entrypoint.sh:~57-60, :71-74` (AI-5 chown stderr-capture pattern). _Nature:_ Story 2.5 hardening will land `tmpfs` for `/tmp` with `nosuid,noexec` + size cap; if `/tmp` is read-only-by-accident or the tmpfs is full, the stderr redirect fails under `set -e` and the container startup aborts. _Defer reason:_ AR-2 (this re-run's patch) mitigates partially by replacing `/tmp/chown.err` with `mktemp -t chown.XXXXXX` + `chown_err=${chown_err:-/dev/null}` fallback. Full hardening against tmpfs-full landed tmpfs contract is Story 2.5 concern. _Carry-to:_ Story 2.5 hardening pass — verify entrypoint.sh chown-err capture still works under the new tmpfs tenure.

- **`mkdir -p` on read-only `/home` fails under `set -e`.** _Source:_ Edge Case Hunter #13 (E13). _Location:_ `packages/devbox/entrypoint.sh:~65-67` named-volume dir pre-creation loop. _Nature:_ Story 2.5 hardening will mount named-volumes at `/home/dev/.claude/` + `/home/dev/.config/gh/`; the loop in entrypoint.sh pre-creates those dirs — if the parent filesystem is readonly (future hardening), `mkdir -p` fails with EROFS and `set -e` aborts the container. _Defer reason:_ Story 2.5 contract concern; the named-volume mounts will satisfy the dir-existence precondition at compose runtime, making the pre-creation loop redundant. _Carry-to:_ Story 2.5 hardening pass — remove or refactor the mkdir loop once named-volumes land.

- **Flag `=value` form (`--skip-cold=1`) ergonomic gap.** _Source:_ Edge Case Hunter #18 (E18). _Location:_ `packages/devbox/scripts/benchmark.sh:~62-68` flag parser case statement. _Nature:_ Current parser accepts only `--skip-cold` (bare flag) and `--allow-broad-prune`; common-ergonomic `--skip-cold=1` or `--skip-cold=true` forms fall into the default unknown-arg exit 2. _Defer reason:_ Story 2.2 QoL pass will rewrite the flag parser with a uniform `case "$arg" in --skip-cold|--skip-cold=*) ...` pattern alongside the full `pnpm devbox:*` lifecycle CLI scope. _Carry-to:_ Story 2.2 lifecycle CLI.

## Deferred from: code review RE-RUN-2 of story-2.1 (2026-04-21)

_Story 2.1 — iter-144 `/bmad-code-review (args: "2")` RE-RUN-2 after iter-139..iter-143 drained AR-1..AR-5. Three-layer Ralph-hosted adversarial fan-out against `git diff main...HEAD` (2,704 lines / 20 files) post-AR-* fix-pass. **Acceptance Auditor verdict**: 4 ACs re-re-verified SATISFIED ×2 + PARTIAL ×2 (AC 3 + AC 4 under pre-waived operator-owned carve-out from iter-126); scope-creep CLEAN; defer-list discipline CLEAN; all 9 AI-*s + all 5 AR-*s closure-evidence byte-matches the diff (ZERO closure regressions); context-doc consistency CLEAN across devbox-dind.md / architecture.md / prd.md; sprint-status + trace-matrix CLEAN. **ZERO SUBSTANTIVE FINDINGS**. **Blind Hunter** 20 findings; **Edge Case Hunter** 16 findings; deduplicated to 33 unique findings → **0 patch** (the zero-patch verdict that closes the three-cycle CR sequence) + **7 defers below** + **26 dismissed** (noise / intentional / by-design / duplicates-of-existing-defers). Third CR gate for Story 2.1 (iter-128 + iter-138 + iter-144); matches Story 1.9's iter-7 → iter-14 → iter-17 multi-cycle envelope precedent. First zero-patch CR for Story 2.1 at its third gate; closes the `fixes-pending → done` transition and sets the three-cycle upper envelope as the Epic-2-infrastructure-smoke-story ceiling._

- **`KEEL_DEVBOX_WORKSPACE_OWNER` regex rejects numeric UID:GID (e.g. `1000:1000`).** _Source:_ Blind Hunter (F1) + Edge Case Hunter (E1; HIGH convergence, two independent subagents). _Location:_ `packages/devbox/entrypoint.sh:51` AR-2 regex anchor `^[A-Za-z_][A-Za-z0-9_.+-]*:[A-Za-z_][A-Za-z0-9_.+-]*$`. _Nature:_ Linux devcontainer-convention numeric-UID operator patterns (ubiquitous `KEEL_DEVBOX_WORKSPACE_OWNER=$(id -u):$(id -g)` shape for host-UID matching) rejected by the `[A-Za-z_]` first-char anchor; `chown` itself natively accepts numeric UID:GID. Story 2.1 default `root:root` passes regex (not blocking in default operation); iteration-context macOS Docker Desktop virtualises bind-mount ownership (numeric-UID unused today). _Defer reason:_ Story 2.5's non-root `dev` user redesign supersedes this surface — Story 2.5 bakes a fixed `dev:dev` user (UID 1000 baked-in), making the numeric-UID operator escape hatch superfluous. AI-5 (iter-133) + AR-2 (iter-140) intentionally scoped to POSIX username shape for option-parse-injection defence; widening now creates technical debt for Story 2.5's redesign. _Carry-to:_ Story 2.5 `USER dev` + non-root hardening pass — reassess chown regex shape (likely alternating `^([A-Za-z_][A-Za-z0-9_.+-]*|[0-9]+):([A-Za-z_][A-Za-z0-9_.+-]*|[0-9]+)$` if regex survives at all) in lock-step with the `dev:dev` default.

- **VERSIONS.md primary-table pin propagation asymmetry (AWS CLI + git-delta rows).** _Source:_ Edge Case Hunter (E11). _Location:_ `packages/devbox/VERSIONS.md:27,29` primary `## Baked at image-build` table. _Nature:_ AR-3 (iter-141) propagated iter-123 bake-log pins to the primary table for `uv` (0.11.7) and updated the Supabase row's Notes wording to match AI-6's fail-closed fix; AR-3 did NOT touch AWS CLI row (L27, still "v2 (arch-aware installer)") or git-delta row (L29, still "latest release `.deb` (arch-aware)") — both of which have captured pins in the `## Bake log § iter-123` table at L133-142 (AWS CLI 2.34.33; git-delta 0.19.2). Two-tier table-vs-bake-log asymmetry; Renovate `apt` custom manager scans the primary table first. _Defer reason:_ AR-3 scope was deliberately narrow (uv + supabase were the named-in-finding rows); completeness polish belongs to Story 2.6's `pnpm devbox:rebuild` lifecycle CLI which auto-regens VERSIONS.md on every bake, making pin propagation automatic rather than manual. _Carry-to:_ Story 2.6 `pnpm devbox:rebuild` → VERSIONS.md bake-log-append + primary-table sync side-effect.

- **`benchmark.sh` SIGPIPE uncovered in per-signal trap set.** _Source:_ Edge Case Hunter (E2). _Location:_ `packages/devbox/scripts/benchmark.sh:215-218` per-signal trap registration. _Nature:_ AR-1 (iter-139) bundled INT/TERM/HUP/QUIT per-signal traps; SIGPIPE (e.g. `benchmark.sh | head -1`) takes bash default behaviour (exit 141). The EXIT trap still fires — `cleanup_on_exit` runs `docker compose down` — so cleanup coverage is complete. However no per-signal `trap 'exit 141' PIPE` wrapper means exit-code semantics aren't script-controlled for the pipe-termination path; callers cannot distinguish "pipe consumer died" from "normal exit 141" (vanishingly rare for a shell script). _Defer reason:_ cleanup coverage is complete via the EXIT trap (AR-1's critical contribution); only exit-code DISTINGUISHABILITY is missing. Adding PIPE to the trap list would extend AR-1's comment block + add a thin wrapper, but the finding is Low-severity polish. _Carry-to:_ Epic 13 CI bench-timing harness where comprehensive signal-coverage (PIPE + USR1 + USR2 + ABRT) is natural alongside structured failure reporting.

- **`benchmark.sh` missing ERR diagnosis trap (no `file:line` of `set -e` failure).** _Source:_ Edge Case Hunter (E3). _Location:_ `packages/devbox/scripts/benchmark.sh:214` trap-registration block. _Nature:_ AR-1's EXIT-trap handoff for the ERR path provides cleanup coverage (verified iter-139 smoke: `false` under `set -e` → `cleanup fired; exit_code=1`) but no diagnostic — script never prints WHERE `set -e` tripped. An operator seeing `docker compose build` fail mid-pass gets the cleanup message with no indication of the failing command / line. _Defer reason:_ cleanup is correct (AR-1 target met); only DIAGNOSIS is missing. Adding `trap 'echo "ERR: ${BASH_SOURCE[0]}:${BASH_LINENO[0]}: failed command: ${BASH_COMMAND}" >&2' ERR` would extend AR-1 but introduces its own edge cases (ERR vs EXIT interaction; `$BASH_COMMAND` availability under `set -euo pipefail` pipefail paths; interaction with the `|| true` guards already in cleanup_on_exit). Low-severity polish; Epic 13 CI harness owns structured failure reporting. _Carry-to:_ Epic 13 CI bench-timing harness (JSON-lines format: `{"file": "...", "line": N, "signal": "ERR", "exit_code": N, "failed_command": "..."}`).

- **`detect_backend()` `/.dockerenv` heuristic misclassifies non-Docker OCI runtimes (podman / containerd+nerdctl / finch).** _Source:_ Edge Case Hunter (E6). _Location:_ `packages/devbox/scripts/benchmark.sh:148` `[[ -f /.dockerenv ]]` check. _Nature:_ Story 2.1 targets Docker/Compose per `INV-devbox-dind-available` § Backend contract. Non-Docker OCI runtimes use different container markers: podman → `/run/.containerenv`; containerd+nerdctl → no canonical marker; finch → uses its own daemon identifier. Script falls through `/.dockerenv` check to `echo A` → broad prune authorised against what may be a non-Docker daemon. _Defer reason:_ INV-devbox-dind-available § Backend contract currently enumerates Docker-specific backends A (true DinD) / B (host socket-passthrough); a podman-equivalent would require a backend C (isolated rootful/rootless podman daemon) with its own case-pattern discrimination + `/run/.containerenv` fallback. Multi-runtime support is Story 2.11 shared-mode (first-class multi-backend) or Epic 15a Growth-tier fork scope. Not a live defect in iteration context (backend-B macOS Docker Desktop) or operator M4-Pro baseline (native macOS Docker Desktop). _Carry-to:_ Story 2.11 shared-mode multi-backend support OR Epic 15a Growth-tier fork work (podman/finch-specific fork variants).

- **Ubuntu:24.04 base image not pinned by SHA digest (`FROM ubuntu:24.04@sha256:<digest>`).** _Source:_ Blind Hunter (F7). _Location:_ `packages/devbox/Dockerfile:17` `FROM ubuntu:24.04`. _Nature:_ Ubuntu's `24.04` tag is mutable on Docker Hub; Canonical pushes regular security-patch updates to the tag, so two bakes at different times ship different base layers. AI-1 through AI-9 (iter-128 gate) + AR-1 through AR-5 (iter-138 gate) hardened package-manager-level pins (claude-code, pnpm, uv, supabase-cli, git-delta); image-digest pinning is a further supply-chain layer not touched by those iterations. _Defer reason:_ extends the existing iter-128 defer "`curl | sh` checksum-pinning for NodeSource / Astral / AWS / GitHub releases" from package-fetch hardening to image-fetch hardening — same cross-substrate OWASP ASVS L1 concern, same scope carve-out (cross-Dockerfile supply-chain belongs to Epic 4). Renovate's `docker` custom manager tracks digest bumps when pinned. _Carry-to:_ Epic 4 Story 4.10 OWASP ASVS L1 baseline — bundle image-digest pin + Renovate `docker` manager alongside package-fetch hardening.

- **`detect_backend()` case-sensitive backend-name pattern matching (`Docker-Desktop` mixed-case, Colima / Rancher / Finch prefixes).** _Source:_ Edge Case Hunter (E13; extends iter-138 defer F6 "detect_backend fast-path case-variant fragility"). _Location:_ `packages/devbox/scripts/benchmark.sh:133-138` case-pattern statement (`docker-desktop|*-docker-desktop|moby|linuxkit-*`). _Nature:_ enterprise Windows Docker Desktop installs occasionally report `Docker-Desktop` (mixed-case casing); Colima (`colima-*`) wraps the daemon name without a canonical prefix recognised by this case list; Rancher Desktop and Finch use their own identifiers. For these variants the case-statement falls through, `/.dockerenv` fallback at L148 fail-safes to B. Safety is preserved — destructive prune is refused by default — but fast-path ergonomics degrade: operators running genuinely native backend-A Docker Desktop with a non-canonical casing / wrapper must pass `--allow-broad-prune` flag even though they are not in a nested container. _Defer reason:_ safety is preserved (fail-safe-B is the intentional posture from iter-131 AI-3); case-variant tolerance is operator-UX polish; same class as iter-138 defer F6 with Finch/Rancher extension. _Carry-to:_ Story 2.11 shared-mode multi-backend support (first-class Colima + Rancher + Finch treatment) OR Epic 15a Growth-tier fork work (enterprise Windows variants).

## Deferred from: code review of story-2-2-envrc-parameterisation-contract (2026-04-21)

_Story 2.2 (`.envrc` parameterisation contract) — iter-151 `/bmad-code-review (args: "2")` first CR gate against `git show ff8121a` (16 files / 429+/109−). Three-layer Ralph-hosted adversarial fan-out (Blind Hunter + Edge Case Hunter + Acceptance Auditor). **Auditor verdict: 1 finding** (invariant-doc negative-smoke example contradicts basename-anchored regex — classified PATCH, absorbed into § Review Findings as action item AR-1). **Blind Hunter** ≈19 findings; **Edge Case Hunter** 15 JSON findings; deduplicated to 20 unique → **2 patch** (both doc-accuracy drifts in `docs/invariants/gitignored-secret-commit-deny.md`) + **14 defers below** + **5 dismissed** (3-layer pnpm indirection = standard workspace pattern; shebang-less dist binary = npm-bin handles chmod; gitignore bang-negation ordering = verified correct; tmpfs knobs unconsumed = BY DESIGN per AC 1 scope-clarification forward-declaring Story 2.5 consumers; `.env` payload not in denylist = OUT OF AC 5 SCOPE which names only `.envrc`/`.envrc.local`/`.secrets`). First Epic-2 post-SM CR opener with only 2 patch findings — materially tighter than Story 2.1 iter-128's 9-AI opener, validating iter-150 v1.4 SM forecast (NO operator-owned carve-out + all 5 ACs live-verified at trace + SM time + 36-line Node-only leaf lint-rule = minimal adversarial attack surface)._

- **Unit-suffix injection in `mem_limit` / `shm_size` / `cpus` parameterisation.** _Source:_ Blind Hunter (F4/F13) + Edge Case Hunter (E1/E2/E3). _Location:_ `packages/devbox/docker-compose.yml:69-71` `${KEEL_DEVBOX_MEMORY_GB:-12}g` / `${KEEL_DEVBOX_SHM_GB:-2}g` / `'${KEEL_DEVBOX_CPUS:-8}'`. _Nature:_ operator setting `KEEL_DEVBOX_MEMORY_GB=16g` (mirroring the `mem_limit` idiom they see in compose docs) renders as `16gg`; `KEEL_DEVBOX_SHM_GB=512m` renders as `512mg`; `KEEL_DEVBOX_CPUS=0` / `-1` / `abc` / `8.5` flows through verbatim into YAML. `docker compose config` exits 0 for all these malformed substitutions because YAML-parse is lenient; only `docker compose up` (at daemon-attach time) rejects. _Defer reason:_ Story 2.2 AC 3 scope is retunability-at-YAML-substitution-time, not operator-input-validation. The `.envrc.example` header already documents "integer GB only, no suffix" style via the reference-default format + inline comments; defensive validation is Story 2.16 operator-UX / hooks scope OR a dedicated `pnpm devbox:env:check` preflight in Story 2.6's `pnpm devbox:*` lifecycle CLI. _Carry-to:_ Story 2.6 `pnpm devbox:env:check` or Story 2.16 pretooluse-hooks substrate — bundle with `KEEL_DEVBOX_NOFILE` overflow defer below under a shared "operator-env defensive validation" umbrella.

- **`KEEL_DEVBOX_NOFILE` overflow / suffix / zero / negative values pass YAML-parse gate.** _Source:_ Blind Hunter (F5 inline) + Edge Case Hunter (E4). _Location:_ `packages/devbox/docker-compose.yml:76-77` `ulimits.nofile.{soft,hard}: ${KEEL_DEVBOX_NOFILE:-65536}`. _Nature:_ operator setting `KEEL_DEVBOX_NOFILE=9999999999` (above kernel `fs.nr_open`) / `0` / `-1` / `65536k` flows through verbatim; `docker compose config` exits 0; container start fails with `invalid ulimit` or kernel EPERM / ERANGE. Separate sub-finding: `KEEL_DEVBOX_NOFILE` aliases both `soft:` and `hard:` — operator can never raise hard > soft without editing compose (semantic mismatch with `ulimit`'s separate-cap convention). _Defer reason:_ same defensive-validation class as `mem_limit` / `cpus` cluster above; soft/hard aliasing is BY DESIGN for Story 2.2 single-knob ergonomics (AC 1 naming convention mirrors the "knobs NOT underlying compose keys" scope clarification). _Carry-to:_ Story 2.6 `pnpm devbox:env:check` preflight OR Story 2.16 hooks defensive-validation substrate; split into `KEEL_DEVBOX_NOFILE_SOFT` + `KEEL_DEVBOX_NOFILE_HARD` only if Story 2.5 hardening's ulimit tuning needs the axis.

- **`KEEL_DEVBOX_CPUS` invalid-value pass-through (0 / −1 / fractional / non-numeric).** _Source:_ Edge Case Hunter (E3). _Location:_ `packages/devbox/docker-compose.yml:69` `cpus: '${KEEL_DEVBOX_CPUS:-8}'`. _Nature:_ listed as sub-finding but separate because the `cpus:` key is the most likely candidate for operator mis-use — docs often describe CPU caps as floats, and `0` has a genuine "no cap" interpretation in some tools (not compose). `docker compose config` passes all invalid values; daemon runtime rejects later. _Defer reason:_ same cluster; separate bullet recorded because the sub-mechanism differs (CPU scheduler has different edge values than memory). _Carry-to:_ Story 2.6 `pnpm devbox:env:check` preflight.

- **Whitespace-only env var expansion passes `${VAR:-default}` substitution gate.** _Source:_ Edge Case Hunter (E13). _Location:_ `packages/devbox/docker-compose.yml:70` (+ the whole `${KEEL_DEVBOX_*:-<default>}` cluster). _Nature:_ compose `${VAR:-default}` substitutes the default on unset/empty, NOT on whitespace-only. `export KEEL_DEVBOX_MEMORY_GB="  "` renders as `mem_limit: "  "g` — parse error. _Defer reason:_ operator-input-validation class; `.envrc.example` format mitigates by providing clean reference values, but a fork retune during editing can introduce whitespace trivially. _Carry-to:_ same cluster as the unit-suffix defer above — a `pnpm devbox:env:check` trim-and-validate pass handles this alongside unit-suffix validation.

- **Platform/arch mismatch cryptic failure on amd64 host without `KEEL_DEVBOX_ARCH` override.** _Source:_ Blind Hunter (F15) + Edge Case Hunter (E5). _Location:_ `packages/devbox/docker-compose.yml:34` `platform: ${KEEL_DEVBOX_ARCH:-linux/arm64}`. _Nature:_ amd64 operator who clones the repo and runs `docker compose up` without reading `.envrc.example` gets an arm64 image on amd64 — requires QEMU; if QEMU absent, failure is cryptic (`exec format error`). The `.envrc.example` header documents override, but operator-workstation-first-run may bypass the direnv copy-seed flow. _Defer reason:_ operator-env polish; `.envrc.example` comment already documents the knob; Story 2.6 `pnpm devbox:up` wrapper can shell out to `uname -m` and warn on mismatch. _Carry-to:_ Story 2.6 `pnpm devbox:up` preflight warning OR Story 2.11 shared-mode architecture-autodetection.

- **Loopback-only port publication (`127.0.0.1:…`) unreachable from remote-DinD / `DOCKER_HOST=tcp://…` operators.** _Source:_ Blind Hunter (F12). _Location:_ `packages/devbox/docker-compose.yml:78-82` four hardcoded `- '127.0.0.1:${KEEL_DEVBOX_PORT_*:-<default>}:<default>'` entries. _Nature:_ iter-146 pre-dev SM review fix (6) explicitly mandated `127.0.0.1:` prefix on port entries to reduce Story 2.12 rework surface. For operators driving a remote Docker daemon (remote DinD, `DOCKER_HOST=tcp://…`, corporate shared-runner setups), the bind IP `127.0.0.1` is the REMOTE host's loopback — unreachable from local workstation. No knob to override the bind IP. _Defer reason:_ Story 2.12 (loopback-bound port publication opt-in + `KEEL_DEVBOX_SSH` + `sshd`) explicitly owns this cross-cutting port-publication surface per epic plan; Story 2.2's role was to forward-declare the port knobs, not to parameterise the bind IP. _Carry-to:_ Story 2.12 `KEEL_DEVBOX_BIND_IP` knob alongside the SSH opt-in toggle — canonical remote-operator story for the loopback-bound-port-publication surface.

- **Port-knob collision (e.g. `KEEL_DEVBOX_PORT_WEB=3001` matches API default) has no cross-knob validation.** _Source:_ Edge Case Hunter (E6). _Location:_ `packages/devbox/docker-compose.yml:78-82` four parameterised port entries. _Nature:_ operator retuning `KEEL_DEVBOX_PORT_WEB=3001` without adjusting `KEEL_DEVBOX_PORT_API` (default 3001) collides on the same host port — `docker compose up` fails with `bind: address already in use`. Same class for any two of the four ports. No cross-knob validation in compose; no warning in `.envrc.example` to keep ports distinct. _Defer reason:_ operator-UX polish; README § Retuning could document "ports must be pairwise distinct" as a note — Story 2.6 `pnpm devbox:env:check` preflight validates. _Carry-to:_ Story 2.6 preflight; alongside the defensive-validation cluster above.

- **Denylist regex case-sensitivity + Windows backslash + filesystem-root-path (`/.envrc`) edge cases.** _Source:_ Blind Hunter (F1) + Edge Case Hunter (E7/E8/E9). _Location:_ `packages/keel-invariants/src/check-no-committed-dotfiles.ts:17-19` three denylist entries `/^(.+\/)?\.envrc$/` / `\.envrc\.local$/` / `\.secrets$/`. _Nature:_ three sub-edges: (a) regex is case-sensitive — `.ENVRC` on macOS APFS (case-insensitive default) aliases to `.envrc` on the filesystem but git tracks the staged name as given; `git add .ENVRC` slips past the denylist. (b) regex anchors on forward-slash separator — Windows checkout emitting `packages\devbox\.envrc` is not matched; hook is effectively disabled on Windows. (c) `(.+\/)?` prefix requires 1+ chars before `/` — `/.envrc` (filesystem-root basename) fails to match; prek passes repo-relative paths so this is a direct-CLI edge only. _Defer reason:_ cross-platform hardening is out of Story 2.2 scope (Linux-substrate repo; cc-devbox runs Linux containers; macOS operators run Docker Desktop Linux VM). AC 5 scope is gitignored-secret commit-deny at the default prek-invoked path. Case-insensitivity is a BYPASS; Windows operator support belongs to Epic 15a Growth-tier fork work; filesystem-root is a narrow direct-CLI smoke scenario. _Carry-to:_ Epic 15a Growth-tier fork variants (Windows / enterprise); Vitest regex-fixture table-test lands the test coverage at Epic 13 test-framework landing.

- **Hook zero-argv silent-pass (`pass_filenames: true` invoked with empty argv → exit 0 without warning).** _Source:_ Blind Hunter (F10) + Edge Case Hunter (E10). _Location:_ `packages/keel-invariants/src/check-no-committed-dotfiles.ts:22-28` `const stagedFiles = process.argv.slice(2); const violations = stagedFiles.flatMap(...)`. _Nature:_ zero-argv runs exit 0 silently. If `.pre-commit-config.yaml` `files:` regex is ever mis-edited so the hook is invoked with empty filename list, the hook silently "passes" — no "hook received no filenames, aborting" safety net. sync-gate via contentHash catches the `.pre-commit-config.yaml` change but not the semantic regression of the guard becoming a no-op. _Defer reason:_ not exploitable today (sync-gate watches the config file; human review of YAML + sync-gate suffice at 1.0); defense-in-depth hardening class. _Carry-to:_ Epic 13 CI test-harness — a test that runs the hook with empty argv AND with empty-string filename AND with a filename that doesn't exist, asserting expected failure modes. Or a one-line `if (stagedFiles.length === 0) process.exit(0);` with a debug-log warning behind `DEBUG=keel-invariants` env.

- **Missing automated regex test for `check-no-committed-dotfiles.ts`.** _Source:_ Edge Case Hunter (E15). _Location:_ `packages/keel-invariants/src/check-no-committed-dotfiles.ts` (no test file exists). _Nature:_ ATDD-WAIVED at iter-147 (twelfth cumulative precedent — FR14n ATDD-skip + hybrid ground-(a)-(b)-(c)-(d)). The regex is a core security guard; a regression (accidentally dropping the `$` anchor, flipping to case-insensitive too broadly, widening the prefix group) ships green through `pnpm -w typecheck/lint/format:check` and through `docker compose config`. _Defer reason:_ ATDD was formally WAIVED with a precedent chain — deferring test coverage to post-Epic-13 test-framework landing is the stated posture for this class of story. _Carry-to:_ Epic 13 Vitest harness — `check-no-committed-dotfiles.test.ts` with stringified-argv table test covering positive / negative / boundary / case-variant / whitespace / filesystem-root / Windows-path fixtures. Retroactively raises the regression-catch floor.

- **Two manifest entries share `sourcePath: .pre-commit-config.yaml` → latent contentHash drift-trap.** _Source:_ Blind Hunter (F6). _Location:_ `packages/keel-invariants/src/invariants.manifest.ts:93,109` `INV-prek-pre-commit-config` + `INV-prek-commit-msg-config` both point at `.pre-commit-config.yaml`. _Nature:_ any single-line edit to `.pre-commit-config.yaml` requires simultaneously updating two independent `contentHash` values; if a future contributor updates only one, sync-gate catches drift on only that entry. _Defer reason:_ pre-existing Epic 1 pattern (shares root with the same concern recorded at Story 1.8 iter-7 CR defer "Shared-sourcePath cross-entry `contentHash` consistency refine" — which expected Story 1.9's drift-detection to catch this via whole-file sha256 recomputation at manifest-use time). Story 2.2 inherits the pattern when it adds `INV-gitignored-secret-commit-deny` adjacent to the pre-existing prek entries. _Carry-to:_ Epic 13 test-harness OR a one-shot refactor story that replaces the per-entry `contentHash` with a per-`sourcePath` computed hash at walker time — aligns with Story 1.9's sync-gate semantics and eliminates the manual two-hash update surface.

- **compose v2 non-swarm service-level `mem_limit` / `cpus` / `shm_size` deprecation future-compat.** _Source:_ Edge Case Hunter (E14). _Location:_ `packages/devbox/docker-compose.yml:69-71`. _Nature:_ upstream Docker Compose v2.x is gradually deprecating top-level service-level `mem_limit` / `cpus` / `shm_size` in favour of `deploy.resources.limits.*` (which itself has non-swarm semantics in v2.20+). Story 2.2's AC 2 scope-clarification EXPLICITLY locks non-swarm service-level form per iter-146 fix (3) — `NOT deploy.resources.limits.*`. This is a correctness decision for the current compose version; future versions may emit deprecation warnings or remove the keys. _Defer reason:_ AC 2 scope-clarification locks the current form; a future compose-version migration is a spec-level change, not a CR patch. _Carry-to:_ Renovate-triggered or periodic "devbox compose version sweep" story when upstream compose deprecation-warning level fires; at that point the full compose file migrates + AC 2 scope clarification is superseded. Epic 13+ release-gated tier is the natural CI-surface to detect deprecation warnings at release time.

- **Manifest-entry description drift re `.env*` coverage breadth.** _Source:_ Blind Hunter (F14). _Location:_ `packages/keel-invariants/src/invariants.manifest.ts` (new `INV-gitignored-secret-commit-deny` entry, description field). _Nature:_ description says "refuses additions of .envrc, .envrc.local, and .secrets at any path" — accurate for the invariant's scope. But `.gitignore` broader `.env*` coverage is a SEPARATE mechanism, not governed by this invariant. Risk: future reader interprets invariant as covering broader `.env*` scope (e.g. `.env`, `.env.production`). _Defer reason:_ invariant is correctly scoped in its description; the interpretation risk is doc-polish level. Adding a one-line note "(does not cover `.env` / `.env.production` — those are `.gitignore`-only; see Story 2.16 hooks for filesystem-level secret denylist)" would close the drift. _Carry-to:_ Story 2.16 Claude-pretooluse-hooks substrate — when hooks-level secret-denylist lands, cross-reference both mechanisms in a shared invariant-doc index.

- **Pointer-error stderr may reference the refused file itself (self-cross-reference).** _Source:_ Blind Hunter (F11). _Location:_ `packages/keel-invariants/src/check-no-committed-dotfiles.ts:32-34` stderr message references `packages/devbox/.envrc.example` + `packages/devbox/.secrets.example` as schema sources. _Nature:_ if the refused path is itself under `packages/devbox/` OR if `.envrc.example` has been deleted from the working tree, the pointer is hollow. Narrow edge — a developer who deleted `.envrc.example` mid-work and tried to commit `.envrc` would get a pointer to the (now-missing) schema file. _Defer reason:_ minor edge; `.envrc.example` is part of the committed schema and wouldn't be deleted in normal workflow; the pointer-error still conveys the essential "refused to commit" message even if the referenced file is momentarily absent. _Carry-to:_ optional polish — add an existence check + fall back to "(see Story 2.2 AC 5 for schema layout)" if file missing; low priority.

- **`.envrc.example.bak` / `.envrc.local.example` / nested-companion variants remain ignored under `.env*` with no dedicated negation.** _Source:_ Edge Case Hunter (E11). _Location:_ `.gitignore:35-44` rule block. _Nature:_ `.env*` catches backup files (e.g., editor-created `.envrc.example.bak`) and nested copies (e.g., `packages/devbox/sub/.envrc.example`); explicit bang-negations only cover the exact literal names. Operator creating backup files during editing sees `git add` silently no-op. _Defer reason:_ edge operator workflow; conservative scope keeps Story 2.2 at AC-declared companions only (`.envrc.example` + `.secrets.example` at repo root + `packages/devbox/`). Expanding negation rules would admit untested file-name variants. _Carry-to:_ documentation polish — README § Retuning adds a note "don't create `.envrc*.bak` backup files; git silently ignores them"; no code change required.

## Re-run from: code review of story-2-2-envrc-parameterisation-contract (2026-04-21 iter-154)

_Story 2.2 `/bmad-code-review (args: "2")` re-run at `fixes-pending → done` confirmation gate. Scope: cumulative Story 2.2 substrate diff `ff8121a^..HEAD` filtered to 12 substrate files / 341-line delta (excluding `_bmad-output/`, `.ralph/`, `RALPH.md`, `_bmad/`). Three-layer Ralph-hosted adversarial fan-out (Blind Hunter diff-only + Edge Case Hunter with repo-read + Acceptance Auditor with spec + context docs). Raw: Blind ≈3 LOW after 2 self-withdrawals + Edge 7 LOW + 1 MEDIUM + Auditor CLEAN on code + 2 spec-hygiene observations. Dedup across layers: 15 raw findings → 9 unique after blind+edge+auditor merges → **0 new PATCH + 1 genuinely new DEFER + 4 re-surfaces of iter-151 defers confirmed HOLD + 2 DISMISS**. Both iter-151 PATCHes (AR-1 iter-152 `/tmp/fake.envrc → /tmp/.envrc` + AR-2 iter-153 `.envrc.local.example` allow-list removal) end-to-end verified DRAINED under live smokes. Auditor spec-hygiene items (flip `[ ]` → `[x]` at v1.5 patch rows 328-329 + append v1.6/v1.7/v1.8 Change Log entries) applied in-commit as natural CR-closure housekeeping per Option-2 re-run interpretation (self-closing by the closure commit itself, not re-queued as fix-drain iters). **Forecast-envelope validation: iter-150 v1.4 SM projection landed on schedule at iter-154** (4-iter fix-chain vs Story 2.1's 16-iter chain — 4× tighter envelope for hybrid infrastructure-smoke + configuration-surface class with ZERO operator-owned carve-out)._

### Re-surfaced iter-151 defers confirmed HOLD (4)

- **`12gg` / `unlimited` compose numeric-suffix fallback fragility.** _Source:_ Blind Hunter + Edge Case Hunter re-run (cross-layer convergent — both layers independently surfaced the `KEEL_DEVBOX_MEMORY_GB=12g → mem_limit: 12gg` failure mode). _Re-surface-of:_ iter-151 defer "Unit-suffix injection in `mem_limit` / `shm_size` / `cpus` parameterisation" (`deferred-work.md` line 262). _Live-verification this iter:_ `KEEL_DEVBOX_MEMORY_GB=12g docker compose config` rejects with `'services[devbox].mem_limit' invalid suffix: 'gg'`; `KEEL_DEVBOX_NOFILE=unlimited` fails with `strconv.Atoi: parsing "unlimited"`. _Defer reason:_ operator-UX defensive-validation cluster remains deferred to Story 2.6 `pnpm devbox:env:check` preflight per iter-151 scoping; re-surface raises confidence in the defer (still LOW-MEDIUM op-impact, not a PATCH). _Carry-to:_ **HOLD — Story 2.6 `pnpm devbox:env:check` preflight** (unchanged from iter-151).
- **`.envrc~` vim-swap / trailing whitespace / exotic-path regex escape.** _Source:_ Edge Case Hunter re-run. _Re-surface-of:_ iter-151 defer "Denylist regex case-sensitivity + Windows backslash + filesystem-root-path edges" (`deferred-work.md` line 276) — vim-swap `.envrc~` is a NEW dimension in the same cross-platform-hardening cluster. _Live-verification this iter:_ node probe confirmed `".envrc "` / `".envrc\n"` / `".envrc~"` / `".envrc.swp"` all render `ALLOW` against the current regex. _Defer reason:_ extension of an existing cluster; git path normalisation mostly covers exotic whitespace; `.envrc~` vim-swap is realistic but still LOW. _Carry-to:_ **HOLD — Epic 15a Growth-tier fork variants + Epic 13 Vitest regex-fixture table-test** (iter-151's cross-platform cluster; adds `.envrc~` as a boundary row in the Vitest fixture table).
- **AC 5 argv=∅ behaviour no automated assertion.** _Source:_ Edge Case Hunter re-run. _Re-surface-of:_ iter-151 defer "Hook zero-argv silent-pass" (`deferred-work.md` line 278) — narrower framing: iter-151 focused on exploitability (not today); iter-154 adds the missing-regression-test-pin axis. _Defer reason:_ same as iter-151 — no test runner at 1.0; Epic 13 Vitest harness is the natural home. _Carry-to:_ **HOLD — Epic 13 Vitest harness** `check-no-committed-dotfiles.test.ts` — add argv=∅ case alongside iter-151's stringified-argv table-test row.
- **`127.0.0.1` hardcoded host_ip (no IPv6-only fallback; not parameterisable).** _Source:_ Edge Case Hunter re-run. _Re-surface-of:_ iter-151 defer "Loopback-only port publication (`127.0.0.1:…`) unreachable from remote-DinD / `DOCKER_HOST=tcp://…` operators" (`deferred-work.md` line 272) — extended axis: IPv6-only hosts (dual-stack-only docker bind behaviour). _Defer reason:_ same as iter-151 — Story 2.12 owns the bind-IP parameterisation surface + `KEEL_DEVBOX_SSH` opt-in; Story 2.2's role was to forward-declare the port knobs with the AC 2 mandated `127.0.0.1:` prefix per iter-146 fix (6). _Carry-to:_ **HOLD — Story 2.12 `KEEL_DEVBOX_BIND_IP` knob** (unchanged from iter-151).

### New iter-154 defer (1)

- **Manifest-description schema-companion asymmetry: `.envrc.local.example` gitignored without negation.** _Source:_ Blind Hunter + Edge Case Hunter (cross-layer convergent at 100% — strongest signal of a real finding per iter-151 lesson). _Location:_ `packages/keel-invariants/src/invariants.manifest.ts:260` description text ("Committed schemas (`.envrc.example`, `.secrets.example`) are exempt via anchored regex end-match") + `.gitignore:36-42` (`.env*` glob). _Nature:_ the manifest description globally claims "schemas are exempt" but `.gitignore` has no `!.envrc.local.example` negation — `.env*` silently ignores any `.envrc.local.example` a fork might author as a symmetric companion to `.envrc.example`. Live-verification: `git check-ignore -v -- .envrc.local.example` → `.gitignore:36:.env*  .envrc.local.example` (ignored). AR-2 iter-153 drained the doc's allow-list sentence, but the manifest description + `INVARIANTS.md` anchor bullet still use the broader "schemas … exempt" language. _Defer reason:_ Story 2.2 AC 1 + AC 4 never commit to `.envrc.local.example` as a schema companion — this is future-proofing for a name no story consumes at 1.0. Doc-polish level, not a behaviour defect (the manifest's invariant scope IS accurate for the three filenames it actually governs). Adjacent to iter-151 defer "Manifest-entry description drift re `.env*` coverage breadth" (`deferred-work.md` line 286) which already deferred `.env*` breadth drift; this is the fork-symmetry axis of the same doc-polish cluster. _Carry-to:_ **Story 2.16 Claude-pretooluse-hooks substrate — when hooks-level secret-denylist lands, cross-reference `.envrc.local.example` fork-symmetry question in a shared invariant-doc index** (aligns with iter-151 F14 carry-to). Alternative downstream home: Epic 15a Growth-tier fork variants — if a fork operator explicitly needs `.envrc.local.example` committable, a Growth-tier opt-in pattern (similar to `INVARIANTS.fork.md` scaffold) is the right shape.

### Iter-154 dismissals (2)

- **README Epic 2 table "(landed iter-148)" annotation staleness.** _Source:_ Blind Hunter. _Location:_ `packages/devbox/README.md:247`. _Dismiss reason:_ MISREAD — the annotation IS correct. Story 2.2 dev-story DID land at iter-148 (`ff8121a`); iter-151 (CR opener), iter-152 (AR-1 drain), iter-153 (AR-2 drain), iter-154 (CR re-run closure) are CR + fix-chain iterations on the landed implementation, not fresh implementation. The README strikethrough-plus-landing-annotation pattern is Story 2.1's precedent (also reflects the iter the dev-story landed, not the iter the CR closed).
- **Three tmpfs knobs published without consumer (orphan in compose).** _Source:_ Edge Case Hunter. _Location:_ `packages/devbox/.envrc.example:30-32` (`KEEL_DEVBOX_TMPFS_TMP_MB` / `KEEL_DEVBOX_TMPFS_VARTMP_MB` / `KEEL_DEVBOX_TMPFS_LOGS_MB`). _Dismiss reason:_ BY DESIGN per AC 1 scope-clarification (forward-declares knobs for Story 2.5 consumption). `.envrc.example:30-32` already has inline "Active Story 2.5. Story 2.2 publishes the knob only." — this is the contract, not a drift. Re-affirms iter-151 dismissal #4 ("tmpfs knobs published but unconsumed = BY DESIGN"); re-surface here is the re-run adversarial layer rediscovering the same posture.

## Deferred from: code review of 2-3-egress-policy-dnsmasq-nftables-fail-closed-ipv4-ipv6-parity-atomic-reload (2026-04-21)

_Story 2.3 (egress policy: dnsmasq + nftables, fail-closed, IPv4/IPv6 parity, atomic reload) — iter-161 `/bmad-code-review (args: "2")`. Three-layer Ralph-hosted adversarial fan-out (Blind Hunter + Edge Case Hunter + Acceptance Auditor per skill `step-02-review.md`) against cumulative Story 2.3 substrate diff `69e3103..HEAD` (19 files / 1031+/8−; ~650 LOC implementation + invariants manifest + invariant doc + README updates). **Acceptance Auditor verdict: HIGH-confidence PASS** (5/5 AC SATISFIED + 17/17 SC SATISFIED with file:line substrate evidence, pressure-tested iter-160 SM ZERO-PATCH verdict — upheld). **Blind Hunter: 18 findings** (2 CRITICAL + 5 HIGH + 6 MEDIUM + 5 LOW). **Edge Case Hunter: 15 findings** (1 CRITICAL + 3 HIGH + 6 MEDIUM + 5 LOW). Triaged ~50 raw → **9 PATCH + 9 DEFER + ~9 DISMISS** after live-substrate verification against bash 5.x + nft 1.0.9 + Docker bind-mount behaviour + entrypoint.sh L23 `set -euo pipefail` exit propagation._

_The 9 defers below are all out-of-scope for Story 2.3's substrate-commitment scope: observability-supervision (not enforcement) / architectural trade-offs acknowledged in architecture § S5 / Epic-4 consumer concerns / future-compat / doc-hash-only invariant manifest by design. None are exploitable against Story 2.3's fail-closed AC commitments; they are hardening + observability candidates natural-scope for downstream stories or deferred to operator-workstation bake-time._

- **Rotation synchronously blocks tail pipe during `gzip`.** _Source:_ Blind Hunter HIGH. _Location:_ `packages/devbox/scripts/egress-log-tailer.sh:42-78`. _Nature:_ `rotate_if_needed` is called synchronously inside the per-line loop; while `gzip` runs (potentially seconds on 50 MB) the tail pipe buffer fills and dnsmasq log lines may be dropped upstream. _Defer reason:_ 50 MB gzip is typically sub-second on modern CPUs; the SC-4 rotation cadence is operator-workstation-tuned (pinned at 50 MB to cap worst-case disk at ~125 MB per invariant doc L73). Optimisation candidate. _Carry-to:_ operator-workstation bake-time perf verification alongside Task 12.7 log-rotation smoke (iter-158 DEFER-annotation) — if rotation latency impacts observability on baseline NFR2 operator-workstation, fork `gzip` into background with `exec 3>&-; (gzip -f …; mv …) &` and reopen fd 3 against a freshly `touch`-ed file before the background gzip completes.
- **egress-log-tailer.sh has no supervisor — silent death cuts observability.** _Source:_ Blind Hunter MEDIUM + Edge Case Hunter HIGH (cross-layer convergent). _Location:_ `packages/devbox/scripts/egress-log-tailer.sh:198-200` + `packages/devbox/scripts/start-egress.sh:100`. _Nature:_ if `tail -F` dies (OOM, fs error, SIGPIPE, rotation-time `gzip` failure under `set -e`) the `while read` loop exits and the tailer script ends; no respawn, pidfile stays pointing at a dead pid, `/workspace/logs/egress-queries.jsonl` stops advancing, nftables + dnsmasq continue enforcing fail-closed policy (egress still works) but FR37 security-evidence stream is silently cut. Epic 4 consumer cannot distinguish "no queries happened" from "tailer dead". _Defer reason:_ observability concern not enforcement guarantee; supervisor/respawn pattern belongs to a dedicated observability-hardening story OR Story 2.13 healthcheck (probe tailer liveness via `kill -0 "$(cat /run/keel-egress-tailer.pid)"` and surface non-zero healthcheck on tailer death). _Carry-to:_ **Story 2.13 healthcheck OR Epic 4 FR37 security-evidence consumer** (whichever lands first should author the liveness probe + respawn pattern).
- **Tailer rotation SIGTERM mid-rotation may leave JSONL file transiently missing.** _Source:_ Edge Case Hunter MEDIUM. _Location:_ `packages/devbox/scripts/egress-log-tailer.sh:71-77`. _Nature:_ the rotation sequence `exec 3>&-; mv to .1.tmp; gzip; touch new; reopen fd` is not atomic against SIGTERM; the `shutdown` trap fires `exec 3>&-` (already closed) and exits 0 — in the window between the first `mv` and the final `touch`, the active `/workspace/logs/egress-queries.jsonl` may not exist. External consumers opening the file during this window get ENOENT; `monitor.sh`'s `tail -F` reattaches on file creation but naive consumers may fail-loudly. _Defer reason:_ low-likelihood race with a documented consumer contract (tail-F recovery is the standard pattern); disabling signal handling during rotation (`trap '' TERM INT` around the block) is a one-line fix but layers trap-state complexity. _Carry-to:_ **Story 2.13 healthcheck hardening** if healthcheck-consumer open-on-path fails during rotation; otherwise hold for an observability-hardening pass.
- **SERVFAIL / upstream-timeout not emitted in JSONL.** _Source:_ Edge Case Hunter MEDIUM. _Location:_ `packages/devbox/scripts/egress-log-tailer.sh:130-180`. _Nature:_ `KEEL_DEVBOX_DNS_UPSTREAM=1.1.1.1` becomes unreachable (partition, rate limit, CF outage) → dnsmasq's forwards time out → returns SERVFAIL; the tailer regex catalog (`query`, `forwarded`, `reply`, `config`) has no pattern for dnsmasq SERVFAIL / REFUSED / upstream-timeout log lines; SC-3 schema advertises `result: "servfail"` but no code path emits it. Fail-closed posture is preserved (clients see resolution failure) but the observability contract is incomplete. _Defer reason:_ Epic 4 FR37 security-evidence consumer may refine the result-classifier; SC-3's declared 5-value result enum (`allow|block|nxdomain|servfail|parse-error`) holds at the schema level; the regex gap is a code-vs-schema drift, not an AC violation. _Carry-to:_ **Epic 4 FR37 security-evidence consumer story** — when Epic 4 authors the JSONL consumer, it will need a dnsmasq SERVFAIL fixture line + a tailer regex branch matching dnsmasq's `reply <domain> is <SERVFAIL|REFUSED>` / `failed to send packet` upstream-failure log lines, emitting `result: "servfail"`.
- **Whitelist-snapshot DNS-rotation drift.** _Source:_ Blind Hunter HIGH. _Location:_ `packages/devbox/scripts/reload-egress.sh:100-117`. _Nature:_ nftables allow-list snapshots literal IPs resolved at reload time; domains that rotate A records (cloud CDNs, `api.anthropic.com`, `raw.githubusercontent.com`, `ghcr.io`, `*.npmjs.org`) return new IPs that aren't in the nftables allow-list → TCP SYN gets dropped silently even though dnsmasq correctly resolves (JSONL logs `allow`, layer-3 drops). _Defer reason:_ architectural trade-off ACCEPTED per architecture § S5 dual-layer belt-and-braces; Story 2.4's `whitelist.sh sync` CLI is the mitigation path (frequent reload via operator trigger or Story 2.13 healthcheck-driven reload). An IP-set driven from dnsmasq's `--dnsmasq-script` hook would be strictly better but adds substrate complexity out of scope for 1.0. _Carry-to:_ **Story 2.4 `whitelist.sh sync`** is the sanctioned mitigation; a post-1.0 hardening story may introduce dnsmasq-hook-driven IP-set updates if CDN rotation frequency empirically breaks fork-operator workflows.
- **dnsmasq `bind-interfaces` + `::1` racing container boot.** _Source:_ Blind Hunter MEDIUM. _Location:_ `packages/devbox/dnsmasq/dnsmasq.conf:28`. _Nature:_ `bind-interfaces` asks dnsmasq to bind each matching interface address (not `0.0.0.0`); if `lo` is not yet configured with `::1` when dnsmasq starts (uncommon on backend B where `lo` is typically up-and-dual-stack at boot, but possible on fork-operator workstations with aggressive namespace setup), the `::1` bind fails and dnsmasq exits non-zero → start-egress exits under `set -e` → container dies at boot. _Defer reason:_ low-likelihood on backend B per SM iter-160's operator-workstation-defer-acknowledgment; dnsmasq has no native retry. _Carry-to:_ operator-workstation bake-time verification as part of Task 12.1 smokes; if observed in practice, drop `::1` from `listen-address` or add a retry wrapper around dnsmasq's first-start in `reload-egress.sh`.
- **`INV-devbox-egress-contract` is doc-hash-only — no runtime enforcement.** _Source:_ Edge Case Hunter LOW. _Location:_ `packages/keel-invariants/src/invariants.manifest.ts:257-263` + `docs/invariants/devbox-egress.md` frontmatter. _Nature:_ the invariant claim is "fail-closed DNS + IPv4/IPv6 parity + atomic reload" but the manifest-side enforcement is only content-hash sync gating between the doc and the `INVARIANTS.md` anchor. There is NO machine-enforced runtime check (live `nft list chain`, JSONL schema `jq` probe, or `kill -HUP` round-trip) — the doc itself explicitly defers these to "a dedicated packages/keel-invariants/ rule + unit test on a later Ralph iteration". _Defer reason:_ acknowledged-in-band; the doc frontmatter declares enforcement as doc-hash only pending future runtime probe. _Carry-to:_ **Epic 13 CI harness OR a dedicated `packages/keel-invariants/` runtime-rule story post-Epic-13** — land the runtime probe so the invariant claim is actually enforced; explicitly mark the manifest entry's enforcement class if the schema adds such a field.
- **In-flight conntrack priming before first `nft -f`.** _Source:_ Edge Case Hunter LOW. _Location:_ `packages/devbox/nftables/egress.nft:46,70`. _Nature:_ the atomic-reload guarantee relies on `ct state established,related accept` preserving in-flight flows across reload; on the first reload at container start, there are no connections (moot); but any future init-script adding a pre-policy outbound flow (e.g., a probe) would leave that flow with no conntrack state when the first `nft -f` applies rules that require `ct state established,related accept`. If `nf_conntrack` module isn't loaded for the netns before the first apply (unusual but possible on restricted kernels), established flows get dropped. _Defer reason:_ moot on first boot per current `entrypoint.sh` sequence (no outbound flows before start-egress); fragile if a future init-script changes. _Carry-to:_ operator-workstation bake-time verification via `modprobe nf_conntrack` pre-flight in start-egress, OR document in AGENTS.md/Story-2.13 that nothing may open an outbound connection before start-egress completes.
- **monitor.sh `jq` pipeline fragile against malformed lines.** _Source:_ Edge Case Hunter LOW. _Location:_ `packages/devbox/scripts/monitor.sh:21`. _Nature:_ `tail -Fn0 … | jq -c --unbuffered '.'` under `set -euo pipefail` exits non-zero if any JSONL line is malformed (partial write during rotation, parse-error payload with incomplete `tr -d` cleanup); operator tool with a re-run recovery path, not production path. _Defer reason:_ operator-facing; re-run is the recovery; low impact. _Carry-to:_ operator-UX polish (hardening pass) — pipe through `jq -c --unbuffered -R 'fromjson? // .'` so malformed lines pass through as raw strings rather than crashing jq.

## Deferred from: code review of 2-3-egress-policy-dnsmasq-nftables-fail-closed-ipv4-ipv6-parity-atomic-reload.md (2026-04-21 re-run)

_Iter-171 `/bmad-code-review (args: "2")` closure gate after the 9-PATCH drain (AR-1..AR-9 landed iter-162..iter-170). Three-layer Ralph-hosted adversarial fan-out against drain-only diff (bfcd500..HEAD scoped to packages/devbox/, docs/invariants/, packages/keel-invariants/) produced 0 PATCH + 0 DECISION_NEEDED + 8 DEFER + ~36 DISMISS. These 8 deferred items are all defensive-hardening / spec-amendment / diagnostic-precision candidates out-of-scope for a code-review fix-drain. None block the iter-171 `fixes-pending → done` transition; all are sanctioned carry-overs to downstream stories or future `/bmad-edit-prd` / observability-hardening passes._

- **D1 — SC-13 spec-text amendment to carve out AR-1 bootstrap-detour.** _Source:_ Acceptance Auditor iter-171. _Location:_ `_bmad-output/implementation-artifacts/2-3-egress-policy-dnsmasq-nftables-fail-closed-ipv4-ipv6-parity-atomic-reload.md` §Scope clarifications SC-13 (lines 65–66) + `packages/devbox/scripts/reload-egress.sh:156-161`. _Nature:_ SC-13's literal prose "DO NOT write `8.8.8.8`, `1.1.1.1`, or any upstream to `resolv.conf`" was not amended when iter-161 CR elected AR-1's bootstrap-detour; AR-1 temporarily writes `${UPSTREAM_RESOLVER}` (default `1.1.1.1`) to `/etc/resolv.conf` during first-boot resolution before `restore_resolv_pin()` re-pins to `127.0.0.1` on every exit path. SC-13 steady-state contract (`nameserver 127.0.0.1` at steady state) holds on every exit via the EXIT trap; only the first-boot resolution window (getent calls) temporarily repoints. _Defer reason:_ material SC-text amendments are out-of-scope for a code-review fix-drain; the iter-161 CR process implicitly elected AR-1 as the narrowest-scope fix and drained it `[x]` without spec amendment. _Carry-to:_ **`/bmad-edit-prd` pass or dedicated spec-amendment story** — amend SC-13 to add a carve-out for the first-boot bootstrap window with explicit pre/post-condition language matching the `restore_resolv_pin()` contract in code.
- **D2 — Domain regex validation at reload time.** _Source:_ Edge Case Hunter iter-171. _Location:_ `packages/devbox/scripts/reload-egress.sh:181-200,263-288`. _Nature:_ `reload-egress.sh` interpolates each whitelist domain into `server=/<domain>/<upstream>` dnsmasq directives and `ip daddr <ip> accept` nftables rules without `[[ =~ ^[A-Za-z0-9.-]+$ ]]` sanitisation; a malformed whitelist entry (accidental `/`, shell metacharacter, zero-width unicode, percent-encoded) would produce a malformed dnsmasq directive → SIGHUP fails → dnsmasq reload stalls → fail-open gap. _Defer reason:_ whitelist source is developer-controlled + source-versioned at Story 2.3 (baseline `whitelist.default.txt` + `whitelist/*.txt` fragments under repo control); threat model does not include malicious whitelist content. _Carry-to:_ **Story 2.4 `whitelist.sh sync` CLI** is the first operator-editable entry point — domain regex validation should land there so malformed input never reaches `reload-egress.sh`.
- **D3 — SIGTERM-during-flock-trap gap.** _Source:_ Edge Case Hunter iter-171. _Location:_ `packages/devbox/scripts/reload-egress.sh:105-132`. _Nature:_ ~24-line window between `exec 200>"${LOCK_FILE}"` + `flock -x -w 10 200` and `trap cleanup EXIT` registration leaves a tiny race where SIGTERM could leak fd 200 without invoking `cleanup()`. Impact is recoverable (kernel auto-releases fd 200 on process exit; next reload re-acquires flock cleanly). _Defer reason:_ low-likelihood (container receives SIGTERM at shutdown not mid-reload); fd 200 leak is recovered on the next reload; no persistent corruption. _Carry-to:_ observability-hardening pass OR register `trap cleanup EXIT` immediately after `set -euo pipefail` with a no-op cleanup body that's populated later; inverts the current scope-before-trap ordering.
- **D4 — Tailer liveness check post-`nohup` launch.** _Source:_ Edge Case Hunter iter-171. _Location:_ `packages/devbox/scripts/start-egress.sh:125-128`. _Nature:_ `start-egress.sh` records `$!` to `TAILER_PID_FILE` but does not verify the tailer is still alive after a short settle; a tailer that exits immediately (script syntax error, missing `/var/log/dnsmasq.log`, binary incompat) leaves JSONL emitter silently dormant. FR37 security-evidence stream is silently cut; fail-closed enforcement is preserved by nftables + dnsmasq independently. _Defer reason:_ observability concern not enforcement; mirrors the already-deferred "egress-log-tailer.sh has no supervisor" item at iter-161 but at a different layer (launch vs steady-state). _Carry-to:_ **Story 2.13 healthcheck OR Epic 4 FR37 consumer** — same supervisor pattern as the iter-161 deferred observability gap.
- **D5 — getent NODATA not explicitly distinguished from resolution failure.** _Source:_ Edge Case Hunter iter-171. _Location:_ `packages/devbox/scripts/reload-egress.sh:182-199`. _Nature:_ when upstream returns NOERROR NODATA (domain valid, no A record), `getent ahostsv4` exits 0 with empty stdout; current code emits no rule and no WARN, making NODATA indistinguishable from the populated-stdout happy path in logs. Fail-closed default still handles the no-rule case correctly (client sees DNS-level NODATA before hitting layer-3). _Defer reason:_ diagnostic precision improvement; not an AC violation. _Carry-to:_ observability-hardening pass — add an `[[ -z "${ipv4_raw// }" ]] && log "WARN: ipv4 NODATA for ${domain}"` branch inside the `if getent` success path.
- **D6 — `mv /etc/dnsmasq.conf` cross-filesystem failure not explicitly mapped to exit 7.** _Source:_ Edge Case Hunter iter-171. _Location:_ `packages/devbox/scripts/reload-egress.sh:285-295`. _Nature:_ `mv "${dnsmasq_rendered}" "${DNSMASQ_CONF}"` after a successful `nft -f` apply could fail on a cross-filesystem rename if `/etc` and `TMPDIR` are on different overlay mounts; under `set -e` the script aborts without the documented exit-7 diagnostic, leaving nftables applied + dnsmasq on stale config. _Defer reason:_ low-likelihood on standard devbox image layout (`/etc` and `TMPDIR` share overlay fs); partial-state scenario is auditable via the exit code. _Carry-to:_ broader SC-5 partial-state audit — map every fallible seam after `nft -f` to an explicit exit code with diagnostic rather than relying on `set -e` abort.
- **D7 — Drain diff carries no regression tests for new preflight/bootstrap/probe logic.** _Source:_ Blind Hunter iter-171. _Location:_ `packages/devbox/scripts/*` (AR-1 bootstrap-detour in `reload-egress.sh`; AR-5 composite-probe in `start-egress.sh`; AR-6 `validate_markers()` preflight in `reload-egress.sh`; AR-9 `mapfile+find` fragment enumeration in `start-egress.sh`). _Nature:_ all four substantive new behaviours ship without `*_test.sh` / shellcheck-harness / fixture coverage. _Defer reason:_ pre-existing condition — Story 2.3 ATDD was explicitly skipped at iter-157 per the `atdd-skip` carve-out; test-runner hasn't landed yet in the project (Epic 13 scope). _Carry-to:_ **Epic 13 CI harness smokes** — when Epic 13 lands a devbox test harness, backfill fixtures for each AR-N behaviour (bootstrap-detour + composite-probe + marker-preflight + mapfile-enumeration).
- **D8 — `INV-devbox-egress-contract` manifest entry lacks `description` / `anchors` enrichment alongside AR-5 contentHash bump.** _Source:_ Blind Hunter iter-171. _Location:_ `packages/keel-invariants/src/invariants.manifest.ts:257-263`. _Nature:_ the iter-166 refresh updated `sourcePath` contentHash to match `docs/invariants/devbox-egress.md`'s new composite-probe section but did not add per-section anchors beyond the existing top-level `INV-devbox-egress-contract` anchor. Readers scanning the manifest see an opaque hash change with no hint that AR-5/AR-6/AR-7/AR-8/AR-9 moved underlying sub-contracts. _Defer reason:_ manifest-UX enhancement; the sync-gate (Story 1.9 FR43) only requires contentHash parity, not anchor precision. _Carry-to:_ invariants-manifest UX pass OR a post-1.0 story that extends the schema with per-hash provenance metadata (iter-N references, drain-item IDs).

## Deferred from: code review of story-2-4 (2026-04-22)

_Story 2.4 (Whitelist source-of-truth + `pnpm devbox:whitelist` atomic-reload CLI) — iter-177 `/bmad-code-review (args: "2")` CR opener. Three-layer Ralph-hosted adversarial fan-out (Blind Hunter cynical review 37 findings / Edge Case Hunter JSON path-tracer 33 entries / Acceptance Auditor spec-fidelity — ALL 5 ACs SATISFIED zero violations). Triage: 5 PATCH (AI-1..AI-5 queued for iter-178..iter-182 drain) + 4 DEFER (below) + ~30 DISMISS. First user-facing-CLI class CR opener precedent — patches cluster at defensive-boundary of CLI layer (filesystem atomicity, redirect/rename primitives, perm-change silence, tempfile predictability) rather than kernel/daemon boundaries (Story 2.3 class)._

- **D1 — `LC_ALL=C` around validator regex + length check.** _Source:_ Blind Hunter iter-177 (finding #11) + confirmatory Edge Case E18. _Location:_ `packages/devbox/scripts/whitelist.sh:121-173` (`validate_sources`) + `whitelist.sh:176-182` (`validate_domain`). _Nature:_ bash `[[ =~ ]]` POSIX ERE matching and `${#var}` character-count operator are locale-sensitive under `LC_CTYPE`. The DOMAIN_REGEX character classes `[a-zA-Z0-9-]` are ASCII-scoped and the 253-byte bound is strictly byte-scoped per RFC 1035 SC-5 pin; under non-C locales a regex `a-z` range could unexpectedly match non-ASCII "lowercase" codepoints AND `${#var}` counts characters (not bytes) — mis-accepting multibyte-encoded "valid" domains that exceed 253 bytes. _Defer reason:_ vacuous in current runtime — in-container Ubuntu 24.04 defaults to `C.UTF-8` LC_CTYPE which under glibc keeps `a-z` range ASCII-literal + keeps `${#var}` character-count consistent with byte-count for pure-ASCII input (and DOMAIN_REGEX rejects non-ASCII before `${#}` runs). _Carry-to:_ **Story 2.6 host-side pnpm CLI** wrapping — macOS hosts with non-C `LANG` (e.g., `en_US.UTF-8` + case-folding locales) + Windows Git-Bash locale variance warrant explicit `LC_ALL=C LC_COLLATE=C` prefix on the validator pipeline; aligns with the host-ingress invocation-env hardening scope.
- **D2 — Trap extension to `INT TERM HUP` + explicit fd-201 close in cmd_add/cmd_remove atomic-write sections.** _Source:_ Blind Hunter iter-177 (finding #10) + Edge Case E7 + E10 + E27. _Location:_ `packages/devbox/scripts/whitelist.sh:200,303,387` (EXIT-only traps; fd 201 mutation lock released only via explicit `exec 201>&-` at success-path lines 324, 395 + EXIT-driven process exit). _Nature:_ bash EXIT trap fires on most signal-induced termination via implicit shell-cleanup chain, and fd 201 is kernel-reaped on process exit; there is no observable current leak. Defensive-tightening would add explicit `INT TERM HUP` signal handlers that cleanly `exec 201>&-` before abort, making the cleanup deterministic even if the script is embedded in a long-running supervisor (e.g., future `pnpm devbox:watch` wrapper that spawns cmd_add under a persistent shell). _Defer reason:_ no exploitation path in current 1.0 posture (single-shot CLI invocation per `docker exec` / `pnpm` call); cleanup is kernel-guaranteed at process exit. _Carry-to:_ **Story 2.5 container-hardening sweep** — the non-root `dev` user + `cap_drop: [ALL]` + `security_opt: [no-new-privileges:true]` changes the process-lifetime model (e.g., a `dev`-user supervisor loop re-exec'ing whitelist.sh would benefit from deterministic cleanup) enough to warrant a cross-cutting signal-handling audit across all Story 2.3/2.4 scripts (start-egress.sh, reload-egress.sh, whitelist.sh, egress-log-tailer.sh, monitor.sh).
- **D3 — `reload-egress.sh` executable pre-check in cmd_sync.** _Source:_ Edge Case Hunter iter-177 (E20). _Location:_ `packages/devbox/scripts/whitelist.sh:250` (`"${RELOAD_SCRIPT}" "${COMPOSED_WHITELIST}" || reload_rc=$?`). _Nature:_ the direct `${RELOAD_SCRIPT}` invocation would exit 127 under `set -e` if the script is missing or non-executable — bypassing the SC-11 `5/6/7` passthrough contract with a confusing diagnostic (operator sees "exec format error" / "no such file" rather than "reload-egress.sh missing or non-executable"). Cleanest guard is a `[[ -x "${RELOAD_SCRIPT}" ]] || { log "ERROR: reload-egress.sh not executable at ${RELOAD_SCRIPT}"; exit 3; }` preflight. _Defer reason:_ zero operational exposure in current runtime — `reload-egress.sh` is installed alongside `whitelist.sh` in the baked devbox image (Story 2.1 Dockerfile copies both with 0755 perms) and has been executable since iter-158 Story 2.3 landing. The pre-check is a diagnostic-quality improvement, not a correctness fix. _Carry-to:_ **Story 2.3 primitive-polish follow-up** (natural home — the contract "reload-egress.sh must be executable" is Story 2.3's SC-13 commitment; Story 2.4 as downstream consumer inherits but does not own) OR **Epic 13 CI harness smoke** that exercises a deliberately-broken RELOAD_SCRIPT path to verify exit-3 diagnostic.
- **D4 — `whitelist.local.txt` CRLF line-ending normalisation in grep-Fxq / grep-Fxv operations.** _Source:_ Edge Case Hunter iter-177 (E14) + Blind Hunter concurrence (finding #15). _Location:_ `packages/devbox/scripts/whitelist.sh:293,379,390` (`grep -Fxq -- "${domain}" "${WHITELIST_LOCAL}"` idempotent-add + idempotent-remove-check; `grep -Fxv -- "${domain}" "${WHITELIST_LOCAL}" > "${tempfile}"` remove-filter). _Nature:_ `grep -Fx`'s byte-exact match treats a CR-suffixed line (`domain.com\r\n`) as distinct from the input literal (`domain.com`). An operator who hand-edits `whitelist.local.txt` via a Windows/SMB pathway and preserves CRLF would observe: (a) `add domain.com` silently duplicates (idempotent-check misses the CR-suffixed entry → atomic-append writes a second copy; `sort -u` during compose collapses them but the `log "... already present; no-op"` path is bypassed and the mutation lock / reload cycle fires spuriously); (b) `remove domain.com` hits the "not present" idempotent no-op path (false-negative) even when a CR-suffixed entry exists. _Defer reason:_ current in-container edit pathway is `docker exec keel-devbox vi /workspace/.../whitelist.local.txt` which preserves LF line-endings; the hand-edit-via-Windows-SMB pathway is not a documented 1.0 invocation mode. _Carry-to:_ **Epic 13 devbox operator-UX hardening pass** (defensive normalisation at grep boundaries via `tr -d '\r' < file | grep -Fxq ...`) OR **Story 2.6 host-side pnpm CLI wrapping** (the host→container CRLF bridge is a more natural concern at that layer — if `pnpm devbox:whitelist add` on macOS/Windows writes directly to a mounted `whitelist.local.txt`, the CRLF-preservation risk reappears at the host layer).

## Deferred from: code review RE-RUN of story-2.4 (2026-04-22)

_Story 2.4 iter-183 `/bmad-code-review (args: "2")` CR re-run after iter-178..iter-182 AI-1..AI-5 surgical-Edit drain (5 PATCHes closed one-per-iter per Ralph Guardrail 5). Three-layer Ralph-hosted adversarial fan-out (Blind Hunter + Edge Case Hunter + Acceptance Auditor) against Story-2.4-scoped post-drain diff `770e4e7^..84fff37` (5 substrate files: `whitelist.sh`, `start-egress.sh`, `whitelist.default.txt`, `README.md`, `package.json`; 625 patch lines). **Acceptance Auditor verdict: CR RE-RUN CLOSES ZERO-PATCH** — all 5 ACs SATISFIED byte-identical to iter-177 opener reading; all 5 AI-N drain commits PRESENT AND CLOSE their opener findings with file-line evidence (AI-1 `:300` `/tmp→/run`; AI-2 `:493-502` rc-capture; AI-3 `:344,:360` chmod fail-loud; AI-4 `:404,:489` mktemp; AI-5 README `:263-268` exit-code table); no regressions detected; no new HIGH findings surfaced outside opener's scope. **Blind Hunter + Edge Case Hunter triage: ZERO PATCH + 13 DEFER + 4 DISMISS.** The 13 DEFERs below are all defensive-hardening post-1.0 CLI refinement (mv-before-reload cosmetic drift; `$(cat)` trailing-newline normalisation; diff-summary blank-line formatting; GNU-diff flag portability; compose-enumeration TOCTOU fail-closed-safe; cmd_add persists-on-validate-fail narrow-precondition; cmd_add substrate-duplicate CLI-usability; mktemp/mkdir adverse-env diagnostics; cmd_remove zero-byte-file; cmd_list no-shared-lock; cp-non-atomic snapshot crash-window; locale-sensitive regex; symlink clobber); none are exploitable under the 1.0 root-posture single-operator single-container posture. **4 DISMISSes** (not persisted here): (1) Blind Hunter self-downgraded trap-shadowing; (2) Edge Case Hunter "cmd_sync lacks MUTATE_LOCK" architecture-sanctioned per SC-8; (3) **DOMAIN_REGEX multi-line bypass FALSE POSITIVE** empirically verified — bash `[[ =~ ]]` uses POSIX ERE where `$` = end-of-string (not end-of-line; REG_NEWLINE not set); multi-line input returns NO_MATCH; `validate_sources` reads per-line independently; (4) validate_sources docstring aggregation applies to exit-2 (validation errors) not exit-3 (readability failures — distinct category, fail-fast correct)._

_Closure: FR14n Story State `fixes-pending → done`; Status `fixes-pending → done`; sprint-status row `2-4: review → done`; Epic 2 counter 3/17 `done` → 4/17 `done`; PR #230 stays Draft (Epic 2 closes at Story 2.17). Fifth Epic-2 CR re-run ZERO-PATCH closure (Story 2.1 iter-133 / 2.2 iter-154 / 2.3 iter-171 / 2.4 iter-183); 6-iter Story 2.4 chain at forecast (trace + SM + opener + 5-PATCH drain + CR re-run = 6)._

- **RR-D1 — mv onto `COMPOSED_WHITELIST` precedes reload-egress.sh success check, creating disk/kernel drift window on reload failure.** _Source:_ Blind Hunter. _Location:_ `packages/devbox/scripts/whitelist.sh:343-358`. _Nature:_ `cmd_sync` does atomic `mv "${tempfile}" "${COMPOSED_WHITELIST}"` + chmod + trap-clear at lines 343-346, THEN invokes `reload-egress.sh` at :352. If reload returns non-zero, `COMPOSED_WHITELIST` on disk reflects the new staged state while the kernel's active ruleset stays on the previous policy (reload-egress.sh's own fail-closed contract). No fail-open at the kernel layer (reload-egress.sh correctly rolls back on nft/dnsmasq failure); PREVIOUS_COMPOSED intentionally NOT updated on reload-fail path (lines 350-351) so next sync's diff baseline stays correct. However, tools that read COMPOSED_WHITELIST as "current active allow-list ground truth" (none in the 1.0 codebase — reload-egress.sh reads it as INPUT, not as state-of-truth) would disagree with kernel state during the fail-window. _Defer reason:_ cosmetic diagnostic-only under reload failure; no AC violation (AC 3 fail-closed preserved at kernel layer); next sync converges either via operator retry or via automatic healthcheck (Story 2.13 future). Future-Ralph refinement: snapshot+restore `COMPOSED_WHITELIST` on reload-fail OR pass `tempfile` path to reload-egress.sh and rename onto canonical path only on reload success. _Carry-to:_ **Story 2.13 (healthcheck on dnsmasq/sshd)** — natural home since healthcheck semantics need to reason about "intended vs applied" policy; OR **post-1.0 CLI polish iteration** if a simpler snapshot+restore approach is preferred.
- **RR-D2 — `$(cat "${WHITELIST_LOCAL}")` round-trip adds trailing newline when source file lacks one.** _Source:_ Blind Hunter. _Location:_ `packages/devbox/scripts/whitelist.sh:412-416`. _Nature:_ `existing="$(cat "${WHITELIST_LOCAL}")"` in `cmd_add` uses command substitution, which strips trailing newlines per POSIX semantics. Then `printf '%s\n' "${existing}"` re-emits the content with a terminating `\n`. Net effect: if operator hand-edited `whitelist.local.txt` without a final newline, the first `cmd_add` normalises it to end in `\n`. This is benign normalisation (POSIX convention dictates text files end in `\n`) but operators who deliberately preserved the no-trailing-newline form may observe an unexpected byte-level mutation. Command substitution also strips NUL bytes silently (only relevant if the file was ever corrupted — impossible via CLI; possible via out-of-band hand-edit). _Defer reason:_ benign normalisation; aligns with POSIX convention; no data corruption. The scale concern (O(size) memory for the round-trip) is vacuous — whitelist.local.txt is expected to be <100 entries. _Carry-to:_ **post-1.0 CLI polish** — refactor to `{ [[ -f "${WHITELIST_LOCAL}" ]] && cat "${WHITELIST_LOCAL}"; printf '%s\n' "${domain}"; } > "${tempfile}"` with optional `tail -c1` probe to preserve original form.
- **RR-D3 — `printf '%s\n' "${additions}"` in diff summary emits a trailing blank line between `+` additions and `-` removals.** _Source:_ Blind Hunter. _Location:_ `packages/devbox/scripts/whitelist.sh:334,339,367`. _Nature:_ `additions` and `removals` are pre-newline-terminated (each is output of `awk 'NF{print ...}' | sort`). `printf '%s\n' "${non-empty}"` adds a stray `\n` between additions block and removals block. Cosmetic: diff summary still readable and parseable, but renders with a blank line separator instead of a tight `+…\n-…` block. No functional defect; SC-7 "grouped +/-" contract preserved at the alphabetical-within-group level. _Defer reason:_ cosmetic formatting only. _Carry-to:_ **post-1.0 docs/output polish** — switch to `echo "${additions}"` OR `printf '%s' "${additions}$(echo)"` to omit trailing `\n` when followed by more output.
- **RR-D4 — GNU `diff --new-line-format` / `--old-line-format` flags not portable to BSD/macOS diff.** _Source:_ Blind Hunter. _Location:_ `packages/devbox/scripts/whitelist.sh:318`. _Nature:_ `diff --new-line-format='+%L' --old-line-format='-%L' --unchanged-line-format='' prev new || true` uses GNU-diff-specific format-string extensions. Story 2.4 ships the IN-CONTAINER CLI (SC-16 operator invocation locus) where diff is GNU coreutils 9.4 — portable within that scope. The concern: when Story 2.6 lands host-side `pnpm devbox:whitelist` wrapper that `docker exec`s the script, the macOS/BSD host diff is never invoked (exec'd inside the container). So the portability concern is vacuous at 1.0. However, the `|| true` mask would swallow a GNU-diff failure silently, misreporting the SC-7 diff summary. _Defer reason:_ out of Story 2.4 scope — host-side portability is Story 2.6's concern AND the `|| true` mask is a separate defensive-nit (the in-container runtime diff has no failure mode that `|| true` would mask — sources are pre-validated by validate_sources + tempfile is in-memory-then-file). _Carry-to:_ **Story 2.6 (host-side pnpm devbox:* CLI surface)** — if the wrapper ever composes diff on host, swap to `comm -23`/`comm -13` on pre-sorted inputs (POSIX-portable).
- **RR-D5 — `compose_whitelist_into` silent-skips fragment that becomes unreadable between `find` enumeration and `cat`.** _Source:_ Blind Hunter (defensive-nit). _Location:_ `packages/devbox/scripts/whitelist.sh:200-205`. _Nature:_ TOCTOU race: `validate_sources` enumerates sources (exit 3 on first-seen-unreadable); `compose_whitelist_into` re-enumerates (`[[ -r "${fragment}" ]] || continue` silently skips). If a fragment becomes unreadable between the two enumerations (permissions change, deletion, mount unmount), validate passes and compose silently drops the fragment — resulting allow-list is a SUBSET of expected. Fail-closed safe (smaller allow-list means legitimate traffic is dropped, not extra traffic allowed). _Defer reason:_ very narrow race window (microseconds between two enumerations in the same process); fail-closed outcome; no security exposure. _Carry-to:_ **post-1.0 CLI polish** — symmetric exit-3 on unreadable fragment in `compose_whitelist_into` OR fd-based open-held-across-both-ops.
- **RR-D6 — `cmd_add` persists new domain to disk even when pre-existing sibling lines fail `validate_sources` in the subsequent `cmd_sync`.** _Source:_ Edge Case Hunter. _Location:_ `packages/devbox/scripts/whitelist.sh:319-327`. _Nature:_ Precondition: operator hand-edited an invalid line into `whitelist.local.txt` (SC-1 positions add/remove as canonical mutation surface — this precondition is non-canonical use). Flow: `cmd_add valid.com` validates the arg (passes), atomic-writes `whitelist.local.txt` (persists `valid.com`), releases mutation lock, invokes `cmd_sync`. cmd_sync runs `validate_sources` → sees the pre-existing invalid line → exits 2. Disk state: `whitelist.local.txt` advanced with the new valid domain. Kernel state: previous policy (no reload invoked; fail-closed). Disk-vs-policy divergence until operator fixes the invalid line + re-syncs. Recovery path: fix invalid line, run `sync`, converges. No security/correctness violation; fail-closed holds. _Defer reason:_ narrow precondition (non-canonical operator hand-edit); self-recovering; fail-closed. _Carry-to:_ **post-1.0 CLI polish** — (a) validate all sources BEFORE `mv` in cmd_add (atomic precondition), OR (b) rollback `whitelist.local.txt` to pre-add state on cmd_sync validate-fail.
- **RR-D7 — `cmd_add` allows substrate-duplicate into `whitelist.local.txt`; `cmd_remove` blocks its cleanup via substrate-protection.** _Source:_ Edge Case Hunter. _Location:_ `packages/devbox/scripts/whitelist.sh:293-298,456-478`. _Nature:_ `cmd_add github.com` where `github.com` is already in `whitelist/anthropic.txt` fragment: idempotency check at :293-298 only reads `WHITELIST_LOCAL` (not composed union), so the append proceeds — `whitelist.local.txt` now has a redundant `github.com` entry. Later `cmd_remove github.com` hits substrate-protection at :456-478 → exit 2 (per SC-1 "remove cannot edit substrate"). The redundant L-entry is wedged in `whitelist.local.txt` with no CLI-based removal path. Workaround: operator hand-edits `whitelist.local.txt` (it's the operator's own file per SC-2/SC-3, NOT substrate). Fail-closed preserved: `sort -u` dedupes the duplicate in composed output; kernel ruleset is correct. _Defer reason:_ CLI-usability gap, not correctness defect. Operator has a clear workaround (hand-edit own file). _Carry-to:_ **post-1.0 CLI polish** — on cmd_add, compose substrate (default + fragments) and skip append if domain is already covered. Consistent with SC-4 "additive-only composition" — redundant L-entry adds no policy, should be a no-op.
- **RR-D8 — `mktemp` / `mkdir -p /run` / `mktemp "${WHITELIST_LOCAL}.XXXXXX.tmp"` produce uninformative errors under read-only DEVBOX_DIR or read-only rootfs.** _Source:_ Edge Case Hunter. _Location:_ `packages/devbox/scripts/whitelist.sh:286-288,302,489`. _Nature:_ Under adverse environments (CI sandbox with ro-mount, hardened container with /run on ro overlay, security-hardened devbox derivative), `mktemp` or `mkdir -p` fails with raw coreutils stderr ("mktemp: failed to create file via template", "mkdir: cannot create directory") — no `whitelist:` log prefix, no SC-11 exit-3 wrapping, no operator-targeted context. `set -e` propagates the non-zero rc correctly (fail-loud), but diagnosability is poor. _Defer reason:_ adverse-environment diagnostic-only; 1.0 devbox runtime has /run as writable tmpfs + DEVBOX_DIR writable (verified Story 2.1 iter-127 bake). _Carry-to:_ **Story 2.5 container-hardening sweep** — when tmpfs/named-volume topology is revised, add writability probes at ensure_run_dir + cmd_add/cmd_remove entry; log contextual `exit 3` with diagnostic.
- **RR-D9 — `cmd_remove` leaves zero-byte `whitelist.local.txt` after removing the last entry instead of unlinking.** _Source:_ Edge Case Hunter. _Location:_ `packages/devbox/scripts/whitelist.sh:497-504`. _Nature:_ When `whitelist.local.txt` contains one entry and operator removes it, `grep -Fxv` yields empty tempfile; `mv` replaces target with zero-byte file. `stat` / `ls -la` shows file present; `cmd_list` shows zero L-entries; compose pipeline correctly reads empty file as zero additions (fail-closed preserved). Asymmetric with SC-9's "remove with non-existent target = no-op success" semantics: a file that was deleted vs never-existed is not distinguished in CLI UX. _Defer reason:_ cosmetic; no policy impact. _Carry-to:_ **post-1.0 CLI polish** — `[[ -s "${tempfile}" ]] || rm -f "${WHITELIST_LOCAL}"` after mv, mirroring SC-9 symmetry.
- **RR-D10 — `cmd_list` reads sources without shared lock.** _Source:_ Edge Case Hunter. _Location:_ `packages/devbox/scripts/whitelist.sh:519-563`. _Nature:_ `cmd_list` reads `whitelist.default.txt` + `whitelist/*.txt` fragments + `whitelist.local.txt` without acquiring fd-201 mutation lock. Concurrent `cmd_add`/`cmd_remove` atomic-mv can produce split-brain source attribution: the same domain might be labelled `L` in one scan (read after remove's mv of empty file) and `F:<frag>` in a later invocation. `mv` on same-fs is kernel-atomic, so partial-file reads are impossible — but `list`'s multi-file scan is NOT atomic across files, so the snapshot is inconsistent under high-concurrency. No state corruption (list is read-only); attribution cosmetic drift only. _Defer reason:_ diagnostic-only; list is read-only. _Carry-to:_ **post-1.0 CLI polish** — `flock -s -w 10 201 || exit 4` shared lock over the full scan; pairs with cmd_list's existing fd-201 convention in cmd_add/cmd_remove.
- **RR-D11 — `cp -f "${COMPOSED_WHITELIST}" "${PREVIOUS_COMPOSED}"` is non-atomic; SIGKILL/crash between reload success and snapshot update leaves PREVIOUS_COMPOSED stale.** _Source:_ Edge Case Hunter. _Location:_ `packages/devbox/scripts/whitelist.sh:357-358`. _Nature:_ Sequence: reload-egress.sh returns 0 at :352, then `cp -f COMPOSED → PREVIOUS` at :357-358. Window between these two steps: SIGKILL/poweroff leaves PREVIOUS_COMPOSED reflecting the PRE-reload state while kernel state reflects POST-reload. Next sync's SC-7 diff misreports additions/removals (shows all reloaded domains as "new" relative to stale previous). No policy impact (kernel is correctly on current policy); cosmetic diff-on-next-sync misattribution. Converges on next successful sync (PREVIOUS gets updated). _Defer reason:_ narrow crash window; self-correcting. _Carry-to:_ **post-1.0 CLI polish** — `cp -f` to adjacent tempfile then `mv` for atomic snapshot update; OR `ln -f COMPOSED PREVIOUS` (hardlink) if same-fs, avoiding the copy entirely.
- **RR-D12 — Locale-sensitive DOMAIN_REGEX via bash `[[ =~ ]]` under non-C `LC_CTYPE`.** _Source:_ Edge Case Hunter (new angle; pairs with iter-177 opener D1 defer). _Location:_ `packages/devbox/scripts/whitelist.sh:155`. _Nature:_ bash `[[ =~ ]]` honours `LC_CTYPE` in some implementations. Under locales where `[0-9]` in POSIX bracket expressions expands to Unicode digits (Arabic-Indic, Devanagari), the validator could accept digits that dnsmasq/nft reject downstream. In-container runtime is `C.UTF-8` (Ubuntu 24.04 default) where `[0-9]` is ASCII-only — zero active exposure. When Story 2.6 lands host-side pnpm wrappers, macOS operators may have `LC_CTYPE=en_US.UTF-8` where behavior is implementation-defined. _Defer reason:_ vacuous in current runtime; downstream fail-closed. Subsumes iter-177 opener defer D1 (which also called out `LC_ALL=C` scoping) — narrower in scope but same remediation. _Carry-to:_ **Story 2.6 host-side pnpm CLI wrapping** — add `LC_ALL=C` local scoping at validate_domain + validate_sources sites; mirrors `LC_ALL=C` used on `sort -u` at :214.
- **RR-D13 — `mv tempfile whitelist.local.txt` clobbers symlink target if operator symlinked `whitelist.local.txt` for shared fork state.** _Source:_ Edge Case Hunter. _Location:_ `packages/devbox/scripts/whitelist.sh:319,503`. _Nature:_ An operator who symlinks `whitelist.local.txt → /shared/whitelist.txt` (e.g., multi-devbox fork with shared overrides) would see: `mv tempfile whitelist.local.txt` removes the symlink and replaces with a regular file on first mutation, breaking the shared-state contract. Subsequent devboxes see stale state. Undocumented usage — the 1.0 spec does not mention symlinks; SC-2 positions the override as a gitignored per-fork file (not cross-fork shared). _Defer reason:_ unusual fork setup; undocumented usage; operator self-assumed the symlink semantic. _Carry-to:_ **post-1.0 CLI polish** OR **docs clarification** — at cmd_add/cmd_remove entry, reject symlinked override (`[[ ! -L "${WHITELIST_LOCAL}" ]]`) with operator-education message pointing to `docker volume` / bind-mount patterns for cross-devbox sharing.

## Deferred from: code review of story-2-5 (2026-04-22)

_Iter-190 `/bmad-code-review (args: "2")` CR opener against cumulative Story 2.5 substrate diff (`b2533b0..HEAD` / 8 SC-19 in-scope files / 413 lines) produced 6 PATCH + **4 DEFER** + ~17 DISMISS. The four deferred items are all integration-boundary or scope-extension concerns sanctioned by spec (SC-14, SC-10) or deferrable to downstream stories. None block the iter-196 `fixes-pending → sm-verified → done` CR-closure gate; all are carried over to Story 2.6 (host-side CLI + operator-migration docs), Story 2.17 (runtime compose-shape check), or operator-workstation smoke._

- **AR-7 — /run writes under USER dev fail for whitelist.sh state files + dnsmasq pid-file.** _Source:_ Edge Case Hunter iter-190 (probes EC3 + EC12 + EC13 merged). _Location:_ `packages/devbox/scripts/whitelist.sh:198,286,341` + `packages/devbox/scripts/start-egress.sh:30-32` + `packages/devbox/dnsmasq/dnsmasq.conf:52`. _Nature:_ Under USER dev (Dockerfile:268), processes run as UID 1000. `/run` in the Ubuntu base image is root:root mode 0755; dev cannot mkdir/write. Whitelist.sh expects to mktemp/flock/write under `/run/keel-whitelist-*` (lines 198/286/341); start-egress.sh expects `/run/keel-whitelist.composed.txt` + `/run/dnsmasq.pid` + `/run/keel-egress-tailer.pid`; dnsmasq expects `/run/dnsmasq.pid`. All fail EACCES if Docker's tmpfs auto-mount at /run does NOT preserve image-layer ownership. _Defer reason:_ SC-14 branch (ii) is the explicit Story 2.5 spec for this — if operator-workstation smoke surfaces ownership problem, relocation to `/tmp/keel-state/` is a Story 2.6 carry-over. iter-187 assumed SC-14 branch (i) happy path (Docker tmpfs auto-mount preserves image-layer ownership established by `RUN chown dev:dev /run` at Dockerfile-build time, which Story 2.5 did NOT add — branch (i) relies on implicit tmpfs-auto-mount-preserves-image-ownership which is uncertain). _Carry-to:_ **Story 2.6 host-side `pnpm devbox:*` CLI surface** — which is the scope-owner of `scripts/whitelist.sh` entry-points + is-in-scope to relocate state files. Concrete remediation: either (a) Dockerfile pre-creates `/run` as dev-owned (`RUN chown dev:dev /run` — may not survive Docker's tmpfs auto-mount) OR (b) relocate all `/run/keel-*` paths to `/tmp/keel-state/` (tmpfs with `exec=false,suid=false` is fine for state files) across whitelist.sh + start-egress.sh + reload-egress.sh + dnsmasq.conf. Branch (b) is scope-extensive but avoids the tmpfs-ownership-empirics uncertainty.

- **AR-8 — Fork `docker-compose.override.yml` may replace `volumes:` stanza or strip `NET_BIND_SERVICE` from `cap_add`.** _Source:_ Edge Case Hunter iter-190 (probes EC10 + EC15 merged). _Location:_ `packages/devbox/docker-compose.yml:118-125,154-155`. _Nature:_ Compose's default merge strategy for `services.<svc>.volumes` + `services.<svc>.cap_add` arrays is REPLACE (not merge) — a fork's `docker-compose.override.yml` with a differently-shaped `volumes:` block or `cap_add:` list silently bypasses the `INV-devbox-homedev-named-volume` invariant. Upstream cc-devbox pattern `./dev-home:/home/dev:delegated` bind-mount is an obvious override-replacement that would leak Claude Code OAuth tokens to host filesystem, defeating NFR10. NET_BIND_SERVICE strip (e.g., a fork using the drafted AC 2 two-cap list) produces dnsmasq :53-bind EPERM at runtime. _Defer reason:_ SC-10 explicitly defers the active runtime compose-shape check to Story 2.17 / `packages/keel-invariants/src/check-devbox-compose-shape.ts`. Story 2.5 ships the invariant surface + Story 1.9 sync-gate provides static-drift detection of the sourcePath doc; compose-override runtime parse-and-assert is a distinct concern with its own test matrix. _Carry-to:_ **Story 2.17 `check-devbox-compose-shape.ts` check** — parses `docker-compose.yml` + any `docker-compose.override.yml` at pre-merge time; asserts `volumes.keel_home_dev` stanza + `cap_add` list membership (NET_ADMIN + NET_RAW + NET_BIND_SERVICE); rejects accidental bind-mount drift or cap-strip. Pairs with the `INV-devbox-homedev-named-volume` invariant ID already registered at 1.0.

- **AR-9 — `/etc/resolv.conf` + `/etc/dnsmasq.conf` writes from reload-egress.sh + start-egress.sh fail EACCES under USER dev.** _Source:_ Edge Case Hunter iter-190 (probe EC9). _Location:_ `packages/devbox/scripts/start-egress.sh:50,60` + `packages/devbox/scripts/reload-egress.sh:119,158,288-289`. _Nature:_ start-egress.sh pins `/etc/resolv.conf` to `nameserver 127.0.0.1` at boot (SC-13 of Story 2.3). Under USER dev, /etc/resolv.conf (Docker bind-mount managed; typically root-owned) and /etc/dnsmasq.conf (root-owned) are unwriteable by UID 1000. start-egress.sh:56 `log "FATAL: cannot write /etc/resolv.conf — ${resolv_err}"` branch will fire; container fail-closes at boot. Egress initialisation never completes. _Defer reason:_ Fix is scope-extensive — options are (a) chown /etc/resolv.conf + /etc/dnsmasq.conf at image-build time (risky: /etc/resolv.conf is Docker-managed at runtime; bind-mount typically preserves host perms), (b) scope-extend to edit Story 2.3 primitive scripts to run under sudo-effective or as a setuid helper (SC-19 forbidden — scripts/start-egress.sh + scripts/reload-egress.sh are explicitly Story-2.3-owned), (c) compose-level `user: root` override for the specific RUN (conflicts with USER dev intent), (d) pre-seed /etc/dnsmasq.conf + /etc/resolv.conf with dev ownership AND accept Docker's bind-mount override of resolv.conf. None is clean at Story 2.5's SC-19 boundary. _Carry-to:_ **Story 2.6 host-side CLI + scope-extension** — the fix belongs with the first story that legitimately touches Story 2.3 scripts for a non-scope-creep reason. Alternatively, operator-workstation smoke may surface a surprise (e.g. Docker's bind-mount of /etc/resolv.conf preserves the image-layer ownership such that dev CAN write to it) which would retire this defer in-place. iter-187 did NOT exercise runtime egress initialisation — the FIRST operator-workstation boot is the adjudicating event.

- **AR-10 — Pre-Story-2.5 named-volume re-use preserves root-owned `/home/dev` tree.** _Source:_ Edge Case Hunter iter-190 (probe EC2). _Location:_ `packages/devbox/docker-compose.yml:74-76,154-155` (volume lifecycle). _Nature:_ If an operator has a `keel_home_dev` (or project-prefixed `devbox_keel_home_dev` / `keel-devbox_keel_home_dev` depending on AI-3 resolution) volume from a pre-Story-2.5 container run (when processes ran as root and /home/dev files were root-owned), Docker sees the volume non-empty on Story 2.5 container start and SKIPS populate-from-image semantics — the root-owned tree mounts over /home/dev. Under USER dev + dropped CAP_CHOWN, entrypoint's chown calls fail silently per SC-5 expected-failure posture. Claude Code OAuth (Story 2.8) + gh login (Story 2.9) + shell history + vim swap files all EACCES on write. No diagnostic is emitted; operator must manually `docker volume rm <name>` to recover. _Defer reason:_ Fresh-volume first-boot happy path per SC-6 is not regressed; only pre-existing-volume migrations surface the issue. No current-state regression; requires operator education OR detection logic. _Carry-to:_ **Story 2.6 operator-migration documentation** — add a § Upgrading from pre-Story-2.5 images sub-section to `packages/devbox/README.md § Hardening` documenting the `docker volume rm` recovery procedure + the first-boot `stat /home/dev` diagnostic. Alternatively, add a boot-time entrypoint detection (`stat -c '%u' /home/dev` ≠ 1000 → emit WARN) — that path touches entrypoint.sh which is in Story 2.5's SC-19 allow list, but empirical detection-under-dropped-CAP_CHOWN is itself fragile; docs-first is the cleaner 1.0 posture.

## Deferred from: code review of story-2-5 closure (2026-04-22)

_Iter-197 `/bmad-code-review (args: "2")` CR-closure re-run against same `b2533b0..HEAD` diff baseline (9 SC-19 in-scope files post-drain, 645 lines) produced **ZERO-PATCH close** + **2 DEFER** + ~13 DISMISS. Acceptance Auditor verdict: all 5 ACs + 19 SCs satisfied; CR drain complete; all 6 AI-1..AI-6 opener patches verified landed without regression. The two new DEFERs below are operator-docs / Story-2.6-scope concerns surfaced only on re-read of the post-drain state (not visible at opener due to being downstream-of-drain observations). Neither blocks `fixes-pending → sm-verified → done` closure._

- **AR-11 — `KEEL_DEVBOX_TMPFS_{TMP,VARTMP}_MB` envvar validation missing.** _Source:_ Edge Case Hunter iter-197 (probe EC3). _Location:_ `packages/devbox/docker-compose.yml:~153-155` (tmpfs long-syntax with `size=${KEEL_DEVBOX_TMPFS_TMP_MB:-2048}m`). _Nature:_ Operator setting `KEEL_DEVBOX_TMPFS_TMP_MB=0` (or empty / negative / non-numeric like `"2gb"`) passes Compose variable-substitution verbatim to the kernel tmpfs mount. `size=0m` produces a zero-byte tmpfs (every write ENOSPC — affects reload-egress.sh mktemp, entrypoint.sh chown_err mktemp, dnsmasq temp-alloc). Non-numeric values produce Compose parse errors at `compose up` time. Fail-closed intact (operator sees unambiguous startup failure), but better DX warrants entrypoint-level preflight (is-numeric + positive-bounds) OR host-side pnpm-CLI preflight. _Defer reason:_ SC-18 pins Story 2.2 as the owner of `KEEL_DEVBOX_*` knob definitions; Story 2.5 consumes the knobs but does NOT own validation. Better-fit surface is Story 2.6 (`pnpm devbox:*` host-side CLI) where envvar preflight is natural. Neither AC 3 nor SC-8 demands runtime validation — both ACs are satisfied by the tmpfs stanza being *parameterized correctly*; validating operator input is hygiene, not contract. _Carry-to:_ **Story 2.6 `pnpm devbox:*` host-side CLI surface** (primary) — add preflight validator that reads `.envrc` (or operator env), confirms `KEEL_DEVBOX_TMPFS_TMP_MB` + `KEEL_DEVBOX_TMPFS_VARTMP_MB` are positive-integer strings matching `^[1-9][0-9]*$` with a soft upper bound (e.g. ≤ 16384 = 16 GB). Alternative: **Story 2.2 amendment PR** — add a bash assertion in `packages/devbox/.envrc.example` (via a `_validate_keel_devbox_envvars` function sourced at enter-directory time).

- **AR-12 — `COMPOSE_PROJECT_NAME` / `-p` CLI flag overrides `name: keel-devbox` top-level key; verification-command volume FQN prefix drifts.** _Source:_ Edge Case Hunter iter-197 (probe EC6). _Location:_ `packages/devbox/README.md § Hardening § Verification` (`docker volume inspect keel-devbox_keel_home_dev`) + `docs/invariants/devbox-hardening.md § Verification` (same command). _Nature:_ Compose v2 documented precedence for project name resolution: `-p` CLI flag > `COMPOSE_PROJECT_NAME` env > top-level `name:` in compose file > directory basename. The `name: keel-devbox` top-level key added at AI-3 drain (iter-194) pins the substrate default, but a fork operator setting `COMPOSE_PROJECT_NAME=myfork` resolves the volume FQN as `myfork_keel_home_dev` instead of `keel-devbox_keel_home_dev`. The documented `docker volume inspect keel-devbox_keel_home_dev` command fails with "No such volume"; AC 4 verification under fork override requires substituting the fork's project name in the FQN. _Defer reason:_ Standard documented Compose behavior — not a substrate regression. AC 4 still SATISFIED (named-volume contract held; only the volume's FQN prefix changes per Compose precedence). Verification-command prose could qualify ("where `keel-devbox` is your Compose project name; substitute `${COMPOSE_PROJECT_NAME}_keel_home_dev` for forks with custom project names"). Cosmetic, not load-bearing. _Carry-to:_ **Story 2.6 operator-migration docs** — qualify the verification commands in both README and invariant-doc with a one-line note about Compose project-name override precedence. Alternatively, rewrite verification to use `docker compose -p keel-devbox ... config --format json | jq .volumes` so the project name is explicit in the invocation. Neither changes substrate behavior; pure docs polish.

## Deferred from: code review of story-2-6 (2026-04-22)

_Iter-204 `/bmad-code-review (args: "2")` opener against cumulative Story 2.6 substrate diff (`65ef3a3..HEAD` / 26 files / 2152 insertions). Three-layer adversarial fan-out: Blind Hunter + Edge Case Hunter + Acceptance Auditor. Acceptance Auditor: ZERO PATCH-or-higher AC deviations (all 9 ACs + 25 SCs satisfied; SC-15 monitor-pin + SC-9 bare-`running` + 2× invariant contentHash refresh verified legitimate). Code-quality triage: 11 PATCH (AI-1..AI-11; mirrored to story file `## Review Findings § Code Review (iter-204, v1.2) — fixes-pending`) + **7 DEFERs (AR-13..AR-19 below)** + 6 DISMISSED. None of the 7 DEFERs blocks `fixes-pending → sm-verified → done`; all are scope-boundary, intentional-design, or carry-forward items._

- **AR-13 — `start.sh` accepts bare `running` as healthy on first poll → 30 s grace + 120 s timeout are dead code in pre-Story-2.13 substrate.** _Source:_ Blind Hunter iter-204. _Location:_ `packages/devbox/scripts/start.sh:73-77`. _Nature:_ The poll loop's `case "${health}" in healthy|running)` arm exits 0 immediately. Until Story 2.13 wires a real `HEALTHCHECK` directive into `docker-compose.yml`, `docker inspect ... .State.Health.Status` is empty so the template fallback at line 71 returns `.State.Status = running` immediately after `docker compose up -d` returns. The 30 s grace deadline (line 62) and 120 s healthcheck timeout (line 30) are effectively unreachable. The header docstring (lines 14-21) advertises healthcheck-poll behaviour the implementation does not deliver in the current substrate. _Defer reason:_ SC-9 explicitly designates this as the stub-friendly pre-Story-2.13 posture: "accept `healthy`, `starting` (within first 30s grace), or bare `running` (when no healthcheck is configured — pre-Story-2.13 posture)". Acceptance Auditor verified intentional. Story 2.13 is the healthcheck-definition scope-owner; activating the dead branches is its responsibility. _Carry-to:_ **Story 2.13** — when `HEALTHCHECK` is wired into docker-compose.yml + Dockerfile, the `running` arm should be removed (or scoped to "no-healthcheck-configured" detection only) and the grace + timeout become live. Add a Story 2.13 verification step that exercises start.sh against a healthcheck that takes 35-45 s to flip to `healthy` (validates the grace + timeout windows are reached).

- **AR-14 — `env-check.sh` treats explicit empty-string (`KEEL_DEVBOX_DNS_UPSTREAM=`) as "present" rather than "missing".** _Source:_ Edge Case Hunter iter-204. _Location:_ `packages/devbox/scripts/env-check.sh:67-68,95`. _Nature:_ The presence check at line 95 (`[[ -z "${parsed[${v}]+x}" ]]`) tests for KEY existence in the associative array, not value emptiness. An operator who writes `KEEL_DEVBOX_DNS_UPSTREAM=` (explicit empty) passes the missing-var check. Downstream consumers (compose variable substitution, dnsmasq.conf) may fail later with no env-check hint. The shape-validator at line 100-109 covers tmpfs MB knobs but NOT integer/path-typed knobs like `KEEL_DEVBOX_PORT_*`, `KEEL_DEVBOX_CPUS`, `KEEL_DEVBOX_NOFILE`. _Defer reason:_ The comment block at lines 67-68 explicitly documents the intentional posture: "operators may explicitly set `X=` to signal 'present but empty'". SC-14 scope was integer-shape validation (tmpfs MB) only; non-integer empty-string treatment remains an explicit design decision. AC 8 satisfied by the presence-of-name check. _Carry-to:_ **Future ergonomic-pass story** — extend the shape validator to cover all positive-integer knobs (`KEEL_DEVBOX_PORT_*`, `KEEL_DEVBOX_CPUS`, `KEEL_DEVBOX_NOFILE`, `KEEL_DEVBOX_MEMORY_GB`, `KEEL_DEVBOX_SHM_GB`) AND optionally a `SHAPE_NONEMPTY` set for stringly-typed required knobs. Alternative: Story 2.7+ may refactor the validator into a shared `_validate.sh` library if the host-side CLI grows further envvar-checking surface.

- **AR-15 — `start.sh` TOCTOU between `docker image inspect` and `docker compose up -d`.** _Source:_ Edge Case Hunter iter-204. _Location:_ `packages/devbox/scripts/start.sh:39-58`. _Nature:_ Line 39 inspects `${IMAGE_TAG}` (default `keel-devbox:local`) for fast-fail exit-10 when the image is missing. Between line 42 (inspect-pass) and line 58 (`docker compose up -d devbox`), an external `docker rmi` could remove the image, OR the image could be present but corrupt (truncated download), OR compose could pull/build a different SHA than was inspected. start.sh has no SHA pinning — there's no detection of "image present but stale". _Defer reason:_ Compose handles the actual build/pull and emits its own error if the image is unavailable at `up` time; the inspect check is a fast-fail UX hint, not a correctness gate. The script header (line 19) documents "image not built (run `pnpm devbox:build` first)" as a best-effort hint. AC 3 satisfied — start succeeds when the image is present and compose-up returns clean. _Carry-to:_ **Story 2.13 healthcheck integration OR a future image-provenance hardening story** — pin the expected image SHA in `INV-devbox-image-provenance` (new invariant) + assert it at start.sh, similar to the contentHash pattern used by `INV-prek-prepare-lifecycle`. Out of Story 2.6 scope.

- **AR-16 — `shell.sh` argument forwarding pattern `bash -l "$@"` rejects bare commands; `pnpm devbox:shell whoami` errors "No such file or directory".** _Source:_ Edge Case Hunter iter-204. _Location:_ `packages/devbox/scripts/shell.sh:32-34`. _Nature:_ The `bash -l "$@"` pattern interprets the first non-option argument as a SCRIPT FILE, not a command. `pnpm devbox:shell -c 'whoami'` works (bash `-l -c <cmd>` is valid); `pnpm devbox:shell whoami` fails (bash `-l whoami` looks for an executable script named `whoami`). _Defer reason:_ AC 5 specifies "interactive shell as the dev user"; argument forwarding was not in the AC. The script header docstring shows only the no-args interactive form. Operator UX enhancement, not a defect. _Carry-to:_ **Future ergonomic-pass story** — either add a `--help` form documenting the `-c '<cmd>'` constraint, OR detect bare-command invocation and auto-wrap as `bash -l -c "$*"`. Out of Story 2.6 scope.

- **AR-17 — `env-check.sh` quote-stripping is asymmetric: peels one quote each side without checking they match.** _Source:_ Blind Hunter iter-204. _Location:_ `packages/devbox/scripts/env-check.sh:82-83`. _Nature:_ Lines 82-83 (`value="${value#\"}"; value="${value%\"}"`) strip at most one leading and one trailing double quote (and similarly for single quotes) without verifying the quotes paired. A malformed `KEEL_DEVBOX_FOO="5` (unmatched opening quote) becomes `5` silently. _Defer reason:_ Defensive parsing already filters realistic mis-typings via the upstream missing-var check + tmpfs shape regex. Hardening would require quote-state tracking; not load-bearing at 1.0. _Carry-to:_ **Future ergonomic-pass story** OR fold into AR-14 if a broader env-check refactor lands.

- **AR-18 — 12 of 13 scripts lack `--help`/`-h` handling (only `clean.sh:62` has `usage`).** _Source:_ Blind Hunter iter-204. _Location:_ `packages/devbox/scripts/{attach,build,rebuild,start,stop,restart,shell,status,logs,monitor-host,whitelist-host,env-check}.sh`. _Nature:_ Operator running `pnpm devbox:start --help` gets `docker compose up -d --help` output (compose's help, NOT script-level usage). README + AGENTS.md document the verb table externally; per-script `--help` would be more discoverable. _Defer reason:_ AC 1-9 do not mandate per-script `--help`; ergonomic enhancement. _Carry-to:_ **Future ergonomic-pass story** — add a uniform `case "$1" in -h|--help) usage; exit 0 ;; esac` block to each script. Could pair naturally with AR-19 (shared `_lib.sh`).

- **AR-19 — Eight scripts duplicate the `docker info` + `docker inspect .State.Status` pre-flight; sourcing a shared `_lib.sh` would deduplicate ~24 lines and ensure consistent exit-8/exit-9 messaging.** _Source:_ Edge Case Hunter iter-204. _Location:_ `packages/devbox/scripts/{start,stop,restart,shell,attach,status,logs,monitor-host,whitelist-host}.sh`. _Nature:_ DRY violation. Any future change to the docker-unreachable hint (e.g., "is colima running?") must touch 8+ files. _Defer reason:_ Refactor opportunity, not bug. Carry to a future cleanup story OR Story 2.7+ when the script set grows further. _Carry-to:_ **Story 2.7+ host-side CLI consolidation** OR **Future hygiene-pass story** — extract `packages/devbox/scripts/_lib.sh` with `_check_docker_info()` + `_check_container_running()` + `_log_with_prefix()` helpers; source from each script's preamble.

## Deferred from: Story 2.6 AI-8 iter-212 (2026-04-22)

_AI-8 drain applied `unset COMPOSE_PROJECT_NAME` to the 9 CR-flagged container-discovery scripts (start / stop / restart / shell / attach / status / logs / monitor-host / whitelist-host). The sibling drift-vector review surfaces three adjacent scripts outside the CR-flagged set whose `docker compose` invocations are ALSO affected by an operator-shell `COMPOSE_PROJECT_NAME` override. None were flagged by the CR Blind / Edge Case / Acceptance Auditor layers at iter-204; recording here so a future ergonomic-pass or Story 2.7+ host-side consolidation can extend the `unset` uniformly without re-drafting the audit._

- **AR-20 — `clean.sh:113,140` `docker compose down` under `COMPOSE_PROJECT_NAME` override operates on the wrong project label; named-volume destruction with `--with-volumes --force-backend-b` would target `${COMPOSE_PROJECT_NAME}_keel_home_dev` rather than the INV-devbox-homedev-named-volume-pinned `keel-devbox_keel_home_dev`. PROMOTED TO AI-12 iter-217 (re-triaged at iter-216 CR closure; Edge Case Hunter re-scored as PATCH because `--remove-orphans` on the override project reaches unrelated containers — collateral-damage risk higher than the iter-212 "silent no-op" analysis credited).** _Source:_ AI-8 sibling-vector review (iter-212) → promoted at iter-216 CR closure. _Location:_ `packages/devbox/scripts/clean.sh:113,140`. _Nature:_ Same drift class as AI-8 but on the destructive (down-volume) surface rather than container-discovery. An operator with `export COMPOSE_PROJECT_NAME=my-fork` plus `pnpm devbox:clean --with-volumes --force-backend-b --yes` would: (a) attempt to remove `my-fork_keel_home_dev` volume (doesn't exist under fresh substrate → no-op) AND (b) LEAVE the real `keel-devbox_keel_home_dev` volume untouched (operator intent was "destroy the devbox volume"; actual effect is "destroy nothing") AND (c) `--remove-orphans` removes any `my-fork`-labelled container as collateral damage. Iter-216 closure re-triage weighted (c) above the iter-212 analysis's no-op framing. _Defer reason (SUPERSEDED iter-216):_ Promoted to AI-12 — tracked in story file § Review Findings § Code Review Closure (iter-216). _Carry-to:_ CLOSED — AI-12 LANDED iter-217 (`packages/devbox/scripts/clean.sh:36` `unset COMPOSE_PROJECT_NAME` with 10-line rationale comment anchoring AR-20 + `INV-devbox-homedev-named-volume`; env-shim smoke confirms CPN unset before first docker call; source-level ordering guard: unset@L36 < first-docker@L125; see story file Change Log v1.14).

- **AR-21 — `build.sh:28` + `rebuild.sh:32` `docker compose build` under `COMPOSE_PROJECT_NAME` override tags the image in an unrelated project's label namespace.** _Source:_ AI-8 sibling-vector review (iter-212). _Location:_ `packages/devbox/scripts/build.sh:28` + `packages/devbox/scripts/rebuild.sh:32`. _Nature:_ Image tags (`keel-devbox:local`) are NOT project-scoped, so the build output is reusable across projects. The drift is cosmetic: compose-build logs emit `Building service devbox` under the override's project name (`my-fork` instead of `keel-devbox`), and buildkit intermediate image labels carry the wrong project. Operator running `docker image inspect keel-devbox:local` still finds the image; downstream `start.sh` (now AI-8-fixed) finds it too. _Defer reason:_ Cosmetic log drift; no correctness or invariant impact. CR-scoped AI-8 excluded. Iter-216 closure re-triage kept AR-21 deferred (build/rebuild cosmetic-only log drift; no destructive surface). _Carry-to:_ **Story 2.7+ host-side CLI consolidation** OR **Future hygiene-pass story** — extend AI-12's `unset` block to build + rebuild for complete uniformity across the devbox:* CLI surface.

## Deferred from: Story 2.6 CR closure iter-216 (2026-04-22)

_`/bmad-code-review (args: "2")` closure re-run surfaced 2 PATCH + 7 NITPICK findings against the cumulative Story 2.6 diff (`2f57265..HEAD`, iter-201 landing + iter-205..iter-215 drain). 2 PATCH → QUEUE fix tasks (AI-12 clean.sh AI-8 drain-gap, AI-13 env-check export-whitespace). 7 NITPICK triaged DEFER (AR-22..AR-28 below). These are posture / edge-case / heuristic polish items that do not block Story 2.6 closure; carry-forward to operator-workstation smoke OR downstream stories as noted per-entry._

- **AR-22 — `env-check.sh` silently accepts unterminated quoted values (e.g. `FOO="bar` with no closing quote).** _Source:_ Blind Hunter iter-216. _Location:_ `packages/devbox/scripts/env-check.sh:101-113`. _Nature:_ The AI-6 quoting-aware parse (`${rest%%${first_char}*}`) returns `rest` unchanged when no closing quote exists → leading quote stripped but remainder captured verbatim. Operator sees a silently-accepted malformed value rather than a warning. No security risk (the accepted value is still bounded by line-length); UX risk is a confusing parse result that matches the buggy `.envrc` literally. _Defer reason:_ Defensive parser edge case, very low frequency; realistic mis-typings are caught by upstream missing-var check + downstream shape validator. _Carry-to:_ **Future ergonomic-pass story** — add quote-closure detection (compare `rest` to `value` post-strip); if unchanged, warn "unterminated quote in value for KEY on line N". Fold naturally into AR-17 broader env-check hardening.

- **AR-23 — `restart.sh` indefinite-`running` state falls through to `start.sh` on poll timeout (soft-gate posture).** _Source:_ Blind Hunter iter-216. _Location:_ `packages/devbox/scripts/restart.sh:55-73`. _Nature:_ AI-11 (iter-215) documented the SOFT-gate decision in its RESOLVED block: on timeout, log "timed out" + proceed regardless. Pathological scenario: `docker compose stop` returns 0 (SIGTERM sent + grace expired) but daemon-state stays `running` (kernel-level container freeze, cgroups-v1 parent-slice lock) → poll loop hits `*)` arm until 10s timeout then proceeds to `start.sh`, which sees `running` and declares success against a doomed-but-not-killed container. _Defer reason:_ Intentional soft-gate; start.sh has its own 120s healthcheck bound as downstream validation, so a truly-stuck container would fail the outer restart via the healthcheck path rather than silently "succeed". Hard-abort would fail transient-daemon-pressure restarts that resolve within the broader 120s outer bound. _Carry-to:_ **Future hardening** — if telemetry shows pathological stuck-`running` daemons trapping operators, add an escalation path: on timeout, `docker kill "${CONTAINER_NAME}" || true` then proceed (or exit 11 with a "stop did not take effect" hint). Re-evaluate at end-of-Epic-2 retrospective.

- **AR-24 — `env-check.sh` `git rev-parse` three-tier resolver leaks into vendored/submodule layouts where fork is INSIDE parent-repo's git tree.** _Source:_ Blind Hunter iter-216. _Location:_ `packages/devbox/scripts/env-check.sh:38-46`. _Nature:_ AI-5's three-tier resolver prefers `git rev-parse --show-toplevel` as tier 2 — authoritative when `.git` is present. In the vendored-inside-git layout (fork is a subdirectory of a parent repo that has its own `.git`), rev-parse returns the PARENT repo's toplevel, not the fork's. `.envrc` is then searched at the parent root, missing the substrate `.envrc` at the fork root. _Defer reason:_ `KEEL_DEVBOX_REPO_ROOT` operator escape hatch covers — `env-check.sh:81` error message explicitly guides operators to export it. Tier-1 override is unconditional, so the vendored-inside-git operator has a documented one-line fix. _Carry-to:_ **Future vendoring-UX story** IF fork operator feedback indicates friction — add a `package.json` name-match probe between git-toplevel and `${DEVBOX_DIR}/../..`; prefer the tier-3 arithmetic root when its `package.json` name matches the substrate's package.json name. Otherwise keep the override path.

- **AR-25 — `monitor-host.sh` host-filesystem probe is indirect proxy for container's bind-mounted file.** _Source:_ Blind Hunter iter-216. _Location:_ `packages/devbox/scripts/monitor-host.sh:54`. _Nature:_ `[[ ! -f "${DEVBOX_DIR}/scripts/monitor.sh" ]]` checks the HOST filesystem as a proxy for the container's bind-mounted `/workspace/packages/devbox/scripts/monitor.sh`. If the workspace bind-mount is broken (operator edited `docker-compose.yml` removing the mount), host-probe passes but `docker exec` fails with a terser "No such file or directory" error. Blind Hunter's own note flagged as "acceptable heuristic". _Defer reason:_ Correctness-as-heuristic; the common failure mode (fresh clone missing the file after a bad merge) is correctly detected. Bind-mount-broken is a separate Story 2.1 substrate regression that would surface via `pnpm devbox:start` healthcheck failure, not monitor invocation. _Carry-to:_ **Future ergonomic-pass story** OR **AR-19 `_lib.sh` refactor** — add an additional `docker exec "${CONTAINER_NAME}" test -f /workspace/packages/devbox/scripts/monitor.sh` probe OR translate the terser exec-rc into a friendly "workspace bind-mount may be broken — check docker-compose.yml" error.

- **AR-26 — `start.sh`/`restart.sh` arithmetic on non-integer timeout envvar aborts with cryptic bash error.** _Source:_ Edge Case Hunter iter-216. _Location:_ `packages/devbox/scripts/start.sh:75`, `packages/devbox/scripts/restart.sh:51`. _Nature:_ `stop_deadline=$(( $(date +%s) + STOP_POLL_TIMEOUT_S ))` / `healthcheck_deadline=$(( $(date +%s) + HEALTHCHECK_TIMEOUT_S ))` under `set -euo pipefail` aborts with rc 1 + stderr "value too great for base" if operator sets the envvar with units: `KEEL_DEVBOX_START_HEALTHCHECK_TIMEOUT_S=120s` or `KEEL_DEVBOX_RESTART_STOP_POLL_TIMEOUT_S=10s`. No shape validation; no friendly message. _Defer reason:_ Defensive UX polish. No current shape-validation contract for timeout envvars; AR-11 (iter-201) scoped shape validation to tmpfs-MB knobs only. Realistic operator input (integer seconds per the env-var docstring) works correctly. _Carry-to:_ **Future env-check hardening pass** — extend shape validation from tmpfs-MB knobs to timeout knobs (positive-integer seconds); pair with AR-17 broader env-check refactor. Fails `env-check.sh` upstream before the arithmetic ever runs.

- **AR-27 — `start.sh` `created` state falls through to `*)` arm and polls to 120s timeout with misleading "failed to reach healthy state" message.** _Source:_ Edge Case Hunter iter-216. _Location:_ `packages/devbox/scripts/start.sh:105-119`. _Nature:_ If `docker compose up -d` fails AFTER creating the container but before starting it (rare: image-pull race, cgroupsv1 parent-slice resource-limit reject, buildkit failure mid-start), `.State.Status=created` hits the unrecognised-state arm; script polls for 120s then exits 11 with misleading "failed to reach healthy state" (container never reached a healthcheck because it never started). _Defer reason:_ Rare edge case. The AI-2 (iter-206) fix already split the case arm into `unhealthy`/`exited|dead|removing|paused`; extending the fatal-state arm to include `created` is a minor symmetry fix. _Carry-to:_ **Future start.sh hardening** — add `created` to the fatal-state arm at `start.sh:105` with a friendly "container created but never started — check compose config" message.

- **AR-28 — `monitor-host.sh` missing `--user dev` pin (asymmetric with whitelist-host.sh AI-9 posture).** _Source:_ Edge Case Hunter iter-216. _Location:_ `packages/devbox/scripts/monitor-host.sh:66`. _Nature:_ AI-9 (iter-213) added `--user dev` to `whitelist-host.sh` as an explicit-pin defense against fork override flips to `user: root`. `monitor-host.sh` runs `tail -F` on `/workspace/logs/egress-queries.jsonl` without the pin. If the override flips to root, tail reads as root — low-risk (read-only, no write-state footprint), but posture-asymmetric with AI-9. _Defer reason:_ Read-only operation; AI-9's mutation-lock concern (EPERM on flock or atomic-rename) does not apply. Symmetry fix, not correctness fix. _Carry-to:_ **Future posture-consistency pass** OR **AR-19 `_lib.sh` refactor** — add `--user dev` to `monitor-host.sh:66` exec line for posture consistency with AI-9.

## Deferred from: Story 2.6 CR closure iter-219 (2026-04-22)

_`/bmad-code-review (args: "2")` 2nd closure re-run surfaced 7 new DEFER items against the cumulative Story 2.6 diff (`65ef3a3..HEAD` / 14 scripts + README.md + root package.json / 1162 diff lines). Acceptance Auditor ZERO-FINDING for 3rd consecutive pass (iter-204 / iter-216 / iter-219) — AC-to-code mapping clean. Edge Case Hunter surfaced 3 pre-existing env-check.sh parser edge cases (AR-29/30/31); Blind Hunter surfaced 4 docs/cosmetic drifts (AR-32/33/34/35). All DEFERRED — no PATCH remaining; CR closure achieves ZERO-PATCH. Story 2.6 → `done`; within-epic auto-advance to Story 2.7 per § Cross-epic transition._

- **AR-29 — `env-check.sh:89` `declare -A parsed` requires bash ≥ 4; macOS default `/bin/bash` (3.2.57) aborts with cryptic `declare: -A: invalid option`.** _Source:_ Edge Case Hunter iter-219. _Location:_ `packages/devbox/scripts/env-check.sh:89`. _Nature:_ Pre-existing since iter-201 landing (not drain-introduced). On a macOS operator workstation without Homebrew bash (or with default-`/bin/bash`-first PATH), `#!/usr/bin/env bash` picks `/bin/bash` 3.2.57; `declare -A` is bash ≥ 4 syntax and errors with `declare: -A: invalid option` before any useful hint prints. No first-run UX for macOS operators in the unusual-PATH case. _Defer reason:_ Practical M4-Pro Homebrew operators (per NFR2 + direnv-conventional setup) have `/opt/homebrew/bin/bash` first on PATH and hit zero friction. Hardening = 2-line preflight guard `if (( BASH_VERSINFO[0] < 4 )); then echo 'env-check requires bash 4+ (macOS: brew install bash)' >&2; exit 2; fi` at env-check.sh top. First-run UX polish for an edge-case PATH configuration; not load-bearing. _Carry-to:_ **Future env-check hardening pass** OR **Story 2.10 prerequisite-check** — Story 2.10 scope-owns docker/claude-auth/gh-auth prerequisite checks; extending to cover `bash >= 4` aligns naturally.

- **AR-30 — `env-check.sh:85` `.envrc` path resolves to a readable directory (not a regular file) aborts with noisy bash `read: <path> is a directory` instead of clean exit-3.** _Source:_ Edge Case Hunter iter-219. _Location:_ `packages/devbox/scripts/env-check.sh:85`. _Nature:_ `[[ -r "${ENVRC_PATH}" ]]` check passes for a readable directory; the subsequent `while IFS= read -r line` at line 93 errors loudly (not exit 3, not the expected "copy from .envrc.example" hint). Fix = one additional `[[ ! -f "${ENVRC_PATH}" ]]` check alongside `[[ ! -r ]]`. _Defer reason:_ Operator would have to `mkdir .envrc` (reverse of the substrate pattern) to trigger this; the noisy-bash-read-error is its own signal that something is wrong. Extremely low frequency; ergonomic polish at best. _Carry-to:_ **Future env-check hardening pass** — fold into AR-17/AR-22 broader env-check parser refactor.

- **AR-31 — `env-check.sh:93` UTF-8 BOM (EF BB BF) on first `.envrc` line silently skips the first variable (typically `KEEL_DEVBOX_ARCH` → "missing").** _Source:_ Edge Case Hunter iter-219. _Location:_ `packages/devbox/scripts/env-check.sh:93`. _Nature:_ Operators who edit `.envrc` in Windows Notepad or PowerShell `Set-Content -Encoding UTF8` without `-NoNewline` emit a UTF-8 BOM at file start. AI-7 (iter-211) line-level `\r` strip does NOT strip BOM bytes. First `export KEEL_DEVBOX_ARCH=...` line reads as `<BOM>export ...`; the `^KEEL_DEVBOX_` regex (and the AI-13 `^export[[:space:]]` guard) both fail to match → variable silently skipped → reported missing. Fix = one-line BOM strip at loop head: `line="${line#$'\xef\xbb\xbf'}"` alongside AI-7's `\r` strip. _Defer reason:_ Symmetrical with AI-7 in spirit but pre-existing since iter-201 (not a drain regression); operator workaround is "save as UTF-8 (no BOM)" — same guidance as for CRLF on Windows. AI-7 was drain-addressed because CRLF was the dominant drift-pattern; BOM is secondary. _Carry-to:_ **Future env-check hardening pass** — extend the line-sanitisation block at the top of the parse loop to strip BOM + CR + any future byte-order-mark variants uniformly. Could pair naturally with AR-17/AR-22 env-check refactor.

- **AR-32 — `packages/devbox/README.md:469` verification recipe `pnpm devbox:shell -c 'whoami && pwd && id'` may not forward `-c` cleanly across pnpm versions.** _Source:_ Blind Hunter iter-219. _Location:_ `packages/devbox/README.md:469`. _Nature:_ pnpm 8.x forwards args after the script name automatically, but `-c` is a recognised pnpm global flag in legacy versions (pnpm 7 pre-`--`-convention); the unambiguous safe form is `pnpm devbox:shell -- -c 'whoami && pwd && id'` which ensures the `-c` reaches `shell.sh "$@"` → `bash -l -c '…'` regardless of pnpm version. _Defer reason:_ Project pins pnpm via `packageManager` in root `package.json`; modern pnpm is known to forward unrecognised flags to the script. Real-world operator impact is near-zero; docs polish. _Carry-to:_ **Future README polish pass** OR **AR-18 (per-script `--help` rollout)** — update the verification recipe to the `--`-form at the same time as adding `--help` handling to `shell.sh`.

- **AR-33 — `packages/devbox/scripts/clean.sh:16` docstring "RESERVED; rejected at 1.0 (SC-11)" wording vs actual no-op-with-log behaviour.** _Source:_ Blind Hunter iter-219. _Location:_ `packages/devbox/scripts/clean.sh:16`. _Nature:_ Top-of-file comment block line 16 says `--allow-broad-prune` is "rejected at 1.0" (suggesting exit non-zero), but the implementation (lines 78-83) accepts the flag as a no-op with a warning log per SC-11. The usage() block at line 59 uses the correct wording ("RESERVED at 1.0; no-op"); only the top-of-file comment drifts. Fix = 1-word edit: `rejected` → `no-op`. _Defer reason:_ Operator reading `clean.sh --help` (future AR-18) or the usage block on unknown-flag sees the correct wording; only a reader of the top-of-file docstring sees the drift. Aesthetic, not functional. _Carry-to:_ **Future README/docs polish pass** OR **AR-18 `--help` rollout** — align the top-of-file comment block with the usage() wording at the same pass.

- **AR-34 — `packages/devbox/README.md:476` operator-verification recipe uses raw `docker volume inspect keel-devbox_keel_home_dev` without FR1-exception annotation.** _Source:_ Blind Hunter iter-219. _Location:_ `packages/devbox/README.md:476`. _Nature:_ FR1 says "Operators never type `docker`, `docker compose`, or `docker exec` directly" — the migration-recovery verification section (§ Operator migration § Verification) intentionally uses `docker volume inspect` as the read-only primitive because there is no `pnpm devbox:volume:inspect` verb. Fix options: (a) add a `pnpm devbox:volume:inspect` verb (scope-creep; would couple to volume naming which Story 2.11 shared-devbox-mode may refactor); (b) add a prose annotation acknowledging the FR1 exception for emergency migration. _Defer reason:_ Section is operator-recovery, not day-to-day operation; FR1 applies to routine interaction, not emergency migration. Annotation would be nice but not load-bearing. _Carry-to:_ **Future README polish pass** OR **Story 2.11 shared-devbox-mode** — if Story 2.11 adds a volume-naming abstraction, it could also add volume-introspection verbs.

- **AR-35 — `.envrc.example` ↔ `env-check.sh:471-485` `REQUIRED_VARS` array drift risk (no pre-merge gate asserts byte-for-byte parity).** _Source:_ Blind Hunter iter-219. _Location:_ `packages/devbox/.envrc.example` ↔ `packages/devbox/scripts/env-check.sh:471-485`. _Nature:_ Story 2.2 shipped `.envrc.example`; Story 2.6 iter-201 landed `REQUIRED_VARS` in env-check.sh. If a future PR edits one without the other, drift goes undetected until an operator's `pnpm devbox:env:check` reports a phantom-missing or missing-required variable. Fix = runtime contract-shape check (pnpm devbox:env:check-contract: parse `.envrc.example` → extract `KEEL_DEVBOX_*` names → diff against `REQUIRED_VARS` → exit 2 on drift) OR pre-merge sync-gate invariant. _Defer reason:_ Story 2.17 (compose-shape runtime check via `packages/keel-invariants/src/check-devbox-compose-shape.ts`) is the explicit scope-owner for runtime shape-drift detection per Story 2.5 carry-forward; `.envrc`-shape is the natural companion check. _Carry-to:_ **Story 2.17 `check-devbox-compose-shape.ts`** — extend scope to include `.envrc.example` ↔ `env-check.sh REQUIRED_VARS` parity, OR create a sibling `check-devbox-envrc-shape.ts` invariant.

## Deferred from: Story 2.7 CR iter-226 (2026-04-22)

_`/bmad-code-review (args: "2")` one-pass closure against Story 2.7 iter-223 implementation commit `098aa41` (5 files / 231 diff lines: `packages/devbox/scripts/ralph-build-host.sh` + `ralph-plan-host.sh` + `package.json` + `packages/devbox/README.md` + `AGENTS.md`). 3-layer adversarial triage: Blind Hunter 17 findings + Edge Case Hunter 20 findings + Acceptance Auditor 0 findings = 37 raw → **0 PATCH / 0 DECISION / 15 DEFER / 22 DISMISS** after deduplication + classification. ZERO-PATCH means no fix-drain queue — CR closure achieved in a single pass (contrast Story 2.6: 3 CR passes + 13 AI drains). SECOND cumulative CR-gate ZERO-PATCH precedent; FIRST one-pass outcome. Story 2.7 → `done`; within-epic auto-advance to Story 2.8 per § Cross-epic transition._

- **AR-36 — `KEEL_RALPH_MODE` host-shell export does not propagate to container PID 1 via `docker attach`.** _Source:_ Blind Hunter + Edge Case Hunter iter-226. _Location:_ `packages/devbox/scripts/ralph-build-host.sh:60` + `ralph-plan-host.sh:60`. _Nature:_ `export KEEL_RALPH_MODE="${RALPH_MODE}"` sets the variable in the host shell. `docker attach` then connects host stdio to a container PID 1 whose environment was frozen at container-start (via `docker compose up`). The export happens AFTER `start.sh` is invoked, so compose-env pass-through cannot see it either. Under current `CMD: [sleep, infinity]`, no consumer exists in the container; SC-2 Epic-3 carve-out governs. _Defer reason:_ Epic 3 owns the in-container Ralph TUI runtime and the mode-signal consumer design. Epic 3 can choose to (a) read a sidecar file the wrapper writes to a mounted volume before attach, (b) require the wrapper to `export KEEL_RALPH_MODE` before `start.sh` so compose's `environment:` clause propagates it to the container's PID 1, or (c) inject via `docker exec -e KEEL_RALPH_MODE=...` ahead of each iteration. Envelope-level export is the spec-sanctioned AC 5 signal; consumer wiring is Epic 3's scope. _Carry-to:_ **Epic 3 Story 3.x — in-container Ralph TUI runtime** — verify mode-signal mechanism; may require `ralph-*-host.sh` to move the export BEFORE `start.sh` invocation AND add `KEEL_RALPH_MODE: ${KEEL_RALPH_MODE:-build}` to `packages/devbox/docker-compose.yml environment:` stanza (substrate touch — AMEND path per FR44), OR switch to sidecar-file signalling (wrapper-only change, no substrate touch).

- **AR-37 — `start.sh || exit $?` passthrough swallows per-exit-code differentiation.** _Source:_ Blind Hunter + Edge Case Hunter iter-226. _Location:_ `packages/devbox/scripts/ralph-build-host.sh:46` + `ralph-plan-host.sh:46`. _Nature:_ The wrapper cannot distinguish "image not built (10)" from "healthcheck timeout (11)" from "docker unreachable (8)" for its own log output; operator sees only `start.sh`'s own stderr + the final exit code. The exit-code-meaning table in README § Ralph loop promises differentiated meaning the wrapper does not deliver at its own log layer. _Defer reason:_ SC-12 pins the exit-code passthrough contract (schema is inherited from Story 2.6 unchanged); per-code logging is UX polish, not correctness. Story 2.6 *-host.sh shims follow the same passthrough pattern — posture consistency governs. _Carry-to:_ **`_lib.sh` refactor** (Story 2.6 AR-19 substrate-wide threshold; reached at Story 2.8/2.9 potentially) — add shared `log_and_exit <code> <context>` helper that branches on the exit code and emits a contextual hint (e.g. "image not built; run `pnpm devbox:build`" for 10, "healthcheck timeout; check `pnpm devbox:logs`" for 11). Applies uniformly to all shims that call `start.sh`.

- **AR-38 — Post-auto-start race fallback is a single re-inspect with no retry/backoff.** _Source:_ Blind Hunter + Edge Case Hunter iter-226. _Location:_ `packages/devbox/scripts/ralph-build-host.sh:49-53` + `ralph-plan-host.sh:49-53`. _Nature:_ Between `start.sh` returning 0 and the wrapper's `docker inspect` re-check, the container could enter `running → exited` (PID 1 crash), `starting → restarting` (init failure), or still be in eventual-consistency lag (docker daemon caching). A single re-inspect misses legitimate in-flight transitions; the wrapper emits `exit 9` when a short retry-with-backoff (e.g. 3 × 1s) would have captured the settled state. _Defer reason:_ Spec explicitly scopes this as a "rare race" (Story 2.7 Task 1 PATCH 3 comment at `ralph-build-host.sh:50`); healthcheck polling inside `start.sh` already eliminates most eventual-consistency windows. Retry-with-backoff is infrastructure-grade complexity for a near-zero-frequency scenario. _Carry-to:_ **`_lib.sh` refactor** — shared `wait_for_container_running <name> <timeout>` helper with configurable retry/backoff. Applies to any shim that performs post-start state reconciliation.

- **AR-39 — `AGENTS.md § Ralph loop` "NEVER call `docker attach` directly" wording contradicts wrapper's own `exec docker attach` (substrate carve-out implicit but unstated).** _Source:_ Blind Hunter iter-226. _Location:_ `AGENTS.md:114` (new § Ralph loop (Story 2.7)). _Nature:_ The FR1 non-toggle-able invariant text reads as a blanket prohibition; the wrapper itself is the implementation that makes `docker attach` unnecessary for operators/agents. A future grep-based invariant-lint scanning `packages/devbox/scripts/` for `docker attach` would flag this file as a violation if naive. The same implicit carve-out applies to `docker compose`, `docker exec`, `docker info`, `docker inspect` usage inside `packages/devbox/scripts/`. _Defer reason:_ Readers currently understand the carve-out from context (the surrounding bullets describe the wrapper implementation, so the "NEVER" clearly governs operators/agents, not the shim itself). Explicit wording is a polish pass, not a correctness fix. _Carry-to:_ **AGENTS.md invariant-rules wording pass** (potentially Story 2.17 sync-gate integration) — add "(substrate scripts under `packages/devbox/scripts/` are the implementing carve-out — they wrap docker primitives so operators and agents don't have to)" clarification to the FR1 bullet.

- **AR-40 — `log()` writes all output to stderr unconditionally — info and error messages undifferentiated.** _Source:_ Blind Hunter iter-226. _Location:_ `packages/devbox/scripts/ralph-build-host.sh:32` + `ralph-plan-host.sh:32`. _Nature:_ `log() { printf '[ralph:%s] %s\n' "${RALPH_MODE}" "$*" >&2; }` sends benign info messages ("container already running; attaching directly") to stderr alongside genuine errors ("docker unreachable"). Tooling that captures stderr separately (CI log aggregators, operator terminal multiplexers) will see false positives for info-level events. _Defer reason:_ Story 2.6 *-host.sh shims (13 of them) follow identical stderr-only logging pattern — refactoring just the 2 Ralph wrappers creates cross-shim inconsistency. Info/error differentiation is a substrate-wide concern, not a Story 2.7 concern. _Carry-to:_ **`_lib.sh` refactor** — shared `log_info` (stdout) + `log_error` (stderr) differentiated helpers. Applies uniformly to all 15 host-side shims.

- **AR-41 — No `command -v docker` pre-flight guard — missing docker CLI produces interleaved `command not found` on stderr before the documented "docker unreachable" log + exit 8.** _Source:_ Blind Hunter + Edge Case Hunter iter-226. _Location:_ `packages/devbox/scripts/ralph-build-host.sh:36` + `ralph-plan-host.sh:36`. _Nature:_ `if ! docker info --format '...' >/dev/null 2>&1;` sends `docker`'s own "command not found" to stderr when the binary is absent, BEFORE the `log "docker unreachable"` line runs. Operator sees `bash: docker: command not found` immediately followed by `[ralph:build] docker unreachable — is the daemon running?` — confusing because the message blames the daemon when the real cause is the CLI itself. _Defer reason:_ Story 2.6 *-host.sh shims don't guard `command -v docker` either — extending the guard only to Ralph shims creates 13-vs-2 inconsistency. Extremely rare scenario (docker binary absent but pnpm reachable implies a severely broken operator environment). _Carry-to:_ **`_lib.sh` refactor** OR **Story 2.10 prerequisite-check** (scope-owner for docker CLI availability gating) — uniform `ensure_docker_cli` pre-flight across all shims.

- **AR-42 — `status=paused` not handled (attach proceeds against paused PID 1 instead of unpause-or-abort).** _Source:_ Edge Case Hunter iter-226. _Location:_ `packages/devbox/scripts/ralph-build-host.sh:42-47` + `ralph-plan-host.sh:42-47`. _Nature:_ `if [[ "${status}" != "running" ]]` treats `paused` as "not running" and invokes `start.sh`, but `docker compose up -d` on a paused container does not unpause it — the container stays paused and the subsequent `docker attach` hangs because PID 1 cannot process stdio. _Defer reason:_ Operator-explicit `docker pause keel-devbox` is non-standard workflow; no pnpm verb pauses the container. Extremely rare. Workaround is `docker unpause` + retry. _Carry-to:_ **`_lib.sh` refactor** — shared `ensure_container_runnable` helper treats paused/exited/restarting as uniform preconditions (unpause-if-paused, start-if-exited, wait-if-restarting).

- **AR-43 — `status=restarting` transient state treated as "not running" → redundant `start.sh` invocation during restart loop.** _Source:_ Edge Case Hunter iter-226. _Location:_ `packages/devbox/scripts/ralph-build-host.sh:44` + `ralph-plan-host.sh:44`. _Nature:_ When a container is mid-restart (docker's `restart: unless-stopped` or manual `docker restart` in flight), `status=restarting`; the wrapper invokes `start.sh` redundantly. Compose's `up -d` is idempotent, so this is wasteful rather than destructive — but it adds latency + muddies logs. _Defer reason:_ Compose's idempotency means no correctness impact; the only cost is a few seconds of unnecessary healthcheck polling. _Carry-to:_ **`_lib.sh` refactor** — shared container-state reconciler handles `paused` + `restarting` + `exited` uniformly.

- **AR-44 — Container running-but-unhealthy → wrapper attaches to broken PID 1 (no pre-attach health gate on skip-start branch).** _Source:_ Edge Case Hunter iter-226. _Location:_ `packages/devbox/scripts/ralph-build-host.sh:65` + `ralph-plan-host.sh:65`. _Nature:_ The skip-start branch trusts `status=running` and proceeds directly to `docker attach`. If the container's healthcheck has transitioned `healthy → unhealthy` mid-session (e.g., dnsmasq crashed; whitelist-reload stuck), the operator attaches to a container whose PID 1 may be unresponsive or mid-recovery. Under current `CMD: [sleep, infinity]` there is no Ralph process to attach to anyway; Epic 3's TUI would observe a blank terminal. _Defer reason:_ `start.sh` owns the healthcheck contract on the auto-start branch; the skip-start branch explicitly opts out per SC-2 ("AC 2 skip-start"). Health gating on re-attach is Story 2.13's scope (healthcheck endpoints). _Carry-to:_ **`_lib.sh` refactor** OR **Story 2.13 healthcheck endpoints** — add pre-attach `docker inspect --format '{{.State.Health.Status}}'` gate; exit `11` on unhealthy.

- **AR-45 — Concurrent wrapper invocations (multi-terminal) multiplex stdin to same PID 1; no single-attach lock.** _Source:_ Edge Case Hunter iter-226. _Location:_ `packages/devbox/scripts/ralph-build-host.sh:65` + `ralph-plan-host.sh:65`. _Nature:_ Two operators running `pnpm ralph:build` from separate terminals both call `exec docker attach` against the same container. Docker's native semantics multiplex — both stdin streams interleave, both see duplicate stdout, detach-keys from either session detach both. This is surprising but spec-silent. _Defer reason:_ Docker's multiplex behavior is documented + long-standing; fighting it with a flock-based lock would break operator workflows that intentionally share terminals (pair programming, multi-screen setup). Operator-discipline is the norm. _Carry-to:_ **Future operator-UX pass** — add "only one attach at a time" note to README § Ralph loop.

- **AR-46 — `docker attach` silent exit on PID 1 crash (no post-exec log message; operator sees only shell prompt return).** _Source:_ Edge Case Hunter iter-226. _Location:_ `packages/devbox/scripts/ralph-build-host.sh:65` + `ralph-plan-host.sh:65`. _Nature:_ `exec docker attach` replaces the wrapper shell; post-exec instrumentation is impossible without dropping `exec` (which re-introduces the "No signal trapping" gotcha — Story 2.7 PATCH 3 comment explicitly guards against defensive traps). On container crash, docker attach exits with the container's exit code and the operator returns to the host shell with no contextual message beyond `[docker output]`. _Defer reason:_ `exec` is load-bearing for signal propagation (SIGINT/SIGTERM/SIGPIPE reach PID 1 directly). Any post-exec instrumentation breaks the propagation guarantee. _Carry-to:_ **`_lib.sh` refactor** — if ever wrapping `exec`, document the signal-forwarding contract explicitly so the wrapper can be removed if contract breaks.

- **AR-47 — `docker info` succeeds but operator lacks `inspect` permission → wrapper proceeds to `start.sh` which re-fails opaquely.** _Source:_ Edge Case Hunter iter-226. _Location:_ `packages/devbox/scripts/ralph-build-host.sh:42` + `ralph-plan-host.sh:42`. _Nature:_ Custom docker RBAC (some enterprise setups) grants `docker info` (server-version read) but not per-container `docker inspect`. The pre-flight at line 36 passes; the inspect at line 42 returns empty `status=""`; the wrapper enters the auto-start branch; `start.sh`'s own `docker inspect` also fails opaquely. _Defer reason:_ Default dockerd grants equal rights for info + inspect on containers owned by the calling user; custom RBAC is non-standard. Extremely rare; no real-world reports. _Carry-to:_ **Future docker-permission hardening pass** (no immediate action needed given zero evidence of occurrence).

- **AR-48 — `docker inspect` has no timeout gate → daemon hang blocks wrapper indefinitely.** _Source:_ Edge Case Hunter iter-226. _Location:_ `packages/devbox/scripts/ralph-build-host.sh:42,49` + `ralph-plan-host.sh:42,49`. _Nature:_ An unresponsive docker daemon (OS-level hang, out-of-disk on `/var/lib/docker`) causes `docker inspect` to block. The wrapper offers no `timeout 10 docker inspect` guard; operator sees hung terminal with no feedback. _Defer reason:_ Daemon-hang is an OS-level failure; hardcoded timeout could introduce false-positive exits on cold-start daemons (RAM-pressure, post-reboot warm-up). Operator response to hang is `docker restart` or machine reboot. _Carry-to:_ **`_lib.sh` refactor** — shared `docker_with_timeout <seconds> <cmd...>` helper, gated by evidence of daemon-hang occurrence.

- **AR-49 — `BASH_SOURCE[0]` symlink drift: `SCRIPT_DIR` resolution doesn't `readlink -f` before `cd`.** _Source:_ Edge Case Hunter iter-226. _Location:_ `packages/devbox/scripts/ralph-build-host.sh:29` + `ralph-plan-host.sh:29`. _Nature:_ If an operator symlinks `/usr/local/bin/ralph-build` → `packages/devbox/scripts/ralph-build-host.sh` and invokes the symlink, `BASH_SOURCE[0]` resolves to the symlink path; `dirname + cd + pwd` gives `/usr/local/bin`, not `packages/devbox/scripts/`. The subsequent `"${SCRIPT_DIR}/start.sh"` fails with "No such file". _Defer reason:_ All 13 Story 2.6 *-host.sh shims use the same pattern — consistency governs. Symlinking shims is non-standard; pnpm invokes them directly via `package.json`'s script mapping, not via PATH-symlink. Zero real-world risk for the documented invocation surface. _Carry-to:_ **`_lib.sh` refactor** — shared `SCRIPT_DIR` derivation using `readlink -f` for symlink safety. Applies uniformly to all shims.

- **AR-50 — `KEEL_DEVBOX_CONTAINER_NAME` override is not documented in README § Ralph loop (nor in AGENTS.md § Ralph loop).** _Source:_ Blind Hunter iter-226. _Location:_ `packages/devbox/README.md:505` (new § Ralph loop). _Nature:_ The wrapper honors `KEEL_DEVBOX_CONTAINER_NAME` per SC-11 (`CONTAINER_NAME="${KEEL_DEVBOX_CONTAINER_NAME:-keel-devbox}"`), but neither the operator-facing README section nor the agent-facing AGENTS.md section mentions this override. Operators running multiple devboxes (fork + worktree + main) have no visible knob documented in the Story 2.7 section. _Defer reason:_ Story 2.2 introduced the override and documented it in `.envrc.example`; Story 2.7 correctly inherits it without re-documenting. Documenting every env-var flow in every consuming story's README would balloon each section; cross-reference to Story 2.2's upstream doc is the cleaner pattern. _Carry-to:_ **Future README polish pass** — add consolidated `## Environment variables` section at `packages/devbox/README.md` root OR add a one-line cross-ref bullet "(container name override via `KEEL_DEVBOX_CONTAINER_NAME` — see `.envrc.example`)" under § Auto-start contract.

## Deferred from: code review of story-2.8 (2026-04-22)

_Story 2.8 (`claude-code-oauth-via-pnpm-claude.sh` — FR3 Claude-side invocation path) — iter-233 `/bmad-code-review (args: "2")`. Three-layer adversarial review (Blind Hunter + Edge Case Hunter + Acceptance Auditor). **Zero PATCHes surfaced** — THIRD cumulative one-pass ZERO-PATCH CR precedent (Story 1.13 iter-85 first; Story 2.7 iter-226 second; Story 2.8 iter-233 third). The nine defers below are: (a) spec-authorised choices pinned by SC-4 / SC-6 / SC-13 / SC-15 / SC-16; (b) substrate-owned contracts (Story 2.5 image + named-volume); (c) upstream-CLI-owned semantics (Anthropic token lifecycle); or (d) narrow races (TOCTOU between `docker inspect` and `docker exec`). Auditor returned zero AC/SC violations; wrapper matches § Shim structure template verbatim._

- **AR-51 — Non-TTY invocation fails on hardcoded `-it` (cron / CI / piped stdin).** _Source:_ Edge Case Hunter iter-233 (HIGH) + Blind Hunter iter-233 (LOW). _Location:_ `packages/devbox/scripts/claude-host.sh:58`. _Nature:_ Hardcoded `docker exec -it` fails with "the input device is not a TTY" in non-TTY contexts (`pnpm claude < /dev/null`, cron, CI runners, `pnpm claude | tee log.txt`). Sibling `shell.sh:37-42` forks on `$#` to pick `-i` vs `-it`; `claude-host.sh` has no such fork. _Defer reason:_ SC-6 explicitly pins hardcoded `-it` — "OAuth flow IS the AC" — and the wrapper is interactive-only by contract. Non-TTY contexts are outside the AC 1-5 scope at 1.0. Story 2.7's `ralph-build-host.sh` shares the same interactive-by-contract posture. _Carry-to:_ **Post-Epic-2 polish** — if a downstream story (e.g., CI-driven `pnpm claude -p "hello"` composition) surfaces a non-TTY use case, fork on `[[ -t 0 && -t 1 ]]` to drop `-t` like sibling `shell.sh`; requires an FR44 AMEND because SC-6 is a pinned contract.
- **AR-52 — TOCTOU between `docker inspect` status check and `docker exec`.** _Source:_ Blind Hunter iter-233 (LOW). _Location:_ `packages/devbox/scripts/claude-host.sh:46-58`. _Nature:_ Container could stop between the inspect call (line 46-49) and `docker exec` (line 58); `docker exec` then fails with its own error and propagates a non-uniform exit code (not `9`), violating the documented exit-code schema. _Defer reason:_ Narrow race — container stopping mid-wrapper is rare in operator workflows. AGENTS.md § Ralph loop (Story 2.7) documents an identical "rare race" precedent for the ralph wrappers ("container exited between start.sh's success and the attach call"). Docker's own error bubbles via the `*` schema slot. _Carry-to:_ **`_lib.sh` refactor** — shared `docker_exec_with_retry <N> <cmd...>` helper could collapse the gap, but only if evidence shows the race fires in practice. Low priority.
- **AR-53 — Exit code `8` catches only `docker info`; daemon-dies-between races collapse to misleading exit `9`.** _Source:_ Blind Hunter iter-233 (LOW). _Location:_ `packages/devbox/scripts/claude-host.sh:40-49`. _Nature:_ If the docker daemon dies between the `docker info` check (line 40) and `docker inspect` (line 46), the inspect's `|| true` swallows the daemon error and `state` becomes empty, producing exit `9` ("container not running") instead of `8` ("docker unreachable"). _Defer reason:_ Narrow race; spec-silent on mid-flight-daemon-death semantics. The swallow pattern is inherited from the § Shim structure template and matches sibling scripts. _Carry-to:_ **`_lib.sh` refactor** — distinguish empty-state from docker-error via stderr capture in the shared helper.
- **AR-54 — Non-running container states (paused/exited/restarting/created) all collapse to a single exit-9 message.** _Source:_ Edge Case Hunter iter-233 (LOW). _Location:_ `packages/devbox/scripts/claude-host.sh:49-52`. _Nature:_ `docker inspect --format '{{.State.Status}}'` returns one of `created/restarting/running/removing/paused/exited/dead`; the wrapper compares only to `"running"` and emits "container 'X' is not running — run 'pnpm devbox:start' first". This is wrong for `paused` (needs `unpause`) and misleading for `restarting` (may auto-recover). _Defer reason:_ UX refinement, not a functional issue. Aligns with sibling `shell.sh` / `attach.sh` precedent which share the same collapse. _Carry-to:_ **`_lib.sh` refactor** — shared `assert_container_running <name>` helper with per-state guidance (`paused → unpause`, `exited → start`, `restarting → wait`). Would apply uniformly to all 16 shims.
- **AR-55 — Nonexistent vs stopped container indistinguishable in exit-9 message.** _Source:_ Edge Case Hunter iter-233 (LOW). _Location:_ `packages/devbox/scripts/claude-host.sh:46-52`. _Nature:_ `docker inspect … 2>/dev/null || true` masks stderr; when the container doesn't exist at all (typo in `KEEL_DEVBOX_CONTAINER_NAME`, e.g. `keel-devbx`), `state=""` and the message says "container 'keel-devbx' is not running — run 'pnpm devbox:start' first" which will not help because `start.sh` creates a differently-named container. _Defer reason:_ Same UX family as AR-54; operator-typo recovery is low-value vs. template consistency cost. _Carry-to:_ **`_lib.sh` refactor** — distinguish empty-state from non-empty-non-running state in the shared helper.
- **AR-56 — UID 1000 ≠ `dev` inside container → token lands at wrong HOME.** _Source:_ Edge Case Hunter iter-233 (MEDIUM). _Location:_ `packages/devbox/scripts/claude-host.sh:58`. _Nature:_ The wrapper passes `--user dev`, which resolves via container's `/etc/passwd` to whatever UID/HOME that entry has. If `dev`'s HOME is not `/home/dev`, the token persists elsewhere and the volume mount `/home/dev/.claude/` stays empty → re-prompts OAuth on every invocation. Wrapper exit is clean (exit 0); user perceives "auth didn't stick." _Defer reason:_ Substrate concern owned by Story 2.5 image contract (Dockerfile `useradd --uid 1000 --gid 1000 --shell /bin/bash --create-home dev` + HOME=/home/dev fixed). Wrapper cannot enforce HOME; image-drift is caught by Story 1.9 sync-gate for Dockerfile drift. _Carry-to:_ **Substrate invariant** — could tighten `INV-devbox-homedev-named-volume` to also assert `dev` user's HOME resolves to `/home/dev` at image build time; not load-bearing at 1.0.
- **AR-57 — `/home/dev/.claude/` non-writable by UID 1000 silently fails inside claude.** _Source:_ Edge Case Hunter iter-233 (MEDIUM). _Location:_ `packages/devbox/scripts/claude-host.sh:58`. _Nature:_ If the named volume is pre-populated as root (e.g., by a prior `--user root` exec), `dev` cannot write. Wrapper cannot see claude's internal error; claude may exit 0 after printing a warning, or may retry OAuth on every invocation. _Defer reason:_ Substrate concern; Dockerfile:251-252 placeholder (`/home/dev/.claude/` pre-created chown'd to `dev:dev`) + Story 2.5 named-volume auto-init semantics own the writability contract. SC-13 explicitly forbids the wrapper from attempting `mkdir` / `chmod`. _Carry-to:_ **Operator runbook** — document triage with `ls -la /home/dev/.claude` pointer in a future README polish; no wrapper change.
- **AR-58 — Concurrent `pnpm claude` races OAuth token write on first-run.** _Source:_ Edge Case Hunter iter-233 (LOW). _Location:_ `packages/devbox/scripts/claude-host.sh:58`. _Nature:_ Two terminals running `pnpm claude` simultaneously on first-run both spawn `docker exec`, both hit the OAuth flow, last writer wins in `/home/dev/.claude/`. Not corrupting (token file is atomic), but wasted UX. _Defer reason:_ First-run race only; multi-terminal OAuth flow is outside AC 1-5 scope. Spec does not mandate concurrency control at the wrapper layer. _Carry-to:_ **Upstream CLI concern** — if `@anthropic-ai/claude-code` gains a lockfile or token-write serialization, wrapper inherits it for free.
- **AR-59 — `pnpm claude --help` consumed by pnpm; operator must use `pnpm claude -- --help`.** _Source:_ Edge Case Hunter iter-233 (MEDIUM). _Location:_ `packages/devbox/README.md:91-102` (Quick start). _Nature:_ `pnpm <script> --flag` may have pnpm consume specific flags like `--help`; operators expecting `claude --help` behaviour get pnpm's help instead. Wrapper itself (`"$@"`) is correct; issue is in pnpm's invocation contract. _Defer reason:_ pnpm CLI contract outside wrapper scope. SC-16 carries Story 2.6 AR-18 `--help` interception deferral explicitly. _Carry-to:_ **Future README polish pass** — add a one-line note to `packages/devbox/README.md § Quick start` reminding operators to use `pnpm claude -- --help` for claude's own help; grouped with the post-Epic-2 `_lib.sh` + README consolidation work.

## Deferred from: code review of story-2.9 (2026-04-23)

_Story 2.9 (`gh` CLI OAuth via `pnpm gh:auth` — FR3 gh-side invocation path) — iter-244 `/bmad-code-review (args: "2")`. Three-layer Ralph-hosted adversarial review (Blind Hunter as `bmad-review-adversarial-general` via `general-purpose` subagent; Edge Case Hunter as `bmad-review-edge-case-hunter` via `general-purpose` subagent with repo read access; Acceptance Auditor via `general-purpose` subagent with spec file + substrate cross-refs). **Zero PATCHes surfaced** — FOURTH cumulative one-pass ZERO-PATCH CR precedent (Story 1.13 iter-85 first; Story 2.7 iter-226 second; Story 2.8 iter-233 third; Story 2.9 iter-244 fourth). Scope: iter-237 impl commit `fdbd5a2` — 4 files (1 new `gh-auth-host.sh` 68 lines + `package.json` +1 + `README.md` +59 + `AGENTS.md` +14) = 222-line diff. Iter-238..241 orthogonal infrastructure retrofits explicitly out of AC envelope per IP § Context + iter-243 post-dev SM subagent-A verdict. The 11 defers below are: (a) substrate-wide pre-existing patterns inherited from shell.sh + attach.sh + claude-host.sh carrying to the post-Epic-2 `_lib.sh` refactor (SC-14 chain); (b) upstream-CLI-owned semantics already documented in SC-15; (c) narrow races + substrate-UX improvements natural-scope for Story 2.17 Epic 2 close-out or a dedicated lib-extraction story. Acceptance Auditor returned zero AC/SC violations ("All ACs and SCs verified satisfied by the diff"); wrapper matches § Shim structure template verbatim. Triage counts: **0 PATCH / 11 DEFER / 19 DISMISS** after dedupe + SC cross-check._

- **AR-60 — `docker inspect` TOCTOU race between inspect + exec.** _Source:_ Blind Hunter + Edge Case Hunter (convergent). _Location:_ `packages/devbox/scripts/gh-auth-host.sh:62-68`. _Nature:_ Between the `docker inspect` (line ~62) and `docker exec` (line ~68), the container can stop, restart, or be removed. If the race fires, `docker exec` fails with a raw exit-code mapped to `*` rather than the advertised exit 9. Identical race exists in shell.sh:32-35 + attach.sh:32-36 + claude-host.sh:49-52 (AR-52 duplicate confirmation). _Defer reason:_ Fixing it for gh-auth-host.sh alone breaks SC-17 mirror discipline ("MIRRORS claude-host.sh verbatim"); substrate-wide pattern. _Carry-to:_ **post-Epic-2 `_lib.sh` refactor** (SC-14 chain) — shared `ensure_container_running()` helper can add retry-after-inspect loop or race-safe `docker exec` with container-status-check propagation. Applies uniformly to all 17 host-side shims.
- **AR-61 — `state` assignment masks `docker inspect` non-ENOENT failures behind `|| true`.** _Source:_ Blind Hunter + Edge Case Hunter (convergent). _Location:_ `packages/devbox/scripts/gh-auth-host.sh:62`. _Nature:_ `|| true` collapses permission-denied, API-version-mismatch, daemon-hiccup, and malformed-name errors into empty state string → exit 9 "container not running" diagnostic, misdirecting the operator when the real fault is docker-daemon auth/state rather than container state. AR-53 duplicate confirmation. _Defer reason:_ Identical inherited pattern across all 17 host-side shims. _Carry-to:_ **post-Epic-2 `_lib.sh` refactor** — shared helper can branch on `docker inspect` distinct exit codes (2 = no such object vs other = daemon/API fault) and emit exit 8 vs exit 9 appropriately.
- **AR-62 — `CONTAINER_NAME` env var not validated against leading-dash / whitespace / metacharacters.** _Source:_ Blind Hunter + Edge Case Hunter (convergent). _Location:_ `packages/devbox/scripts/gh-auth-host.sh:45`. _Nature:_ `${KEEL_DEVBOX_CONTAINER_NAME:-keel-devbox}` fallback with no `--` separator before `"${CONTAINER_NAME}"` in the subsequent `docker inspect` / `docker exec` calls; a value with a leading `-` is mis-parsed by docker as a flag, and whitespace-bearing values produce unpredictable target containers. _Defer reason:_ Story 2.6 SC-11 + SC-2 precedent — the `${VAR:-default}` pattern is substrate-wide convention; fork operators are trusted not to set malicious env vars (threat model: operator-trusted input, not attacker-controlled). Substrate-wide; all 17 shims share the pattern. _Carry-to:_ **post-Epic-2 `_lib.sh` refactor** — shared `validate_container_name()` helper can enforce `[A-Za-z0-9_.-]+` pattern + emit exit 2 on violation.
- **AR-63 — SIGPIPE-forwarding claim in wrapper comment + SC-10 is technically imprecise.** _Source:_ Blind Hunter. _Location:_ `packages/devbox/scripts/gh-auth-host.sh:64-66`. _Nature:_ Wrapper comment states "`docker exec` forwards SIGINT/SIGTERM/SIGPIPE to gh's PID inside the container." `docker exec` does forward SIGINT/SIGTERM via the Docker API, but SIGPIPE is a per-process kernel signal raised on write-to-closed-pipe inside the target process — it is not "forwarded" from the host. Comment is verbatim inheritance from SC-10 + Story 2.1 iter-144 SIGPIPE precedent + claude-host.sh:45. _Defer reason:_ Correcting it in gh-auth-host.sh alone breaks SC-17 "mirrors claude-host.sh verbatim" discipline. _Carry-to:_ **post-Epic-2 `_lib.sh` refactor** or a standalone docs-cleanup pass — refine comment to "`docker exec` forwards SIGINT/SIGTERM to gh's PID; SIGPIPE is raised locally in the target process by the kernel on write-to-closed-pipe" across all 17 shims atomically.
- **AR-64 — `github.txt` 7-entry enumeration cited across AGENTS.md + README.md + story file without drift gate.** _Source:_ Blind Hunter. _Location:_ `AGENTS.md § gh CLI authentication (Story 2.9) § Scope carve-out` + mirrored in story file Dev Notes + CR forecast text. _Nature:_ AGENTS.md enumerates all 7 GitHub whitelist entries inline; if `packages/devbox/whitelist/github.txt` drifts (entry added/removed at CI security review), the AGENTS.md enumeration becomes stale with no lint gate to catch it. _Defer reason:_ Story 1.9 INVARIANTS.md↔manifest sync-gate covers content-hash drift (`INV-devbox-egress-contract` hashes the whitelist file) but the cross-file prose enumerations in AGENTS.md + README.md are not currently sync-gate-watched. Same class as Story 2.7 AR-50 `KEEL_DEVBOX_CONTAINER_NAME` doc-drift concern. _Carry-to:_ **Story 1.9+ sync-gate extension** OR a dedicated substrate lint rule (`tools/lint-substrate-enumerations.ts`) that diffs `packages/devbox/whitelist/github.txt` content against AGENTS.md + README.md prose enumerations and fails on drift. Low-cost once framework lands.
- **AR-65 — OAuth device-code surface on stdout → log-leak risk if operator `tee`s output.** _Source:_ Blind Hunter. _Location:_ `packages/devbox/README.md § First-run OAuth flow (AC 1, AC 2)`. _Nature:_ OAuth device codes (valid ~15 min, single-use per GitHub's device-flow spec) appear on operator stdout during `gh auth login`. If operator runs `pnpm gh:auth 2>&1 | tee gh-auth.log` (common troubleshooting pattern), the code lands in a logfile. Practically low severity (attacker would race the operator's own OAuth completion and GitHub allows multiple tokens per account), but worth operator-warning. Applies symmetrically to `pnpm claude` OAuth flow (Story 2.8). _Defer reason:_ Substrate-wide (both auth verbs); docs-hint addition is a polish-pass scope item. _Carry-to:_ **Story 2.17 Epic 2 close-out polish** — add a `> **Security note:** OAuth flow emits a short-lived device code to stdout; do not `tee`/pipe the output to a logfile during the flow.` callout to README.md sections for both auth verbs atomically.
- **AR-66 — No `IFS=$' \t\n'` reset at top of wrapper.** _Source:_ Blind Hunter. _Location:_ `packages/devbox/scripts/gh-auth-host.sh:31` (just after `set -euo pipefail`). _Nature:_ A parent shell with non-default IFS could affect `"$@"` expansion in edge cases (whitespace in scope lists, tab characters, etc.). Defensive bash scripts normally pin `IFS=$' \t\n'` alongside `set -euo pipefail`. Zero live exposure at 1.0 (pnpm shell-out strips abnormal IFS; no explicit word-splitting on untrusted values inside the script body). _Defer reason:_ Identical inherited pattern across all 17 host-side shims. _Carry-to:_ **post-Epic-2 `_lib.sh` refactor** — shared boilerplate block can set `IFS=$' \t\n'` + `set -euo pipefail` + `unset COMPOSE_PROJECT_NAME` atomically.
- **AR-67 — `docker info` has no timeout (hangs if daemon alive but unresponsive).** _Source:_ Edge Case Hunter. _Location:_ `packages/devbox/scripts/gh-auth-host.sh:53`. _Nature:_ Bare `docker info --format '{{.ServerVersion}}'` hangs indefinitely on a slow/unresponsive daemon; operator cannot distinguish from an in-progress OAuth prompt. `timeout 10s docker info ...` would fail-closed with exit 8 after bound. Matches Story 2.7 AR-48 duplicate confirmation. _Defer reason:_ Identical inherited pattern across all 17 host-side shims; daemon-hang is OS-level failure. _Carry-to:_ **post-Epic-2 `_lib.sh` refactor** — shared `docker_info_check()` helper can wrap the call in `timeout 10s` and emit exit 8 on timeout with a distinct diagnostic ("docker daemon unresponsive after 10s").
- **AR-68 — Ambiguous container name across multi-compose-project scenarios.** _Source:_ Edge Case Hunter. _Location:_ `packages/devbox/scripts/gh-auth-host.sh:62`. _Nature:_ If two containers share the name via compose-project override (unlikely under `unset COMPOSE_PROJECT_NAME` pin but theoretically possible if operator runs two worktrees with overlapping `.envrc` values), `docker inspect` targets one non-deterministically. A `docker ps -aq --filter "name=^${CONTAINER_NAME}$" | wc -l` pre-check catches the duplicate case. _Defer reason:_ Same substrate-wide pattern; the `unset COMPOSE_PROJECT_NAME` + `.envrc` `KEEL_DEVBOX_CONTAINER_NAME` convention at 1.0 makes duplicates unlikely in practice. _Carry-to:_ **post-Epic-2 `_lib.sh` refactor** or a dedicated Story 2.11 (shared-devbox mode) task — the shared-mode work explicitly touches multi-container naming and is the natural home for this guard.
- **AR-69 — `paused` / `restarting` / `removing` container states misdiagnosed as "not running".** _Source:_ Edge Case Hunter. _Location:_ `packages/devbox/scripts/gh-auth-host.sh:63`. _Nature:_ `[[ "${state}" != "running" ]]` branch emits "container is not running — run `pnpm devbox:start` first" for any non-running state, including transient (`restarting`, `removing`) and suspended (`paused`) states. A `case`-based dispatch could emit more-diagnostic messages per state. AR-54 + AR-43 duplicate confirmation. _Defer reason:_ Identical inherited pattern; no operator friction has surfaced in Stories 2.1-2.8 usage. _Carry-to:_ **post-Epic-2 `_lib.sh` refactor** — shared `ensure_container_running()` helper can branch on `paused` (hint: `docker unpause`), `restarting` (hint: "wait 10s and retry"), `removing` (hint: "stale container; `pnpm devbox:clean` to cycle"), `exited`/`dead`/`created` (hint: `pnpm devbox:start`).
- **AR-70 — "Mirrors `claude-host.sh` verbatim" has no drift gate.** _Source:_ Blind Hunter. _Location:_ `packages/devbox/scripts/gh-auth-host.sh` vs `packages/devbox/scripts/claude-host.sh`. _Nature:_ AGENTS.md + spec Dev Notes SC-1 claim the two scripts mirror each other 1:1 with documented substitutions. If `claude-host.sh` receives a security/robustness fix in isolation (e.g., the `docker inspect` TOCTOU fix, or a `docker info` timeout), nothing auto-propagates to `gh-auth-host.sh` — the mirror claim silently degrades over time. _Defer reason:_ This is exactly the problem the SC-14 `_lib.sh` refactor solves by factoring the shared pre-flight into one sourced library; the forcing-function for that refactor is the post-Epic-2 amortization point. _Carry-to:_ **post-Epic-2 `_lib.sh` refactor** — extract `packages/devbox/scripts/lib/docker-preflight.sh` (shared `unset COMPOSE_PROJECT_NAME` + `docker_info_check()` + `ensure_container_running()` + `log()`). Story 2.9 post-iter-239 retrofit already extracted `lib/main-repo-resolver.sh` + `lib/check-mount-source.sh` as a first slice of this refactor — trajectory is now visible + the amortization point is closer than forecast at iter-233.
