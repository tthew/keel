---
stepsCompleted:
  - step-01-load-context
  - step-02-discover-tests
  - step-03-map-criteria
  - step-04-analyze-gaps
  - step-05-gate-decision
lastStep: step-05-gate-decision
lastSaved: 2026-04-26
workflowType: testarch-trace
inputDocuments:
  - _bmad-output/implementation-artifacts/1-18-bootstrap-python-test-runner-pytest-under-uv.md
  - _bmad-output/planning-artifacts/epics.md
  - _bmad-output/planning-artifacts/prd.md
  - _bmad-output/planning-artifacts/architecture.md
coverageBasis: acceptance_criteria
oracleConfidence: high
oracleResolutionMode: formal_requirements
oracleSources:
  - _bmad-output/implementation-artifacts/1-18-bootstrap-python-test-runner-pytest-under-uv.md (AC1–AC5)
externalPointerStatus: not_used
tempCoverageMatrixPath: /tmp/tea-trace-coverage-matrix-1-18.json
---

# Traceability Matrix & Gate Decision — Story 1.18 Bootstrap Python test runner (pytest under uv) + root pyproject.toml

**Target:** Story 1.18 — Bootstrap Python test runner (pytest under uv) + root pyproject.toml
**Date:** 2026-04-26 (iter-367)
**Evaluator:** Tthew (TEA Agent via Ralph build-mode)
**Coverage Oracle:** acceptance_criteria
**Oracle Confidence:** high
**Oracle Sources:** Story 1.18 ACs 1–5 (formal requirements)

---

## PHASE 1: REQUIREMENTS TRACEABILITY

### Coverage Summary

| Priority  | Total Criteria | FULL Coverage | Coverage % | Status                  |
| --------- | -------------- | ------------- | ---------- | ----------------------- |
| P0        | 2              | 1             | 50%        | ❌ NOT_MET              |
| P1        | 1              | 0             | 0%         | ❌ NOT_MET              |
| P2        | 2              | 0             | 0%         | informational           |
| P3        | 0              | 0             | n/a        | n/a                     |
| **Total** | **5**          | **1**         | **20%**    | **WAIVED (structural)** |

**Gate decision: WAIVED.** The deterministic FAIL signal (P0 50%, overall 20%) is a structural artefact. Story 1.18 IS the Python test infrastructure being bootstrapped — there is no pre-existing pytest corpus to enforce coverage against. Substrate-verification + Story 1.9 sync-gate (`pnpm keel-invariants:check`) + iter-env smoke matrix (Subtasks 9.1–9.5) form the verification harness pre-Story 1.21 audit. Hybrid grounds (a) + (c) variant-(ii)+(iii) per IP § NOW directive (FR14n § ATDD-skip ground-(b) sunset post-Story-1.17 per issue #233 amendment).

### Detailed Mapping

#### AC1: `uv sync && uv run pytest` discovers and runs the three test scaffolds (P0)

- **Coverage:** FULL ✅
- **Tests:**
  - `1.18-UNIT-001` — `tests/test_ralph.py::test_format_duration_basic` (assertion: `ralph.format_duration(0.0) == "0s"`)
    - **Given:** root `pyproject.toml` with `[tool.pytest.ini_options] testpaths = ["tests", "scripts/tests", "packages/devbox/tui/tests"]` + `pythonpath = [".", "scripts", "packages/devbox"]`; `uv.lock` deterministic; `textual==8.2.4` installed for `ralph.py:66` top-level `from textual.app import` (Subtask 3.1 substrate-probe-gap correction)
    - **When:** `uv sync --extra dev && uv run pytest` runs from worktree root
    - **Then:** pytest 9.0.3 collects + runs the test; passes; canonical zero-input output `"0s"` (verified via `ralph.py:89-96` reading at dev-story per Completion Notes)
  - `1.18-UNIT-002` — `tests/test_ralph.py::test_format_duration_hour_plus_ends_with_s` (assertion: `ralph.format_duration(3725.0).endswith("s")` is True; second optional smoke per Subtask 3.1)
    - **Given:** same harness as UNIT-001
    - **When:** `uv run pytest` invokes the second smoke
    - **Then:** `"1h02m05s".endswith("s") == True`; passes
  - `1.18-UNIT-003` — `scripts/tests/test_bootstrap_bmad_agents.py::test_module_loads_with_constants` (assertion: `"Edit" in module.EXECUTION_TOOLS` AND `"Read" in module.ADVISORY_TOOLS`)
    - **Given:** importlib pattern (`spec_from_file_location` + `exec_module`) loads the hyphen-named source per Subtask 4.1; module-level constants at `scripts/bootstrap-bmad-agents.py:24-25`
    - **When:** test imports + asserts list membership
    - **Then:** both list memberships hold; passes
  - `1.18-UNIT-004` — `packages/devbox/tui/tests/test_theme.py::test_theme_neutral_500` (assertion: `theme.colors.neutral_500 == "oklch(52% 0 0)"`)
    - **Given:** `pythonpath` includes `packages/devbox`; `tui` is a proper Python package (`packages/devbox/tui/__init__.py`); `theme.colors.neutral_500` is the autogenerated literal at `theme.py:15` (stable since Story 1.13 token landing)
    - **When:** test imports `from tui.theme import theme` + asserts the literal
    - **Then:** equality holds; passes
- **Evidence:** Dev Agent Record § Debug Log References (v1.3, iter-366): `UV_PYTHON_PREFERENCE=only-system uv run pytest` reports `4 passed in 0.97s` (pytest 9.0.3 / pluggy 1.6.0 / asyncio-1.3.0). Exit code 0.
- **Recommendation:** none — AC verified end-to-end at iter-env smoke baseline. Note: AC1 maps to 4 tests vs Story 1.17 AC1's 1 — Story 1.18 ships 3 distinct module-family scaffolds plus an optional second `format_duration` assertion, satisfying the "all three scaffolds pass" verbatim AC1 clause.

#### AC2: `uv.lock` resolves deterministically + Story 1.9 sync-gate stays green (P0)

- **Coverage:** PARTIAL ⚠️
- **Tests:**
  - `1.18-CHECK-001` — `UV_PYTHON_PREFERENCE=only-system uv sync --frozen --extra dev` (deterministic re-resolution per AC2 first clause)
    - **Given:** `uv.lock` produced by Subtask 2.1 `uv sync --extra dev` (70159 bytes; deterministic dev-extras + transitive resolution including `textual==8.2.4`)
    - **When:** `--frozen` re-run executes
    - **Then:** exit 0; identical resolution across runs (verified at Subtask 9.2 per Dev Agent Record § Debug Log References v1.3)
  - `1.18-CHECK-002` — `pnpm keel-invariants:check` (Story 1.9 sync-gate runtime; iter-env baseline)
    - **Given:** Story 1.18 substrate edits land (`pyproject.toml`, `uv.lock`, three test directories, `.github/workflows/ci.yml` python job, CLAUDE.md row, AGENTS.md sentence rewrite)
    - **When:** sync-gate runs against `invariants.manifest.ts` registered rules
    - **Then:** `INV-deps-version-pinning` row stays GREEN; the three pre-existing `INV-git-hooks-preservation` drifts (RALPH.md iter-358 gotcha) persist UNCHANGED — ZERO new drifts attributable to Story 1.18 edits
- **Gaps:**
  - Missing: dedicated `INV-pytest-pin` / `INV-uv-lock-determinism` / `INV-pyproject-toml` manifest rules. Per SC-8, day-1 manifest registration would flap the sync-gate against in-flight stories — Python-dep manifest extension is deferred to Story 1.20 OR Story 1.21 audit (per SC-9 two-path deferral).
  - Missing: `INV-deps-version-pinning` extension to drift-detect `uv.lock` content-hash (analogue of `pnpm-lock.yaml` coverage). Per SC-9, Story 1.20 picks the path; Story 1.21 audit cleans up if Story 1.20 declines.
  - Missing: pre-existing 3× `INV-git-hooks-preservation` drifts on `feat/epic-2-packaged-devbox` head are out of scope per RALPH.md iter-358 gotcha + SC-9 carve-out; address before Story 1.20 close-out. AC2 satisfied because the AC's specific check is "no NEW drift attributable to this story's edits" — verified via direct comparison: contentHash mismatch `cb27263d… → 42a42b16…` matches what was observed at Story 1.17 iter-359 dev-story (pre-existing baseline).
- **Recommendation:** Story 1.20 / 1.21 extend `INV-deps-version-pinning` to cover `uv.lock` AND register the worktree-mode resolver fix for `INV-git-hooks-preservation`.
- **Evidence:** Dev Agent Record § Completion Notes (v1.3, iter-366): sync-gate posture summarised; `INV-deps-version-pinning` GREEN; pre-existing drifts identical to Story 1.17 iter-359 baseline.

#### AC3: `.github/workflows/ci.yml` extended with `python` job; node job byte-identical (P1)

- **Coverage:** PARTIAL ⚠️
- **Tests:**
  - `1.18-SUBSTRATE-001` — `.github/workflows/ci.yml:30+` substrate verification (additive `python` job appended after `node` job per AC3 + SC-5)
    - **Given:** workflow file extended at Subtask 6.1; `node` job (lines 17–29 pre-edit) preserved BYTE-IDENTICAL post-edit per SC-5 additive clause
    - **When:** YAML parsed by `pnpm exec js-yaml < .github/workflows/ci.yml > /dev/null` OR by GitHub Actions ingestion post-PR-base-flip-to-main
    - **Then:** valid YAML; two top-level jobs (`node` + `python`); `python` job uses `runs-on: ubuntu-latest` + `actions/checkout@v4` + `astral-sh/setup-uv@v6` with `version: '0.11.7'` (prettier-normalised single-quote per repo convention) + `uv sync --extra dev --frozen` + `uv run pytest`
- **Gaps:**
  - Missing: `actionlint` validation in iter env (binary unavailable per Subtask 9.5; AC3 fallback clause invokes GH ingestion-side validation post-push). PARTIAL ground-(c) variant-(ii) per RALPH.md iter-299 — substrate half FULL, syntactic-validity behavioural half deferred to GH ingestion. The PR is Draft against `feat/epic-2-packaged-devbox` (not `main`), so the workflow does not fire on this push (`on: { pull_request: { branches: [main] } }` triggers only against `main`-targeting PRs).
  - Missing: `INV-fr14i-ci-workflow-presence` invariant registration enforcing the file's continued existence + per-job presence (`node` + `python`). Per SC-6, FR14i activation lands in Story 1.20.
  - Missing: branch-protection `python`-as-required-check configuration is GH-UI / admin-scope (out of substrate per SC-6) — explicitly diverges from epics.md:1188.
- **Recommendation:** Story 1.20 registers `INV-fr14i-ci-workflow-presence` (covering both jobs); first GH-side ingestion validation occurs when the Epic 1 reopen-arc PR ladder base-flips to `main`.

#### AC4: CLAUDE.md `## Common commands` table documents `uv run pytest` (P2)

- **Coverage:** PARTIAL ⚠️
- **Tests:**
  - `1.18-SUBSTRATE-002` — `CLAUDE.md:26` substrate verification (`| Run Python tests | \`uv run pytest\` |` row appended after the `Run lint` row at line 25)
    - **Given:** CLAUDE.md table extension per Subtask 7.1
    - **When:** `prettier --check` runs against CLAUDE.md
    - **Then:** file prettier-idempotent (`pnpm format:check` GREEN per Dev Agent Record § Debug Log References v1.3)
- **Gaps:**
  - Missing: prose-quality assertion (no automated check that the docs remain accurate as `uv run pytest` semantics evolve). Documentation drift is human-maintained via cross-file knowledge contract per CLAUDE.md `## Knowledge-file contract`.
  - Missing: line 23's "Run all tests" wording asymmetry intentional per AC4 second clause (no retitling to "Run TS tests" per SC-5 byte-identical-Story-1.17-substrate clause); no automated check enforces the asymmetry.
- **Recommendation:** none at this scope — docs are substrate-verified at landing; future drift mitigated by sync-gate when applicable rules are registered (Story 1.20 / 1.21 territory).

#### AC5: AGENTS.md `## Testing` section updated to active-tense `uv run pytest` language (P2)

- **Coverage:** PARTIAL ⚠️
- **Tests:**
  - `1.18-SUBSTRATE-003` — `AGENTS.md:43-45` substrate verification (second sentence rewritten to byte-pinned wording per AC5; first + third sentences byte-identical pre/post)
    - **Given:** AGENTS.md `## Testing` section edit per Subtask 8.1; byte-pinned replacement string matches AC5's exact wording: "Run `uv run pytest` from the worktree root for the workspace-wide pytest suite covering `ralph.py` (`tests/`), `scripts/bootstrap-bmad-agents.py` (`scripts/tests/`), and `packages/devbox/tui/` (`packages/devbox/tui/tests/`); dev deps + Python 3.10+ are pinned in root `pyproject.toml` + `uv.lock` (D6)."
    - **When:** `prettier --check` runs against AGENTS.md
    - **Then:** file prettier-idempotent (`pnpm format:check` GREEN per Dev Agent Record § Debug Log References v1.3 + Subtask 8.2)
- **Gaps:**
  - Missing: prose-quality assertion (same shape as AC4 — no automated assertion that the byte-pinned replacement persists across future agent edits). Architecture.md § M0 cross-reference (third sentence) preserved BYTE-IDENTICAL per AC5 third clause; no automated assertion enforces the cross-ref.
  - Missing: `INV-agents-md-testing-section` content-hash invariant. Out of scope per SC-8; if a future story decides AGENTS.md `## Testing` section warrants drift detection, Story 1.21 audit may codify.
- **Recommendation:** none at this scope — substrate is verified at landing; future drift is mitigated by sync-gate when applicable rules are registered.

---

### Gap Analysis

#### Critical Gaps (BLOCKER) ❌

0 critical gaps. AC1 (test runner discovery + 3 module-family smokes pass) and AC2 (deterministic resolution + sync-gate green for new drift) are the two P0 ACs; AC1 is FULL, AC2 is PARTIAL with the missing pieces (`INV-pytest-pin` / `INV-uv-lock-determinism` registration + 3× pre-existing drifts) explicitly deferred per SC-8 + SC-9 to Story 1.20 / 1.21.

#### High Priority Gaps (PR BLOCKER) ⚠️

1 P1 PARTIAL gap (AC3): `actionlint` unavailable in iter env. Behavioural half deferred to GH ingestion-side validation post-PR-base-flip-to-main per AC3 fallback clause + Story 1.17 Subtask 10.4 precedent.

#### Medium Priority Gaps (Nightly) ⚠️

2 P2 PARTIAL gaps (AC4 + AC5): doc-drift placeholders (no automated prose-quality assertion). Both intentional per § Knowledge-file contract.

#### Low Priority Gaps (Optional) ℹ️

0.

---

### Coverage Heuristics Findings

- **Endpoint coverage gaps:** not applicable (no API surface in Story 1.18; the story is bootstrap test-runner + CI scaffolding).
- **Auth/Authz negative-path gaps:** not applicable.
- **Happy-path-only gaps:** the four smoke tests assert pinned literals (zero-input `format_duration`, optional second `endswith("s")`, list membership for `EXECUTION_TOOLS`/`ADVISORY_TOOLS`, exact `oklch(52% 0 0)` literal) — happy-path-only by design (the smokes are intentionally minimal per SC-1; they exist ONLY to satisfy AC1 + the FR14n § ground-(a) substrate-verification clause). Story 1.21 audit / future Python coverage backfill adds adversarial test coverage.
- **UI journey gaps:** not applicable.
- **UI state gaps:** not applicable.

---

### Quality Assessment

- **Tests passing quality gates:** 4/4 (the smoke suite) — passes pytest 9.0.3 with 0.97s wall time, deterministic, single-assertion per test (UNIT-001/003/004) plus optional second assertion (UNIT-002).
- **No flaky tests detected** (pytest reporter `4 passed in 0.97s` is deterministic across re-runs per Subtask 9.1).

---

### Coverage by Test Level

| Test Level | Tests | Criteria Covered | Coverage % |
| ---------- | ----- | ---------------- | ---------- |
| E2E        | 0     | 0                | n/a        |
| API        | 0     | 0                | n/a        |
| Component  | 0     | 0                | n/a        |
| Unit       | 4     | 1 (AC1)          | 20%        |
| **Total**  | **4** | **1**            | **20%**    |

Note: the 4 PARTIAL ACs (AC2–AC5) are covered by substrate-verification + sync-gate runtime check + prettier idempotence check, none of which is a discoverable pytest test. They are tracked outside the test-level inventory above.

---

### Traceability Recommendations

#### Immediate Actions (Before PR Merge)

1. **None — gate is WAIVED** with structural rationale. Proceed to post-dev SM (`/bmad-create-story (args: "review")`) per IP § QUEUE first item.

#### Short-term Actions (This Milestone — Epic 1 reopen arc, Stories 1.17–1.21)

1. **Story 1.19** — Backfill keel-invariants tests (TS side; resolves AC1 happy-path-only minimality for the TS half).
2. **Story 1.20** — Activate FR14i (`INV-fr14i-ci-workflow-presence` invariant registration + branch-protection alignment) AND extend `INV-deps-version-pinning` to cover `uv.lock` (per SC-9 path-(a)) OR defer to Story 1.21.
3. **Story 1.21** — Audit + sweep (`INV-vitest-pin` + `INV-pytest-pin` registration + `INV-git-hooks-preservation` worktree-mode resolver fix + ATDD-skip retro-sweep into test-debt.md + Python coverage backfill if scoped).

#### Long-term Actions (Backlog — beyond Epic 1 reopen arc)

1. **Epic 13** — Test infrastructure hardening pass (per Story 2.18 iter-351 trace precedent: Epic 13 territory is where formal test runners + persisted fixtures land for both runtimes).
2. **Epic 4** — Per-iteration security verification stories (FR35–FR40) extend `packages/keel-invariants/`; Python test-time deps may need a `[project.optional-dependencies] tests` group split if Story 1.21 audit codifies.

---

## PHASE 2: QUALITY GATE DECISION

**Gate Type:** story
**Decision Mode:** deterministic (with structural-WAIVED override per IP § NOW directive)

### Evidence Summary

#### Test Execution Results

- **Total Tests**: 4 (smokes)
- **Passed**: 4 (100%)
- **Failed**: 0
- **Skipped**: 0
- **Duration**: 0.97s wall time (pytest 9.0.3 reporter)
- **Test Results Source**: iter-env Subtask 9.1 (`UV_PYTHON_PREFERENCE=only-system uv sync --extra dev && uv run pytest`); evidence captured in Story 1.18 § Dev Agent Record § Debug Log References v1.3

#### Coverage Summary (from Phase 1)

- **P0 ACs**: 1/2 FULL (50%) — AC1 four-smoke FULL; AC2 sync-gate PARTIAL (deferred manifest registration + 3× pre-existing drifts carved out)
- **P1 ACs**: 0/1 FULL (0%) — AC3 ci.yml python-job PARTIAL (deferred GH ingestion fallback + SC-6 branch-protection deferral)
- **P2 ACs**: 0/2 FULL (0%) — AC4 + AC5 PARTIAL (doc-drift placeholders)
- **Overall**: 20% (1/5 FULL)
- **Coverage Source**: this report

#### Non-Functional Requirements (NFRs)

- **Security**: NOT_ASSESSED — Story 1.18 does not touch auth/authz/secrets; no NFR-relevant surface.
- **Performance**: NOT_ASSESSED — 4 smoke tests in 0.97s; no perf surface.
- **Reliability**: PASS — pytest 9.0.3 + pytest-asyncio 1.3.0 + ruff 0.15.12 + mypy 1.20.2 + textual 8.2.4 pinned exactly per I7 (deterministic resolution via `--resolution=highest`); CI workflow uses pinned `actions/checkout@v4` + `astral-sh/setup-uv@v6` + exact `version: '0.11.7'` per SC-3/4.
- **Maintainability**: PASS — substrate-extension class per RALPH.md iter-344 (additive edits, no rewrites; PEP 723 coexistence per SC-10 — no migration; `ralph.py:2-6` PEP 723 block byte-identical post-Story-1.18).

#### Flakiness Validation

- **Burn-in Iterations**: not run (4 sub-second smoke tests; flake risk near-zero).
- **Flaky Tests Detected**: 0.
- **Burn-in Source**: not_available (Story 1.21 audit may add burn-in fixtures if scoped).

---

### Decision Criteria Evaluation

#### P0 Criteria

| Criterion             | Threshold | Actual            | Status   |
| --------------------- | --------- | ----------------- | -------- |
| P0 Coverage           | 100%      | 50%               | ❌ FAIL  |
| P0 Test Pass Rate     | 100%      | 100% (4/4 active) | ✅ PASS  |
| Security Issues       | 0         | 0                 | ✅ PASS  |
| Critical NFR Failures | 0         | 0                 | ✅ PASS  |
| Flaky Tests           | 0         | 0                 | ✅ PASS  |

**P0 Evaluation**: Deterministic FAIL on coverage-only criterion; pass-rate + NFR + flake all GREEN.

#### P1 Criteria

| Criterion              | Threshold | Actual | Status   |
| ---------------------- | --------- | ------ | -------- |
| P1 Coverage            | ≥80%      | 0%     | ❌ FAIL  |
| P1 Test Pass Rate      | ≥80%      | n/a    | n/a      |
| Overall Test Pass Rate | ≥80%      | 100%   | ✅ PASS  |
| Overall Coverage       | ≥80%      | 20%    | ❌ FAIL  |

**P1 Evaluation**: Deterministic FAIL on coverage-only criteria; pass-rate GREEN.

#### P2/P3 Criteria

| Criterion         | Actual | Notes                                  |
| ----------------- | ------ | -------------------------------------- |
| P2 Test Pass Rate | n/a    | No P2 tests; coverage-only PARTIAL ACs |
| P3 Test Pass Rate | n/a    | No P3 ACs                              |

---

### GATE DECISION: WAIVED 🔓

### Rationale

Story 1.18 is the FIRST formal Python test runner the substrate has ever shipped. Per FR14n § ATDD-skip ground-(b) sunset (issue #233 amendment, post-Story-1.17), the bootstrap context cannot satisfy the deterministic gate via runtime test coverage alone — the runtime IS what is being authored (for the Python half). Substrate-verification + Story 1.9 sync-gate runtime + iter-env smoke matrix (Subtasks 9.1–9.5) form the verification harness for AC1–AC5.

**Hybrid grounds applied:**

- **(a) substrate-verification covers AC**: every AC ↔ substrate file 1:1 — AC1 ↔ four pytest smokes across `tests/test_ralph.py` + `scripts/tests/test_bootstrap_bmad_agents.py` + `packages/devbox/tui/tests/test_theme.py`; AC2 ↔ `pyproject.toml` + `uv.lock` deterministic resolution + `INV-deps-version-pinning` sync-gate posture (with SC-9 carve-out for pre-existing drifts); AC3 ↔ `.github/workflows/ci.yml` python-job extension; AC4 ↔ CLAUDE.md row append; AC5 ↔ AGENTS.md byte-pinned sentence rewrite. Substrate landed cleanly per dev-story v1.3 (iter-366); iter-env Subtask 9.1 confirms `uv run pytest` exit 0 with `4 passed in 0.97s`.
- **(c) variant-(ii) split for AC3**: substrate half FULL (`.github/workflows/ci.yml` python-job appended with canonical shape; node-job byte-identical per SC-5); behavioural half deferred to GH ingestion-side validation post-PR-base-flip-to-main per AC3 fallback clause (`actionlint` unavailable in iter env). Same pattern Story 1.17 iter-360 + Story 2.18 iter-351 applied.
- **(c) variant-(iii) for SC-9 deferral**: AC2's "extended in Story 1.20 to cover Python deps if applicable" clause is spec-declared CR-substitution — the manifest-extension decision is two-path-deferred to Story 1.20 OR 1.21 audit per SC-9. Same shape as Story 1.17 iter-360's SC-7 forward-compat deferral.

**Cumulative trace-WAIVED counter:** 30th cumulative trace-WAIVED across substrate stories (1.7→1.16, 2.1→2.18, 1.17, now 1.18); 2nd Epic-1-reopen-arc (after Story 1.17 iter-360); 3rd course-correction-origin trace-WAIVED (after Story 2.18 iter-351 + Story 1.17 iter-360). Pattern: course-correction-origin stories targeting bootstrap surfaces inherit the substrate-extension trace-WAIVED posture from the broader Epic-1 / Epic-2 main pass; cross-runtime extension (Python sibling of TS-side Story 1.17) preserves the class.

**Why not FAIL?** The deterministic FAIL signal here is structural artefact — there is no Python test corpus to enforce coverage against because Story 1.18 is that corpus's first member. Defaulting to FAIL would block all subsequent Python-test-foundation stories from landing.

**Why not PASS?** Coverage is genuinely 20% in absolute terms; the four smoke tests do not adversarially exercise `ralph.py` / `bootstrap-bmad-agents.py` / `theme.py` logic. Story 1.21 audit (or future Python coverage backfill story) is the explicit follow-up that produces the corpus PASS gates assume.

### Residual Risks (For WAIVED)

1. **AC2's Python-dep manifest registration deferred** to Story 1.20 / 1.21 (per SC-8 + SC-9).
   - **Priority**: P1
   - **Probability**: Low — exact-pin literals are in `pyproject.toml` `[project.optional-dependencies] dev` + `uv.lock`; manual drift would require co-edits to both files.
   - **Impact**: Medium — if drift slips through review, pytest/ruff/mypy versions could shift across operator workstations.
   - **Mitigation**: `uv.lock` + `uv sync --frozen` enforce deterministic resolution; sync-gate re-runs at Story 1.20 / 1.21 will catch any drift retroactively.
   - **Remediation**: Story 1.20 (path-a per SC-9: extend `INV-deps-version-pinning` to `uv.lock` content-hash) OR Story 1.21 audit (path-b: register dedicated `INV-pytest-pin` / `INV-uv-lock-determinism`).
2. **AC3 `actionlint` deferred** to GH ingestion (post-PR-base-flip-to-main).
   - **Priority**: P1
   - **Probability**: Low — workflow YAML follows official `astral-sh/setup-uv@v6` examples; no custom syntax; the `python` job is structurally identical to Story 1.17's `node` job at the YAML shape level.
   - **Impact**: Medium — a malformed workflow would fail GH ingestion the moment the PR retargets `main` (catastrophic case still surfaces, just later).
   - **Mitigation**: workflow fires only on PRs targeting `main`; the PR is Draft against `feat/epic-2-packaged-devbox` (no fire path until Epic-1 reopen-arc PR ladder reaches main).
   - **Remediation**: Story 1.20 activates FR14i + `INV-fr14i-ci-workflow-presence` registers the file; first ingestion validation is the Epic 1 reopen-arc PR's eventual base-flip to `main`.
3. **Pre-existing 3× `INV-git-hooks-preservation` drifts** persist on `feat/epic-2-packaged-devbox` head (RALPH.md iter-358 gotcha — sync-gate resolves `<worktree>/.git/hooks` which is empty in worktree mode; actual hooks live at `core.hooksPath = /workspace/ralph-bmad/.git/hooks`).
   - **Priority**: P2
   - **Probability**: certain (currently present in iter-env; not introduced by Story 1.18; identical posture to Story 1.17 iter-360 trace).
   - **Impact**: Low — drifts are advisory (sync-gate is not in `.pre-commit-config.yaml`); fires only on manual `pnpm keel-invariants:check`.
   - **Mitigation**: out of scope per gotcha guidance ("Address before Story 1.20 close-out") + SC-9 carve-out.
   - **Remediation**: Story 1.20 / 1.21 fix-task — patch the resolver to honour `core.hooksPath` when set.
4. **Substrate-probe gap class identified at iter-366 dev-story** (RALPH.md iter-366 lessons-learned): SM-validate substrate probes can miss top-level imports of non-stdlib packages inside otherwise-guarded modules (`ralph.py:66` `from textual.app import` evaluated unconditionally despite `if __name__ == "__main__":` guard at `:2015-2016`).
   - **Priority**: P2
   - **Probability**: Medium for future Python-substrate stories that test/load substrate modules (Story 1.19 keel-invariants TS-not-Python; Story 1.20 / Epic 4 may touch Python).
   - **Impact**: Low — caught at dev-story time + corrected via +1 dev-dep deviation (textual==8.2.4 added; SC-3 policy preserved).
   - **Mitigation**: RALPH.md § Lessons learned codifies the carry-rule (Grep `^from\|^import` + verify every non-stdlib top-level import is declared OR lazy-imported).
   - **Remediation**: future SM-validate substrate probes apply the carry-rule explicitly.

**Overall Residual Risk:** LOW — all deferred signals are bounded + tracked + scheduled.

### Waiver Details

**Original Decision**: ❌ FAIL (P0 50%, P1 0%, overall 20% under deterministic rule).

**Reason for Failure**: Structural — no pre-existing Python pytest corpus to enforce coverage; Story 1.18 IS that corpus's first member.

**Waiver Information**:

- **Waiver Reason**: bootstrap-Python-test-runner story; substrate-verification + sync-gate + iter-env smoke matrix substitute for runtime coverage (FR14n § ATDD-skip ground-(a) + ground-(c) variant-(ii)+(iii)).
- **Waiver Approver**: Tthew (substrate operator) via `/bmad-correct-course` issue #233 Sprint Change Proposal § 4.6 (same SCP that authorised Story 1.17 iter-360 trace-WAIVED).
- **Approval Date**: 2026-04-25 (iter-1 of Epic 1 reopen arc; SCP authored prior to Story 1.17 + 1.18 drafts).
- **Waiver Expiry**: Story 1.21 close-out (Epic 1 reopen-arc terminus).
- **Cumulative Precedent**: 30th cumulative trace-WAIVED across 29 prior stories (1.7→2.18 + 1.17); 2nd Epic-1-reopen-arc; 3rd course-correction-origin.

**Monitoring Plan**:

- Epic 1 reopen-arc tracks Story 1.21 audit progress via sprint-status.yaml.
- Story 1.21 audit runs `/bmad-testarch-test-review` against all five 1.18-class artefacts to confirm waiver discharge alongside Story 1.17.

**Remediation Plan**:

- **Fix Target**: Story 1.20 (FR14i activation) + Story 1.21 (audit / `INV-pytest-pin` + Python coverage backfill if scoped).
- **Due Date**: Epic 1 reopen-arc terminus (estimated ~iter-380; bandwidth-sensitive).
- **Owner**: Ralph build-mode autonomy.
- **Verification**: re-run `/bmad-testarch-trace (args: "yolo")` per story against the matured corpus; expected gate transitions FAIL→CONCERNS→PASS as backfill lands.

**Business Justification**: Without a WAIVED gate here, Story 1.18 close-out blocks Story 1.19 keel-invariants backfill (which requires the test runners to exist) AND Story 1.20 FR14i activation (which requires the workflow to ship its full job set). Sequenced WAIVED gates discharge the structural debt under Story 1.21's audit sweep (per SCP § 4.6 D4).

---

### Gate Recommendations

1. **Proceed to post-dev SM** (`/bmad-create-story (args: "review")`) per IP § QUEUE first item. Forecast 1–4 PATCH (RALPH.md iter-352 narrow-substrate-extension empirical envelope; iter-361 confirmed Story 1.17 post-dev SM-verify yielded 4 PATCH at gate within band).
2. **Land Story 1.18 close-out** (sprint-status flip + Change Log v1.5+ depending on CR class).
3. **Carry forward to Story 1.19** (keel-invariants test backfill — TS side) per FR14n state matrix; draft via `/bmad-create-story` from sprint-status next-backlog row after Story 1.18 marked done.

---

### Next Steps

**Immediate Actions**:

1. Record this gate WAIVED in Story 1.18 § Change Log v1.4.
2. Update IP § Story State `in-dev → traced`; advance NOW to post-dev SM.
3. Update RALPH.md § Signposts with iter-367 trace-WAIVED entry (30th cumulative + 2nd Epic-1-reopen-arc + 3rd course-correction-origin).

**Follow-up Actions**:

1. Story 1.20 / 1.21 register Python-dep manifest coverage (SC-9 path decision) + resolve `INV-git-hooks-preservation` worktree-mode resolver bug (per Residual Risk #3).
2. Epic 1 reopen-arc PR base-flip to `main` triggers GH ingestion-side workflow YAML validation (per Residual Risk #2).
3. Future SM-validate substrate probes apply the iter-366 substrate-probe-gap carry-rule (per Residual Risk #4).

---

## Integrated YAML Snippet (CI/CD)

```yaml
traceability_and_gate:
  traceability:
    story_id: '1.18'
    date: '2026-04-26'
    coverage:
      overall: 20%
      p0: 50%
      p1: 0%
      p2: 0%
      p3: n/a
    gaps:
      critical: 0
      high: 1
      medium: 2
      low: 0
    quality:
      passing_tests: 4
      total_tests: 4
      blocker_issues: 0
      warning_issues: 0
    recommendations:
      - 'Story 1.20 activates FR14i (INV-fr14i-ci-workflow-presence) covering both node + python jobs'
      - 'Story 1.20 / 1.21 extend INV-deps-version-pinning to cover uv.lock (SC-9 path decision)'
      - 'Story 1.21 audit registers INV-pytest-pin + fixes INV-git-hooks-preservation worktree resolver'

  gate_decision:
    decision: 'WAIVED'
    gate_type: 'story'
    decision_mode: 'deterministic'
    criteria:
      p0_coverage: 50%
      p0_pass_rate: 100%
      p1_coverage: 0%
      p1_pass_rate: 'n/a'
      overall_pass_rate: 100%
      overall_coverage: 20%
      security_issues: 0
      critical_nfrs_fail: 0
      flaky_tests: 0
    thresholds:
      min_p0_coverage: 100
      min_p0_pass_rate: 100
      min_p1_coverage: 80
      min_p1_pass_rate: 80
      min_overall_pass_rate: 80
      min_coverage: 80
    evidence:
      test_results: 'iter-env Subtask 9.1; pytest 9.0.3 reporter `4 passed in 0.97s`'
      traceability: '_bmad-output/test-artifacts/traceability/1-18-bootstrap-python-test-runner-pytest-under-uv.md'
      nfr_assessment: 'inline (Reliability + Maintainability PASS)'
      code_coverage: 'not enabled at this scope (Story 1.21 audit territory)'
    next_steps: 'Run /bmad-create-story (args: "review") for post-dev SM verification'
    waiver:
      reason: 'bootstrap-Python-test-runner; substrate-verification + sync-gate + iter-env smoke substitute for runtime coverage (FR14n § ground-(a) + (c)-(ii)+(iii))'
      approver: 'Tthew (substrate operator) via /bmad-correct-course issue #233 SCP § 4.6'
      expiry: 'Story 1.21 close-out'
      remediation_due: 'Epic 1 reopen-arc terminus'
```

---

## Related Artifacts

- **Story File:** `_bmad-output/implementation-artifacts/1-18-bootstrap-python-test-runner-pytest-under-uv.md`
- **Test Design:** none authored; substrate-extension class per RALPH.md iter-344 (third confirmation: Story 2.18 iter-350 → Story 1.17 iter-359 → Story 1.18 iter-366)
- **Tech Spec:** `_bmad-output/planning-artifacts/architecture.md § M0 substrate developer-productivity floor` (lines 198–240; Python runtime substrate sub-section at 210–215)
- **Test Results:** `uv run pytest` iter-env GREEN per dev-story v1.3 § Debug Log References (`4 passed in 0.97s`)
- **NFR Assessment:** inline (Reliability + Maintainability PASS)
- **Test Files:** `tests/test_ralph.py` (2 unit tests) + `scripts/tests/test_bootstrap_bmad_agents.py` (1 unit test) + `packages/devbox/tui/tests/test_theme.py` (1 unit test)
- **Sibling Trace:** `_bmad-output/test-artifacts/traceability/1-17-bootstrap-typescript-test-runner-vitest-minimal-ci.md` (Story 1.17 iter-360 trace-WAIVED; structural mirror)

---

## Sign-Off

**Phase 1 — Traceability Assessment:**

- Overall Coverage: 20% (1/5 FULL; 4/5 PARTIAL)
- P0 Coverage: 50% (AC1 FULL — 4 smokes; AC2 PARTIAL — sync-gate green for new drift, manifest registration deferred per SC-8 + SC-9)
- P1 Coverage: 0% (AC3 PARTIAL — substrate present, actionlint deferred to GH ingestion)
- Critical Gaps: 0 — none structurally blocking
- High Priority Gaps: 1 — AC3 actionlint behavioural half (deferred)

**Phase 2 — Gate Decision:**

- **Decision**: WAIVED 🔓
- **P0 Evaluation**: structurally NOT_MET (50% coverage); waived per bootstrap rationale
- **P1 Evaluation**: structurally NOT_MET (0% coverage); waived per bootstrap rationale
- **Overall Status**: WAIVED 🔓 — 30th cumulative + 2nd Epic-1-reopen-arc + 3rd course-correction-origin

**Generated:** 2026-04-26 (iter-367)
**Workflow:** testarch-trace v4.0 (Enhanced with Gate Decision)

---

<!-- Powered by BMAD-CORE™ -->
