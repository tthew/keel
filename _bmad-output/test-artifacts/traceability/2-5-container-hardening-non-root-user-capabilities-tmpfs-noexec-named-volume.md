---
stepsCompleted:
  [
    'step-01-load-context',
    'step-02-discover-tests',
    'step-03-map-criteria',
    'step-04-analyze-gaps',
    'step-05-gate-decision',
  ]
lastStep: 'step-05-gate-decision'
lastSaved: '2026-04-22'
workflowType: 'testarch-trace'
inputDocuments:
  [
    '_bmad-output/implementation-artifacts/2-5-container-hardening-non-root-user-capabilities-tmpfs-noexec-named-volume.md',
    '_bmad-output/planning-artifacts/epics.md',
    '_bmad-output/planning-artifacts/prd.md',
    '_bmad-output/planning-artifacts/architecture.md',
    'INVARIANTS.md',
    'AGENTS.md',
    'CLAUDE.md',
    'RALPH.md',
    'docs/invariants/devbox-hardening.md',
    'docs/invariants/devbox-dind.md',
    'docs/invariants/devbox-egress.md',
    'packages/devbox/Dockerfile',
    'packages/devbox/docker-compose.yml',
    'packages/devbox/entrypoint.sh',
    'packages/devbox/README.md',
    'packages/keel-invariants/src/invariants.manifest.ts',
    '_bmad-output/test-artifacts/traceability/2-4-gate-decision.json',
  ]
coverageBasis: 'acceptance_criteria'
oracleConfidence: 'high'
oracleResolutionMode: 'formal_requirements'
oracleSources:
  [
    '_bmad-output/implementation-artifacts/2-5-container-hardening-non-root-user-capabilities-tmpfs-noexec-named-volume.md#Acceptance Criteria',
  ]
externalPointerStatus: 'not_used'
tempCoverageMatrixPath: '_bmad-output/test-artifacts/traceability/2-5-coverage-matrix.json'
---

# Traceability Matrix & Gate Decision — Story 2.5 container hardening (non-root user + capabilities + tmpfs noexec + named volume)

**Target:** Story 2.5 — Docker-layer security posture combining non-root `dev` user (UID/GID 1000 via `useradd` + `USER dev` in Dockerfile) + capability bounding set (`cap_drop: [ALL]` + `cap_add: [NET_ADMIN, NET_RAW, NET_BIND_SERVICE]` per SC-4) + `security_opt: [no-new-privileges:true]` + tmpfs `/tmp` + `/var/tmp` with `exec=false,suid=false` (noexec + nosuid) parameterised via Story 2.2 knobs + named Docker volume `keel_home_dev` for `/home/dev` (non-toggle, registered as `INV-devbox-homedev-named-volume`). Five ACs delivered iter-187 via `/bmad-dev-story` single-iteration landing (12 Tasks / 19 SCs all green; 1 new file + 7 modified files exactly matching the v0.2 SM-reviewed forecast). Story State `in-dev` at iter-188 trace entry — iter-187 `/bmad-dev-story` completed AC 1–AC 5 end-to-end at substrate level + `docker compose config` parse-smoke PASSED + sync-gate GREEN with 21 manifest entries valid + `pnpm check-all` GREEN + typecheck / lint (16 successful) / format:check all GREEN. Live container smokes (Task 11 per-AC: AC 1 Subtask 11.1-11.2-11.4 user-verification; AC 2 Subtask 11.5-11.7 capability-exercise; AC 3 Subtask 11.3 findmnt; AC 4 Subtask 11.8-11.9 volume-inspection; AC 5 Smoke A noexec + Smoke B no-new-privileges) are **backend-A-only operator-workstation-deferred** per SC-13 + Story 2.4 SC-17 precedent (DinD-B iteration env cannot safely exercise `docker exec` sequences against cap-dropped containers without risk of poisoning host docker state under cap_drop:[ALL]; M4-Pro native Docker Desktop is the authoritative AC 1-5 verification environment). Canonical `docker exec` recipes pinned in `docs/invariants/devbox-hardening.md § Verification` + `packages/devbox/README.md § Hardening § Verification` for the operator close-out pass.

**Date:** 2026-04-22
**Evaluator:** Tthew / TEA Agent
**Coverage Oracle:** acceptance_criteria (formal requirements — Story 2.5 § Acceptance Criteria lines 13–40)
**Oracle Confidence:** high
**Oracle Sources:** `_bmad-output/implementation-artifacts/2-5-container-hardening-non-root-user-capabilities-tmpfs-noexec-named-volume.md` (AC 1–AC 5)

---

Note: This workflow does not generate tests. Story 2.5 is an **architecture-security / container-hardening class substrate** story (FIFTH of Epic 2 + FIRST of its class: Docker-layer security posture — non-root user + capability bounding set + tmpfs noexec/nosuid + no-new-privileges + named-volume non-toggle invariant — operating at the Docker daemon / kernel-security subsystem boundary, orthogonal to Story 2.3's daemon+kernel-rule infrastructure-security class AND Story 2.4's user-facing-CLI class) whose § Dev-agent guardrails + v1.0 Change Log iter-186 row (ATDD-skip per Story 2.4 iter-173 no-Change-Log-row precedent) + v1.0 iter-187 dev-story landing Change Log row explicitly declare:

> _"FIFTEENTH cumulative Epic ATDD-skip precedent (Stories 1.7 iter-14 + 1.8 iter-29 + 1.9 iter-36 + 1.10 iter-43 + 1.11 iter-50 + 1.12 iter-57 + 1.13 iter-64 + 1.14 iter-71 + 1.15 iter-83 + 1.16 iter-90 + 2.1 iter-98 + 2.2 iter-147 + 2.3 iter-157 + 2.4 iter-173 → 2.5 iter-186) — **fifth Epic 2 ATDD-skip** + **first 'architecture-security / container-hardening class' ATDD-skip** (Story 2.1 = infrastructure-smoke; Story 2.2 = hybrid infrastructure-smoke + configuration-surface; Story 2.3 = infrastructure-security: daemon + kernel-rule + atomic-reload + log-tailer; Story 2.4 = user-facing-CLI: bash CLI with regex validation + mutation-lock + atomic-replace + subcommand dispatcher; Story 2.5 = architecture-security: Docker-layer security posture at daemon / kernel-security subsystem boundary). `/bmad-testarch-atdd` skill NOT invoked — preflight would HALT at Step 1.2 (zero-test-runner substrate: no vitest.config.\*/jest.config.\*/playwright.config.\*/cypress.config.\*/pyproject.toml/go.mod/Gemfile/Cargo.toml/csproj anywhere in tree; TEA test_framework: auto autodetects nothing; Epic 13 is the formal test-framework landing per PRD RS6). Rationale — ground (c) hybrid variant-(ii)+(iii): (ii) downstream integration-gate coverage (Story 2.6 host-side pnpm wrapper exercises AC 1 user-context shell-exec against running container; Story 2.8 Claude Code OAuth token persistence validates AC 4 /home/dev/.claude/ subpath first-boot populate + cross-restart persistence; Story 2.9 gh CLI OAuth validates /home/dev/.config/gh/ subpath; Story 2.17 runtime compose-shape check parses docker-compose.yml and rejects bind-mount drift for /home/dev per SC-10 deferral; Epic 13 test-runner landing owns regression coverage); (iii) spec-declared adversarial coverage substitution — § Dev-agent guardrails + 19 pinned scope-clarifications (SC-1 through SC-19) + forthcoming /bmad-code-review (args: \"2\") adversarial envelope (Blind Hunter + Edge Case Hunter + Acceptance Auditor fan-out against cumulative Story 2.5 substrate diff: ~8 files) substitute for red-phase scaffolds."_

Automated per-AC test coverage is intentionally deferred; see § Rationale below. This gate decision mirrors the FR14n ATDD-skip clause already applied at iter-186, per the hybrid ground-(a)-(b)-(c)-(d) variant-(ii)+(iii) rationale (substrate-verification-covers-AC + iteration-env-parse-smoke-augment + no-runner + Epic 13 test-runner landing + spec-declared-CR-substitution + upstream-provenance-precedent) pinned in `.ralph/@plan.md § Context` and RALPH.md Signposts 2026-04-22. **FIFTEENTH cumulative trace-WAIVED precedent** — fifth Epic 2 trace-WAIVED and first **architecture-security / container-hardening class** trace-WAIVED. Under the ATDD-skip-trace-WAIVED co-application rule, this is the **SIXTEENTH pairing** overall (10 Epic-1 pairings + 2.1 + 2.2 + 2.3 + 2.4 + 2.5).

## PHASE 1: REQUIREMENTS TRACEABILITY

### Coverage Summary

| Priority  | Total Criteria | FULL Coverage | Coverage % | Status  |
| --------- | -------------- | ------------- | ---------- | ------- |
| P0        | 0              | 0             | 100%       | ✅ n/a  |
| P1        | 0              | 0             | 100%       | ✅ n/a  |
| P2        | 5              | 0             | 0%         | ❌ FAIL |
| P3        | 0              | 0             | 100%       | ✅ n/a  |
| **Total** | **5**          | **0**         | **0%**     | **❌**  |

**Legend:**

- ✅ PASS — Coverage meets quality gate threshold
- ⚠️ WARN — Coverage below threshold but not critical
- ❌ FAIL — Coverage below minimum threshold (blocker)

All five ACs are **architecture-security / container-hardening substrate** assertions over the Story 2.5 deliverables (AC 1: Dockerfile `useradd dev` UID/GID 1000 + `USER dev` directive before ENTRYPOINT; AC 2: compose `cap_drop: [ALL]` + `cap_add: [NET_ADMIN, NET_RAW, NET_BIND_SERVICE]` + `security_opt: [no-new-privileges:true]`; AC 3: compose tmpfs `/tmp` + `/var/tmp` with `exec=false,suid=false` parameterised via Story 2.2 knobs; AC 4: compose named volume `keel_home_dev` for `/home/dev` + non-toggle-able invariant `INV-devbox-homedev-named-volume` registered; AC 5: runtime kernel enforcement — `/tmp` exec fails with `noexec`; setuid elevation blocked by `nosuid` + `no-new-privileges`). Priority `P2` reflects secondary-workflow classification per `test-priorities-matrix.md` consistent with Stories 2.1 + 2.2 + 2.3 + 2.4 precedent (no P0 auth/payment/data-loss at substrate; no P1 primary user journey — end-user operator interacts with the hardened container posture transparently once Story 2.6 host-side `pnpm devbox:*` wrapper ships and the per-story subscribers wire in at /home/dev subpath level). Downstream test-runner landing may retro-classify AC 2 + AC 5 as P1 under runtime-harm taxonomy (capability-grant mistake could regress privilege-drop posture; noexec bypass would violate defence-in-depth); Story 2.5 ships P2-uniform matching Stories 2.1-2.4 precedent.

---

### Detailed Mapping

#### AC-1: Non-root `dev` user created with stable UID/GID; Dockerfile `USER dev` directive switches before entrypoint runs (P2)

- **Coverage:** NONE ❌ (deferred to Story 2.5-test-runner-landing + M4-Pro operator-workstation live smokes + iter-CR adversarial backstop)
- **Tests:** 0 automated tests
- **Substrate verification (non-gate-eligible evidence — STRONG; Task 1 authoring + iter-187 live file + iteration-env `docker compose config` parse-smoke probe AC 1 at Dockerfile-layer + Compose-schema level):**
  - **`packages/devbox/Dockerfile` (iter-187)**: `RUN groupadd --gid 1000 dev && useradd --uid 1000 --gid 1000 --shell /bin/bash --create-home dev && chown -R dev:dev /home/dev` chained into a single RUN step per SC-1 + SC-3. Stable UID/GID 1000 is load-bearing: Docker auto-init-from-image populates the `keel_home_dev` named volume with correct ownership on first boot per [docs.docker.com/storage/volumes](https://docs.docker.com/storage/volumes/#populate-a-volume-using-a-container) semantics. `--create-home` generates `/home/dev` pre-volume-mount so the directory exists in the image layer for Docker's empty-volume-population behaviour. Shell `/bin/bash` (not `/bin/sh`) — entrypoint.sh uses `#!/usr/bin/env bash`; operator-shell consistency.
  - **`packages/devbox/Dockerfile` `USER dev` placement (iter-187)**: USER directive on its own line AFTER `COPY entrypoint.sh` + `RUN chmod +x /usr/local/bin/entrypoint.sh` and BEFORE `WORKDIR /workspace` per SC-2. The pre-USER `RUN mkdir -p /workspace /home/dev/.claude ...` block runs under root so `/workspace` at filesystem root can be created; USER switches to dev after chmod so ENTRYPOINT resolves as dev at container-start time.
  - **`packages/devbox/Dockerfile` `RUN setcap +eip` (iter-187)**: `RUN setcap cap_net_admin,cap_net_raw,cap_net_bind_service+eip /usr/sbin/dnsmasq && setcap cap_net_admin+eip /usr/sbin/nft` after the Playwright deps block per SC-15 defence-in-depth portability fallback. Pin-rationale comment above the setcap block cites SC-15 ambient-cap automation (Docker ≥19.03 + PR_SET_NO_NEW_PRIVS interaction): under the reference runtime ambient-cap propagation via `prctl(PR_CAP_AMBIENT, PR_CAP_AMBIENT_RAISE, …)` is the PRIMARY mechanism; setcap is harmless-no-op under NNP (kernel masks file-cap F(effective) on exec per capabilities(7)) but provides a working fallback for older Docker / Podman-compat runtimes.
  - **`packages/devbox/Dockerfile` pin-rationale comments (iter-187)**: useradd block comment cites SC-1 UID/GID-1000 convention + `/bin/bash` over `/bin/sh` rationale + `--create-home` for Docker auto-init-from-image + named-volume first-boot-populate semantics. setcap block comment cites SC-15 ambient-cap vs NNP file-cap masking mechanism + belt-and-braces fallback rationale.
  - **`packages/devbox/entrypoint.sh:26` (iter-187)**: `WORKSPACE_OWNER="${KEEL_DEVBOX_WORKSPACE_OWNER:-dev:dev}"` (was `:-root:root` pre-Story-2.5) per SC-7 declarative operator intent. Existing CR-AI-5 input-shape validation regex at `:51` (`^[A-Za-z_][A-Za-z0-9_.+-]*:[A-Za-z_][A-Za-z0-9_.+-]*$`) already accepts `dev:dev` — no regex change needed. Multi-line completed-substrate reference block replaces the `TODO(Story 2.5)` marker at `:25-26` pointing to `docs/invariants/devbox-hardening.md § Capability bounding set`; file-header comment refreshed to reference Story 2.5 landing.
  - **`packages/devbox/entrypoint.sh` (iter-187) runtime chown posture (SC-5)**: existing `chown` calls at `:81` (workspace + /home/dev/.claude + /home/dev/.config/gh) retained verbatim with `2>/dev/null || true` stderr-capture + continue semantics. Under `cap_drop: [ALL]` without CAP_CHOWN these calls fail harmlessly; image-build-time chown (SC-3) for `/home/dev/*` is the load-bearing mechanism on first-boot + host UID passthrough handles `/workspace` ownership on UID-1000-aligned hosts. Operators with non-matching host UIDs get best-effort stderr warnings + container still starts.
  - **Iteration-env `docker compose config` parse-smoke (iter-187)**: PASSED — compose file parses cleanly with every hardening stanza present; output includes `User: ""` (inherits Dockerfile USER dev), cap_drop/cap_add lists, security_opt, tmpfs long-syntax entries, service volumes with keel_home_dev → /home/dev, top-level volumes.keel_home_dev. Schema validity proves the Dockerfile USER + compose overrides do not conflict. This is a SUBSTRATE-LEVEL assertion; live kernel-enforcement verification requires a running container.
  - **Live-smoke AC 1 runtime verification (Subtask 11.1 `docker exec keel-devbox id` → `uid=1000(dev) gid=1000(dev) groups=1000(dev)`; Subtask 11.2 `docker exec keel-devbox stat -c '%u:%g' /home/dev` → `1000:1000`; Subtask 11.4 `docker inspect --format '{{.Config.User}}' keel-devbox` → `dev`)** deferred to M4-Pro operator workstation per SC-13 backend-B live-smoke constraint. Canonical recipes pinned in `docs/invariants/devbox-hardening.md § Verification` + `packages/devbox/README.md § Hardening § Verification`.
  - **Adversarial AC-1 coverage delegated to iter-CR** per § Dev-agent guardrails hybrid ground-(c) variant-(iii) spec-declared-CR-substitution: Blind Hunter examines Dockerfile layer ordering (package install → useradd → chown -R → setcap → COPY entrypoint → chmod +x → USER dev → WORKDIR → ENTRYPOINT) + verifies USER dev does not mask a build-time root-required operation; Edge Case Hunter probes `--create-home` interaction with empty named-volume first-boot population + UID-collision edge (host UID ≠ 1000) + Docker tmpfs auto-mount ownership preservation + Podman-compat ambient-cap absence; Acceptance Auditor verifies AC 1 verbatim match — `USER dev` on its own line before ENTRYPOINT, UID/GID 1000, stable IDs matching Linux first-human-user convention.

---

#### AC-2: `docker-compose.yml` declares `cap_drop: [ALL]` + `cap_add: [NET_ADMIN, NET_RAW, NET_BIND_SERVICE]` + `security_opt: [no-new-privileges:true]` (P2)

- **Coverage:** NONE ❌ (deferred to Story 2.5-test-runner-landing + M4-Pro operator-workstation live smokes + iter-CR adversarial backstop)
- **Tests:** 0 automated tests
- **Substrate verification (non-gate-eligible evidence — STRONG; Task 2 authoring + iter-187 live file + iteration-env `docker compose config` parse-smoke probe AC 2 at Compose-schema level):**
  - **`packages/devbox/docker-compose.yml` capability stanzas (iter-187)**: `cap_drop: [ALL]` followed by `cap_add: [NET_ADMIN, NET_RAW, NET_BIND_SERVICE]` per SC-4 three-cap list. Order — cap_drop BEFORE cap_add — enforces semantic clarity (drop-everything-then-grant-three-narrow-caps). Inline comment documents SC-4 rationale:
    - `NET_ADMIN` — nftables netlink rule-load (Story 2.3 reload-egress.sh invokes `nft -f` + `kill -HUP $(cat /run/dnsmasq.pid)`).
    - `NET_RAW` — raw-socket probes dnsmasq may issue during health checks (Story 2.3 dnsmasq posture).
    - `NET_BIND_SERVICE` — bind to <1024 (dnsmasq port 53 under cap_drop:[ALL]). Without this third cap the kernel rejects :<1024 bind from ANY process — cap_drop:[ALL] strips NET_BIND_SERVICE from the bounding set per capabilities(7). The AC 2 parenthetical "(required by nftables in Story 2.3)" is the RATIONALE for the first two caps, not an exclusivity clause — SC-4 reconciles the PRD/architecture text + epic AC with the runtime port-bind requirement.
  - **`packages/devbox/docker-compose.yml` security_opt (iter-187)**: `security_opt: [no-new-privileges:true]` added immediately after `cap_add` block. Sets `PR_SET_NO_NEW_PRIVS=1` on PID 1 — kernel masks file-cap `F(effective)` on exec + disables setuid-bit elevation (capabilities(7) + no_new_privs semantics).
  - **`packages/devbox/docker-compose.yml` header checklist (iter-187)**: refreshed to `LANDED iter-187` with each hardening stanza marked green — cap_drop, cap_add (three caps), security_opt, tmpfs /tmp, tmpfs /var/tmp, service volumes → keel_home_dev, top-level volumes.keel_home_dev. Pre-existing Story 2.11/2.12/2.13/2.8-2.9 TODO markers preserved per SC-19 scope isolation.
  - **Iteration-env `docker compose config` parse-smoke (iter-187)**: PASSED — cap_drop/cap_add/security_opt stanzas parse cleanly. Output includes literal `cap_drop: ["ALL"]` + three-cap `cap_add: [NET_ADMIN, NET_RAW, NET_BIND_SERVICE]` + `security_opt: [no-new-privileges:true]`. Schema validity proves Docker Compose accepts the declaration; kernel enforcement of the bounding set + NNP is the operator-workstation live-smoke concern (Subtask 11.5-11.7).
  - **SC-15 ambient-cap vs setcap mechanism (iter-187)**: pinned in `docs/invariants/devbox-hardening.md § Capability bounding set` — under USER dev + cap_drop:[ALL] + cap_add:[three caps] + no-new-privileges:true, Docker ≥19.03's ambient-cap automation (`prctl(PR_CAP_AMBIENT, PR_CAP_AMBIENT_RAISE, …)`) is the PRIMARY privilege-propagation mechanism for PID 1 → entrypoint → start-egress → dnsmasq/nft chain. Ambient caps are preserved across `exec()` regardless of NNP (this is ambient's kernel-design purpose per capabilities(7)). File capabilities via `setcap +eip` are kernel-disabled under PR_SET_NO_NEW_PRIVS=1 — setcap is the defence-in-depth PORTABILITY fallback for older Docker / Podman-compat runtimes without automated ambient-cap handling.
  - **Live-smoke AC 2 runtime verification (Subtask 11.5 `docker inspect --format '{{.HostConfig.CapDrop}} {{.HostConfig.CapAdd}}' keel-devbox` → `[ALL] [NET_ADMIN NET_RAW NET_BIND_SERVICE]`; Subtask 11.6 `docker inspect --format '{{.HostConfig.SecurityOpt}}' keel-devbox` → `[no-new-privileges]`; Subtask 11.7 capability-exercise smoke `docker exec keel-devbox sh -c 'ss -tlnp | grep :53 && nft list table inet keel_egress'` → dnsmasq bound on :53 + nftables rules loaded proving ambient caps raised correctly)** deferred to M4-Pro operator workstation per SC-13 + Story 2.4 SC-17 precedent.
  - **Adversarial AC-2 coverage delegated to iter-CR**: Blind Hunter examines cap_drop-before-cap_add ordering + three-cap set for over/under-grant (NET_BIND_SERVICE proves required by port-bind semantics per SC-4; CAP_CHOWN deliberately excluded per SC-5); Edge Case Hunter probes ambient-cap-missing fallback path (older Docker) + setcap fallback correctness + capability bounding set interaction with non-root UID + Podman-compat automated-ambient-cap absence; Acceptance Auditor verifies AC 2 verbatim match + SC-4 reconciliation (the parenthetical "required by nftables in Story 2.3" is rationale not exclusivity).

---

#### AC-3: `/tmp` and `/var/tmp` are tmpfs-mounted with `noexec,nosuid` options; sizes parameterised via Story 2.2-published knobs (P2)

- **Coverage:** NONE ❌ (deferred to Story 2.5-test-runner-landing + M4-Pro operator-workstation live smokes + iter-CR adversarial backstop)
- **Tests:** 0 automated tests
- **Substrate verification (non-gate-eligible evidence — STRONG; Task 2 + Task 4 authoring + iter-187 live file + iteration-env parse-smoke probe AC 3 at Compose-schema level):**
  - **`packages/devbox/docker-compose.yml` tmpfs long-syntax (iter-187)**: service-level `tmpfs:` block per SC-8 with two entries:
    - `/tmp:exec=false,suid=false,size=${KEEL_DEVBOX_TMPFS_TMP_MB:-2048}m`
    - `/var/tmp:exec=false,suid=false,size=${KEEL_DEVBOX_TMPFS_VARTMP_MB:-1024}m`
    Docker Compose's `exec=false` = kernel `noexec`; `suid=false` = kernel `nosuid`. Sizes in MB (Compose parses `m`/`g` case-insensitively).
  - **`packages/devbox/.envrc.example:29-31` (Story 2.2 iter-151 — UNTOUCHED by Story 2.5 per SC-18 + SC-19 scope isolation)**: Story 2.2 published `KEEL_DEVBOX_TMPFS_TMP_MB=2048` + `KEEL_DEVBOX_TMPFS_VARTMP_MB=1024` + `KEEL_DEVBOX_TMPFS_LOGS_MB=500` as operator-configurable knobs. Story 2.5 CONSUMES the first two; `KEEL_DEVBOX_TMPFS_LOGS_MB` remains **inert-at-Story-2.5** per SC-8 rationale (`/var/log` NOT tmpfs-mounted — dnsmasq/nftables logs live under `/workspace/logs/` per Story 2.3 SC-17 bind-mount; keeping `/var/log` as a normal image layer avoids invalidating dnsmasq log-dir initialization behaviour).
  - **`docs/invariants/devbox-hardening.md` § tmpfs posture sub-contract (iter-187 NEW)**: documents the two tmpfs mounts + the `/var/log` non-tmpfs carve-out rationale (Story 2.3 log-bind-mount precedence) + the inert `LOGS_MB` knob status. H3 sub-contract under the single-sourcePath-single-contentHash umbrella entry `INV-devbox-homedev-named-volume` per Story 2.3 SC-10 + Story 2.5 SC-9 disciplines.
  - **Iteration-env `docker compose config` parse-smoke (iter-187)**: PASSED — tmpfs long-syntax parses cleanly with env-var defaults applied; output shows `size=2048m` for `/tmp` + `size=1024m` for `/var/tmp` when no .envrc override is active. Schema validity proves Compose accepts the tmpfs long-syntax declaration + env-var interpolation with fallback-default; kernel enforcement of noexec + nosuid at mount time is the operator-workstation live-smoke concern (Subtask 11.3 + AC 5 Smoke A).
  - **Live-smoke AC 3 runtime verification (Subtask 11.3 `docker exec keel-devbox findmnt -no OPTIONS /tmp /var/tmp` → both mounts show `noexec,nosuid` in options column)** deferred to M4-Pro operator workstation per SC-13. The `findmnt` output is the canonical assertion surface: kernel-reported mount options reflect the tmpfs long-syntax `exec=false,suid=false` translation.
  - **Adversarial AC-3 coverage delegated to iter-CR**: Blind Hunter examines tmpfs long-syntax parsing order (option-list comma-separation; Compose's `size=...m` suffix parsing; env-var interpolation with fallback-default); Edge Case Hunter probes size-knob override bounds (empty override; non-numeric; absurdly small/large values) + /var/log non-tmpfs decision boundary (Story 2.3 log-dir-bind-mount + dnsmasq log-dir-initialization interaction) + inert LOGS_MB knob status clarity; Acceptance Auditor verifies AC 3 verbatim match — `/tmp` + `/var/tmp` tmpfs-mounted with noexec,nosuid + parameterisation via Story 2.2-published knobs.

---

#### AC-4: Named Docker volume `keel_home_dev` mounted at `/home/dev`; no host bind-mount under any `KEEL_DEVBOX_*` setting; non-toggle-able invariant registered as `INV-devbox-homedev-named-volume` (P2)

- **Coverage:** NONE ❌ (deferred to Story 2.5-test-runner-landing + M4-Pro operator-workstation live smokes + iter-CR adversarial backstop)
- **Tests:** 0 automated tests
- **Substrate verification (non-gate-eligible evidence — STRONG; Task 2 + Task 3 + Task 5 + Task 6 + Task 7 + Task 8 + Task 10 authoring + iter-187 live files + iteration-env sync-gate + `docker compose config` parse-smoke + `pnpm check-all` probe AC 4 at manifest + Compose-schema level):**
  - **`packages/devbox/docker-compose.yml` service volumes extension (iter-187)**: service `volumes:` block extended with long-syntax entry `type: volume, source: keel_home_dev, target: /home/dev` AFTER the pre-existing `/workspace` bind-mount per SC-6. Long-syntax form makes the `type: volume` declaration explicit (not a bind-mount, not a tmpfs) and machine-readable for downstream compose-shape validation at Story 2.17.
  - **`packages/devbox/docker-compose.yml` top-level volumes stanza (iter-187)**: `volumes: { keel_home_dev: {} }` added at the end of the file (same indentation as `services:`). Default `local` driver — no `driver_opts` — for portability across Docker Desktop (macOS, Linux) + rootless Docker + Podman-compat forks (SC-6). Inline comment documents non-toggle posture: the invariant is explicitly NOT parameterized by any `KEEL_DEVBOX_*` knob per AC 4 ("no host bind-mount is used for /home/dev under any KEEL_DEVBOX_* setting").
  - **`docs/invariants/devbox-hardening.md` (iter-187 NEW ~130 LOC)**: primary sourcePath doc for `INV-devbox-homedev-named-volume` per SC-9. Structure: H1 + § Intent + § Contract with four H3 sub-contracts (1. Non-root `dev` user — UID/GID 1000, USER dev in Dockerfile before ENTRYPOINT; 2. Capability bounding set — cap_drop:[ALL] + cap_add:[NET_ADMIN, NET_RAW, NET_BIND_SERVICE] + security_opt:[no-new-privileges:true]; 3. tmpfs posture — /tmp + /var/tmp with noexec,nosuid, sizes parameterised; 4. /home/dev named volume — keel_home_dev, never a host bind-mount, non-toggle under KEEL_DEVBOX_*) + § Verification with Smoke A + Smoke B + per-AC live-smoke recipes. The `INV-devbox-homedev-named-volume` anchor appears backtick-wrapped in the doc body per SC-11 anchor-location convention (walker finds literal anchors in body, NOT H3 headers; same convention as Story 2.3 SC-10).
  - **`packages/keel-invariants/src/invariants.manifest.ts` NEW entry (iter-187)**: `INV-devbox-homedev-named-volume` registered with sourcePath `docs/invariants/devbox-hardening.md` + contentHash `5b2e95462566cc67fe2a575886b0d94cd28a796cd2cf9dce26480598524d67f4` + anchors pointing at each of the four H3 sub-contract anchors + description citing SC-9 umbrella entry posture. **Sync-gate exit 0 GREEN** at iter-187 validating 21 manifest entries (20 pre-existing + NEW `INV-devbox-homedev-named-volume`); contentHash captured via the Story 2.3 iter-162 placeholder-then-actualHash protocol per SC-11 — placeholder `0000...` inserted; `pnpm keel-invariants:check` reported `actualHash: 5b2e95462566cc67fe2a575886b0d94cd28a796cd2cf9dce26480598524d67f4`; placeholder replaced; re-run → exit 0.
  - **`INVARIANTS.md` new H3 (iter-187)**: `### Devbox hardening (Story 2.5) — INV-devbox-homedev-named-volume` section + manifest-index anchor bullet citing sourcePath + Story-2.5 registration. Cross-references the four sub-contracts + the Story 2.17 runtime-compose-shape-check deferral (SC-10).
  - **`packages/devbox/README.md` new `## Hardening` H2 section (iter-187)**: below the existing § Egress policy section. Documents AC 1-5 verbatim (operator-readable spec pin) + SC-4 three-cap rationale + SC-14 whitelist.sh `/run` compatibility note (happy-path branch assumed; if tmpfs auto-mount zeros ownership, relocation to `/tmp/keel-state/` is a Story 2.6 carry-over per SC-14 branch (ii)) + § Verification subsection with Smoke A + Smoke B from SC-13 as copy-paste operator commands.
  - **`AGENTS.md` new `### Container hardening (Story 2.5)` H3 subsection (iter-187)**: under § Devbox iteration environment documenting (a) non-root dev UID/GID 1000 posture; (b) cap-drop + three-cap-allow list with rationale per cap; (c) `no-new-privileges` + tmpfs noexec/nosuid posture; (d) named-volume `keel_home_dev` contract for `/home/dev` (non-toggle); (e) runtime-chown best-effort posture under dropped CAP_CHOWN (SC-5 known limitation). Companion to Story 2.4's `### Per-fork whitelist override (Story 2.4)` subsection per SC-16.
  - **Iteration-env substrate verification (iter-187)**: `pnpm keel-invariants:check` (sync-gate) exit 0 GREEN validating 21 manifest entries including the new `INV-devbox-homedev-named-volume` entry. `pnpm check-all` GREEN (all 16 invariants). `pnpm typecheck` GREEN. `pnpm lint` GREEN (16 successful passes). `pnpm format:check` GREEN. `docker compose config` parse-smoke GREEN showing service.volumes includes `keel_home_dev → /home/dev` volume + top-level `volumes.keel_home_dev` definition with default driver `local`.
  - **Live-smoke AC 4 runtime verification (Subtask 11.8 `docker volume inspect keel-devbox_keel_home_dev` → exists + mountpoint under Docker Desktop vm + empty on first-boot; Subtask 11.9 `docker inspect --format '{{range .Mounts}}{{.Type}} {{.Source}} {{.Destination}}{{println}}{{end}}' keel-devbox` → includes `volume keel_home_dev /home/dev` line)** deferred to M4-Pro operator workstation per SC-13. Structural static-contract-assertion (`type: volume` in compose + top-level volumes block) pins the non-toggle-able invariant statically; runtime inspection verifies Docker wires it correctly. **SC-10 pins the runtime compose-shape check (parsing docker-compose.yml + rejecting accidental bind-mount drift for /home/dev) lands at Story 2.17 or a dedicated `packages/keel-invariants/src/check-devbox-compose-shape.ts` consumer — NOT at Story 2.5 itself.** Story 2.5 establishes the invariant surface (manifest + sourcePath + anchors); Story 2.17 ships the TypeScript check.
  - **Adversarial AC-4 coverage delegated to iter-CR**: Blind Hunter examines SC-6 named-volume naming (keel_home_dev underscore convention) + top-level volumes block placement + default `local` driver portability + absence of any `KEEL_DEVBOX_*` knob that could toggle bind-mount (non-toggle contract); Edge Case Hunter probes first-boot empty-volume Docker auto-init-from-image ownership propagation (depends on SC-1 UID/GID 1000 + SC-3 /home/dev chown) + volume-name-collision with upstream cc-devbox legacy `dev-home` bind-mount + Podman-compat driver differences + cross-restart persistence of Claude/gh OAuth subpaths; Acceptance Auditor verifies AC 4 verbatim match — named volume at `/home/dev`, no bind-mount under any knob, invariant registered with ID `INV-devbox-homedev-named-volume` per SC-9.

---

#### AC-5: Executing a binary from `/tmp` fails with `noexec`; setuid privilege-gain blocked by `nosuid` + `no-new-privileges` (P2)

- **Coverage:** NONE ❌ (deferred to Story 2.5-test-runner-landing + M4-Pro operator-workstation live smokes + iter-CR adversarial backstop)
- **Tests:** 0 automated tests
- **Substrate verification (non-gate-eligible evidence — STRONG; Task 2 + Task 7 + Task 8 authoring + iter-187 live file + SC-13 smoke recipe pinning probe AC 5 at declaration-surface level — kernel enforcement requires live container):**
  - **Kernel enforcement surface declared (iter-187)**: AC 5 asserts the runtime effect of AC 3's `tmpfs: [/tmp:exec=false,suid=false,…]` + AC 2's `security_opt: [no-new-privileges:true]` declarations. Substrate completeness proves the declaration exists and is syntactically correct at Compose schema level; kernel-level binary-exec rejection + setuid-bit masking + NNP PR_SET_NO_NEW_PRIVS enforcement requires a live container (SC-13).
  - **`docs/invariants/devbox-hardening.md` § Verification § Smoke A (iter-187 NEW)**: pinned as copy-paste operator command per SC-13:
    ```
    docker exec keel-devbox sh -c 'printf "#!/bin/sh\necho hello\n" > /tmp/t.sh && chmod +x /tmp/t.sh && /tmp/t.sh'
    ```
    Expect exit nonzero with `Permission denied` or `exec format error` (kernel's `noexec` mount flag). **SC-13 pin-rationale**: `printf` NOT `echo` — POSIX echo does not interpret `\n` as newline without `-e` (not portable); `printf` reliably emits the shebang + body as two lines so the test actually attempts to execute a valid shell script.
  - **`docs/invariants/devbox-hardening.md` § Verification § Smoke B (iter-187 NEW)**: pinned as copy-paste operator command per SC-13:
    ```
    docker exec keel-devbox sh -c 'ls -la /usr/bin/sudo; echo exit=$?'
    ```
    Sudo binary exists but is unusable under `no-new-privileges:true` — the setuid bit is honoured by the kernel ONLY if `no_new_privs` is 0; compose's `security_opt: [no-new-privileges:true]` sets it to 1. Expected: attempting `sudo something` inside the container returns a sudo error (sudo detects the posture and declines).
  - **`packages/devbox/README.md § Hardening § Verification (iter-187)**: Smoke A + Smoke B repeated verbatim as operator-readable copy-paste commands. Canonical verification environment: M4-Pro native Docker Desktop with backend-A true Docker-in-Docker OR direct-native Docker (not backend-B host-socket-passthrough per SC-13 live-smoke carve-out).
  - **Iteration-env substrate verification (iter-187)**: AC 5 has ZERO in-iteration-executable smokes — the DinD backend-B iteration environment cannot safely `docker exec` against cap-dropped containers without risk of poisoning host docker state (Story 2.4 SC-17 precedent inherited verbatim). Parse-level `docker compose config` GREEN proves the noexec/nosuid/no-new-privileges declarations are present + schema-valid; kernel-level exec-rejection verification requires a running container on the operator workstation.
  - **SC-13 AC 5 backend-B live-smoke carve-out**: explicit scope-pin at Story 2.5 draft time that AC 5's TWO distinct kernel enforcement mechanisms (tmpfs noexec + NNP setuid masking) are verified together on the M4-Pro operator workstation — not in iteration env. This matches Story 2.3 iter-159 zero-iteration-env-runtime-assertions precedent + Story 2.4 iter-175 backend-B-deferred-live-reload-chain precedent. Story 2.5 contributes iteration-env substrate-schema validation + operator-workstation authoritative live-kernel verification.
  - **Adversarial AC-5 coverage delegated to iter-CR**: Blind Hunter examines the chain noexec → tmpfs mount options → Compose long-syntax translation → kernel mount flag + nosuid → setuid-bit masking + PR_SET_NO_NEW_PRIVS=1 → file-cap F(effective) masking (capabilities(7)); Edge Case Hunter probes `/tmp` exec-attempt via shebang interpreter (kernel enforces noexec on exec(2) syscall against the executable file — BUT interpreter path like `sh /tmp/t.sh` invokes /bin/sh as the exec target not /tmp/t.sh, bypassing noexec; documented as known limitation in SC-13 smoke rationale — expected operator understanding) + sudo-replacement binaries under NNP + capability-elevation via file-caps under NNP (kernel masks F(effective)); Acceptance Auditor verifies AC 5 verbatim match — exec attempt from /tmp fails + setuid elevation blocked.

---

## PHASE 2: TEST INVENTORY

| Level     | Tests | Files |
| --------- | ----- | ----- |
| E2E       | 0     | 0     |
| API       | 0     | 0     |
| Component | 0     | 0     |
| Unit      | 0     | 0     |
| Other     | 0     | 0     |

---

## PHASE 3: GAP ANALYSIS

### Critical Gaps (Priority P0)

None — AC coverage breakdown has no P0 rows (architecture-security / container-hardening substrate at Story 2.5 substrate stage, matching Stories 2.1 + 2.2 + 2.3 + 2.4 P2-uniform precedent).

### High-Priority Gaps (Priority P1)

None — AC coverage breakdown has no P1 rows. See § Coverage Summary note: downstream test-runner landing may retro-classify AC 2 + AC 5 as P1 under runtime-harm taxonomy (capability-grant mistake could regress privilege-drop posture; noexec bypass would violate defence-in-depth); Story 2.5 ships P2-uniform matching Stories 2.1-2.4 precedent.

### Medium-Priority Gaps (Priority P2)

| ID   | Coverage | Reason                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| AC-1 | NONE     | Architecture-security substrate; no test runner at 1.0 (Epic 13 scope). Structural claim-verifiable via future container smoke (docker exec): `id` → uid=1000(dev); `stat -c '%u:%g' /home/dev` → 1000:1000; `docker inspect Config.User` → dev. Template-level Dockerfile useradd + chown -R + USER dev + setcap + pin-rationale comments + entrypoint.sh WORKSPACE_OWNER default all verified iter-187. Iteration-env `docker compose config` parse-smoke PASSED. |
| AC-2 | NONE     | Architecture-security substrate; no test runner at 1.0. Structural claim-verifiable via future container smoke: `docker inspect CapDrop/CapAdd/SecurityOpt` + capability-exercise (`ss :53` + `nft list table`). Template-level cap_drop + cap_add + security_opt + SC-4 three-cap rationale + SC-15 ambient-cap mechanism all verified iter-187. Parse-smoke PASSED with cap stanzas valid.                                                                   |
| AC-3 | NONE     | Architecture-security substrate; no test runner at 1.0. Structural claim-verifiable via future container smoke: `findmnt -no OPTIONS /tmp /var/tmp` → noexec,nosuid. Template-level tmpfs long-syntax + Story 2.2 knob consumption + `/var/log` non-tmpfs carve-out rationale all verified iter-187. Parse-smoke PASSED with tmpfs stanzas valid + env-var defaults applied.                                                                                     |
| AC-4 | NONE     | Architecture-security substrate; no test runner at 1.0. Structural claim-verifiable via future container smoke: `docker volume inspect` + `docker inspect Mounts`. Template-level compose service.volumes + top-level volumes.keel_home_dev + INV-devbox-homedev-named-volume manifest entry (contentHash 5b2e95462566cc67fe2a575886b0d94cd28a796cd2cf9dce26480598524d67f4) + sourcePath doc + INVARIANTS.md H3 + README.md + AGENTS.md all verified iter-187. Sync-gate GREEN 21 entries. |
| AC-5 | NONE     | Kernel-enforcement contract (not a declaration assertion). Requires live container to verify kernel reject-on-exec + NNP setuid-masking + sudo-decline. Smoke A + Smoke B recipes pinned in `docs/invariants/devbox-hardening.md § Verification` + `packages/devbox/README.md § Hardening § Verification`. Declaration-surface (tmpfs noexec,nosuid + security_opt no-new-privileges:true) verified at compose-schema level via parse-smoke iter-187. |

### Heuristic Coverage Signals

- **Endpoint gaps:** none (not applicable — this is a container hardening posture, not a request-response API endpoint surface).
- **Auth negative-path gaps:** not applicable (no auth flow at this stage — Story 2.5 predates Epic 5 platform authentication work).
- **Happy-path only:** not applicable (error-path coverage is intentionally **PRESENT** — AC 5 noexec-rejection + NNP setuid-blocking are explicit kernel-enforcement error-paths with Smoke A/B recipes pinned).
- **UI journey gaps:** not applicable (container substrate; no UI surface).
- **UI state gaps:** not applicable.

---

## PHASE 4: GATE DECISION

**Status:** `WAIVED`

**Determination:** Deterministic coverage computation reports `overall_coverage_pct = 0%` (below 80% minimum threshold). Under normal gate criteria this FAILS. Per hybrid ATDD-skip clause + FIFTEENTH cumulative trace-WAIVED precedent + ground-(a)-(b)-(c)-(d) variant-(ii)+(iii) rationale, the zero-test-runner posture is **accepted** at Story 2.5 substrate stage with downstream test-runner-landing + M4-Pro operator-workstation live smokes explicitly deferring the runner-hosted + kernel-enforcement coverage paths.

**Gate criteria:**

- P0 coverage required 100% → actual 100% (no P0 criteria, vacuous PASS) — **MET**
- P1 coverage target 90% / minimum 80% → actual 100% (no P1 criteria, vacuous PASS) — **MET**
- Overall coverage minimum 80% → actual 0% (no tests at substrate stage) — **NOT MET**

### Rationale

**FIFTEENTH cumulative Epic trace-WAIVED precedent (Stories 1.7 iter-4 + 1.8 iter-3 + 1.9 iter-3 + 1.10 iter-46 + 1.11 iter-57 + 1.12 iter-64 + 1.13 iter-71 + 1.14 iter-78 + 1.15 iter-85 + 1.16 iter-92 → 2.1 iter-126 → 2.2 iter-149 → 2.3 iter-159 → 2.4 iter-175 → 2.5 iter-188).** Story 2.5 is the **first architecture-security / container-hardening class** story — the combination of Dockerfile `USER dev` + compose cap_drop/cap_add/security_opt/tmpfs/volumes + named-volume non-toggle invariant operates at the Docker daemon / kernel-security subsystem boundary, distinct from both Story 2.3's daemon+kernel-rule infrastructure-security class (dnsmasq + nftables + atomic-reload + log-tailer) AND Story 2.4's user-facing-CLI class (bash CLI subcommand dispatcher + domain-regex validation + mutation-lock + atomic-replace + diff summary). Unlike Story 2.4 (which had MORE iteration-env evidence than Story 2.3 because CLI composition paths are verifiable via shell-level probes without live kernel rules), Story 2.5 has SIMILAR iteration-env evidence posture to Story 2.3 — `docker compose config` parse-smoke + sync-gate + check-all + typecheck/lint/format all GREEN are the in-iteration-executable surface; live kernel-enforcement smokes (USER dev runtime resolution + cap_drop/cap_add runtime inspection + tmpfs noexec/nosuid mount-option + named-volume runtime wiring + /tmp noexec-reject + NNP setuid-mask) require a live container which backend-B DinD cannot safely exercise under cap-dropped posture without poisoning host docker state (Story 2.4 SC-17 precedent inherited verbatim). Under the ATDD-skip-trace-WAIVED co-application rule, this is the **SIXTEENTH cumulative pairing** overall (10 Epic-1 pairings + 2.1 + 2.2 + 2.3 + 2.4 + 2.5).

**Ground (a) — substrate-verification-covers-ACs at Dockerfile-layer + Compose-schema + manifest level + augmented by iteration-env parse-smokes.** All 5 ACs have STRONG substrate evidence verified LIVE at iter-187:

- **AC 1** — `packages/devbox/Dockerfile` iter-187: `groupadd --gid 1000 dev && useradd --uid 1000 --gid 1000 --shell /bin/bash --create-home dev && chown -R dev:dev /home/dev` single-RUN-step per SC-1+SC-3; `USER dev` between `chmod +x entrypoint.sh` and `WORKDIR /workspace` per SC-2; `setcap cap_net_admin,cap_net_raw,cap_net_bind_service+eip /usr/sbin/dnsmasq && setcap cap_net_admin+eip /usr/sbin/nft` per SC-15 defence-in-depth fallback; pin-rationale comments for both blocks; `packages/devbox/entrypoint.sh:26` `WORKSPACE_OWNER=dev:dev` default (SC-7); existing runtime chown calls at `:81` retained (SC-5 harmless no-op under dropped CAP_CHOWN). **Iteration-env `docker compose config` parse-smoke PASSED** validating the combined Dockerfile USER + compose declarations are schema-valid.
- **AC 2** — `packages/devbox/docker-compose.yml` iter-187: `cap_drop: [ALL]` + `cap_add: [NET_ADMIN, NET_RAW, NET_BIND_SERVICE]` per SC-4 three-cap rationale (NET_ADMIN for nftables netlink; NET_RAW for dnsmasq raw-socket probes; NET_BIND_SERVICE for :53 bind under cap_drop:[ALL] per kernel port-bind semantics); `security_opt: [no-new-privileges:true]` setting PR_SET_NO_NEW_PRIVS=1; SC-15 ambient-cap vs setcap mechanism pinned in `docs/invariants/devbox-hardening.md § Capability bounding set` — Docker ≥19.03 `prctl(PR_CAP_AMBIENT_RAISE)` is primary; setcap is defence-in-depth portability fallback. **Iteration-env parse-smoke PASSED** with cap_drop/cap_add/security_opt stanzas valid.
- **AC 3** — `packages/devbox/docker-compose.yml` service tmpfs long-syntax per SC-8: `/tmp:exec=false,suid=false,size=${KEEL_DEVBOX_TMPFS_TMP_MB:-2048}m` + `/var/tmp:exec=false,suid=false,size=${KEEL_DEVBOX_TMPFS_VARTMP_MB:-1024}m`; Story 2.2 `.envrc.example:29-31` knobs consumed (UNTOUCHED per SC-18); `KEEL_DEVBOX_TMPFS_LOGS_MB` inert at 1.0 per SC-8 (/var/log NOT tmpfs-mounted — dnsmasq/nftables logs under /workspace/logs/ per Story 2.3 SC-17). **Iteration-env parse-smoke PASSED** with tmpfs long-syntax valid + env-var defaults applied.
- **AC 4** — `packages/devbox/docker-compose.yml` service volumes extended with `type: volume, source: keel_home_dev, target: /home/dev` + top-level `volumes: { keel_home_dev: {} }` per SC-6 default `local` driver for portability; `docs/invariants/devbox-hardening.md` NEW ~130 LOC sourcePath doc with four H3 sub-contracts per SC-9 umbrella entry posture; `packages/keel-invariants/src/invariants.manifest.ts` NEW entry `INV-devbox-homedev-named-volume` with contentHash `5b2e95462566cc67fe2a575886b0d94cd28a796cd2cf9dce26480598524d67f4` captured via Story 2.3 iter-162 placeholder-then-actualHash protocol per SC-11; `INVARIANTS.md` new H3 section + anchor bullet; `packages/devbox/README.md` new `## Hardening` H2 section; `AGENTS.md` new `### Container hardening (Story 2.5)` H3 per SC-16. **Sync-gate exit 0 GREEN at iter-187** validating 21 manifest entries including the new entry; **`pnpm check-all` GREEN**; **`pnpm typecheck` GREEN**; **`pnpm lint` GREEN (16 successful passes)**; **`pnpm format:check` GREEN**; **`docker compose config` parse-smoke GREEN** showing the named-volume declaration.
- **AC 5** — kernel-enforcement surface declared via tmpfs `exec=false,suid=false` + `security_opt: [no-new-privileges:true]`; Smoke A (noexec) + Smoke B (NNP) pinned as copy-paste operator commands in `docs/invariants/devbox-hardening.md § Verification` + `packages/devbox/README.md § Hardening § Verification` per SC-13 + SC-17. **Parse-smoke PASSED** proving declarations are schema-valid; kernel enforcement requires live container (SC-13).

**Sync-gate exit 0 green** at iter-187 — `pnpm keel-invariants:check` validates 21 manifest entries including the NEW `INV-devbox-homedev-named-volume` entry with contentHash `5b2e95462566cc67fe2a575886b0d94cd28a796cd2cf9dce26480598524d67f4` matching the sourcePath canonicalization. No other manifest churn (Story 2.4's `INV-devbox-egress-contract` contentHash unchanged; `INV-devbox-dind-available` unchanged).

**Ground (b) — no test runner at 1.0.** Framework prerequisite unmet; Epic 13 delivers formal test-framework landing per PRD RS6. Recursive probe for vitest/jest/playwright/cypress configs returns zero matches at iter-188; stack detection returns none (`package.json` has no react/vue/angular/next/playwright/cypress/vitest/jest dependencies — Epic 13 delivers framework landing per prior fourteen precedents).

**Ground (c) — HYBRID variant-(ii)+(iii) adversarial-coverage substitution.**

- **Variant (ii) downstream test-runner-landing-covers-per-AC-coverage:** Story 2.5-test-runner-landing (Epic 13 scope) + M4-Pro operator-workstation live smokes (Task 11 per-AC: Subtask 11.1-11.2-11.4 user-verification for AC 1; Subtask 11.5-11.7 capability-exercise for AC 2; Subtask 11.3 findmnt for AC 3; Subtask 11.8-11.9 volume-inspection for AC 4; Smoke A noexec + Smoke B no-new-privileges for AC 5) will unlock per-AC runtime probes. Story 2.6 host-side `pnpm devbox:*` CLI surface exercises AC 1 user-context shell-exec against running container (validates the USER dev chain survives bind-mount + exec). Story 2.8 Claude Code OAuth token persistence validates AC 4 `/home/dev/.claude/` subpath first-boot populate + cross-restart persistence semantics. Story 2.9 gh CLI OAuth token persistence validates `/home/dev/.config/gh/` subpath (same). Story 2.11 shared-workspace mode orthogonal (bind-mount at `/workspace`, not `/home/dev`). Story 2.13 healthcheck orthogonal (probes dnsmasq + sshd liveness, not hardening posture). Story 2.17 ships `packages/keel-invariants/src/check-devbox-compose-shape.ts` runtime compose-shape check (SC-10 deferral) parsing `docker-compose.yml` + rejecting accidental bind-mount drift for `/home/dev`. None of these block Story 2.5 `review → done` transition under the WAIVED precedent — substrate evidence + iteration-env parse-smoke at iter-188 are complete for the in-iteration-executable portion of the story.
- **Variant (iii) spec-declared-CR-substitution:** Story 2.5 § Dev-agent guardrails + 19 pinned scope-clarifications (SC-1 through SC-19 covering Dockerfile dev-user shape + USER placement + image-build chown + NET_BIND_SERVICE reconciliation + runtime chown best-effort + named-volume naming + WORKSPACE_OWNER default + tmpfs long-syntax + new INV manifest entry + Story 2.17 compose-shape-check deferral + contentHash protocol + NFR2 cold-start budget + AC 5 verification smoke details + Story 2.4 whitelist.sh compatibility + ambient-cap mechanism + AGENTS.md posture + README.md hardening + .envrc.example no-edits + no-scope-creep) + forthcoming `/bmad-code-review (args: "2")` adversarial envelope (Blind Hunter + Edge Case Hunter + Acceptance Auditor fan-out against cumulative Story 2.5 substrate diff: ~8 files — 1 NEW + 7 modified) substitute for red-phase scaffolds.

**Ground (d) — upstream-provenance-precedent.** Story 2.1 iter-98 established ground-(d) for Epic 2 stories absorbing upstream cc-devbox content: upstream cc-devbox has no test suite, absorbing preserves that posture faithfully. Story 2.5 is a **substrate-security upgrade OVER upstream** — upstream cc-devbox runs as root with full Linux capabilities + bind-mounted dev-home (`./dev-home:/home/dev:delegated`) + no tmpfs posture + no `no-new-privileges`. Story 2.5 supersedes all four upstream postures simultaneously (non-root `dev` user + minimal three-cap bounding set + tmpfs noexec/nosuid + named-volume `keel_home_dev` + NNP). Scope isolation (SC-19) avoids touching Stories 2.1/2.2/2.3/2.4 deliverables except where the hardening layer requires (SC-7 entrypoint.sh WORKSPACE_OWNER default update to `dev:dev`; SC-14 whitelist.sh `/run` happy-path branch assumed at iter-187 — if operator-workstation smoke surfaces tmpfs auto-mount ownership zeroing, relocation to `/tmp/keel-state/` is a Story 2.6 carry-over per SC-14 branch (ii)).

**Story 2.5 fix-chain forecast vs Story 2.4.** Per the iter-151/154/155 equation (carve-out × 3 + live-AC-coverage × 3 + impl-surface-LOC / 100): **Story 2.5 = (0 carve-out × 3) + (backend-B live-smoke defer × 3) + (~130 new LOC hardening doc + ~40 compose lines + ~15 Dockerfile lines + ~10 entrypoint/README/AGENTS/manifest/INVARIANTS edits ≈ ~200 LOC / 100)** → **~5 ceiling projected; expected 6-10 PATCH opener for infrastructure-security class** (Story 2.3 precedent 9-PATCH CR; Story 2.4 precedent 5-PATCH CR — Story 2.5 falls between per architecture-security class combining Story 2.3-class invariant-authoring discipline + Story 2.4-class surgical edits).

**Story State transition:** FR14n `Story State` transitions `in-dev → traced` at iter-188 completion. Next QUEUE item: `/bmad-create-story (args: "review")` post-dev SM requirements-satisfaction verification (`traced → sm-verified` or `sm-fixes-pending`).

### Blockers

None.

### Recommendations

1. **[MEDIUM] Accept WAIVED posture** — five P2 ACs cover an architecture-security / container-hardening class story (fifth Epic 2 + first of its class: Docker-layer security posture combining non-root user + capability bounding set + tmpfs noexec/nosuid + no-new-privileges + named-volume non-toggle invariant at the Docker daemon / kernel-security subsystem boundary) with no live test runner at 1.0. All 5 ACs have STRONG substrate evidence (Dockerfile useradd + USER dev + setcap; compose cap_drop/cap_add/security_opt/tmpfs/volumes; entrypoint.sh WORKSPACE_OWNER default; NEW docs/invariants/devbox-hardening.md; INVARIANTS.md H3; manifest entry contentHash-captured; README.md ## Hardening; AGENTS.md ### Container hardening; iteration-env parse-smoke GREEN; sync-gate GREEN 21 entries; check-all GREEN; typecheck/lint/format all GREEN) verified LIVE at iter-187. Per-AC runner-hosted + kernel-enforcement coverage deferred to Story 2.5-test-runner-landing (Epic 13 scope) + M4-Pro operator-workstation live smokes (Task 11 Subtask 11.1-11.9 + Smoke A + Smoke B).
2. **[MEDIUM]** Story 2.5 authors the architecture-security / container-hardening substrate: 1 NEW file (`docs/invariants/devbox-hardening.md` ~130 LOC) + 7 modified files (`packages/devbox/Dockerfile` + `packages/devbox/docker-compose.yml` + `packages/devbox/entrypoint.sh` + `INVARIANTS.md` + `packages/keel-invariants/src/invariants.manifest.ts` + `packages/devbox/README.md` + `AGENTS.md`) exactly matching v0.2 SM-reviewed forecast. NEW `INV-devbox-homedev-named-volume` manifest entry with contentHash captured via the Story 2.3 iter-162 placeholder-then-actualHash protocol per SC-11 (do NOT hand-compute sha256 — walker canonicalization may differ from naive file hashing). Sync-gate GREEN with 21 manifest entries valid (20 pre-existing + NEW). Downstream consumers: Story 2.6 host-side `pnpm devbox:*` CLI surface runs as dev user (validates AC 1 at shell-exec-time against running container); Story 2.8 Claude Code OAuth subscribes to `/home/dev/.claude/` subpath (validates AC 4 first-boot populate); Story 2.9 gh CLI OAuth subscribes to `/home/dev/.config/gh/` subpath (same); Story 2.17 ships the runtime compose-shape check (SC-10 deferral).
3. **[MEDIUM]** Story 2.5 test-runner landing (Epic 13 scope) + backend-A M4-Pro operator-workstation live smokes will unlock per-AC runner-hosted probes per Task 11: AC 1 (`docker exec keel-devbox id` → `uid=1000(dev)`; `stat -c '%u:%g' /home/dev` → `1000:1000`; `docker inspect --format '{{.Config.User}}' keel-devbox` → `dev`); AC 2 (`docker inspect --format '{{.HostConfig.CapDrop}} {{.HostConfig.CapAdd}}' keel-devbox` → `[ALL] [NET_ADMIN NET_RAW NET_BIND_SERVICE]`; `docker inspect --format '{{.HostConfig.SecurityOpt}}' keel-devbox` → `[no-new-privileges]`; capability-exercise `ss -tlnp | grep :53 && nft list table inet keel_egress` proves ambient-cap propagation); AC 3 (`docker exec keel-devbox findmnt -no OPTIONS /tmp /var/tmp` → both show `noexec,nosuid`); AC 4 (`docker volume inspect keel-devbox_keel_home_dev`; `docker inspect` Mounts array); AC 5 Smoke A (`printf "#!/bin/sh\necho hello\n" > /tmp/t.sh && chmod +x /tmp/t.sh && /tmp/t.sh` → exit nonzero Permission denied / exec format error proving kernel noexec; `printf` NOT `echo -e` per SC-13 portability); AC 5 Smoke B (`ls -la /usr/bin/sudo; sudo id` → sudo declines under NNP posture). None of these block Story 2.5 `review → done` transition under the WAIVED precedent.
4. **[LOW]** Run `/bmad-testarch-test-review` to assess test quality (no tests exist — no-op; recorded for parity with downstream pipelines).

---

## Collection Status: COLLECTED

All static analysis complete. Acknowledged zero runner-hosted tests at this substrate stage per hybrid ATDD-skip clause (FIFTEENTH cumulative trace-WAIVED precedent; fifth Epic 2 + first architecture-security / container-hardening class). Gate status: `WAIVED`. FR14n `Story State` transitions `in-dev → traced` at iter-188 completion. Next QUEUE item: `/bmad-create-story (args: "review")` post-dev SM requirements-satisfaction verification.

---

## Change Log

| Date       | Version | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Author                     |
| ---------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- |
| 2026-04-22 | 1.0     | Initial trace created iter-188. Coverage oracle: acceptance_criteria (formal requirements); coverage confidence: high. Result: **WAIVED (FIFTEENTH cumulative trace-WAIVED precedent + SIXTEENTH ATDD-skip-trace-WAIVED co-application pairing)** — 5 P2 ACs with STRONG substrate evidence + iteration-env `docker compose config` parse-smoke GREEN + sync-gate GREEN 21 manifest entries + check-all/typecheck/lint/format:check all GREEN. Per-AC runner-hosted + kernel-enforcement live smokes (AC 1 Subtask 11.1-11.2-11.4; AC 2 Subtask 11.5-11.7; AC 3 Subtask 11.3; AC 4 Subtask 11.8-11.9; AC 5 Smoke A + Smoke B) deferred to M4-Pro operator-workstation per SC-13 + Story 2.4 SC-17 backend-B-deferred-live-smoke precedent (DinD-B cannot safely exercise cap-dropped containers without poisoning host docker state). Substrate-evidence LIVE verification at iter-187: sync-gate exit 0 (21 manifest entries valid; NEW `INV-devbox-homedev-named-volume` with contentHash `5b2e95462566cc67fe2a575886b0d94cd28a796cd2cf9dce26480598524d67f4` via Story 2.3 iter-162 placeholder-then-actualHash protocol per SC-11); file + content + line-number verification on Dockerfile + docker-compose.yml + entrypoint.sh + docs/invariants/devbox-hardening.md (NEW ~130 LOC) + INVARIANTS.md + invariants.manifest.ts + packages/devbox/README.md + AGENTS.md. CR-forecast envelope ~6-10 PATCH opener per iter-155 equation (architecture-security class falls between Story 2.3's 9-PATCH and Story 2.4's 5-PATCH). Story State transition `in-dev → traced`. | TEA Agent (Ralph iter-188) |
